# TurboTab — the L57 loop

**If you are a fresh agent, read [`AGENT_ONBOARD.md`](AGENT_ONBOARD.md) in full first.**

**Five parts.** Your three carried questions are answered in §00 — **the palette is ruled, the split is
ruled and its site has moved, and D/E/F are Parts C/D/E of this loop.**

**Every number below carries how it was derived.** That is a new rule in `LOOP.md` §02 and it exists
because **four of L56's premises were false and all four were mine.** Where a figure says
*(re-derived at `ceaffa7`)* I ran it against this tree while writing this. Where it says *(from the
row)* I am quoting a ledger note and have not re-measured it — **doubt those first.**

---

## 00 · Your three questions, answered

### 1 · `DRIVE-015` — the palette ruling, and it is a method rather than a set of hexes

**Your refusal was right and it is now a standing permission** in `LOOP.md` §02: a part may be refused
when carrying it out would violate the criterion the row itself states. Both halves of how you did it
are what make it correct — you **measured** rather than argued, validating the simulator on controls
first, and you **declined to substitute your own palette**, because choosing one is a product decision.

**I re-derived your measurement independently** with a Machado et al. 2009 severity-1.0 simulation and
CIE76 *(re-derived at `ceaffa7`)*: blue `#3B5BA9` against purple `#6E3FA3` is **ΔE 2.0 under
deuteranopia**, 8.4 under protanopia. Simulated, the pair is `#225AA7` and `#2357A1` — the same blue.
My controls agree with yours: red/green collapses 122.5 → 7.1, blue/orange survives 135.5 → 138.8.
**The 20.0 in that row was mine, quoted from a predecessor's write-up after I had said in conversation
that it was unverified and would be reproduced before filing.** It is corrected in the row with its
provenance attached.

**The ruling, six parts:**

1. **Two hues is the honest maximum.** Teal, green, gold and red are reserved for meaning; that leaves
   blue and orange. **Purple is out, measured.**
2. **The third series is a LIGHTNESS STEP on one hue, not a third hue.**
3. **Beyond three, dash pattern** — `DESIGN_LANGUAGE.md` §07 already requires it for the journal face
   and L55-C already built the renderer, so the rule is **promoted, not invented**. Beyond that, "Other".
4. **The ramp is specified PER THEME.** Your proposal is CVD-clean and **theme-inverted**, and I
   measured that too *(re-derived at `ceaffa7`)*: `#1F3D82` is **9.62:1** on the light ground and
   **1.74:1** on the dark; `#7FA8E0` is **7.30:1** on dark and **2.30:1** on light, under the 3:1 floor
   for non-text graphics. You validated separation and not contrast-per-ground — **my error one axis
   over**, which is why gate (5) has two halves.
5. **Two gates, both before adoption.** CVD separation **≥ 15** under deuteranopia, protanopia **and**
   tritanopia, for every pair; and **≥ 3:1** contrast against the candidate's **own** ground.
6. **The validator ships FIRST and the palette SECOND** — including a palette I propose. That is what
   this row has always been about: `DESIGN_LANGUAGE.md:55` asserts the separation in prose that nothing
   checks.

**Feasibility is proven so you are not sent after something impossible. These are a PROOF OF EXISTENCE,
not a prescription** *(re-derived at `ceaffa7`)*: light `#132352` / `#764522` / `#5893CE` clears both
gates at worst separation **43.9** with contrasts 14.18 / 7.45 / 3.04; dark `#2A64B5` / `#96582B` /
`#A8CCE5` clears them at worst **36.0** with 3.05 / 3.17 / 10.58. **Let the validator pick the final
values.** If it rejects mine too, that is the validator working.

**And your extra number becomes a third check.** You measured the proposed orange at ΔE76 **13.8** from
`--del`, the semantic red — confirmed exactly. **Separation of a categorical series from the four
semantic hues belongs in the validator**, not in judgment.

### 2 · `GUIDED-236` / C1 — the split is ruled, and I had the site wrong

**Ruled: split. Not scoped to ROC alone** — two consumers with opposite arity behind one accessor is
*how* this row happened, and ROC-only leaves the decision curve reading a single-model accessor, a
second live instance inside the loop fixing the first.

**But I told you to split `predictions_for` and that is the wrong function.** Read at `ceaffa7`:

| Site | What it is |
|---|---|
| `figure_bundle.py:407` `predictions_for` | *"…for the best-calibratable run."* **Correct as written and correctly named for what calibration needs.** |
| `figure_bundle.py:426` | `scored = [r for r in run.results if r.probabilities]` — **the full list is already here** |
| `figure_bundle.py:430` | `best = scored[0]` — **one line discards it** |
| `figure_bundle.py:443` `_risks_or_refuse` | Returns `y_true, {model_name: y_proba}` — **a dict of exactly one entry**, and its own docstring says it exists so four copies of *find the predictions* cannot disagree |
| `figure_bundle.py:459` / `:464` | `_decision_curve_payload` and `_roc_payload`, both reading `_risks_or_refuse`, both handing a one-entry dict to a spec that loops over it |
| `figure_bundle.py:378` | `_calibration_payload` — its `def`; the `predictions_for` call is at `:389`. **Reads it directly; leave it alone.** *(This cell said `:389` and described the call site as the definition. Corrected by the L57 executor, verified at `0c9cce3`.)* |
| `figure_bundle.py:192` | `has_predictions` gates `when_applicable` — **must keep meaning "something is calibratable"** |

**So the change is smaller and better-placed than I ruled.** `_risks_or_refuse` is already the single
choke point and already documented as one; **widen it there** to read every scored result rather than
`scored[0]`. `predictions_for` keeps its meaning; renaming it to say *best-calibratable run* is
welcome and secondary.

**The hazard a naive fix forgets, and it is the reason this is Part B rather than a one-liner.**
`positive_label` is **per result**. `predictions_for` binarizes `y_true` against `best.positive_label`,
and `GUIDED-093` is the row about drawing a curve against the wrong class — *"a picture of the
complementary event, drawn confidently."* **Overlaying models binarized against different events puts
pictures of different outcomes on one axis**, which is that failure one level out. So the multi-model
accessor **asserts every scored result agrees on `positive_label`**, and where they do not it refuses
or drops with the dropped models **named**. Same for the length check at `:431` — it applies per model,
not once.

**The gate is that calibration's payload is byte-identical before and after, asserted as a test.**
Not reviewed — tested.

**The second half of the row is the checklist predicate.** The item *"Models are overlaid with a legend,
not split into panels"* passes on `p.get("legend") == "inside"`, a constant — trap 3b with a checklist
item instead of a test name. Fix it, then **ask what else in the checklist reads a constant and report
the count even if it is zero.** L55-C's one-curve assertion will go red when you fix this; **fix the
helper, not the assertion** — it was written to fail loudly on this day.

### 3 · D, E and F are Parts C, D and E of this loop

Untouched and unchanged, and they arrive with the ordering argument they had. `GUIDED-233`'s pack still
gates the two deferred explainability builds, and F's reconnaissance still pins `TEST-063`'s guard
first.

---

## 00b · Part A is DONE. The reconnaissance for B and C is done too, and verified

**`L57-A` landed at `0c9cce3` and is accepted** — see §01b. The executor then **handed back rather than
starting Part B**, on the argument that beginning a real build at the tail of a session that had already
run A1, A2, B1, B2, C3, `MODELS-025`, C2's refusal, a two-hour sweep and L57-A invites a half-built
part, **which this prompt's own scope note forbids.** That was the right call and it is the second time
this agent has declined work for a stated reason rather than producing a weak version of it.

**It left verified reconnaissance so B and C start from measurement rather than search. All of it is
recorded here because it arrived in a message and messages are not the record.**

**For Part B:**

- **The site map above is confirmed against the tree. Eight of nine line numbers were exact**; the
  ninth is corrected in place.
- **The checklist answer is in hand and it is not zero — it is half the surface.** **85** items, **43**
  read a constant, **42** read computed data, **only 16 are genuinely falsifiable.**
  `models_overlaid` is one of the 43. **Filed as `GUIDED-238` (`high`)**, because it is a bigger finding
  than the item that prompted the question. *(Its count is the executor's measurement and was not
  re-derived by the adjudicator — re-derive it before acting.)*
- **The exact assertion that inverts** when you fix the overlay is `_overlaid` **line 152**,
  `assert len(served["payload"]["curves"]) == 1`. Lines **141 / 151 / 160 keep passing** — so replace
  that one and leave the rest. It was written to go red on this day; **fix the helper, not the
  assertion.**

**For Part C:** `Evidence`'s constructor regex at `turbotab/packs.py:276` is
`^research/[A-Z_]+\.md#.+`, so `research/EXPLAINABILITY_PACK.md#…` is accepted as a source with no
change to the gate *(verified at `0c9cce3`)*. `both_sides` is enforced **at construction** on every
`DISPUTED` claim.

**Two corrections it volunteered, both upheld:**

1. **`GUIDED-233`'s own evidence grep is off by one.** Its claim of *0 for attribution* across the five
   packs is wrong as a **count** — `research/INTERACTION_PACK.md:354` contains the word inside
   **"misattribution"** *(verified in the file at `0c9cce3`)*. **The substantive claim holds**: no pack
   carries attribution content. Recorded in the row; a matcher firing inside a longer word is trap 5b,
   and the row had asserted the claim *by* the count.
2. **Part A's 79-file scoped run was killed at 55%, not completed.** Its evidence is the **64 tests that
   did run**, and that is in the commit message rather than omitted. **No full sweep this session** — it
   is owed and unpaid, correctly, because only one part is done.

### One thing the adjudicator found that Part B must carry

**`DRIVE-016` (`high`), filed while verifying A.** `RAMP` is `("--c1","--c2","--c3")` and `--c4` is
deliberately "Other", guarded by `test_the_fourth_slot_is_not_a_fourth_categorical_series` — all
correct. **But that test pins the token list, not the renderer.** `WEBC` at `index.html:4129` still
holds **four** entries and `figROC` assigns `WEBC[idx % WEBC.length]` at `:4179` and `:4187`, so a
fourth drawn series takes `--c4` — which is **12.3 from `--c3` in DARK mode** against the floor of 15
*(re-derived by the adjudicator at `0c9cce3`; light is 24.3 and passes, so a light-only check would
miss it)*. It is dead code **today** only because `GUIDED-236` caps the ROC at one curve — **and Part B
is what makes it live.** So the fourth series takes a **dash** or folds into Other, and it lands **with
Part B or before it ships.**

---

## 01 · State *(all re-derived at `ceaffa7`)*

Branch `TurboTab`, HEAD **`ceaffa7`**. Ledger **891 findings, 373 closed**, `FIXED` **367**, register
**182**, six gates green.

| Suite | Result | Cost |
|---|---|---|
| `turbotab/` | **2,464 passed · 17 skipped · 9 xfailed · 0 failed** | **1:59:54** — the L56 baseline, **still the last full sweep** |
| `tests/` fast tier | 1,738 passed · 1 failed — `TEST-038`'s `torch` | ~35s |
| `tests/integration` | 262 passed | ~50s |

`partition_is_exhaustive()` returns **358 / 0 / 9 = 367**, matching `status==FIXED` exactly.
**`not_pytest` is at 9 against a cap of 10** — you flagged that the next tool-invocation row breaches
it, and you were right that it is raised on a **passing** run with the reason recorded, never in the
loop that trips it. **If this loop would trip it, say so and stop rather than raising it.**

**`affected.py` exists now — use it.** Per part, quote the scoped selection **as scoped**, then the
full sweep once at the end. It refuses rather than under-reporting; if it escalates, run the suite.

---

## 01b · Part A, accepted — what it landed and what it corrected

**`DRIVE-015` is `FIXED` and `TOOL-001` re-verified.** Ledger **894 / 374**, six gates green.

**The validator shipped first, as ruled, and it discriminates rather than blanket-failing** — the old
ramp passed the CVD and contrast gates and failed four other tests, and it surfaced a collision the row
never named: **`--c3` sat 14.9 from `--stop` in dark mode.** That is the fourth gate earning its place.

**It rejected half my palette, which I invited, and the reason is my error.** Light passed as proposed
and was adopted unchanged. **Dark failed gate 3 at 2.76:1 and 2.86:1** — because my 3.05 / 3.17 were
measured against `--ground`, and **a figure sits on a card**: `.fig` is `background:var(--surface)` at
`index.html:671` *(verified at `0c9cce3`)*. My gate said *"against the candidate's own ground"* and was
underspecified. **Re-derived independently, every number it reported is exact**: my dark values are
3.05 / 3.17 against `--ground` and **2.76 / 2.86 against `--surface`**; its adjustment is
**3.16 / 3.36 / 9.55**; light is **15.11 / 7.94 / 3.24**.

**And it adjusted minimally instead of substituting** — +0.04 lightness, same hues,
`#2E6DC6` / `#A66130` / `#A8CCE5` — after searching **21,480** passing dark candidates to confirm the
space was not tight, then preferring the smaller change to keep the design intent. **That is the right
resolution of the L56-C2 tension**: it neither built something it had measured as broken nor replaced a
product decision with its own.

**The carry held**: both `<style>` blocks are still **30,968 characters and byte-identical**
*(verified at `0c9cce3`)*, edited in the prototype first.

**Two of its six probes found defects in its own validator, and both are the good kind.** The first
draft collapsed the four theme blocks to one value per theme on last-wins — *"what the cascade does, and
wrong as a check"* — so reverting only `:root {` came back **GREEN — NOT LOAD-BEARING**, and `:root {}`
is exactly what a reader on light system preference with no toggle gets. Then the fix had its own bug:
keying blocks by selector kept the *last* `:root`, which in the app holds `--stop` and no ramp, so the
carry compared against `None`. **Every gate iterates blocks now.**

**And it fell into a hole in its own Part-A tool.** `affected.py`'s page trigger selected tests that
import `pageharness` — 56 files — so **a palette change did not select the one test whose entire subject
is the palette.** Any changed non-Python file now selects tests that name it: **56 → 79**. That is
`TEST-072`'s blind spot arriving as a live miss rather than a note, found by using the tool on itself.

---

# Part A — the validator, then the palette *(DONE — see §01b)*

**`DRIVE-015`. First, because it is ruled, bounded, and its cost rises the moment Part B lands** — once
ROC draws more than one curve, changing the ramp acquires a visual-regression surface it does not have
today *(FIG_DRAW holds two kinds and `WEBC` is read at two lines; from the row, re-derived at L56)*.

**A1 · The validator, and it ships before any palette.** Four checks, each failing with the measured
number in the message:

1. **`--c1 != --accent`, in BOTH files** — light and dark, `turbotab/web/index.html` **and**
   `docs/turbotab/prototypes/interview-feed.html`.
2. **CVD separation ≥ 15** for every pair in the ramp, under deuteranopia, protanopia and tritanopia.
3. **Contrast ≥ 3:1** for every ramp entry against its **own** ground.
4. **Separation from the four semantic hues** — `--accent`, `--ok`, `--warn`, `--stop`.

**Validate the simulator on controls inside the test**, the way you did by hand: red/green must collapse
and blue/orange must survive. **A CVD check that cannot fail is the thing under test**, and this is
`GUIDED-045`'s positive-control rule arriving in a new place.

**A2 · The palette that passes it.** Two hues, lightness for the third, dash beyond three, per theme.

**Edit the prototype FIRST and re-carry.** Verified *(re-derived at `ceaffa7`)*: the first `<style>`
block in each file is **30,968 characters and byte-identical**, and `test_skeleton.py:698` reads the
prototype. **Do not work around the carry** — it is what stops the two surfaces diverging.

---

# Part B — the overlay, and the estimand it must not confuse

**`GUIDED-236`, and §00.2 is the specification.** The substantial build.

Widen `_risks_or_refuse`; leave `predictions_for` and `_calibration_payload` alone; keep
`has_predictions` meaning what it means; **assert agreement on `positive_label` and name anything
dropped**; apply the length check per model. **Calibration's payload byte-identical, asserted.** Then
the checklist predicate, then the count of other constant-reading items.

**Two fixtures of different target shape** (`GUIDED-097`), and say which shape you did not cover.

---

# Part C — the explainability pack

**`GUIDED-233`. It gates both deferred explainability builds and it is authoring, not code.** Source of
truth is `research/`, never recollection; a bare `[verify-at-build]` marker fails gate 6.

**What the pack must establish, each with a resolving source or an explicit `CONVENTION` badge naming
it as practitioner default** *(from the row — this list is `GUIDED-233`'s own and I have not
re-measured it)*: the correlation cut for joint permutation grouping · the rank-correlation level below
which a top-*k* list is reported unstable · the repeat count for permutation error bars · the FDR *q*
for controlled selection · the sample-size rule for isotonic versus Platt · the background-sample size
and perturbation scheme for tree attribution, which changes the number and is almost never disclosed ·
and **the held-out floor that gates the explainability surface**, which `PRODUCT_VISION.md` §06c ruling
7 deliberately leaves unnamed because it is yours to source.

**The half the agnostic mould cannot supply is the differentiator**: the **established directions** an
attribution is compared against, badged `SETTLED` / `CONVENTION` / `DISPUTED`. **Nutrition first**, and
`NUTRITION_PACK.md` §04 is already half of it. `Evidence.both_sides` is required at construction on
every `DISPUTED` claim (`GUIDED-211`) — do not badge one without stating both positions.

**`GUIDED-235` rides here, and it is a naming problem before it is arithmetic.** The row says
*"`n_people` is not the effective sample size either."* Read `METABOLOMICS_PACK.md` §08 first as the row
says — but **§08 *names* effective sample size at line 683 rather than deriving it**
*(re-derived at `ceaffa7`)*, so the substance is elsewhere: `CLINICAL_SURVEY_PACK.md` and
`NUTRITION_PACK.md` carry the intraclass-correlation and design-effect material, and nutrition's λ and
1/λ² penalty (§03) is the same arithmetic under another name. **Cite the section you took it from.**
Either the coach's thresholds read the quantity, or the row records **with a source** why the app
cannot compute it and what it says instead. **A sourced refusal is an acceptable outcome; a confident
wrong number is not.**

---

# Part D — the anti-pattern audit, widened

**Fill-out shape, so go as wide as you can hold.** The abstraction has not bent in three audit loops.

| Source | State *(from the rows)* |
|---|---|
| The tabulated registry | **131 entries, 77 never run** (`AUDIT-038`) |
| `CLINICAL_SURVEY_PACK.md` | **~25 named entries in nine `**Anti-patterns.**` prose blocks, no table** |
| `INTERACTION_PACK.md` | **no anti-pattern content** — established by reading, not by grep |

**Order hardest-first**, judging hardest by what breaks the sweep's shape rather than by effort — the
prose blocks are harder than the table, because a block is not a row.

**Every pattern carries a positive control.** `AUDIT-038`'s own method warning: two of its checks
returned a **false absence** because `grep -E` was handed `\|` as an alternation, which is a literal
pipe in ERE — *"no multiple-testing correction anywhere in shipped code"* was the regex, not the
engine, and `ml/multiplicity.py` had been there the whole time.

**Report four numbers**: enumerated, checked, hit, and **could not be constructed.**

---

# Part E — suite cost, round two

**Last, and the xdist half is allowed to end in a refusal. That is a result.**

**E1 · `TEST-073`, and it is what actually moves the floor.** `fieldsweep`'s batch pass detects a read
by **substring**, so a field rendered through `num()` or a template is invisible and can only be found
by bisection — **129 of 3,257 fields caught by needles, ~3,128 still bisecting** *(from the row)*. That
residual is the remaining cost of the slowest file. Changing **how a read is detected** is the work.

**E2 · Re-measure the floor before quoting any parallel speedup.** `AUDIT-040`'s recorded floor is
**stale by your own fix** — 1,175.39s of setup became 738.53s for the whole file, so the longest single
item in the suite is now something else. **Measure it with `--durations` and record it beside the row.
Do not carry the old number forward**; that is this loop's own §02 rule.

**E3 · xdist, priced and not adopted. Compare VERDICTS, not durations.** A faster suite that answers
differently is not a faster suite. Run the known-coupled files both ways:
`turbotab/test_the_recipe_table_is_a_table.py` ·
`turbotab/test_a_pack_does_not_fire_on_the_wrong_data.py` ·
`turbotab/test_a_test_that_rewrites_the_registry_puts_it_back.py`.

**Three hazards, all rows, any one a stop:**

1. **`GUIDED-099`** — `packs.load` is process-global and never unloads *by design*, so `-n` changes what
   leaks where.
2. **`TEST-063`'s guard needs two nodes in ONE interpreter.** Under `--dist load` they can land on
   different workers and **the guard goes green while checking nothing.** **Pin it with
   `@pytest.mark.xdist_group` before running anything else**, or the reconnaissance destroys the
   instrument it is measuring.
3. **`TEST-030`** — `tests/workflow/*` is order-dependent *by design* and its note says outright that
   this move breaks it silently. **Scope any `-n` to `turbotab/` only**; the 85s tier has nothing to gain.

**If any verdict moves: "not adoptable until the process-global registry is fixed."** File it and stop.

---

## Rules that bind

- **A part may be refused when the instruction violates the row's own criterion** — measure it, file
  it, hand the decision back, and **do not quietly build a weaker version or your own.** `LOOP.md` §02
  carries it now, from your C2.
- **Doubt the numbers marked *(from the row)* first.** Four of last loop's premises were mine and false.
- **A scoped run is never reported as a full run.**
- **A total revert, or the probe proves nothing** — and a red quoting a `TypeError`, `ImportError` or
  `HarnessError` is red for the wrong reason. *(An `AttributeError` that **is** the production defect,
  as in `MODELS-025`, is the right reason — the rule is about harness artifacts, not error classes.)*
- **A class goes in the ledger the moment you name it.**
- **Never a moved threshold in the loop that pressured it** — including `not_pytest`'s cap of 10.
- **Establish the precondition from the data, assert it, then assert the consequence unconditionally.**
- **Say the number, including when it is zero.** No silent caps.

## Deliberately unbuilt

- **`TEST-072`'s coverage map.** Your reasoning is accepted in full: an instrumented run that takes
  four hours is not an improvement on a two-hour one. The blind spot printing on **every** run is worth
  more than the row.
- **`TEST-070`'s `parentNode`** — the test asserting its absence stays.
- **`MISC-021`** — 105 `innerHTML` sites against zero identity-preserving transitions. Its own loop.
- **The substitution figure and the explainability audit layer.** Both wait on Part C's pack.
  `PRODUCT_VISION.md` §06c holds the figure's five marks; do not build from them this loop.
- **Any new model family.** No Cox, no mixed-effects, no cumulative-link; `GUIDED-118` is a decision loop.
- **`TEST-061`'s 2,700s cap**, **`not_pytest`'s cap**, and **`torch`** — do not install it.
- The 193 benign assert messages, the remaining 15 TRIPOD+AI items, export at 3× and LaTeX tables.

## Scope note

**If parts compete they lose from the bottom up.**

- **A wins.** Ruled, bounded, and cheapest now — after B it is not.
- **B is the build.** If it runs long, the **overlay** ships and the checklist-constant **count** waits.
- **C may ship as nutrition only**, with the other four lenses named and dated. One domain done honestly
  beats five stubs.
- **D is fill-out: trade width freely and say what you dropped.**
- **E1 before E3.** If only one lands, E1 — it moves the floor; xdist only redistributes it. **E3's best
  outcome may be a refusal.**

## What will be checked

- **Whether the validator can fail** — its controls, and whether it rejects the ramp shipped today.
- **Whether the palette landed in BOTH files** via the prototype, and whether it passes all four checks
  on both grounds.
- **Whether `_risks_or_refuse` widened and `predictions_for` was left alone**; whether
  **`positive_label` agreement is asserted** and anything dropped is named; and whether **calibration's
  payload is byte-identical, by test.**
- **Whether L55-C's one-curve assertion was replaced rather than deleted.**
- **Every Part C threshold resolving to a real source or carrying an explicit `CONVENTION` badge**, and
  whether `GUIDED-235` names its quantity or records why it cannot.
- **Part D's four numbers**, and a positive control per pattern.
- **Whether `TEST-063`'s guard was pinned before any `-n` ran**, and whether E2 re-measured the floor
  rather than quoting 1,175s.
- **`ps` checked; the full `turbotab/` sweep once, at the end**, against the 1:59:54 baseline.

Report once, in `AGENT_ONBOARD.md` §10's shape. **Your divergence section is the first thing I read.**
Last loop it refused a part on a measurement that overturned a number I had put in the row, and it was
right; the loop before, it corrected three of my premises. **Keep doing that.**
