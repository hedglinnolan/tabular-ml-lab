# TurboTab — the L61 loop

**If you are a fresh agent, read [`AGENT_ONBOARD.md`](AGENT_ONBOARD.md) in full first.**

**Five parts.** L60's A–D are accepted and verified. **E reported 81 red and that is this loop's
first job.** A fourth human drive ran on a clean build and found the thing that had blocked the
previous three, and it was never in the code.

**Every number below carries how it was derived.** *(re-derived at `cf7129c`)* means I ran it against
this tree while writing this. *(from the row)* means I am quoting and have **not** re-measured —
**doubt those first.**

---

## 00 · Where L60 landed, and the two things that outrank everything else

**A–D accepted.** I re-ran the acceptance test verbatim: unanswered `case`/`control` refuses **400**
and `'control'` never comes back; unanswered numeric `0`/`1` now **raises the question** and refuses;
answered `case` fits *(re-derived at `1d2206d`)*. Both of `DRIVE-032`'s defects are closed and
`GUIDED-093`'s substance with them. **B, C and D landed with their probes.**

**And the value check found the capability before the sweep did.** `wide_assay.csv`'s target
`responder` is `int64 {0,1}`, so Part A's trigger fired on a fixture no part of the loop touched —
`questions_asked` 4 → 5 and `findings_driven` 0.0 → **0.2**, the first observed *rise* of a metric
whose two recorded movements were both falls. Adjudicated and re-recorded; `verdict.passes`
unchanged.

### The sweep: 81 failed · 2,486 passed · 17 skipped · 9 xfailed in 2:00:18 *(from L60-E)*

**73 of the 81 quote Part A's new refusal.** The blast radius was measured wrong and the report says
so plainly: a grep for `POST "/train"` found 3 files, while **nine files call `training.train()`
in-process and fan out to 73 tests across 21**. *"The grep answered 'does this text appear' when the
question was 'does this run'"* — trap #5, in the one measurement the part's scope rested on.

**The gate was not weakened and that was right.** Those fixtures fit a classification with the event
unchosen, which is exactly what `DRIVE-032` removed. **Relaxing the refusal to make them green is
trap #3c**, and the narrower alternative was measured rather than argued: on the four worst-hit files,
**35 failed as committed and 22 failed with the gate removed** — *"better, not clean, failing for the
same reason."*

### And the `/models` 500 was never in the code

**Four drives blocked, and the cause is the launch command.** The run-4 driver captured the mechanism
exactly — `ml/model_registry.py` imports the estimator stack at module scope, triggered lazily inside
`shelf()`, reaching Starlette unhandled as 21 characters — and concluded the host venv needed a
reinstall. **Their own caveat is the one that applied.** *(all re-derived at `cf7129c`)*:

| environment | inventory |
|---|---|
| `venv/` — every test, every probe, four loops of numbers | **complete**: xgboost 3.3.0, lightgbm 4.7.0 |
| `.venv/` — python3.12, the old Streamlit one | not used |
| **`turbotab/.venv` — what the server was actually running** | `fastapi` ✓ `pandas` ✓ · **`sklearn` ✗ `xgboost` ✗ `lightgbm` ✗ `shap` ✗** |

`lsof` on the serving process is how that was established. **So the first failure is `sklearn` at
`model_registry.py:6`, twelve lines before the `xgboost` the sandbox traceback named** — both
tracebacks are real, and neither is the general statement, which is that the module imports the whole
stack eagerly and **the first absence wins**.

**Proven fixed end to end**: same code, same page, launched from `venv/bin/python`, `GET /models` over
real HTTP returns **200, `n_available` 13** *(re-derived at `cf7129c`)*.

**Why four drives and two in-process investigations missed it: every test ran under `venv/` and every
browser request was answered by `turbotab/.venv`.** A reproduction attempt in-process asks a different
interpreter the same question.

---

# Part A — the 73, and it is ruled

`DRIVE-041`, **critical**. **The execution agent recommended (a) and declined to do it unilaterally
because it is a chunk and because relaxing a gate under failure pressure is a shape this codebase has
a rule against. Both halves of that were right.**

**Ruled: (a). Update the fixtures.** They fit a classification with the event unchosen, so **they were
passing by virtue of the defect** — they are not tests the fix broke, they are the population that
depended on the wrong default. `LOOP.md` §05: a capability ships with its consumer, and here **the
fixtures are the consumer.**

**Option (b) is refused, on the agent's own measurement rather than on preference.** 35 → 22 is
*"better, not clean, failing for the same reason"*, and *"never invent without refusing"* only
relocates the refusal — which is precisely how `figure_bundle.py:432` became a branch nothing could
reach.

**A1 · Re-derive the count before you touch anything.** **73 tests across 21 files, nine calling
`training.train()` in-process** is *(from the row)* and it is the number whose first measurement was
wrong. **Establish it by running, not by grep** — that is the row's own lesson.

**A2 · Fix it at the shared seam if there is one.** Nine call sites fanning to 73 tests suggests a
helper. **Find it first.** Editing 73 call sites where one fixture factory would do is how a
mechanical change becomes a merge hazard.

**A3 · A fixture that chooses the event must choose it the way a user does** — through the recorded
decision, not by reaching into the project. A test that sets the field directly would pass while the
door it is standing in for stays shut, which is `DRIVE-017` written into the test suite.

**A4 · Say the number, including the ones you did not fix.** If some of the 73 are failing for a
second reason, they are a separate row, not a rounding error.

---

# Part B — one command, and the app says what it is running

**The product owner's standing requirement, in his words: the app should be *easy to launch and
self-contained each time*.** This part is that requirement plus the two rows the drive produced, and
they are one theme: **it should be impossible to launch this wrong, and if it is wrong the app should
say so.**

**B1 · There is no launch command, and that is the root of all of it** *(re-derived at `cf7129c`)*.
`make serve` runs **`streamlit run app.py --server.port 8501`** — the old app, not TurboTab. Nothing
in `README.md` or `AGENT_ONBOARD.md` says how to start TurboTab at all. **Every launch for four drives
was hand-typed, and one of them picked an interpreter with no `sklearn`.**

**Build the command.** A `make turbotab` target, or a script it calls. It must:

- use **`venv/`** explicitly rather than whatever `python` resolves to;
- **verify the engine's stack imports before binding the port**, and **refuse with a legible message
  naming the missing package and the fix** if it does not — a server that starts and then 500s at the
  Train step is the failure this loop is about;
- print the interpreter and the rev it is serving, so the first line of the terminal answers the
  question three drives could not.

**Do not delete or repoint `make serve`.** It launches the Streamlit app, which is still the parity
reference; renaming it is a separate decision.

**B2 · `MODELS-026` — guard the eager imports.** `ml/model_registry.py:6-19` imports sklearn, then
xgboost and lightgbm, at module scope. **`TEST-038` is the standard already in this codebase**: torch
is deliberately absent, that absence is a named expected condition, and it does not take an endpoint
down. **A missing estimator should shorten the shelf with a stated reason, or the endpoint should
refuse legibly — never an opaque 500.** `PRODUCT_VISION.md`'s *"the shelf is never shortened"* is
about not hiding models from a user, not about refusing to start.

**B3 · `TEST-087` — `/dev/status` names the interpreter.** Last loop's B3 was built because three
drives carried a version question, and **it worked exactly as designed and still told the driver
nothing**: `rev` matched HEAD, `page_newer_than_engine` was `false`, they re-checked it mid-drive and
wrote *"everything on screen is trustworthy."* **All true, and the app could not fit a model.**

**Honest and insufficient in the same breath is worse than absent**, because it licensed a conclusion
it could not support. `_SERVED_BUILD` is already stamped once at import — **that is where
`sys.executable` and a guarded import probe of the engine's stack belong.**

**Same shape three times, one layer further out each time**: `ahead 31` was a fact about the remote;
then the right directory and the wrong code; now the right code and the wrong environment. **Each fix
answered the previous question and left the next one unasked. Ask this one properly.**

---

# Part C — the frozen baseline, which is holding the branch

`TEST-086`, **high**, and **the branch cannot be pushed until it is ruled.** I diagnosed it and
deliberately did not rule it at the tail of a session.

**Classic's behavior did not change — the denominator did.** `wide_assay.csv`'s `responder` and
`leaky_sepsis.csv`'s `sepsis` are both `int64 {0,1}` *(verified at `1d2206d`)*, so L60-A makes
choosing the event a **required decision** on both. Classic does not ask it —
`target-positive-class` is `guided-only`. So Classic covers one of two instead of one of one:

| dataset | `required_decisions` | `irrelevant_questions` | `coverage` |
|---|---|---|---|
| `wide-assay` | 1 → **2** | 30 → **29** | 1.0 → **0.5** |
| `leaky-sepsis` | 1 → **2** | 30 → **29** | 1.0 → **0.5** |

**`VALUE_CHECK_ADJUDICATION.md` §"The denominator moved" already ruled this exact movement** for
`messy-clinic` and `longitudinal` — same deltas `(1,2)`, `(31,30)`, `(1.0,0.5)`, and
`ADJUDICATED_KEY_DELTAS` naming the added key as `repair::positive_class__outcome`. **Same finding,
two datasets further.**

**Two mechanisms, not one, which is why this is a part and not a table edit.** `wide-assay` runs
through `ADJUDICATED_DELTAS`; **`leaky-sepsis` compares against its own `routing-baseline-leaky.json`
with no deltas table at all.** Record the new measurement **beside** the old one and keep the chain
readable the way `routing-baseline-l9` → `l9c` does.

**And say the words.** Extending an enumerated allowance in the same loop as the change that pressured
it is `LOOP.md` **§06.2**. The exception very likely applies — these entries encode *the same purpose*
rather than a nudged value — but **invoke it deliberately and in those terms**, and enumerate every
new entry exactly, because *"a second drift cannot hide inside the first"* is a property that survives
extension only if nothing is waved through.

---

# Part D — two sentences the app should stop saying

**D1 · `DRIVE-040` — the figure names `1.0` instead of the level.** After the user picks **`True`**
from a card that named `True` and `False` with counts, `positive_label` is `"1.0"` and
`figure_bundle.py:400` emits `payload["event"] = str(event)` → `"1.0"` *(confirmed by the run-4
driver at source and payload level)*.

**The driver's Condition-3 verdict, which is the answer to the question I asked them:** it reads
**badly**, and it *"would read equally badly on screen"* — a value the user never typed, appearing
nowhere in their column, decodable only from one sentence in the transcript.

**Carry the chosen level alongside the encoded value.** The transcript already holds the exact
sentence — *"'meds_hbp' was encoded with True as the event (1) and False as the comparison (0)"* — so
the name exists and the figure simply is not given it. **The app stopped asserting something false
and became less legible in the same move; finish the move.**

**D2 · `DRIVE-036`'s false promise, pulled.** Two human drives have now read the seal receipt saying
*"you can name it at any point before the seal"* — **and no such control exists anywhere on the
page** *(from the row, confirmed twice)*. The identifier follow-up is declared in the API contract
(`GET /grain` carries `follow_up: "which column identifies the person?"`) and never rendered.

**Building the control is not this part.** **Pulling the sentence is.** A promise of an action the
interface does not provide is the governing rule's *assert something false* branch wearing an offer,
and it costs one string to stop saying it. **File the control as the remaining half and leave
`DRIVE-036` open.**

---

# Part E — evidence

**Part A should turn 81 red into 0.** That is the number to report, and **the sweep is the only thing
that can say it** — 73 of the 81 are the population Part A is about.

**`ps` first.** L60's own timing is not quotable: the product owner drove the app while it ran, and
the report said so before I did. **Results stayed valid; duration did not.** The last clean timing is
L56's **1:59:54**; L60-E measured **2:00:18** with the machine shared *(from L60-E)*.

**The xfail count is 9**, down from 10 — L60-C removed `choose_preparation_mode`'s `NOT_BUILT` entry
on purpose. **If it moves again, say why.**

**If `affected.py` escalates, run the full sweep** — it has escalated on every range since `07c25c6`.
**A scoped run is never reported as a full run**, and reproduce the escalation verbatim if you scope.

---

## Deliberately not this loop

- **`DRIVE-036`'s identifier control** — D2 pulls the false promise; building the follow-up card is
  its own part and wants the router's attention.
- **`DRIVE-038`'s `[object Object]`** — run 4 could not reach it behind the 500 and the server-side
  params are clean. **Re-drive before hunting it.**
- **`DRIVE-020`'s ordering half**, **`GUIDED-238`**, and the **`SEQN` / survey-design / pooled-cycle**
  gaps, which are `GUIDED-231`'s port.
- **L57's ROC overlay and `GUIDED-233`'s pack.** The overlay's prerequisite is now met — the event is
  recorded rather than guessed — so it is genuinely next once the suite is green.

## Rules that bind

- **A part may be refused when carrying it out would violate the criterion the row itself states.**
  Your `DRIVE-041` handback is the model: you measured the alternative, filed both options, and did
  not relax a gate under failure pressure.
- **Where a row is about something a user does, drive the doing.** And where it is about an
  environment, **check the environment** — `lsof` on the process, not `pip list` in your shell.
- **A grep answers "does this text appear", not "does this run."** Trap #5, and it is what made the
  81 a surprise.
- **A total revert, or the probe proves nothing.** A red quoting a `TypeError`, `ImportError` or
  `HarnessError` is red for the wrong reason.
- **A pin that cannot flip is a comment** — `TEST-077`. **`__harness.dispatch(type, el)`** takes the
  event first — `TEST-083`.
- **A class goes in the ledger the moment you name it.** **Say the number, including when it is zero.**

## Scope note

**If parts compete they lose from the bottom up.**

- **A wins.** Nothing else can be measured while 81 tests are red for one reason.
- **B is the product owner's standing requirement and B1 is the whole of it.** If B runs long, **the
  launch command ships and the guard waits** — say which.
- **C unblocks the push and is bounded.** Do not let it slip behind D.
- **D is two strings. D2 is the cheaper and the more dishonest of the two** — pull it even if D1 waits.
- **E is evidence, not work.**

## What will be checked

- **Whether the 73 were re-derived by running rather than grepping**, and whether the fix landed at a
  shared seam or 73 call sites.
- **Whether any fixture sets the event by reaching into the project** instead of recording a decision.
- **Whether `make turbotab` (or its equivalent) refuses to start** on a stack it cannot import — driven
  by breaking the import, not by reading the script.
- **Whether `/dev/status` now names the interpreter**, and whether the run-4 situation would now be
  visible on screen.
- **Whether C invoked §06.2 in those words** and enumerated every new entry.
- **Whether the figure names the level the user chose.**
- **`ps` checked. 81 → 0, or the remainder named and filed.**

Report once, in `AGENT_ONBOARD.md` §10's shape. **Your divergence section is the first thing I read.**
You have corrected me in five consecutive loops, and last loop you reported a blast radius you had
measured wrong, in the same breath as the number that exposed it, and declined to fix it by relaxing
the thing under test. **That is the standard. §00 above is mine and hours old — check it too.**
