# TurboTab — the L52 loop

**If you are a fresh agent, read [`AGENT_ONBOARD.md`](AGENT_ONBOARD.md) in full first.**

**Four parts, and Part C starts the thing three of four research threads called the feature most
likely to make researchers recommend this tool.**

**L51 was accepted.** It lost four subagents to a weekly account limit mid-work, salvaged all four
chunks by hand, and got the probe wrong twice before getting it right — **and reported all of it
rather than quoting the first answer.** §00 is the response you are owed, and it contains a ruling
you escalated correctly.

---

## 00 · Your divergence section, answered

**`TEST-060` is the most valuable thing in the report, and you were right that it was not yours to
build.** Four of eight regression tests passed with the entire changeset reverted, written by four
independent agents. Your diagnosis is the correct one and it is not about carelessness: **the
cheapest passing assertion is the one describing what the code now does**, so a pass that writes both
the change and the check will write a check the change does not need.

**Ruled, in two tiers, and Part A writes both into `AGENT_ONBOARD.md` §08:**

1. **A subagent returning a `FIXED` disposition returns the probe output with it** — the revert, the
   red, and the sentence it was red for. **A disposition arriving without one is `PARTIAL` by
   default**, not on review. Your wording, adopted.
2. **Where the fan-out has room, the probe for a chunk is run by a different subagent than the one
   that wrote it.** `LOOP.md` §05 already says a builder verifying their own work reads intent where
   the job is to read code — that rule existed for whole loops and was never extended to subagents.
   **Tier 1 makes the evidence visible; tier 2 removes the cause.** Tier 2 costs agents, so it binds
   when there are agents to spare and tier 1 always binds.

**`GUIDED-212` is closed and I verified it by planting a bare marker**: the gate refuses it and names
three resolutions. Your 5 held out / 3 declared / 2 legends is right — my own scan said 4/5 because
it counted digits and missed the declared forms, which makes the gate the better instrument and my
check the crude one. **And holding out a second number exposing three defects in the gate's own
reader — docstrings, a nested apostrophe, f-strings on 3.12+, all replaced by `tokenize`** — is the
loop finding its own instrument's floor, which is the shape L49-A1 had too.

**`GUIDED-217`'s fixture is the detail worth keeping**: sparsity generated from a depth × gamma
gradient **so it is a consequence rather than a number written to clear a threshold.** That is the
difference between a fixture and a fixture-shaped assertion, and it should be the house style.

**`AUDIT-028` is the model for every remaining row in Part B.** The old sentence said *"Every
statistic in it is fitted inside the training folds"*; the door has no folds — verified, nothing in
`turbotab/` imports `KFold`, `cross_val_score` or `cross_validate`, while `ml/` does — and the new
sentence says *"fitted once over the N training rows, and the held-out rows inform none of them."*
**The claim was corrected to the one the door can keep, not dropped.** Nine tests pinning the
stronger claim went red and you fixed the tests rather than reverting the correction, which is trap
#3c handled correctly.

**One correction to `AUDIT-037`, and it is recorded in the row.** `INTERACTION_PACK.md` genuinely has
no anti-pattern content. **`CLINICAL_SURVEY_PACK.md` does** — nine `**Anti-patterns.**` blocks at
lines 230, 381, 415, 441, 626, 813, 886, 946 and 1097, roughly twenty-five named entries and that is
a floor. `NUTRITION_PACK.md` is the same shape with four `### Anti-patterns` subsections. **So 131
counts *tabulated* entries, not named anti-patterns.**

**The class is `MISC-014` in a third costume** — *unrouted is not absent*, then *un-modularized is
not absent* (L50, where the adjudicator nearly reported two packs as having no code), now
**untabulated is not absent.** Each time the sweep asked for a **shape** rather than for the content.
And it inverts the row's headline: the survey door is not missing its conformance surface, it is
missing the **table**, so that work is *harder* than iterating a registry rather than cheaper.

---

## 01 · State

Branch `TurboTab`. Ledger **859 findings, 347 closed**. Register **181 rows**. Six gates green.

Re-measured by the adjudicator in an isolated worktree at `e298ddf`: `tests/integration` **211
passed**, `tests/` **1692 passed / 4 failed** — the four known environmental.
`tests/test_a_fixed_row_names_a_test_that_actually_runs.py` was excluded from that run **because it
cannot finish**, which is Part A.

`make test` still aborts at collection on `TEST-038`. **Do not work around it.**

---

## 02 · How this loop runs

**Everything from L49–L51 §02 binds unchanged.** Plus the two from §00, which are now rules rather
than proposals, and the two L51 earned the hard way:

- **A `FIXED` from a subagent arrives with its probe output or it is `PARTIAL`.**
- **`zsh` does not word-split unquoted variables**, so a loop over `$FILES` reverts nothing and
  reports green. L51 lost a probe pass to this. **Quote, or use an array, or use Python.**
- **`grep -E` treats `\|` as a literal pipe.** L51's sweep reported *"no multiple-testing correction
  anywhere in shipped code"* while `ml/multiplicity.py` had been there the whole time. **A sweep that
  finds nothing is a claim, and the first thing to doubt is the pattern.**
- **Check `ps` before anything heavy**, and file ledger notes through a Python file, never a heredoc.

**Fan out on B.** Keep A and C in one hand — A changes the guard everything else is measured by, and
C is a new artifact.

---

# Part A — the guard that stopped checking

**`TEST-061` (`high`), and it is first because it is the instrument.**

`test_every_fixed_rows_named_test_actually_runs` ran **1800.03s against its own
`pytest.mark.timeout(1800)`** and died there. **It reported not one offender**, because it never
finished collecting them — so its red says nothing about whether any `FIXED` row rests on a skipping
test, which is the single question it exists to answer. **347 closed rows currently rest on a guard
that cannot run.**

**Its own docstring predicted this**: *"scoped to those files rather than the whole suite because a
twenty-minute check is a check somebody turns off."* It is a thirty-minute check now.

**Your two directions are both live and the row states them. Take the first unless it fails:** run
each named node directly rather than running the whole file and then asking which node skipped — the
guard already has `_node_skips` for the second pass, and doing it first makes the file-level pass
unnecessary. Parallelizing is the fallback.

> **Raising the timeout is not on the menu.** It converts a check nobody can wait for into a check
> nobody runs, and the row says so.

**And then run it**, because nobody has seen its answer since it outgrew its budget. **Report the
offenders.** If there are none, that is the most reassuring number in the project; if there are some,
they are `FIXED` rows resting on tests that do not execute.

**A2 · Write both tiers of the `TEST-060` ruling into `AGENT_ONBOARD.md` §08**, with the measurement
behind them: four of eight, four independent agents, one fan-out.

---

# Part B — the app stops asserting things that are not true

**Twenty-three real defects open, plus two meta-rows.** L51 closed four of fifteen attempted and said
so plainly; this is the rest. **Fan out, and every chunk returns its probe output.**

**Take the manuscript and its captions first** — they are the artifact that leaves the building:

| | |
|---|---|
| **Manuscript prose** | `AUDIT-022`, `023`, `026` (`PARTIAL`), `029` (`PARTIAL`), `031`, `032`, `034` |
| **Figure captions** | `AUDIT-016`, `018`, `036`, and `008` which is the class beneath them |
| **The record / seal** | `AUDIT-011`, `027` |
| **Advice that cannot tell two things apart** | `AUDIT-003`, `012`, `033`, `035`, `006` |
| **Classic pages** | `AUDIT-007`, `013`, `024`, `025` |

**`AUDIT-028` is the pattern to copy**, per §00: **correct the claim to the one the surface can keep.
Do not delete it, and do not weaken it into vagueness** — a sentence that says less but true is the
goal, and `AUDIT-021`'s handling at `ml/model_coach.py:1466`, which records the before and the after,
is the second model.

**`AUDIT-031` and `AUDIT-032` carry standing red xfails.** They stay red unless the xfail goes with a
recorded reason. **Do not close them to make a suite green.**

**`AUDIT-037` and `AUDIT-038` are not defects** — they are the registry's own bookkeeping and they
close when the registries are run, which is not this loop.

---

# Part C — the checklist engine, the half that does not wait

**Three of four research threads independently named the auto-populated reporting checklist as the
deliverable most likely to make researchers recommend this tool.** It has slipped twice, both times
correctly, because it reads the manuscript and the manuscript was changing.

**That argument only ever covered half of it.** `DOMAIN_SCIENCE.md` §01.7: the checklist is *"a
checklist-shaped artifact with **two column types**: what the app knows, and what it must ask."*
**The second column depends on nothing.** Build the artifact and that column now, so L53 wires
population into a structure that already exists instead of inventing both at once.

**Seed with one checklist, not four** — `DOMAIN_SCIENCE.md` §05 item 6 is explicit. **Seed with
TRIPOD+AI**, because a clinical prediction model is what this app mostly builds and TRIPOD+AI is the
instrument a reviewer will actually apply.

**The specification is written and you should not invent any of it.**

- **`CLINICAL_SURVEY_PACK.md` §A6** — TRIPOD+AI, Collins et al., *BMJ* 2024;385:e078378, **27 items**,
  and it **names which ones the app can substantially auto-populate**: participant flow · outcome
  definition · predictor definitions and timing relative to the index · missing-data handling *with
  the mechanism discussed, not just the method named* · sample-size justification · model-building
  including selection and all hyperparameters · **discrimination AND calibration AND clinical
  utility** · model presentation sufficient for others to compute predictions · fairness/subgroup
  evaluation · open-science items · limitations and intended use.
- **The presentation is specified as four columns**, verbatim: **item | where addressed | auto-filled
  text | ⚠ needs your input.**
- **`NUTRITION_PACK.md` §09's table is the shape of the two column types made concrete** — fourteen
  requirements each marked `user`, `app`, `app detects + user confirms`, or `user + app template`.
  **Use that vocabulary rather than a boolean**, because *"the app detects it and you confirm"* is a
  real third state and a two-value column would lose it.

**Build in this loop:** the artifact, its item set for TRIPOD+AI, the four-column rendering, and the
**needs-your-input column fully populated.** **Do not build auto-population** — that is L53, and it
depends on Part B landing.

**Two things that make it honest rather than decorative:**

- **An item the app cannot fill says which quantity is missing and why**, in the vocabulary
  `GUIDED-179` established — *not estimable, and here is what would make it estimable* — never a
  blank cell and never `None`.
- **`PROBAST` gets a mention and not a build.** §A6 carries it — 4 domains, 20 signaling questions —
  with the line worth surfacing: *"If your paper is later included in a systematic review, this is
  the instrument that will be applied to it."* One sentence, sourced. **Not a second checklist.**

**A capability ships with its consumer.** The checklist must reach the Report step in this loop or
ship with a failing test naming the consumer it lacks — `GUIDED-119`'s `xfail(strict=True)`.

---

# Part D — small, and non-tradeable

- **`TEST-059`'s class, swept once.** A conditional skip is a green test over a regression, and
  pytest counts a skip as not-a-failure. **How many other tests skip on a condition the fix was
  supposed to establish?** One sweep, report the count, fix what it finds or file it.
- **`GUIDED-178` stays `PARTIAL`** and its note stands — the deck is blocked pre-seal and measured
  as such. **Do not revisit it.**
- **The prototype stylesheet rule** — L51 broke it in Part B, caught by the standing gate rather than
  by the agent. It is carried, not edited. No action beyond knowing it.

---

## Rules that bind

- **A `FIXED` arrives with its probe output.** New, and it is §00's ruling.
- **Correct a claim to the one the surface can keep; do not delete it and do not blur it.**
  `AUDIT-028` is the model.
- **A sweep that finds nothing is a claim, and the pattern is the first thing to doubt.**
- **Ambiguity is `OPEN`, never `FIXED`.**
- **A capability ships with its consumer**, or a **failing** test naming the one it lacks.
- **Domain science from `docs/turbotab/research/`, by section, never from recollection.** Part C in
  particular: every item traces to §A6 or §09.
- **Say the number**, and say which file it came from.

## Deliberately unbuilt

- **Auto-population of the checklist** — L53, and it depends on B.
- **A second, third or fourth checklist.** STROBE-nut, COSMIN and mQACC come after TRIPOD+AI proves
  the artifact. `DOMAIN_SCIENCE.md` §05 item 6.
- **PROBAST as a built instrument** — one sourced sentence only.
- **The 77 unchecked registry entries** (`AUDIT-038`) and the clinical/survey pack's ~25 untabulated
  ones. They come back when a loop is shaped for them, and §00 says why that work is harder than it
  looked.
- **`GUIDED-105`, `GUIDED-106`, `GUIDED-118`** — the missing model families; time-to-event is next
  after the checklist.
- **`TEST-049`'s instrument half**, `GUIDED-073`, `GUIDED-155`'s Propagate half, `GUIDED-120`,
  anything at Train, and the standing unchanged list from L51.

## Scope note

**A is not tradeable and it is first.** Three hundred and forty-seven closed rows currently rest on a
guard that cannot finish.

**C is the loop.** It is the deliverable the research ranks highest and it has slipped twice.

**B is wide and expected to be partial.** Report what you disposed of and what you did not — four of
fifteen was accepted last loop for being honest about it.

**D is small.**

**If B and C compete, C wins.** B has been partial for two loops and will survive a third; the
checklist has been deferred twice and a third would be the pattern this project calls decay.

## What will be checked

- **Whether the `FIXED`-row guard now finishes, and what it found.** A green run that reports zero
  offenders is a real result; a run that still cannot finish is Part A unmet.
- **Whether every Part B row was corrected rather than deleted or blurred.**
- **Whether `AUDIT-031`/`032`'s xfails are still red.**
- **Whether the checklist reaches the Report step**, or ships with a failing test naming its missing
  consumer.
- **Whether any checklist item was invented.** Every one traces to §A6 or §09, and the gate resolves
  the source.
- **Whether an unfillable item says which quantity is missing**, rather than rendering blank.
- **Whether any subagent returned a `FIXED` without its probe output.** That is now `PARTIAL` by
  default and the report should say if it happened.

Report once, in `AGENT_ONBOARD.md` §10's shape. **Your divergence section is the first thing I read.
Last loop it escalated a decision that was genuinely mine rather than making it quietly, and that is
the hardest thing on this list to get right.**
