# TurboTab — the L53 loop

**If you are a fresh agent, read [`AGENT_ONBOARD.md`](AGENT_ONBOARD.md) in full first.**

**Four parts. Part B is the checklist's second column — the half that has been waiting for the
manuscript to stop moving, and it has.**

**L52 was accepted.** §00 is the response you are owed, and it contains one ruling you escalated,
one correction to your own framing that makes your position *better* than you stated it, and the
verification of Part C, which is the best-built artifact this project has shipped.

---

## 00 · Your divergence section, answered

**The `pytest-xdist` dependency: keep it.** You escalated it rather than burying it, which was right.
It is dev-only, it sits in `requirements-dev.txt`, the guard has a **serial fallback** so it still
runs where xdist is absent, and the alternative is a guard that cannot finish. That is a cheap,
fenced, reversible dependency and the measurement justifies it.

**`--dist loadfile` → `--dist load` is self-correction on a measurement and it is the right kind.**
Your reasoning for `loadfile` was sound *a priori* — module-scoped fixtures — and it put four tests
totalling 2157s on one worker. **You measured instead of defending it.** `AUDIT-040` is the durable
half: **632s is a hard floor** on this guard however wide it runs, because parallelism cannot go
below the longest single test, and before this nobody knew which file held the budget.

**`TEST-061` is correctly `PARTIAL`, and the distinction you drew is the one that matters.** *Met on
its question, not met on its budget.* The answer is **0 offenders across 880 targets and 311 rows** —
and nobody had seen that number since the check outgrew its budget, so it is worth stating plainly:
**no `FIXED` row in this ledger rests on a test that skips.**

**Your second check is the better half of Part A.** Four rows named a function that exists nowhere,
and the old guard **structurally could not have found them** — it asked whether a named node
*skipped*, and an uncollectable node never reports a skip. `STATE-110` left `PARTIAL` rather than
renamed onto a test whose own name asserts the opposite of the row's second half **is the right
call**: a dangling pointer is honest and a wrong pointer is not.

**§08.1 tier 2 earned its keep on its first run and the case is exactly the one it was written for.**
`AUDIT-008`'s builder claimed `FIXED` and quoted four real `AssertionError`s — **and had probed with
a partial revert that preserved a key its own fix introduced**, so the test could not survive a full
revert long enough to reach its claim. An independent verifier found it. That is the ruling paying
for itself in one loop, and it is why tier 2 stays.

### The correction, and your position is better than you stated it

**Divergence items 5 and 6 are one finding, and it is not trap #1.**

You reported *"two modules built for rows in this set have no importer — trap #1, in the artifact
that leaves the building."* Verified, and the framing is worse than the facts:

- `ml/sample_size_claim.py`'s own docstring says it is for **`AUDIT-022`**. That row is **`OPEN`**.
- `ml/candidate_predictors.py`'s says **`AUDIT-023`**. Also **`OPEN`**.
- And the `AUDIT-020`/`021` fix — the one that actually ships — is `ml/sample_size.py`, which **is**
  wired: `ml/dataset_profile.py` twice, `ml/model_coach.py`, `turbotab/resolution.py`.

**Trap #1 is dangerous when the row reads closed.** These rows read `OPEN`, so the ledger is telling
the truth. What you actually have is **finished logic for two open rows, blocked from its wiring
site by your own file partition** — which is divergence item 5, `AUDIT-022` and `AUDIT-023` needing
`pages/10` and `pages/04` that another chunk owned. **The two observations are the same event seen
from both ends**, and together they make Part A cheap: the logic exists, only the wiring is missing.

**And the partition lesson is the durable one.** You honored *a subagent never writes a shared file*
by giving each chunk a disjoint **row** list, and rows do not partition by file. **Partition by fix
site instead**: group rows that land in the same file into the same chunk, even when they are
otherwise unrelated. §02 carries it now.

### Part C, verified

Driven by the adjudicator rather than read. `/checklist` returns 200; the page fetches it at
`index.html:3509` and renders it into `reportBox` at `:3632`. **Twelve rows, every one tracing to
`research/CLINICAL_SURVEY_PACK.md#A6`.** All four of §09's fill states in use — `app` 5,
`app detects + user confirms` 3, `user + app template` 3, `user` 1 — and every value one of the
declared four. `needs_from_you` populated on **every** row, with sentences that say what the app can
see and what it cannot.

**The coverage block is the best thing in it**, and it is the governing rule applied to the
artifact's own completeness:

> `instrument_items: 27, enumerated_here: 12, not_yet_enumerated: 15`, with *why* — *"writing them
> from recollection of the source paper is the one thing this project does not do with domain
> science"* — and *how_to_close*: *"It is pack authoring, not a code change."*

**And the nulls are the right design, which is sharper than the prompt asked for.** The prompt said
no cell renders `None`. You made the **structured** field `null` and the **rendered** field a
sentence — `where_addressed: null` beside `where_addressed_text: "not filled by the app yet"` and a
`not_filled_because`. That is trap #9 and trap #7 held at once: the machine value is honestly absent
and the human value is honestly a sentence, and neither is lossier than the other.

**One thing I could not check and you should say next time:** whether `checklistHTML()` renders
`not_filled_because` or only the short text. The payload carries the reason; I did not confirm the
page shows it.

---

## 01 · State

Branch `TurboTab`. Ledger **861 findings, 353 closed**. Register **181 rows**. Six gates green.

Re-measured at `e29c9c7`: `tests/integration` **230 passed** (was 211 — L52 added nineteen).
`turbotab/` re-running as this was written; L51's was **2274 / 0**.

`make test` still aborts at collection on `TEST-038`. **Do not work around it.**

---

## 02 · How this loop runs

**Everything from L49–L52 §02 binds**, plus the two this loop adds:

- **Partition a fan-out by FIX SITE, not by row.** L52 gave each chunk a disjoint row list and two
  rows died because their fix belonged in another chunk's file. Group by the file the fix lands in.
- **A matcher that fires on prose has silence that means nothing.** L52 met this three times in one
  loop — `'tests'` matching the English word in *(seven tests)*, a leaf-segment index missing
  `TestCommaReading`, and a `None`-detector firing on *"**None** of them is a property of the
  data."* **The adjudicator then did it again** while verifying Part C, with a `"None" in blob`
  check that hit the same prose. **Anchor the pattern, or match structure instead of text.**

**And §08.1 both tiers bind**: a subagent's `FIXED` arrives with its probe output or it is `PARTIAL`,
and where there are agents to spare the probe is run by a different one than wrote the fix. **The
revert must be total** — `AUDIT-008`'s builder reverted partially and preserved the key its own fix
introduced.

---

# Part A — wire what is already built

**Cheap, and it unblocks Part B.**

`ml/sample_size_claim.py` (`AUDIT-022`) and `ml/candidate_predictors.py` (`AUDIT-023`) are finished
logic with no consumer, because their wiring sites — `pages/10` and `pages/04` — belonged to another
chunk. **Wire them and close both rows.**

`AUDIT-022` is the manuscript's Strengths list printing *"Sample size of N observations"* behind
`if analysis_total > 0`, so forty rows and forty thousand earn the same bullet — and the same
document can print it as a strength and a limitation in one breath. `AUDIT-023` is the candidate
predictor count reporting the **kept** features while calling them *candidates*, which is
`§A5.4`'s ⚠ item.

**Both land in the manuscript, so both are Part B's input.** Do A first.

**A2 · `TEST-061`'s budget half.** The question is answered; the cap is not met. `AUDIT-040` says
**632s is the floor**, so the honest options are to make the four monsters cheaper, to raise the cap
*with the measurement recorded beside it*, or to declare the guard a nightly rather than a suite
member. **Raising the cap silently is still off the menu; raising it with `AUDIT-040` quoted beside
it is a different act.** Your call — you have the measurement and I do not.

---

# Part B — the checklist fills itself

**The half that waited, and the wait is over.** `AUDIT-026` and `AUDIT-029` — the cross-validation
claims — are `FIXED`. `AUDIT-016`'s caption is `FIXED`. Part A closes `022` and `023`. **The
manuscript has stopped moving under the checklist.**

**Fill the `auto-filled text` and `where addressed` columns** for the items §A6 says the app can
substantially auto-populate, reading from the record rather than composing new prose:

- **Participant flow** — the counts the app already holds at every step it performed.
- **Missing-data handling with the mechanism discussed, not just the method named** — you have the
  mechanism, because `route_missingness` asks for it and records it.
- **Sample-size justification** — `ml/sample_size.py` after Part A, and `AUDIT-021`'s corrected
  position that EPV≥10 is superseded.
- **Model-building including selection and all hyperparameters.**
- **Discrimination AND calibration AND clinical utility** — all three or the item says which is
  absent.
- **Predictor definitions and timing relative to the index.**

**Three rules, and they are what stop this becoming prose generation:**

1. **Quote the record; do not re-compose it.** L36 established this — the methods section quotes the
   record's own string, and that is only safe because L35-B made the string true of the fit. **An
   auto-filled cell that paraphrases is a second copy of a claim.**
2. **`where addressed` points at a section that exists.** A pointer to a section the draft does not
   contain is `AUDIT-001`'s shape inside the artifact built to catch it.
3. **An item that cannot be filled keeps its sentence.** You built that vocabulary in L52; do not let
   auto-population overwrite a good *"not filled because"* with a blank.

**`test_the_artifact_says_auto_population_is_not_built` goes red this loop.** You wrote it to. **Its
replacement asserts the consequence** — that a named item is filled from the record — rather than
that the flag flipped.

---

# Part C — the rest of the false-assertion tail

**Ten `OPEN`, five `PARTIAL`.** Fan out, partitioned **by fix site** per §02.

`AUDIT-006`, `013`, `018`, `025`, `031`, `032`, `034`, `035`, and the `PARTIAL`s `003`, `007`,
`008`, `011`, `012`. `AUDIT-022`/`023` leave this list via Part A.

**`AUDIT-008` is the one to take first** — it is `PARTIAL` because its forest half dies on a
`KeyError` for a key the fix itself introduced, so **finishing it means the test survives a total
revert.** It is also the class row beneath the caption findings.

**`AUDIT-031`/`032`'s xfails stay red** unless the xfail goes with a recorded reason.

**And a verifier found something that outranks several of these**: all twelve xfail guards in the
a5/b6 registry file are **unconditional `pytest.fail`** and **none reads shipped code.** A registry
of twelve guards that cannot observe the thing they guard is a green suite over an unknown. **Check
whether it is filed; if not, file it, and do not fix it here** — it is its own loop.

---

# Part D — small

- **`AUDIT-039`** — twelve tests skip on a condition their own fix was supposed to establish. You
  fixed two and both *passed*, meaning they were running all along and would have gone **quiet
  rather than red**. The remaining twelve have file, line and guard recorded. **Take as many as the
  loop has room for.**
- **`STATE-110` stays `PARTIAL`.** The dangling pointer is the honest state until the test it should
  name exists.

---

## Rules that bind

- **Quote the record, do not re-compose it.** Part B's central rule.
- **A total revert, or the probe proves nothing.**
- **Correct a claim to the one the surface can keep** — `AUDIT-028`'s model, and L52 applied it
  seven times.
- **A matcher that fires on prose has silence that means nothing.**
- **Ambiguity is `OPEN`, never `FIXED`.**
- **A capability ships with its consumer** — Part A is two modules that did not.
- **Domain science from `docs/turbotab/research/`, by section, never from recollection.** Part B's
  items trace to §A6 exactly as Part C's did in L52.
- **Say the number.**

## Deliberately unbuilt

- **The remaining 15 TRIPOD+AI items.** `coverage.how_to_close` is right: pack authoring, not a code
  change, and it belongs in a loop that reads the instrument.
- **A second checklist** — STROBE-nut, COSMIN, mQACC come after TRIPOD+AI's population proves out.
- **The twelve a5/b6 xfail guards** — file, do not fix.
- **The 77 unchecked registry entries** and the clinical/survey pack's ~25 untabulated ones.
- **`GUIDED-105`, `GUIDED-106`, `GUIDED-118`** — time-to-event is next after this.
- **`TEST-049`'s instrument half**, `GUIDED-073`, `GUIDED-155`'s Propagate half, `GUIDED-120`,
  `GUIDED-178`, anything at Train, and the standing unchanged list from L52.

## Scope note

**A is small and first**, because it unblocks B and the logic already exists.

**B is the loop.** The checklist has been the highest-ranked deliverable in the research since the
domain work began, and this is the half that makes it more than a form.

**C is wide and expected to be partial.** Report disposed and not.

**D is the trade.**

**If B and C compete, B wins.**

## What will be checked

- **Whether the two modules now have consumers**, and whether `AUDIT-022`/`023` closed against them.
- **Whether an auto-filled cell quotes the record or paraphrases it.** A paraphrase is a second copy
  of a claim and this project has ruled on that twice.
- **Whether `where addressed` points at a section the draft actually contains.**
- **Whether the not-filled sentences survived auto-population**, or got overwritten with blanks.
- **Whether `test_the_artifact_says_auto_population_is_not_built`'s replacement asserts a
  consequence** rather than a flag.
- **Whether any subagent's `FIXED` arrived without a total revert.**
- **`turbotab/`, measured on a quiet machine**, with `ps` checked.

Report once, in `AGENT_ONBOARD.md` §10's shape. **Your divergence section is the first thing I read.
Last loop it escalated a dependency, self-corrected a design choice on a measurement, and reported
three sweeps that were wrong before they were right — and the adjudicator then made the same
prose-matcher mistake while checking your work.**
