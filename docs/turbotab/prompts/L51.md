# TurboTab — the L51 loop

**If you are a fresh agent, read [`AGENT_ONBOARD.md`](AGENT_ONBOARD.md) in full first.**

**Five parts, run unattended overnight, and the product owner has explicitly authorized heavy
orchestration for it.** Fan out as wide as you can hold on C and E; keep A, B and D in one hand.

**Part B is the last thing standing from the product owner's own drive.**

He asked, this turn, whether his NHANES findings had been addressed. Measured: **19 of 24 closed,
both criticals among them.** Of the five left, **three are one design** — `GUIDED-160`, `GUIDED-174`,
`GUIDED-178` — and it has been deferred three times, once by the adjudicator on his own
time-pressure ruling. **`GUIDED-174` is his proposal, filed at his request.** It stops being deferred
here.

**L50 was accepted, six of six**, and it was the widest loop this project has run: detectors
**22 → 38**, claims 52 → 65, eleven fixtures. §00 is the response you are owed.

---

## 00 · Your divergence section, answered

**You were right about the badges and the prompt was wrong.** L50-F2 asserted all thirteen of
`METABOLOMICS_PACK.md` §11's hedges are `DISPUTED`. **Re-verified in the file by the adjudicator**:
§08 marks OPLS-DA's rotation **`[SETTLED among chemometricians]`**, §04 marks Pareto
**`[CONVENTION]`**, §02 marks the 80% thresholds **`[CONVENTION]`**. Your distribution — 7 / 2 / 4 —
is the file's.

**And your reason for refusing is the one that matters.** Badging all thirteen `DISPUTED` would make
*"a rotation does not reduce overfitting"* read exactly like *"nobody agrees on the QC RSD
threshold"* — `AGENT_ONBOARD.md` §00's **second uncalibrated layer, arriving through a prompt
instruction** rather than through timidity. You refused it on the onboard's own argument. That is the
third loop running your divergence section has corrected the adjudicator, and the most consequential
of the three, because it would have degraded the badge system itself rather than one row.

**`GUIDED-212` is the sharpest thing in your report and it is confirmed.** Nine `[verify-at-build]`
markers in the corpus; gate 6 extracts numbers from the marker's **payload**, so the eight bare ones
contribute nothing and are never checked. **A guarantee three documents call structural held for one
of nine.** It is Part A.

**`GUIDED-211` is correctly `PARTIAL`.** Closed for the §11 register where the fix landed, open for
seven pre-existing positions — *ambiguity is `OPEN`, never `FIXED`*, applied to your own work. Its
class `TEST-052` is sharper than trap #6 and you named why: **a raising constructor makes the
obligation read as closed.**

**`GUIDED-217` was raised `medium` → `high` by the adjudicator**, on your own evidence: the loop made
it **worse rather than neutral**. Not `critical`, because no fixture here is single-cell and that
reachability line is what `critical` has meant in this ledger — but a confident false classification
the loop itself created is not `medium`. It is Part A beside `GUIDED-212`.

**Two of your own fixture numbers were wrong and two subagents caught it independently**, because
both re-derived rather than quoting the brief forward. That is this project's rule working in the
direction it was written for, and `TEST-056`'s guard then catching your own companion is the loop
auditing itself.

**And the stall is a rule we did not have.** Your generalization is right and §06 was missing it: **a
suite is quotable only if nothing else is competing for the machine**, not merely if nothing else is
writing the tree. Part A writes it. *(The adjudicator has still not measured `turbotab/` for the same
reason — two of your suites and a day-old orphaned uvicorn were live, and a fourth competitor would
produce a number worth nothing.)*

---

## 01 · State

Branch `TurboTab`. Ledger **853 findings, 335 closed**. Register **181 rows**. Six gates green.
Detectors **38** across five lenses.

`tests/integration` **211 passed** and `tests/` **1652 / 4 environmental**, both re-measured by the
adjudicator in an isolated worktree. **`turbotab/` is unmeasured** — establish it yourself, on a
quiet machine, and **check `ps` first.**

`make test` still aborts at collection on `TEST-038`. **Do not work around it.**

---

## 02 · How this loop runs

**Everything from L49 §02 and L50 §02 binds unchanged**: one writer on the data files, no subagent
commits, no subagent runs a full suite, no subagent runs a tree-wide git operation, a subagent gets
its own worktree or no write tool. **`worktree.py` now refuses a bad base** — L50-A1 built that, and
it exists because L49's isolation branched three agents 367 commits off.

**Two additions, both from L50.**

- **Check `ps` before you start anything heavy, and again at twenty minutes.** A suite competing with
  three others is not a measurement.
- **File ledger notes through a Python file, never a shell heredoc.** The heredoc eats backticks and
  has damaged notes across three loops now.

**This loop runs overnight and unattended, and the product owner has authorized orchestration for
it.** That changes the width, not the rules.

- **Fan out on C and E, hard**, in worktree-isolated chunks of two or three items. Both are wide and
  their items are independent. **A dead subagent must cost a chunk, not the batch** — L48 lost
  thirteen rows to two subagents dying on a session limit.
- **Do not fan out B.** It is one design and four agents would produce four. Build it yourself.
- **A and D stay in one hand** — A1 changes a gate every other part is measured by, and D is four
  dispositions.
- **Commit per chunk where the chunk is worktree-isolated** (that is now honest, because the tree is
  not shared); otherwise ledger per chunk and code once, and **say which you did.**
- **Sequence C before E.** C disposes of defects already found; E finds more. Running E first would
  grow the queue while the queue is what C is for.

---

# Part A — the guarantee that held for one of nine

**A1 · `GUIDED-212`.** `[verify-at-build]` is called *structurally forbidden from shipping as a
constant* in `AGENT_ONBOARD.md` §00, `ROADMAP.md` condition 6 and `LOOP.md` §04. Gate 6 in
`docs/turbotab/tools/evidence.py` builds its `unverified` set from `_NUMBER.findall(m.group(1))` —
the marker's payload — so **`[verify-at-build]` with nothing after it protects nothing.** Eight of
nine markers are bare.

**The fix is not only the regex.** A bare marker names a claim whose number the research could not
read; the gate needs to know *which number*, and the corpus does not currently say. So this is two
moves: **make a bare marker a gate failure in its own right** — it cannot be checked, and an
uncheckable marker is the recorded-absence rule's case, not a pass — **and go back through the eight
and give each one its number**, from the research section it sits in. Where the number genuinely
cannot be named, that is a `BLOCKED.md` entry.

**A2 · `GUIDED-217` (`high`).** The refusal goes **in front of** the classification cascade, not
after it. `GENOMICS_PACK.md`'s scope paragraph states the three thresholds. No fixture is
single-cell, so it ships **with a fixture or with `xfail(strict=True)` naming the missing detector**
— `GUIDED-119`'s model. **This is the one input the research says must be refused, and the loop that
built the reader made the app confident about it.**

**A3 · The machine rule.** Add the sibling to `AGENT_ONBOARD.md` §06: *a suite is quotable only if
nothing else is competing for the machine.* Your sentence, your finding.

---

# Part B — the right column, and it is the product owner's own design

**Build this yourself. It is one design and it has three legs.**

**The rule that binds it, and it is his**: *"In addition to being correct, the engine must surface and
it must be beautiful."* `ROADMAP.md` condition 7. This is that condition's remaining work.

**B1 · `GUIDED-174` — the working table, beside the decisions being made about it.** His proposal, in
his words, filed at his request. The layout already has room: `index.html:88`
`.shell{display:flex;max-width:1240px}`, `:89` `.rail{flex:0 0 236px}`, `:136`
`.feed{flex:1;max-width:800px}` — 236 + 800 inside 1240, and the rail hides under 900px. **The
premise holds; the column exists.**

**B2 · `GUIDED-160` — education layer 3.** `DESIGN_LANGUAGE.md` §10 specifies four layers and the app
ships two, so layer 2 is carrying layer 3's job at **135 abstract words in a disclosure the spec caps
at two or three sentences.** §10 names the failure before it happened: layer 1 cites *expertise
reversal*, layer 2 cites *split attention*, and a long abstract disclosure breaks both at once.

**These two are one panel, and that is the design.** §10's layer 3 is *"the concept explained with
THEIR columns and THEIR numbers, not abstractions"* — which **needs the table on screen to point
at.** He proposed the column for the data; the design language reserved it for teaching. Reading them
as competitors is the mistake. **The working table is layer 3's substrate.**

**Do not shorten the question to make the teaching shorter** — his standing ruling is that hard
questions stay hard and we invest in pedagogy. This is a container problem.

**B3 · `GUIDED-178`'s buildable half only.** The deck of per-model before/after tables is **blocked
and the blocker is measured**: `resolved_recipes()` short-circuits at `project.py:1733-1734` on
`if not self.selected_models: return out`, and `selected_models` is gated on `barrier_raised` — so
`/recipes.models` is `{}` for the **entire pre-seal journey** and there is no per-model data at
Preprocess to render. **Build the panel's Preprocess state as the honest placeholder it must be**,
the way `/recipes` already renders *"Nothing is missing — the step that fills it has not happened"*.
**Do not invent the deck.** And note the row's own ruling: what the card can honestly show is a
**training-row preview labeled *preview, not applied***, never *"what will be fed into the model"*,
because clause 06 fits every stateful transform inside the fold and those numbers do not exist until
the fold does.

**His phase change is right and keep it**: after the seal the working table stops being the object of
attention and the run becomes it.

---

# Part C — the app stops asserting things that are not true

**The wide part. Fan out.**

**Twenty-seven `AUDIT` rows are open, twelve of them `high`**, and they are overwhelmingly one class:
**the app tells the user something that is not true about what it did.** That is `AUDIT-001`'s exact
shape — the finding that started this registry — twenty-seven times over. Grouped by where they land:

| Surface | Rows |
|---|---|
| **The manuscript / methods prose** | `AUDIT-017`, `022`, `023`, `026`, `028`, `029`, `031`, `032`, `034` — **nine** |
| Sample-size and task-type advice | `AUDIT-003`, `012`, `020`, `021`, `033`, `035`, `006` |
| Classic pages | `AUDIT-007`, `013`, `024`, `025` |
| Figure captions | `AUDIT-008`, `016`, `018`, `036` |
| The record / seal | `AUDIT-011`, `019`, `027` |

**Take the manuscript nine first.** They are in the artifact that leaves the building, which is where
this project has ruled a false assertion costs most — a methods section stating cross-validation was
run when it was not, an imputation declaration writing *"within each training fold"* into the record,
a Strengths list asserting what the dataset contained. **Order the rest hardest-first.**

**Two of them are deliberately parked and stay parked**: `AUDIT-031` and `AUDIT-032` carry standing
red xfails. **Do not close them to make a suite green** — if you fix them, the xfail must go with a
recorded reason.

**And this is not a sweep — the checks already ran.** These are hits sitting open. **What Part C owes
is dispositions, not discovery**: fixed with a named test, or `PARTIAL` with what remains, or
`WONTFIX` with a reason. **A found-and-unfixed false assertion is worse than an unrun check** — which
is why C runs before E and is not tradeable against it.

---

# Part D — the tail, and it stops growing

**Non-tradeable, and small.**

- **`GUIDED-189` (`high`)** — a built pull chip opens onto **zero cards**, because the gate that
  raises it and the gate that fills it disagree. **Third deferral.** Two thresholds 4× apart. If the
  fix requires moving one, **§06.2's exception is available and must be invoked in those words**,
  naming which quantity is gated — or leave both and state why.
- **`GUIDED-196`** — the boot's terminal `.catch` swallows a controller-wide `ReferenceError`.
  `GUIDED-139` is why this matters: one such error once killed every pull affordance in the door and
  nothing said so.
- **Park `GUIDED-169` and `GUIDED-172` with a dated reason.** Both are the product owner's, both
  `medium`, both told to drop first twice. **Parking is honest; deferring a fourth time is how a row
  becomes furniture.** Say in the note that they return when a loop has room, and what would make
  them worth doing.


---

# Part E — the registries that have never been run

**The product owner asked for both, and this is the second one.** Roughly **114 of ~150** named
anti-patterns across the five packs have never been checked against the engine. Thirty-six are
filed; nine are fixed.

**The adjudicator's stated reservation, recorded so it is not rediscovered**: running more checks
while twenty-seven hits sit open adds to a queue rather than clearing it, and every hit E finds
becomes a row somebody must dispose of. **He has ruled to do both anyway, and that ruling stands** —
the registries are simultaneously pack content and a conformance suite, and this is a long
unattended run with room for both.

**So E is scoped to make that safe: E files, it does not fix.**

- **Every hit gets a row** with the registry entry it came from, its pack and section, and its
  severity — and **nothing gets fixed in E** except where the fix is a one-line correction to a
  string the check itself names. Fixing is C's job and next loop's.
- **Run the two assay registries first** — `GENOMICS_PACK.md` §11 (26 entries) and
  `METABOLOMICS_PACK.md` §10 — because L50 built those two packs and dropped their registries as its
  scope note directed, so they are the freshest and the most likely to hit code written this week.
- **Then the rest, hardest-first**, judging hardest by which is most likely to hit **shipped** code
  rather than pack content.
- **Report the denominator.** How many entries the registries actually contain — the adjudicator's
  ~150 is itself an estimate from `DOMAIN_SCIENCE.md` §03b and has never been counted. **Count it.**
  Then how many you checked, how many hit, how many you could not check and why.

**A registry entry that cannot be checked is a result**, not an omission — several are about
practice rather than code. Say which, and say why.

---

## Rules that bind

- **A guard whose description is broader than its behavior is the defect this project finds most.**
  Part A1 is one; ask of anything you touch whether its docstring claims more than its assertions.
- **`FIXED` requires a named regression test verified to fail on revert, for the stated reason** —
  and not while anything else is writing the tree **or competing for the machine.**
- **Ambiguity is `OPEN`, never `FIXED`.** You applied this to your own `GUIDED-211`; keep doing it.
- **Never accept a moved threshold in the same loop as the change that pressured it**, unless
  §06.2 is invoked in those words. `GUIDED-189` is the one place it may be.
- **The shelf is never shortened.** Part C will tempt you to delete a claim rather than correct it.
  **A sentence that is wrong gets corrected, not removed**, unless nothing true can be said — and
  then the silence is stated.
- **Domain science from `docs/turbotab/research/`, by section, never from recollection.**
- **Say the number.**

## Deliberately unbuilt

- **Fixing anything Part E finds.** E files; C and L52 fix. That boundary is what makes running both
  safe in one loop.
- **The checklist engine** — it reads the manuscript, and Part C changes what the manuscript says.
  **That dependency is why it is L52 and not this loop.**
- **`GUIDED-178`'s actual deck** — blocked pre-seal, and B3 builds the honest placeholder only.
- **`GUIDED-105`, `GUIDED-106`, `GUIDED-118`** — the missing model families, and time-to-event is
  L53.
- **`TEST-049`'s instrument half**, `GUIDED-073`, `GUIDED-155`'s Propagate half, `GUIDED-120`,
  anything at Train, and the standing unchanged list from L50.

## Scope note

**B is the loop.** It is the last of the product owner's drive and it has been deferred three times.
If everything else goes, B ships.

**A is small and not tradeable** — A1 is a guarantee that is currently false in three documents.

**C is wide and expected to be partial.** Take the manuscript nine, then as far as you get. **Report
the count you disposed of and the count you did not** — eleven of twenty-one was accepted at L44 and
this is the same shape.

**D is small and non-tradeable.** It exists so the tail stops growing.

**E is last and it is the tradeable one.** It runs after C, it files rather than fixes, and it is
expected to be partial. **If E gets one registry done, that is one registry done** — say which, and
say what the denominator was.

**If you finish everything**, do not start the checklist engine — write what its first checklist
would contain and stop.

## What will be checked

- **Whether a bare `[verify-at-build]` marker now fails the gate**, and whether the eight got their
  numbers.
- **Whether a single-cell matrix is refused before it is classified**, driven or `xfail`-named.
- **Whether B is one panel or two.** Two panels is the design read as competitors, which the row
  says is the mistake.
- **Whether B3 built a placeholder or invented a deck.** `/recipes.models` is `{}` pre-seal; a deck
  with content there would be fabricated.
- **Whether any Part C row was closed by deleting the claim** rather than correcting it.
- **Whether `AUDIT-031`/`032`'s xfails are still red**, or went with a recorded reason.
- **Whether `GUIDED-169` and `GUIDED-172` were parked with a date**, not silently deferred again.
- **Whether Part E fixed anything.** It should not have. A fix inside E is a defect disposed of
  without the scrutiny C gives one.
- **The registry denominator, counted rather than quoted.** ~150 has been carried in
  `DOMAIN_SCIENCE.md` §03b for a dozen loops and nobody has counted it.
- **`turbotab/`, measured on a quiet machine**, with `ps` checked.

Report once, in `AGENT_ONBOARD.md` §10's shape. **Your divergence section is the first thing I read.
It has corrected me three loops running, and last loop it stopped a prompt instruction from
degrading the badge system.** Write it that way again.
