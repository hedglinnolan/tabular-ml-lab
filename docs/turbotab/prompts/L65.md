# L65 — the count says what it counted, the reconciliation gets a comparand, and the hook proves it can run

**You are the execution agent for TurboTab and your predecessor was cleared. Read
`docs/turbotab/prompts/AGENT_ONBOARD.md` in full before you touch anything.** It is the timeless half of
your onboard — the role, the rules, the tools and the traps this project has already paid for.
Everything in it binds. This prompt is the other half: it carries the state, the corrected premises and
the four parts.

**Branch `TurboTab`. Ledger 989 findings, 434 closed. Six pre-commit gates green. No loop running.**
**Read HEAD off the machine** — the commit that adds this file moves it:

```bash
git log --oneline -1 ; git status --short ; ps aux | grep "[p]ytest"
git config core.hooksPath .githooks     # first command in a fresh clone; git does not version hooks
```

**Every premise below was derived at `2761ab8` by an agent that opened the file, and each load-bearing
one was then adversarially re-checked by a different agent that was told to break it.** Two of the
adjudicator's own claims were destroyed that way and are corrected in §00. Where a line number is
quoted, that is where it was.

---

## §00 · Seven things you would otherwise inherit as true, and are not

### 1 · The denominator is a PAIR, not a number, and both published values are wrong

`MISC-029`'s note and `LOOP.md` §03's L64 row say **eight of thirteen** validator checks are *"decided
before the manuscript is read"* and that *"the honest Guided denominator is 5."* **Neither survives.**

**`turbotab/manuscript.py::_counts` produces TWO Guided contexts, both carrying exactly the same three
keys, so any sentence beginning *"the Guided context"* selects neither.** Driven, holding the context
**and** `task_type` fixed and varying only the manuscript bundle:

| branch | reached when | pinned | live |
|---|---|---:|---:|
| **run** (`manuscript.py:240-245`) | a run is held in `_RUNS` | **3** — #2, #5, #9, all at PASS | 10 |
| **no-run / lockbox** (`:267-269`) | `turbotab/api.py:2773` finds no run | **4** — #5, #6, #9 at PASS and **#2 at FAIL** | 9 |

**The no-run branch is the find, and it is the governing rule failing in the direction nobody was
looking.** That branch writes no `train_n` and no `val_n`, so `int(population.get(key) or 0)` makes the
split sum `test_n` alone — 14 against an `analysis_total` of 72. **Check #2 is pinned at FAIL: 4,000
randomized bundles returned the verdict set `{'FAIL'}` and nothing else.** So an unfitted project is
shown a validation failure that describes no real manuscript defect and that **no edit the user can make
will ever clear.** The production path was driven — `MS.validate` via `turbotab/api.py:2827` — and returns
**3 FAIL on both fixtures with `fit=False`**, "Split counts reconcile to analysis population" among them.
**File it. It is not `MISC-028` and it is not `MISC-029`; it is those two rows' shared cause seen from the
other side.**

**And #6 joins the pinned set there** — `included_models` is `[]`, so the loop at
`ml/manuscript_validator.py:257` never executes and `missing_models` is always empty.

### 2 · The "five" was never a measurement. It is L64's own complement.

**Do not treat 8, 5 and 3 as three instruments disagreeing.** `MISC-029`'s note contains both *"5 FROZEN
PASS"* and *"the honest Guided denominator is 5"* — the same digit standing for a sub-category in one
sentence and for `13 − 8` in the next. **Any re-derivation of "five" is reading L64 back to itself.** The
disagreement is two-way and it is 8 against 3.

**L64's eight is also internally inconsistent**, and this is why no scalar is publishable: it froze #12
*because its only producers are the Classic door* — yet #10, #11 and #13 sit in exactly that position on
the same measurement and were filed as *genuinely computed*. **Applied uniformly, L64's own rule returns
11 or 12, not 8.** The only rule-reproducible quantity is *what no bundle can move*, and even that is two
numbers because there are two branches. **Publish the pair with its branch named. Do not publish a
fourth scalar** — L64's refusal to do exactly that was upheld and the epistemology endorsed.

### 3 · The L64-B shape does NOT transfer, and this correction is the adjudicator's own

`PM_TRANSITION.md` §10 prescribes *"the shape L64-B already shipped one registry over: a scored/declared
field on the validation check"* — a `scored_when` **predicate on the payload**. **Measured: it does not
work here.** Run as a payload predicate on a clean fitted Guided draft, **only 5 of the 8 come out
declared**, giving *"13 checks, 5 unmet"*. The three that refuse — analysis population, split counts,
final predictor count — **have their inputs present**; they are vacuous for a structural reason
(`analysis_total` is defined as the sum it is checked against; #1 and #3 compare two prose statements
rendered from the same counts object) that **no predicate over the payload can see.**

**So the declaration is structural, and where it is derivable it is derivable from the CONTEXT rather
than from the payload** — #5 and #9 are exactly *"this key is absent"*, and #6 is *"this list is empty"*.
**#2 is not derivable at all, and Part C is why: it should not be declared, it should be repaired.**

> **This is the L64 error arriving one loop later, and the adjudicator made it again.** A correct
> measurement of one registry became a prescription for another without anyone asking whether the
> mechanism transferred. **It did not.** If any part of this prompt prescribes a mechanism that your own
> measurement contradicts, the measurement wins and the divergence section is where you say so.

### 4 · Classic is HEALTHIER than Guided, and the two doors are not one defect

`MISC-029`'s first correction says the same 13 feed a Classic count that gates downloads, implying the
same inflation. **They feed it; it is not inflated.** `pages/10_Report_Export.py::_build_manuscript_context`
(`:409-510`, the sole producer, called unconditionally at `:1916`) **writes all four keys whose absence
freezes checks on the Guided path**: `feature_names_for_manuscript` `:486`, `feature_counts["original"]`
`:487`, `manuscript_primary_model` `:488`, `best_metric_name` `:490`.

**Driven with identical needles: Classic 13/13 checks dissent; the exact dict `_counts` returns lets only
11/13.** **Do not give the two surfaces the same treatment.**

**Two further corrections to the Classic claim, both driven:**

- **The gate is leaky and partly cosmetic.** Of the page's 13 `st.download_button` calls, **7 consume
  `exports_blocked`** (including the primary ZIP at `:2591`), **1 is always disabled, and 5 are never
  gated at all** — TRIPOD `:2021`, Table 1 CSV `:2055`, Table 1 LaTeX `:2060`, Evidence Map `:2211`,
  Mermaid `:2260`. And Streamlit 1.60's `_download_button` calls `marshall_file` **before** applying
  `disabled`, so the ZIP is assembled and **a live download URL reaches the browser even when blocked.**
- **The `All 13 validation checks passed.` assertion at `:2084` renders inside a COLLAPSED expander** —
  `:2082` sets `expanded=not validation_report.passed`, and the success branch fires only when `passed`
  is True. It is far less visible than the Guided header. **File both; neither is this loop's build.**

### 5 · `ledger.py set --ev` ALREADY WORKS

Driven in a sandboxed copy: it is an argparse **abbreviation of `--evidence`** and it writes
**`verified_ev`**. **Adding a literal `--ev` option silently flips the destination of a flag that works
today** — same spelling, different field, no error and no warning. Part D rules on it.

### 6 · `TEST-109`'s own closing note is false

It says *"the schema refuses the area TOOL while TOOL-001 exists."* `TOOL` is not in `AREA_NAME` — but
neither is **`DRIVE`, and ten committed rows carry it** (`DRIVE-001`…`010`), which `add` rejects today.
And `ledger.py check` — the half the pre-commit gate runs — **does not validate `area` at all**, so the
two halves of the schema disagree. **Correct the row; do not repeat its claim.**

### 7 · The figure population is 87, and L64-B's badge reaches nobody

**87 checklist items across 17 specs — not 86** — re-derived two ways that agree (a static AST walk of
`figure_specs.py` and a runtime walk of `turbotab.figures.REGISTRY`): **9 converted, 78 on the default.**
`test_a_checklist_item_says_whether_it_was_scored.py:98` already asserts `== 87` while its own docstring
and a `capsys` print still say 86. **Do not "correct" the assertion to match the prose.**

**And the badge does not reach a user.** Driven end to end on `clinical_risk.csv`: a real bundle serves
**28 checklist rows — 27 PASS, 1 FAIL, 0 DECLARED.** Eight of the nine converted items sit on the four
`*_instability` specs, which have **no entry in `figure_bundle.SOURCES`** and can never be drawn; the
ninth is gated behind an unanswered lens question. **The mechanism is proven in unit tests and unproven
in service.** That is the trap Part B must not repeat.

---

## Guardrails

> **Hard rules.** Stay on branch `TurboTab`. Never push to `main`, never force-push, never open a pull
> request. `ml/import_doctor.py`, `ml/join_doctor.py`, `utils/combine*.py` and
> `pages/01_Upload_and_Audit.py` are **frozen — `TRANSITION_PLAN.md` §05** is the one statement of what
> that permits; do not restate it from memory. **`pages/10_Report_Export.py` is NOT frozen.** Never edit
> `FINDINGS_LEDGER.md` by hand. Never mark a finding `FIXED` without a regression test **verified to fail
> when the fix is reverted, for the stated reason**. Commit after every part. **Domain science comes from
> `docs/turbotab/research/`, never from recollection.** Blocked or something looks structurally wrong →
> `docs/turbotab/BLOCKED.md`, do not guess.

**You are the only writer** to `findings.json`, `register.json` and their generated markdown.

**File every ledger note through a `.py` file, never a shell heredoc** — zsh eats backticks, and it eats
an unquoted `--include=*.py`. **`ledger.py set` REPLACES the note**; read the existing one and append.
**`set` refuses a `FIXED` status without `--test`**, so pass the existing value back. **`findings.json`
serializes at `indent=1`** — it is 13,082 lines and an `indent=2` dump rewrites 13,080 of them.

**Never `git add -A`.** Stage explicit paths and check the diffstat.

**Two environment facts, measured.** `torch` is **not installed**, so any test that would fit `nn` must
skip and **say so**. And `tests/test_engine_is_headless.py::test_core_modules_import_for_any_reason` is
**red at HEAD for that same reason** — unrelated to every part below. **Do not chase it.**

---

## Part A · `TEST-108` — the hook proves it can run, and the probe has a hole

**First, because it is the previous loop's unpaid debt and it is small.** L64-D2 fixed the hook; the
disposition was downgraded to `PARTIAL` because **the row's named test cannot go red for the fix in any
state.** It skips at `tests/test_a_documented_command_still_works.py:202` because `venv/bin/python` does
not exist in a linked worktree — and, stronger than the row says, **that file never mentions `.githooks`
at all** across its 276 lines.

### A1 · The assertion the row is owed, and the shape that survives the guards

**Driven at HEAD, so the target state is known to exist:** inside a detached worktree the hook prints
**six ticks, zero crosses, exit 0, in 3.8 s**; under a bare `python3` it prints **three ticks and three
crosses**, all three attributable to a missing module.

**Two things make the naive assertion wrong:**

- **`GATES CANNOT RUN` still exits 1.** It changes the message, not the blocking. **Assert on stdout,
  never on the return code.**
- **The `python parses` gate is interpreter-*version* dependent** — under `/usr/bin/python3` (3.9.6) it
  reports a false failure on an f-string. So the assertion is ***"no cross attributable to a missing
  module"***, which is what the row asks for. Not *"exit 0"*, and not *"six ticks"*.

**Do NOT go through `docs/turbotab/tools/worktree.py`.** Three reasons, all driven: `add` uses
`git worktree add -B wt-<name>`, which **force-writes a shared branch ref**, while
`tests/test_a_fixed_row_names_a_test_that_actually_runs.py:157` runs named nodes under `-n auto`, so two
concurrent runs collide; `add` hard-fails if `.worktrees/<name>` survives a crashed run, wedging the test
until someone cleans it by hand; and it plants a nested checkout inside the repo. **Use
`git worktree add --detach` straight into `tmp_path`, and remove it in a `finally`.**

### A2 · The trap, and it is not the one you would guess

`.worktrees/` is **not** the hazard — `tests/repo_write_guard.py:412-413` already excludes it and
`.gitignore:87` covers it. **The hazard is `UNRESOLVED_CEILING = 32` at
`tests/test_no_test_writes_a_path_git_tracks.py:61`, and the live corpus sits at exactly 32.** Zero
headroom: one new unresolvable write destination turns that guard red. It **follows a subprocess into an
in-repo script it spawns**, which is why it can see this at all.

**Driven, the shapes that are safe and the shapes that are not:**

- **SAFE** — bind `wt = tmp_path / "wt"` **first**, then pass `str(wt)` in `argv`. Zero unresolved.
- **UNSAFE** — compose `str(tmp_path / "wt")` **inline in `argv`**. Adds one unresolved; the guard fails.
- **UNSAFE** — `.mkdir()` on a `tmp_path`-derived name. Adds one unresolved. *(`git worktree add` creates
  the directory itself, so no `mkdir` is needed.)*

**And the tree-wide-git rule does not prohibit this.** `AGENT_ONBOARD.md` §06 enumerates `stash`,
`checkout`, `clean`, `reset`, `restore`. A `--detach` add writes only `.git/worktrees/<name>/` and the new
directory — verified: `git status --porcelain` empty and HEAD unchanged after an add-and-remove cycle.

### A3 · The hole the driving found, which is a real defect in L64's own fix

`gates_can_run` at `.githooks/lib.sh:86-93` probes exactly **two** modules, `("pandas", "pytest")`. **The
`evidence badges` gate also needs `fastapi`** — `evidence.py:302` does
`importlib.import_module(f"turbotab.{path.stem}")`, reaching `turbotab/api.py:29`'s `from fastapi import
FastAPI`. **So an interpreter carrying pandas and pytest but not fastapi passes the probe and then
produces a cross attributable to a missing module — the exact false red the third state was built to
prevent.** Driven with an import blocker: the probe said *gates CAN run*, then the hook printed five ticks
and `✗ evidence badges … No module named 'fastapi'`.

**One word at `lib.sh:90`, and it needs its own ledger row.** The class is *the probe enumerates a subset
of what the gates import*, and naming only today's third module is how this recurs. **Note in the row that
neither environment this repo builds can reach it** — `venv` has all three and `turbotab/.venv` has pandas,
pytest and fastapi — **so it is latent rather than live. Say that rather than dressing it up.**

---

## Part B · `MISC-029` — the count says what it counted

**The substantial build.** A user is shown **"13 checks, 0 unmet"** over a set in which three cannot
dissent, and beside it the page asserts **"Every consistency check the validator makes is met by this
draft."**

### B1 · State the harm precisely, because the obvious statement of it is wrong

**The sentence is not literally false, and if you attack it as a falsehood you will fix the wrong thing.**
It renders only when nothing failed (`turbotab/web/index.html:3891-3894`, gated on `!failed.length` and
both promoted-figure lists being empty) — and when nothing failed, every check the validator made did
pass. **The false assertion is the number.** `index.html:3884-3885` renders
`MANU.rows.length + " checks, " + failed.length + " unmet"`, and **"13 checks" asserts thirteen units of
scrutiny where ten were applied and three were decided before the draft existed.** The sentence then
ratifies it. **The claim to repair is the denominator, not the sentence.**

### B2 · The mechanism, and the two ways it is NOT the figure registry's

**Express the distinction as new ROW FIELDS — a `scored` boolean and a `declared_because` string — and
leave `status` strictly two-valued.** Measured, both halves:

| shape | committed tests red |
|---|---:|
| new `scored` / `declared_because` row fields, `status` untouched | **0** — 132 tests across 11 files driven |
| a third value on `status` | **6** across 3 files |

**Why a third status value is wrong, driven end to end on a clean fitted draft:**
`ml/manuscript_validator.py:39` collapses **any** non-`PASS` status to the literal `"FAIL"` in
`to_rows()`, while `failed_checks` at `:30` keys on `status == "FAIL"` and **excludes** a third value. The
result is **13 rows carrying 8 FAIL beside `n_failed: 0` and `passed: True`**, and a header reading
**"13 checks, 8 unmet" on a clean draft.** That is trap #7 — *the machine-readable form is lossier than
the sentence* — committed inside the fix for it.

**There are FOUR Status-keyed consumers, not two.** Beyond `failed_checks` and `to_rows`:
`_checklist_counts` at `turbotab/manuscript.py:796` serves `n_unmet_checks`, and
`n_failed_for_a_missing_section` at `:905-907` keys the same way. **The contradiction would sit inside one
served object** — `n_failed: 0` beside `n_unmet_checks: 8` — and
`turbotab/test_a_check_that_cannot_run_says_which_quantity_is_missing.py:391` asserts those agree. It is
one of the six reds, and it is the guard working.

**And the declaration is STRUCTURAL, not a payload predicate — §00.3.** Where it is derivable it comes
from the **context**: #5 is *"`feature_names_for_manuscript` is absent"*, #9 is *"`feature_counts['original']`
is absent"*, #6 is *"`included_models` is empty"*. **Each check states its own basis and the app computes
it per render** — so nothing here writes a count down, and the run and no-run branches answer differently
without anyone maintaining two lists. **#2 is deliberately excluded: Part C repairs it instead.**

### B3 · Where it has to reach, and the count that proves it did

1. **`ManuscriptValidationCheck`** (`ml/manuscript_validator.py:12-19`) grows the two fields.
2. **`to_rows()`** carries them without touching `Status`.
3. **`_checklist_counts`** (`turbotab/manuscript.py:770-826`) grows the scored and declared counts. **This
   function already exists and nothing reads it** — its own docstring admits *"this sentence reaches no
   screen yet. That is a stated limit, not a claim of completion."* **Closing that is part of this part;
   it is `GUIDED-179`'s missing consumer.**
4. **`index.html:3884-3885`** stops computing its own denominator client-side and **reads the served
   field**, so the header can read *"10 checks, 0 unmet · 3 declared"* — and the sentence at `:3891-3894`
   is re-scoped to what was actually checked.

**The proving number is not a unit test.** L64-B's badge passed its unit tests and reached nobody (§00.7).
**Drive a real project through to the manuscript panel and report the rendered header string and the count
of declared rows a person actually sees, on BOTH branches — with a run and without one.** **If it is zero,
that is the same failure one loop later; say so plainly rather than ship it.**

### B4 · Two things already true that will save you an hour, and one that will cost you

- **The copy deck carries neither the header string nor the sentence** — confirmed by importing the deck
  and enumerating all 77 hand entries, and `copydeck.py check` is green. **No regen for this part.** Note
  the deck *"catches a string that changed and not one that was added"*, so a new sentence will not trip it.
- **`report-manuscript-validation` already exists in `register.json`** with state `core`. **Update that
  row's description; do not add a second.**
- **The cost:** `pages/10_Report_Export.py:2080` does `pd.DataFrame(validation_report.to_rows())`, so **two
  new columns land in the Classic table with no code change and no test covering them.** Decide that
  deliberately — render them, or select columns explicitly — and **do not let Classic's download gate move
  in this part.** Classic's own count is honest (§00.4) and must stay gated on the same set it gates on today.

---

## Part C · `MISC-028` — the reconciliation gets a comparand it can fail against

**Deliberately a different shape from B, and chosen because the two stress each other.** B *declares* what
cannot be scored. C takes the one check B deliberately refused to declare and **makes it honest on both
branches.** If B's mechanism is right, #2 needs no declaration afterwards — **and if you find yourself
hand-declaring #2 to make B's numbers come out, B's abstraction is wrong and that is the finding.**

### C1 · The row conflates two defects on two branches — and the second is worse than filed

- **The tautology is on the RUN branch.** `turbotab/manuscript.py:240-245` sets
  `analysis_total = train_n + test_n` with `val_n = 0`, and #2 sums those same three. **It cannot fail.**
- **The permanent false alarm is on the NO-RUN branch** (`:267-269`), which writes only `analysis_total`
  and `test_n`. **It cannot pass** (§00.1). **Same check, opposite failure, two branches** — which is why
  a repair aimed at only one of them is not a repair.

**And there is a second, independent producer the row never mentions:**
`pages/10_Report_Export.py:492-498` sets `analysis_total = train_n + val_n + test_n` **literally** and
feeds the same validator at `:2072`. **One consumer, three producers.** Fixing only `turbotab/` leaves the
Classic tautology exactly as it was — **say what you did about it either way.**

### C2 · The comparand, and it needs no plumbing

**The honest comparand is already inside `project_dict` as `lockbox["resolution"]["n"]`.** No threading
through `to_dict()` is required, which matters because `to_dict()` carries no outcome-row count at all.
**Two docstrings already assert this equality while nothing checks it; find them and make one of them
true.**

### C3 · The fixture trap, which is trap #3 and is not optional

**The existing test filters outcome-blank rows out BEFORE building the project**, so `outcome_rows == df`
by construction and a new assertion over that fixture would be **a fixture supplying what production
cannot.** Deleting the filter makes the classification shape falsifiable for free — that fixture has real
blanks — **but the regression shape has none and needs them injected.**

**Assert in the fixture itself that outcome rows differ from all rows**, so a shape supplying no blanks
turns the fixture red rather than passing vacuously. **Cover both target shapes, and name the one you did
not cover if you reach only one.**

---

## Part D · `TEST-109` — the ledger can repair its own citations, and then a count

### D1 · The repair, and the foot-gun in front of it

`cmd_add` at `ledger.py:198` is the sole writer of `ev` repo-wide; `cmd_set` at `:135-160` writes `status`,
`note`, `test` and `verified_ev` and never touches `ev`. The edit is about three lines and **needs no
`check` wiring** — `cmd_check` never looks at `ev` or at `area`.

**But `set --ev` already works as an abbreviation of `--evidence` (§00.5).** **Rule on it explicitly:**
either choose a spelling that cannot collide, or disable abbreviation for that parser and say what you
broke. **Do not add the flag and leave the collision undocumented.**

**Two things the three lines do not cover, and both strengthen the case:**

- **`cmd_regen` renders `ev` but never renders `verified_ev`** (`:287-296`), so every correction filed to
  date through `set --evidence` is **invisible in the generated ledger.**
- **`act` has the identical defect and is already its own open row**, `TEST-079`. Both fields is five or
  six lines in the same two places and closes two rows instead of one.

### D2 · The count, which is the part that is a probe

The row's `act` asks for it: **sweep the closed rows for evidence citations that no longer resolve — the
count is the finding whatever it is, including zero.** The live instance is `GUIDED-245`, whose `ev` cites
`training.py:644-646` where HEAD has comment lines; the real sites are `:648` and `:650`.

**Two warnings, because this project has paid for both.** A citation that *resolves* is not a citation
that is *right* — **say which you measured.** And **a matcher over `ev` strings will fire on prose**:
anchor it, report what you could not parse as its own number, and **do not report a bare zero from a
pattern you have not positively controlled.**

---

## Part E · The sweep

```bash
venv/bin/python -m pytest turbotab/ -q -n 8 --dist loadfile     # L64 measured 41:49
venv/bin/python -m pytest tests/ --ignore=tests/integration \
    --ignore=tests/test_nn_modernization.py \
    --ignore=tests/test_a_fixed_row_names_a_test_that_actually_runs.py -q   # measured 47 s
venv/bin/python -m pytest tests/integration -q                  # measured 53 s
```

**The parallel form is licensed as of L63.** **Run `ps` first and again at twenty minutes.** A suite is
quotable only if nothing else is writing the tree *or competing for the machine*, including you.

**`setsid` does not exist on this host and a PID is not a running process** (`TEST-099`). **Assert the
output** — one `ls` of the log, or a `tail` for a line the run must print. That cost a previous loop three
hours, and Part E is the one thing here you will want to background.

**Baseline to beat: 0 failed · 2,709 passed · 17 skipped · 9 xfailed**, against `turbotab/` collecting
**2,735** at HEAD. Anything else red is yours, except the `torch` one named in the guardrails.

---

## Deliberately not built

- **The Classic download gate's five ungated buttons and its assemble-before-disable behavior** (§00.4).
  **File them; do not fix them here.** Changing what gates a download in the same loop that changes what
  the gate counts is how a gate stops being trustworthy.
- **`GUIDED-238`'s remaining 78 items.** The mechanism exists; **the reason to wait is §00.7** — the nine
  already converted reach nobody, and widening a conversion that does not render builds the same wall
  taller. **Fixing the reach is the next loop's Part A if B lands.**
- **`MISC-030`** — `_why_not`'s fallback for nine figure ids. The count is right; L64-A4 spent the row's
  example, and it needs re-scoping before it is worth a part.
- **`GUIDED-231`** and **`GUIDED-232`** — both large, both blocked on a product decision rather than on
  execution. **`GUIDED-233`**, whose own sequencing puts the faithfulness harness first.
- **Any page renderer for the decision curve.** That is `GUIDED-202`'s.

## Scope note

**If B runs long, ship B1–B3 and file B4's Classic column question** — the Guided surface is the one a
person is looking at. **If C runs long, ship it before D** — C removes a false alarm from the artifact
that leaves the building; D is an instrument repair that loses nothing by waiting. **If E cannot run on an
idle machine, say so and do not quote a timing.**

**A part may be REFUSED when carrying it out would violate the criterion the row itself states.** Standing
permission, right five times now. **Measure, file, and hand the decision back — do not build a weaker
version and do not silently substitute your own.**

## How to report

One report, at the end. What each part did and which you did not reach — **three of four is three parts.**
**The counts your probes owe**: what you enumerated, what passed, what failed, and what you could not
construct. Every finding you made rather than fixed, with its ledger id. **The fixture shapes you did not
cover.** And **where you diverged from this prompt or were unsure** — that section has corrected the
adjudicator eight loops running, it corrected two of his claims before this prompt was even written, and
it is read first.

**No silent caps.** If you bounded something, say what you dropped and why.
