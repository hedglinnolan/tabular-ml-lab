# TurboTab — the L49 loop

**If you are a fresh agent, read [`AGENT_ONBOARD.md`](AGENT_ONBOARD.md) in full first.**

**Five parts, and the first one is the instruments.** L48 closed the drive — eleven of thirteen rows,
both criticals long since gone, `GUIDED-167` closed on its own evidence — and in doing so it found
that **two of this project's own verification instruments answer the wrong question**, and named the
two defects that survived because of it.

**Nothing built through a lying instrument is worth more than the instrument.** That is why Part A
is first and why it does not end at the fix.

**L48 was accepted, five of five.** §00 is the response you are owed, including three protocol
rulings — one of which says **you were right and the doctrine was wrong.**

---

## 00 · Your divergence section, answered

**Four of §03's premises did not survive being driven, and all four are mine.**

1. **`GUIDED-159`'s premise was half wrong and your correction is worse than the claim.** The row
   said Train can never wear `now`. It can — as `reveal()`'s leftover, never on purpose — and one
   render past the seal leaves **six steps wearing `now` at once**, with the last writer decided by
   which fetch resolved last. `DESIGN_LANGUAGE.md` §02 budgets **one accent moment per viewport**.
   That is a better finding than the row.
2. **`GUIDED-164`'s numbers match no shipped fixture.** `n_features: 26 / per_page: 6 / n_pages: 5`
   and *"a fifteen-column finding"* came from the **product owner's own 21,849 × 29 file**, and the
   prompt carried them without saying so — so they read as something you could reproduce here and
   they are not. The defect was as described; the evidence was his, not the repository's.
3. **`GUIDED-176`'s "55 sites, 44 `str(exc)`" was a grep's answer.** Your `ast` re-derivation — 70
   sites, 70 of 70 with a string detail, and `grep -c` missing 14 calls that wrap onto the next line
   — is trap #5 landing in a row that describes a class. I quoted a count I had not derived.
4. **`GUIDED-171`'s field name was wrong.** `needs` names extra parameters and is empty on every
   two-column transform; `n_inputs` counts columns. And `needs` turned out to be its own defect —
   `GUIDED-198`.

**And the correction that lands on your own work is the best thing in the report.** L48-A's gate said
*"posting controls"* and that was too narrow: `data-feat-preview` is a **GET**, its 400 carries a
string detail, and its whole failure path went to `setErr` — the exact defect the gate exists for, on
a control the gate did not look at. **"A press produces a response or it does not; GET versus POST is
not a property of what a user sees"** is the right rule and it should have been the prompt's. 36 of
36 is the honest denominator.

**Two instruments caught you and you reported both.** Your first `data-miss-choose` probe came back
`GREEN — NOT LOAD-BEARING` because the synthetic press supplied the `data-ac` whose absence is the
defect — **trap #3, in your own fixture, in the loop closing a row about exactly that.** The press is
built from the rendered button now, and the test carries the story. And **`GUIDED-182` was yours and
you refuted it**: `actions.offers` runs behind `/finding/<id>/offers` and your L47 check read
`previewable` off the project payload instead. The adjudicator reproduced the refutation
independently — every offer enumerated came back previewable and every preview 200. `NOT-A-DEFECT`
is right, and **removing the L47 test's stated limit rather than leaving it standing** is the part
that makes it a real retraction.

### The three protocol rulings

**1 · A subagent edited `index.html` against instruction. The rule stands, and the rule was not
enforceable.** You kept the change because it was sound and disjoint, which was the right call at the
time. But *"tell subagents not to"* is an instruction a tired agent can skip, and this project has
ruled that such a thing is not a gate. **§02 now makes it structural** rather than remembered.

**2 · A subagent ran `git stash push` on four shared modules while four other agents were writing
that tree. This is the serious one and you surfaced it yourself.** It popped clean and nothing was
lost — **and nothing would have said so if it had not.** Unlike `git add -A`, which makes a commit
assert something false, a stash can **destroy another writer's uncommitted work and leave no
record.**

**Your generalization is the finding and it was in prose, not in the ledger** — so the adjudicator
filed it as **`TEST-049`** (`high`): *the revert harness is not safe under a second writer, and
neither is a stash.* `revertprobe.py` reverts a fix, runs a test and restores, **in the live tree**,
and L48 ran **24 probes while five chunks wrote that tree in parallel**. The hazard is not that a
probe fails — it is that a probe's revert races another agent's edit and goes red **for that agent's
reason**, which reads as a probe passing on a defect that was never load-bearing. Part A carries the
rule half; the instrument half is designed, not rushed.

**3 · You diverged from §02's chunking doctrine and you were right. The doctrine was wrong.** §02
said *commit after each chunk lands*; you ran five chunks in parallel in one tree, so `api.py` held
three chunks' partial work at once and a per-chunk commit **would have asserted something false about
its contents** — which is the deeper rule the chunking instruction was supposed to serve. Committing
the ledger per chunk and the code once, and **saying so**, honored it. §02 is amended below.

---

## 01 · State

Branch `TurboTab`. Ledger **817 findings, 314 closed** — you reported 816/314 and `TEST-049` was
filed by the adjudicator since. Register **181 rows**. Six gates green.

Re-measured by the adjudicator at `23f0036`, **in an isolated worktree** — because last loop the
adjudicator ran a suite against the live tree while your loop was writing it and reported a failure
that was a measurement artifact:

| Suite | Result |
|---|---|
| `tests/integration` | **211 passed** |
| `tests/` and `turbotab/` | you reported 1655 / 4 environmental and 1893 / 0 |

Independently driven and confirmed: `GUIDED-167`'s own evidence — the typed **409** (object detail)
and the complete-case **400** (string detail) both reach `ac-miss-<column>`, by the two different
code paths you named. Ten of thirteen B rows `FIXED`, `GUIDED-166` `PARTIAL`, the two dropped rows
`OPEN` — exactly as reported.

`make test` still aborts at collection on `TEST-038`. **Do not work around it.**

---

## 02 · Fan-out doctrine — amended by what happened

**Where to fan out is unchanged and it worked**: wide on B, freely on reading, never on a part that
is discovering a shape. Three changes, all from L48.

**A · Chunking, corrected.** The old rule said *commit after each chunk*. That assumed serial chunks.
**Parallel chunks share a tree, so a per-chunk code commit is a false claim about its contents.** Two
honest forms, and you pick:

- **Ledger per chunk, code once at the end, and say why** — what you did, and it is correct.
- **Or isolate each chunk in its own `git worktree`**, which makes per-chunk commits honest again and
  composes with parallelism instead of serializing it. **Try this one if the chunk count is high**;
  it is also the shape `TEST-049` wants for the revert probe.

**B · A subagent never writes a shared file, and this is now structural.** Do not rely on the
instruction. **Either** give the subagent its own worktree, **or** give it no write tool at all and
take its diff as text. A subagent that can write `turbotab/web/index.html` will eventually write it.

**C · A subagent never runs a tree-wide git operation.** No `stash`, no `checkout`, no `clean`, no
`reset`, no `restore`. `git add -A` makes a commit lie; these **destroy another writer's work and
leave no record.** This belongs in the same sentence as the `add -A` rule and it is there now.

**Unchanged, and they held all loop:**

1. **`findings.json` and `register.json` have exactly one writer: you, serially, through the tools.**
2. **No subagent commits.**
3. **No subagent runs a full suite** (`TEST-040`).
4. **A subagent that finds something outside its brief reports it and does not act.**

---

# Part A — the instruments answered the wrong question

**First, because everything else in this loop is verified through them.**

**A1 · `TEST-048` (`high`) — `pageharness`'s `document.querySelector` returns `null` for selectors
that are in the page.** Your own words: *this is why a six-way `now` survived every drive.* This is
the project's primary behavior-verification tool and **every negative assertion ever made through it
is provisional until this is fixed.** Fix it, and say which selector shapes were affected.

**A2 · `GUIDED-197` (`high`) — a sweep that reports without gating.** It enumerates `/features`,
`/recipes` and `/preprocess` and **reports** rather than **fails**, which is why `GUIDED-171`
survived. A sweep that cannot go red is a document. Make it a gate, and where a family must be
exempt, **list it by name with a reason** — the pattern `GUIDED-180`'s unclassified state already
establishes.

**A3 · And this is the part that is not the fix.** A repaired instrument **invalidates the clean bills
of health it issued.** L34 is the precedent — *the loop where the harness stopped lying and
immediately found an orphan*, and the orphan had been there for three loops behind the same blind
spot.

**So: re-run what these two blessed, and report what changed.** Specifically the negative
assertions — every *"nothing renders"*, *"no consumer"*, *"zero hits"* that was established through
`querySelector` or through the ungated sweep. **A count of assertions re-checked and a count that
moved.** If nothing moved, say so; that is a real result and it is the one that makes the rest of
this loop trustworthy.

**A4 · `TEST-049`'s rule half only.** Put the tree-wide-git prohibition in `AGENT_ONBOARD.md` §06
beside `git add -A`, with the reason: `add -A` makes a commit assert something false; a stash can
destroy another writer's work and leave no record. **The instrument half — locking or worktree-ing
`revertprobe.py` — is deliberately not this loop.** It needs a design decision about whether probes
serialize or isolate, and rushing it would put a race inside the thing that certifies fixes.

---

# Part B — controls that cannot be satisfied

**Your own recommendation, and it is the right one.** In your words: *L47 made those refusals honest
and L48 made them visible, and the next thing a person deserves is a control that can be satisfied.*

That is the arc completing. Two loops ago six of these controls refused invisibly; now they refuse in
plain sight, which is better and is still not good. **A control that always refuses is a control that
should not be offered in that state** — and the shelf rule decides the form: not removal, but a
control whose precondition is stated, or one the user can actually satisfy.

- **`GUIDED-198` (`high`)** — six of eighteen feature transforms 400 on **every** press, because the
  page never reads `needs`. Note the field-name history: `needs` names extra parameters, `n_inputs`
  counts columns, and the prompt that got this wrong is the one you corrected.
- **`GUIDED-190` (`medium`)** — an offer list composed **without consulting the function that would
  refuse it**, with two more live surfaces. This is `GUIDED-198`'s class one layer up: the offer is
  made before anyone asks whether it can be taken.
- **`GUIDED-189` (`high`)** — a built pull chip opens onto **zero cards**, because the gate that
  raises it and the gate that fills it disagree. **Two thresholds 4× apart, and you deliberately did
  not move them in the loop that pressured them** — correct, and this is the loop where the ruling is
  owed. §06.2's exception is available to you **if you invoke it in those words** and say which
  quantity is gated.

**Order hardest-first**, and judge hardest by which one most tests whether *"state the precondition"*
generalizes.

---

# Part C — a blank's provenance

**Deliberately a different shape from B: B is reachability, C is the record.**

**`GUIDED-191` (`high`)** — the app writes a value into the working table and **files no record that
it did**; `coerce_numeric` is live. **`GUIDED-166` (`PARTIAL`)** — the impossibility pass manufactures
blanks nothing can distinguish, and L47-B2 made that real rather than hypothetical.

**These are one design and it is the same one.** A declaration about *"never asked"* governing *"the
app deleted a corrupt reading"* is opposite correct handling, and the reason is that **a blank has no
provenance**. Give it one, once, and both rows close against it.

`PRODUCT_VISION.md` §04's *the transcript and the manuscript are one object at two levels of
formality* is the standard: a blank the app created is a methods sentence, and a blank that was never
asked is a different one.

**Watch the shape B2 already established** — a machine-readable payload beside the sentence, so a
consumer never has to string-match prose to know what happened.

---

# Part D — the silent truncation audit

**`GUIDED-195` (`medium`)** is a class with named instances: a rendered list with a silent
`slice(0, N)`, **six sites in the page**, and the sharpest is a **permutation-importance table cut to
15**. That last one is not cosmetic — an importance ranking truncated without saying so is a figure
that asserts a top-15 is the whole story.

**Sweep it properly and gate it**, the way `GUIDED-197` should have been: every `slice(` in a render
path either states its bound to the user or is listed by name with a reason. `GUIDED-164`'s fix is
the model — the card announced fifteen and showed three, and the answer was to say so.

**And carry `GUIDED-192` (`high`) with it** — two of the three skippable question families render
**nowhere** in either renderer. That is `GUIDED-156`'s exact shape one family over, and finding it
again immediately after closing it is the argument for making D a gate rather than a sweep.

**Report counts**: sites enumerated, sites that state their bound, sites exempted by name, and — the
one that matters — **what the same lens finds one surface over.**

---

# Part E — the small ones, tradeable

- **`GUIDED-184` (`high`)** — two REVISE-shaped resolve exits render **disabled**. This is
  `GUIDED-183`'s class, which you closed last loop and reported as *bigger than the row*. Same shape,
  two more instances.
- **`GUIDED-186`** — two blockers collide and the exit **contradicts its own message**.
- **`GUIDED-187`** — `exits.is_actionable` states a premise the page stopped holding.
- **`GUIDED-196`** — the boot's terminal `.catch` swallows a controller-wide `ReferenceError`. Note
  `GUIDED-139`: a controller-wide `ReferenceError` once killed every pull affordance in the door and
  nothing said so.
- **`GUIDED-200`** — `var PICKED` declared twice in one function scope.
- **`TEST-047`** — an assertion that cannot distinguish.

**Trade E before you trade B or C.**

---

## Rules that bind

- **A repaired instrument invalidates what it blessed.** Part A3 is the rule and L34 is the
  precedent.
- **A capability ships with its consumer**, or a **failing** test naming the one it lacks.
- **`FIXED` requires a named regression test verified to fail on revert, for the stated reason.**
  Twenty-four probes with four wrong on first attempt is a normal rate and reporting it is the point.
  **And under `TEST-049`, do not run a probe while a subagent is writing the tree.**
- **Never accept a moved threshold in the same loop as the change that pressured it** — `GUIDED-189`
  is the one place §06.2's exception may be invoked, **in those words**.
- **Ambiguity is `OPEN`, never `FIXED`.** Ask per row: does the fix reach the thing the row's own
  evidence describes? That question reopened `GUIDED-167` and it is the right one.
- **The shelf is never shortened.** Part B is *state the precondition*, never *remove the control*.
- **Say the number**, and say whose file it came from — `GUIDED-164`'s numbers were the product
  owner's and the prompt did not say so.

## Deliberately unbuilt

- **The right column — `GUIDED-160`, `GUIDED-174`, `GUIDED-178` — is L50, and this is the third
  deferral, so it is now a commitment rather than a postponement.** It has been deferred behind two
  criticals, then thirteen drive rows, then two lying instruments, and each was the right call at the
  time. **What still blocks its third leg**: `/recipes.models` is `{}` for the whole pre-seal
  journey, so `GUIDED-178`'s deck has no payload until Train. **`GUIDED-160` and `GUIDED-174` do not
  share that block.** If L49 finishes early, **write the design rather than building it.**
- **`TEST-049`'s instrument half** — the rule lands, the lock-or-worktree decision does not.
- **`GUIDED-181`'s twenty-nine missing `EFFECTS` sentences** and the sixteen dated
  `ACTION_CONTRACT` rows — `GUIDED-180` is `PARTIAL` on purpose.
- **`GUIDED-172`, `GUIDED-169`** — dropped twice now; they come back when a loop has room.
- **A fifth motion slot**, `startViewTransition`, FLIP — `GUIDED-073`.
- **`GUIDED-155`'s Propagate half**.
- **Anything at Train**, and the three `PACK_DEFER` train destinations.
- **`is_id_like` / the lens-aware identifier predicate** — `GUIDED-120`.
- **`AUDIT-031`, `AUDIT-032`** and the seven `§A5`/`§B6` rows never applied — xfails stay red.
- **`GUIDED-185`**, **`GUIDED-188`**, **`GUIDED-193`**, **`GUIDED-194`**, **`GUIDED-199`**,
  **`GUIDED-151`**, **`GUIDED-150`**, **`TEST-044`**, **`IMPORT-109`** (frozen), **`GUIDED-147`**,
  **`GUIDED-146`**, **`GUIDED-132`**, **`GUIDED-133`**, **`GUIDED-137`**, **`GUIDED-138`**,
  **`GUIDED-127`**, **`GUIDED-111`**, **`GUIDED-067`**, **`GUIDED-118`**, **`GUIDED-105`**,
  **`GUIDED-106`**, **`GUIDED-096`** — unchanged.

## Scope note

**A is not tradeable and A3 is the half that matters.** Fixing two instruments is a morning; re-running
what they blessed is the loop's real contribution, and it is the part a tired agent drops.

**B is the loop.** It is the arc L47 and L48 built toward.

**C is one design serving two rows** — do not fix them separately.

**D is tradeable in depth**: if it runs long, gate the render paths and list the rest by name.

**E before B or C if either goes long.**

**If you finish everything, write the right column's design and stop.** Do not build it.

## What will be checked

`AGENT_ONBOARD.md` §08's list, plus:

- **Whether A3 happened at all**, and what its counts were. A loop that fixes two instruments and
  re-runs nothing has done the easy half.
- **Whether `GUIDED-198`'s six transforms can now be satisfied**, driven — a control that states a
  precondition it is still impossible to meet has moved the defect rather than fixed it.
- **Whether `GUIDED-189`'s threshold move invoked §06.2 in those words**, or left the thresholds
  alone.
- **Whether C gave the blank one provenance mechanism or two.**
- **Whether any subagent wrote a shared file or ran a tree-wide git operation.** The diffstat and the
  reflog will say.
- **Whether a named defect *class* was filed, or only its instance.** Sixteen rows last loop and one
  class still ended up in prose — `TEST-049`, which the adjudicator filed.

**The loop's gate:** every control the Guided door offers can either be **satisfied** or **states the
precondition it needs**, driven on two fixtures of different target shape — and no negative assertion
in this repository rests on an instrument that returns `null` for elements that exist.

Report once, in `AGENT_ONBOARD.md` §10's shape. **Your divergence section is the first thing I read.
Last loop it corrected four of my premises, refuted one of your own findings, and reported two
protocol breaches nobody would have seen otherwise.** Write it that way again.
