# TurboTab — the L58 loop

**If you are a fresh agent, read [`AGENT_ONBOARD.md`](AGENT_ONBOARD.md) in full first.**

**Four parts, and the loop has been re-sequenced around a human's finding.** L57's Part A is
accepted and committed; **its B, C, D and E are deferred, and §04 says why.** A product-owner demo
found that **no human can fit a model through the Guided door**, and that outranks everything that
was queued.

**Every number below carries how it was derived.** *(re-derived at `d66ce3b`)* means I ran it against
this tree while writing this. *(from the row)* means I am quoting a ledger note and have **not**
re-measured it — **doubt those first.** Four of L56's premises were false and all four were mine.

---

## 00 · What the drive found, and what I measured before writing this

A human drove the app on a 21,849 × 29 NHANES fasting/diet file through the Guided door. The report is
`DRIVE_UX_SURFACING_NHANES.md`; the notes are `DRIVE_NOTES_NHANES_L57.md`. **The interview asks lens,
target, task type and purpose and then jumps straight to Explore** — no grain card, no eligibility
card, no seal. Preprocess prints the gate in its own copy (*"models are chosen after the held-out set
is sealed"*), **Train renders a heading with no shelf and no fit control**, Explain is gated on a
metric that cannot exist, and the manuscript renders its whole TRIPOD scaffold with every app-filled
row reading *"not filled by the app yet."*

**A human at the screen is the only instrument this project has for condition three.** Their report is
primary evidence, not a claim awaiting a probe — `PRODUCT_VISION.md` §06b, and §08.1's probe rule does
not apply to it.

### What I established by running the code, before changing anything

The row asked for a thirty-line drive. I ran it *(all of the following re-derived at `d66ce3b`)*.
Method: an **NHANES-shaped synthetic frame** — SEQN unique per row, `glucose` continuous, six
`imputed_*` bools, 29 columns. The tester's file is not on disk, and the router's grain condition is a
function of the **record**, not the file, so shape is what matters.

1. **The router serves the grain question.** `GET /interview?step=data` returns `state_grain` with
   `status: "asked"` and `seq: "03"` the moment a target exists. `ml/router.py:632` is the emitter and
   its whole condition is `step == "data" and target and "state_grain" not in answered`.
   **The hypothesis in `DRIVE-017` — that the router judges grain settled from the file and skips it —
   is REFUTED.** There is no skip because nothing skipped.
2. **The page renders it nowhere.** Running the real controller under `turbotab/pageharness.py` over
   that served plan, the question appears in **none** of the 135 ids in the markup, across 105k+ chars
   of rendered HTML.
3. **The cause, by controlled counterfactual rather than by reading.** Serve the *same* question twice
   in one plan — once as `state_grain`, once as a twin under a key absent from
   `HANDLED_QUESTION_KEYS`, identical in every other field. **The twin renders with a working answer
   control; the original does not.** `state_lens` is the positive control and renders both.
   `index.html:6434` lists `state_grain` in `HANDLED_QUESTION_KEYS`, whose comment says every key in it
   has *"a dedicated section above"*; `renderAsked` (`:6607`) therefore filters it out. **No such
   section exists.** The card was never built and the list asserts that it was.
4. **`state_eligibility` is the same defect in the same list** (`:6435`), same counterfactual verdict.
   It is served `asked` as soon as grain is answered, and nothing draws it either.
5. **Neither key has an `ANSWERABLE` entry** (`:8573–8612`). **Un-listing alone is a trap**: it would
   render cards whose buttons post nothing, which is `DRIVE-001` rebuilt, and the comment above
   `ANSWERABLE` names that failure in advance.
6. **The seal is not a router question at all** — no key, no plan entry. It is a decision
   (`EFFECTS.seal`, `:8541`) with no control anywhere on the page. **Grain and eligibility are routing;
   the seal is a build.**

**What I deliberately did NOT establish, and you must not inherit as settled:** whether `choose_models`
— the Train shelf, the seventh entry in that list — is a third false claim. It is served `asked` at
`step=preprocess` at every stage **including after a seal drawn through the API**, but the generic
channel only ever renders the **data**-step plan, so my probe says nothing about it. **Part B measures
it.**

### A method note, because two versions of my own probe lied

The first collected an **empty blob** — the harness shim returns nothing for
`querySelectorAll("[id]")` and `document.body.innerHTML` — and reported *every* question as
unrendered. The second put the original's title **inside the twin's title**, so the original matched
inside the twin's own card and read as rendered. Both are *a matcher whose silence means nothing* in a
new costume. **The probes now assert the reader collected >5,000 chars before believing any absence.
Do the same.**

---

# Part A — the door: grain and eligibility

**This is the loop. It is certain plumbing, fully priced, and it is what lets a human reach a model.**
`DRIVE-017`, and `DRIVE-019` is coupled to it.

**A1 · Un-list both keys and make them answerable.** Remove `state_grain` and `state_eligibility` from
`HANDLED_QUESTION_KEYS` and add `ANSWERABLE` entries. Both decisions take the answer in
`payload.answer` *(re-derived at `d66ce3b`, `api.py:564` and `:620`)*:

```
state_grain:       { kind: "set_grain",       field: "answer" }
state_eligibility: { kind: "set_eligibility", field: "answer" }
```

**All four grain options post in one press with no group column** — I checked each against the API and
every one returns 200 *(re-derived at `d66ce3b`)*. So there is no unbuilt option to disclose on grain,
and `one_row_per_person` is the NHANES answer in a single click.

**Eligibility's second option is the exception and it gets the treatment that already exists.** The
options are *"No, the study is about everyone here"* and *"Yes → which column, and what range?"*
*(read from `turbotab/eligibility.py` at `d66ce3b`)*. The first is one press. The second needs a
column-and-range control that is not built, so render it **visibly unbuilt with its reason** — the
`unbuilt` mechanism `choose_selection` already uses at `:8595–8608` for `chosen_subset`, filed as
`DRIVE-011`. **Copy that pattern; do not invent a second one, and do not ship a button that no-ops.**

**A2 · Render the 409, because the engine already refuses correctly and the page must show it.** On a
file where people repeat, answering `one_row_per_person` returns **409** with the evidence and the
exits in the body *(re-derived at `d66ce3b`: `SEQN` has 200 distinct values across 600 rows, about 3
each)*. `api.py:576` says the exits travel **with** the refusal *"so an interface cannot render the
interruption without also rendering its way out."* **Render both.** The harness can drive a refusing
route — `{"__status": 409, "body": {...}}`.

**A3 · The grain card is one of the three TAUGHT questions** (`index.html:8619`,
`turbotab/teaching.py:56`), and `GET /project/{id}/teaching/state_grain` already serves its panel.
**Wire it.** A generic card that drops layer 3 on the hardest question in the sequence is a
regression against `DESIGN_LANGUAGE.md` §10.

**A4 · The group column is an enhancement, not a blocker.** `GET /project/{id}/grain` serves a
`suggestion` with candidate columns and per-column evidence — on the longitudinal fixture it returns
`SEQN` with `rows_per: 3.0` *(re-derived at `d66ce3b`)*. **If A1–A3 land with room, offer it on the
`people_repeat` branch. If not, say so and file it.** `set_grain` accepts `people_repeat` without it.

**Ship A with its consumer.** `LOOP.md` §05. The consumer here is a human reaching the eligibility
question, so **prove it end to end under `pageharness`**: grain served → rendered → pressed → the
POST body is right → eligibility appears → rendered → pressed. Assert *the record changed*, not that
the page contains a string.

---

# Part B — the guard that proves the renderer, and the shelf it will measure

`DRIVE-022`, filed at `d66ce3b`. **This is why the defect survived, and it ships with A.**

`test_the_page_asks_what_the_router_serves.py:147` checks that every served key is *in* one of two
lists. **A key in the wrong list satisfies it.** Its own failure message says *"or to
`HANDLED_QUESTION_KEYS` if a dedicated section already asks it"* — **the `if` is the unchecked half**,
and adding a key to that list is the documented way to make this guard green without building
anything.

**Make the guard prove the renderer.** For every key in `HANDLED_QUESTION_KEYS`, drive the page under
`pageharness` in a state where the router serves that key as `asked`, and assert the question reaches
the DOM. **The twin counterfactual in `DRIVE-017`'s note is a working implementation — reuse the
method, including its positive control.** A key that cannot be driven into an asked state is itself
the finding: **file it, do not skip it.**

**This answers the `choose_models` question as a by-product**, which is why it is Part B and not
Part D. **Report the verdict for all seven keys, including the ones that pass.** If the shelf is a
third false claim, say so in a row and **do not fix it inside this part** — it is Part C's input.

**This is the same shape as `DESIGN_LANGUAGE.md:55`**, which asserted categorical separation in prose
that nothing checked until L57-A shipped a validator. Same remedy: **run the thing, do not read the
claim about it.**

---

# Part C — the seal control

**Gated on A, and it is a build rather than a routing fix.** There is no `state_seal` question, so
nothing can be un-listed into existence.

**Only start C if A and B are both done and probed.** If B finds the Train shelf orphaned, **the shelf
comes before the seal** — a seal with nothing behind it is the same trap one step later.

**The engine side is done and it refuses in the right order** — driven end to end
*(re-derived at `d66ce3b`)*:

| `POST /decision {"kind":"seal"}` | |
|---|---|
| before a target | **400** |
| after target, before grain | **400** — *"The grain question comes before the seal…"* |
| after grain, before eligibility | **400** — *"The eligibility question comes before the seal…"* |
| after both | **200** |

**So the seal needs no new engine work and its preconditions are already legible sentences.** Part A
is what makes them reachable, and those two refusals are the copy the control should be showing while
it is not yet pressable. `EFFECTS.seal` already carries the sentence — *"Draws the held-out rows and
closes them. Sealed once — this cannot be redrawn."* **Sealed once is the whole design; the control
must say so before it is pressed, not after.**

---

# Part D — L57-A's regression evidence, scoped and named as scoped

**The `turbotab/` sweep is owed for L57-A and the full run is not in this loop's critical path.** The
product owner's constraint is stated: *"These full suite tests are simply taking too long… They run
over two hours and occasionally time out."*

**Run the pattern L55-D established**: the `turbotab/` files that actually reach the changed code —
L57-A touched a stylesheet carried byte-for-byte into a test fixture, so that is the prototype/skeleton
set including `test_skeleton.py` — plus everything Parts A–C touch. **Quote it honestly as scoped, with
the file list and the count.** The last full sweep is L56's: **2,464 passed · 17 skipped · 9 xfailed ·
0 failed, 1:59:54 at `07c25c6`** *(from the row)*.

**A scoped run is never reported as a full run.** Say the number, say what it covered, say what it did
not.

---

## Deferred from L57, with the reason

- **Part B, the multi-model ROC overlay (`GUIDED-236`).** Ruled and well-specified — `_risks_or_refuse`
  at `figure_bundle.py:443`, not `predictions_for` — and **it builds a capability no user can reach.**
  That is trap #1, this codebase's oldest habit, and shipping it now would be the adjudicator
  committing it. **`DRIVE-016` travels with it and is deferred with it.** `prompts/L57.md` §00.2 holds
  the whole specification; it does not expire.
- **Part C, `GUIDED-233`'s explainability pack.** Still the long pole and still the shared prerequisite
  for both deferred builds. Deferred on time, not on merit.
- **Part D, the anti-pattern audit**, and **Part E, suite cost.** E1 (`TEST-073`) and E2 (re-measuring
  `AUDIT-040`'s floor, which L56's own fix invalidated) keep their value whenever they run.
- **`GUIDED-238`** — filed from the L57 handback, **not independently re-derived**. Re-derive the
  85/43/16 partition before acting on it.

## Rules that bind

- **A part may be refused when carrying it out would violate the criterion the row itself states.**
  Measure it, file it, hand the decision back, and **do not quietly build a weaker version or your
  own.** `LOOP.md` §02, from your L56-C2. **Handing back beats a half-built part** — your L57 handback
  was right and it is why §00b of that prompt exists.
- **Doubt the numbers marked *(from the row)* first.**
- **A total revert, or the probe proves nothing.** A red quoting a `TypeError`, `ImportError` or
  `HarnessError` is red for the wrong reason; an `AttributeError` that **is** the production defect is
  the right reason.
- **A class goes in the ledger the moment you name it.**
- **A matcher whose silence means nothing is not evidence** — assert your reader read something first.
  Two of my own probes failed this while writing §00.
- **Say the number, including when it is zero.** No silent caps.

## Scope note

**If parts compete they lose from the bottom up.**

- **A wins, and A1+A2 win inside it.** A user reaching the eligibility question in one press is the
  whole point; A3 and A4 are the quality of that door, not its existence.
- **B ships with A.** Without it this recurs, and it is cheap once A exists.
- **C only if A and B are done and probed** — and behind the shelf if B finds the shelf orphaned.
- **D is evidence, not work. Never trade it for scope**, and never report it as more than it is.

## What will be checked

- **Whether a human can now answer grain and reach eligibility**, proven under `pageharness` as a
  driven sequence rather than as a rendered string.
- **Whether eligibility's second option is visibly unbuilt with its reason**, using
  `choose_selection`'s existing mechanism rather than a second one.
- **Whether the 409 renders with its exits**, and whether it was driven with a refusing route.
- **Whether the teaching panel reaches the grain card.**
- **Whether Part B's guard can FAIL** — put `state_grain` back in `HANDLED_QUESTION_KEYS` with no
  renderer and confirm the guard goes red. A coverage check that cannot fail is the thing under test,
  and that is the same standard L57-A's validator was held to.
- **The verdict for all seven `HANDLED_QUESTION_KEYS` entries, including passes**, and whether
  `choose_models` was measured rather than assumed.
- **Whether `DRIVE-019` was CHECKED after A rather than closed by it** — its note says so explicitly.
- **`ps` checked before any suite. Part D quoted as scoped, with its file list.**

Report once, in `AGENT_ONBOARD.md` §10's shape. **Your divergence section is the first thing I read.**
You have corrected the adjudicator's premises in each of the last two loops and refused a part on a
measurement that overturned a number I had put in a row. **§00 of this prompt is mine and it is three
hours old. Check it.**
