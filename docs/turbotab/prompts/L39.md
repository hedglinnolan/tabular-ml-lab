# TurboTab — the L39 loop

Four parts. Fresh agent: read `prompts/L35.md` §00–§02 first, then come back.

**L37 and L38 were both accepted.** L38 completed three of six parts and said so plainly, which
is the right way to report it. This loop finishes what L38 started and closes the gap its own
engine left.

---

## State

Branch `TurboTab`, HEAD `b6c3370`. Ledger **699 findings, 260 closed**, register 149 rows, all
five gates green. **1167** (`turbotab/`) + **1646** (`tests/`) + **211** (integration) passing,
four known environment failures (`torch`, `shap`). `make test` still aborts at collection on
`TEST-038` — `BLOCKED.md`, do not work around it.

```bash
venv/bin/python -m pytest turbotab/ -q
venv/bin/python -m pytest tests/ --ignore=tests/integration \
    --ignore=tests/test_nn_modernization.py -q
venv/bin/python -m pytest tests/integration -q
```

**One ruling from the adjudication you should have:** `GUIDED-104` was correctly *not* upgraded.
Resampling measures how stable a selection is; it does not make the reported model fold-local, so
`train_folds` would still overclaim about the model the user is shown. The prompt was wrong and
the divergence was right.

---

# Part A — the gap: what the model and the figure are fed

Two `high` rows, both about something reaching a computation that should not, and both in code
L38 shipped.

**A1 · `GUIDED-114` — the bootstrap draws rows on a grouped table, and says nothing.**

The row is filed and its own note is exact: on a longitudinal table a row bootstrap draws the same
person into a resample several times, the refits look more agreeable than they are, and **the
instability plot understates the spread — a figure that errs toward reassurance.**

**Driven by the adjudicator, and this is the part that makes it Part A**: on
`clinical_longitudinal.csv` with `grain.group_col = subject_id` and the seal drawn, the
instability payload is **141,126 characters across eighteen keys and contains the words *group*,
*cluster*, *subject*, *person*, *understate* and *repeated* zero times.**

That fails the precedent this project has already set twice and accepted twice. `GUIDED-089`: the
trainer could not apply the recorded plan, so **every run said so in its own notes**, and that
disclosure is what made the gap acceptable rather than silent. L38's own divergence discipline
does the same thing per model. The instability engine does neither.

**So: either draw clusters, or say you did not — on every grouped project, in the payload and on
the page.** The row itself says the fix is not a name list, because `grain.group_col` already
holds the answer; it is a second sampling scheme with real edge cases (unequal cluster sizes, a
group entirely in the holdout). Both outcomes are acceptable. **Silence is not.**

**A2 · `GUIDED-108` — a per-row identifier is handed to the model as a candidate predictor.**

Found at L37 while rejecting a trigger, and the measurement is the argument: `respondent_id` is
**299 of `survey_instrument.csv`'s 344 parameters.** An identifier can separate the outcome
perfectly and explains nothing, and it inflates every parameter count the app reasons with —
including the one `GUIDED-102`'s card would have used had the third trigger shipped.

The app already knows: `ml/dataset_profile.py` computes `id_like_features`. This is `AUDIT-008`
again — the capability exists and the path that needs it does not read it. **The shelf is never
shortened**, so the answer is not silent removal: an id-like column is offered with the concern
stated, or excluded with a sentence the user can overturn.

---

# Part B — finish the manuscript

**`GUIDED-107` is `PARTIAL` and this closes it.** Four rows, and the first is the one with a
correctness consequence in the artifact that leaves the building.

**B1 · `GUIDED-116` — there is no Model Development and no Model Evaluation section.** The
validator found it: the methods prose states no analysis population, reconciles no split counts,
and states **no final predictor count while the abstract asserts one.** That is `AUDIT-001`'s exact
class in an exported document. The row also names the cause and the fix, so do not re-derive them:
`draft.py` is a pure fold over *decisions* and both missing sections describe a *run*, so the fix
is **a second source, not a new decision kind** — the run already holds the model, the metrics,
the plan and the divergences, and `manuscript.structure` already takes it as an argument.

**B2 · `GUIDED-117` — four of the six rendered sections are extracted by nothing.** A heading is an
interface here: a renamed one silently disables a check rather than failing it. Make the extraction
contract explicit enough that a rename breaks a test.

**B3 · `GUIDED-119` — promotion has no page control.** It is recorded, served and checked, and no
control records one. It ships with an `xfail(strict)` test naming the gap, which is `LOOP.md` §05
working exactly as intended — now close it. **Per the product owner's ruling in
`PRODUCT_VISION.md`: promoted as the author marked it, no tier annotation in the caption, and the
validator is where a promoted `EXPLORATORY` figure is reported.** Read that subsection; do not
re-derive it.

**B4 · `GUIDED-115` — the LaTeX renderer**, deferred at L38 with the cost named: those validator
checks are inert, not passing. `ml/latex_report.py` is 1,066 lines, detainted, imports headless,
and is the exporter Classic uses. *One core, no forks.*

---

# Part C — the figures L38 did not reach

Seven, and the machinery has now carried seven figures without bending, so this is fill-out.
**Hardest-first**, per `LOOP.md` §02.

- **C1 · Decision curve analysis** (`CLINICAL_SURVEY_PACK.md` §A4.5, marked ★) — the clinical-utility
  figure. Needs predicted risks and a threshold sweep, both of which exist now.
- **C2 · The decision-curve and classification instability plots** §A4.8 specified and L38
  deferred, because the decision curve did not exist. It does after C1.
- **C3 · Forest plot** (§A4.7) — **log-scale x-axis for ratio measures is non-negotiable**, and it
  is labeled *"model coefficients,"* not *"risk factors."* Both are in the spec.
- **C4 · ROC curve** (§A4.4) — easiest, and the pack ranks calibration above discrimination
  (§A5.1, §A5.3), so its caption and companions must not present it as the headline.
- **C5–C8 · The survey four**: scree plot with parallel analysis and factor loadings (§B5.5, the
  one with a real method choice, so first), inter-item correlation matrix (§B5.4), floor and
  ceiling effects (§B5.3), item-response distribution panel (§B5.2).

**Every one must reach a user**, per §05. A figure registered and unreachable is `GUIDED-058`
again and this project has paid for that twice.

---

# Part D — the probe

`GUIDED-113` says the instability engine plots one class's probability on a multiclass target and
**does not refuse.** Multiclass is also the shape three separate parts have now named as uncovered
— L37's resolution trigger is calibrated for binary because chance is 1/k, L38's Parts B and C both
list it, and `GUIDED-113` is the defect version of the same gap.

**So the probe is multiclass, end to end.** Drive a three-class outcome from upload to manuscript
and report, per surface, whether it is correct, refuses, or is silently wrong. The last category is
the finding. Count the surfaces you checked, not just the ones that failed.

There is no multiclass fixture. **Making one is part of the probe** — and per `GUIDED-097`'s rule,
it is what lets the fixture shapes stop being named as uncovered in six files.

---

## Rules that bind

- **`GUIDED-097`'s fixture rule** on every part: two fixtures of different target shape, and name
  the shapes not covered, in the file.
- **§05's capability rule**: ships with its consumer, or with a failing test naming the one it
  lacks — `GUIDED-119`'s `xfail(strict)` is the model.
- **Say the number.** Any threshold introduced ships stated in the surface that uses it.
- **`[verify-at-build]` numbers may not ship as constants.**

## Deliberately unbuilt

- **`GUIDED-096`** — post-seal Explore reading the held-out rows. Still with the product owner.
- **`GUIDED-118`** — the time-to-event target type. L38 refused Kaplan–Meier correctly; that
  refusal stands and this loop does not lift it.
- **`GUIDED-105`** — the inference model families. A dependency decision first. Part C will make
  you want a survival model; do not add one.
- **`GUIDED-106`** — subgroup analysis. Specified, ruled, not this loop.
- The checklist engine (L10), the DRI tables (`GUIDED-067`), `research/INTERACTION_PACK.md`, any
  client-stack change.

## Scope note

**Part A is not tradeable.** It is two `high` rows in code that shipped last loop, and one of them
makes a figure read as more reassuring than the data supports.

If Part B runs long, ship `GUIDED-116` and `117` and defer the LaTeX renderer again — **but say so,
and say again that the checks it would exercise are inert rather than passing.** A deferral stated
twice is honest; a deferral stated once and then forgotten is how a gap becomes invisible.

If Part C runs long, trade the survey four before the clinical four. If Part D runs long, build the
multiclass fixture and report the surface count even if you cannot fix what you find — **the
inventory is the deliverable there, not the repairs.**

## The checks that will be run on your report

Counts, re-counted. The load-bearing claim **driven**, not verified through the API.
A revert probe on each `FIXED`, required to fail *for the stated reason* — and L38 set the bar
here: a probe that passes when it should not is itself the finding. Whether a named defect **class**
was filed or only its instance. Whether a threshold moved in the same loop as the change that
pressured it.

Report once: what each part did, Part D's surface counts, every finding you made rather than fixed,
the fixture shapes you did not cover, and **anything you were unsure about or diverged on** — that
line has now been right against the prompt twice in a row.
