# L63 — the figures say which model they are about, the shelf says which rows it ranked, and the sweep gets cheap

Read `docs/turbotab/prompts/AGENT_ONBOARD.md` in full before you touch anything. Everything in it binds.

**Branch `TurboTab`. Ledger 974 findings, 415 closed. Six pre-commit gates green. No loop running.**
**Read HEAD off the machine rather than off this page** — the commit that adds this file moves it, and
a prompt asserting its own sha is a claim that is false the moment it is written:

```bash
git log --oneline -1 ; git status --short ; ps aux | grep "[p]ytest"
```

**Every premise below was derived at `bf7e148`.** Where one names a line number, that is where it was;
if the file has moved under you, say so in your report — that is a finding, not noise.

---

## §00 · Read this first: the paperwork that precedes you is wrong in four places, and I checked

Every premise below was re-derived at `bf7e148` by an independent reader and then adversarially
verified by a second one. **All six subjects came back `NEEDS_CORRECTION`.** Six of the corrections
are to rows *I* or my predecessor wrote, and two of them would have sent you to the wrong function.

**Where a ledger row and this prompt disagree, this prompt wins, and the row now says so.** Read the
amended notes — they carry the measurement.

**Four things you would otherwise inherit as true and which are false:**

1. **`GUIDED-236`'s `act` field tells you to split `predictions_for`. Do not.** It was retracted at
   `L57.md:66` — *"But I told you to split `predictions_for` and that is the wrong function."* The
   change is at `_risks_or_refuse`. Splitting `predictions_for` breaks calibration, whose producer
   takes a probability *vector*, and un-admits the ROC, which gates on `has_predictions`.
2. **Every line number in `L57.md`'s site table is stale — seven of eight.** It was written against
   `ceaffa7`. `L58.md:268` repeats one of them and `DRIVE-016`'s note carries three more. The claims
   survive; the numbers do not. **Do not copy a line number out of prior paperwork.** The ones in
   *this* file were re-derived at `bf7e148`.
3. **`TEST-098`'s false green is withdrawn.** It was the entire reason the xdist adoption was held.
   Four probes across four document states could not reach it: every cross-file reader is green under
   the corrupted window, because the guard's held-out numbers are duplicated in a second pack file.
   **My predecessor asserted a false green it had not driven.** What survives is vector (a), and it
   is measured.
4. **`L55-C`'s "the ROC has no renderer — 2 `<svg>` in 8,479 lines" is superseded.** The file is
   9,407 lines, `figROC` loops every curve, assigns a hue per series, and emits dash patterns in
   journal view. **There is no page work in Part B.** Driven through `pageharness`.

---

## Guardrails

> **Hard rules.** Stay on branch `TurboTab`. Never push to `main`, never force-push, never open a pull
> request. `ml/import_doctor.py`, `ml/join_doctor.py`, `utils/combine*.py` and
> `pages/01_Upload_and_Audit.py` are **frozen — `TRANSITION_PLAN.md` §05 is the one statement** of what
> that permits. Never edit `FINDINGS_LEDGER.md` by hand. Never mark a finding `FIXED` without a
> regression test **verified to fail when the fix is reverted, for the stated reason**. First command
> in a fresh clone: `git config core.hooksPath .githooks`. Commit after every part. **Domain science
> comes from `docs/turbotab/research/`, never from recollection.** If you are blocked or something
> looks structurally wrong, stop and write to `docs/turbotab/BLOCKED.md` rather than guessing.

**You are the only writer** to `findings.json`, `register.json` and their generated markdown.

**File every ledger note through a `.py` file, never a shell heredoc.** zsh eats backticks inside
`<<'EOF'` and inside `-c "…"`. This has damaged three notes across two loops. It also eats an
unquoted `--include=*.py`.

**`ledger.py set` REPLACES the note.** Read the existing one and append.

**Never `git add -A`.** Stage explicit paths, check the diffstat.

**A file a tool owns has exactly one writer and it is the tool.** `ledger.py` serializes at `indent=1`.

---

## Part A · `TEST-098` — the four cross-file writers, and the race that is already live

**This is Part A because it is pure plumbing with no unknowns, and because it pays out on every loop
after it.** `LOOP.md` §02: do not sequence certain plumbing behind unpriced discovery.

**The framing has changed and it is more urgent, not less.** The row held the xdist adoption on a
false-green claim that does not reproduce. But the race does not need the adoption at all:

> `tests/test_a_fixed_row_names_a_test_that_actually_runs.py:143` spawns
> `pytest -n auto --dist load` — **per-test distribution, deliberately not `loadfile`, measured and
> commented at `:128-133`** — over every node a `FIXED` row names. **The writers are in that set**
> (`GUIDED-212` names the bare-marker node; `TEST-084` and `TEST-097` name the build-answered node)
> **and so are their readers.** So the cross-file race is realizable at HEAD, today, whenever that
> guard runs, and it is the cheapest reproduction: no `-n 8` sweep required.

*(That file's own comments say 671 nodes and 880 targets. `tests/fixed_row_guard.resolve()` returns
**1,345** target nodes across **396** `FIXED` rows at HEAD. Correct the comments while you are there,
or say why not.)*

### A1 · There are FOUR writers, not two. Three write a tracked path.

Enumerated by scanning all 370 tracked `.py` files under `turbotab/` and `tests/` with twenty
write-shaped patterns — 97 hits across 47 files. Everything not listed is `tmp_path`, a
`TemporaryDirectory`, an in-memory `df.to_csv(index=False).encode()` upload payload, product code, or
a pandas `.rename(columns=)` false positive.

**Note the methodological trap, because it will bite your own sweep:** a grep for *write calls inside
test files* **cannot find writer 1**. That test does not write — it spawns a subprocess that writes.
The 97-hit sweep returned **zero** for that file. Sweep for `subprocess.run` too.

| # | Writer | What it does |
|---|---|---|
| **1** | `turbotab/test_the_fixture_constants_match_the_fixtures.py::test_running_the_generator_twice_writes_the_same_bytes` (def `:81`) runs `turbotab/sample_data/make_genomics_siblings.py` as a subprocess at `:90` | That generator's `_write()` at `:48-49` truncates and rewrites **six** CSVs and **six** `.md` companions — **twelve tracked files, 1,455,369 bytes**. *(The row said five CSVs. It was six at `d464e0b` too, so the row was wrong at the sweep commit, not merely stale.)* |
| **2** | `turbotab/test_a_bare_verify_at_build_marker_fails_the_gate.py::test_a_bare_marker_fails_the_gate` (def `:119`) | Rewrites `docs/turbotab/research/METABOLOMICS_PACK.md` at `:131`, restores at `:139`. **It already takes a `tmp_path` fixture at `:119` and never uses it.** The isolation was requested and then not used — the fix is half-written. |
| **3** | `turbotab/test_the_app_says_which_build_answered.py:112/:121/:124` | `os.utime` on `turbotab/web/index.html`, which is tracked. Least dangerous: the only cross-file reader of that block, `test_the_app_says_which_interpreter_answered.py:73`, asserts key **absence** and never the mtime-derived value. |
| **4** | `turbotab/test_three_more_figures_reach_a_user.py::test_the_refusal_reaches_the_user_through_the_endpoint` (def `:222`) — **in no ledger row at all** | Creates `turbotab/sample_data/_autoscaled_tmp.csv` inside the tracked fixture directory at `:232-233`, unlinks at `:239`. Measured: visible **0.293 s**, `git status --porcelain` dirty for **0.210 s**, during which the three `glob("*.csv")` readers see **31** fixtures instead of 30. |

### A2 · Vector (a) is the one that matters, and it is measured rather than argued

The generator runs in **0.421 s**. Six concurrent readers doing exactly what the cross-file reader
does — `pd.read_csv(DATA / name)` — logged **56 corrupted observations in 261 reads**: 55
`EmptyDataError: No columns to parse from file`, and **one silent 59-row frame where 60 rows are
committed.** The silent short read is the one to fear. It does not raise; it computes statistics on a
partial matrix.

**Six other files read those fixtures.** Three by name —
`test_the_genomics_data_type_card_reaches_a_person.py` (`:115`, called at fourteen sites),
`test_a_single_cell_matrix_is_refused_before_it_is_classified.py:128-130`,
`test_the_gene_id_diagnostics_reach_an_upload.py:178` — and three by directory glob:
`test_a_deferred_noticing_comes_back_where_it_said.py:94`, `test_a_column_that_names_a_row.py:336`,
`test_the_badge_reaches_the_finding_and_the_refusal.py:110`.

**Vector (b) is real and smaller than the row says.** The bare-marker window is confirmed at
**0.666 s** over 66,925 polls, and its realized consequence is a false **RED** in two other files
(three nodes), confined to a sub-millisecond truncation window. **A false red is the tolerable
direction.** Fix it anyway — it is two lines — but do not restate the false-green claim.

### A3 · The fix shapes, and every assertion survives

Each of the four has a real subject and none may be deleted or weakened.

- **1** — copy `make_genomics_siblings.py` and its source into `tmp_path`, run it there, hash the
  outputs against the **committed** files. The assertion gets **stronger**: today it only compares run
  N against run N+1; the copy version also asserts the committed files are what the generator now
  produces.
- **2** — use the `tmp_path` it already asks for. Watch `docs/turbotab/tools/evidence.py:79`
  (`RESEARCH = ROOT / "docs" / "turbotab" / "research"`, `ROOT` derived from the tool's own location):
  the copy must carry the tool or the tool must accept a root override.
- **3** — `api.py`'s `_SERVED_BUILD` reads the real page path, so `tmp_path` alone will not do it.
  Monkeypatch the path the API consults. **If no seam exists, say so** — this is the one writer where
  an `xdist_group` pin is defensible, and note that pinning changes the dist mode and would invalidate
  the 41:23 measurement.
- **4** — point the drive helper at `tmp_path`. Lowest risk of the four.

**One trap-3c pin sits in writer 3's file.** `TEST-100`: `assert restored["page_newer_than_engine"] is
False` was silently relaxed to `assert isinstance(restored["page_newer_than_engine"], bool)` at
`:133` — **a type check on a field `api.py` always produces via `bool()`, so it can never fail.**
Restoring it goes red unless the restore arm is fixed too. Make the restored state deterministic and
assert against it; do not assert nothing. *(`TEST-100`'s other half — "the repair introduces an
every-run `os.utime` … which never executed during either sweep" — **is false**. The same calls
existed at `d464e0b` at lines 100 and 105. Corrected in the row.)*

### A4 · Do not look for this class with `git status`

**Every one of the four restores.** After each run `git status` is clean and `git diff` is empty — the
generator rewrites byte-identically, the doc restores to a verified checksum, the utime is put back,
the stray CSV is unlinked. **The tree never drifts; only the disk moves, mid-run.** That is why two
full sweeps never saw it. **Look for it with a poller during a run**, not with `git status` after one.

Then **file the class**: *a test that writes a path git tracks*. Write the guard — enumerate tracked
test files, find repo-path writes **including through `subprocess`**, and fail on any that is not
`tmp_path`. Report the count including zero.

---

## Part B · `GUIDED-236` / `GUIDED-243` / `GUIDED-242` — the figures say which model they are about

**The substantial build.** Three figures answer the question *which model is this about?* and all
three answer it wrongly, in three different ways. Ordered **hardest-first**, judging hardest by what is
most likely to bend the abstraction.

### B1 · `GUIDED-243` first, because it is a blocker and it is one line

`figure_specs.roc_payload` creates **one** `rng = np.random.default_rng(42)` at `figure_specs.py:2230`
and consumes it **inside** the per-model loop at `:2233`, drawing at `:2242`. Each model's 200
bootstrap resamples therefore depend on how many models were looped before it.

**Measured at HEAD:** `roc_payload(y, {'A': a})` → A interval `[0.6919, 0.875]`.
`roc_payload(y, {'B': b, 'A': a})` → A interval `[0.6815, 0.882]`. **Same y, same probabilities, same
model, different published interval.** The point estimate is stable; only the interval moves. The dict
order is the user's tick order.

This is unreachable today because the payload holds one curve. **B3 makes it reachable.** So it lands
first, and the guard is: the same model's interval is identical whether it is passed alone, first, or
last.

**This is why "zero changes to the producers" is wrong, and the first reader said exactly that.**

### B2 · `GUIDED-242` — the forest plot, hardest-first because the answer is *not* "widen"

`_coefficients_for` (`figure_bundle.py:510`) loops `run.results` at `:544` and **returns at `:564` on
the first model that exposes `coef_`**. `_forest_payload` (`:493`) hands that to
`figure_specs.forest_payload` (`:2392`), whose payload carries **no model key** and whose caption at
`:2521-2525` reads *"Model coefficients for N predictors, per standard deviation, on a log-scale
axis"* — **and never names the model.** Calibration at least sets `payload["model"]` at
`figure_bundle.py:404`; the forest names nothing.

**Measured on `clinical_risk.csv` with logreg and LDA both fitted, both exposing `coef_`:** ticking
`['logreg','lda']` publishes `age=0.5908`, `albumin_g_dl=-0.3483`, `charlson_index=0.4331`; ticking
`['lda','logreg']` publishes `0.5905`, `-0.3663`, `0.4785`. **Same rows, same data, different published
coefficients, no model named.** Driven: `forest` is admitted in the same bundle as the ROC.

**This is the case that tests the abstraction**, because widening is not the answer — a forest plot of
four overlaid coefficient sets is not a figure. Either **name the model** in payload and caption, or
**refuse** when more than one fitted model exposes coefficients. The governing rule permits silence and
refusal; it does not permit a coefficient table that will not say whose coefficients it is. **Rule it,
build it, and say which you chose and why.**

### B3 · `GUIDED-236` — widen `_risks_or_refuse`, and nothing else

**The defect is one line.** `figure_bundle.py:450` already builds the full list —
`scored = [r for r in run.results if r.probabilities]` — and `:454` throws it away with
`best = scored[0]`. `predictions_for` (`:431`) returns that one model at `:464`, and `_risks_or_refuse`
(`:467`) wraps it at `:480` as `return y_true, {model_name: y_proba}`, a dict of exactly one. Both
`_decision_curve_payload` (`:483`) and `_roc_payload` (`:488`) read it, **so the decision curve has the
same defect as the ROC.**

**Nothing downstream needs building.** `figure_specs.roc_payload` (`:2220`) has always taken
`risks: Dict[str, Any]` and looped it at `:2233`; its docstring already promises *"One curve per model,
overlaid"*. The caption counts them at `:2320-2321`. **And the page already renders multi-series
geometry**: `figROC` at `index.html:4250` loops `names.forEach` at `:4275`, is registered at `:4364`,
assigns a hue per series at `:4288` and a journal dash at `:4290`. Driven with a two-curve payload: app
face renders 2 paths with distinct strokes; journal face renders 2 paths in one ink with one carrying
`stroke-dasharray="6 3"`. **`DESIGN_LANGUAGE.md` §07 is already satisfied for N=2.**

**Leave `predictions_for` alone** — it is correctly named for calibration. **Leave
`figure_bundle.py:192`'s `has_predictions` alone**; it must keep meaning *"something is calibratable"*
and it gates the ROC's own admission at `figure_specs.py:2277`. That is the whole consumer map: three
readers of `predictions_for` (`:192`, `:398`, `:475`), two of `_risks_or_refuse` (`:484`, `:489`).

**The hazard.** `positive_label` is per result (`training.py:646`). Overlaying models binarized against
different events puts pictures of different outcomes on one axis — `GUIDED-093` one level out. **Assert
every scored result agrees on `positive_label`, name anything dropped**, and apply the length check
from `:455` per model rather than once. It is a defensive assertion rather than an observed failure:
failed fits already have `probabilities = None` and are excluded by the `scored` filter, and all four
models agreed on a real drive. Say if you cannot construct a disagreeing fixture.

**A GREEN TEST PINS THIS DEFECT ON PURPOSE AND WILL GO RED. DO NOT CHASE IT.**
`turbotab/test_the_journal_view_is_the_published_figure.py:156` reads
`assert len(served["payload"]["curves"]) == 1,` inside the helper `_overlaid` (def `:126`), whose own
message says: *"GUIDED-236 may be fixed — if so this helper should be deleted and the served payload
used directly, because it is only here to stand in for a bundle that could not overlay models."*
**Do exactly that**: delete `_overlaid`, and at its only caller `:276` pass `FB.render(project)`
straight through. The assertion at `:278` then becomes a real end-to-end check instead of a check on a
hand-composed stand-in — strictly stronger.

**This is the only committed test that goes red**, verified four ways. The other ROC-touching files stay
green **because their fixtures train exactly one model** — not because they bypass `figure_bundle`;
three of them render the real bundle or hit the real `/figures`. **So do not add a second model to any
existing fixture while landing this.**

### B4 · The consequences the widening creates, which must land with it

- **`DRIVE-016` — the series cap, and it is already ruled.** `index.html:4238` holds four colors
  including `--c4`, indexed `[idx % length]`, and `:4237` holds four dashes. **The models page offers
  twelve models and nothing caps the selection** — driven, `n_available: 12`, and four ticked fitted
  four. **A fifth series is byte-identical to the first in both hue and dash.** The ruling exists:
  `turbotab/test_the_categorical_ramp_is_separable_and_legible.py:65` sets `RAMP = ("--c1","--c2","--c3")`
  and `:427-430` asserts `"--c4" not in RAMP` with *"Three is the measured maximum"*. `DRIVE-016`'s own
  `act` says: cap `WEBC` at the three RAMP tokens, give the fourth and later series a dash from the
  existing journal ramp, and **assert it on a four-series payload in DARK mode specifically, since
  light passes and dark does not.** Do that. **Do not extend the ramp** — it would have to re-pass the
  CVD gate at `:318-345` and edit `docs/turbotab/prototypes/` first, which `:288-300` checks
  byte-for-byte.
- **The decision curve's risk rug.** `figure_specs.py:2086-2088` caps at `[:200]` **per model**, so
  widening concatenates every model's risks — driven, **120 values became 480**, with no per-model key.
  Its checklist guard at `:2139` is `lambda p: bool(p.get("risk_rug"))` and stays green over four
  overlaid distributions. **Same trap-3b shape as the item this row is named for, one figure over,
  created by this fix.** Key it per model, or suppress it when N>1, and say which.
- **The checklist item this row is named for.** `figure_specs.py:2317`'s predicate is
  `lambda p: p.get("legend") == "inside"`, reading a key the producer hardcodes at `:2267` — **it
  cannot ever be false.** Driven on a real two-model project through the real routes: the served item
  came back `"passed": true` over a payload holding one curve. Replace it with a predicate that reads
  the data. **No test anywhere asserts this item** — `grep` for `models_overlaid` across `test_*.py`
  returns zero while the identifier does exist in source, so the matcher works and the silence is real.
- **The companion asymmetry.** The ROC declares `companions=("calibration","decision_curve")` at
  `figure_specs.py:2337`, and after the widening it overlays N models while calibration is about **one**
  — driven, calibration's payload model was `'Logistic Regression'` while three models scored. Nothing
  on the wire says the companion covers one of the N, and the companion rule promotes them into the
  manuscript together. **Either the calibration payload states it is one of the N, or the ROC caption
  states which curve the companion covers.** Rule it and build it.

---

## Part C · `DRIVE-050` / `TEST-101` — the shelf says which rows it ranked, and the two tests stop disagreeing

**Deliberately different in shape from B**: B adds a capability, C removes a false sentence and a
contradiction. `LOOP.md` §02 — two builds of different shape expose an abstraction's seams.

### C0 · The three populations, in one paragraph, because getting this backwards makes it worse

`training_rows` (`project.py:2456`, mask at `:2436`) is every row the seal did not take — *which rows
may inform a decision*. `analysis_rows` (`:2494`, mask at `:2461`) is that set narrowed to rows whose
outcome is present — *which rows a model will actually be fitted on*, ground truth `training.py:559`,
`X_train = features[has_y & ~is_test]`. `outcome_rows` (`:2499`) is every outcome-present row
**including the sealed ones** — *which rows the paper describes*, and it is what Table 1 and the
manuscript's `analysis_total` mean. **`analysis_rows ⊆ training_rows`, never the reverse.**

### C1 · `DRIVE-050` — and the people count is wrong too

`recorded_design()` sets `rows = self.training_rows` at `project.py:1895`, counts `n_people` off it at
`:1899`, passes `n_rows=int(len(rows))` at `:1908`. **Thirty-three lines below, at `:1928`**,
`model_shelf_ranked` ranks on `self.analysis_rows` — the L62-A fix, same class, same file. `api.py:2237`
serves the first as `design`; `models.py:371-380` renders *"This order was computed from N rows, which
are M people"* and `:396-397` adds *"the ranking was computed on the row count."*

**Driven on a 900-row fixture (300 people × 3 visits, 600 blank outcomes, 25% seal): the ONE `/models`
payload served `n_rows_seen: 225` and, thirty lines of source away, "This order was computed from 825
rows, which are 275 people."** The correct pair is **225 rows / 75 people**. **The people count is the
one a repeated-measures reader acts on, and no row had named it before now.** It is rendered:
`index.html:5988` reads `SHELF.design` and emits `d.effect`.

**`models.py:371-380` is correct and incomplete. Four more sites carry the same false claim**, and one
is the field's own contract: `models.py:396-397`, `:400-401`, `:61-62`, and — worst — **`:157-159`**,
which states `n_rows`/`n_people` *"describe the rows the shelf was RANKED on … computed on the training
rows"*. **A fix that changes `:1895` and leaves `:157-159` standing leaves the next builder a written
instruction to re-commit the class.**

**Safety, checked:** `n_rows`/`n_people`/`group_column` are read by exactly one consumer,
`models.design_statement` at `models.py:371-377`. Changing `:1895` cannot move the shelf's order.

### C2 · The thing that decides whether C is real: **no committed test goes red**

`test_the_shelf_reads_the_recorded_design.py:349-359` **pins the defect** — it computes
`rows = len(project.training_rows)` and asserts that string is in the served effect — but its fixture
`clinical_longitudinal.csv` has **0 NaN in `progressed`** (measured, 600×13), so
`analysis_rows == training_rows` and **both implementations satisfy it.** Same for
`test_every_ranking_is_computed_on_the_training_rows.py:263`, whose `clinic_visits.csv` has **0 NaN in
`hba1c`** (measured, 140×14).

**A fix landed against these fixtures is unfalsifiable.** So:

- Ship a fixture **with blank outcomes**, and add the control the file lacks:
  `assert len(project.training_rows) > len(project.analysis_rows)`. *(The file does have a control at
  `:351` — `assert people < rows` — but on the **wrong axis**. Add beside it, do not replace.)*
- **The guard must match the SENTENCE, not `n=`.** Driven: the design effect contains **no `n=` at
  all**, which is why L62-A's regex at `test_the_shelf_reasons_about_rows_a_model_will_see.py:180`,
  scoped to `model["concern"]`, could never have seen this. **The guard's shape, not only its scope, is
  why that sweep terminated where it did.**
- The design clause is gated behind `design.answered("repeat_kind")`, which needs a `people_repeat`
  grain. **Both existing fixtures are `one_row_per_person`**, so L62's tests could not have reached it
  even if they had looked.

### C3 · `TEST-101` — split the loop per route; do not delete it

The post-fix guard is **two** sites: `test_the_shelf_reasons_about_rows_a_model_will_see.py:122`
(`/models`) and `:138` (`/recipes`), against `_trainable` at `:84-91`. The pre-fix one the row does not
name is **`test_every_ranking_is_computed_on_the_training_rows.py:263`** —
`assert body["n_rows_seen"] == n_all - n_sealed` — **inside a loop at `:257-259` over THREE routes.**

**At HEAD that expectation is CORRECT for `/selection/evidence`** (whose count still comes off
`project.training_mask` at `api.py:2498`) and wrong for `/models` and `/recipes`. **`n_rows_seen`
genuinely means two different things per route** — driven, on one fixture: 225, 225, 825. **So split the
loop per route and say in the file why the third differs.** Deleting `:263` leaves that route's count
unguarded.

**Two hazards for whoever splits it.** The function name `test_the_served_row_count_excludes_the_lockbox`
is `GUIDED-092`'s named test, and `ledger.py check` only asserts the `test` field is a non-empty
**string** (`ledger.py:211-229`) — it does not resolve the node. Only
`tests/test_a_fixed_rows_named_test_resolves_in_five_seconds.py` would catch a rename, and that is not
in the ordinary run. **Keep the function name, or move `GUIDED-092`'s field in the same commit.**

**And trap 3c bites in BOTH directions here.** Reverting `api.py:2245`/`:2333` to `training_rows` turns
`:122`, `:138` and the capacity-clause test red — **that is the revert, not the fix.** But the sharper
hazard is the opposite: **correcting `:263` on `clinic_visits.csv` produces no signal at all**, 98 == 98
before and after. **"Still green" does not mean "no change needed."** Do not swap `clinic_visits.csv` —
it is shared by the poison probe, its positive control and five parametrized cases. Add a second fixture.

### C4 · The corrections you would otherwise make wrongly

- **The mislabel class is five sites, not one, and two are manuscript-bound captions.**
  `instability.py:157` calls an already-outcome-filtered count *"training rows"* while the correct
  phrasing sits four lines down at `:232`; `training.py:604-605`; `instability.py:344`/`:351`; and
  **`figure_specs.py:1769` and `:1918`**, which consume instability's `n` and print the analysis count
  labeled *"training rows"* **in a publication caption.** A sweep that classified the numbers as
  correct and never checked their labels is the same blind spot that let the design sentence survive.
- **`rankings.py`: do NOT rename a scope, and here is why the obvious remedy fails.**
  `scope=TRAINING_ROWS` at `:100`, `:111`, `:122`. **`analysis_rows` is a strict subset of
  `training_rows`, so the declaration is still true** under the registry's own two-valued definition —
  the vocabulary simply cannot express the third population, and `Surface.__post_init__` at `:79-83`
  **hard-refuses** a third value with a `ValueError`. Flipping any of them to `WHOLE_TABLE` turns
  `test_a_ranking_does_not_move_when_only_the_held_out_rows_change` red at `:177`, which requires a
  `WHOLE_TABLE` surface to **move** under the poison probe. **And the poison probe cannot see this class
  at all**: `_poison` overwrites values only on rows in `lockbox["labels"]`, while the two populations
  differ exactly on the outcome-**blank** rows, which are not sealed. **A scope rename would produce a
  registry that looks guarded and is not** — `MISC-022`'s own shape. What is needed is a **second
  probe**: blank an outcome on unsealed rows and assert the surface moves. Build that.
  *(Also worth knowing: nothing that ships reads `rankings.SURFACES`. Two test files import it. A
  registry-only edit changes no served byte.)*
- **`GUIDED-092` one level down, and it is why a sixth surface is already written.** Five production
  sites still spell `analysis_rows` inline as `training_rows` then `.notna()`: `api.py:2910-2911`,
  `figure_bundle.py:535/537`, `instability.py:228-229`, `explain.py:158-159` — after L62-A gave the rule
  a name. **`project.py:1948-1950` says in as many words that three inline copies of one rule is why
  `training_rows` became a property.** Collapse them onto the property, or file why not.
- **`identifiers.py:206-221` is NOT a defect and is the model to copy.** It reads `training_rows`
  deliberately and **says why**, which is exactly the escape clause `analysis_mask`'s own docstring
  grants at `project.py:2483-2485`.

---

## Part D · `DRIVE-051` / `TEST-104` — three counts that do not partition, under a guard that says they do

### D1 · The defect, stated correctly, because the row states it wrongly

`/models` (`api.py:2245`, `:2251`, `:2252`) and `/recipes` (`:2333`, `:2339`, `:2340`) serve three
counts side by side, and `test_the_shelf_reasons_about_rows_a_model_will_see.py:160-163` asserts they
**partition** the table.

**The row's clause *"'withheld' still silently means sealed OR unusable"* is FALSE at HEAD.**
`n_rows_withheld` is `(~training_mask).sum()` (`project.py:2525`) — the seal and nothing else. Driven:
it reported **225** while 600 rows had no outcome, and **150** while 60 did. That conflation was the
pre-split defect L62-A already removed.

**What is wrong now is a double-count, and the algebra is closed.** `seen ∩ withheld` and
`seen ∩ without_outcome` are empty **by construction**, because `analysis_mask` is
`mask & df[target].notna()`. **So the overshoot is exactly `|sealed ∧ outcome-less|` and nothing else.**

**Why the guard is green: the fixture's *history*, not its data.** `engine.draw_holdout` opens with
`eligible = list(df.index[y.notna()])` (`engine.py:920`), so the seal draws only from outcome-bearing
rows and the intersection is zero at seal time. **The overlap therefore requires a row to LOSE its
outcome after being sealed** — which is a mechanism, not an accident, and it narrows the build to two
paths. **Both driven over HTTP from a fresh project, by two independent agents on different frames:**

1. Seal on a complete column, then **change the target**. `set_target` is not refused post-seal and does
   not clear the lockbox (`api.py:526-539`, `project.py:553-569`). 900 rows → seen 227, withheld 225,
   no-outcome 600, **sum 1,052 against 900, over by 152 == the intersection.**
2. **The ordinary path, needing no target change**: a post-seal data-quality repair.
   `set_impossible_missing` writes NaN into the target and is not barrier-gated —
   `PRE_BARRIER_ONLY_FIXES` is only `{promote_header, melt_repeated}` (`project.py:58`). 600 rows → seen
   404, withheld 150, no-outcome 60, **sum 614 against 600, over by 14 == the intersection.**

The second verifier reproduced both identities on its own frames (140 and 19). **A prompt written around
path 1 alone would let you dismiss this as an edge case. It is not.**

### D2 · The fix-shape trap, and it is why this part needs care rather than speed

**The obvious repair turns the guard into a tautology.** If `n_rows_without_an_outcome` becomes
`training_mask & df[target].isna()`, then `|T∧Y| + |¬T| + |T∧¬Y| == |T| + |¬T| == len(df)` **for every
dataset**, so the assertion can never fail again. That is the same loss as relaxing it to `<=`,
arriving by the other door — and this project has already ruled that a check which cannot fail is not a
check.

**So any fix ships a replacement assertion that can still go red**: pin the served intersection against
a **second** fixture that actually has sealed-and-outcome-less rows. Keep the existing `sealed` fixture
— it still guards the `DRIVE-045` numbers.

**Three numbers presented as a breakdown of a whole assert that they sum to the whole.** Making them
disjoint alone leaves a name asserting something false about its own value; renaming alone leaves the
app asserting a sum it does not honor. If the true blank-outcome count is still wanted, serve it as a
**fourth, explicitly-overlapping** field. **An overlap served as a number is legible; an overlap a
reader infers from an arithmetic failure is the defect.**

**A rename trips TWO assertions in `test_the_three_unswept_payloads_are_swept.py`**, not one: `assert
not undeclared` at `:309` as well as `assert not stale` at `:315`, because `_family` is
`(route tail, shape key)`. The `FILED` table must move in **both** directions in one commit.

**Nothing renders these three counts** — an exhaustive grep returns zero hits for all four field names
in `index.html`, and the page's only `/models` fetch reads `SHELF.groups`, `SHELF.design`,
`SHELF.disclosure`. So a rename is cheap now and expensive after `GUIDED-202` wires it. **But do not
read that as "this payload's row counts are invisible": `DRIVE-050`'s design sentence IS rendered, out
of `SHELF.design`, from the same response.**

### D3 · `TEST-104` — the guard beside it is falsifiable, and stays green through the wrong state

`assert_identity_intact` (`project.py:2373-2383`) has **exactly one call site**, `project.py:2154`
inside `trim_training_rows`. **`apply_fix` does not call it**, and `drop_rows` is not in
`PRE_BARRIER_ONLY_FIXES`, so `check_repair_allowed('drop_rows')` post-seal returns without raising.

**Driven at HEAD:** sealed 75 rows, dropped 10 of them through `apply_fix`. Nothing raised.
`n_rows_held_out` fell to **65** while `lockbox['labels']` stayed **75**, and `/models` served
`withheld=65`. `assert_identity_intact()` **would** have raised. **And in that same state the three
counts summed to 890 == `len(df)` 890, so the partition assertion at `:160` stayed GREEN while `:154`'s
`withheld == len(lockbox["labels"])` was false by 10.**

**The partition guard is not merely fixture-kind — it is satisfiable by a state that is wrong in a
different way.** Call `assert_identity_intact` on every post-seal repair path, or make
`n_rows_held_out` and the lockbox's label list one derivation. *(The drive called the project method
directly rather than an HTTP `apply` carrying a live `drop_rows` finding, so end-to-end API
reachability is one step short of driven. Close that.)*

**D and C both edit `test_the_shelf_reasons_about_rows_a_model_will_see.py`. Do not run them in
parallel.**

---

## Part E · `MISC-023` / `TEST-105` / `TEST-106` — the instruments, audited

### E1 · `MISC-023`: the obvious fix is a measured no-op, and I checked before asking you

`test_the_level_a_reader_sees_is_the_one_they_chose.py:198-218` builds its population at `:211` from
`list(root.glob("*.py")) + list(root.parent.glob("ml/*.py"))` — 53 non-test `turbotab/*.py` plus 47
`ml/*.py` = **100 files**, non-recursive — filters at `:213` on a raw substring
`"outcome_level_names" in p.read_text(...)`, and asserts equality at `:214` against a four-name roster.

**Two corrections to the row before you touch it.** The guard **does not overclaim**: its docstring at
`:203-205` and its message at `:218` both say a new surface is *uncovered* rather than *caught*. The
false sentence lived in `MISC-019`'s note and was corrected at `e6b5251`. And **widening the glob is a
measured no-op**: the predicate was run over `ml/ + turbotab/ + pages/ + utils/ + launcher/ + index.html`,
and then over **all 489 tracked `.py`**, and the roster does not move in either case. Nothing outside
`turbotab/` and `ml/` names the mapping. **A loop that widens the glob and reports a fix has produced a
`FIXED` row over a change with zero observable effect.**

**What is buildable, and it has an in-tree exemplar:** a **positive control on the sweep's own
instrument**. `tests/test_the_landing_page_says_where_the_decision_curve_lives.py:158` asserts the sweep
can find a symbol it **must** find before trusting its silence. Four lines, and unlike the widening it
produces a test that can fail.

**And the shipped instance the row asserted without evidence does exist — inside a rostered file.**
`turbotab/training.py:110-111` serializes `"positive_label": (… str(self.positive_label))` — the raw
class level — into `ModelResult.to_dict()`, i.e. into a served payload; `figure_bundle.py:422` does
`payload["event"] = level if level is not None else str(event)`. **Both files are in the roster, so the
file-granular guard blesses the file and can never see the surface inside it.** That is the concrete
instance, and it does not depend on the Guided/Classic argument at all.

**Do not touch** `test_the_pca_group_counts_fall_back_to_the_value` (`:160`) or
`test_table_one_headers_keep_the_encoded_value_when_the_record_cannot_say` (`:125`). Both assert the app
renders the **encoded** value when the record cannot name a level — the deliberate silent branch.

### E2 · `TEST-105` — file the class, and file only the half that is new

Six instances at HEAD of *a guard whose population is filtered by the compliance predicate*:
`test_the_level_a_reader_sees_is_the_one_they_chose.py:211-215`;
`test_every_ranking_is_computed_on_the_training_rows.py:269-308`;
`tests/test_the_landing_page_says_where_the_decision_curve_lives.py:144-169`;
`test_the_purpose_card_names_only_the_decisions_that_read_it.py:137-162`;
**`docs/turbotab/tools/evidence.py:315-317` and `:340-342`** — the shape inside the gate this project
trusts most; and `test_every_guard_is_planted_against_the_real_path.py:207-210`, whose own docstring
states the thesis and which a test whose body is `pass` satisfies.

**The contrast case, same six lines of Python and opposite epistemics:**
`test_every_writer_that_can_blank_a_cell_files_it.py:258-263`, where the filter token **is** the defect.

**Add the POLARITY half to `AGENT_ONBOARD.md` §07 and only that half.** The method half — a substring
over globbed files standing in for behavior — **is trap #5 and is not new**; `rankings.py:40-46` already
carries the standing answer in production source. **Filing the whole thing as a seventh variant would
duplicate #5**, which is what the fan-out's own verifier caught its recon doing.

### E3 · `TEST-106` — and the §06.2 form is already satisfied

The spelling gate walks into `.claude/worktrees/` and fails on another checkout's exempt prose. Its own
comment at `tests/test_american_spelling.py:27-34` names the class — *a nested checkout of this
repository is not more source* — and the code beneath names one **instance**, the string `".worktrees"`.
The harness puts its worktrees in `.claude/worktrees/`, which is also gitignored, **so the gate is
reading files git does not track.**

**Enumerate from `git ls-files`**, or skip any directory containing a `.git` entry. **Do not simply add
`.claude` to `SKIP_DIRS` — that is the same fix that failed.** §06.2 is satisfied: the reasoning was
recorded at `73e6f37`, before the repair, on a passing run. **Verify the gate got stronger, not
smaller** — reading from `git ls-files` scans the same tracked prose, once, at its real path, and
additionally stops it reading ignored files.

---

## Part F · The sweep, and the command it licenses

**Last, and it is machine time rather than thinking time.** After A lands:

```bash
venv/bin/python -m pytest turbotab/ -q -n 8 --dist loadfile     # L62 measured 41:23
venv/bin/python -m pytest turbotab/ -q                          # L62 measured 2:00:46
```

**Run `ps` first and again at twenty minutes.** A suite is quotable only if nothing else is writing the
tree *or competing for the machine* — including you. **Diff the failure lists, do not compare the
counts**; byte-identical lists is the check that catches a swap.

**`setsid` does not exist on this host and a PID is not a running process — `TEST-099`, and it cost L62
three hours.** The shell forks, two PIDs appear, the binary is not found, nothing runs, and the loop
reports the sweep as started. **Assert the output, never the process**: one `ls` of the log, or a
`tail` for a line the run must print. This is the trap most likely to eat your evening, because Part F
is the one thing here you will want to background.

**Note that `AGENT_ONBOARD.md` §03 now marks the parallel form HELD**, with `TEST-098` named. That
paragraph was added the same day this prompt was written, because §03 documented the command while
three other documents said it must not be — see `MISC-025`. **Part A is what lifts the hold and Part F
is what licenses the line**, so until A lands, quote the serial form and say which runner produced any
number you report.

**The last full sweep was at `d464e0b` and came back 2 failed. Both were repaired and it has not been
re-run since**, so the honest starting claim is *unknown red count at HEAD*, not zero.

**If only one run fits, run the serial one.** `TEST-096`: a parallel run never produces the serial
order and structurally cannot see an ordering defect. If both agree, **document the parallel command in
`AGENT_ONBOARD.md` §03 with its date and commit** — that is the hour and twenty minutes this loop buys
back on every loop after it.

---

## Deliberately not built

- **`MISC-024`.** Its `act` is **falsified**: `evidence.py`'s `_resolve` (`:164-174`) checks file and
  **heading** existence, and `git show e6b5251 -- <both docs> | grep -E '^[+-]#{1,6} '` returns
  **empty** — not one heading line moved while the sentences beneath went stale. **The named mechanism
  would have run green over the exact defect the row was filed for**, and `evidence.py:56-57` says so
  itself. `LOOP.md:539` puts this squarely in *"new construction under a governing rule about what may
  be asserted — design work, not loop work."* **The deliverable is a decision and it is mine, not
  yours.** *(If you want the bounded fragment: `deck_faces.py:49` and `:80` already ship
  machine-shaped citations into both documents, neither resolves, and nothing checks them. That is real
  loop work and it is **not** the defect class. Do not build it this loop.)*
- **`GUIDED-244`** — `/selection/evidence`'s count. Filed, ruled, and deliberately deferred: C3 splits
  `:263` on the current truth, and fixing this count in the same loop would make that split stale in the
  loop that wrote it. The row explains it.
- **`GUIDED-241`** (the refusal carrying its control), **`GUIDED-233`** (the explainability pack),
  **`DRIVE-036`**, **`DRIVE-052`/`053`**. Parked, dispositioned.
- **Any page renderer for the ROC or the decision curve.** `figROC` already loops series. Nothing draws
  the decision curve in-app at all (`FIG_DRAW` holds `roc` and `item_correlations`) — that is
  `GUIDED-202`'s, not yours.

## Scope note

**If B runs long, ship B1–B3 and file B4's items rather than dropping C.** B1 is a blocker and B3 is the
row; B4's four items are consequences that can each carry their own row. **If F cannot run on an idle
machine, say so and do not quote a timing** — L60 quoted a duration taken while the product owner drove
the app and had to withdraw it. A result with no attributable timing is still a result.

**A part may be REFUSED when carrying it out would violate the criterion the row itself states.** That
is a standing permission and it has been the right call four times. Measure, file, and hand the decision
back — do not build a weaker version and do not silently substitute your own.

## How to report

One report, at the end. What each part did and which you did not reach. **The counts your probes owe** —
what you enumerated, what passed, what failed, and what you could not construct. Every finding you made
rather than fixed, with its ledger id. **The fixture shapes you did not cover.** And **where you diverged
from this prompt or were unsure** — that section has corrected the adjudicator six loops running and it
is read first.

**No silent caps.** If you bounded something, say what you dropped and why.
