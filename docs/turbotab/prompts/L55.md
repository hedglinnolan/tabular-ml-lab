# TurboTab — the L55 loop

**If you are a fresh agent, read [`AGENT_ONBOARD.md`](AGENT_ONBOARD.md) in full first.**

**Four parts, ordered certainty-first. The part L54 could not reach is Part B and it runs before
anything containing an unknown.** That ordering is a rule now, and it exists because of L54.

**L54 was accepted as three of four parts.** §00 is the response you are owed. **The missing part is
the adjudicator's fault and is named as such below.**

---

## 00 · Your divergence section, answered

**Your report named the missing part first, before anything you had built. That is the right order
and it is rarer than it should be.**

**Part C's absence is mine, not yours.** L54 quoted `DESIGN_LANGUAGE.md` §05.2's zeros — **0**
`startViewTransition`, **0** FLIP, **0** `.animate()`, **0** `transitionend` — as the *argument* for
building B0, and never drew the second inference: **a harness is a shim built to run the page, so an
API the page never used is an API the harness never implemented.** The same table was both the
motivation and the warning and I read only the half that fit. You then paid for it in rounds, and
the part that died was **pure plumbing with no unknowns at all.** `LOOP.md` §02 carries two rules
now — *verify the instrument before building on a mechanism with zero instances*, and *do not
sequence certain plumbing behind unpriced discovery.*

**`TEST-066` is the most valuable row in the loop and it is not close.** `appendChild` **copying
rather than moving** is worse than a missing method: move semantics are the whole mechanism behind
identity-preserving reorder, so **the instrument cannot observe the claim B0 exists to make.** Your
framing of the risk is the durable part — *the tool is too poor, so it pushes production code toward
the subset it implements* — and that is a different failure from a tool that is too permissive.

**B0 is verified.** Zero `innerHTML` in the deck region with comments stripped, and none of
`insertBefore` / `replaceChildren` / `.hidden` in shipped code. **The proof is what makes it**: the
element is stamped, the study panel is toggled twice **through its real control**, and the stamps
must survive. That is identity demonstrated, not asserted.

**`TEST-064` said the number when the number was zero** — 8,852 asserts, 3,783 with messages, 193
subscripting, **0** of the sharp shape. A sweep that reports a nil count is doing its job.

**`TEST-065` answered better than the prompt asked.** I asked whether a structural check was
possible; you said mostly not — *"a test that could make it would be a second implementation of
reading"* — and then named what **is** assertable. **Pinning the sentence each mapping was validated
against is a real design** and it is available whenever a loop wants it.

**Item 4 deserves saying plainly**: you wrote `TEST-064`'s defect into the test written to check
retention, on the same day you filed the row naming that class, and the revert probe then died on
your own region locator. Both self-caught, both reported. **The class is genuinely hard to see from
inside, which is the entire reason it is a row.**

**One correction, and it is mine again.** Item 5's fifth assertion goes red on a `HarnessError` under
total revert. §08.1 does not accept that as discharge and you did not claim it did — `GUIDED-178`
stayed `PARTIAL`, correctly. **Part D fixes the instrument so the fifth assertion can be made
honest** rather than leaving it as a known-soft corner.

### Found after your report, and not by you

**`TEST-063` was `FIXED` naming `test_a_pack_may_replace_a_core_operation_only_by_saying_so`, which
is in no collected test.** I have reopened it to `PARTIAL` and repointed it at the snapshot/restore
contract, **unconfirmed**.

**Why it survived two green loops is Part A.** The guard that catches it costs **5s**. It shares a
file with one costing **2,473s**. Every loop excludes the *file* for the slow test, which silently
excludes the fast one — so the check that validates the core claim of **every closed row** had not
run since it was written, and the first time it ran it found a live violation. **That is not
housekeeping.**

---

## 01 · State

Branch `TurboTab`, HEAD at the L54 adjudication. Ledger **873 findings, 361 closed** — the reopen,
not a regression. Register 181 rows. Six gates green.

**The pace ruling stands: the fast tier after every batch, `turbotab/` ONCE at the end.**

| Suite | Tests | Cost |
|---|---|---|
| `tests/` + `tests/integration` | ~1,985 | **~72 seconds** |
| `turbotab/` | 2,413 | ~2 hours serial |

**`AUDIT-040`'s floor is corrected**: the longest single test is 2,473s and the longest in the
intended-exclusion set is **1,175s of SETUP** in `turbotab/test_the_three_unswept_payloads_are_swept.py`.
Parallelism cannot go below the longest single test.

---

## 02 · How this loop runs

**Everything from L50–L54 §02 binds**, and two of those are new this loop:

- **Verify the instrument before building on a mechanism the codebase has zero instances of.** If a
  part needs a DOM API, a fixture shape or a harness capability that nothing currently exercises,
  **the first commit of that part is the reconnaissance** — a five-line probe that proves the
  instrument supports it. L54 spent rounds discovering this at build time.
- **Do not sequence certain plumbing behind unpriced discovery.** This loop is ordered accordingly.

---

# Part A — the guard that could not run

**Small, first, and it changes what every later batch catches.**

**A1 · Split `tests/test_a_fixed_row_names_a_test_that_actually_runs.py`.** Two checks live in it and
their costs differ by ~700×:

| Check | Cost | What it answers |
|---|---|---|
| `test_every_fixed_rows_named_test_exists` | **5s** | does every `FIXED` row's named test *resolve*? |
| `test_every_fixed_rows_named_test_actually_runs` | **2,473s** | does it *run* rather than skip? |

**Move the 5-second check into a file nothing excludes**, so it runs in the ~72s tier every batch.
Leave the slow one where it is, excluded as now. **Name the new file so its cost is obvious** and say
in its docstring why the split exists — the reason is the finding, not the split.

**A2 · Confirm or replace `TEST-063`'s guard.** It is `PARTIAL`, repointed at
`turbotab/test_the_recipe_table_is_a_table.py::test_the_pack_is_gone_when_the_test_is`,
**unconfirmed**. Either demonstrate that guard fails on a **total** revert of the leak fix — in which
case set it `FIXED` with the probe output — or write the guard that does. **You wrote the fix and
know what it was; I did not.** The underlying repair looks real and only its record was wrong.

---

# Part B — the lens reaches the model shelf

**This is the loop, it is what L54 could not reach, and it contains no unknowns.** `GUIDED-230`.

**The measurement, re-derived at `f20a93d` and yours:** the lens is consumed by **13 non-test
modules** — `router`, `eda_recommender`, `engine`, `project`, `api`, `figures`, `figure_specs`,
`figure_bundle`, `pipeline_plan`, `recipes`, `survey`, `repeats`, `clinical`. Under a metabolomics
lens `ml/router.py:340` **rewords the missingness question.** It stops at exactly one place:
`grep -c lens` is **0** in `turbotab/models.py` and **0** in `ml/model_registry.py`, `shelf()` is
`(profile, task_type, probe)`, and `project.py:1866` passes none of `purpose` (`project.py:264`),
`repeat_kind` (`:279`) or `unit_of_analysis` (`:280`) — **all three recorded fields the app
deliberately asks for.**

**Carry the three answers into `shelf()`.** That is the whole task.

**Three rules, and they are what stop this becoming a filter:**

1. **The shelf is never shortened.** A model that does not fit the recorded design **ranks lower and
   says why.** It is never removed, never disabled, never hidden. `PRODUCT_VISION.md` is explicit
   that judgment is rendered as **order, not absence**, and this is the first place that rule meets
   a domain answer.
2. **The sentence is the deliverable, not the ordering.** A reordered shelf with no account of why
   is a black box wearing a lens. Each affected entry carries a clause naming **which recorded answer
   moved it** — *"repeated measures were recorded on subject_id, and this model treats rows as
   independent"* — and that clause **quotes the recorded decision**, not a paraphrase of it.
3. **An unanswered question changes nothing.** If `repeat_kind` was never asked, the order is exactly
   what it is today. **Absence of an answer is not an answer**, and this is the `(None, None)` rule
   one level up.

**Do not add a model family this loop.** No Cox, no mixed-effects, no cumulative-link. **The plumbing,
the ordering, and the sentence.** `GUIDED-105`/`106`/`118` become ordinary work once this lands, and
time-to-event is the next loop.

---

# Part C — the journal view

**`DRIVE-014`, and it is cheap because the expensive half already shipped.**

`.fig-canvas.journal` is fully styled at `index.html:299-301` — white ground, Georgia serif captions —
and the literal `journal` appears **zero** times in the page's script region. **A CSS rule is the one
dead capability no test and no import graph will ever flag**, which is your own line and it is why
this is worth a part rather than a footnote.

**Wire the toggle.** `DESIGN_LANGUAGE.md` §07 is the specification: in-app gets tooltips and
clickable legends; journal view re-renders the same figure as it will be published — serif, greyscale,
**series distinguished by dash pattern rather than color alone**, printed *r* values, numbered caption.

**Scope it honestly.** The toggle and the journal rendering are this part. **Export at 3× and LaTeX
tables are not** — say so rather than half-building them. **Move `DRIVE-009` to reflect whatever
lands**; it is `PARTIAL` with one third named, and this is the second third.

---

# Part D — the instrument

**`TEST-066`, and it is what makes Part B of L54 finishable later.**

The harness lacks `insertBefore`, `replaceChildren` and `hidden`, **and its `appendChild` copies
rather than moves** — re-appending three cards in reverse gives six.

**Fix move semantics first**; the missing methods are secondary and may be left with a row if the
loop runs short. Move semantics are what let
`test_a_card_is_moved_rather_than_rebuilt_when_the_deck_reorders` be red for the **right** reason
under a total revert instead of a `HarnessError`, and they are the prerequisite for the deck's face 3.

**Then make that fifth assertion honest** and say whether `GUIDED-178` moves.

**A harness bug is a claim about every test that uses it.** When you fix `appendChild`, **say what
else changed** — a test that passed because the harness copied is a test whose meaning just moved.

---

## Rules that bind

- **The shelf is never shortened** — Part B's central rule, and the one most likely to be broken by
  accident while ordering.
- **Quote the recorded decision, do not re-compose it.**
- **A total revert, or the probe proves nothing.**
- **Absence of an answer is not an answer.**
- **Verify the instrument before building on a mechanism with zero instances.**
- **A class goes in the ledger the moment you name it.**
- **Ambiguity is `OPEN`, never `FIXED`** — `TEST-063` is the standing example.
- **Say the number**, including when it is zero.

## Deliberately unbuilt

- **Any new model family.** Part B is plumbing. Time-to-event (`GUIDED-118`) is L56.
- **Deck faces 2 and 3.** Registered `Pending` already; face 3 waits on Part D.
- **Export at 3× and LaTeX tables.**
- **The 193 benign assert messages** from `TEST-064` — rewriting them is churn, as you said.
- **The 77 unchecked registry entries** and the clinical pack's ~25 untabulated.
- **The remaining 15 TRIPOD+AI items** — pack authoring, not code.
- **`GUIDED-080`, `096`, `169`, `172`**, and the standing unchanged list from L54.

## Scope note

**A is small and first.** **B is the loop and it is certain work** — if anything competes with B, B
wins, and unlike last loop B contains nothing that can overrun. **C is cheap and user-facing.**
**D is the instrument, and it is the one part allowed to run long**, because a wrong harness makes
every future page test a claim about the harness.

**If D competes with C, D wins** — the instrument outranks a feature.

## What will be checked

- **Whether `shelf()` reads the three answers**, and **whether any model was removed, disabled or
  hidden rather than reordered.** That is the failure condition and I will look for it first.
- **Whether the ordering clause quotes the recorded decision** or paraphrases it.
- **Whether an unanswered question leaves the order unchanged.**
- **Whether the 5-second guard now runs in the fast tier**, and whether `TEST-063` was confirmed on a
  total revert or replaced.
- **Whether the journal toggle renders dash-pattern series** rather than color alone.
- **Whether fixing `appendChild` changed the meaning of any test that passed under the old harness.**
- **The ~72s tier per batch; `turbotab/` once, on a quiet machine, with `ps` checked.**

Report once, in `AGENT_ONBOARD.md` §10's shape. **Your divergence section is the first thing I read.
Last loop it led with the part you did not build, found a harness defect that would have invalidated
your own proof, and reported writing a freshly-filed defect class into your own test — and the part
you missed was mine to price, not yours to reach.**
