# TurboTab — the L59 loop

**If you are a fresh agent, read [`AGENT_ONBOARD.md`](AGENT_ONBOARD.md) in full first.**

**Five parts, and the loop is sequenced by a second human drive.** L58 is accepted and closed, four
of four. A product owner's tester drove the app again on 2026-08-12 at `b4b8246`:
`DRIVE_UX_SURFACING_NHANES_RUN2.md`. **The seal path you built works** — grain, purpose, eligibility
and the seal all fire in order and the seal drew 945 rows. **The back half is still unreachable, for
a different reason, and the reason is a swallowed fetch.**

**Every number below carries how it was derived.** *(re-derived at `f200f7a`)* means I ran it against
this tree while writing this. *(from the row)* means I am quoting and have **not** re-measured —
**doubt those first.**

---

## 00 · What the drive found, and what I measured before writing this

**The tester DOM-verified `controlCount: 0` rather than reading a screenshot**, which is the right
instrument and it is why this is not arguable. Train renders its heading — *"Which models should be
fitted?"* — with **no shelf and no fit control**, so Explain, the figures, the checklist and the
manuscript's performance rows all stay empty. Run 1's blocker survives with a new proximate cause:
**run 1 the seal never drew; run 2 the seal draws and Train is empty.**

### The mechanism is established. The trigger is not. They are filed apart on purpose.

*(all of the following re-derived at `f200f7a`)*

1. **Serving `GET /project/{id}/models` as a 500 under `pageharness` reproduces the tester's render
   exactly**: `0` occurrences of `data-pick-model`, no `data-train-start`, `#card-train` empty — and
   **no refusal band, no message, no text anywhere** saying the shelf could not load. Healthy, the
   same drive renders **13 model controls and a fit control**.
2. **The two lines that do it.** `index.html:6070` — `if (!P || !SHELF) return;` — and `:6127–6137`,
   where `renderTrainStep` gates on `P.barrier_raised` and ends its fetch in a bare
   `.catch(function(){})`. **`SHELF` is fetched once and there is no retry**, so one failed fetch ends
   that session's ability to train, silently and permanently.
3. **What did NOT reproduce it, so you do not spend the loop re-running it.** Settling Features and
   Preprocess *after* the seal renders **13 controls**, identical to seal-only, in a matched A/B.
   `/models` is **healthy for a target shaped like the tester's** — 71.2% target-missing, 7.2:1
   imbalance — at `n_available: 12`, and 13 for regression. **The tester's stale-flag hypothesis is
   not supported**: `#stale-train` carries the same class in both arms and the shelf renders anyway.
   Their symptom is real and their diagnosis is not, which is the normal shape of a drive finding.

### And the number in my own L58 adjudication was measured wrong

I ruled *"`choose_models` PASS — 0 controls before the seal, 13 after."* That is true, and **I measured
it by loading a page into an already-sealed project.** A human loads the page **once** and seals inside
it. Nothing in my measurement exercised the transition, and `SHELF` has no retry. **Measuring the state
where the user experiences a transition** is the same error as accepting a `200` for a consequence in
`DRIVE-024` — the third instance this session, all mine. **Where a row is about something a user does,
drive the doing.**

---

# Part A — the Train step says why, and can try again

`DRIVE-030`, **critical**. **This is the loop, and it is small.** It does not require finding the
tester's trigger; it makes the app hand you the trigger.

**A1 · Report the failure where the step is.** A failed `/models` currently produces a heading and
silence. **The user cannot tell a step that broke from a step with no content**, which is neither of
the two things `PRODUCT_VISION.md` §04b permits — the app may be silent, and it may refuse, and this
is a third thing: a step asserting it has nothing to offer when it does.

**The prior art is `GUIDED-176` and it is one step milder than this.** There, a GET's `.catch` was
`setErr(err.message)` and the message landed in `#upErr` inside a hidden subtree — *an error nobody
saw*. **Here there is no error at all.** Same class, one degree worse. Fix it the way this codebase
already fixes it: the message goes where the thing that failed lives.

**A2 · Give it a retry.** One failed fetch must not end the session. There are **48 `.catch(function`
sites in `index.html`** *(counted at `f200f7a`)* and most are correctly benign — a lost palette, an
absent overlay. **This one is not benign and the difference is that nothing else can load the shelf.**
Scope the retry to this fetch; do not sweep all 48.

**A3 · The guard, and it is `DRIVE-022`'s instrument again.** `test_every_handled_key_reaches_the_dom.py`
already drives `choose_models` into an asked state and counts controls. **Add the failing case**: serve
`/models` as `{"__status": 500, ...}` and assert the render **names the failure** and **offers the
retry**. The harness supports a refusing route and that is the whole reason it does.

**What NOT to do.** Do not chase the tester's trigger this loop. Their file is not on disk, their
session is gone, and A1 makes the next drive self-diagnosing. **If you find the trigger for free while
doing A, file it; do not go looking.**

---

# Part B — the receipt says the base it actually drew from

`DRIVE-031`, **critical**, and it is one true sentence in two places.

**Reproduced verbatim at `f200f7a`** on a 21,849-row frame with 15,552 target-missing rows:

> eligibility: *"No eligibility restriction: **all 21849 rows** are in the study population, and the
> held-out set is **drawn from all of them**."*
> seal: *"**945 rows (15%)** are held out…"*

**945/6,297 = 15.0%. 945/21,849 = 4.3%.** Both sentences are on one page and they cannot both be true.

**The app already knows the right number**, which is what makes this cheap and what makes it serious:
the read-as-draft manuscript prints *"Of 6,297 rows with a value for the outcome, 945 were sealed …
and 5,352 were available for fitting."* **So the engine is right and one user-facing sentence is
false** — trap 6's shape with the error in the composed string rather than in the render.

**Why critical rather than high.** It is a claim about **the split**, which is the one thing the
lockbox exists to be trustworthy about, and it goes into a methods section. A reader who trusts it
mis-states their sample.

**One thing to establish rather than assume**: the sentence is composed in the **eligibility** branch
and the drop is a property of the **target**. If those are two different moments in the record, this
may be one sentence owed by two rows — **say which, and do not fold them silently.**

---

# Part C — the positive class, established before it is fixed

`DRIVE-032`, **high**. **This part may end in a measurement and a row rather than a build, and that is
an acceptable outcome.**

**Verified at `f200f7a`**: driven on a binary `Int64` target with 15,552 missing, the interview plans
across `data`, `explore`, `features` and `preprocess` serve `confirm_task_type`, `state_grain`,
`choose_features`, `choose_selection`, `choose_models` and six `look::` pulls — **and nothing about the
event level.** `project.positive_label` is **`None`**. The only near-match is
`look::r5_target_classification`, which is a pull-palette explorer and not the question.

**The register says `target-positive-class` is always asked and never pre-selected at any confidence.**

**C1 · Establish first, and this is the question that decides the row's size.** Does anything
downstream substitute `1` when `positive_label` is `None`? **Drive it — fit something and read what
the metric was computed against — rather than reading for it.**

- **If something silently defaults**, the app is choosing the event without asking, which the register
  forbids in those words. The row is a **wrong default**, not an absent question, and it is upstream of
  `GUIDED-093` — *"a picture of the complementary event, drawn confidently"* — and of L57's entire ROC
  ruling, which turns on every scored result agreeing on `positive_label`. **Say so and re-file.**
- **If it refuses instead**, the row is an unreachable question and is smaller.

**C2 · Then wire the question**, if C1 leaves room. **If C1 consumes the part, hand C2 back.** A
measured answer to C1 is worth more than a rushed card, and `LOOP.md` §02's refusal permission covers
this.

---

# Part D — two small ones that a human has now hit twice

**D1 · `DRIVE-033`, high — undo reverts one column of nine.** After applying *"read as binary"* to a
9-column group, *"Undo the last change"* restored only `gender`; `meds_hbp`, `meds_chol` and six
`imputed_*` stayed `Int64` *(from the row — the tester read the dtypes in the target picker; **I did
not reproduce this**, so establish it before fixing it)*. The apply tooltip promises the decision
*"covers the whole group. It can be undone."* **A partial undo leaves the table in a state no decision
describes**, which is the transcript disagreeing with the frame.

**It is also a confound for `DRIVE-030`** — the tester did this undo before reaching Train and could
not re-run without it. **A clean run with no undo is the first thing to try if you ever do chase that
trigger.**

**D2 · `DRIVE-020`'s numbering half only.** The lens card and the target card **both render "01"**,
flagged in run 1 and again in run 2. The row's own note says the numbering is **"a pure display defect
and can be fixed without touching the router"** *(from the row)*. The Router already serves the right
positions — `state_lens` `01`, `choose_target` `02`, `state_purpose` `2.5`, `state_grain` `03`, from
`SEQUENCE` at `ml/router.py:244` *(re-derived at `f200f7a`)* — and `renderRepairGroups` already reads
`q.seq`. **So a surface is numbering itself instead of reading what it was served.**

**Leave the ORDERING half of `DRIVE-020` alone.** Moving the lens ahead of the diagnosis is a router
change and is not this loop.

---

# Part E — regression evidence

**`affected.py --since 07c25c6` at `f200f7a` reports 136 of 154 files from 28 changed paths, and it
escalates** — it prints, verbatim:

```
! the closure covers >=80% of the suite — this change reaches the API aggregator,
  so scoping buys little and the full sweep is the honest run.
ESCALATE — this change cannot be scoped:
  · docs/turbotab/tools/affected.py
```

**L58 met this by running the full suite and that was the right call.** Do the same if the machine is
free: **check `ps` first**, and the last clean baseline is L56's **2,464 passed · 17 skipped · 9
xfailed · 0 failed in 1:59:54 at `07c25c6`** *(from the row)*.

**If the full sweep will not fit, run the scoped selection and reproduce the escalation verbatim** —
do not paraphrase it into reassurance. **A scoped run is never reported as a full run.**

**And the fast tier is `~35 s`, not `~20 min`** — three `--ignore` paths, `AGENT_ONBOARD.md` §03, fixed
at `b4b8246` after it cost me 35 minutes. `TEST-078`.

---

## Deliberately not this loop

- **`DRIVE-034`** — the prevalence widget computed an **EAR cut-point** result for `kcal`, carrying a
  **SETTLED** badge. Energy may have no EAR in the sense the method requires. **This needs
  `research/NUTRITION_PACK.md` read, and the primary source if the pack is silent** — not a report
  quoted and not a grep. It is a pack question, and pack questions get their own loop.
- **`DRIVE-020`'s ordering half**, **`GUIDED-238`** (*85 items, 43 constant-reading, 16 falsifiable —
  from the row, and re-derive it before acting*), **`DRIVE-026`**'s preparation-mode row, and
  **`SEQN`/survey-design/pooled-cycle** flagging, which the tester raised again and which is
  `GUIDED-231`'s port rather than a defect.
- **L57's deferred B and C** — the ROC overlay and `GUIDED-233`'s explainability pack. **The overlay
  is still behind a surface no user can reach**, and Part A is what changes that.

## Rules that bind

- **A part may be refused when carrying it out would violate the criterion the row itself states.**
  Measure it, file it, hand the decision back, and **do not build a weaker version or your own.**
- **Where a row is about something a user does, drive the doing, not the resting state.** §00's own
  correction, and it is the adjudicator's error three times running.
- **A total revert, or the probe proves nothing.** A red quoting a `TypeError`, `ImportError` or
  `HarnessError` is red for the wrong reason.
- **A pin that cannot flip is a comment** — `TEST-077`. `pytest.xfail()` in a body never `XPASS`es;
  use `mark.xfail(strict=True)` on the case.
- **A class goes in the ledger the moment you name it.**
- **A matcher whose silence means nothing is not evidence.** Assert your reader read something first.
- **Say the number, including when it is zero.**

## Scope note

**If parts compete they lose from the bottom up.**

- **A wins, and A1 wins inside it.** A step that names its own failure is what makes the next human
  drive worth running; the retry is the quality of that, not its existence.
- **B is one sentence and should not slip.** It is the only part where the app currently states
  something false.
- **C may end in a measurement.** C1 alone is a good part.
- **D is fill-out. D2 is cheaper than D1** — trade freely and say what you dropped.
- **E is evidence, not work.** Never trade it, never report it as more than it is.

## What will be checked

- **Whether a failing `/models` now says so on screen**, proven by driving a refusing route — not by
  reading the handler.
- **Whether the retry actually re-fetches**, and whether the guard's new case **can fail**: make the
  fetch succeed and confirm the failure-path assertion goes red.
- **Whether B's sentence names the modeled base**, and whether the seal and the manuscript now agree
  **by test** rather than by review.
- **Whether C1 was driven to a fitted metric** rather than argued from the code, and whether the row
  was re-filed to match what it found.
- **Whether D1 was established before it was fixed** — I did not reproduce it and neither should you
  assume it.
- **`ps` checked. Part E quoted honestly, with the escalation reproduced if the sweep is scoped.**

Report once, in `AGENT_ONBOARD.md` §10's shape. **Your divergence section is the first thing I read.**
You have corrected me in each of the last three loops, and this prompt's §00 corrects a number I put
in my own L58 adjudication two days ago. **Check it too.**
