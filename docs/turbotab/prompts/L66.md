# L66 — the substrate: the page stops answering questions on the user's behalf

**Branch `TurboTab`. Read `docs/turbotab/prompts/AGENT_ONBOARD.md` first; everything in it binds this
loop.** The ledger holds **1,005 findings, 440 closed** (`OPEN` 482 · `PARTIAL` 83 · `FIXED` 430 ·
`NOT-A-DEFECT` 10) *(from `ledger.py stats` at `183dfe8`)*.

**Three agents run this loop and §00.6 is the coordination contract.** It is not advice: this project
has lost a shared data file to concurrent writers once, and `TEST-112` records one agent's stray
`conftest.py` silently poisoning another agent's measurement.

**Run the suite with `./venv/bin/python -m pytest`.** The system `python3` has no pytest and reaching
for it produces a false failure that reads like a broken tree *(measured both ways at `183dfe8`)*.
For the tools, `python` is not on PATH at all — use `python3 docs/turbotab/tools/…`.

**This prompt was refuted before it shipped, and eight blockers came back.** Every correction below is
in the text rather than in a footnote. Two of the eight were the adjudicator repeating a reader's
claim without opening the file, and one was the adjudicator quoting half a comment whose other half
was the counterexample. **Assume this prompt still contains one of those and say so when you find it.**

---

## 00 · What was measured, and what was corrected

### 00.1 · The DOM-write census

**Re-derive it yourself in one command:** `python3 docs/turbotab/tools/count_dom_writes.py`. That
tool is in the repository *because* three documents carried three different numbers for this
quantity, all of them produced by counting grep lines over a file whose own comments quote the tokens
being counted. It applies two independent methods and **fails the run if they disagree**.

| Quantity | Value at `183dfe8` | Why a grep gets it wrong |
|---|---|---|
| `.innerHTML =` assignments | **126** (zero `+=`) | A raw line count says **127**. The extra is `:3461`, prose quoting the L47 figure. `DRIVE-054`'s `act` says 127 and two readers repeated it. |
| ...whose value begins on the **next** line | **17** | `:2972 :4247 :4789 :5115 :5680 :5685 :5790 :5802 :6010 :6037 :6042 :6435 :6470 :6655 :6760 :6871 :7530` |
| `replaceChildren` call sites | **1** — `:3491`, in `deckClear` | 3 lines match; two are comments. |
| `insertBefore` call sites | **0** | 1 line matches; it is a comment. Exists only in the harness shim. |
| `startViewTransition` call sites | **0** | 2 lines match; **both are comments asserting the app has none.** `DESIGN_LANGUAGE.md:286` pre-registers this trap: *"Recorded here so the next person to grep does not read their own guardrail as a violation."* |
| `appendChild` · `removeChild` | **19** · **3** | |
| `scrollIntoView` | **1**, `:8462`, the rail's `data-map` branch | |
| `window.scrollTo` · `.scrollTop` · `scrollBy(` | **0 · 0 · 0** | The scroll diagnosis holds: no scroll call is involved in `DRIVE-054`. |

**A guard in the tree has a *latent* version of the line-broken blind spot, and the first draft of
this prompt got it badly wrong.** `test_the_deck_region_never_assigns_innerhtml`
(`test_the_deck_keeps_its_cards.py:93-126`) does **not** census the file. It slices the deck region —
`page.find("/* ═════════ THE DECK ═════════")` to `page.find("function renderStudy(){")`, resolving
to `index.html:3433-3638` — and asserts that region assigns `innerHTML` **zero** times. Its matcher
`\binnerHTML\s*=\s*[^=]` is applied per line, so it cannot see any of the 17 line-broken forms; and
**inside that region there are zero assignments today** (all five `innerHTML` occurrences there are
prose), so the hole currently masks nothing. The matcher already drops lines starting `*` or `/*`,
which is why it survives `:3461` sitting inside its own region.

**File it as a defect class stated honestly — 0 of 0 today, live the moment the pattern is copied to
a region that uses the line-broken form.** *Do not file the version this prompt first carried,* which
said the guard reports 109 of 126 sites and is blind to 17. That was false, and it would have put a
false line in a record whose whole premise is provenance.

### 00.2 · The cause is one render pass, and its guarding is ad hoc rather than absent

`index.html:3653` defines `renderAll()`, which invokes **18** sub-renderers unconditionally.
`renderAllGuarded` wraps it, and **all 6 call sites have the shape**
`.then(function(p){ P = p; renderAllGuarded("the project render"); })` *(read at `183dfe8`)*.

**The comment at `:3689-3693` is quoted here in full, because the first draft of this prompt quoted
only the half that supported its argument:**

> *"FETCHED ONCE PER STATE OF THE RECORD, not once per render. `/explain` refits the model and
> permutes every column, which is seconds on a wide assay table — and `renderAll` runs after every
> decision. The key is the record's own invalidation depth, so a new decision refetches and a repaint
> does not."*

So `renderAll` does run after every decision — **and the same comment describes a sub-renderer that
deliberately does not act on every call.** At least two sub-renderers already guard:
`renderExplainStep` early-returns on an unchanged cache key (`:3694-3696`), and the shelf writes are
wrapped in `if (list.innerHTML !== html) list.innerHTML = html;` (`:6463`, `:6466`).

**The defect is therefore not that guarding is absent. It is that guarding is ad hoc** — hand-placed
at two sites out of 126, with no rule saying where it belongs. Do not write, and do not let this
prompt tempt you into writing, that the pass has "two settings."

**The measurement that shows the harm is real**, driven at `183dfe8` through `pageharness.py` along a
user path (seal `clinical_labs.csv`, select three models, stamp every card's `dataset`, dispatch a
click on a genuine `data-keep-id` control which POSTs `/project/{id}/decision` and returns through
`renderAllGuarded`): the **deck**, written node-owning at `:3650`, kept **3 of 3** stamps; its two
immediate siblings in the same `renderStudy()` pass — `studyTable` and `studyTeach`, written by
`innerHTML` at `:3648-3649` — kept **0**. One press, one pass, one region survives and two are
destroyed and rebuilt.

### 00.3 · The rule is already written, and its own last sentence is the part that matters

**`MISC-021` (OPEN, high) is the class `L54-B0` said to file. Its `act`, verbatim and complete** — the
first draft of this prompt truncated it one sentence early, and the missing sentence is the criterion:

> *"NOT a sweep of the other 105 — that is churn and most of them redraw content whose identity
> nobody tracks. The act is a RULE plus a helper: any surface whose elements have identity a user
> follows … is written node-owning, and the deck's `deckCard`/`deckRow`/`deckClear` are the pattern
> to copy. A surface that merely reprints text may keep `innerHTML`. **The distinction is whether
> anything is supposed to SURVIVE the write.**"*

**Do not sweep the 126.** That is ruled against in the ledger, before this prompt existed.

**Where this prompt disagrees with that row, stated so you can judge it rather than inherit it.**
`MISC-021`'s criterion is *survival* — whether anything in the container is supposed to outlive the
write. `DRIVE-054`'s harm is *displacement* — whether anything above the pointer changed height.
**These are different questions.** A container that reprints text nothing was meant to survive still
moves every pixel below it when the new text wraps to a different number of lines.

*Falsification, and B0.2 is built to test it:* if the regions that change size during a decision turn
out to be exactly the regions with something meant to survive, the two criteria coincide on this page,
`MISC-021`'s rule is sufficient, and this paragraph is wrong. **Report which it is.**

### 00.4 · Three precedents, and two seams in them

1. **`deckCard`/`deckRow`/`deckClear`** — a keyed node cache. `:3509` `var card = DECK.cards[model];
   if (card) return card;`, `:3547` `var key = model + "::" + r.operation;`. **Keyed, not
   positional.** Driven above through a real control.
2. **`ownChild(parent, id, cls, tag)`** at `:5709` — create-or-reuse a child by id, ownership tracked
   on `_ownedBy`. **20 call sites besides its own definition** *(re-derived at `183dfe8`)*.
3. **The idempotent-write guard** at `:6463`/`:6466` — the write is skipped when the content is
   unchanged, so nothing reflows.

**Two seams, found by reading the precedent rather than by using it. Both go live the moment it is
generalized:**

- **The node cache never evicts.** There is no `delete DECK.cards[...]` in the file. Invisible at
  three models; a leak keyed on anything unbounded.
- **`deckRow(card, key)` ignores its `card` parameter entirely** *(`:3494-3505`)*. One global row
  map, safe only because the key embeds the model id. **A second container reusing a key would
  silently share a node.**
- **And `ownChild` has a third seam that Part C turns out to depend on: it reconciles only the
  children it owns, so anything a prior `innerHTML =` left in the same parent is orphaned
  permanently.** See Part C, instance three.

### 00.5 · What any guard here can and cannot prove

**`pageharness.py` has no layout.** `getBoundingClientRect` returns `{width: 0, height: 0}` with a
settable `_rect` top *(`:568-571`)*. **No guard this loop writes can measure displacement.** One that
appeared to would re-commit `TEST-113`'s error a level deeper.

**The showcase instance of that class, stated correctly this time.**
`test_the_page_never_moves_the_viewport.py` has **11 assertions, not 3**. Three are
`out["scrolls"] == []` (`:156`, `:173`, `:257`); four are source-text checks (`:184`, `:186`, `:269`,
`:273`); three are positive controls (`:153`, `:227`, `:256`); and **one asserts `out["scrolls"]` is
NON-empty** (`:230`), for the rail's single licensed scroll. `scrolls` is populated only by
`window.scrollTo` and `El.prototype.scrollIntoView` *(`pageharness.py:728`, `:585-586`)`*. **No
amount of reflow can redden any of the eleven.** The file's name promises the consequence; every
assertion measures the mechanism.

> **Operational warning for B1:** `:269` asserts `script.count("scrollIntoView") == 1`. If your
> conversions add or remove one, that test goes red **for the wrong reason**. Recognize it and do not
> "fix" it by moving the number.

**What the harness *can* prove, measured rather than assumed:** node identity survives and is
observable. Stamping node-owned children and repainting their container with `innerHTML` leaves 3
queryable nodes and **0** surviving stamps, while elements derived from assigned markup are stable
objects across re-queries and accept `dataset` stamps. **A red-first identity test against an
`innerHTML` container is writable today.**

**So the guard you write is an identity-and-structure guard, and its docstring says what it does not
cover** — that a surviving node whose text grew and wrapped to a second line still moves everything
below it, and that no instrument in this repository can see that. **That is the mechanical reason a
human drive gates this loop's acceptance.** The drive is not a precaution around the guard; it is the
only instrument that reaches the consequence.

The model to copy is `test_a_card_is_moved_rather_than_rebuilt_when_the_deck_reorders`
*(`test_the_deck_keeps_its_cards.py:239-251`)*: child count, document order, node identity by `===`,
and survival of a `dataset.stamp` written before the render — four assertions, one drive.

### 00.6 · The coordination contract

`LOOP.md` §05: **one writer at a time on `findings.json` and `register.json`.**

1. **Branches, and this is stated because git makes the naive version impossible.** `TurboTab` is
   already checked out in the primary tree, so a second worktree **cannot** check it out. Each agent
   cuts its own branch from `183dfe8` — `TurboTab-L66-A1`, `-A2`, `-A3` — in its own worktree.
   **Commit to that branch, never to a detached HEAD**, or the work does not survive. Agent 1 merges
   `-A2` and `-A3` into `TurboTab` at the end. **This is the one licensed departure from §04's "stay
   on branch `TurboTab`"; every other clause of §04 stands, and nothing goes near `main`.**
2. **No agent calls `ledger.py set` or `add` directly.** Each writes
   `docs/turbotab/tools/pending/L66-<AGENT>.py` — a committed Python file holding its exact ledger
   invocations, note text as Python string literals. This keeps the one-writer rule, satisfies *file
   notes through a Python file, never a shell heredoc*, and survives a cleared session.
   **`set` replaces the note — read the existing one and append.**
3. **Agent 1 runs the serializing pass** after the merges, in agent order, and commits
   `findings.json` **once**. **Check the diffstat**: `ledger.py` serializes at `indent=1` and a wrong
   writer reformats thirteen thousand lines.
4. **File ownership, assigned explicitly, because the first draft of this prompt violated its own
   contract:**
   - **Agent 1 owns `turbotab/web/index.html`** and everything under `turbotab/`.
   - **Agent 2 owns `pages/10_Report_Export.py`** — *including Part A's prose site at `:2102-2107`*,
     which is why Part A hands that one site to Agent 2 rather than doing it.
   - **Agent 3 owns `ml/import_doctor.py`, `ml/join_doctor.py`, `utils/combine.py`,
     `data_processor.py`.**
   - **If a fix reaches outside your set, stop and report.** That is `TEST-112`'s gate.
5. **Scoped test numbers only, quoted as scoped.** Nobody quotes a full sweep while another agent
   competes for the machine. **The machine sits beside where the product owner sleeps** — targeted
   files, and say the time of day in your report. **Do not run
   `test_the_page_never_moves_the_viewport.py` casually: it takes 124 seconds alone.** The four
   structural DOM guards run 27 tests in 7.45s and are your inner loop.

---

# Part A · `MISC-033` — prose asserting the opposite of what shipped

**Agent 1, except for one site. Plumbing with no unknowns, so it goes first** — `LOOP.md` §02, after
`L54` sequenced a mechanical part behind a discovery part and the mechanical part is the one that died.

L65 left seven sites across four files asserting the negation of what it shipped *(from `MISC-033`,
`ev`)*: `ml/manuscript_validator.py:221-227` and `:277-279`;
`turbotab/test_the_checklist_count_says_what_it_counted.py:49-56`, `:96-99`, `:113` and `:292`;
`pages/10_Report_Export.py:2102-2107`; and the `note` fields of two ledger rows.

**`pages/10_Report_Export.py:2102-2107` belongs to Agent 2** (§00.6.4). Agent 1 takes the rest.

**The sharpest instance is a tripwire:** the test file at `:49-56` writes down the condition under
which its own abstraction would be wrong, `:113` meets that condition two commits later, and a green
test at `:292` asserts `split.scored is fit` — the negation of two docstrings and two ledger notes,
one of them in the module that test imports as its authority.

**Prescription:** correct the prose to what the code does, and **prefer deriving a count in a
docstring from the code over restating it**, so the next commit cannot falsify the sentence.

*Falsification:* if any of the seven is in fact consistent with current behavior, do not "correct" it
— report which, with the measurement. **Verify the line numbers before you edit**; the row was filed
at a different commit and this loop's own Part D exists partly because `ev` citations in this ledger
drift.

**The ledger tool can repair all of this, and an earlier draft of this prompt said otherwise.**
`ledger.py set` writes `status`, `note`, `test`, `verified_ev`, and — since `7ef775a`, the commit
whose subject is *"the tool can repair the two fields it could only create"* (`TEST-109`/`TEST-079`)
— **`ev` via `--ev` and `act` via `--act`.** The subcommand parses with `allow_abbrev=False`, so
`--ev` and `--evidence` cannot be confused; `--evidence` still writes `verified_ev`.

---

# Part B · The substrate — the page stops repainting what did not change

**Agent 1. This is the loop.** `DRIVE-054` is `critical`, it is primary evidence from a human at the
screen, and it cost ten wrong state changes across six runs — **the target changed twice**, a lens
mis-ticked twice, a study objective set by a click that landed on a moving page. Each left a permanent
false line in a transcript whose entire premise is provenance.

`DESIGN_LANGUAGE.md:193-197` already rules the requirement:

> *"So whatever renders this app must mutate in place, not repaint."* … *"This is a requirement, not a
> stack choice."*

and names the mechanism at `:185-191`: `transitioncancel` does not fire when an element is removed, so
a repaint cannot report what it interrupted — **and the focus ring is lost, because the focused node
is gone and focus falls to `<body>`.**

## B0 · Instrument first, then measure. Do not skip to the fix

**This step exists because the adjudicator has three recorded instances of turning a correct
measurement into a prescription it did not license** (`PM_TRANSITION.md` §06.1, §06.5). **This prompt
refuses to name B's mechanism until B0 has run.**

### B0.1 — the instrument does not currently support B0.2, and fixing it is the first deliverable

A refuter **attempted** B0.2 as this prompt first wrote it and it cannot be done. Three obstacles,
each measured rather than inferred:

- **The hook is refused.** `pageharness.py:232` declares
  `Object.defineProperty(El.prototype, "innerHTML", {get:…, set:…})` with no `configurable`, which
  defaults to `false`. A drive body that wraps the setter dies with
  `TypeError: Cannot redefine property: innerHTML`.
- **The bootstrap is unobservable.** `run()` emits the drive body inside an async wrapper that awaits
  several turns first, so the body executes **after** the initial render. Measured: bootstrap writes
  visible to an instrument installed from a drive body — **0**.
- **The shim cannot tell `innerHTML` from `textContent`.** `pageharness.py:259-268` defines
  `textContent`'s setter as `this.innerHTML = String(v)`, so an `innerHTML` hook fires on
  `textContent` writes too. Measured over one drive: **16** distinct source sites fired, of which only
  **6** are genuine `innerHTML =` sites.

**So B0.1 is a harness change**, and it is `TEST-066`'s shape exactly — *an API the page never used is
an API the harness never implemented*, one door over. Make `innerHTML` configurable, and give the
shim a write log recording **the property actually assigned** and the source site. **File the harness
edit as its own row**, and re-take B0.1's four-API check afterward.

### B0.2 — the measurement, with a unit that survives contact with the data

During **one real decision driven along a user path**, record for every write on the `innerHTML`
channel: the target container, the assigning source site, **which property was assigned**, and whether
the assigned string **differs from what it replaced**.

**Report writes and sites separately.** A site is not the unit — the same site fires repeatedly within
one pass with mixed verdicts. **Expect most sites to be silent:** one drive exercised 6 of 126 (4.8%),
so 120 sites produced no datum at all, and any conclusion of the form "most sites…" has no denominator
a single drive can populate.

**Three branches, not two:**

- **Most firing writes are no-ops** → the idempotent guard at `:6463`/`:6466`, applied through a
  helper, removes the **no-op** reflow. **It does not remove all reflow**, and do not let anyone write
  that it does: a container's `innerHTML` is one blob (`studyTableHTML()` returns heading, table and
  paragraph together, `:3383-3395`), so a single changed cell reassigns and reflows the whole region.
- **Most firing writes genuinely change** → keyed reconciliation on the regions that move.
- **Most sites silent and the few that fire mixed** → **scope the build to the firing set and say
  which regions it covers.** This is the outcome one drive already suggests; treat it as likely.

**One bias to disclose in your own report:** `:6463`/`:6466` are already guarded, so they can never
land in the no-op bucket. The two sites this prompt offers as the first branch's model are
pre-excluded from that branch's evidence.

**Falsification of the whole framing:** if the regions rewritten during a decision all sit **below**
the answered card in document order, nothing above the pointer moved, `renderAll` is not the cause,
and `DRIVE-054`'s `act` is wrong. **Report that and stop** — it would be the most valuable result this
loop could produce.

## B1 · Build what B0 licensed, hardest region first

**Order hardest-first** (`LOOP.md` §02), judging hardest by what is most likely to break the
abstraction. `studyTable` is the *easiest* — rows already keyed by a stable server id (`c.name`),
homogeneous `<tr>` of three cells, every cell escaped text *(`:3385-3388`)* — so it is the **control**,
not the first build. Take a region with heterogeneous children or no natural key first, and **say how
it bent the abstraction**.

**The two seams in §00.4 go live when you generalize.** A cache that never evicts and a row lookup
that ignores its parent are safe at three models and not safe keyed on columns. **Fix them as part of
generalizing, or state in a comment why they are still safe.**

**Ship the guard with the build** — an identity-and-structure test over the regions you converted, red
when the fix is reverted. `revertprobe.py` does textual reverts on any repo-relative path, so this is
a real probe. **The docstring states the limit** (§00.5).

**`pageprobe.py` carries a standing trigger naming this exact situation** — *"run it in any loop that
edits `web/index.html`"*. It is a ~6-minute mutation sweep that once found three tests green against
an empty body. **Run it, and say when.**

**Deliberately unbuilt:** no animation, no `startViewTransition`, no FLIP. §05 rule 5 is *"Nothing else
moves."* This part removes motion nobody asked for; it does not add motion. **And do not sweep the
126** (§00.3).

---

# Part C · The panels that never learn

**Agent 1.** Three instances, and **the first draft of this prompt got their relationship wrong in
both directions** — first claiming C consumed B's mechanism, then over-correcting to "B ships nothing
C can use." **The measured truth is split:**

- **Instances one and two need repairs B does not ship.**
- **Instance three is a node-ownership defect and B's rule is exactly its repair.**

### C1 · `DRIVE-056` — the reviewer panel

**`DRIVE-056`'s `act` field is false and you should not follow it.** It says *"give the manuscript
builder the run."* The builder **already receives the run**:
`manuscript.structure(project_dict, *, run=None, …)` folds it through `_with_run_sections`, and
`api.get_manuscript` supplies it at `api.py:2830` (`run=held["run"].to_dict() if held else None`).
**`GUIDED-116` is `FIXED` and its own `act` records that exact repair.** A builder sent to `draft.py`
would find the work done. This is the `GUIDED-236` shape a second time — an `act` field sending a
builder to the wrong file.

**Driven over the real route chain** (`POST /project` → decisions → seal → event → `POST /train` → job
settle → `GET /manuscript`), a fitted project returns `unsourced_sections: []`, `passed: True`, header
**`11 checks, 0 unmet · 2 declared`** — byte-identical to the number L65-B was accepted on. **The
server is not the defect.**

**The break is that the page never asks.** After the fit press the only routes fetched are
`POST /train`, `GET /job/…`, `GET /training`, `GET /figures`. The site is `pollJob`'s terminal branch
at `:6496-6500`, which calls `renderTrainState()` and `renderFigureSurface()` and nothing else.
`renderReportStep()` — which issues `/draft`, `/manuscript`, `/checklist` — is reached only from
`renderAll()`.

**And the freeze is recoverable**, which narrows the row rather than the finding: any subsequent
decision re-fetches the panel, so the post-fit state **is** reachable by a real user, one click later.
**My withdrawal of the L65-B post-fit claim was right for a smaller reason than `DRIVE-056`'s note
states, and the note should be corrected to say so.**

*Falsification:* if adding `renderReportStep()` to `pollJob`'s terminal branch does not move the header
on a driven fit, this diagnosis is wrong. **Check before you write the row.**

### C2 · `DRIVE-060`'s Explain half — a cache key that cannot see a fit

`renderExplainStep`'s guard at `:3694-3696` keys on
`(P.stale_downstream || []).length + "/" + (P.target || "")`. **Fitting a model records no decision,
so neither term moves and the key is unchanged.** Measured: in a drive where a decision *did* fire
`renderAll()`, `/manuscript` was re-fetched and `/explain` was **not**. So repairing `pollJob` closes
C1 and leaves *"No model has been fitted yet"* live after a fit.

### C3 · The seal sentence — and it is a leaked node, not a state bug

**This was driven at `183dfe8` and the answer is not what the first draft of this prompt assumed.**
It told you to find why `SHELF` was null or `SHELF_WAITING` still set. **Both are false of that
state**: at the moment the sentence is on screen, the shelf has rendered correctly and
`SHELF_WAITING` is null. A builder sent to hunt a state bug would find nothing and burn the part.

**The cause:** `renderTrain`'s waiting branch writes `$("shelfBox").innerHTML = '<p …>'` at `:6435`.
The success path at `:6455-6475` **never clears `shelfBox`** — it only `ownChild`-appends
`shelfDesign` (`:6461`), `shelfList` (`:6464`) and `shelfAct` (`:6468`) **into** it. Nothing in the
file ever removes that paragraph. `grep -n shelfBox` returns writes at `:6433`/`:6435` (waiting),
`:6446`/`:6447` (failure), `:8484`/`:8488` (retry), and a read at `:6458` — **and no clear anywhere.**
So the sentence survives every later render for the life of the session, sitting above the shelf.

**This is a fourth cause class, and the first draft's taxonomy could not receive it:** not a missing
re-render, not a stale payload, not a constant string, but **a container written by `innerHTML` in one
branch and by `ownChild` appends in another, where the append path never clears what the assign path
left.** It is `ownChild`'s third seam (§00.4) as a live defect, and **it is exactly what B1's
discipline fixes** — which is why C3 and B1 touch the same function (`renderTrain`, `:6420-6478`) and
must be done by the same agent in the same pass.

### The rule for all three

**Each fix ships with a test driven along the path a user walks** — `LOOP.md` §06's new check, which
exists precisely because `DRIVE-056` was created by a probe that handed the harness a state the
interface never delivers. **Ask of every acceptance probe: how did this state arise?**

---

# Part D · Backlog closure, in separated areas

The sprint L58–L65 closed **2** of roughly 520 pre-existing open findings, and the product owner ruled
that indefensible: *"Not a policy I would defend at all."*

**Two corrections to how this backlog is usually described:**

- **It is not unverified.** It was re-read at L1 on 2026-07-27; 75 of 78 STATE rows and 63 of 65
  CONTRACT rows carry a distinct `verified_ev`. **The re-read is 26 days stale.**
- **Line numbers in `ev` and `verified_ev` are systematically wrong** — `STATE-015` cites `:1615` and
  is at `:1737`; `CONTRACT-019` cites `:736-745` and is at `:845-854`. **Greping the cited line,
  finding nothing, and concluding `FIXED` will be wrong. Re-locate by symbol.**
- **About 49 of the 143 STATE+CONTRACT rows are not closeable defects**: 25 STATE rows carry
  `act: preserve` (port invariants) and 24 CONTRACT rows carry `act: project` (a boundary catalogue).
  Do not count them toward any target.

## D1 — Agent 2 · STATE and CONTRACT, and `pages/10_Report_Export.py` is yours

**You also own Part A's prose site at `pages/10_Report_Export.py:2102-2107`** (§00.6.4). Do it first;
it is small.

**One free closure**, already fixed with the ledger merely stale:

1. `CONTRACT-067` [low] → **FIXED**. `models/rf.py`; the duplicate was deleted in L64-A5 and the
   comment at `:87-93` says so. Test: AST-parse every `models/*.py`, assert no class body defines one
   name twice.

**`STATE-020` was on that list in the first draft of this prompt and it is NOT fixed.** A refuter read
`build_export_context()` (`pages/10_Report_Export.py:321-373`) and it returns **no**
`manuscript_context` key; the only producer of that key is `_build_latex_export_bundle` at `:1045`, a
different dict that never reaches `export_ctx`. So the read at `:1428` is `None` on every run and the
baseline section is skipped — **exactly what the row records.** The reader that called it fixed
reported confidence *read*, not *measured*, and never opened `build_export_context`. **Repair it, do
not close it**, with a test asserting the report contains the baseline heading when baselines exist.

**Then, reproducing and single-file, each with a clean revert test:**

2. `CONTRACT-010` + `STATE-017` [high + landmine] — **two rows, one fix.**
   `pages/06_Train_and_Compare.py:1575-1576` reads a session key nothing writes; use
   `task_type_final_local` from `:1548`.
3. `STATE-015` [landmine] — `pages/02_EDA.py:1737` reads `high_pairs`; the writer
   (`ml/eda_recommender.py:511,519`) writes `high_corr_pairs`. **Test the class**: assert every key
   page 02 reads off `collinearity_summary` is one the recommender writes.
4. `STATE-021` + `CONTRACT-008` — two rows, one fix. `data_audit` lacks `missing_counts`;
   `pages/10:805,836` both test for it.
5. `CONTRACT-019` [high] — `utils/workflow_provenance.py:845-854` catches only `ImportError`. Test by
   calling `get_provenance()` from a `threading.Thread`.
6. `STATE-005` [landmine] — `utils/session_state.py:533-539` falls back to an arbitrary pipeline.
7. `STATE-018` [landmine] — `pages/10:968` reads `sensitivity_feature_dropout`; `pages/08:499` writes
   `sensitivity_dropout_results`.
8. `STATE-045` [landmine] — `utils/session_state.py:387` restores `feature_cols` only when already
   truthy, while `:383` pops the source regardless.
9. `CONTRACT-006` [critical] — `utils/session_state.py:675`, a blanket `except Exception: pass`.
   **Highest severity here and the widest blast radius. Take it alone or not at all.**

**Refuse these and say why rather than rediscovering it:** `CONTRACT-004` is blocked on a product
decision — its own `act` says *"decide deliberately"*. `CONTRACT-020` has drifted and can close
`PARTIAL` at best. `STATE-023` and `STATE-003` have unconfirmed premises.

**Do not drive these with `pageharness.py`** — it exercises the TurboTab web controller and reaches
none of the Streamlit app where every one of these defects lives.

## D2 — Agent 3 · IMPORT, and the freeze gate it moves

**Read `TRANSITION_PLAN.md` §05 and quote it back in your report.** Frozen paths:
`ml/import_doctor.py`, `ml/join_doctor.py`, `utils/combine*.py`, `pages/01_Upload_and_Audit.py`. **The
freeze is LIFTED FOR REPAIR** — a fix to a dispositioned `IMPORT-*` row may land with a named
regression test. Only **new construction** waits on the gates. **Everything below is repair.**

**Gate 2 requires that no `IMPORT-*` row of severity `critical` or `landmine` is `OPEN`. 23 rows block
it at `183dfe8`** — 16 critical, 7 landmine *(re-derived from `findings.json`)*. **Do not repeat §05's
"25 rows"; it is stale and counts `PARTIAL` rows the gate's wording excludes.**

**The 2^53 identifier collapse — `IMPORT-209` + `IMPORT-234` + `IMPORT-260`.** All three driven at
`183dfe8`: the JSON loader turns one blank ID into a merged pair; `normalize_key` canonicalizes the
already-collapsed digits as an exact match; `execute_stack` widens int64 to float64 with
`plan.warnings`, `plan.notes` and `plan.type_conflicts` **all empty**.

**Four corrections to how the first draft of this prompt described the repair, all measured:**

1. **There IS a partial shared surface.** `utils/combine.py:313` imports `normalize_key` from
   `ml.join_doctor` inside `_same_people` — a function-local import a top-level scan misses. **A
   change to the canonicalizer also moves the stack-versus-link decision, so drive `_same_people` as
   part of the `IMPORT-234` repair.** `data_processor.py` imports neither, so the JSON path still
   needs its own fix.
2. **The predicate cannot carry the repair on its own.** `numeric_conversion_would_lose`
   (`ml/import_doctor.py:228`) is real and its docstring is verbatim as quoted — but **both production
   callers are gated on text** (`:269` `if not _is_text(s): continue`, and `:654`
   `_each_column_matching(df, _is_text)`), so a dtype branch inside it is unreachable from either.
   It has **two** production call sites, not three.
3. **On a column that is already float64 the digits are gone before the predicate can see them**
   (driven). So the predicate can **warn**; it cannot repair `IMPORT-209`, whose loss happens at load.
   **The repair has to sit where the widening happens.**
4. **The "cheapest regression test" claim was wrong and its replacement matters.** A float64 fixture is
   **not** a usable test: `pd.Series([9007199254740993.0, 9007199254740992.0])` already holds one value
   twice at construction, so `tokens[0] != tokens[1]` becomes **permanently red** — no implementation
   could make it green. **The test must carry the distinction in a form float64 has not already
   destroyed** — a text or int64 column handed to the loader or the stack — and must assert on the path
   that widens it, not on `normalize_key` of an already-collapsed series.

**`IMPORT-249` is dispositioned in its own row as a duplicate of `IMPORT-142`**, so one repair to the
collision list at `ml/join_doctor.py:771-773` may close **two** landmine gate-2 rows. **This was read,
not driven — verify before you count it.**

**`IMPORT-219`** [landmine] — driven on a 200-column fixture: with integer gene counts the subject
column is never inspected and `suggest_best` returns `None`; cast the **non-key** columns to float64
and it returns the right pair. **The dtype of the columns that are not the key decides whether the key
is found.** The defect is the tuple at `ml/join_doctor.py:497`. **Do not extend the name-hint list** —
the row and the constitution both forbid it.

**Do not write that closing `IMPORT-257` "closes all four" of `020`/`022`/`219`/`257`.** `IMPORT-020`
and `IMPORT-022` are `PARTIAL` and are not gate-2 blockers, and `IMPORT-219` is a different repair in
a different function.

---

## 01 · Deliberately unbuilt, and say so in your report

- **`IMPORT-257` / `DRIVE-057` — the subject-column lever.** These are **the same missing lever seen
  from two doors**: `CohortStructureDetection` (`utils/session_state.py:31-56`) already carries the
  candidate fields and resolves `entity_id_final`; six production modules consume it correctly;
  `ml/triage.py:139` already computes the answer; and **nothing in production writes any of the four
  fields.** The app holds the answer it says it lacks, in both doors. **It is the rule+lever fusion
  the product owner has ruled the shape of, and it is the strongest user-facing work available — which
  is why it is not being squeezed into this loop's margins. It is L67's Part B.**
- **The `TEST-113` audit.** As filed — *"walk the repository's guards"* — the population is 316 test
  files, 103,360 lines, 9,907 assertions. That is three loop-parts. **The scoped population is
  measured and waiting**: 71 files drive the page through the harness, 17 name a DOM mechanism, 7
  files and 16 assertions assert on a DOM mechanism directly. Start at the 17, triage against the 71.
  **L67 carries it, with those numbers in the prompt.**
- **No new interaction patterns.** This loop removes an anti-behavior and repairs re-render gaps.

**And the fan-out control arm is designated, so that it cannot quietly become never.**
`RETROSPECTIVE.md` §07 assigns the half-size configuration to *"the first backlog-closure loop — the
cheapest, best-understood kind of loop the method now has."* **L66 is not that loop**: it is a
substrate loop carrying a closure slice, its central build is the least-precedented thing in the
project, and its own prompt was refuted eight ways before shipping. A cost experiment run here would
measure this loop's variance rather than the fan-out's value, and a confounded control arm is worse
than none. **So L66 keeps full-size refutation, and L67 is designated the dedicated backlog-closure
loop that carries the half-size arm** — recorded here rather than left as an intention, because "we
will do it next time" is how the frozen backlog happened. The product owner may overrule this.

## 02 · Scope note

**If B0 runs long, that is the loop working.** B0 is the step whose absence let `L54-B0` be built on a
false instrument assumption, and this prompt's own B0.2 was refuted for the same reason before it
shipped. **Trade B1's breadth, not B0's rigor** — convert fewer regions and keep the guard.

**If B and C compete, B wins — except C3**, which is inside B1's pass and costs almost nothing extra.
**If A is not done before B starts, A is what got lost.**

**Part D is independent by construction.** If Agent 1 overruns, D still lands.

## 03 · What this loop owes the record

- The `LOOP.md` §03 row, written by the adjudicator on acceptance.
- **A row for the latent matcher blind spot** (§00.1), stated as 0-of-0 today — **not** the false
  version this prompt first carried.
- **A row for the harness change** B0.1 requires.
- **Corrections through `ledger.py set --act` / `--ev`**: `DRIVE-054`'s `act` (126, not 127);
  `DRIVE-056`'s `act` (the run already reaches the builder) and `note` (the state is reachable one
  click later); `MISC-021`'s note (whichever way B0.2 settles §00.3's disagreement).
- **`STATE-020` corrected in whichever direction the work goes**, since a reader called it fixed and
  it is not.

**A drive gates this loop's acceptance and the product owner has ruled it.** Reflow is what no harness
can feel, and §00.5 is the mechanical proof rather than an intuition.

---

## 04 · Guardrails — `LOOP.md` §05, binding every agent

> **Hard rules.** Never push to `main`, never force-push, never open a pull request.
> `ml/import_doctor.py`, `ml/join_doctor.py`, `utils/combine*.py` and `pages/01_Upload_and_Audit.py`
> are **frozen — see `TRANSITION_PLAN.md` §05 for the one statement of what that permits and the
> gates that lift it.** Never edit `FINDINGS_LEDGER.md` by hand; it is generated. Never mark a finding
> `FIXED` without a regression test **verified to fail when the fix is reverted**. **First command in a
> fresh clone:** `git config core.hooksPath .githooks`. Commit after every batch so nothing is lost.
> **Domain science comes from `docs/turbotab/research/`, never from recollection** — where a research
> file and your memory disagree, the file wins, and a number marked `[verify-at-build]` may not ship as
> a hard-coded constant. If you are blocked or something looks structurally wrong, stop and write what
> you found to `docs/turbotab/BLOCKED.md` rather than guessing.

**Branch clause, amended for this loop only:** §00.6.1 licenses `TurboTab-L66-A1/-A2/-A3` cut from
`183dfe8`, because git cannot check `TurboTab` out twice. Agent 1 merges them back. Nothing else about
the rule changes.

**Standing rules the agents earned — do not re-derive them:**

- **A `FIXED` arrives with its probe output or it is `PARTIAL`. The revert must be total**, and a red
  quoting a `TypeError`, `ImportError` or signature mismatch is red for the **wrong** reason. The cheap
  detector: run the loop's new tests with the loop's own source diff reverted.
- **A part may be REFUSED when carrying it out would violate the criterion the row itself states**, and
  a refusal is a result. **Refuse, measure, file, and hand the decision back** — do not quietly build a
  weaker version, and do not quietly build your own.
- **A pin that cannot flip is a comment.** `mark.xfail(strict=True)`, not `pytest.xfail()`.
- **A grep answers "does this text appear", not "does this run."** §00.1 is what that costs on this file.
- **A matcher that fires on prose has silence that means nothing** — and writing *about* a matcher is
  enough to trip it. Name the class; do not quote the tokens.
- **A class goes in the ledger the moment you name it. Say the number, including when it is zero.**
- **Never `git add -A`.** Stage explicit paths and check the diffstat.
- **Never accept a moved threshold in the same loop as the change that pressured it.**
- **Ambiguity is `OPEN`, never `FIXED`.**
- **Write American on the first pass** — a pre-commit gate enforces it.

**Six pre-commit gates run on every commit** (`parsecheck.py`, `ledger.py check`, `register.py check`,
the spelling test, `copydeck.py check`, `evidence.py check`). Refusing a bad commit is the system
working. **Bypass with `--no-verify` only with a stated reason.**

**Three guards parse `AGENT_ONBOARD.md` §03**, all in
`tests/test_a_fixed_rows_named_test_resolves_in_five_seconds.py`. Five lines there carry the parallel
invocation and **three are rows of a measurement table**; every one must keep `--dist loadfile` or the
guard goes red. **Do not touch that block or that table this loop.**
