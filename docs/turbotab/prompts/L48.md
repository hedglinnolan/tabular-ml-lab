# TurboTab — the L48 loop

**If you are a fresh agent, read [`AGENT_ONBOARD.md`](AGENT_ONBOARD.md) in full first.**

**Five parts. This loop finishes the drive.** L47 landed six of seven and traded Part F whole, which
the scope note permitted — so the thirteen rows the product owner's NHANES drive produced are still
open and they are Part B here. He has ruled they outrank other scheduled work; that ruling still
stands.

**L47 was accepted, six of seven.** The adjudicator drove the load-bearing claims rather than
reading them, and two of them came back **better than the report**. §00 is the response you are owed
and it includes one row reopened against you.

---

## 00 · Your divergence section, answered — and you were right three times

**You corrected §03 in five particulars. Three are mine and I own them.**

1. **The fixture asymmetry does not exist.** All three shipped NHANES fixtures carry `SEQN` as
   `int64` 1..120 and `identifiers.detect` flags it in all three. The reconnaissance checked
   `nhanes_dietary.csv` and **I generalized one fixture to three**, then built an instruction on it.
   Your conclusion is sharper than the instruction I gave: the real file's condition cannot be
   reproduced from any fixture, so the refusal has to hold without detection **by construction** —
   which is what `test_the_refusal_holds_when_nothing_detects_an_identifier` asserts.
2. **`:5927` is `data-att-send`, not a Feature control.** Carried from the recon without checking.
3. **The delegate test had two structural bugs and I named one.** The winner-takes-all
   `if len(found) > len(best)` would have hidden a delegate even with the parameter name fixed.
   That is a better find than mine and the comment you left on it is the right length.

**Two are half-corrections, and the precision matters because precision is this project's safety
property.**

- **`setErr` sites.** §03 said *"sixteen **message-writing** `setErr(` call sites"*. Your twenty is
  the total — sixteen write a message, four clear it. Different denominators, not an error.
- **`bp_di`.** You are right that it carries no digit and my hazard example was wrong. But *"the
  sentence backticks its object anyway"* is **false of the pre-L47 code** — it interpolated the
  column bare, `"Entries of " + t.dataset.impossible + " outside…"`. **Your own B2 fix added the
  backticks.** Take the credit; the finding was real and you closed it.

**And you were right to doubt `GUIDED-165`'s evidence line.** Driven and confirmed: the two buttons
did record distinct sentences. The defect was that one of them was false and that nothing
machine-readable distinguished a repair from a refusal to repair — which is what your two kinds fix.

### What the adjudicator drove, and what it found

**B2 is stronger than you reported.** On `clinical_labs.csv`: `n_impossible` **4 → 0**, `sbp` gone
from the impossible list entirely, the record reading *"4 entries of `sbp` outside the impossibility
band of 40.0–300.0 mmHg were set to missing"* with a payload carrying `n_set`, `low`, `high`, `unit`,
`tier`, `band`. **And the control holds** — `keep_impossible` leaves 4 → 4 and records *"were kept as
recorded"*, which is now true. Two kinds rather than one was the right call and the reason you gave
is the right reason.

**B1 verifies with its positive controls.** `SEQN`, `WTDRD1`, `SDMVSTRA` and `environment_score` all
refuse, each naming what the column actually is, as a **200** carrying `forbidden:
prevalence_of_inadequacy` and the house `per_nutrient_distribution` offer. `calcium`, `energy_kcal`
and `protein_g` still answer. **And `fiber_g` refuses on the AI axis**, which is the correct
pre-existing science rather than a false refusal — the registry-not-substring decision is what makes
that hold, and `"iron" in "environment"` is the right reason to have made it.

**E verifies, and your own caveat was too conservative.** `None` is gone — *"Expected analysis N not
estimable… This check has nothing to compare"* — and the header/list mismatch is resolved the honest
way, by giving unsourced sections their own category rather than making the header read the list.
You wrote that the served sentence *"reaches no screen yet"*; the page reads `r.Detail` and the
server serves `Detail`. It reaches the screen.

### The one row reopened against you

**`GUIDED-167` goes back to `PARTIAL`.** The mechanism is right and it stays.

Three of thirty posting controls emit a slot — `find-`, `earmark-`, `feat-add-` — and
**`data-miss-choose` is not one of them.** So on the press the product owner actually made,
`t.getAttribute("data-ac")` is null, `AT_CONTROL` becomes `""`, `atControl("")` returns false, and
the typed 409 lands only in `#upErr` — still inside `#sub-upload`, still hidden by `renderData()` at
`index.html:2952`. **The row's own reproduction still produces nothing a person can see.**

Your note states the limit and Part D publishes the number, and that honesty is why this is a
reopening rather than a rejection. But the row was `FIXED`, and the standing disposition is
**ambiguity is `OPEN`, never `FIXED`** — and this is `MISC-019`'s exact shape: a row describing a
class, a test covering a different instance, and the closed count moving anyway. Part A closes it
properly.

**And one trap #3b, on the loop that was handed trap #3b in writing.**
`test_a_refusal_lands_beside_the_control_that_caused_it`'s docstring says *"The press is a
`data-feat-add`"*; the body dispatches `data-dismiss`. Every assertion in it is true and the
description is not — which is the variant where reading assertions never finds it.

**Part F traded whole was the right call** and the scope note permitted it. Reporting zero of
thirteen as zero is what the report is for. §02 has the change that stops it costing thirteen next
time.

---

## 01 · State

Branch `TurboTab`. Ledger **795 findings, 299 closed** — you reported 300 and `GUIDED-167` has been
reopened to `PARTIAL` since. Register **181 rows**. Six gates green.

Re-measured by the adjudicator at `ce41cbe`:

| Suite | Result |
|---|---|
| `tests/integration` | **211 passed** |
| `tests/` (minus integration, minus `test_nn_modernization`) | **1655 passed, 4 failed** — the four known environmental |
| `turbotab/` | you reported 1730 / 0 failed / 15 skipped / 17 xfailed |

`make test` still aborts at collection on `TEST-038`. **Do not work around it.**

---

## 02 · How to run this loop — fan-out, corrected by what happened last time

**Both of Part F's subagents died on the session limit and you had held their edits back, so
thirteen rows cost thirteen rows.** Holding the edits was right — an edit applied by a dying agent
is worse than no edit. **The batching was what failed.** Three changes:

- **Chunk B into runs of two or three rows, not one run of thirteen.** A subagent that dies takes
  its chunk, not the batch.
- **A subagent returns a unified diff and a test, as text, in its final message.** You apply it. That
  is unchanged and it is why last loop lost nothing — but a diff that arrives is worth more than a
  perfect one that does not, so **tell each subagent to report progressively**: the diff for row one
  before starting row two.
- **Commit after each chunk lands and its targeted tests pass.** Eight commits last loop was the
  right granularity; keep it.

**Everything else about fan-out is unchanged and it worked:**

- **Fan out on B, hard**, ordered hardest-first — judge hardest by what is most likely to break the
  slot pattern Part A generalizes, not by effort.
- **Fan out on reading, anywhere, freely.**
- **Do NOT fan out Part A or Part C.** A generalizes one mechanism across twenty-seven controls and
  wants one hand on it. C is a contract decision per kind and each row is a claim that needs driving.

**The rules a subagent must never break**, and they held perfectly last loop — keep them verbatim:

1. **`findings.json` and `register.json` have exactly one writer: you, serially, through
   `ledger.py` / `register.py`.** No subagent runs `add`, `set` or `regen`.
2. **No subagent commits.**
3. **No subagent runs a full suite.** `TEST-040`.
4. **Two subagents never edit the same file** — and most of B touches
   `turbotab/web/index.html`. Serialize through yourself.
5. **A subagent that finds something outside its brief reports it and does not act.**

---

# Part A — the two gaps L47 left

**Small, and A1 is the completion of L47-C rather than a new design.**

**A1 · `GUIDED-167` — the slot reaches the control the row was filed about.** L47 built
`atControlSlot` / `atControl` and wired three controls. **Generalize it to every control that
posts.** Part D's sweep already publishes the list: thirty post, three can answer, five are
unconstructible by a synthetic press, twenty-two have no slot, two are conditional.

**Start with `data-miss-choose`**, because that is the press in the row's own evidence —
`route_missingness` with `mechanism=informative` and `card_option=impute_median` returns a typed 409
naming the column and counting the blanks, and `card_option=drop_rows` returns a 400 with the
complete-case explanation. **Both are exactly right and neither reaches a person.** Then the rest.

**Where a control genuinely should not carry a slot, say so in the sweep rather than skipping it** —
a control listed with a reason is a decision and a control silently absent is a hole, which is the
same distinction `GUIDED-180` is about one layer down.

**A2 · The docstring that names a control its body does not press.**
`test_a_refusal_lands_beside_the_control_that_caused_it` says *"The press is a `data-feat-add`"* and
dispatches `data-dismiss`. **Trap #3b.** Either press what the docstring says or say what the body
presses. And **file the class if you find a second instance** while you are in there — a test
description that overstates is a claim with no record behind it, and the sweep is the natural place
to detect it.

---

# Part B — the thirteen drive rows

**The wide part. Fan out. Order hardest-first. Report what you did not reach.**

These are unchanged from L47's Part F. Judge "hardest" by what is most likely to bend Part A's
generalized slot pattern — several of these are controls that will need one.

| Row | Sev | What it is |
|---|---|---|
| `GUIDED-163` | high | Median fill offered on a column the constitution **blocks**. `meds_hbp` observed `{True: 5527, False: 770}` with 15,552 missing; the median is 1, so it assigns **every person of unknown medication status to being on blood-pressure medication**, taking the column to 96.5% ones with *"not asked"* and *"yes"* encoding identically. `blocks('informative','impute_median')` already returns `True`. **It sits second in a list of two under a heading reading "What the app can do."** The shelf is never shortened — ordering and a stated concern, not removal. |
| `GUIDED-159` | high | The analysis map can never mark Train as `now`; only `target` and `eda` can. And `DESIGN_LANGUAGE.md` §04 specifies **four** states where the CSS defines **three** — there is no `waiting`, so a step never reached and a step that is next render identically. He read the absence as a verdict about availability. |
| `GUIDED-168` | high | A borrowed label promising a different analysis. *"Missingness Pattern Analysis"* is the **core's** title (`ml/eda_recommender.py:292`) with a stated deliverable including MCAR vs MAR vs MNAR; the Guided endpoint returns per-column *"is the missingness informative?"* cards — which the server's own capability table correctly calls **"Missingness by feature."** |
| `GUIDED-157` | high | The bulk binary repair records **which columns** and never **which value became 1**. `gender ∈ {female, male}` — so *"is the coefficient on male or on female"* has no answer in the record. Produces no wrong number and makes a reported number **uninterpretable**. Note L47's B2 is the model: two kinds where one was ambiguous, and a machine-readable payload beside the sentence. |
| `GUIDED-164` | high | Three plots for a fifteen-column finding, five chips, **neither labeled**. The pager **is already built** — `/evidence/histograms` returns `n_features: 26, per_page: 6, n_pages: 5` and the handler already has `data-hist-page`. Reachable from everywhere except the card that motivates it. |
| `GUIDED-156` | high | *"Ask me anyway"* reopens a question that renders **nowhere**. Two correct rules composing into a hole: the skip renderer draws `status === "skipped"`, `renderAsked` draws `status === "asked" && !handledElsewhere(key)`, and `confirm_task_type` is on `HANDLED_QUESTION_KEYS` — so once reopened it is **both**, and neither renderer takes it. **`test_ask_me_anyway_reopens_the_question.py` holds six passing tests and every assertion is true**; they watch the wire and the record and **none renders the page after the reopen.** That coverage gap is the finding. `handledElsewhere` also matches eight **prefixes**, not just the exact-key list. |
| `GUIDED-158` | medium | Nine assertions, **eight false about the data.** The card titles every hit *"is a binary variable written as text"*; six of nine are dtype `bool`, two are `object` holding Python `True`/`False`, and **only `gender` holds strings.** Nothing downstream is wrong. **The cost is trust** — first card on the file, nine claims, eight wrong about something the reader checks in one glance at their own CSV. |
| `GUIDED-166` | high | The impossibility pass **manufactures blanks nothing can distinguish.** Explore asks the mechanism; the impossibility card two cards later **now actually creates** blanks — L47-B2 made it real, which raises this row's severity in practice. Nothing marks a blank's provenance. **His three instincts map onto three real routes and the app offers one**: set-to-missing is clause 06's row-local repair (now built), exclude-the-rows is clause 04's eligibility criterion — and `ml/pipeline.apply_plausibility_filter:109` **already implements it** — and mark-the-column-corrupted is `GUIDED-096`'s split. |
| `GUIDED-176` | medium | *"Show me what it does"* renders nothing for the user-supplied cut-point branch. **Sibling of `GUIDED-175`**, which L47 closed. `api()` attaches `err.detail` only when the server's `detail` is an **object**, and `FeatureRefusal` → `HTTPException(400, str(exc))` sends a **string** — so `showRefusal` never fires. **This is every 400 in the app**, 55 sites, 44 of them `str(exc)`, not one path. **Note `GUIDED-182`**: no shipped fixture reaches the offer-preview branch at all, so decide whether you are fixing this or proving it unreachable. |
| `GUIDED-171` | high | The feature-engineering before/after shows only the **first operand**, so a two-column formula previews as one. `/features` is on the field sweep's **exemption list**, which is why `needs` and `all_columns` have no reader and nothing caught it. |
| `GUIDED-177` | high | The feature-selection evidence table **always ranks by absolute Pearson correlation** regardless of the method chosen. The absent-score vocabulary already exists in this exact payload and renderer — `turbotab/selection.py:181-184` returns `{"score": None, "measure": "not numeric — not ranked here"}` and `selEvidenceHTML` renders it as *"not computable"*. |
| `GUIDED-172` | medium | Feature engineering is a static list of formula templates with no ranking. `FEATURE_REGISTER.md`'s `prep-unit-harmonization` row already rules the dimensional check belongs in the Features catalogue, and `ml/clinical_units.py::CLINICAL_VARIABLES` is the canonical unit table. **Drop this one first.** |
| `GUIDED-169` | medium | Six of ten palette entries are **NOT BUILT** — and those titles resolve to the core recommender, with `pages/02_EDA.py:1782` running dose-response **today**. `MISC-014` applies **before** the mistake this time: **unrouted is not absent.** **Label correction only; the re-binning by question is deliberately unbuilt.** |

---

# Part C — `GUIDED-180`, scoped to the half that can be driven

**Deliberately a different shape from B: B is user-facing repair, C is a contract.**

Eighteen live decision kinds have no `ACTION_CONTRACT` row, and **two of them — `apply_bulk` and
`route_missingness_bulk` — mutate the working table**, so `devchecks.py`'s three guards are inert on
the highest-blast-radius paths in the app.

**Your own row says filling all eighteen is not one loop**, because each is a claim about whether a
kind touches the table and how many things it makes stale, and each needs driving. **That ruling
stands.** Do this instead:

1. **Drive and declare the two that mutate.** `apply_bulk` and `route_missingness_bulk` get real
   rows, derived by driving rather than by reading — change the input, watch the table and the stale
   list move, and record what actually moved.
2. **Make "unlisted" a state rather than a hole.** `.get(kind)` returning `None` means *unchecked*,
   and unchecked and *declared as touching nothing* are two different claims rendering as one.
   **That is the seal's `group_col: None` exactly** — `DESIGN_LANGUAGE.md` §09's recorded-absence
   rule is the pattern, and `undetermined` being first-class is the precedent. An unlisted kind
   should be **reported as unclassified**, by name, and not silently skipped.
3. **The other sixteen become an explicit unclassified list with a deadline**, not sixteen guesses.

**This is `LOOP.md` §05's rule applied to a guard**: ship the capability with its consumer, or with
something that **fails** and names what is missing. A guard that skips what it does not recognize is
a green test over an unknown.

---

# Part D — the slot gate, and the drive that judges the loop

**Turn Part D's number into an invariant.** L47's sweep publishes *three of thirty can answer at the
control* against a stated definition. **That number is now a gate**: after Part A it should be
thirty of thirty, or every exception listed **by name with a reason** in the test itself.

The definition stays yours and stays published — a press can answer at the control when the page can
write into a node whose id is `ac-<its own data-ac>`. **Keep the honesty about what it cannot see**:
whether the response is visible needs layout, and nothing here has layout. Keep `NO SLOT` distinct
from `renders elsewhere` for the same reason.

**And drive the product owner's file end to end.** `nhanes_1999_2018_yayhoo_fasting_diet_imputed.csv`
is not in the repository, so build the nearest equivalent from what is — the three `nhanes_*`
fixtures — and walk upload → target → explore → features → preprocess, pressing every control that
appears. **Report which presses produce something and which do not.** That is the loop's gate and it
is the drive's own acceptance criterion: *every control he presses produces something he can see.*

**Carry a control**, as last loop did. A sweep with no negative case cannot tell you it is working.

---

# Part E — the two small ones, tradeable

**`GUIDED-183`** — `INDICATOR_EXITS[0]`, the `impute_median` RESOLVE at `purpose.py:157-160`, carries
no `retry` key, so `showRefusal` emits `disabled` and **the safe way out renders disabled beside a
live attest.** `GUIDED-087` is the same shape and has its own build; this is a second instance on a
path nothing tests, so the claim that closes it must cover the **purpose-contraindication** path
rather than only the missingness one.

**`GUIDED-182`** — no shipped fixture produces a previewable offer at all, so the whole
offer-preview branch is unreachable from any test here. **Ship a fixture that reaches it, or state
that the branch is unreachable pre-seal and why.** This is the fifth gap in the sweep family: four
granularities ask *is this field read* or *is this route fetched*, and this branch answers yes to
both while no user path reaches it. **It is the reason `GUIDED-162`'s caption survived four loops.**

**Trade E before you trade B.**

---

## Rules that bind

- **A capability ships with its consumer**, or a **failing** test naming the one it lacks. This is
  the loop's dominant rule again: Part A exists because a mechanism shipped ahead of twenty-seven of
  its consumers.
- **`FIXED` requires a named regression test verified to fail on revert, for the stated reason.**
  Ten probes last loop, all red for their reasons, three wrong on the first attempt — that rate is
  normal and reporting it was right. `revertprobe.py`'s verdicts print an **em dash**.
- **Ambiguity is `OPEN`, never `FIXED`.** `GUIDED-167` reopened because a row about a class closed on
  a test covering a different instance. **Ask, per row: does the fix reach the thing the row's own
  evidence describes?**
- **Read the test before you touch the fix** — trap #3c. `test_the_page_never_moves_the_viewport.py`
  stays green.
- **`GUIDED-097`'s fixture rule** — two fixtures of different target shape, and name the shape you
  did not cover.
- **The shelf is never shortened.** `GUIDED-163` and `GUIDED-169` are both places where removing
  something would make a sentence true. That is not the fix.
- **Domain science comes from `docs/turbotab/research/`, by section, never from recollection.**
- **Say the number.**

## Deliberately unbuilt

- **The right column** — `GUIDED-160`, `GUIDED-174`, `GUIDED-178` are **one design** and they are
  the loop after this one. `/recipes.models` is `{}` for the whole pre-seal journey anyway
  (`project.py:1733-1734`), so the deck has no payload until Train. **Do not start it.**
- **The other sixteen `ACTION_CONTRACT` rows** — Part C declares two and lists the rest.
- **`GUIDED-181`'s twenty-nine missing `EFFECTS` sentences** — filed, and the same argument applies.
- **A fifth motion slot**, `startViewTransition`, FLIP — `GUIDED-073`.
- **`GUIDED-155`'s Propagate half** — ruled at L47 §00.
- **Anything at Train**, including the three `PACK_DEFER` train destinations.
- **`is_id_like` / the lens-aware identifier predicate** — `GUIDED-120`.
- **`GUIDED-169`'s re-binning of the palette by question** — labels only.
- **`AUDIT-031`, `AUDIT-032`** and the seven `§A5`/`§B6` rows never applied — xfails stay red.
- **`GUIDED-151`**, **`GUIDED-150`**, **`TEST-044`**, **`IMPORT-109`** (frozen), **`GUIDED-147`**,
  **`GUIDED-146`**, **`GUIDED-132`**, **`GUIDED-133`**, **`GUIDED-137`**, **`GUIDED-138`**,
  **`GUIDED-127`**, **`GUIDED-111`**, **`GUIDED-067`**, **`GUIDED-118`**, **`GUIDED-105`**,
  **`GUIDED-106`**, **`GUIDED-096`** — unchanged.

## Scope note

**A is small and not tradeable.** It closes a reopened row and it is the completion of work you
already designed.

**B is the loop.** Thirteen rows, expected to be partial, chunked so a dead subagent costs one chunk.
Drop from the bottom: `GUIDED-172` first, then `GUIDED-169`.

**C is bounded on purpose** — two driven rows and one guard change, not eighteen.

**D is tradeable in depth, not in existence.** If it runs long, keep the gate and narrow the drive,
and say what you dropped.

**E before B if B is going long**, because both E rows are small and one of them is a safe-exit
rendering disabled.

**If you finish everything, do not start the right column.** Write what you would do instead, and
stop.

## What will be checked

`AGENT_ONBOARD.md` §08's list, plus:

- **Whether `data-miss-choose` now shows its 409 to a person.** The adjudicator will drive
  `route_missingness` with `mechanism=informative` and `card_option=impute_median` and look for the
  server's sentence at the control. This is `GUIDED-167`'s own evidence and it is the reopening's
  test.
- **Whether every posting control has a slot or a named exception.** Three of thirty is the
  starting number.
- **Whether Part C's two contract rows were driven rather than reasoned**, and whether an unlisted
  kind now reports as unclassified instead of passing.
- **Whether any test's name or docstring names a control its body does not press.** Trap #3b, and
  A2 exists because one did.
- **Whether a named defect *class* was filed, or only its instance.**
- **Whether any subagent wrote to `findings.json`.** The diffstat will say. It did not last loop.

**The loop's gate:** upload an `nhanes_*` fixture, walk it to Preprocess, and **every control pressed
produces something a person can see** — or is listed, by name, with the reason it does not.

Report once, in `AGENT_ONBOARD.md` §10's shape. **Your divergence section is the first thing I read.
It corrected me three times last loop and one of those corrections was to a claim I had built an
instruction on.** Write it that way again.
