# TurboTab — the L60 loop

**If you are a fresh agent, read [`AGENT_ONBOARD.md`](AGENT_ONBOARD.md) in full first.**

**Five parts.** L59 is accepted and closed, five of five, and **it overturned my own §00 correction** —
the L58 verdict survived because you drove the transition I only theorized about. **The lead this loop
is the thing L59-C1 found and handed back rather than half-building.**

**Every number below carries how it was derived.** *(re-derived at `d4a5943`)* means I ran it against
this tree while writing this. *(from the row)* means I am quoting and have **not** re-measured —
**doubt those first.**

---

## 00 · The event, and what I measured that the row does not say

**`DRIVE-032` says the positive-class question is never asked. That is not what is happening, and the
truth is two defects rather than one.** I drove it while writing this *(all re-derived at `d4a5943`)*.

### It is not unbuilt. It is on the wrong rail, and it is not binding.

**`set_positive_class` already exists** as a repair `fix_kind`, raised by `ml/binary_text.py:291` as a
finding titled *"Which of these is the event you are predicting?"* — and `turbotab/api.py:1055` already
**refuses a repair with no choice**: *"There is no default: whether the event is (say) death or
survival is the research question, not something the file can say."* **That refusal is correct and it
is exactly the right sentence.** The machinery is built.

**Defect one — it fires on the detector, not on the target.** Measured, one fit per shape:

| Target | `positive_class` finding raised |
|---|---|
| text `case` / `control` | **yes** |
| text `died` / `survived` | **yes** |
| text `Yes` / `No` | **yes** |
| **numeric `0` / `1`** | **NO** |

It rides `binary_text`, so a two-level target that is already numeric is never asked. **That is the
tester's `meds_hbp` exactly**, and it is why the run-2 report saw nothing.

**Defect two, and it is the larger one: when the question IS raised, leaving it unanswered changes
nothing.** On a 132-case / 1,068-control study *(re-derived at `d4a5943`)*: the finding is **still
open at seal time**, the **seal succeeds anyway**, and the fit then records
**`positive_label = 'control'`** — the larger group, the non-event — which `/figures` serves as
`"event": "control"`.

**So the app raises the right question, refuses to guess when asked directly, and then guesses
anyway on a path that never consults it.** `turbotab/training.py:463` sets
`result.positive_label = classes[1]` for every two-class fit — sklearn's **sorted-second** level.

**And that guess makes an honest refusal unreachable.** `figure_bundle.py:432` returns `None` when
`positive_label is None`, with a comment saying a guess *"would be right on a 0/1 target and silently
wrong on every other one."* **`training.py` guarantees that branch never runs.** The refusal was
written to prevent precisely the thing happening one module upstream.

**One more site, and I have NOT verified it** — the register's own `reason` for
`target-positive-class` says `ml/splits.py:300` label-encodes a categorical target with
`LabelEncoder().fit_transform(y.astype(str))`, which orders **alphabetically**, so the event is
whichever level sorts last *(from the register, which says "verified, not assumed" — but I did not
re-run it)*. **Check whether that is a third instance of the same guess before you decide the fix's
shape.**

---

# Part A — the event is asked wherever it exists, and never guessed

`DRIVE-032`, **high**, and this is the loop. **C2 from L59, which you handed back rather than
half-building. That was right and the reason still holds: deleting the default alone would make every
figure refuse, which is the weaker version `LOOP.md` §02 forbids.** So both halves land together.

**A1 · Raise it for every two-level target, not just a textual one.** The finding, its copy and its
refusal all exist; what is missing is the trigger on a numeric binary. **Establish where the shape is
decided** — `binary_text`'s detector is one candidate site and the target's own task detection is
another — and put the question where *two levels* is the condition, not *two levels that are text*.

**A2 · An unanswered event must not be overruled.** Today the seal draws and the fit runs with the
question open. **Decide which of these it is and say which you chose:**

- the fit **refuses** while the event is unrecorded, the way `api.py:1055` already refuses the repair; or
- the event is a **gate** on the seal, the way grain and eligibility are.

**I am not ruling between them and that is deliberate** — the first is narrower and the second matches
`OPENING_SEQUENCE.md`'s existing shape. **Both are defensible; pick one, and put the argument in the
row.** What is not acceptable is a third state where the question is open and the engine has already
answered it.

**A3 · Make `figure_bundle.py:432`'s refusal reachable again.** It is correct code with no path to it.
When A2 lands, assert it can fire — **a branch that cannot execute is `TEST-077`'s class**, and this
one is guarding the app's most consequential silent error.

**A4 · The guard, and it must be about the EVENT rather than the question.** Assert on a
`case`/`control` fixture that the recorded event is what the user said, and that with nothing said
the app **refuses or blocks rather than choosing.** `GUIDED-093` is the row this protects —
*"a picture of the complementary event, drawn confidently."*

**What will make this fail review**: a fix that asks the question and still lets `classes[1]` through
on any path. **The measurement above is the acceptance test — re-run it and quote it.**

---

# Part B — the guard `ml/router.py` says exists

`TEST-081`, **low effort and it is the guard that would have caught a defect two humans hit.**

`ml/router.py:237` says *"`test_the_marker_is_the_constitutional_position` asserts the two agree so the
document cannot drift from the interface silently."* **That test exists nowhere in the tree** —
confirmed by the adjudicator at `d4a5943`, and the only occurrence of the name is the comment itself.

**Write it.** `SEQUENCE` at `ml/router.py:244` against `OPENING_SEQUENCE.md` §01, both directions: a
key with no documented position, and a documented position with no key. **This is `TEST-077`'s class
for the third time — a claim in a comment standing in for a mechanism — so if the comment turns out
to be describing something that should not exist, correct the comment and say so instead.**

---

# Part C — the preparation-mode row, which L58 pinned for you

`DRIVE-026`, **high**. The third key in `HANDLED_QUESTION_KEYS` with no section, found by L58-B's own
guard and left as a **strict xfail** rather than a green suite over an unreachable question.

**The probe is already written and it flips when you build it.** That is what
`mark.xfail(strict=True)` buys and it is why `TEST-077` mattered: the day the row exists,
`test_a_handled_key_reaches_a_control_in_the_dom[choose_preparation_mode]` goes **red as an
unexpected pass**, and **removing the `NOT_BUILT` entry is part of the work, not an afterthought.**

**The server side is done**: `set_preparation_mode` is handled at `turbotab/api.py:726` and folded into
`answered` at `:3087`, and the Router serves `choose_preparation_mode` at `step=preprocess` once models
are chosen *(re-derived at `d4a5943`)*. **What is missing is the surface.**

**And the overlay is silenced too, which is the part not to miss**: `drawnElsewhere` reads the same
list, so `#prepPlan` — the surface whose whole job is to draw an asked question no other surface holds
— renders **0 characters** *(from `DRIVE-026`)*. **Re-derive that before you rely on it.**

---

# Part D — the EAR question, and it is a pack question

`DRIVE-034`, **medium**, deferred twice and it should stop being deferred.

The prevalence widget computed *"Prevalence of inadequacy for `kcal` is computed by the EAR cut-point
method"* and **carried a SETTLED badge** *(from the run-2 drive)*. Energy may have no EAR in the sense
the cut-point method requires.

**This is settled by reading `research/NUTRITION_PACK.md`, and the primary source if the pack is
silent — not by a grep and not by quoting the report.** `PM_TRANSITION.md` §07's first four entries are
all an adjudicator asserting an absence without reading, so **read.**

**Three outcomes and all three are acceptable results:**

1. **The pack excludes energy** → the widget must refuse, with the reason, and the refusal is the fix.
2. **The pack is silent** → *that* is the finding. File it against the pack, not the widget.
3. **The pack permits it** → say so, cite the section, and close `DRIVE-034` `NOT-A-DEFECT`.

**A SETTLED badge on a method applied outside its domain costs more than an unbadged wrong answer**,
because the badge is the apparatus that keeps a settled fact and a disputed one from reading alike.

---

# Part E — regression evidence

**L59's baseline is the one to beat**: `turbotab/` **2,520 passed · 17 skipped · 10 xfailed · 0 failed
in 2:00:44** *(from L59's report, full sweep, `ps` checked)*.

`affected.py` has escalated on every range since `07c25c6` because the change reaches the API
aggregator. **If it escalates again, run the full sweep** — L59 did and it was right. **If you run
scoped instead, reproduce the escalation verbatim.**

**Part C changes the xfail count by one on purpose.** Say so explicitly rather than letting a reader
wonder: 10 becomes 9 when `choose_preparation_mode` is built and its `NOT_BUILT` entry removed.

**The fast tier is `~35 s` with three `--ignore` paths** — `AGENT_ONBOARD.md` §03, `TEST-078`.

---

## Deliberately not this loop

- **`DRIVE-035`** — the trigger behind the tester's empty Train card. **L59-A made the app print the
  server's reason, so the next human drive hands it over.** Do not hunt it.
- **`DRIVE-020`'s ordering half** — the lens before the diagnosis is a router change and wants its own
  loop. The numbering half is done.
- **`GUIDED-238`** (*85 items, 43 constant-reading, 16 falsifiable — from the row; re-derive before
  acting*), **`SEQN`/survey-design/pooled-cycle** flagging, which is `GUIDED-231`'s port.
- **L57's deferred ROC overlay and `GUIDED-233`'s explainability pack.** **Part A is the overlay's
  real prerequisite** — `positive_label` agreement across models is the hazard L57 named, and it
  cannot be asserted while the value is a guess.

## Rules that bind

- **A part may be refused when carrying it out would violate the criterion the row itself states.**
  Measure it, file it, hand the decision back, and **do not build a weaker version or your own.**
  Your L59-C2 handback is the model and it is why Part A exists in one piece.
- **Where a row is about something a user does, drive the doing, not the resting state.**
- **A total revert, or the probe proves nothing.** A red quoting a `TypeError`, `ImportError` or
  `HarnessError` is red for the wrong reason.
- **A pin that cannot flip is a comment** — `TEST-077`. And **`__harness.dispatch(type, el)`** takes
  the event FIRST; swapped, it silently does nothing and your driven claim means nothing
  (**`TEST-083`**, which cost me a wrong conclusion last loop).
- **A class goes in the ledger the moment you name it.**
- **A matcher whose silence means nothing is not evidence.**
- **Say the number, including when it is zero.**

## Scope note

**If parts compete they lose from the bottom up.**

- **A wins, and A1+A2 win inside it.** A question that is asked and then overruled is worse than one
  that is missing, because the transcript then carries an answer nobody gave.
- **B is twenty minutes and closes a three-time class.** Do not let it slip behind D.
- **C is a real build with its probe already written.** If it runs long, **the card ships and the
  overlay waits** — say which.
- **D may end in a filed row rather than a fix, and that is a complete result.**
- **E is evidence, not work.**

## What will be checked

- **Whether a numeric 0/1 target now raises the event question**, driven on the tester's shape.
- **Whether an unanswered event can still reach a fitted `positive_label`** — I will re-run the
  case/control measurement above verbatim, and `'control'` coming back is a failed part.
- **Which of A2's two options you chose and the argument for it**, in the row.
- **Whether `figure_bundle.py:432`'s refusal can now fire**, asserted rather than reasoned.
- **Whether `ml/splits.py:300` was checked** for the same guess, and what was found.
- **Whether Part C removed the `NOT_BUILT` entry** and the strict xfail went red before it went green.
- **`ps` checked. Part E's xfail count explained rather than left to be noticed.**

Report once, in `AGENT_ONBOARD.md` §10's shape. **Your divergence section is the first thing I read.**
You have corrected me in four consecutive loops, and last loop you overturned a correction I had
written about my own earlier measurement. **§00 above is three hours old and it contradicts a ledger
row I wrote myself. Check it too.**
