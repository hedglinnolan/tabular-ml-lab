# TurboTab — the L43 loop

**If you are a fresh agent, read [`AGENT_ONBOARD.md`](AGENT_ONBOARD.md) in full first.** It gained
two entries from your L42 report — trap **#3b** and the owned-file rule in §06. Both came from you.

Four parts. **L42 was accepted, all four.**

---

## 01 · State

Branch `TurboTab`, HEAD is the adjudication commit. Ledger **729 findings, 282 closed**, register
**176 rows**, all five gates green. `turbotab/` **1423** + `tests/` **1647** + integration **211**,
plus the four known environment failures. `make test` aborts on `TEST-038` — expected.

**Your report was the best of the six I have adjudicated, and the reason is section 8.** You kept
Part C when Part C was the tradeable part, and keeping it is the only reason anyone knows that
**B does not subsume it**: B's positive verdicts are sound, its negatives rest on group-negative
bisection, and its blind spot is a sentinel appearing inside an error render — `GUIDED-139`'s exact
shape. Two independent detectors for a `critical` class is the right number. One is the number that
decays.

**Three rulings you asked for, all in the rows:** `GUIDED-147` stays one row with two conditions,
`GUIDED-140`'s static half is wanted with the bar named, and the unreachable
`chronological_grouped` basis stays but its `# pragma: no cover` becomes a test. Read those `act`
fields before Part A.

**And the indent finding was yours to make and mine to have made first.** Commit `5881aa8` carries
a `docs(turbotab):` subject and 18,554 reformatted lines in `findings.json` — the adjudicator doing
exactly what you caught yourself doing, one loop earlier, with nothing catching it. You were right
that the rule was written nowhere. It is written now, in two places, and it binds me first.

---

# Part A — the gap you named yourself

Your section 4 said it plainly: **three payloads are reachable today and you did not open those
steps.** `/features`, `/recipes`, `/preprocess`. You called it a cost decision rather than a
shape-of-the-problem decision and said it was the part of B you were least comfortable with.

**Sweep them.** B's claim is that the field surface is guarded; three reachable steps unswept is a
hole the same shape as the one B was built to close, and it is roughly eighteen minutes of drives.

**A2 · `GUIDED-147`'s two conditions.** The row says fifteen families and its evidence lists
sixteen — in a row whose entire content is a count. Reconcile it, and say which family moved to
`GUIDED-146`. Then make the row splittable: state, in the row, that a family leaves it the moment
it gets a disposition, and that it closes when the list is empty rather than on a verdict over
fifteen unexamined things.

**A3 · The pragma becomes a test.** `chronological_grouped` behind an always-False constant is
correct and stays — §03's *three states, never two*, and a basis set omitting the honorable state
cannot say the app is missing it. But assert the unreachability rather than annotating it, and name
`GUIDED-143` in the assertion. `GUIDED-134`'s `# pragma: no cover` sat on a line its own test
executed; a pragma is a claim with no guard.

**A4 · `TEST-040` — and this one came out of re-counting your report.**

Your `turbotab/` count was **1423 passed, 0 failed**. Mine, on your tree, was **1 failed, 1422
passed** — same commit, same ordering (`pytest-randomly` is not installed, so the flag is a no-op).
**Your report was not wrong.** The failure is
`test_claim[the run says what it actually fitted]` with `('running', None)`: the training job had not
finished when the poll gave up. It passes alone in 5 seconds and the whole file passes alone in 82;
my run took 890 seconds against your 8-to-9-minute ones. **The difference is machine load.**

Four sites do this — `test_the_page_says_what_the_record_says.py` at lines 1343, 1399 and 1484, and
`test_the_companion_rule_reaches_the_document.py` — each a bounded `range(N)` loop that re-fetches
the job and breaks on `terminal`, **with no wait between polls**, so N round-trips elapse in
milliseconds while a real fit takes seconds.

**Poll against a deadline, not an iteration count.** Wait between polls and fail with *"the job did
not finish in T seconds"* rather than with the job's non-terminal status. A test that cannot
distinguish *the app produced the wrong answer* from *the app had not answered yet* reports the
second as the first — the governing rule's forbidden assertion, made by the instrument instead of by
the app.

**Why this is `high`.** It is not an app defect and it is worse than one here: the count this project
reports every loop, and that every adjudication re-measures, is load-dependent. A suite result that
differs between two honest runs of one commit is a record that cannot settle anything. `TEST-030` is
the same axis in the other direction — order-dependence producing false *passes*.

**Not in scope:** `GUIDED-140`'s static half. It is ruled wanted and it is not this loop — see
*deliberately unbuilt*.

---

# Part B — the anti-pattern audit · §A5 and §B6

**This has been re-ruled once already and does not move again.** It was L42's Part B, deferred with
a stated reason — `GUIDED-142` meant large parts of the pack layer were not shipped to anyone, and
the audit's own premise from `DOMAIN_SCIENCE.md` §03b is that *a defect the research already found
in **shipped** code outranks an unbuilt pack feature.* That premise holds now.

**Source of truth: `research/CLINICAL_SURVEY_PACK.md` §A5 and §B6.** Read both sections first. §A5
is *Modeling* — calibration first, the class-imbalance flagship, discrimination vs calibration,
Riley sample size, modeling practice. §B6 is the survey modeling section including the
proportional-odds test `GUIDED-130` has no task type to hang on.

**Run the registries against the engine we already have**, which is §03b's instruction and the move
that produced `AUDIT-001` — the generated manuscript reporting a raw *p* < 0.05 count and naming no
correction. That was found by reading the registry against shipped code, not by reading the code.

**What makes this a build and not a reading exercise:** every hit is a *checkable behavior*. A hit
becomes a test that fails against current code, plus a ledger row, plus the fix where the fix is in
scope. A hit that becomes prose is a hit that gets forgotten — this is `LOOP.md` §06's first check
and it is the most common gap in an otherwise good report.

**Report the counts**: anti-patterns checked, hits, hits already filed, hits new, and hits you could
not check with a reason each. **The uncheckable count is a real column**, not an omission.

**Bound it honestly.** §A5 and §B6 only. The other sections' registries are L44's, and saying which
you did not run is part of the result.

---

# Part C — the chronological grouped draw · `GUIDED-143`'s second half

Deliberately a different shape from B: B is a conformance sweep against literature, **C is a
splitter.**

A1 at L42 made the record honest — the seal now says `chronological_requested_not_drawn` rather
than describing a chronology nobody drew. **This draws it.**

`ml/splits.py` knows five strategies and no combined one, and `choose_strategy` returns `grouped`
when both a group column and a time column are given. That is the gap. The draw needs: **which
column is time**, people ordered by their **last** observation, and the tail held out **with whole
people kept together** — because a person split across the boundary is the grain violation the seal
exists to prevent, and a chronological split that breaks grouping trades one leak for another.

**TRIPOD+AI treats temporal validation as its own validation type**, so this is wanted rather than
tolerated, and the manuscript should be able to say which it did.

**Two things it must not do.** It must not silently fall back to `grouped` when the time column is
missing or unusable — that is the false assertion L42 just removed, arriving through the splitter
instead of through the sentence. And it must not resequence anything in the pre-seal order; the
lockbox constitution §01 is fixed.

**`GUIDED-143` closes here**, or it does not close and you say why.

---

# Part D — the probe: B's negatives, independently

Your section 5 named the limit precisely: **D falsified B's positives directly and its negatives
only relatively.** Nothing independently re-derives a `none` verdict; that rests on B's own
group-negative bisection argument, and the cross-lens comparison is relative rather than absolute.

**Close it from the other side.** For a sample of fields B calls unread, **make them matter** — put
a value in that the page would have to render differently if it read the field at all — and assert
the render is unchanged. A field that can be set to anything without moving the DOM is unread by
demonstration rather than by bisection.

This is the revert probe applied to a negative verdict, and it is the same move that made
`GUIDED-114` closeable: a second probe that draws correctly and says nothing, and fails.

**And the 291 unjudged shapes are the other half.** Values too short to search a 400 KB DOM for
without false positives — counted rather than assumed, which was right. Now reduce the number:
longer sentinels, a narrower search scope per field, or a declared reason why a shape cannot be
judged. **Report the count you got it down to**, and what remains.

**A third lens.** Two lenses showed a two-shape disagreement and you noted that three would show
whether it is systematic. Add one.

---

## Rules that bind

- **A file a tool owns has exactly one writer.** `AGENT_ONBOARD.md` §06, new this loop, from you.
- **A capability ships with its consumer**, or with a **failing** test naming the one it lacks.
- **`GUIDED-097`'s fixture rule**, and Part D's third lens is that rule being paid.
- **Domain science from `research/`, never from recollection.** Part B is entirely this.
- **`[verify-at-build]` numbers may not ship as constants.** §A5's Riley arithmetic in particular —
  derive it or hold it out.
- **Say the number**, and say the uncheckable count.

## Deliberately unbuilt

- **`GUIDED-140`'s static half.** Ruled wanted; the bar is **zero false positives with an empty
  exemption list**, because your own measurement is that six of `nudge`'s seven call sites were on
  paths nothing drove. It is a part of its own in a later loop, and a tokenizer and splitting the
  page into modules are both legitimate answers — the second may be cheaper.
- **The other packs' anti-pattern registries.** §A5 and §B6 only this loop.
- **A p025/p975 reference interval** — reference data, D4, primary sources. `MISC-018` renamed the
  band and that was the whole of it.
- **`GUIDED-146`** (the duplicated `n_rows` trio) — low, filed, parked.
- **`GUIDED-132`**, **`GUIDED-133`**, **`GUIDED-137`**, **`GUIDED-138`** — all filed, all parked.
  `GUIDED-138`'s strict `xfail` is doing its job; leave it red.
- **`GUIDED-127`** polychoric · **`GUIDED-111`** the checklist engine · **`GUIDED-067`** the DRI
  tables · **`GUIDED-118`** time-to-event, whose refusal stands · **`GUIDED-105`** inference
  families · **`GUIDED-106`** subgroups · **`GUIDED-096`** post-seal Explore · **`GUIDED-073`** any
  client-stack change.
- **Instrument fingerprinting** and **C/IER screening** — unchanged, for L41's stated reasons.

## Scope note

**Part B is the loop and is not tradeable.** It has been deferred once with a reason and a second
deferral would be the pattern the deferral rule exists to stop.

**If the loop runs long, trade Part D's third lens** — keep the negative-falsification and the
unjudged-count reduction, drop the lens, and say so. Two lenses with a sound negative probe beats
three with the same relative argument.

**If Part C runs long**, ship the splitter with the time column **asked** rather than inferred and
say what inference you did not build. Asking is the constitution's own answer for grain
(*"the domain question is asked, never inferred"*) and it is not a lesser result.

## What will be checked

`AGENT_ONBOARD.md` §08's standing list, plus: whether Part A's three payloads were swept with the
same rigor as B's original six, whether every Part B hit became a **failing test** rather than a
row, whether Part C's splitter refuses rather than falling back, and whether Part D's negative probe
is genuinely independent of B's bisection.

Report once, in §10's shape. **Your divergence section is now the first thing I read** — it has
corrected the adjudicator six loops running, and in the last one it corrected work I had committed
myself.
