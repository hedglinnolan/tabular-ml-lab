# TurboTab — the L56 loop

**If you are a fresh agent, read [`AGENT_ONBOARD.md`](AGENT_ONBOARD.md) in full first.** Two of its
paragraphs were corrected on 2026-08-09 and both were telling you something false: §04 named **five**
pre-commit gates where the hook runs **six**, and §03 told you to expect **three** environmental reds
in `tests/test_shap_and_sensitivity.py` — `shap` is installed now and that file passes **10 of 10**.

**Six parts, and the loop is deliberately wide.** The product owner asked for maximum scope, *within
reason*. The ordering is the reason: **A and B are certain work that make every part after them
cheaper or safer**, C is the build, D is authoring, E is fill-out that widens to whatever you can
hold, and **F is allowed to end in a refusal.**

**There is a new adjudicator.** The previous one was cleared after L55. Everything below is verified
at `f8fa95b` rather than carried forward — including the numbers, because quoting a stale number
forward is the failure the last one recorded against themselves twice.

---

## 00 · Your divergence section, answered

**Ten items, and it corrected the adjudicator on four of them.** It is read first for a reason.

**1 · Part C's premise was half wrong and you were right to say so first.** The correction — no figure
was drawn at all, **2** `<svg>` in 8,479 lines, styling for an element with **no producer** — is bigger
than the row and is now in `DRIVE-014`'s note and the §03 log. The prompt's premise was the
adjudicator's.

**2 · `TEST-063`: you pushed back on my predecessor's sentence and the pushback is upheld in full.**
The commissioning sentence *"the underlying repair looks real and only its record was wrong"* is
**retracted in the row.** Both halves are false and you measured them.

**One correction to your correction, and it is small but it is a date.** You wrote that the `table`
fixture *"has snapshotted and restored `_OPERATIONS` since L35."* Re-derived with `git log -S`: the
`snapshot()`/`finally: restore()` pair entered at **`7c68dd6`, which is L18**; what arrived at
**`a3e2395`, L35** is the **reset-to-core** half (`R._install_core()`). Two different halves, two
different loops, both present at `773a626^`. Your argument is unaffected — it is stronger with the
right dates, because the restore predates the row by seventeen loops rather than by twenty.

**3 · Two of your own guards were defective on their first probe and you reported both.** Trap 5b
inside the guard written to close trap 5b is the sharpest self-report in this project's record. Kept.

**4 · `ModelCapabilities.exposes_coefficients` is accepted, and it earned its keep immediately.** Your
argument is the right one — `interpretability_tier` is not the question the clause has to answer. It
also produced `GUIDED-234`, and **that row is Part C of this loop.**

**5 · The repeat answers not reordering anything: your call stands, and the row is re-scoped.** You
were right that demoting all twelve models moves nothing while reading as a judgment, and right that
the honest deliverable was the number. **I am not overturning it.** `GUIDED-235`'s live content is not
the ranking — it is that the coach's *thresholds* are applied to an inflated n. And your own note is
why that is not Part A: *"n_people is not the effective sample size either."* It needs a sourced
quantity, so it goes to **Part D with the pack**, not to plumbing.

**6 · Editing `AGENT_ONBOARD.md` was correct and the guard should keep reading §03.** Ruled: keep it
that way. **I edited the same file after you**, for the two stale claims at the top of this prompt —
so §03's `--ignore` block is unchanged and your parse still holds.

**7 · Both drifted numbers were the adjudicator's fault, and there is now a third of the same kind —
also mine, not yours.** My predecessor's write-up asserted *"21 built figure specs use the failing
ramp."* Re-derived: `figure_specs.py` holds **17** `register(FigureSpec(` and **4**
`register_pending(Pending(`, so 21 is 17 built plus 4 declared-pending. **A count is a claim and the
first thing to doubt is the thing you counted.** It is now the fifth entry in `PM_TRANSITION.md` §07's
calibration list and the only one that is an asserted *presence*.

**8 · Declining `parentNode` was right and `TEST-070` stays unbuilt this loop.** Adding a property
that activates dead production branches, inside the part fixing the harness, would have been
`TEST-066`'s own defect committed by its fix. The test asserting the absence is the correct artifact.

**9 · The stated cap in Part A's guard is accepted** — two nodes of a 226s file, reason in the source
beside the list. That is what a stated cap looks like.

**10 · Killing the 55-file sweep and running the 11 files that actually reach an `appendChild` call
site — 217 passed in 6m08s — is the most useful thing in your report and you filed it as a footnote.**
It is **Part A of this loop.** The product owner's constraint, in his words: *"These full suite tests
are simply taking too long for the workflow we are currently in. They run over two hours and
occasionally time out."* You solved that by hand, once, under time pressure. Part A makes it a tool.

### Found after your report, and not by you

**`TEST-071`: the `FIXED`-row resolver drops 26 of 362 rows into no bucket at all.** Found by the
adjudicator while checking two rows it had just written itself. `resolve()` computes
`funcs = [f for f in _FUNC.findall(test) if f not in stems]` — and for the convention
`turbotab/test_foo.py::test_foo`, **the function name *is* the file stem**, so the filter discards it,
`funcs` empties, and control flow `if hits: … elif funcs: …` appends the row to **nothing**.

**The consequence is why it is `high`:** `test_every_fixed_rows_named_test_exists` asserts
`missing == []`, and `missing` can only fill when `funcs` is non-empty. **For every row using this
project's dominant naming convention, a named test that does not exist produces exactly the same
silence as one that does.** Your guard caught `TEST-063` only because that row's function name
happened to differ from its file name. **It is Part B.**

Seven rows were repointed at the handover rather than the resolver relaxed — all seven had real guards
behind malformed pointers, and **five closed rows are now checked for the first time.** `not_pytest`
went 10 → 7 against its cap of 10 **with the cap untouched.**

---

## 01 · State

Branch `TurboTab`, HEAD **`f8fa95b`**. Ledger **887 findings, 368 closed**. Register **182 rows**. Six
gates green. **L55 accepted, four of four.**

| Suite | Result | Cost |
|---|---|---|
| `tests/` fast tier | 1,730 passed · 1 failed — the failure is `TEST-038`'s `torch` | ~22s |
| `tests/integration` | 262 passed | ~51s |
| `turbotab/` | **2,449 passed · 21 skipped · 9 xfailed · 0 failed** | **2:01:04** |

**That `turbotab/` run is the baseline Part A exists to replace.** It is also the first full sweep
under `numpy` 2.4.6, which `shap` 0.52.0's install pinned, and it came back clean.

**`PRODUCT_VISION.md` §06c was rewritten on 2026-08-09** after two adversarial reviewers returned
SERIOUS PROBLEMS. **If you read a pre-`bea4878` copy you will build against refuted claims.** The
instrument is a **multivariate forward marginal effect aggregated as an AME** (Scholbeck et al. 2024,
`fmeffects`), *not* an ALE; its "stays inside the data's support" claim was **measurably false**; and
`NUTRITION_PACK.md` §04's *"because"* was corrected because it made the substitution curve read as a
remedy for a bias it does not remedy.

---

## 02 · How this loop runs

**Everything from L50–L55 §02 binds.** Three things are specific to a loop this wide:

- **Partition by FIX SITE, not by row.** This loop names sixteen rows across six parts. Several share
  a file. Group your work by the file that changes, or two chunks will fight over `figure_specs.py`.
- **Part A changes how you verify Parts B–F, so land it first and then use it.** Do not run the
  two-hour sweep six times. Run Part A's scoped selection per part, and the full sweep **once**, at the
  end, on a quiet machine, with `ps` checked.
- **Say the number, including when it is zero, and never cap silently.** Parts E and F are sweeps.
  A sweep that reports only what it found has not reported its coverage.

---

# Part A — the loop stops costing two hours

**First, because every part after it is verified more cheaply, and because it is the product owner's
stated constraint rather than an optimization someone thought of.**

## A1 · `docs/turbotab/tools/affected.py`

**Given a diff, name the `turbotab/` test files that reach the changed code.** You did this by hand at
L55-D and it turned a 55-file sweep into **11 files, 217 tests, 6m08s.**

**Build it as a tool, and be honest about what it cannot see.** The import graph is measured and
already in the repository — `docs/turbotab/data/import-graph.json` and `reverse-deps.json`. A test that
reaches production code through a fixture, a captured API response or `pageharness.py` is **not**
import-visible, and the tool must **say so** rather than imply coverage.

**The output is two lists, not one:** the files it selected, and **the reason each was selected**. A
selection nobody can audit is a silent cap with a CLI.

**The rule it enables, and write it into `LOOP.md` §05:** a loop's regression evidence may be the
scoped selection **quoted as scoped**, with the full sweep run once at the end. **A scoped run is never
reported as a full run.**

## A2 · `AUDIT-040`'s floor

**`turbotab/test_the_three_unswept_payloads_are_swept.py` holds 1,175.39 seconds of SETUP.** That is
the single largest item in the intended-exclusion set and it is the hard floor under any parallel
scheme, so it is worth more than parallelism is.

**Make it cheaper without weakening it.** The setup is the cost, so the question is whether three
payload sweeps need three independent drives or one module-scoped drive they share. **If they do need
three, say so and say why, and the row stays open with the reason** — that is a real answer.

**Do not touch `TEST-061`'s 2,700s cap.** It changed *purpose, not value* at L53-A2 and it is a hang
detector now. Touching it in the loop that makes the suite cheaper is the threshold rule's exact
prohibition.

---

# Part B — the guard that cannot see what it claims to check

**`TEST-071`, and it goes before anything that closes a row, because 26 closed rows are currently
unverifiable and this loop will close more.**

## B1 · The resolver

**Two halves, and the second is the durable one:**

1. **Read the function from the node, not from the whole string.** Split on `::`. A path with no `::`
   names a **file**, which is what those rows mean; treat it that way rather than discarding it.
2. **Assert the partition sums.** `len(resolved) + len(missing) + len(not_pytest) == len(fixed_rows())`.
   A three-way partition that returns three lists and checks nothing is exhaustive is how a fourth
   outcome stayed silent through the loop that wrote it and the adjudication that accepted it.

**The revert probe is specified, because the obvious one is wrong.** Reverting the fix and watching the
guard still pass proves nothing — it passes today. **Add a row naming a nonexistent test in the
`file.py::same_name` shape and require the guard to go RED for it.** That is the only probe that
observes the defect. Remove the row afterwards, and do not leave it in `findings.json`.

**Then re-run it and report the number.** 26 rows enter the resolver's blind spot today. After the fix
some will resolve and some may turn out to name nothing. **Either answer is the deliverable** — and if
any closed row names a test that does not exist, **it reopens**, exactly as `TEST-063` did.

## B2 · `AUDIT-039`'s remaining six

**Six, not twelve — the row already corrected itself and the corrected count is what binds.** Five were
fixed at L53-D, and one of the remaining seven is on second look **environmental** (a git-history skip),
so it belongs with the 55 the sweep classified as correct. **The six, each already located:**

| File | Line |
|---|---|
| `turbotab/test_the_promoted_card_says_why_it_is_there.py` | **315 and 326** |
| `turbotab/test_a_client_holding_the_payload_can_act.py` | 109 |
| `turbotab/test_a_prevalence_needs_a_nutrient_to_be_about.py` | 109 |
| `turbotab/test_an_unlisted_decision_kind_reports_as_unclassified.py` | 181 |
| `turbotab/test_the_harness_reports_and_does_not_stop_the_drive.py` | 371 |

**The promoted-card pair is the worst and the hardest, and the row already names the honest repair:**
its precondition varies **by parametrization**, so **narrow the `parametrize` list to the lenses that
have a remainder and name the ones you dropped** — `GUIDED-097`'s rule applied to a skip. Do not assert
over all of them.

**Establish the precondition from the DATA, assert it, then assert the consequence unconditionally.**
A test that declines to look when the defect is present is green over exactly the defect it was
written for, and pytest counts a skip as not-a-failure.

---

# Part C — the figure surface, and three rows that are one mechanism

**The substantial build. Three rows, one fix site, and they are ordered hardest-first because the
hardest one determines whether the other two mean anything.**

## C1 · `GUIDED-236` — the ROC can never overlay more than one model

**Hardest first, and it is the one that makes C2 matter.** `figure_bundle.predictions_for` takes
`scored[0]` while `roc_payload` takes a dict and loops it, the caption reads *"Discrimination for N
model(s)"*, and the checklist item *"Models are overlaid with a legend, not split into panels"* passes
on `p.get("legend") == "inside"` — **a constant.** Trap 3b with a checklist item instead of a test
name, green for as long as the figure has existed.

**L55-C did not work around it silently** — its dash test composes a two-curve payload from the
production producer and **asserts the served bundle carried exactly one curve, naming this row**, so
that helper fails loudly the day you fix it. **Expect that failure and fix the helper rather than the
assertion.**

**The checklist predicate is the other half.** An item whose text carries a consequence its predicate
never checks is the class, not the instance — so ask what **else** in the checklist reads a constant,
and report the count even if it is zero.

## C2 · `DRIVE-015` — the categorical ramp

**`--c1` is byte-identical to `--accent` in both themes** — `#0E7368` light at `index.html:13` and
`:45`, `#45BFAF` dark at `:31` and `:58` — while `DESIGN_LANGUAGE.md:55` asserts *"charts use a
separate categorical ramp so semantic color stays semantic."* **The separation is asserted and not
implemented**, so a categorical series is indistinguishable from *"the current position."*

**The shipped ramp fails three of six checks and warns on a fourth**: chroma floor (`c1`, `c4` read
gray), normal-vision floor (`c1`↔`c2` ΔE **13.8** against a floor of 15; tritan ΔE **3.3**), and a
dark-mode contrast WARN at **2.63:1**.

**The replacement is measured, not chosen. Three series pass all six in both modes:**

| | light | dark |
|---|---|---|
| `--c1` | `#3B5BA9` | `#5B7BC9` |
| `--c2` | `#B15A33` | `#D0743F` |
| `--c3` | `#6E3FA3` | `#9A6BC4` |

Worst CVD ΔE **20.0**. **Four cannot pass** — teal, green, gold and red are semantically reserved and
the remaining hue space collapses under deuteranopia. **The fourth series folds into "Other," a small
multiple, or dash pattern — and `DESIGN_LANGUAGE.md` §07 already requires dash for the journal face,
so promote that rule in-app rather than inventing one.** L55-C already built the dash renderer.

**Ship a validator test, and this is the part that matters more than the palette.** The claim at
`DESIGN_LANGUAGE.md:55` is currently prose that nothing checks. **Assert `--c1 != --accent` in both
themes, and assert the ramp's separation floors**, so the document and the stylesheet cannot drift.
A design claim with no check is the condition-three gap `PRODUCT_VISION.md` §06b names.

**Correct the row's own arithmetic in your report if you find it wrong again.** The write-up said 21
built specs use this ramp; it is 17 built plus 4 pending, `FIG_DRAW` holds **two** kinds, and `WEBC` is
consumed at exactly two lines inside the multi-series path. **Blast radius today is one figure**, which
is precisely why this is cheap now and expensive later.

## C3 · `GUIDED-234` — two "interpretable" models cannot draw a coefficient

`GLMWrapper` and `HuberGLMWrapper` hold a `LinearRegression`/`LogisticRegression` in `self.model` and
never forward `coef_`, while both are registered `interpretability_tier=high`, `supports_shap=linear`,
with notes reading *"Interpretable."*

**Forward the coefficients rather than relaxing the claim.** The shelf currently says the true thing
and reads oddly — GLM ranking below Logistic Regression under a recorded inference objective — and
your own note says why that was right: declaring `exposes_coefficients=True` on the strength of what
the method *is* would make the shelf promise an estimate the app cannot show. **Fixing the wrapper
makes the odd-reading sentence go away by making the capability true**, which is the right direction.

**When it lands, `exposes_coefficients` must be re-measured against a real fit, not edited.** The field
means what `figure_bundle._coefficients_for` asks. Its whole value is that it is measured.

---

# Part D — the explainability pack, and the quantity nobody has named

**`GUIDED-233`, and it gates both deferred builds — the audit layer and the substitution figure. It is
pack authoring, not code, and it is the part where an unsourced number does the most damage.**

**Source of truth is `research/`, never recollection, and a `[verify-at-build]` marker may not ship as
a constant.** Since L52 a **bare** marker fails gate 6.

## D1 · What the pack must establish, each with a resolving source or an explicit `CONVENTION` badge

- The **correlation cut** at which features are treated as one group for joint permutation.
- The **rank-correlation level** below which a top-*k* importance list is reported as unstable.
- The **repeat count** for permutation error bars.
- The **FDR *q*** for any controlled selection.
- The **sample-size rule** for isotonic versus Platt recalibration.
- The **background-sample size and perturbation scheme** for tree attribution — this changes the
  number and is almost never disclosed.
- **The held-out floor that gates the explainability surface.** §06c's ruling 7 now says generalization
  gates the whole surface and **deliberately does not name the value**, because it is yours to source
  and not the adjudicator's to pick. Until it exists that gate is *specified and unbuilt*.

## D2 · The half the agnostic mould cannot supply, and it is the whole differentiator

**The established directions an attribution is compared against.** §06c ruling 1 makes a disagreement
between a model's attribution and the literature a **badged finding, never a verdict** — and there is
nothing to compare against until a pack records which directions are `SETTLED`, which are
`CONVENTION`, and which are `DISPUTED`. **Start with nutrition; `NUTRITION_PACK.md` §04 is already half
of it.**

**`Evidence.both_sides` is required at construction on every `DISPUTED` claim** (`GUIDED-211`). Do not
badge something `DISPUTED` without stating both positions.

## D3 · `GUIDED-235` — say which quantity is being claimed

**This is here rather than in Part A because your own note is why.** *"`n_people` is not the effective
sample size either — the right quantity depends on the within-person correlation, and asserting
`n_people` would be trading one wrong number for another with more confidence."*

**Read `METABOLOMICS_PACK.md` §08 first, as the row says — but §08 *names* effective sample size
(line 683) rather than deriving it, so the substance is elsewhere and you need all three.**
`CLINICAL_SURVEY_PACK.md` and `NUTRITION_PACK.md` are the two packs that carry intraclass correlation
and design-effect material; the nutrition thread also holds λ and the 1/λ² penalty (`§03`), which is
the same arithmetic wearing a different name. **Cite the section you took the quantity from.**

Then either the coach's thresholds read it, or the row records — **with a source** — why the app cannot
compute it yet and what it says instead. **A refusal with a reason is an acceptable outcome here. A
confident wrong number is not**, and that is the whole reason this row was not closed at L55.

---

# Part E — the anti-pattern audit, widened

**Fill-out shape, so it goes as wide as you can hold.** The abstraction has not bent in three audit
loops; this is not discovery.

**Three populations, and the second and third have never been swept at all:**

| Source | State |
|---|---|
| The tabulated registry | **131 entries, 77 never run** (`AUDIT-038`) |
| `CLINICAL_SURVEY_PACK.md` | **~25 named entries in nine `**Anti-patterns.**` prose blocks, no table** |
| `INTERACTION_PACK.md` | **no anti-pattern content** — and this is confirmed by reading, not by grep |

**Order hardest-first**, judging hardest by what is most likely to break the sweep's shape rather than
by effort — and the prose blocks are harder than the table because a block is not a row.

**Every pattern carries a positive control, and this is `AUDIT-038`'s own method warning.** Two of its
checks returned a **false absence** because `grep -E` was handed `\|` as an alternation, which is a
literal pipe in ERE — *"no multiple-testing correction anywhere in shipped code"* was the regex, not
the engine, and `ml/multiplicity.py` had been there the whole time. **A sweep reporting absences needs
a positive control per pattern**, and a matcher that fires on prose has silence that means nothing.

**Report four numbers**: enumerated, checked, hit, and **could not be constructed**. The last is a
count, not a silent omission.

---

# Part F — xdist, priced and not adopted

**Last, and it is allowed to end in "no." That is a result, not a failure.**

`pytest-xdist` **3.8.0** is installed, it is dev-only in `requirements-dev.txt`, L53 ruled it kept, and
the `FIXED`-row guard already uses it with a **measured** `--dist load` over `loadfile`. **What has
never run under it is the `turbotab/` suite itself.** This machine has 10 cores.

**Price it by comparing VERDICTS, not durations.** A faster suite that answers differently is not a
faster suite. Run the known-coupled files both ways:

- `turbotab/test_the_recipe_table_is_a_table.py`
- `turbotab/test_a_pack_does_not_fire_on_the_wrong_data.py`
- `turbotab/test_a_test_that_rewrites_the_registry_puts_it_back.py`

**Three hazards, all already rows, and any one of them is a stop:**

1. **`GUIDED-099`** — `packs.load` is process-global and never unloads *by design*. `-n` changes which
   tests share an interpreter, so it changes what leaks where.
2. **`TEST-063`'s guard requires two nodes in ONE interpreter** — the defect is cross-test and
   `revertprobe.py` invokes one node with `-x`. Under `--dist load` those nodes can land on different
   workers and **the guard goes green while checking nothing.** Pin it with
   `@pytest.mark.xdist_group` before running anything else, or the reconnaissance destroys the
   instrument it is measuring.
3. **`TEST-030`** — `tests/workflow/*` is order-dependent *by design* and its note says outright that
   *"adding pytest-xdist for a faster migration loop is the obvious move that breaks it silently."*
   **Scope any `-n` to `turbotab/` only.** The `tests/` tier is 72 seconds and has nothing to gain.

**And `AUDIT-040` bounds the prize before you start**: no width goes below the longest single item, so
if A2 does not land, the floor is ~1,175s and the ceiling on the whole exercise is about 20 minutes.
**If A2 landed, re-measure the floor before quoting a speedup.**

**If any verdict moves, the answer is "not adoptable until the process-global registry is fixed," and
that answer is worth more than the speedup.** File it and stop.

---

## Rules that bind

- **A scoped run is never reported as a full run.** Part A's whole value is that the distinction stays
  visible.
- **A total revert, or the probe proves nothing** — and a red quoting a `TypeError`, `ImportError` or
  `HarnessError` is RED FOR THE WRONG REASON.
- **A class goes in the ledger the moment you name it** — not in a docstring, not in the report.
- **Never accept a moved threshold in the same loop as the change that pressured it.** This loop makes
  the suite cheaper and touches `TEST-061`'s cap in no way whatsoever.
- **A claim that something does not exist requires reading, not searching.** `LOOP.md` §06 now carries
  this as its own subsection with five instances, four of them adjudicators'. Say which file you
  opened.
- **A count is a claim, and the first thing to doubt is the thing you counted.** The fifth instance was
  "21 figure specs" and it was the adjudicator's.
- **Ambiguity is `OPEN`, never `FIXED`.**
- **Say the number, including when it is zero.**
- **Verify the instrument before building on a mechanism with zero instances** — Part F is that rule
  applied to the suite runner.

## Deliberately unbuilt

- **`TEST-070`'s `parentNode`.** Your reasoning holds; the test asserting the absence stays.
- **`MISC-021`** — 105 `innerHTML` sites against zero identity-preserving transitions. Its own loop.
- **The substitution figure and the explainability audit layer.** Both wait on Part D's pack. §06c
  carries the figure's five marks; do not build it from them this loop.
- **Any new model family.** No Cox, no mixed-effects, no cumulative-link. `GUIDED-118`'s time-to-event
  needs a two-column target and a censoring indicator, and that is a decision loop.
- **`TEST-061`'s 2,700s cap**, and **`torch`** — do not install it to make `TEST-038` go away.
- **The 193 benign assert messages**, the remaining **15 TRIPOD+AI items**, export at 3× and LaTeX
  tables, and the standing unchanged list from L55.

## Scope note

**Six parts is deliberate and so is the order. If parts compete, they lose in this order, from the
bottom up.**

- **A wins everything.** It is the product owner's stated constraint and it pays for the rest of the
  loop. If A1 alone lands, that is still the most valuable thing here.
- **B is second and it is not negotiable against C**, because 26 closed rows are unverifiable right
  now and this loop will close more. Closing rows on top of a blind guard is how `TEST-063` survived
  two green loops.
- **C is the build.** If it runs long, **C1 and C2 ship and C3 waits** — the ROC overlay and the ramp
  are one mechanism, and a coefficient wrapper is independent of both.
- **D may ship as nutrition only**, with the other four lenses named and dated as unwritten. A pack
  section that covers one domain honestly beats five stubs.
- **E is fill-out: trade width freely and state what you dropped.**
- **F may be dropped entirely with a row saying so.** It is the only part whose best outcome might be
  a refusal.

**If anything in A through C overruns, drop from the bottom. Do not half-build two parts.**

## What will be checked

- **Whether Part A's tool says what it cannot see.** A selector that implies coverage it does not have
  is worse than no selector, and I will look for the fixture/harness/captured-response blind spot
  first.
- **Whether the resolver's partition is ASSERTED to sum**, and whether the probe adds a row in the
  `file.py::same_name` shape and goes red for it. Anything else does not observe the defect.
- **What the 26 blind-spot rows resolve to**, and whether any closed row reopened.
- **Whether `GUIDED-236`'s fix broke L55-C's one-curve assertion, and whether you fixed the helper
  rather than the assertion.**
- **Whether `--c1 != --accent` is asserted in both themes by a test**, not just changed in the file.
- **Whether every pack threshold in Part D resolves to a real source or carries an explicit
  `CONVENTION` badge** — and whether `GUIDED-235` names its quantity or records why it cannot.
- **Part E's four numbers**, and a positive control per pattern.
- **Part F's verdict comparison**, and whether `TEST-063`'s guard was pinned before anything ran.
- **`ps` checked; the full `turbotab/` sweep once, at the end, on a quiet machine.**

Report once, in `AGENT_ONBOARD.md` §10's shape. **Your divergence section is the first thing I read.**
Last loop it corrected the adjudicator on four items including a diagnosis the prompt asserted as
fact — and the one number you got wrong was a date on a fixture, which I only caught because you had
given me the exact commit to check.
