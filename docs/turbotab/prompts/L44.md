# TurboTab — the L44 loop

**If you are a fresh agent, read [`AGENT_ONBOARD.md`](AGENT_ONBOARD.md) in full first.** §07 gained
a fourth variant from your L43 report — trap **#3c**, the green test that *pins* the defect.

Four parts. **L43 was accepted, all four.**

---

## 01 · State

Branch `TurboTab`, HEAD is the adjudication commit. Ledger **758 findings, 286 closed**, register
**178 rows**, all five gates green. `turbotab/` **1492** + `tests/` **1648** + integration **211**,
plus the four known environment failures. **22 xfails are deliberate** — 21 from L43-B's filed
registry hits and `GUIDED-138`'s standing one.

**Your report was the strongest yet, and the reason is that four of your nine divergences are
findings rather than caveats.** Item 1 became `TEST-042`, item 6 became trap #3c in the onboard,
item 4 became `TEST-043`, and item 3 — the revert probe that found `GUIDED-143`'s defect *inside its
own fix* — is the single best use of that instrument this project has recorded.

**Two things in your report I checked and corrected**, both in the rows:

- **`AUDIT-015`'s cause is not `GUIDED-131`.** At `f2266a9` — the commit that *wrote* the gate —
  `api.py` already built `figures` from `_figure_specs_all()` and passed it into the doc, and
  `calibration` is unconditionally in the registry. **The gate was a tautology in the commit that
  created it**, three loops before `GUIDED-131`. What `GUIDED-131` added was the `drawn` field your
  fix uses. The corrected lesson is sharper: not a condition that drifted under its guard, but a
  guard **never valid, not once**, green beside `test_the_manuscript_is_checked` for four loops.
- **`GUIDED-049`'s note did not record that its own fix was incomplete.** A `critical` `FIXED` row
  that reached three of ten surfaces and says nothing about it is the record decaying. Corrected in
  place, pointing at `AUDIT-014`.

---

# Part A — the instrument, twice

**A1 · `TEST-042` — the sixth gate.** None of the five pre-commit gates parses Python, which is how
you committed an `IndentationError` green and why your own imbalance guard passed over a file that
does not compile. Measured: `ast.parse` over `git ls-files '*.py'` is **363 files in 0.44 seconds**
and clean on the current tree.

Add it to `.githooks/pre-commit`, unconditional, naming the file and the line on failure — the
hook's own comment already argues that a conditional gate is how gates get skipped. **It is not a
substitute for the suite** and nothing about it should read as one; it closes exactly one hole at
the moment the mistake is made rather than three commits downstream.

Your own sentence is the generalization and it belongs in the gate's comment: *a check whose input
is a serialization of the thing it cares about will pass on a broken input.*

**A2 · `TEST-041` — the ordering axis.** `figures.REGISTRY` is populated only as an import side
effect of `figure_specs`, and `test_the_companion_rule_reaches_the_document` asserted a count over
it in its first test: alone it is `assert 0 >= 4`, in the suite it is green. **The full-suite green
is the false one.** Four files read the registry this way.

Make the population explicit at the point of use rather than inherited from import order. `TEST-030`
already names this axis for `tests/workflow`; `TEST-040` was its load-dependent twin and you closed
it. This is the third face of the same property: **a count that moves with something other than the
code is not a count.**

---

# Part B — the twenty-one · **WIDE, hardest-first**

L43-B found 40 candidate hits, refuted 7 adversarially, fixed 2, and filed **21 as
`AUDIT-016`…`AUDIT-036`, each with a `xfail(strict=True)` naming its row.** That is §05's pattern
used properly and at scale, and it is now the largest block of *specified, research-backed,
test-covered* defects in the ledger.

**Close as many as you can hold.** This is fill-out by the §02 test — the shape is known, the
research position is written, the failing test already exists, and `strict` means each one flips red
in the other direction if you fix the code and forget the row.

**Order hardest-first**, judging by what is most likely to break the shared position rather than by
effort. Say which ordering you chose and why, and **say which you did not reach** — 21 is a target,
not a promise, and a smaller batch done properly is the better result.

**Two rules bind harder here than usual.**

- **Read the test before you touch the fix.** Trap #3c is now in the onboard because of `AUDIT-014`:
  a green test asserted `"class weights"` on a low-EPV profile, exactly where §A5.2 says the remedy
  is penalization. A loop that chases a newly-red test reverts the correction and records the revert
  as a success.
- **The shelf is never shortened.** Several of these will tempt a removal where the answer is
  routing — the SMOTE fix is the standing example: routed behind purpose, offered with its citation,
  never recommended. Removing advice without replacing it is its own defect.

---

# Part C — `GUIDED-148` · the payload that reaches nobody

Deliberately a different shape from B: B is conformance against literature, **C is wiring.**

`/recipes` — **50 fields, 16 path shapes, every one unread.** It is the per-model preprocessing
table the Preprocess step is built around, and `api.get_recipes`' own docstring describes two axes
of decision that reach no person.

**This is `GUIDED-142`'s shape at a route the route-level check cannot see**: `/recipes` *has* a
reader in the standing check's sense — the page fetches it — and nothing inside it renders. It is
also the direct answer to what L42-B's skipped payloads were hiding, which is why your A1 was worth
delivering even though you had it down as the part you might not reach.

**Measure `/recipes` under the clinical lens before building**, which your own row says to do. A
surface that renders nothing on one lens and something on another is a different defect from one
that renders nothing at all.

---

# Part D — the probe: `MISC-019` · how much of *closed* is closed

**`AUDIT-014` is the instance and the class was not filed, so I filed it.** `GUIDED-049` is
`critical`, `FIXED`, and its fix reached **three call sites out of ten shipped surfaces**. Its named
test passes. `ledger.py check` is satisfied.

**That is L42's granularity lesson arriving at the ledger itself.** The gate enforces that a `FIXED`
row *names* a test — and the tool's docstring is honest that it cannot judge the test's quality —
but the gap is wider than that admission: **a named test proves the fix works at the site the test
covers.** A row whose item describes ten surfaces and whose test covers three passes the gate and
reads as done.

**286 of 758 rows are closed and nothing distinguishes a fix that reached one surface from one that
reached ten.**

Sweep the closed rows. Start where the yield is highest: rows whose `item` contains a plural —
*pages*, *surfaces*, *call sites*, *both doors*, *every* — and **every `critical`**. Re-derive the
surface set from the code and ask whether the fix arrived at all of it.

**Report the counts**: closed rows examined, rows whose fix demonstrably reached every surface their
item names, rows that are actually partial, and rows you could not decide with a reason each. **The
third column is the finding whatever its size**, and if it is zero say so — a zero here is a real
and reassuring result, and it is the first time anyone will have measured it.

**Do not re-open rows in bulk on a judgment call.** A row that is actually partial gets its status
changed with the evidence in the row; a row you are unsure about gets named in the report and left
alone. `LOOP.md` §09: ambiguity is `OPEN`, never `FIXED` — and the converse holds, a wrongly
*re-opened* row is noise in the one measure this project uses for progress.

---

## Rules that bind

- **A file a tool owns has exactly one writer.** Your finding, now §06.
- **A capability ships with its consumer**, or with a **failing** test naming the one it lacks.
- **`GUIDED-097`'s fixture rule.** Part C needs both lenses; Part B needs whatever each hit's
  research section specifies.
- **Domain science from `research/`, never from recollection.** Part B is entirely this.
- **Read the test before you touch the fix** — trap #3c.
- **Say the number**, and say what you did not reach.

## Deliberately unbuilt

- **The other sections' anti-pattern registries.** §A5 and §B6 are run; §A1–A4 and §B1–B5 are a
  later loop. Part B is about *closing* what the audit already found, not extending it.
- **`GUIDED-140`'s static half** — ruled wanted, bar is zero false positives with an empty exemption
  list, still not this loop.
- **`GUIDED-147`** — fifteen families, reconciled and splittable; it splits when someone looks, and
  Part D is not that looking.
- **`GUIDED-146`**, **`GUIDED-132`**, **`GUIDED-133`**, **`GUIDED-137`**, **`GUIDED-138`** — filed,
  parked. `GUIDED-138`'s strict `xfail` stays red.
- **`GUIDED-127`** polychoric · **`GUIDED-111`** the checklist engine · **`GUIDED-067`** the DRI
  tables · **`GUIDED-118`** time-to-event · **`GUIDED-105`** inference families · **`GUIDED-106`**
  subgroups · **`GUIDED-096`** post-seal Explore · **`GUIDED-073`** any client-stack change.
- **Instrument fingerprinting** and **C/IER screening** — unchanged.

## Scope note

**Part A is small and not tradeable** — both halves are about whether this project's own numbers
mean anything, and A1 is under an hour.

**Part D is not tradeable either**, and it is the one I would protect if the loop tightens. It asks
whether the closed count is a fact, and every other number in every report rests on it.

**Trade width inside Part B.** Twelve of twenty-one closed properly beats twenty-one closed thinly,
and the strict `xfail`s mean an unclosed one stays visible rather than going quiet. Say which you
dropped and on what ordering.

**If Part C runs long**, ship the measurement and the wiring for the axis that carries the most —
`api.get_recipes`' docstring names two — and file the other with what it needs.

## What will be checked

`AGENT_ONBOARD.md` §08's standing list, plus: whether A1's gate refuses a deliberately broken file,
whether each Part B closure flipped its `xfail` rather than deleting it, whether Part C measured
both lenses before building, and whether Part D's partial-count came from re-deriving surfaces from
code rather than from re-reading the rows.

Report once, in §10's shape. **Your divergence section is the first thing I read** — six loops
running, and in L43 four of its nine items became rows or onboard entries. Write it that way again.
