[{"id": "LIVE-001", "area": "TIER0", "sev": "critical", "item": "macro_shape @st.cache_data key omits the dataframe; first dataset's PCA/UMAP/persistence/Mapper served to all later datasets and users", "detail": "", "ev": "ml/macro_shape.py:82,240,337,495; call sites pages/02_EDA.py:1163,1205,1226,1265", "act": "", "status": "OPEN \u2014 verified on main"}, {"id": "LIVE-002", "area": "TIER0", "sev": "high", "item": "Cancel Training writes st.session_state.cancel_training; nothing ever reads it", "detail": "", "ev": "pages/06_Train_and_Compare.py:1289-1301 (grep returns only these 4 lines)", "act": "", "status": "OPEN \u2014 verified on main"}, {"id": "LIVE-003", "area": "TIER0", "sev": "high", "item": "SklearnCompatible NN fit() marks fitted without training; clone-and-refit yields a silently untrained model that still predicts", "detail": "", "ev": "models/nn_whuber.py fit() in both regressor and classifier", "act": "", "status": "OPEN \u2014 verified on main"}, {"id": "STRUCT-001", "area": "TIER0", "sev": "critical", "item": "Global fallback pipeline slot picks an arbitrary member by dict insertion order AND returns a shared object that .fit() mutates in place", "detail": "", "ev": "utils/session_state.py:501; pages/06:1813-1821 now DISCLOSES borrowers but root cause unchanged", "act": "", "status": "PARTIAL \u2014 PR #145 added disclosure, not a fix"}, {"id": "STRUCT-002", "area": "TIER0", "sev": "critical", "item": "Row identity: lockbox seals index LABELS, splits store POSITIONS (np.where(mask)[0]), page 07 reads df_raw.iloc[test_indices]", "detail": "", "ev": "pages/06:398,696-702; pages/07:185,196; utils/test_lockbox.py train_row_mask", "act": "", "status": "PARTIAL \u2014 PR #145 fixed the reset_index instance + test_row_labels_are_identities.py; conventions still dual"}, {"id": "STRUCT-003", "area": "TIER0", "sev": "critical", "item": "No split function exists; ~370 lines of split logic live in pages/06:380-760. ml/splits.py is 20 loc with one helper", "detail": "", "ev": "ml/splits.py; pages/06_Train_and_Compare.py", "act": "", "status": "OPEN \u2014 verified on main"}, {"id": "STRUCT-004", "area": "TIER0", "sev": "critical", "item": "The only step-completion state machine is render_sidebar_workflow, filed in utils/theme.py", "detail": "", "ev": "utils/theme.py:685 (938 loc file, 12 importers)", "act": "", "status": "OPEN \u2014 verified on main"}, {"id": "STRUCT-005", "area": "TIER0", "sev": "high", "item": "Coach is a pure annotator; cannot gate, only order. No blocker severity, no own confidence tier, 100% of triggers in pages/", "detail": "", "ev": "ml/model_coach.py; pages/05_Preprocess.py:291-294; pages/02_EDA.py:212,246", "act": "", "status": "OPEN \u2014 feasibility verdict"}, {"id": "TEST-001", "area": "TIER0", "sev": "critical", "item": "tests/integration/conftest.py injects a bare sklearn Ridge where the app stores wrapper objects", "detail": "", "ev": "tests/integration/conftest.py:82,98", "act": "", "status": "OPEN \u2014 verified on main"}, {"id": "TEST-002", "area": "TIER0", "sev": "critical", "item": "test_insight_id_integrity.py is an AST scanner keyed on SCAN_DIRS=['pages','utils','ml'] and the literal name Insight; passes vacuously after rename", "detail": "", "ev": "tests/test_insight_id_integrity.py:23", "act": "", "status": "OPEN \u2014 verified on main"}, {"id": "TEST-003", "area": "TIER0", "sev": "critical", "item": "No test calls the production reset_downstream_results(); three re-implementations test themselves", "detail": "", "ev": "utils/session_state.py; tests/test_cascade_invalidation.py", "act": "", "status": "OPEN"}, {"id": "DROP-001", "area": "TIER0", "sev": "medium", "item": "utils/dataset_db.py \u2014 797 loc, zero importers, superseded by session_projects.py", "detail": "", "ev": "grep: 0 importers on main", "act": "", "status": "OPEN \u2014 verified on main"}, {"id": "DROP-002", "area": "TIER0", "sev": "medium", "item": "setup.py python_requires '>=3.8,<3.10' against a 3.12 repo \u2014 uninstallable", "detail": "", "ev": "setup.py", "act": "", "status": "OPEN \u2014 verified on main"}, {"id": "DROP-003", "area": "TIER0", "sev": "low", "item": "decision_curve_analysis has zero production callers but is README-advertised", "detail": "", "ev": "ml/calibration.py", "act": "", "status": "OPEN \u2014 verified on main"}, {"id": "WIP-001", "area": "TIER0", "sev": "high", "item": "docs/FINDINGS_LEDGER.md 'Still open' tail from two lost audit runs, on the multi-file/JSON import path", "detail": "", "ev": "docs/FINDINGS_LEDGER.md:47", "act": "", "status": "OPEN \u2014 verified on main"}, {"id": "CONTRACT-001", "area": "CONTRACT", "sev": "critical", "item": "Row identity is TWO incompatible conventions: index LABELS (lockbox/cohort) vs POSITIONS (train/val/test_indices)", "detail": "utils/test_lockbox.py stores `labels` as index LABELS and `train_row_mask()` tests `lbl not in test_set`. But pages/06 computes `original_indices = np.where(mask)[0]` \u2014 POSITIONAL offsets into the frame returned by `get_data()` at split time \u2014 and stores `st.session_state.train_indices/val_indices/test_indices = original_indices[...].tolist()`. Page 07 then does `df_raw.iloc[test_indices]` against a FRESH `get_data()` call. If the row set changed between Train and Explain (cohort started/cleared, `filtered_data` written by page 05, FE re-applied, a cleaning action dropping rows) the .iloc selects DIFFERENT PEOPLE with no error \u2014 SHAP/permutation/subgroup results are then computed on the wron\u2026", "ev": "pages/06_Train_and_Compare.py:383 (`original_indices = np.where(mask)[0]`), :683-693; pages/07_Explainability.py:185,196,1196; utils/test_lockbox.py:237-248", "act": "project \u2014 store split membership as index labels (Index/boolean mask) in the Project; drop the positional *_indices keys entirely", "status": "UNVERIFIED"}, {"id": "CONTRACT-002", "area": "CONTRACT", "sev": "critical", "item": "Page 03 Feature Engineering hand-rolls a partial cascade instead of calling reset_downstream_results()", "detail": "The Save handler clears 18 keys inline but MISSES: pdp_results, bootstrap_results, baseline_results, calibration_results, cv_results, train_indices/val_indices/test_indices, target_transformer, target_label_encoder, y_train_original/y_val_original/y_test_original, feature_names, feature_names_by_model, cv_strategy, cv_groups_train, eda_results, dataset_profile, plus the ledger rollback and provenance-section clearing that reset_downstream_results() performs. Result: after FE is applied, Report Export still shows bootstrap CIs, calibration slopes and baseline comparisons for models that no longer exist, and Explainability re-uses a stale target_label_encoder. reset_downstream_results' own doc\u2026", "ev": "pages/03_Feature_Engineering.py:1219-1251 vs utils/session_state.py:283-415", "act": "project \u2014 the invalidation DAG must be the ONLY writer of downstream clears; delete the inline block", "status": "UNVERIFIED"}, {"id": "CONTRACT-003", "area": "CONTRACT", "sev": "critical", "item": "macro_shape's @st.cache_data functions exclude the DataFrame from the cache key", "detail": "`compute_pca(_df_numeric, ...)`, `compute_umap(_df_numeric, ...)`, `compute_persistence(_df_numeric, ...)`, `compute_mapper(_df_numeric, ...)` all take the frame as a LEADING-UNDERSCORE argument, which is Streamlit's marker for 'do not hash this'. The remaining key is scalar defaults only, so a second dataset with identical scalar params returns the FIRST dataset's cached PCA/UMAP/persistence/Mapper result. The codebase already knows the correct pattern \u2014 pages/02_EDA.py:146 passes an explicit `data_id=_data_fingerprint` alongside `_df`. macro_shape omits it.", "ev": "ml/macro_shape.py:82-86, 240-247, 337-342, 495-501; correct pattern at pages/02_EDA.py:146-166", "act": "engine \u2014 make these pure functions with an explicit content-fingerprint argument; move memoization into the Jobs layer", "status": "UNVERIFIED"}, {"id": "CONTRACT-004", "area": "CONTRACT", "sev": "critical", "item": "ml/eda_actions.py imports a function that does not exist, inside a bare except \u2014 4 insights are silently never recorded", "detail": "`from utils.storyline import add_insight` appears 4 times; utils/storyline.py defines only render_breadcrumb, _get_navigation_order, get_prev_next_pages, render_page_navigation. Each call is wrapped in `try: ... except: pass`, so physio_plausibility, missingness_association, target_outliers and collinearity insights have been silently dropped. DANGER IN BOTH DIRECTIONS: leaving the dead import in place keeps hiding 4 findings; 'fixing' the import during migration will suddenly start emitting 4 new ledger entries that flow into the manuscript narrative, changing generated text and TRIPOD status for every existing project.", "ev": "ml/eda_actions.py:196-197, 280-281, 427-428, 617-618; utils/storyline.py (no add_insight)", "act": "record \u2014 decide deliberately: port them to insight_ledger.upsert() as a versioned behavior change, or drop them; never leave the silent except", "status": "UNVERIFIED"}, {"id": "CONTRACT-005", "area": "CONTRACT", "sev": "critical", "item": "session_manager writes every DataFrame with to_parquet(index=False) while lockbox and cohort identify rows by index label", "detail": "`_df_to_parquet_bytes` drops the index; on restore pandas assigns a fresh RangeIndex. lockbox.json restores `labels` verbatim (and deliberately coerces them to int/str to avoid a type mismatch). If raw_data/working_table carried anything other than a contiguous 0..n-1 RangeIndex at save time \u2014 any frame produced by a merge, a row-dropping cleaning action, or a cohort slice \u2014 the restored labels name different rows. `train_row_mask` then returns a wrong mask with no error, and the 'sealed' test set is silently no longer sealed.", "ev": "utils/session_manager.py:169-172, :278-306; utils/test_lockbox.py:16-17 (\"stores index LABELS ... so membership survives\"), :247-248", "act": "project \u2014 persist the index explicitly (to_parquet(index=True)) and add a restore-time assertion that every lockbox label exists in the restored index", "status": "UNVERIFIED"}, {"id": "CONTRACT-006", "area": "CONTRACT", "sev": "critical", "item": "_log_to_ledger swallows every exception", "detail": "`except Exception: pass # Never break methodology logging if ledger has issues`. Any schema drift in Insight (a renamed field, a new required arg) causes methodology decisions to stop reaching the ledger \u2014 and therefore stop reaching the Methods section, TRIPOD auto-completion and the narrative \u2014 with zero signal. This is the highest-value place in the codebase to add a loud failure during migration.", "ev": "utils/session_state.py:655-656", "act": "record \u2014 replace with explicit error surfacing; a dropped methodology entry is a silently wrong manuscript", "status": "UNVERIFIED"}, {"id": "CONTRACT-007", "area": "CONTRACT", "sev": "critical", "item": "reset_downstream_results reaches into the ledger and rolls back resolutions by page name", "detail": "`ledger.rollback_resolutions({'05_Preprocess','06_Train_and_Compare','07_Explainability','08_Sensitivity_Analysis','09_Hypothesis_Testing'} [+ FE/FS pages])` and `ledger.prune_auto_generated({'02_EDA','05_Preprocess','06_Train_and_Compare'})`. The Record \u2014 described as append-mostly \u2014 is mutated by the invalidation path using hard-coded PAGE NAME STRINGS. When pages disappear in the Feed rebuild, these string sets silently match nothing and stale resolutions survive into the manuscript.", "ev": "utils/session_state.py:396-410; utils/insight_ledger.py:803-836", "act": "record \u2014 key rollback on the DAG node that produced the insight, not on a page-name string that the migration deletes", "status": "UNVERIFIED"}, {"id": "MINE-001", "area": "MINE", "sev": "critical", "item": "Positional unit-conversion factors + reconcile_pipeline_columns silently misalign to the wrong variables", "detail": "build_unit_harmonization_config builds `conversion_factors = []` by appending once per column while iterating `numeric_features` in order (ml/pipeline.py:41-64); build_plausibility_bounds does the same with `zip(numeric_features, conversion_factors)` (ml/pipeline.py:84). Both lists are handed positionally into `UnitHarmonizer(unit_harmonization_factors)` (ml/pipeline.py:178) and `PlausibilityGate(lower_bounds, upper_bounds)` (ml/pipeline.py:185-188) as the FIRST steps of the numeric branch, which sees a bare ndarray. reconcile_pipeline_columns then trims the ColumnTransformer's column selector (`kept = [c for c in cols if c in available]`, ml/pipeline.py:528) but does `clone(trans)` (line 53\u2026", "ev": "ml/pipeline.py:41-64, 84, 178, 185-188, 528-531; ml/preprocess_operators.py:21-24, 60-72", "act": "engine \u2014 make UnitHarmonizer/PlausibilityGate take Dict[column_name, factor] and resolve by name at transform time using feature_names_in_. WRONG-BUT-PLAUSIBLE OUTPUT: a mg/dL\u2192mmol/L factor applied to age, and a cholesterol column gated aga\u2026", "status": "UNVERIFIED"}, {"id": "MINE-002", "area": "MINE", "sev": "critical", "item": "Test-set lockbox stores index LABELS but session save writes parquet with index=False", "detail": "The lockbox freezes `\"labels\": list(test_labels)` (utils/test_lockbox.py:213) and membership is tested by label: `return pd.Series([lbl not in test_set for lbl in index], index=index)` (utils/test_lockbox.py:248). The docstring states the contract: 'The lockbox stores index LABELS (not positions) so membership survives feature engineering and row filtering, which preserve the original index' (utils/test_lockbox.py:16-17). But `_df_to_parquet_bytes` does `df.to_parquet(buf, index=False)` (utils/session_manager.py:171), so raw_data / df_engineered / filtered_data / working_table all come back with a fresh RangeIndex. filtered_data and df_engineered are exactly the frames that PRESERVE a non-co\u2026", "ev": "utils/test_lockbox.py:16-17, 213, 248; utils/session_manager.py:73-78, 171, 485-489", "act": "project \u2014 serialize dataframes with the index preserved (index=True + explicit index round-trip), or move the lockbox to a content-addressed row key. WRONG-BUT-PLAUSIBLE OUTPUT: after Save/Resume, labels [5,9,40] land on RangeIndex rows 5,9\u2026", "status": "UNVERIFIED"}, {"id": "MINE-003", "area": "MINE", "sev": "critical", "item": "macro_shape's four @st.cache_data functions hash NOTHING \u2014 every dataset gets the first dataset's PCA/UMAP/TDA", "detail": "Streamlit skips hashing parameters whose name starts with an underscore. compute_pca(_df_numeric, n_components=None, max_features=200), compute_umap(_df_numeric, ...), compute_persistence(_df_numeric, ...), compute_mapper(_df_numeric, ...) take the frame as `_df_numeric` and every other arg has a default. Call sites pass ONLY the frame: `compute_pca(df_numeric)` / `compute_umap(df_numeric)` / `compute_persistence(df_numeric)` / `compute_mapper(df_numeric)` (pages/02_EDA.py:1163, 1205, 1226, 1265). Cache key is therefore constant. pages/02_EDA.py:122-125 shows the team knew about this trap and added `_data_fingerprint` for its own caches \u2014 macro_shape never got one.", "ev": "ml/macro_shape.py:82-85, 240-247, 337-343, 495-502; pages/02_EDA.py:122-125, 1163, 1205, 1226, 1265", "act": "engine \u2014 delete the four decorators when macro_shape moves out of the view (its ONLY streamlit usage is these 4 decorators plus the import). Be aware this is a BEHAVIOR CHANGE that fixes a live bug: screenshots/goldens taken before the move\u2026", "status": "UNVERIFIED"}, {"id": "MINE-004", "area": "MINE", "sev": "critical", "item": "Target-leakage detection is wrapped in `except Exception: pass`", "detail": "The >0.95 feature-target correlation scan that populates `signals.leakage_flags` and `signals.leakage_candidate_cols` is inside a bare try, and any failure yields silence: `except Exception: pass # Skip leakage detection if correlation fails`. The EDA page then reports no leakage candidates, which is indistinguishable from 'checked and clean'.", "ev": "ml/eda_recommender.py:170-183", "act": "engine + record \u2014 on failure emit a ledger Insight ('leakage check could not run') exactly as ml/import_doctor.diagnose does with its `checks_failed` finding (ml/import_doctor.py:812-824). WRONG-BUT-PLAUSIBLE OUTPUT: a post-outcome lab valu\u2026", "status": "UNVERIFIED"}, {"id": "MINE-005", "area": "MINE", "sev": "critical", "item": "train_row_mask silently returns all-True when the lockbox is absent, and render_lockbox_status returns silently too", "detail": "`lb = get_lockbox(); if lb is None or is_exploratory(): return pd.Series(True, index=index)` (utils/test_lockbox.py:244-246) \u2014 no lockbox means every row is visible to target-aware steps. The status chip that would disclose this also returns without rendering anything: `lb = get_lockbox(); if lb is None: return` (utils/test_lockbox.py:261-263). Exploratory mode DOES warn (line 253-260); a missing lockbox does not. get_lockbox itself is a plain session_state read that returns None on any key rename.", "ev": "utils/test_lockbox.py:36-40, 244-246, 261-263", "act": "project + view \u2014 make the absence of a lockbox an explicit, rendered state, not None. WRONG-BUT-PLAUSIBLE OUTPUT: one renamed key during the session_state\u2192Project move silently disables the entire methodological spine of the app. Feature se\u2026", "status": "UNVERIFIED"}, {"id": "MINE-006", "area": "MINE", "sev": "critical", "item": "OutlierCapping sets fitted attributes in __init__ and returns X unchanged when unfitted", "detail": "`self.lower_bounds_: Optional[np.ndarray] = None` and `self.upper_bounds_` are assigned in `__init__` (ml/preprocess_operators.py:81-82). Trailing-underscore attributes are sklearn's fitted marker, so check_is_fitted sees a fitted estimator. transform then does `if self.lower_bounds_ is None or self.upper_bounds_ is None: return X_arr` (lines 102-103) \u2014 a silent no-op. Separately, __init__ also rewrites its own params (`self.params = params or {}`, line 80), which breaks sklearn's clone contract that UnitHarmonizer's docstring explicitly calls out ('Store conversion_factors by reference (no copy) so sklearn clone works', line 13).", "ev": "ml/preprocess_operators.py:13, 78-82, 100-104", "act": "engine \u2014 move fitted attrs out of __init__ and raise NotFittedError in transform. WRONG-BUT-PLAUSIBLE OUTPUT: any migration path that reconstructs a pipeline from stored config and calls transform without fit produces uncapped data while th\u2026", "status": "UNVERIFIED"}, {"id": "MINE-007", "area": "MINE", "sev": "critical", "item": "_log_to_ledger swallows every exception, so methodology decisions vanish from the record while methodology_log looks fine", "detail": "The bridge that writes each log_methodology call into the unified ledger ends with `except Exception: pass # Never break methodology logging if ledger has issues` (utils/session_state.py:655-656). log_methodology appends to `st.session_state.methodology_log` FIRST (line 543) and calls _log_to_ledger after, so the plain log survives while the ledger silently does not. ml/publication.py:153-160 prefers `ledger.get_methodology_log()` and only falls back to methodology_log when the ledger is EMPTY \u2014 a PARTIALLY populated ledger wins and the fallback never fires.", "ev": "utils/session_state.py:543-544, 599, 639-656; ml/publication.py:153-166", "act": "record \u2014 the ledger write must be the primary path and must surface failures. WRONG-BUT-PLAUSIBLE OUTPUT: an Insight dataclass field rename during the Record migration makes ledger.upsert throw for, say, Preprocessing and Model Training onl\u2026", "status": "UNVERIFIED"}, {"id": "MINE-008", "area": "MINE", "sev": "critical", "item": "ml/eda_actions.py imports a function that does not exist, inside bare `except:` \u2014 4 EDA insight classes have been silently dead", "detail": "Four sites do `from utils.storyline import add_insight` then `add_insight(...)`, wrapped in `except:` / `except Exception: pass`. utils/storyline.py has no add_insight \u2014 its docstring says 'Insight management has moved to utils/insight_ledger.py (InsightLedger)' (utils/storyline.py:4). So physio_plausibility, missingness_association, target_outliers and collinearity insights never reach the ledger and never raise. The existing guard test only scans pages/ (`for page_file in sorted(os.listdir(pages_dir))`), so ml/eda_actions.py is outside its blast radius.", "ev": "ml/eda_actions.py:196, 280, 427, 617 (and bare `except:` at 199, 430, 620); utils/storyline.py:4 (module defines only render_breadcrumb/_get_navigation_order/get_prev_next_pages/render_page_navigation); tests/test_unified_ledger_integration\u2026", "act": "engine + record \u2014 rewrite these to return insight payloads the caller records, and widen the legacy-pattern test to ml/ and utils/. WRONG-BUT-PLAUSIBLE OUTPUT: the app has been asserting a clean bill of health on multicollinearity, target o\u2026", "status": "UNVERIFIED"}, {"id": "MINE-009", "area": "MINE", "sev": "critical", "item": "set_global_seed runs at page-06 module scope, so determinism depends on Streamlit rerun ordering", "detail": "`set_global_seed(st.session_state.get('random_seed', 42))` sits at module level in pages/06_Train_and_Compare.py:95, i.e. it re-seeds numpy's and Python's GLOBAL RNG on every script rerun of that page. set_global_seed calls `np.random.seed(seed)`, `random.seed(seed)`, `torch.manual_seed(seed)` and sets `torch.backends.cudnn.deterministic = True` (utils/seed.py:21-29). No other page calls it. get_global_seed reads Streamlit directly: `import streamlit as st; return st.session_state.get('random_seed', 42)` (utils/seed.py:36-37).", "ev": "pages/06_Train_and_Compare.py:95, 120-122; utils/seed.py:14-29, 34-37", "act": "jobs \u2014 each job must seed its own RNG at job start from the Project's seed and, better, thread explicit np.random.Generator objects instead of touching globals. utils/seed.get_global_seed lands in project (drop the streamlit import). WRONG-\u2026", "status": "UNVERIFIED"}, {"id": "MINE-010", "area": "MINE", "sev": "critical", "item": "_content_fingerprint returns None on unhashable cells, downgrading invalidation to schema-only without saying so", "detail": "`try: return hash((df.shape, int(pd.util.hash_pandas_object(df, index=False).sum()))) except Exception: return None` (utils/session_state.py:227-230). set_data only clears downstream results when BOTH fingerprints are non-None and differ: `elif (old_df is not None and old_fp is not None and new_fp is not None and old_fp != new_fp)` (utils/session_state.py:276-277). Nested-JSON imports are a first-class supported path (data_processor._JSON_WRAPPER_KEYS, records_key) and can leave list/dict cells; data_processor.py:211-214 already swallows the check that would flatten them (`except Exception: has_nonscalar = False`).", "ev": "utils/session_state.py:220-230, 266-280; data_processor.py:205-218", "act": "project \u2014 the DAG's dirty-check must not have a silent 'unknown \u2192 assume clean' branch; make it 'unknown \u2192 assume dirty' and disclose. WRONG-BUT-PLAUSIBLE OUTPUT: a researcher re-uploads a corrected version of the same JSON (same columns, f\u2026", "status": "UNVERIFIED"}, {"id": "MINE-011", "area": "MINE", "sev": "critical", "item": "Stability-selection probability divides by n_bootstrap, not by the number of fits that actually succeeded", "detail": "Failed bootstrap fits are skipped with `except Exception: continue` (ml/feature_selection.py:252-253) without decrementing anything, then `selection_probs = selection_counts / n_bootstrap` (line 256) and `selected = [f for f, p in zip(feature_names, selection_probs) if p >= threshold]` (line 257).", "ev": "ml/feature_selection.py:245-258", "act": "engine \u2014 track n_successful and divide by it; refuse (or flag) when the success rate is low. WRONG-BUT-PLAUSIBLE OUTPUT: on a wide matrix where half the Lasso fits fail to converge, every feature's selection probability is halved, nothing c\u2026", "status": "UNVERIFIED"}, {"id": "MINE-012", "area": "MINE", "sev": "critical", "item": "publication.py's 12 session_state reads are each wrapped in `except ImportError: pass`, so Methods paragraphs disappear silently once Streamlit is removed", "detail": "Every session_state read in the manuscript generator is `try: import streamlit as st; ... except ImportError: pass`. Uninstalling streamlit makes the import raise ImportError, which is caught, and the guarded block is skipped entirely. Affected blocks and what they emit: line 597-620 = the entire '### Feature Engineering' section; 1171-1178 = SHAP and Bland-Altman in '### Model Interpretability'; 1220-1235 = the whole Sensitivity Analysis section; 1349-1360 = the CV-on-pre-transformed-data limitation; 1381-1392 = the feature-dropout methodology caveat; 1401-1412 = the double-transformation caveat; 1826-1834 = generate_decision_audit_trail returns '' (the whole decision appendix). Line 161 is\u2026", "ev": "ml/publication.py:159-166, 212-215, 597-620, 731-737, 1171-1178, 1220-1235, 1349-1360, 1381-1392, 1401-1412, 1826-1834", "act": "record \u2014 thread an explicit Project/Record object into generate_methods_section (utils/workflow_provenance.get_methods_context at utils/workflow_provenance.py:479-483 already exists for exactly this: 'This replaces the 100+ scattered sessio\u2026", "status": "UNVERIFIED"}, {"id": "MINE-013", "area": "MINE", "sev": "critical", "item": "models/* use `is_fitted` but ml/estimator_utils only recognizes `is_fitted_`", "detail": "BaseModelWrapper sets `self.is_fitted = False` (models/base.py:22, no trailing underscore) and RegistryModelWrapper sets `self.is_fitted = True` on fit (models/registry_wrappers.py:37); GLMWrapper, HuberGLMWrapper and RFWrapper inherit this. Only models/nn_whuber.py uses `is_fitted_` (lines 104, 126, 162, 185, 567). is_estimator_fitted checks `hasattr(estimator, 'is_fitted_')` in BOTH fallback branches (ml/estimator_utils.py:25-26, 41-42), and check_is_fitted on a non-sklearn wrapper raises TypeError, routing to the permissive branch which looks for coef_/feature_importances_/n_features_in_ on the WRAPPER (they live on the inner .model).", "ev": "models/base.py:22; models/registry_wrappers.py:37, 58, 64; models/nn_whuber.py:104; ml/estimator_utils.py:19-43; pages/07_Explainability.py:172", "act": "engine \u2014 unify on one fitted-marker and add tests; ALL of models/* is currently test-uncovered so nothing catches this. WRONG-BUT-PLAUSIBLE OUTPUT: `if estimator is None or not is_estimator_fitted(estimator): return None, None, None, None` \u2026", "status": "UNVERIFIED"}, {"id": "MINE-014", "area": "MINE", "sev": "critical", "item": "Explainability re-derives the test set POSITIONALLY from a frame that may have changed since training", "detail": "Page 06 stores positions, not labels: `original_indices = np.where(mask)[0]` then `st.session_state.test_indices = original_indices[...].tolist()` (pages/06_Train_and_Compare.py:383, 683-693). Page 07 reads them back as positions into a freshly-fetched frame: `raw_candidate = df_raw.iloc[test_indices]` and `y_raw = df_raw[data_config.target_col].iloc[test_indices].values` where `df_raw = get_data()` (pages/07_Explainability.py:182-198). get_data() applies cohort filtering and prefers df_engineered over filtered_data over raw_data (utils/session_state.py:194-217), so the row set it returns can differ between the two page visits.", "ev": "pages/06_Train_and_Compare.py:383, 683-693; pages/07_Explainability.py:182, 184, 198; utils/session_state.py:194-217", "act": "project \u2014 the Project must expose the test set as row LABELS with a single accessor; positional indices across page boundaries cannot survive a job queue where the frame is re-materialized. WRONG-BUT-PLAUSIBLE OUTPUT: switch cohorts (or app\u2026", "status": "UNVERIFIED"}, {"id": "SWEEP-001", "area": "SWEEP", "sev": "critical", "item": "utils/theme.py:685 render_sidebar_workflow \u2014 the Router's readiness predicate is hiding inside the CSS module", "detail": "938 loc, 53 st.* refs, 20 session_state refs. inject_custom_css() (:10) is pure CSS. But render_sidebar_workflow(current_page) computes the app's entire step-completion state machine inline: data_uploaded=get_data() is not None; data_configured=session_state['data_config'].target_col is not None; features_selected='feature_selection_results'; pipeline_built='preprocessing_pipeline'; models_trained=bool('trained_models'); explainability_run=len('permutation_importance')>0 or len('shap_results')>0; sensitivity_run='sensitivity_seed_results'; report_generated='report_data'; stat_validation_run='hypothesis_test_results' or len('custom_table1_tests')>0; feature_engineering_applied. It also owns t\u2026", "ev": "utils/theme.py:685-745 (predicates at :746-760), imports at :693-695, :713", "act": "router (extract the 10 predicates + core/advanced split verbatim); view (CSS, cards, flash)", "status": "UNVERIFIED"}, {"id": "SWEEP-002", "area": "SWEEP", "sev": "critical", "item": "ml/macro_shape.py \u2014 four @st.cache_data functions whose cache key is CONSTANT; first dataset's PCA/UMAP/TDA is served to every later dataset in the process", "detail": "Streamlit skips hashing parameters whose name starts with '_'. compute_pca(_df_numeric, n_components=None, max_features=200) at :82, compute_umap(_df_numeric, n_neighbors=15, min_dist=0.1, max_samples=10_000, max_features=200) at :240, compute_persistence(_df_numeric, max_samples=2_000, max_features=50, homology_dimensions=(0,1)) at :337, compute_mapper(_df_numeric, n_cubes=10, overlap=0.3, max_samples=3_000, max_features=100) at :495. Every call site passes ONLY the unhashed frame: pages/02_EDA.py:1163 compute_pca(df_numeric), :1205 compute_umap(df_numeric), :1226 compute_persistence(df_numeric), :1265 compute_mapper(df_numeric). So the key reduces to the defaults \u2014 a constant. st.cache_dat\u2026", "ev": "ml/macro_shape.py:82,240,337,495; pages/02_EDA.py:1163,1205,1226,1265 vs :122-124,146,169", "act": "engine (bodies are pure); jobs (long-running: TDA is O(n^3)) \u2014 key any memo on a real content fingerprint, never on an underscore convention", "status": "UNVERIFIED"}, {"id": "SWEEP-003", "area": "SWEEP", "sev": "critical", "item": "utils/datasets.py mutates the PROCESS-GLOBAL numpy RNG \u2014 three np.random.seed() calls that will silently corrupt concurrent jobs", "detail": "generate_linear_with_outliers(:17), generate_nonlinear_regression(:49), generate_imbalanced_classification(:77) each call `np.random.seed(random_state)` rather than using np.random.default_rng. get_builtin_datasets() at :105 returns the name->callable map; single importer pages/01_Upload_and_Audit.py:22. Same class: utils/seed.py:21-27 set_global_seed() sets np.random.seed + random.seed + torch.manual_seed + torch.backends.cudnn.deterministic=True/benchmark=False, and models/nn_whuber.py:314-315 does torch.manual_seed + np.random.seed. Today, one Streamlit script run at a time makes this safe. Under a Jobs worker pool it is not: a user loading a demo dataset in one job reseeds the global RNG\u2026", "ev": "utils/datasets.py:17,49,77; utils/seed.py:21-27; models/nn_whuber.py:314-315", "act": "engine (convert all three to np.random.default_rng(random_state)); jobs (every job must carry an explicit RNG, never touch global state)", "status": "UNVERIFIED"}, {"id": "SWEEP-004", "area": "SWEEP", "sev": "critical", "item": "INVARIANT \u2014 'The app may be silent, and it may refuse, but it must never assert something false.' High confidence is a PROMISE, not a score", "detail": "docs/FINDINGS_LEDGER.md:19-25, restated verbatim in docs/audit/RESUME.md:26-30, is the repo's governing rule: '`high` confidence is the only tier the UI pre-selects, so `high` means the app is asserting. Nothing uncertain may reach it. A confidently-wrong answer is worse than a crash, because a crash gets reported and a wrong number gets published.' A function-level inventory sees `confidence: str` on the import/join doctor results and treats it as data. It is a UI contract: the tier binds to whether the app pre-selects. The Router INHERITS this the moment it decides which question is asked next with a pre-filled answer \u2014 auto-advance IS pre-selection, so the Router may only auto-advance on \u2026", "ev": "docs/FINDINGS_LEDGER.md:17-25; docs/audit/RESUME.md:24-30", "act": "router", "status": "UNVERIFIED"}, {"id": "SWEEP-005", "area": "SWEEP", "sev": "critical", "item": "INVARIANT \u2014 plane authority order: advise \u2192 witness \u2192 publish. The Advisory plane may NEVER write into the Narrative plane", "detail": "docs/ARCHITECTURE_SCROLLYTELLING_BRIEF.md \u00a78: 'The four planes advise/witness/publish in that order of authority. Never draw the Advisory plane writing directly into the Narrative plane \u2014 advice reaches the manuscript only by being acted on and thereby recorded.' Enforced concretely by the manuscript register separation (CODE_REVIEW 'Manuscript-trust wave'): Insight.manuscript_text carries reviewer-facing phrasing for every auto-generated producer so coaching voice ('a reviewer would question\u2026', 'consider\u2026') 'can no longer reach the Discussion verbatim, and the regex cleaner is now a fallback rather than the mechanism'. The NEW Router is precisely the component that will be tempted to short-\u2026", "ev": "docs/ARCHITECTURE_SCROLLYTELLING_BRIEF.md \u00a78; CODE_REVIEW.md 'Manuscript-trust wave' (1); utils/insight_ledger.py:580 theory_anchor / manuscript_text field", "act": "router (Router writes to Project and reads Record; it must have NO write path into Record)", "status": "UNVERIFIED"}, {"id": "SWEEP-006", "area": "SWEEP", "sev": "critical", "item": "INVARIANT \u2014 the no-pickle / _NEVER_PERSIST session contract is a SECURITY property, and the code is deliberately better than its docs", "detail": "ARCHITECTURE.md:182-188: save/load is a ZIP of JSON+Parquet, 'Deliberately not pickle-based: pickle files are rejected on load for security, and fitted models/pipelines are never persisted (_NEVER_PERSIST) \u2014 after restoring a session, models are re-trained by re-running Train & Compare. What round-trips: data, configuration, ledger entries, provenance records, and methodology logs.' Backed by utils/session_manager.py:60 _ACCEPTED_SCHEMA_VERSIONS={'2.0','2.1'}, :65-67 _MAX_UPLOAD_BYTES/_MAX_EXTRACTED_BYTES/_MAX_MEMBERS zip-bomb caps, :123 _SAFE_WIDGET_KEYS, :138 _NEVER_PERSIST. CODE_REVIEW verdict: 'Holds (better than documented)'. THE RISK: the AnalysisProject design already has to_dict/from\u2026", "ev": "ARCHITECTURE.md:182-188; utils/session_manager.py:60,65-67,123,138", "act": "project (Project.to_dict must inherit _NEVER_PERSIST and the pickle refusal by construction, not by convention)", "status": "UNVERIFIED"}, {"id": "SWEEP-007", "area": "SWEEP", "sev": "critical", "item": "INVARIANT \u2014 'nothing is written to disk; each browser session is fully isolated.' A job queue is the single most likely violator", "detail": "ARCHITECTURE.md:13 and :171-174: 'session-state-only project/dataset management (nothing is persisted to disk; sessions are saved as user-downloaded archives)'. CODE_REVIEW verifies it: the SQLite layer and utils/persistence.py are dead code with no callers; session archives are built in-memory; PDF compiles in an auto-cleaned tempdir. The README caveat the reviewers raised is that 'All processing happens in your browser session' is already inaccurate for server-side Streamlit. A Jobs layer needs somewhere to park intermediate state and results between worker and web process \u2014 that is a disk write, and it is invisible to any function-level inventory because no function does it today.", "ev": "ARCHITECTURE.md:13,171-180; CODE_REVIEW.md 'Privacy: Nothing is written to disk' row", "act": "jobs (decide the persistence story explicitly and update ARCHITECTURE.md in the same commit \u2014 design principle #7)", "status": "UNVERIFIED"}, {"id": "SWEEP-008", "area": "SWEEP", "sev": "critical", "item": "INVARIANT \u2014 lockbox timing: sealed at stage 1, respected through 2\u20135, opened EXACTLY ONCE at stage 6. A scrolling interview makes 'exactly once' hard", "detail": "docs/ARCHITECTURE_SCROLLYTELLING_BRIEF.md \u00a74A: '~15% drawn into a sealed vault\u2026 Every target-aware step (EDA, FE fits, feature selection, the coach probe) operates on train_row_mask() rows only\u2026 The vault opens exactly once, at stage 6.' CODE_REVIEW's remediation adds the laundering guard: 'An explicit, watermarked Exploratory mode preserves fast full-data screening, and toggling it in EITHER direction resets downstream results so exploratory selection can never launder into a clean manuscript.' In a single scrolling feed the user can scroll back up and change an earlier answer AFTER stage 6 has opened the vault \u2014 a page-ordered app gets 'before anyone looks' for free from navigation order; \u2026", "ev": "docs/ARCHITECTURE_SCROLLYTELLING_BRIEF.md \u00a74A; CODE_REVIEW.md remediation ('Both leakage criticals'); utils/test_lockbox.py:26,28", "act": "router + project (lockbox draw/open must be Project state transitions the Router gates, not an emergent property of scroll position)", "status": "UNVERIFIED"}, {"id": "SWEEP-009", "area": "SWEEP", "sev": "critical", "item": "INVARIANT \u2014 reset_downstream_results supports PARTIAL resets; a naive 'invalidate everything downstream of node N' DAG cannot express them", "detail": "CODE_REVIEW 2026-07 section: applying a feature selection must clear stale pipelines/splits/models while PRESERVING the selection just made \u2014 `reset_downstream_results(clear_feature_engineering=False, clear_feature_selection=False)`, described as 'a new flag that clears stale pipelines/splits/models while preserving the selection just made.' Feature-Engineering Skip and Reset route through the same helper, and Reset must RESTORE the pre-FE selected_features list (previously it never did, leaving selection referencing engineered columns that no longer existed \u2014 the reported NHANES crash: 'Some column names are not columns of the dataframe: {meds_hbp_has_data, meds_chol_has_data}'). ARCHITECTU\u2026", "ev": "CODE_REVIEW.md '2026-07 \u00b7 Feature-engineering state drift' (04:440, 04:483); ARCHITECTURE.md:278", "act": "project (the invalidation DAG needs per-edge suppression AND a restore-previous-value path, not just cascade-clear)", "status": "UNVERIFIED"}, {"id": "SWEEP-010", "area": "SWEEP", "sev": "critical", "item": "INVARIANT \u2014 get_data() applies the cohort filter ONCE, and exactly two callers are allowed to escape it", "detail": "utils/session_state.py:194-217. Precedence df_engineered > filtered_data > raw_data, then `from utils.cohorts import apply_cohort; return apply_cohort(df)`. Docstring: 'the rows of that cohort are ALL any page sees \u2014 the filter is applied here, once, rather than in each of the nine pages that would each have to remember. Pass full_study=True for the two things that must span the whole study: drawing the test lockbox (every cohort inherits its slice of ONE split) and choosing the target and features (fixed across runs, which is what makes the runs comparable at all).' Production callers of full_study=True: exactly two \u2014 pages/01_Upload_and_Audit.py:619 and utils/cohort_ui.py:324. The invarian\u2026", "ev": "utils/session_state.py:194-217; pages/01_Upload_and_Audit.py:619; utils/cohort_ui.py:324; tests/test_cohort_runs.py:75", "act": "project (two distinct named accessors \u2014 cohort_view and full_study \u2014 never one attribute with a flag)", "status": "UNVERIFIED"}, {"id": "SWEEP-011", "area": "SWEEP", "sev": "critical", "item": "INVARIANT \u2014 per-model pipeline forking is the differentiator and it is real; a Project schema that collapses to one pipeline destroys the product", "detail": "ARCHITECTURE.md:29: 'This layer forks per model. When a user selects Ridge + Random Forest + MLP, three separate preprocessing pipelines exist simultaneously.' Design principle #5: 'Per-model pipelines are first-class\u2026 This is not a complication to manage \u2014 it's the core value proposition.' CODE_REVIEW verified it holds: '05_Preprocess.py:738-874 builds distinct pipelines per model; 06_Train_and_Compare.py:1050-1059 fits each model's own pipeline on train only.' The Project fields are plural by design: preprocessing_pipelines_by_model, preprocessing_config_by_model, fitted_preprocessing_pipelines, feature_names_by_model, fitted_estimators. A Jobs layer that parallelizes 'train the models' mu\u2026", "ev": "ARCHITECTURE.md:29,215,324-338; CODE_REVIEW.md 'What genuinely holds' rows 1-2", "act": "project + jobs", "status": "UNVERIFIED"}, {"id": "SWEEP-012", "area": "SWEEP", "sev": "critical", "item": "INVARIANT \u2014 CODE_REVIEW's seven 'Design principles for future front-end changes' are the rebuild's acceptance criteria, each traced to a real defect", "detail": "Verbatim: (1) 'Defaults carry the rigor; captions carry the disclosure; options carry the speed. The zero-click path must be the methodologically correct one. Never make rigor opt-in.' \u2014 directly constrains the Router's default next question. (2) 'State first, widgets second. Before drawing UI, name the session-state keys: who writes, who reads, who invalidates' \u2014 root cause of calibration_results and selected_models read-never-written; I found two MORE live instances this pass (working_df, dict-vs-dataclass), so the class is NOT closed. (3) 'Every number on screen names its data' \u2014 train/val/test/full-cohort chip; HARDER in a feed where a stale card stays visible above the fold. (4) 'UI cop\u2026", "ev": "CODE_REVIEW.md '## Design principles for future front-end changes' (items 1-7)", "act": "router (1,3,5); project (2,6); view (3,4); all (7)", "status": "UNVERIFIED"}, {"id": "SWEEP-013", "area": "SWEEP", "sev": "critical", "item": "INVARIANT \u2014 'Absent is better than false': the invalidation cascade must visibly clear ALL FOUR planes, not just the data plane", "detail": "docs/ARCHITECTURE_SCROLLYTELLING_BRIEF.md \u00a74B: a 'clear' wave ripples downstream across all four lanes, 'wiping stale results \u2014 pipelines, splits, models, insights, provenance sections. Tagline: Absent is better than false.' Acceptance checklist \u00a79: 'The invalidation cascade visibly clears all four lanes.' A DAG that only tracks data-derived artifacts and leaves the ledger and provenance sections intact reproduces critical finding C5 exactly ('reset_data_dependent_state() never clears shap_results, bootstrap_results, baseline_results, sensitivity_seed_results, hypothesis_test_results, methodology_log, or workflow_provenance\u2026 the manuscript embeds the previous dataset's SHAP plots with no war\u2026", "ev": "docs/ARCHITECTURE_SCROLLYTELLING_BRIEF.md \u00a74B, \u00a79; CODE_REVIEW.md C5; brief \u00a70 table (InsightLedger: rollback_resolutions, prune_auto_generated)", "act": "record + project ('append-mostly' must explicitly carve out rollback_resolutions/prune_auto_generated and provenance-section clearing)", "status": "UNVERIFIED"}, {"id": "SWEEP-014", "area": "SWEEP", "sev": "critical", "item": "tests/test_insight_id_integrity.py \u2014 an AST scanner that will silently stop testing anything the moment the rebuild renames Insight or the ledger variable", "detail": "Docstring: 'extracts, via AST (no imports, no Streamlit runtime): PRODUCED ids: every `...Insight(id=...)` constructor call with a string or f-string id, across pages/, utils/, ml/. REFERENCED ids: string literals passed to ledger.resolve/acknowledge/get/remove calls (receiver name containing \"ledger\")\u2026'. SCAN_DIRS = ['pages','utils','ml'] and _receiver_mentions_ledger() literally checks `'ledger' in ast.dump(node).lower()`. CODE_REVIEW records it caught a third live instance on its first run (train_prefer_simpler). Under the rebuild: SCAN_DIRS no longer exist (pages/ is deleted), the receiver may be `project.record` or `self._ledger`, and the constructor may not be named Insight. The scanne\u2026", "ev": "tests/test_insight_id_integrity.py:1-27,39-42; CODE_REVIEW.md 'Bucket-1 hardening wave'", "act": "record (port the scanner FIRST, against the new names, and assert a nonzero produced-id count so it can never pass vacuously)", "status": "UNVERIFIED"}, {"id": "SWEEP-015", "area": "SWEEP", "sev": "critical", "item": "Known test-fidelity hole exactly at the model-object boundary \u2014 the transition's safety net does not cover models/", "detail": "CODE_REVIEW 'Note on test fidelity': 'tests/integration/conftest.py injects a bare sklearn Ridge into trained_models, but the real Train page stores wrapper objects \u2014 the suites exercise downstream pages against a state shape the app never produces, which is how the Feature Dropout clone() breakage stayed invisible.' The concrete instance: pages/08:382 calls clone(model_obj) on wrapper objects that do not implement get_params, raising TypeError per feature, swallowed \u2192 all impacts 0. This compounds with the measured fact that ALL of models/* is uncovered by any test. So the layer the rebuild most needs to move intact (Engine/models) is simultaneously the least covered AND the one whose exist\u2026", "ev": "CODE_REVIEW.md 'Dynamic testing summary' note; tests/integration/conftest.py:67; pages/08_Sensitivity_Analysis.py:382 vs :104", "act": "engine (write characterization tests against the REAL wrapper objects before moving models/; do not trust the green suite)", "status": "UNVERIFIED"}, {"id": "SWEEP-016", "area": "SWEEP", "sev": "critical", "item": "WORK IN PROGRESS \u2014 two multi-file/JSON audit runs were in flight and their findings never landed; the referenced scratchpad does not exist in this checkout", "detail": "docs/FINDINGS_LEDGER.md:47-58 '## Still open \u2014 Populated from the two audit runs in flight: wf_70254f26-494 (fresh exhaustive hunt, ten lenses over the multi-file path and JSON\u2026) and wf_5446c57b-3f6 (all 48 recovered findings re-run against current HEAD\u2026). Nothing is closed on a verdict alone.' RESUME.md:56-59: 'Two background runs. If they did not finish, their findings are lost and the hunt is worth re-running.' The 'Closed in this wave' table lists 11 FIXED; the original 102-raw/48-confirmed set means a tail remains untriaged. Neither `scratchpad/audit/orig48/` (FINDINGS_LEDGER:64-66) nor `subagents/workflows/wf_e5abb4fe-e32/journal.jsonl` (:68) exists in this container \u2014 only docs/audit/\u2026", "ev": "docs/FINDINGS_LEDGER.md:47-72; docs/audit/RESUME.md:56-72,85-95; `ls scratchpad` \u2192 no such directory", "act": "do not collide \u2014 freeze ml/import_doctor.py, ml/join_doctor.py, utils/combine*.py and pages/01 as engine-move-only (no signature changes) until the ledger tail is closed", "status": "UNVERIFIED"}, {"id": "TEST-001", "area": "TEST", "sev": "critical", "item": "The invalidation DAG has NO test that calls the production function \u2014 three separate re-implementations test themselves", "detail": "LANDS: project (+ characterization test first). The real cascade is utils/session_state.py:283 `reset_downstream_results(clear_feature_engineering=True, restore_pre_fe_features=True, clear_feature_selection=True)`, invoked from pages/01_Upload_and_Audit.py:1058. But tests/workflow/test_state_invalidation.py:40 defines its OWN `simulate_feature_change_cascade(state, new_feature_cols)` with a local 30-key CASCADE_KEYS list, and tests/integration/test_cascade_invalidation.py:134 defines a 32-key DOWNSTREAM_KEYS_CLEARED_BY_FEATURE_CHANGE then manually clears it in a for-loop at line 177 and asserts it is cleared. Both are tautologies: they clear keys, then assert the keys they just cleared are c\u2026", "ev": "utils/session_state.py:283 vs tests/workflow/test_state_invalidation.py:40-51 and tests/integration/test_cascade_invalidation.py:134-196", "act": "BEFORE any move: write tests/characterization/test_invalidation_golden.py that imports the real reset_downstream_results, seeds all 65 production keys with sentinel {'marker':'STALE'}, calls it with each of the 8 flag combinations (clear_fe\u2026", "status": "UNVERIFIED"}, {"id": "TEST-002", "area": "TEST", "sev": "critical", "item": "Test fixtures compute a DIFFERENT invalidation hash than production \u2014 target-swap invalidation is untested by construction", "detail": "LANDS: project. Production hashes the full modeling problem: pages/01_Upload_and_Audit.py:1047-1051 builds `_config_sig = ','.join(sorted(data_config.feature_cols)) + f\"|target={data_config.target_col}|task={data_config.task_type}\"` then md5[:8]. Every test fixture hashes features ONLY: tests/conftest.py:81 `md5(','.join(sorted(feature_cols)))`, tests/integration/conftest.py:74 hashes only NUMERIC features, tests/workflow/conftest.py:76 same, tests/workflow/test_state_invalidation.py:42 same. The code comment at pages/01_Upload_and_Audit.py:1044-1046 says the target-swap case was a real past bug ('A target swap with unchanged features previously kept the old target's models/metrics/SHAP aliv\u2026", "ev": "pages/01_Upload_and_Audit.py:1047-1051 vs tests/conftest.py:81-83, tests/integration/conftest.py:73-76, tests/workflow/conftest.py:76-78", "act": "Characterization test: assert md5 signature DIFFERS for (same features, different target) and (same features, different task_type), and is STABLE under feature reordering. Extract the signature into a pure `config_signature(feature_cols, ta\u2026", "status": "UNVERIFIED"}, {"id": "TEST-003", "area": "TEST", "sev": "critical", "item": "PlausibilityGate violates sklearn's clone() contract \u2014 cross-validation raises RuntimeError whenever plausibility bounds are configured", "detail": "LANDS: engine (fix before move). ml/preprocess_operators.py:49-55 does work in __init__: `self.lower_bounds = np.array([np.nan if v is None else float(v) for v in lower_bounds], dtype=float)`. sklearn.base.clone does `klass(**estimator.get_params())` then asserts `param_new is param_old` for every param; because __init__ rebuilds the array, the identity check fails and clone raises RuntimeError('Cannot clone object ... constructor either does not set or modifies parameter lower_bounds'). ml/pipeline.py:182-189 inserts PlausibilityGate whenever `plausibility_bounds and plausibility_mode != 'filter'`, and ml/eval.py:161 `cross_val_score(model, X, y, cv=cv, scoring=scoring, n_jobs=-1, groups=cv\u2026", "ev": "ml/preprocess_operators.py:49-55; ml/pipeline.py:185; ml/eval.py:161; contrast ml/preprocess_operators.py:13", "act": "Move the array conversion from __init__ into fit() (set self.lower_bounds_/upper_bounds_). Characterization test: `clone(PlausibilityGate([0,None],[10,None]))` must not raise, and `cross_val_score` over a pipeline built with plausibility_bo\u2026", "status": "UNVERIFIED"}, {"id": "TEST-004", "area": "TEST", "sev": "critical", "item": "build_preprocessing_pipeline embeds an unpicklable local closure \u2014 the Jobs layer cannot ship pipelines to workers", "detail": "LANDS: engine (fix before jobs exists). ml/pipeline.py:210-213 defines `def log_transform(X): return np.log1p(np.maximum(X, 0))` INSIDE build_preprocessing_pipeline and wraps it: `numeric_steps.append(('log', FunctionTransformer(log_transform)))`. Standard pickle raises AttributeError: Can't pickle local object 'build_preprocessing_pipeline.<locals>.log_transform'. This is triggered by `numeric_log_transform or numeric_power_transform == 'log1p'`. The identical bug was already found and fixed one file over: ml/eval.py:177 defines module-level `_to_dense` with the explicit comment 'Named (picklable, for n_jobs=-1) densifier for CV pipelines' and uses it at ml/eval.py:195. Today this is masked\u2026", "ev": "ml/pipeline.py:210-213; contrast ml/eval.py:177-180 ('Named (picklable, for n_jobs=-1)')", "act": "Hoist to a module-level `_log1p_nonneg(X)` before building Jobs. Characterization test: for every combination of numeric_log_transform/numeric_power_transform, assert `pickle.dumps(build_preprocessing_pipeline(...))` succeeds and the unpick\u2026", "status": "UNVERIFIED"}, {"id": "TEST-005", "area": "TEST", "sev": "critical", "item": "Cross-layer imports of PRIVATE symbols inside bare `except Exception: pass` \u2014 renames degrade manuscripts silently, with no error", "detail": "LANDS: record/engine boundary. ml/publication.py:1739-1744 does `try: from utils.insight_ledger import _clean_for_manuscript; cleaned = _clean_for_manuscript(cleaned); except Exception: pass`. The imported symbol is underscore-private \u2014 by convention it is the FIRST thing a refactor renames \u2014 and the failure mode is a silently un-cleaned manuscript string, not a traceback. Same pattern at ml/publication.py:154-162 where `from utils.insight_ledger import get_ledger` failing falls through to `st.session_state.get('methodology_log', [])`, so a broken ledger accessor silently produces an EMPTY methods section. Repo-wide there are 47 try/import/except-pass blocks that swallow a failed cross-modul\u2026", "ev": "ml/publication.py:1739-1744 and :154-162; AST scan: 47 swallow-blocks across 16 files", "act": "Before moving anything, convert every one of the 47 to log-and-reraise under an env flag (TURBOTAB_STRICT_IMPORTS=1) and run the suite with it on. Any cross-layer import that only works by accident surfaces immediately instead of after the \u2026", "status": "UNVERIFIED"}, {"id": "TEST-006", "area": "TEST", "sev": "critical", "item": "21 of 65 keys that reset_downstream_results actually clears are asserted by no test in any suite", "detail": "LANDS: project. Union of all three test key-lists (workflow 30, integration 32, RESULT_KEYS 22 at tests/test_review_fixes.py:141) leaves these production-cleared keys unasserted: dataset_profile, cv_strategy, cv_groups_train, cv_results, target_label_encoder, feature_names, explainability_robustness, shap_matplotlib_figs, custom_table1_tests, engineered_feature_names, engineering_log, feature_engineering_applied, pre_fe_feature_cols, flow_diagram, latex_report, tripod_tracker, report_best_model, report_model_selection, report_explain_selection, report_include_results, report_include_llm. Highest-consequence of these is `dataset_profile` \u2014 it is the input the coach/Router reads to decide what\u2026", "ev": "utils/session_state.py:301-415 (65 keys) vs tests/test_review_fixes.py:141-152, tests/workflow/test_state_invalidation.py:25-37, tests/integration/test_cascade_invalidation.py:134-147", "act": "Replace all three hand-maintained lists with ONE golden key-set fixture derived from the Project schema, and add a meta-test that fails when Project gains a result field absent from the golden set (so the list cannot silently drift again).", "status": "UNVERIFIED"}, {"id": "TEST-007", "area": "TEST", "sev": "critical", "item": "40 of 58 test modules break the moment Streamlit is removed; only 18 survive untouched", "detail": "LANDS: view (rewrite) / project (port). Measured by AST transitive-import analysis. DIRECT streamlit import (15): all 9 tests/integration/* AppTest files, plus tests/test_cohorts, test_cohort_runs, test_feature_drift, test_omics_nutrition_reality, test_publication, test_review_fixes. TRANSITIVE via project modules (25): tests/test_stress_regressions, test_unified_ledger_integration, test_workflow_provenance, test_narrative_engine, test_session_manager, test_provenance_integration, test_shap_and_sensitivity, test_manuscript_trust, test_manuscript_validator, test_insight_lifecycle, test_eda_ledger_bridge, test_coach_intelligence, test_comparative_context, test_complexity_coaching, test_model_s\u2026", "ev": "AST transitive dependency scan over 58 test modules rooted at tests/", "act": "Sequence the migration so the 18 clean modules become the engine regression suite on day 1. The 25 transitive ones mostly need only the `st.session_state`->dict swap and should be ported (not rewritten) by parameterizing on a state mapping.", "status": "UNVERIFIED"}, {"id": "TEST-008", "area": "TEST", "sev": "critical", "item": "No golden-number test exists for calculate_regression_metrics / calculate_classification_metrics \u2014 the numbers the whole manuscript rests on", "detail": "LANDS: engine. ml/eval.py:14 `calculate_regression_metrics(y_true, y_pred) -> Dict[str, float]` returns exactly {'MAE','RMSE','R2','MedianAE'} (:47-52) with a documented degenerate path: non-finite pairs are dropped (:29-37) and `if y_true.size < 2` returns all-NaN (:38-40). ml/eval.py:55 `calculate_classification_metrics(y_true, y_pred, y_proba=None)` returns {'Accuracy','F1'} always and conditionally adds 'ROC-AUC','LogLoss','PR-AUC' \u2014 but inside `except Exception: pass` at :90-92, so a metric failure silently yields a SHORTER dict rather than an error. Eleven test modules import ml.eval but all use it as a helper to build fixtures; none pin its output values. Key-name drift ('RMSE' vs 'rm\u2026", "ev": "ml/eval.py:14-52, :55-94 (silent metric drop at :90-92); consumers listed by grep for ml.eval across tests/", "act": "Write tests/characterization/test_metrics_golden.py FIRST. Fixture: y_true = np.arange(100, dtype=float), y_pred = y_true + np.sin(np.arange(100)). Assert the exact 4-key set and each value to 10 decimals as frozen literals. Separately asse\u2026", "status": "UNVERIFIED"}, {"id": "TEST-009", "area": "TEST", "sev": "critical", "item": "ml.triage.detect_task_type is the Router's foundation and has zero coverage", "detail": "LANDS: engine (consumed by router). ml/triage.py:10 `detect_task_type(df: pd.DataFrame, target: str) -> Dict` and :102 `detect_cohort_structure(df: pd.DataFrame, sample_size: int = 1000) -> Dict`. In the new architecture the Router promotes the coach to deciding 'which question is asked next', and task type is the first branch \u2014 regression vs classification changes every downstream question, metric, and plot. A threshold shift in this function silently reroutes the entire interview. It also feeds the invalidation signature (task_type is in the hash, finding #2), so a drift here silently invalidates or fails to invalidate everything.", "ev": "ml/triage.py:10, :102; consumed via pages/01 task_type_detection and DataConfig.task_type", "act": "Golden test over a fixture matrix, asserting the FULL returned dict (not just the verdict): binary int target; binary string target; 3-class; 10-class int (the classification/regression boundary); continuous float; continuous int with <20 u\u2026", "status": "UNVERIFIED"}, {"id": "TEST-010", "area": "TEST", "sev": "critical", "item": "reset_downstream_results mutates the ledger and provenance as a side effect \u2014 an ordering hazard when Record becomes append-mostly", "detail": "LANDS: project/record seam. utils/session_state.py:377-407: the reset reaches INTO the record layer, setting provenance sections to None (`setattr(prov, section, None)` for eda/split/preprocessing/training/explainability/coach) and calling `ledger.rollback_resolutions(_rollback_pages)` then `ledger.prune_auto_generated({'02_EDA','05_Preprocess','06_Train_and_Compare'})`. Both are guarded by hasattr, so if the method is renamed during the Record extraction the call is SILENTLY SKIPPED (no AttributeError) and the manuscript keeps asserting resolved actions that were invalidated \u2014 exactly the failure the code comment at :389-395 says it exists to prevent. This directly contradicts the stated 'R\u2026", "ev": "utils/session_state.py:377-407, esp. hasattr guards at :384, :398, :405", "act": "Replace hasattr guards with explicit protocol calls that raise. Characterization test: seed a ledger with resolutions earned on each rollback page plus auto-generated insights, call reset_downstream_results, assert the exact surviving insig\u2026", "status": "UNVERIFIED"}, {"id": "COACH-001", "area": "COACH", "sev": "landmine", "item": "ml/model_coach.py is streamlit-clean on import and streamlit-DIRTY on call.", "detail": "Reproduced in this environment: importing ml.model_coach succeeds with streamlit absent; calling generate_preprocessing_insights raises ModuleNotFoundError. Because BOTH call sites (pages/05_Preprocess.py:302-322 and pages/06_Train_and_Compare.py:1706-1734) wrap everything in `except Exception: pass`, the failure is completely invisible \u2014 no coaching, no diagnostics, no log line. And since run_post_training_diagnostics has no internal error isolation, _detect_overfit's import failure discards the findings of all six other detectors with it.", "ev": "ml/model_coach.py:634 (inside generate_preprocessing_insights) and ml/model_coach.py:1080 (inside _detect_overfit), both doing `from utils.insight_ledger import \u2026`; utils/insight_ledger.py:46 `import streamlit as st`.", "act": "Extract MODEL_TO_FAMILY / FAMILY_ALIASES / ISSUE_MODEL_RELEVANCE / FAMILY_DISPLAY_NAMES / MODEL_DISPLAY_NAMES into a leaf vocabulary module (e.g. ml/model_families.py) that both engine and record import; delete get_ledger's module-level str\u2026", "status": "UNVERIFIED"}, {"id": "COACH-002", "area": "COACH", "sev": "landmine", "item": "`_detect_low_overall_performance` is permanently dead on the classification path due to a metric-name mismatch, and the test that covers it uses a metric key production never emits.", "detail": "`.get('AUC', 0)` always returns 0 \u2192 `best_auc = 0` \u2192 the `if best_auc >= 0.60 or best_auc <= 0: return []` guard fires on every classification run. `train_low_performance` has never once been raised for a classifier in production. The test suite is green and gives false confidence.", "ev": "ml/model_coach.py:949-953 reads `r.get('metrics', {}).get('AUC', 0)`; ml/eval.py:80-82 writes `metrics['ROC-AUC']`. tests/test_complexity_coaching.py:111 and :121 feed the fictional key `{'AUC': 0.55}` / `{'AUC': 0.75}`.", "act": "Before the move, add an assertion that every metric key the coach reads is a key ml/eval.py actually produces \u2014 a small cross-module contract test over calculate_regression_metrics / calculate_classification_metrics output keys vs the liter\u2026", "status": "UNVERIFIED"}, {"id": "COACH-003", "area": "COACH", "sev": "landmine", "item": "`primary_model` is read but never written, so the heteroscedasticity finding names an arbitrary model.", "detail": "The fallback always fires. `train_heteroscedastic_residuals` reports Spearman rho for whichever model happens to be first in the dict \u2014 i.e. checkbox order \u2014 and its manuscript_text asserts 'residual variance was not constant across the predicted range' about a model that may not be the one reported.", "ev": "pages/06_Train_and_Compare.py:1714 passes `primary_model=st.session_state.get('primary_model', '')`; ml/model_coach.py:1263 `key = primary_model if primary_model in model_results else next(iter(model_results))`. Repo-wide grep finds only RE\u2026", "act": "Make the Project object own `primary_model` explicitly and make the Router pass it. In the meantime change the fallback from `next(iter(...))` to an explicit best-by-metric selection so the choice is at least defensible, and put the model n\u2026", "status": "UNVERIFIED"}, {"id": "COACH-004", "area": "COACH", "sev": "landmine", "item": "`_detect_ci_overlap` can essentially never fire, because bootstrap results do not exist yet when it is called.", "detail": "bootstrap_results is None on the first pass \u2192 `if not bootstrap_results \u2026 return []`. The one detector that could tell a researcher 'your top-two ranking is not established' is inert. There is no log line and no empty-state message.", "ev": "pages/06_Train_and_Compare.py:1713 reads `st.session_state.get('bootstrap_results')` from inside the post-training block; the key is only written at pages/06_Train_and_Compare.py:2072, in a later section that requires a separate user action\u2026", "act": "In TurboTab this is a Jobs dependency problem, not a page-order problem: make the diagnostics job depend on the bootstrap job's completion, or re-run the affected detector when bootstrap results land. Whatever the design, add a test that as\u2026", "status": "UNVERIFIED"}, {"id": "COACH-005", "area": "COACH", "sev": "landmine", "item": "Every coach call site swallows all exceptions, three of them at a log level nobody sees.", "detail": "A coach that produces nothing is visually identical to a coach that has nothing to say. In the viability case (line 864) the model cards simply render with no verdict \u2014 the absence reads as 'the coach has no opinion about this model'. Every migration bug in this domain will land in one of these six wells.", "ev": "pages/05_Preprocess.py:296-298 `except Exception as _coach_err: logging.getLogger(__name__).debug(f'Model coach error: {_coach_err}')`; pages/05_Preprocess.py:323 `except Exception: pass`; pages/06_Train_and_Compare.py:863-864 `except Excep\u2026", "act": "Before touching anything, downgrade the swallows to log.exception and add an explicit empty-state so 'no advice' and 'advice failed' are visually distinct. In the Router, a failed advisor must surface as a degraded-mode banner, never as sil\u2026", "status": "UNVERIFIED"}, {"id": "COACH-006", "area": "COACH", "sev": "landmine", "item": "The coach's model auto-selection writes Streamlit widget-bound keys that Streamlit garbage-collects on navigation.", "detail": "The coach's recommendation quietly evaporates between pages, and _get_selected_models falls back to `preprocess_built_model_keys`. Model-aware coaching then scopes to a different set of families than the coach advised \u2014 insights whose scope no longer matches are dropped outright by utils/coaching_ui.py:170-173. This is the mechanism by which the auto-select and the coaching display can disagree with no error.", "ev": "pages/05_Preprocess.py:290-294 sets `st.session_state[f'train_model_{pick.model_key}'] = True`; those same keys are bound to widgets at pages/06_Train_and_Compare.py:906-907 `st.checkbox('Select', value=is_selected, key=checkbox_key)`. util\u2026", "act": "The Project object must own the selected-model list as first-class state, separate from any widget binding, AND must record PROVENANCE for it \u2014 'coach-selected' vs 'user-selected'. Today narrative_engine.py:935 infers the user followed the \u2026", "status": "UNVERIFIED"}, {"id": "COACH-007", "area": "COACH", "sev": "landmine", "item": "Three of the coach's four preprocessing insights have no resolver anywhere, no manuscript_text, and therefore land in the manuscript's LIMITATIONS section.", "detail": "Reproduced directly: the limitations list comes back containing 'Your linear models (Ridge Regression, LASSO, etc.) are scale-sensitive; the app's default pipeline already standardizes features for them.' and '2 feature(s) have missing values.' \u2014 a reassurance and a neutral fact, both presented to a reviewer as study limitations, with '(s)' pluralization the rest of the codebase deliberately avoids. Gate #2 (pages/06:1698) makes it worse by marking them 'acknowledged', which discussion_points treats identically to unresolved.", "ev": "Produced at ml/model_coach.py:673-799 as preprocess_skewness_transform, preprocess_outlier_handling, preprocess_feature_scaling, preprocess_missing_tree_native. Only preprocess_outlier_handling is resolved (pages/05_Preprocess.py:1190-1203)\u2026", "act": "Give every coach-authored insight an explicit manuscript_text (or an explicit `narrative: false`), and make 'positive/neutral advice' a first-class disposition rather than something inferred from an id prefix. Add a test that asserts no lim\u2026", "status": "UNVERIFIED"}, {"id": "COACH-008", "area": "COACH", "sev": "landmine", "item": "Four post-training detectors pick a single model by dict iteration order, so their CONTENT is non-deterministic across runs that differ only in checkbox order.", "detail": "Two researchers with identical data and identical models get different named models in their findings \u2014 and therefore different manuscript_text \u2014 purely because they checked the boxes in a different order. Nothing flags the tie. The FINDINGS_LEDGER governing rule ('the app may be silent, it may refuse, but it must never assert something false') is violated the moment a tie is asserted as a ranking.", "ev": "ml/model_coach.py:847-850 (_detect_prefer_simpler min/max over dict, exact ties \u2192 first inserted), :1146-1151 (_detect_accuracy_vs_nir, strict `acc > best_acc` \u2192 first wins on ties), :1201-1202 (_detect_ci_overlap, stable sort \u2192 ties by ins\u2026", "act": "Sort by (score, model_key) everywhere a single winner is chosen, and make an exact or near-exact tie produce a 'ranking not established' finding rather than a winner. The machinery for that already exists in _detect_ci_overlap's message \u2014 r\u2026", "status": "UNVERIFIED"}, {"id": "COACH-009", "area": "COACH", "sev": "landmine", "item": "The probe's interpretation thresholds exist twice: as named constants in coach_probe and as bare numeric literals in model_coach.", "detail": "Tune NONLINEAR_GAIN in coach_probe and the probe's own summary() changes while the coach's pick rationale and viability clauses keep the old boundary. The user sees 'linear \u2248 trees in the probe' in one panel and '+0.04 for trees' reasoning in another, from the same numbers. No test compares them.", "ev": "ml/coach_probe.py:42-44 SIGNAL_MARGIN=0.05, NONLINEAR_GAIN=0.04, LEARNING_SLOPE=0.03; ml/model_coach.py:497 `_gain < 0.02`, :523 `_gain > 0.04`, :1361 `_gain > 0.04`, :1363 `_gain < 0.02`. model_coach never imports the constants.", "act": "Import the constants. Add a test asserting model_coach contains no bare float thresholds in the probe branches.", "status": "UNVERIFIED"}, {"id": "COACH-010", "area": "COACH", "sev": "landmine", "item": "An insight whose model_scope matches none of the currently selected model families is discarded with no trace.", "detail": "A warning that was correct when it was written vanishes the moment the user changes their model lineup \u2014 and is actively replaced by a green reassurance. ml/model_coach.py:1076-1079 carries an in-code note that this exact failure already happened once with the coach's display groups.", "ev": "utils/coaching_ui.py:162-173 \u2014 the `else: pass` branch with the comment 'This means it's irrelevant \u2014 skip it'. Reinforced by lines 193-202, which then print '\u2705 No family-specific issues for \u2026'.", "act": "In the interview, a scoped insight whose scope no longer applies should be shown as superseded, not deleted. Never print a reassurance derived from an empty filter result \u2014 distinguish 'checked and clean' from 'nothing matched the filter'.", "status": "UNVERIFIED"}, {"id": "COACH-011", "area": "COACH", "sev": "landmine", "item": "upsert replaces the finding text but preserves the resolution, so a resolution can end up describing an action taken against a different observation.", "detail": "Fixed-id producers re-run on every render (pages/02_EDA.py:542 calls _auto_generate_insights unconditionally; pages/05_Preprocess.py:315 re-upserts the coach insights every pass). If the underlying numbers change, the finding text updates while resolved_by keeps describing the earlier action. to_manuscript_narrative (utils/insight_ledger.py:1282-1284) then emits `{new finding}. {old resolution}.` as one sentence.", "ev": "utils/insight_ledger.py:684-705. On an id match with `existing.resolved and not insight.resolved`, it copies resolved/resolved_by/resolved_on_page/resolved_at/resolution_details onto the NEW object, then `self._insights[idx] = insight`.", "act": "Either invalidate the resolution when the finding's material content changes (hash the numeric metadata), or version the insight so the record keeps both. Silently pairing a new fact with an old action is exactly the assert-something-false \u2026", "status": "UNVERIFIED"}, {"id": "COACH-012", "area": "COACH", "sev": "landmine", "item": "prune_auto_generated deletes the methodology audit-trail bridge entries, not just the stale detections.", "detail": "A downstream reset removes the 'preprocessing pipelines were configured' and 'models were trained' entries from get_methodology_log() and from to_manuscript_narrative(). The methods section loses steps it previously described, with no indication that anything was dropped. The docstring's premise \u2014 'the producer page re-detects fresh insights on its next visit' \u2014 is false for these, because they are written on ACTION, not on visit.", "ev": "utils/session_state.py:407-411 passes {'02_EDA','05_Preprocess','06_Train_and_Compare'}; utils/workflow_provenance.py _log_to_ledger writes method_preprocessing / method_model_training with auto_generated=True and source_page in that set.", "act": "Split the record: detections (safe to prune) vs action log (append-only, never pruned, only marked superseded). The task brief already calls the Record 'append-mostly' \u2014 prune_auto_generated is the one place that is not, and it is deleting \u2026", "status": "UNVERIFIED"}, {"id": "COACH-013", "area": "COACH", "sev": "landmine", "item": "Confidence vocabularies disagree across the three detectors that have one: 'med' vs 'medium'.", "detail": "A Router that gates on `confidence == 'medium'` silently never fires for task-type or cohort detection; one that gates on `'med'` silently never fires for join keys or import findings. Both are string comparisons against untyped values with no enum and no validation.", "ev": "ml/triage.py uses 'low'|'med'|'high' (lines 43,51,59,67,71,78,82,86,92,230,237,244,260,266,272) and utils/session_state.py:17,34 types it as Literal['low','med','high']; ml/import_doctor.py:92 and ml/join_doctor.py:235-259 use 'high'|'mediu\u2026", "act": "One enum, one module, converted at the boundary \u2014 before any Router logic reads a tier. Include the tri-state None (triage returns confidence=None when the target column is missing, ml/triage.py:27).", "status": "UNVERIFIED"}, {"id": "COACH-014", "area": "COACH", "sev": "landmine", "item": "`ShapeFinding.auto_suggestable` \u2014 the property that encodes the app's governing rule \u2014 has zero callers.", "detail": "The rule reads as enforced (there is a property with a docstring quoting it) but is upheld only by the structural accident that utils/import_ui.py renders every fix as a button and auto-applies nothing. A Router that reaches for auto_suggestable inherits an untested, never-exercised code path \u2014 and worse, believes the rule is centrally enforced when it is not.", "ev": "ml/import_doctor.py:92-96. Repo-wide grep for `auto_suggestable` finds only its definition.", "act": "Either wire it into the renderer so it is exercised, or delete it and document the structural enforcement. Do not migrate it as-is: an unexercised safety property is a liability in a Router.", "status": "UNVERIFIED"}, {"id": "COACH-015", "area": "COACH", "sev": "landmine", "item": "utils/combine_ui.py pre-selects a 'medium'-confidence join key, contradicting the governing rule the codebase states twice.", "detail": "docs/FINDINGS_LEDGER.md:22 and ml/join_doctor.py:238-240 both state that 'high' is the only pre-selectable tier because high means the app is asserting. The selectbox pre-selects the top-ranked non-low candidate, which is routinely 'medium'. A user who accepts the default has accepted an assertion the app did not intend to make \u2014 and finding C1 in the ledger is exactly a wrong-join-key defect.", "ev": "utils/combine_ui.py:262-273 \u2014 `usable = [c for c in cands if c.confidence != 'low']` followed by `st.selectbox(..., options, key=f'combine_key_{other}')`, which defaults to index 0.", "act": "Report this as a live governing-rule violation independent of the rebuild. In TurboTab, make 'pre-selected' a property the Router computes from tier, so the rule is enforced in one place rather than re-derived per widget.", "status": "UNVERIFIED"}, {"id": "COACH-016", "area": "COACH", "sev": "landmine", "item": "Insight.category is unvalidated and one producer already writes a value outside the declared vocabulary; separately, DataWarning.category is a DIFFERENT vocabulary with overlapping-looking names.", "detail": "An off-vocabulary category means: __post_init__ auto-fills no tripod_keys (so the TRIPOD checklist under-reports unless the producer passes them by hand, which the upload site happens to do); _manuscript_topic_bucket falls through to 'Workflow Observations'; and get_by_category / get_unresolved(category=\u2026) silently return nothing. Nothing raises. The two-taxonomy problem will bite the moment the rebuild merges DataWarning into Record.", "ev": "utils/insight_ledger.py:465-476 declares CATEGORIES (10 values); pages/01_Upload_and_Audit.py:1091 writes `category='study_design'`, which is in neither CATEGORIES nor TRIPOD_CATEGORY_MAP. Meanwhile ml/dataset_profile.py:363-491 constructs \u2026", "act": "Make category an enum validated in __post_init__ before the merge, and decide explicitly whether DataWarning becomes an Insight or stays a separate engine output. Do not let two `category` fields with different vocabularies land in the same\u2026", "status": "UNVERIFIED"}, {"id": "COACH-017", "area": "COACH", "sev": "landmine", "item": "model_viability is task-blind \u2014 it returns verdicts for classification models on regression data and vice versa.", "detail": "Today the Train page hides the mismatch because it iterates the task-filtered registry (pages/06_Train_and_Compare.py:846-880). A Router that reads model_viability directly to order questions will propose logistic regression for a continuous outcome, with a confident 'good' verdict.", "ev": "ml/model_coach.py:1393-1400 unconditionally sets v['logreg'], v['lda'], v['gaussian_nb'] regardless of task_type; the `low_epv` flag that drives them is defined at line 1354 as classification-only, so on regression data it is always False a\u2026", "act": "Take task_type as an explicit parameter and return only keys valid for it. Extend tests/test_coach_intelligence.py::test_covers_full_registry to assert the complement: no classification key appears in a regression verdict map.", "status": "UNVERIFIED"}, {"id": "COACH-018", "area": "COACH", "sev": "landmine", "item": "infer_theory_anchor's third stage matches on FINDING PROSE, so rewording a coach message silently changes which theory the researcher is taught.", "detail": "No error, no missing-anchor warning \u2014 the card just renders without a theory link, or with the wrong one. The failure has already occurred once in this codebase.", "ev": "utils/theory_anchors.py:438-461 \u2014 a 19-entry keyword scan over insight.finding.lower(). ml/model_coach.py:765-767 carries an in-code note that the scaling demo 'previously rendered only via an accidental text match'.", "act": "Make theory_anchor mandatory on every producer and delete the prose-matching stage, or at minimum add a test that every insight id the codebase can produce resolves to an anchor via stage 1 or 2 only.", "status": "UNVERIFIED"}, {"id": "COACH-019", "area": "COACH", "sev": "landmine", "item": "get_ledger silently discards a ledger that failed to rehydrate.", "detail": "A whole analysis record \u2014 every finding, every resolution, every TRIPOD credit \u2014 is replaced by an empty ledger with no message. The user's next export is a manuscript with no methodology narrative and no explanation of why.", "ev": "utils/insight_ledger.py:1400-1404 \u2014 `if not isinstance(ledger, InsightLedger): st.session_state.insight_ledger = InsightLedger()`, commented 'Recover from serialization edge cases'.", "act": "During the Project/Record migration this is the moment a deserialization bug will be masked. Make rehydration failure loud: keep the raw payload, surface an error, and refuse to export rather than exporting an empty record.", "status": "UNVERIFIED"}, {"id": "COACH-020", "area": "COACH", "sev": "landmine", "item": "The post-training coaching display uses a hardcoded id allowlist, so three of seven detectors reach the ledger and the manuscript but never the screen.", "detail": "A researcher can find a heteroscedasticity limitation or a below-NIR warning in their exported manuscript that they were never shown in the app. New detectors added to run_post_training_diagnostics are invisible by default \u2014 the allowlist is opt-in and nothing reminds you to update it.", "ev": "pages/06_Train_and_Compare.py:2277-2280 \u2014 `_coaching_ids = ['train_prefer_simpler','train_low_performance','train_cv_variance']` plus `f'train_overfit_{k}'`. Not listed: train_accuracy_below_nir, train_ci_overlap_top_models, train_heterosce\u2026", "act": "Render by query (source_page + category), not by id list. If an allowlist must exist, derive it from the detector registry so adding a detector adds its display.", "status": "UNVERIFIED"}, {"id": "COACH-021", "area": "COACH", "sev": "landmine", "item": "Only one insight in the whole app self-heals; the rest go stale when their trigger condition disappears.", "detail": "A user who switches from Ridge to Random Forest keeps a coaching note (and, per landmine 7, a manuscript limitation) about scale sensitivity for models they are no longer training. The coach's own output contradicts the current model lineup with no error.", "ev": "pages/05_Preprocess.py:153-158 calls `_get_hc_ledger().remove('preprocess_high_cardinality')` in both else-branches \u2014 the only self-heal pattern in the codebase. By contrast, generate_preprocessing_insights simply stops emitting preprocess_\u2026", "act": "Make production idempotent in both directions: a producer must be able to declare 'this id should not exist right now'. This is a Record API gap, not a page bug \u2014 add a `sync(source, ids_that_should_exist)` primitive before migrating any pr\u2026", "status": "UNVERIFIED"}, {"id": "MODELS-001", "area": "MODELS", "sev": "landmine", "item": "SklearnCompatibleNNRegressor.fit() and SklearnCompatibleNNClassifier.fit() do not train. They set is_fitted_=True, record n_features_in_ (and classes_), and return self. Any sklearn utility that refits will 'succeed' using the already-trained network.", "detail": "Feed the shim to cross_val_score / GridSearchCV / RFECV / CalibratedClassifierCV / learning_curve and every fold returns predictions from a model fitted on the full dataset. No exception, no warning \u2014 just uniformly excellent CV scores that get written into the manuscript. sklearn's clone() makes it worse: clone reconstructs from get_params() (which reports only wrapper_instance), so is_fitted_ resets to False, fit() flips it back to True without training, and predictions still come from the original network.", "ev": "models/nn_whuber.py:115-128 (regressor) and 174-188 (classifier); reachable through NNWeightedHuberWrapper.get_model() (line 577-582) and get_sklearn_estimator() (561-575). Currently fenced off by two string comparisons: pages/06_Train_and_\u2026", "act": "Before moving anything: write a test that asserts SklearnCompatibleNNRegressor(wrapper).fit(X, y) does NOT change the network's parameters. Then rename the class PredictOnlyAdapter, make fit() raise NotImplementedError, and give it a marker\u2026", "status": "UNVERIFIED"}, {"id": "MODELS-002", "area": "MODELS", "sev": "landmine", "item": "Three disagreeing sources of truth for Random Forest and Neural Network defaults. RF: RFWrapper.__init__ says n_estimators=500/min_samples_leaf=10; ModelConfig says 500/10; ml/model_registry._create_rf says 100/1 and registry['rf'].default_params says 100/1. NN architecture: NNWeightedHuberWrapper.__init__ defaults to [128,128,128]; registry['nn'].default_params says num_layers=3, layer_width=128; but pages/06:1351-1352 and 1153-1154 fall back to `params.get('num_layers', 2)` and `params.get('layer_width', 32)` \u2014 a [32,32] network.", "detail": "Consolidating on 'the registry' \u2014 the obviously correct engine choice \u2014 changes RF from 500 trees / min_leaf 10 to 100 trees / min_leaf 1, and changes the untuned NN from [32,32] to [128,128,128]. Both still train, both still report metrics, and every previously published number shifts. Nobody will suspect the defaults; they will suspect the migration broke the data.", "ev": "models/rf.py:12-13; utils/session_state.py:96-98 (ModelConfig); ml/model_registry.py:142 (_create_rf) and 594 (default_params). models/nn_whuber.py:253; ml/model_registry.py:512-513; pages/06_Train_and_Compare.py:1351-1352, 1153-1154, 1410-\u2026", "act": "Before consolidating, run every registry model on a fixed dataset and snapshot the exact constructor kwargs and the resulting metrics to a golden file. Then pick ONE source, and diff against the golden file \u2014 accept the deltas explicitly, i\u2026", "status": "UNVERIFIED"}, {"id": "MODELS-003", "area": "MODELS", "sev": "landmine", "item": "Each wrapper returns a differently-shaped training result, and the regression-named keys hold classification values. NN: {'history': {'train_loss','val_loss','val_rmse','val_accuracy'}, 'best_val_rmse', 'best_val_accuracy'}. GLM/Huber/RF: {'history': {'val_rmse': [v]}, 'best_val_rmse': v} where for CLASSIFICATION v is accuracy stored under the name val_rmse. RegistryModelWrapper: {'history': {'val_metric': [v]}, 'best_val_metric': v} \u2014 a different key entirely.", "detail": "Two ways. (1) Any generic 'lower is better' ranking on best_val_rmse silently INVERTS for classification, picking the worst model. (2) A TurboTab feed that renders learning curves uniformly KeyErrors on all 21 non-NN models \u2014 currently masked only by the `if name == 'nn'` guard at pages/06:2586. The mislabeling is currently harmless only because best_val_* has ZERO consumers anywhere in the codebase \u2014 verified by grep; the moment the Router starts ranking on it, it breaks.", "ev": "models/nn_whuber.py:432-437 and 551-555; models/glm.py:44-47; models/huber_glm.py:39-42; models/rf.py:64-67; models/registry_wrappers.py:51-54. Consumed at pages/06:1572 (`results.get('history', {})`) and rendered by visualizations.py:12 pl\u2026", "act": "Define one TrainResult TypedDict now: {history: {metric_name: [values]}, best: {metric_name: value}, epochs_run: int, task_type: str} with metric names that state what they are. Write a parametrized test that constructs every wrapper for bo\u2026", "status": "UNVERIFIED"}, {"id": "MODELS-004", "area": "MODELS", "sev": "landmine", "item": "task_type_final and data_config.task_type can diverge (the user's task-type override), and the training loop mixes them. y_test_proba is assigned only in the classification branch but read unconditionally when data_config.task_type == 'classification'.", "detail": "Three distinct failures. (a) override=regression + config=classification: UnboundLocalError on y_test_proba at line 1575, swallowed by the broad `except Exception` at 1605 into a red 'Training failed' box that names a variable, not the cause. (b) override=classification + config=regression: probabilities ARE computed then discarded as None, so ROC/PR at line 2722-2730 does `y_proba[:, 1]` on None. (c) CV silently picks the wrong scorer and fold strategy (StratifiedKFold on a continuous target), fails, and is swallowed at 1562 into 'Cross-validation failed... Skipping CV' \u2014 the user just loses CV and never learns why. Additionally, because y_test_proba is a function-scope local reused across \u2026", "ev": "pages/06_Train_and_Compare.py:161 (`task_type_final = task_type_detection.final if task_type_detection.final else data_config.task_type`; TaskTypeDetection.final returns override_value when override_enabled, utils/session_state.py:22-27). A\u2026", "act": "Resolve task_type ONCE at the top of the engine's train function, pass it as a parameter, and never read data_config.task_type below that point. Initialize y_test_proba = None before the branch. Add a test that sets override_enabled=True wi\u2026", "status": "UNVERIFIED"}, {"id": "MODELS-005", "area": "MODELS", "sev": "landmine", "item": "The 'Cancel Training' button is decorative \u2014 st.session_state.cancel_training is written and NEVER read anywhere in the codebase.", "detail": "Users believe long runs are cancellable. They are not \u2014 the flag is set and the loop never consults it. Migrating the training loop verbatim into a job worker carries the illusion forward: the job API will expose a cancel() that appears wired (the flag exists!) but cannot interrupt anything.", "ev": "pages/06_Train_and_Compare.py:1252-1253 (init), 1263-1264 (set to True). grep for 'cancel_training' across the whole repo returns only these lines plus the button key. No file in models/ or ml/ contains any cancel, abort, stop, or interrupt\u2026", "act": "Treat cancellation as NEW code, not a port. Thread `should_cancel: Callable[[], bool]` into (1) NNWeightedHuberWrapper.fit \u2014 check per epoch AND per batch; (2) the bootstrap_metric resample and jackknife loops \u2014 check every ~50 iterations; \u2026", "status": "UNVERIFIED"}, {"id": "MODELS-006", "area": "MODELS", "sev": "landmine", "item": "NNWeightedHuberWrapper.fit mutates PROCESS-GLOBAL RNG state, and RandomForest / cross_val_score hardcode n_jobs=-1.", "detail": "Under a job queue running two trainings concurrently, np.random.seed from job B silently reseeds the global numpy RNG that job A's sklearn estimators (any without an explicit random_state) are drawing from \u2014 results become non-reproducible in a way that only shows up as small metric drift between identical runs. Separately, n_jobs=-1 inside N concurrent workers spawns N x cores threads; the symptom is a machine that thrashes and a job that 'hangs', not an error. torch.cuda.manual_seed (not manual_seed_all) leaves multi-GPU nondeterministic.", "ev": "models/nn_whuber.py:313-317 \u2014 `torch.manual_seed(random_seed)`, `np.random.seed(random_seed)`, `torch.cuda.manual_seed(random_seed)`. models/rf.py:34 and 42 (`n_jobs=-1`); ml/eval.py:161 (`cross_val_score(..., n_jobs=-1)`). Also models/nn_w\u2026", "act": "Replace the global seeding with a local torch.Generator passed to the DataLoader plus np.random.default_rng(seed) for any numpy draws, before any concurrency exists. Make n_jobs a parameter defaulting to 1 inside workers. Pin the current be\u2026", "status": "UNVERIFIED"}, {"id": "MODELS-007", "area": "MODELS", "sev": "landmine", "item": "Bootstrap CIs and baselines ignore the project seed and disagree on resample count. Every call site takes bootstrap's random_state=42 default while models are trained with st.session_state['random_seed'], and page 06 uses n_resamples=1000 while page 07 uses 500 for the same conceptual quantity.", "detail": "A user changes the project seed to 7, reruns everything, and the CIs are byte-identical \u2014 they will reasonably conclude the CIs are stable when in fact they were never reseeded. And the external-validation CIs in the manuscript are computed from half as many resamples as the internal ones, with no annotation anywhere.", "ev": "pages/06:2046 and 2067 call bootstrap_all_*_metrics with no random_state; pages/06:2135-2139 calls train_baseline_models with no random_state (so its LogisticRegression is seeded 42 regardless of the project); ml/baseline_models.py:56 and 1\u2026", "act": "Thread Project.seed and a single Project.n_bootstrap through every call site, and record both in the Record next to each interval. If you change the effective seed, expect every published interval to shift \u2014 snapshot the current intervals t\u2026", "status": "UNVERIFIED"}, {"id": "MODELS-008", "area": "MODELS", "sev": "landmine", "item": "ModelCapabilities.supports_predict_proba is never read by any code, and its declared values are wrong for the four task-dual models.", "detail": "The Router is exactly the component that would naturally read declarative capability metadata to decide which question to ask next. If it consults supports_predict_proba it will offer calibration, ROC curves, and decision-curve analysis for regression models \u2014 the branches then call predict_proba, get None back (base default, no exception), and render an empty or crashed panel. The metadata LOOKS authoritative because 8 sibling fields on the same dataclass are load-bearing.", "ev": "ml/model_registry.py \u2014 registry['nn'] (line 536), registry['glm'] (557) and registry['rf'] (604) all declare supports_predict_proba=True, but for a REGRESSION task the corresponding wrapper's supports_proba() returns False (models/nn_whuber\u2026", "act": "Either delete supports_predict_proba, recommended_for_high_dim and interpretability_tier, or make them task-parameterized (supports_predict_proba(task_type) -> bool) and add a test that asserts, for every registry key and both task types, t\u2026", "status": "UNVERIFIED"}, {"id": "MODELS-009", "area": "MODELS", "sev": "landmine", "item": "Baselines are transformed through whichever model's preprocessing pipeline happens to have been trained FIRST.", "detail": "Train {ridge, rf} and the baselines run on a scaled/imputed matrix; train {rf, ridge} and they run on an unscaled one. The LinearRegression baseline's R2 changes, so the headline claim 'our model beats the linear baseline by X' changes \u2014 with no indication that anything about the baseline moved. If the transform raises (a pipeline expecting engineered columns), the except swallows it and the baseline silently trains on raw data that may contain NaNs or strings.", "ev": "pages/06_Train_and_Compare.py:2122-2124 \u2014 `first_model = list(st.session_state.get('fitted_preprocessing_pipelines', {}).keys())` then `pipe = st.session_state['fitted_preprocessing_pipelines'][first_model[0]]`. Because pipelines are per-mo\u2026", "act": "Give baselines their OWN explicit pipeline (scaled numeric + simple imputation) recorded in the Record, independent of any user model. Remove the bare except or make it record a visible failure. Pin current baseline metrics before changing \u2026", "status": "UNVERIFIED"}, {"id": "MODELS-010", "area": "MODELS", "sev": "landmine", "item": "The 'slow model' cost model keys off strings that are not registry keys, so the warning is wrong for the models that need it.", "detail": "ExtraTrees never triggers the slow-model warning or the cancel affordance, so a multi-minute run presents with no warning at all. Ported into a jobs layer as a duration estimator, this becomes wrong ETAs and wrong queue priorities \u2014 plausible-looking numbers that nobody validates.", "ev": "pages/06_Train_and_Compare.py:1248 \u2014 `slow_models = {'nn', 'extratrees', 'svc', 'svr'}`. The registry has NO key 'extratrees'; it has 'extratrees_reg' and 'extratrees_clf' (ml/model_registry.py:322, 345). It also omits 'svc'/'svr' siblings \u2026", "act": "Move cost class onto ModelSpec as an explicit field (cheap/moderate/expensive) so it is impossible to key off a string that does not exist, and assert in a test that every cost-class reference resolves to a real registry key.", "status": "UNVERIFIED"}, {"id": "MODELS-011", "area": "MODELS", "sev": "landmine", "item": "RegistryModelWrapper re-derives the task type from the data with a fragile heuristic instead of being told, and it is the only wrapper honoring sample_weight.", "detail": "A regression target that is integer-valued with under 20 distinct levels (ordinal scores, counts, Likert sums \u2014 common in the clinical datasets this app targets) is scored as ACCURACY and reported under 'val_metric'. Conversely a float-encoded class label is scored as RMSE. Nothing errors. The value lands in a history key nobody currently reads, which is exactly why it has survived \u2014 it becomes live the moment the Router or the feed starts reading history generically. Separately, routing rf/glm/huber through RegistryModelWrapper during consolidation would silently DROP their sample_weight support, since RFWrapper/GLMWrapper/HuberGLMWrapper ignore **kwargs entirely.", "ev": "models/registry_wrappers.py:44 \u2014 `if len(np.unique(y_train)) < 20 and y_train.dtype in [np.int64, np.int32, 'int64', 'int32', 'int']`. Its fit is also the only one reading kwargs['sample_weight'] (line 32-36); pages/06:1477-1480 relies on t\u2026", "act": "Pass task_type into RegistryModelWrapper.__init__ from the registry key and delete the heuristic. Before consolidating wrappers, add a test asserting class-weight balancing actually reaches the estimator for every key where capabilities.sup\u2026", "status": "UNVERIFIED"}, {"id": "MODELS-012", "area": "MODELS", "sev": "landmine", "item": "BCa acceleration is computed from a jackknife that is capped at 200 leave-one-out replicates and then PADDED to length n with the mean of those 200.", "detail": "For n > 200 the acceleration term a is computed over an array whose tail is constant, shrinking both the third and second moments in a way that depends on n. The intervals are still produced, still look reasonable, and are still labeled 'BCa' \u2014 but the acceleration correction is progressively damped as the test set grows. Any refactor that 'fixes' this (uncapping the jackknife, or passing only the 200 computed values) CHANGES EVERY PUBLISHED INTERVAL WIDTH, and the change will be attributed to the migration rather than to a deliberate statistical correction.", "ev": "ml/bootstrap.py:152-166 \u2014 `n_jack = min(n, 200)`, `jack_stats = np.full(n, np.nan)`, the loop runs only `for i in range(n_jack)`, then `jack_stats[~np.isfinite(jack_stats)] = jack_fill` fills all n - 200 remaining slots with nanmean of the \u2026", "act": "Do not touch this during the move. Snapshot BootstrapResult values for a fixed (y_true, y_pred, seed) with n well above 200 into a golden file, port byte-identically, verify the golden file passes, and only then open a separate, explicitly-\u2026", "status": "UNVERIFIED"}, {"id": "MODELS-013", "area": "MODELS", "sev": "landmine", "item": "The weighted_huber emphasis window is derived during fit and stored only on the instance as _whuber_t0 / _whuber_s, with legacy glucose constants (t0=180.0, s=20.0) as the getattr fallback.", "detail": "The fallbacks are silent by construction \u2014 getattr with a default never raises. Any path that reconstructs the wrapper from recorded config and refits (a Job retry, a sensitivity re-run, a reproducibility check) that fails to re-enter the weighted_huber branch trains against a Gaussian window centered at 180 with width 20 on a target that may live in [0,1]. The weight collapses to a near-constant 1.0, so it degrades to plain Huber and simply performs slightly worse \u2014 no error, no warning, and the manuscript still claims a weighted loss was used.", "ev": "models/nn_whuber.py:376-378 sets them only in the `elif self.loss_function == 'weighted_huber'` branch; lines 459-461 and 494-496 read them via `getattr(self, '_whuber_t0', 180.0)` / `getattr(self, '_whuber_s', 20.0)`. They appear in NEITHE\u2026", "act": "Compute t0/s in __init__ or make them explicit constructor parameters, include them in get_architecture_summary(), and record them in the Record. Delete the 180.0/20.0 getattr defaults so the failure becomes an AttributeError instead of a s\u2026", "status": "UNVERIFIED"}, {"id": "MODELS-014", "area": "MODELS", "sev": "landmine", "item": "NN classification silently remaps unseen validation classes to index 0, and models/base, glm, huber_glm, rf and registry_wrappers have literally zero test coverage.", "detail": "A stratified split that leaves a rare class out of training maps every such validation row to the FIRST class. Validation accuracy is computed against those fabricated labels, early stopping optimizes against them, and the selected 'best' epoch is chosen on corrupted signal. Meanwhile, with no tests on the other five wrapper files, ANY behavioral change made during the move \u2014 the RF duplicate-method deletion, a shared base-class refactor, normalizing the fit() return shape \u2014 lands with nothing to catch it.", "ev": "models/nn_whuber.py:337 \u2014 `y_val_mapped = np.array([class_to_idx.get(cls, 0) for cls in y_val])` (note: the TRAIN mapping at line 327 uses class_to_idx[cls] and would KeyError, but validation uses .get with a 0 default). Coverage: grep acro\u2026", "act": "WHAT A MIGRATION SUITE MUST PIN FIRST, in order: (1) for every one of the 22 registry keys and both task types, construct via spec.factory and assert the exact constructor kwargs \u2014 this is the defense against landmine 2; (2) fit each wrappe\u2026", "status": "UNVERIFIED"}, {"id": "RECORD-001", "area": "RECORD", "sev": "landmine", "item": "Insight.to_dict() does not serialize manuscript_text. The hand-written field list at utils/insight_ledger.py:617-644 omits it even though it is a declared dataclass field (line 571) and from_dict would happily restore it.", "detail": "After any session save/restore \u2014 and after any TurboTab job round-trip that serializes the Record \u2014 every insight loses its manuscript-register phrasing and silently falls back to the coaching-register finding. The Discussion's Limitations then read in coach voice ('a reviewer would question\u2026', 'consider\u2026') in a submitted manuscript. tests/test_manuscript_trust.py::test_manuscript_text_preferred_over_finding passes because it never serializes.", "ev": "utils/insight_ledger.py:617 (to_dict) vs :571 (field) vs :1188 (discussion_points_for_manuscript's `text = (i.manuscript_text or \"\").strip() or _clean_for_manuscript(i.finding)`); serialization path utils/session_manager.py:257-265 and :536\u2026", "act": "Replace the hand-written to_dict with dataclasses.asdict (or add the field), and add a round-trip test that asserts manuscript_text survives to_dict->from_dict. Do this BEFORE the Record is moved behind a job queue, because jobs make serial\u2026", "status": "UNVERIFIED"}, {"id": "RECORD-002", "area": "RECORD", "sev": "landmine", "item": "Numeric metric values are stored in the Record as whatever object the trainer produced (numpy scalars), and the save path uses json.dumps(default=str).", "detail": "On save, numpy.float64 is stringified to '0.8231'. On restore, TrainingProvenance.metrics_by_model holds STRINGS. NarrativeEngine._normalize_metrics only fixes keys, not values. _gen_results then evaluates `rmse < best_rmse` (str vs float) -> TypeError, which is caught by the blanket `except Exception` in pages/10_Report_Export.py:619, which logs a warning and silently downgrades to the legacy publication.py path \u2014 whose headings the validator cannot parse \u2014 which blocks all exports with two unrelated-looking FAIL rows.", "ev": "pages/06_Train_and_Compare.py:1660 passes res['metrics'] straight through; utils/workflow_provenance.py record_training does only dict(metrics_by_model) (shallow); utils/session_manager.py:271 json.dumps(provenance.to_dict(), default=str)", "act": "Coerce to plain float/int at the record boundary in record_training (and in from_dict as a repair pass). Add a test that saves a provenance containing numpy scalars, restores it, and asserts NarrativeEngine.generate() succeeds and metrics a\u2026", "status": "UNVERIFIED"}, {"id": "RECORD-003", "area": "RECORD", "sev": "landmine", "item": "record_training stores SHALLOW copies: dict(hyperparameters) and dict(metrics_by_model) share their inner per-model dicts with st.session_state['model_results'][name]['metrics'] and ['selected_model_params'].", "detail": "Any later mutation of a model's metrics dict (retraining in place, adding bootstrap CI keys, normalizing casing) retroactively rewrites the 'record of what happened'. The Record stops being append-mostly and starts being a live view. Nothing detects it; the manuscript simply reports numbers that were never the ones the user saw.", "ev": "utils/workflow_provenance.py:386-400", "act": "copy.deepcopy at the record boundary, or freeze to an immutable mapping. Add a test that mutates the source metrics dict after record_training and asserts the provenance is unchanged.", "status": "UNVERIFIED"}, {"id": "RECORD-004", "area": "RECORD", "sev": "landmine", "item": "ml/manuscript_validator.py is written against ml/narrative_engine.py's heading vocabulary ONLY. It extracts level-3 sections named exactly 'Study Design', 'Predictor Variables', 'Model Development', 'Model Evaluation'. The legacy fallback ml/publication.py emits '### Study Design and Participants' and '### Performance Evaluation'.", "detail": "Whenever the NarrativeEngine path throws for ANY reason, the fallback produces a perfectly reasonable manuscript whose Study Design and Model Evaluation sections extract as empty strings. Checks 1 ('analysis population is consistent') and 6 ('model names match between development and evaluation') then FAIL for every model, exports_blocked goes True, and the UI tells the user their counts disagree. The actual cause \u2014 a swallowed exception \u2014 appears only in a logger.warning.", "ev": "ml/manuscript_validator.py:159-162 vs ml/publication.py:413 and :1156; path switch at pages/10_Report_Export.py:608-626", "act": "Delete generate_methods_section (all its facts are in the Record). Until then, make the fallback emit the same headings, surface the swallowed exception in the UI, and add a test that validates a bundle built from the fallback path.", "status": "UNVERIFIED"}, {"id": "RECORD-005", "area": "RECORD", "sev": "landmine", "item": "_contains_any_variant uses substring `in`, not word boundaries, so the two-letter model key 'rf' matches inside the word 'performance'.", "detail": "Check 6 ('Model names match between development and evaluation sections') silently PASSES for Random Forest in any manuscript containing the word 'performance' \u2014 which every Model Evaluation section contains by construction. The check has never actually validated 'rf', and the same class of false-pass applies to any short key. A model genuinely missing from one section is reported as consistent.", "ev": "ml/manuscript_validator.py:111-119 (_model_variants returns [display, key.upper(), key.title()]; _contains_any_variant lowercases and substring-matches)", "act": "Match with re.search(rf'\\b{re.escape(variant)}\\b', ...) as check 11 already does. Add a regression test: a manuscript whose Model Evaluation mentions 'performance' but not Random Forest must FAIL.", "status": "UNVERIFIED"}, {"id": "RECORD-006", "area": "RECORD", "sev": "landmine", "item": "pages/10_Report_Export.py reads st.session_state['sensitivity_feature_dropout'] \u2014 a key that is written nowhere in the repository. Everything else uses 'sensitivity_dropout_results'.", "detail": "sensitivity_summary never contains feature_dropout_conducted, so ml/latex_report.py:853 never emits 'Feature dropout sensitivity analysis was conducted\u2026'. A user who ran feature dropout gets a LaTeX manuscript that does not mention it. No warning, no FAIL \u2014 the section just quietly omits real work.", "ev": "pages/10_Report_Export.py:877 vs pages/08_Sensitivity_Analysis.py:499 and ml/publication.py:1222,:1382", "act": "Fix the key, and then delete the whole function: SensitivityProvenance.feature_dropout already carries this fact. Add a test asserting that recorded sensitivity work appears in the LaTeX.", "status": "UNVERIFIED"}, {"id": "RECORD-007", "area": "RECORD", "sev": "landmine", "item": "generate_latex_report DISCARDS the compiled Results and Discussion whenever model_results is truthy. _convert_markdown_to_latex splits the draft into (methods, results) and everything after '## Results' \u2014 including the entire Discussion with all [AUTHOR REQUIRED] scaffolds \u2014 lands in results_latex, which is only appended in the `elif draft_results:` branch.", "detail": "In the normal case (results exist) the .tex Discussion is rebuilt from scratch out of generic [PLACEHOLDER: \u2026] strings, while the .md Discussion contains the compiled, evidence-citing [AUTHOR REQUIRED] scaffolds. The two exports in the same ZIP make different claims and use different author-marker conventions, and the honesty guarantees pinned by tests/test_manuscript_trust.py::TestDiscussionHonesty apply only to the markdown.", "ev": "ml/latex_report.py:692-700 (draft_results assignment) and :754-785 (`if model_results: \u2026 elif draft_results: \u2026`)", "act": "Render LaTeX from ManuscriptDraft's typed fields (Methods/Results/Discussion) instead of re-parsing markdown; delete _convert_markdown_to_latex. Add a test asserting '[AUTHOR REQUIRED' appears in the generated .tex when model_results is pre\u2026", "status": "UNVERIFIED"}, {"id": "RECORD-008", "area": "RECORD", "sev": "landmine", "item": "Provenance invalidation is incomplete in BOTH directions. record_upload nulls only feature_engineering, feature_selection, preprocessing and training. reset_downstream_results nulls only eda, split, preprocessing, training, explainability and coach. Neither clears statistical_validation; sensitivity is cleared by neither in the upload path.", "detail": "Change the target column or replace the data and StatisticalValidationProvenance.tests_run (and, via record_upload, SplitProvenance and SensitivityProvenance) survive. The Methods draft then states 'Statistical validation was performed using: Shapiro-Wilk, Mann-Whitney\u2026' and reports significance counts for tests run against a variable that is no longer in the analysis. The evidence map reports them as RECORDED. Nothing fails.", "ev": "utils/workflow_provenance.py:233-237 vs utils/session_state.py:376-388", "act": "Move invalidation entirely into the Project's DAG \u2014 the Record must have no invalidation logic of its own. Enumerate the full downstream set per stage and test each edge. tests/test_workflow_provenance.py::test_record_upload_resets_downstre\u2026", "status": "UNVERIFIED"}, {"id": "RECORD-009", "area": "RECORD", "sev": "landmine", "item": "ml/narrative_engine.py, ml/manuscript_validator.py and ml/latex_report.py all require streamlit AT IMPORT TIME, transitively, because utils/insight_ledger.py has `import streamlit as st` at module top level (line 46).", "detail": "The measured fact 'only 3 files in ml/ touch streamlit' is about DIRECT references. In a streamlit-free TurboTab worker, importing the manuscript compiler raises ImportError at module load \u2014 not at a call site \u2014 so a Job that compiles a manuscript fails before it starts, and the failure looks like a packaging problem rather than a coupling problem.", "ev": "ml/narrative_engine.py:25 `from utils.insight_ledger import InsightLedger, MODEL_DISPLAY_NAMES`; ml/manuscript_validator.py:9 `from ml.narrative_engine import _MODEL_NAMES`; ml/latex_report.py:1004 `from ml.publication import generate_decis\u2026", "act": "Move `import streamlit` inside get_ledger() (the only function that needs it, mirroring what workflow_provenance already does correctly), or extract the pure parts of insight_ledger (Insight, InsightLedger, MODEL_DISPLAY_NAMES, _clean_for_m\u2026", "status": "UNVERIFIED"}, {"id": "RECORD-010", "area": "RECORD", "sev": "landmine", "item": "All 32 streamlit references in ml/publication.py are OPTIONAL session-state reads guarded by `try: import streamlit as st \u2026 except ImportError: pass`. They are hidden inputs, not delivery.", "detail": "Run generate_methods_section headlessly and it returns a SHORTER manuscript with no exception: the '### Feature Engineering' section vanishes, per-model preprocessing collapses to the single-config branch, SHAP and Bland-Altman disappear from Model Interpretability, the Sensitivity Analysis section and its seed-CV numbers vanish, and three of the four Methodological Considerations paragraphs vanish. generate_decision_audit_trail() returns '' so the LaTeX Supplementary loses the audit trail \u2014 while the Reproducibility paragraph directly above still tells reviewers it is there.", "ev": "ml/publication.py lines 160,164,212,597,731,1171,1220,1349,1381,1401,1826 (the 11 try-blocks) covering 12 session keys: methodology_log, pre_fe_feature_cols, engineered_feature_names, selected_features, feature_engineering_applied, engineer\u2026", "act": "The logic/delivery split for this file is: there IS no delivery code \u2014 hoist all 12 keys to explicit keyword parameters, then the module is pure and the caller (Project) supplies them. Better: delete the module's manuscript half entirely, s\u2026", "status": "UNVERIFIED"}, {"id": "RECORD-011", "area": "RECORD", "sev": "landmine", "item": "ml/publication.py:1270 references `st` from an outer try-block's import at line 1220; if that import failed, `st` is unbound and the access raises NameError \u2014 caught by the bare `except Exception: pass` at :1284.", "detail": "The seed-stability coefficient-of-variation sentence \u2014 the only quantitative sensitivity result in the whole Methods section \u2014 silently disappears with no log line at all.", "ev": "ml/publication.py:1219-1224 (import) and :1269-1285 (use, inside its own bare except)", "act": "Never reference an outer-scope conditional import. Pass seed_df in as a parameter. At minimum, log the exception.", "status": "UNVERIFIED"}, {"id": "RECORD-012", "area": "RECORD", "sev": "landmine", "item": "The Evidence Map is built from a DIFFERENT context than the draft it is supposed to certify.", "detail": "The evidence map reports raw-provenance values for n_total, models_trained, primary_model, feature counts and metrics, while the draft reports the frozen export-scoped values (which can exclude models via included_models, override the primary model, and substitute population/feature counts). The artifact whose entire purpose is to make 'compiled from the audit trail' checkable can quietly contradict the draft. There is no check that compares them.", "ev": "pages/10_Report_Export.py:2094 `_em_engine = _NE_em(_get_prov_em(), _report_ledger)` \u2014 no manuscript_context \u2014 versus :616 `NarrativeEngine(prov, _report_ledger, manuscript_context=manuscript_context)`", "act": "Build the evidence map from the same _cached_bundle. Add a test that a draft and its evidence map, built from one context, agree on n_total, model list and primary model.", "status": "UNVERIFIED"}, {"id": "RECORD-013", "area": "RECORD", "sev": "landmine", "item": "Four independent model-key -> display-name mappings must stay in agreement or validator check 11 ('No internal model keys leak into export text') fires.", "detail": "Add a model to the registry and update only MODEL_DISPLAY_NAMES: latex_report's fallback dict silently renders 'CATBOOST_REG' where the markdown renders 'CatBoost (Regressor)'. Worse, the leak check derives its vocabulary from the PRIVATE _MODEL_NAMES, so a key present in MODEL_DISPLAY_NAMES but absent from _MODEL_NAMES is never checked for leakage at all.", "ev": "utils/insight_ledger.py:101 MODEL_DISPLAY_NAMES; ml/narrative_engine.py:232 _MODEL_NAMES (= the former plus lasso/xgb/lgbm/svm/knn/logistic/dt); ml/latex_report.py:192-214 a 21-entry HARDCODED fallback dict inside an `except Exception:`; ml\u2026", "act": "Single source of truth in a streamlit-free registry module; make _MODEL_NAMES public; delete latex_report's fallback dict (its `except Exception` was only there to avoid the streamlit import, which the extraction fixes anyway).", "status": "UNVERIFIED"}, {"id": "RECORD-014", "area": "RECORD", "sev": "landmine", "item": "Section timestamps are WRITE time, not stage time, and the dataclass field order does not match the interview order.", "detail": "Any feed that orders cards by timestamp reorders itself when the user re-runs an earlier stage (re-running EDA after training places EDA below training). Any feed that orders by declaration puts the coach's shortlist rationale AFTER the training it informed. Neither ordering is wrong enough to look like a bug \u2014 it just makes the transcript incoherent.", "ev": "utils/workflow_provenance.py:198-208 (declaration order upload, eda, feature_engineering, feature_selection, split, preprocessing, training, \u2026, coach) versus the actual writers: coach is written on page 05 but declared 11th; split is writte\u2026", "act": "Add an explicit monotonically increasing sequence number and a stage_order constant to the Record, and record created_at separately from updated_at per section. Do this before any feed rendering is written.", "status": "UNVERIFIED"}, {"id": "RECORD-015", "area": "RECORD", "sev": "landmine", "item": "WorkflowProvenance.from_dict silently drops any key not in the target dataclass's __dataclass_fields__, and schema_version is read but never branched on.", "detail": "Rename or restructure any field during the rebuild and old saved sessions restore with that fact missing and schema_version still reporting 1. The manuscript compiler then treats the fact as 'NOT RECORDED' and honestly omits the section \u2014 the user sees a shorter, correct-looking manuscript with no indication that data was dropped.", "ev": "utils/workflow_provenance.py:584-655 (11 identical `{k: v for k, v in data[...].items() if k in X.__dataclass_fields__}` filters) and :588 (`prov.schema_version = data.get('schema_version', 1)`)", "act": "Branch on schema_version with explicit migrations, and log (or collect into the returned object) every dropped key. Test restoring a v1 payload into a v2 schema.", "status": "UNVERIFIED"}, {"id": "RECORD-016", "area": "RECORD", "sev": "landmine", "item": "pages/10_Report_Export.py counts author inputs with a raw string count that includes the ownership preamble, while ManuscriptDraft.count_author_inputs() (which excludes it, and is pinned by a test) is never called by the app.", "detail": "The UI always reports one more outstanding author input than exist. The test that would catch it passes, because it tests the unused method.", "ev": "pages/10_Report_Export.py:2068 `st.session_state['methods_section'].count('[AUTHOR REQUIRED')` vs ml/narrative_engine.py:112-118 and tests/test_manuscript_trust.py::test_preamble_not_counted_as_author_input", "act": "Call count_author_inputs(). More generally: audit for other engine methods the app has re-implemented inline \u2014 ManuscriptDraft.to_latex() is dead in the app for the same reason.", "status": "UNVERIFIED"}, {"id": "RECORD-017", "area": "RECORD", "sev": "landmine", "item": "ml/publication.py _format_audit_timestamp assumes tz-naive timestamps are UTC, but every timestamp in the Record and Ledger is datetime.now().isoformat() \u2014 local time.", "detail": "Every line of the Decision Audit Trail in the LaTeX Supplementary is labelled 'YYYY-MM-DD HH:MM UTC' but is actually local wall-clock time, off by the machine's offset. A reviewer reconciling the audit trail against anything else finds a systematic skew.", "ev": "ml/publication.py:1747-1759 (`dt.replace(tzinfo=timezone.utc)`) versus utils/workflow_provenance.py (every record_* uses datetime.now()) and utils/insight_ledger.py:597 (created_at default)", "act": "Record timezone-aware UTC timestamps at the source (datetime.now(timezone.utc)) and render with an explicit conversion. Add a test asserting recorded timestamps carry tzinfo.", "status": "UNVERIFIED"}, {"id": "RECORD-018", "area": "RECORD", "sev": "landmine", "item": "The ZIP omits the Record itself, and ships a competing copy of the same facts. provenance.json and coaching.json are written only by utils/session_manager's separate session-save; the export ZIP instead contains metadata.json, built independently from session_state.", "detail": "metadata.json's dataset.n_rows is len(df) while the Record's n_total may be the analysis total; its splits come from split_config while SplitProvenance may hold different numbers; its models_trained comes from trained_models while TrainingProvenance.models_trained may differ after an invalidation. Two authoritative-looking records in the reviewer's hands, disagreeing, with no cross-check.", "ev": "pages/10_Report_Export.py:2242 generate_metadata() -> metadata.json; utils/session_manager.py:267-276 provenance.json (a different artifact, different file, different trigger)", "act": "Emit provenance.json (and the ledger) into the export package and derive metadata.json from the Record, or delete metadata.json. Add a consistency test asserting the two agree on n, splits and models.", "status": "UNVERIFIED"}, {"id": "RECORD-019", "area": "RECORD", "sev": "landmine", "item": "Seven export artifacts are reachable only via individual buttons and are absent from the ZIP; four of them also bypass the validation gate.", "detail": "A user who downloads 'Download Complete Package (ZIP)' does not get the TRIPOD checklist, Table 1 as CSV/TeX, the evidence map, or the flow diagram \u2014 while the LaTeX Supplementary explicitly says '[See exported TRIPOD checklist CSV/PDF]'. And evidence_map.md, flow_diagram.mmd, tripod_checklist.csv and the Table 1 exports download even when validation FAILED and every other export is blocked.", "ev": "pages/10_Report_Export.py \u2014 tripod_checklist.csv (:1907), table1_final.csv (:1941), table1_final.tex (:1946), methods_section.md (:2074), evidence_map.md (:2097), flow_diagram.mmd (:2132); ZIP builder at :2237-2445", "act": "Make the package the single artifact and generate it from one manifest; apply the gate uniformly. In TurboTab, packaging is a Job with an explicit member list that a test can assert against.", "status": "UNVERIFIED"}, {"id": "STATE-001", "area": "STATE", "sev": "landmine", "item": "apply_plausibility_filter calls reset_index(drop=True), and its output becomes the app's active dataframe \u2014 which silently voids the test lockbox for every downstream step.", "detail": "The lockbox stores index LABELS precisely so membership survives row filtering \u2014 its module docstring (test_lockbox.py:16-17) states: 'stores index LABELS (not positions) so membership survives feature engineering and row filtering, which preserve the original index.' apply_plausibility_filter is the one filter that does NOT preserve it. After enabling plausibility_mode='filter', the frozen labels are matched against a fresh 0..n-1 RangeIndex, so `is_test_row` selects an arbitrary, wrong set of rows. If enough rows happen to match, pages/06:411 does not trip its `< 2 / < 4` guard and the run proceeds with a test set that is not the quarantined one \u2014 while every UI chip and the exported manus\u2026", "ev": "ml/pipeline.py:124 `return df.loc[mask].reset_index(drop=True)` -> pages/05_Preprocess.py:863 `st.session_state['filtered_data'] = filtered_df` -> utils/session_state.py:211-212 get_data() returns filtered_data ahead of raw_data -> utils/te\u2026", "act": "Change apply_plausibility_filter to `return df.loc[mask]` (labels intact) or, better, return the boolean mask and let the Project apply it. Then add an assertion at the lockbox boundary: `matched = len(set(lockbox['labels']) & set(df.index)\u2026", "status": "UNVERIFIED"}, {"id": "STATE-002", "area": "STATE", "sev": "landmine", "item": "PlausibilityGate cannot be sklearn-cloned. It mutates its constructor parameters in __init__, violating the sklearn clone identity contract \u2014 and reconcile_pipeline_columns clones UNCONDITIONALLY on every training run.", "detail": "sklearn.base.clone reads get_params(), deep-copies each value, reconstructs the class, then asserts `param_new is param_old` and raises RuntimeError('Cannot clone object ... as the constructor either does not set or modifies parameter lower_bounds'). PlausibilityGate's __init__ builds a NEW array from whatever it is handed, so the identity check always fails. Consequences, all silent: (1) CROSS-VALIDATION IS SILENTLY DISABLED \u2014 pages/06:1561 catches it as `st.warning(f'Cross-validation failed for {model_name}: {cv_error}. Skipping CV.')`, a transient banner, while the results table and the manuscript render complete without CV; (2) SEED SENSITIVITY RETURNS ALL NaN \u2014 pages/08:211 appends `{'s\u2026", "ev": "ml/preprocess_operators.py:50-55 `self.lower_bounds = np.array([np.nan if v is None else float(v) for v in lower_bounds], dtype=float)`. Clone sites: ml/pipeline.py:531/534/553 (inside reconcile_pipeline_columns, called at pages/06:1299 eve\u2026", "act": "Move all coercion out of __init__ into fit(), storing as fitted attributes (self.lower_bounds_, self.upper_bounds_); keep __init__ as bare assignment, exactly as UnitHarmonizer does. Change OutlierCapping to `self.params = params` and defau\u2026", "status": "UNVERIFIED"}, {"id": "STATE-003", "area": "STATE", "sev": "landmine", "item": "unit_harmonization_factors and plausibility_bounds are POSITIONAL lists aligned to the full numeric_features, but pages/05 passes a FILTERED numeric_features to build_preprocessing_pipeline. PlausibilityGate then silently applies bound j to column j' != j.", "detail": "UnitHarmonizer fails LOUDLY (numpy broadcast ValueError, caught by pages/05's try/except into st.error), but PlausibilityGate does NOT: min() truncates to the shorter length and applies the first k bounds to the first k columns of a differently-ordered, differently-sized column set. A glucose bound of [70, 140] mg/dL gets applied to, say, creatinine; every creatinine value becomes NaN and is then median-imputed by the very next pipeline step. The output has no NaNs, the shape is right, the model trains, and the recipe string cheerfully says 'Plausibility gate (NHANES)'. The same truncation makes reconcile_pipeline_columns dangerous even after the clone bug is fixed: dropping one column from \u2026", "ev": "Built against the full list: ml/pipeline.py:45 `for col in numeric_features` (build_unit_harmonization_config) and ml/pipeline.py:84 `for col, factor in zip(numeric_features, conversion_factors)` (build_plausibility_bounds), both called at \u2026", "act": "Delete the positional interfaces. build_plausibility_bounds already returns 'bounds_by_feature': {col -> {lower, upper, unit}} \u2014 make that the ONLY return, and have PlausibilityGate take a dict keyed by column name plus the ordered column l\u2026", "status": "UNVERIFIED"}, {"id": "STATE-004", "area": "STATE", "sev": "landmine", "item": "The pipelines stored in preprocessing_pipelines_by_model are FITTED ON THE FULL DATASET at build time \u2014 including the lockbox test rows \u2014 and they are what gets exported as the reproducibility artifact and previewed as CSV.", "detail": "Two distinct silent failures. (1) The exported joblib is NOT the pipeline the model was trained with \u2014 pages/06:1312 refits on X_train and stores that in the separate `fitted_preprocessing_pipelines` dict, which page 10 never exports. A reviewer who loads preprocessing/ridge_pipeline.joblib and applies it to new data gets different scaler means/imputer medians/encoder categories than the published model saw. The manifest calls it the model's pipeline. (2) The Preprocess-page CSV download contains data transformed with statistics learned from the held-out test rows. Neither is detectable from the artifact. The exported config JSON is correct, which makes the mismatch harder to notice.", "ev": "Fit on all rows: pages/05_Preprocess.py:945 `temp_pipeline.fit(X_sample)` and :999 `pipeline.fit(X_sample)`, where X_sample = df[all_features] (05:864 or :867). Stored fitted at 05:1005 -> set_preprocessing_pipelines. Consumed WITHOUT refit\u2026", "act": "Export fitted_preprocessing_pipelines (train-fitted), not preprocessing_pipelines_by_model. Better, in the rebuild: never store fitted objects in the Project at all \u2014 store PreprocessSpec, and have the job that trains a model emit a single \u2026", "status": "UNVERIFIED"}, {"id": "STATE-005", "area": "STATE", "sev": "landmine", "item": "get_preprocessing_pipeline(model_key) silently falls back to an arbitrary other model's pipeline, and returns a SHARED MUTABLE OBJECT that the caller then fits in place.", "detail": "Two effects. (A) FALLBACK: a model selected in Train & Compare but never configured in Preprocess trains under a different model's entire transform chain \u2014 log transform, Yeo-Johnson, PCA, KMeans replacement, scaling \u2014 chosen for a model with different assumptions. The only signal is a small '\u26a0\ufe0f No pipeline' badge at pages/06:896, and it is rendered only when `_has_preprocessing` is truthy (06:903). Which model donates its pipeline depends on checkbox render order, so the same project can behave differently between sessions. The manuscript's per-model preprocessing section (pages/10:1223) lists only the CONFIGURED models, so the borrowed one is invisible in the writeup. (B) ALIASING: reconci\u2026", "ev": "utils/session_state.py:448-454 (the fallback) and :468 `default_pipeline = pipelines_by_model.get('default') or next(iter(pipelines_by_model.values()), None)` (which model becomes the default). Consumed at pages/06_Train_and_Compare.py:1286\u2026", "act": "This is the question the brief asked, and the answer is: keep per-model, DELETE THE GLOBAL. In AnalysisProject, pipeline resolution must be total and explicit \u2014 when the interview lands on a model with no spec, the Router asks the question \u2026", "status": "UNVERIFIED"}, {"id": "STATE-006", "area": "STATE", "sev": "landmine", "item": "pages/03_Feature_Engineering.py hand-rolls its own cascade invalidation instead of calling reset_downstream_results(), and the copy has drifted.", "detail": "Saving engineered features clears pipelines, models, and X/y splits \u2014 but leaves behind: train_indices/val_indices/test_indices (stale row positions into a dataframe whose column set just changed), feature_names and feature_names_by_model, target_transformer and y_train_original/y_val_original/y_test_original (so a stale log1p inverse can be applied to a fresh untransformed target), cv_strategy and cv_groups_train (a 'group' strategy leaks into a subsequent standard split), preprocess_built_model_keys and preprocessing_summary (page 06 still believes preprocessing exists; page 10's Methods section still describes the deleted pipelines), cv_results, explainability_robustness, and every report\u2026", "ev": "pages/03_Feature_Engineering.py:1229-1252 (the block after `st.session_state['engineered_feature_transforms'] = ...`), versus the real thing at utils/session_state.py:283-416.", "act": "Replace the block with `reset_downstream_results(clear_feature_engineering=False)` before porting anything. Then, in the rebuild, make invalidation IMPOSSIBLE to hand-roll: AnalysisProject nodes declare `derives_from`, and there is exactly \u2026", "status": "UNVERIFIED"}, {"id": "STATE-007", "area": "STATE", "sev": "landmine", "item": "Split provenance reads three attributes that SplitConfig does not have, so target trimming is never recorded in the Methods section.", "detail": "getattr with a default never raises. record_split() is always told trimming was off with bounds [0.0, 1.0], regardless of what the researcher configured. Meanwhile pages/06:441-458 DOES log the trim correctly to methodology_log, so the two provenance channels disagree and whichever one the Methods generator reads determines whether a published paper discloses that rows were removed from the outcome distribution before splitting. This one is worth fixing before the port simply because the fix is renaming three strings and the consequence is an undisclosed exclusion criterion.", "ev": "pages/06_Train_and_Compare.py:704-706: `target_trim_enabled=bool(getattr(split_config, 'trim_target', False)), target_trim_lower=float(getattr(split_config, 'trim_lower', 0.0)), target_trim_upper=float(getattr(split_config, 'trim_upper', 1.\u2026", "act": "Rename to the real attributes. Then eliminate the class of bug: provenance recorders should take the SplitConfig object itself (or a typed dict built from it), not string-named getattr lookups. Any getattr(obj, 'literal', default) against a\u2026", "status": "UNVERIFIED"}, {"id": "STATE-008", "area": "STATE", "sev": "landmine", "item": "The pipeline recipe rendered into the manuscript omits or misreports real preprocessing steps, including reporting the WRONG outlier threshold.", "detail": "The renderer has no branch for: the 'power_transform' / 'log' / 'power_nan_guard' steps in most configurations (only step_name=='log' is handled, so Yeo-Johnson is invisible), MinMaxScaler (only StandardScaler and RobustScaler have branches at :382-385), TargetEncoder and OrdinalEncoder (only OneHotEncoder at :399), and the entire 'numeric_passthrough' transformer (the loop at :341 only matches names 'numeric' and 'categorical'). Worse, the outlier branch reads attributes OutlierCapping never sets: `hasattr(step_transformer, 'lower_percentile')` is always False so percentile capping degrades to the generic 'Outlier treatment (percentile)' with no thresholds, and the mad branch does `getattr(\u2026", "ev": "ml/pipeline.py:326-424 get_pipeline_recipe(). Consumed at pages/10_Report_Export.py:1226 and :1243 (manuscript) and pages/06:2576 (UI).", "act": "Do not port get_pipeline_recipe as an introspector. Render the recipe from the PreprocessSpec \u2014 the spec knows every option by construction, so omissions become impossible rather than merely unlikely. Add a round-trip test: for each option \u2026", "status": "UNVERIFIED"}, {"id": "STATE-009", "area": "STATE", "sev": "landmine", "item": "reset_downstream_results() does not clear preprocess_built_model_keys, preprocessing_summary, or engineered_feature_transforms, so three consumers keep describing deleted work.", "detail": "After any data change, preprocessing_pipelines_by_model is emptied but preprocess_built_model_keys still lists the old models. pages/06:826 computes `_has_preprocessing = len(_prep_built) > 0 or pipeline is not None` -> True, so the 'you are about to train with unprocessed data' warning at 06:830 is SUPPRESSED exactly when it is needed, and 06:812 re-checks the same model checkboxes. Separately, preprocessing_summary still feeds the manuscript's missing-data/scaling/encoding paragraph (pages/10:632) describing pipelines that no longer exist. And a stale engineered_feature_transforms keeps the double-transform guard excluding columns from log/power/PCA by name after the engineered columns are\u2026", "ev": "utils/session_state.py:283-416 (the function; grep it for these three keys \u2014 absent). Writers: pages/05:1011, pages/05:1037, pages/03:1227. Readers: pages/06:810 and :825 (`_prep_built` -> `_has_preprocessing`), utils/coaching_ui.py:52, uti\u2026", "act": "Add all three to reset_downstream_results now (engineered_feature_transforms belongs in the `if clear_feature_engineering:` block next to engineering_log). Longer term this is the same fix as the DAG: derived state should not be reachable e\u2026", "status": "UNVERIFIED"}, {"id": "STATE-010", "area": "STATE", "sev": "landmine", "item": "Feature selection runs LASSO with absolute alphas on UNSCALED features, so which predictors enter the published model depends on each variable's measurement unit.", "detail": "A variable measured in mg/dL versus mmol/L has ~18x different coefficient scale, so an L1 penalty at a fixed alpha shrinks one and keeps the other purely on units. stability_selection's per-bootstrap `except Exception: continue` (feature_selection.py:252) additionally swallows every failed fit and reports those features at selection probability 0.0 \u2014 indistinguishable from 'reliably not selected'. The result flows into consensus_features, into selected_features, into every downstream pipeline, and into the manuscript's variable-selection paragraph. Note also that consensus_threshold is `max(1, len(results) // 2)` (04:280), so running one or two methods makes 'consensus' mean 'any single meth\u2026", "ev": "pages/04_Feature_Selection.py:139-141 does median imputation only \u2014 no scaler. ml/feature_selection.py:241 `Lasso(alpha=0.01, max_iter=10000, ...)` in stability_selection, and :43 LassoCV in lasso_path_selection.", "act": "Insert StandardScaler before the selectors (fit on the training rows the lockbox already isolated), or switch to a standardized-coefficient formulation. Replace the bare `except: continue` with a counted failure that surfaces in FeatureSele\u2026", "status": "UNVERIFIED"}, {"id": "STATE-011", "area": "STATE", "sev": "landmine", "item": "KMeansFeatures REPLACES the feature matrix with cluster features rather than appending them \u2014 the opposite of what the UI tells the researcher.", "detail": "Enabling the KMeans option discards every original predictor and trains the model on nothing but 5 distance-to-centroid columns. Shapes stay valid, the pipeline fits, metrics compute, SHAP renders on the cluster columns. The recipe line at ml/pipeline.py:407 says 'KMeans features added: 5 clusters, distances to centroids' \u2014 'added'. Nothing indicates the predictors are gone. Two secondary hazards in the same class: if both add_distances and add_onehot_label are False, transform returns X unchanged, which may be a SPARSE matrix from OneHotEncoder(sparse_output=True) and then hits PCA, which raises TypeError('PCA does not support sparse input'); and __init__ sets self.kmeans_ = None / self.clu\u2026", "ev": "ml/feature_steps.py:100-104: comment 'For now, we are replacing X with cluster features (not appending). To append, would need: np.hstack([X, result])' followed by `return result`. UI claim at pages/05_Preprocess.py:784: 'Adds derived colum\u2026", "act": "Decide the semantics explicitly and make code and copy agree \u2014 most likely np.hstack([X, result]) plus an updated recipe line. Move the None initializations out of __init__ (trailing-underscore attributes must only exist after fit). Add a s\u2026", "status": "UNVERIFIED"}, {"id": "STATE-012", "area": "STATE", "sev": "landmine", "item": "data_processor.prepare_data is a complete, leakage-shaped alternative split path that no page uses \u2014 and it looks exactly like the API someone would reach for during the rebuild.", "detail": "It ignores the lockbox entirely, imputes on the full dataset before the split, and returns pre-scaled arrays that bypass the per-model pipeline architecture completely. If the migration moves data_processor.py into the engine wholesale (it also contains get_numeric_columns, which pages/05:110 genuinely depends on), this function arrives with a name that reads as the canonical entry point. Anyone wiring the new backend who calls prepare_data gets a working, plausible, silently-leaking baseline.", "ev": "data_processor.py:576-650. Imputes with `X.fillna(X.median())` at :615 and again at :629 BEFORE splitting at :633, then fits a StandardScaler on train. Only caller in the repo: scripts/smoke_check.py:598.", "act": "Delete it in the same commit that moves get_numeric_columns. If it must survive for smoke_check, rename it `_legacy_unsafe_prepare_data` and have it emit a warning. Also note get_numeric_columns' own quiet hazard: its 'at least one non-null\u2026", "status": "UNVERIFIED"}, {"id": "STATE-013", "area": "STATE", "sev": "landmine", "item": "Seed sensitivity pools train+val+test and re-splits, dissolving the lockbox for numbers that appear beside lockbox-derived metrics.", "detail": "The pooling is deliberate and commented ('Re-seeding a model on a FIXED split measures only model-internal randomness'), and the reasoning is sound \u2014 but the resulting spread is computed against test sets that include rows the lockbox had quarantined, and the manuscript presents it alongside lockbox metrics with no distinction. Combined with the clone landmine, a failure here produces an all-NaN table rather than an error (08:211).", "ev": "pages/08_Sensitivity_Analysis.py:130-141 builds X_pool/y_pool from all three partitions, then :150 `_seed_tts(X_pool, y_pool, test_size=_test_frac, random_state=seed)` per seed.", "act": "Tag these results in Record with `lockbox_applied=False` and have the manuscript renderer state the distinction. Every metric in the Project should carry the provenance of the split it was computed on \u2014 that flag is what lets the Router ref\u2026", "status": "UNVERIFIED"}, {"id": "STATE-014", "area": "STATE", "sev": "landmine", "item": "The 370-line split builder \u2014 the single most leakage-sensitive function in the app \u2014 lives inside a Streamlit button handler with zero unit tests, and writes its outputs through two separate branch-on-split-type blocks that must stay in sync by hand.", "detail": "Any future edit that adds or reorders a branch in one block and not the other produces splits whose stored train_indices/test_indices describe a different partition than X_train/X_test \u2014 used by Explainability to map rows back to the original dataframe. The mismatch is a wrong-rows-in-the-SHAP-plot bug, not a crash. Additionally the whole block mixes positional and label identity: `original_indices = np.where(mask)[0]` is POSITIONAL while `orig_labels = df.index[mask]` is LABEL-based, and both are live in the same scope; train_indices is stored as positions while the lockbox is labels.", "ev": "pages/06_Train_and_Compare.py:349-720. The split arrays are written via set_splits() at :610 or :646; the index lists are written by a SECOND independent branch at :682-693 keyed on the same three conditions.", "act": "Extract `build_splits(df, data_config, split_config, lockbox, cohort_detection) -> SplitResult` FIRST, before any frontend work, with SplitResult as one immutable value object written atomically (arrays + index lists + strategy + seed + loc\u2026", "status": "UNVERIFIED"}, {"id": "STATE-015", "area": "STATE", "sev": "landmine", "item": "Dead recommendation: the VIF/multicollinearity entry in EDA's 'Recommended for Your Data' router panel can never fire \u2014 it reads a key that is never written.", "detail": ".get() on a missing key returns None, so the recommendation is simply never appended. No error, no log. The most-needed diagnostic on collinear data is never suggested \u2014 and because the panel is the closest existing prototype of the Router, porting it verbatim ports the dead branch.", "ev": "pages/02_EDA.py line 1565: `if signals.collinearity_summary.get('high_pairs'):` \u2014 ml/eda_recommender.py lines 213 and 221 write `signals.collinearity_summary['high_corr_pairs']` (and page 02 itself reads the correct key at line 262).", "act": "Fix to 'high_corr_pairs' BEFORE extraction and add a test that every key read from DatasetSignals exists on the dataclass. Better: make the Router consume typed signal objects, not dict .get().", "status": "UNVERIFIED"}, {"id": "STATE-016", "area": "STATE", "sev": "landmine", "item": "Provenance always records target trimming as DISABLED, with thresholds 0.0/1.0, no matter what the user chose.", "detail": "getattr with a default swallows the wrong names. The Methods section generated by NarrativeEngine therefore states no trimming occurred while rows were in fact removed and the prediction population redefined. The on-screen st.info at 06:437-442 and the methodology log at 06:443-459 use the CORRECT fields, so the screen and the manuscript disagree and nobody notices.", "ev": "pages/06_Train_and_Compare.py lines 714-716: `target_trim_enabled=bool(getattr(split_config, 'trim_target', False))`, `target_trim_lower=float(getattr(split_config, 'trim_lower', 0.0))`, `target_trim_upper=float(getattr(split_config, 'trim_\u2026", "act": "Fix the three attribute names now. During the rebuild, ban getattr-with-default for reading a typed config object \u2014 pass the SplitConfig itself.", "status": "UNVERIFIED"}, {"id": "STATE-017", "area": "STATE", "sev": "landmine", "item": "The manuscript's stated model-selection criterion is always 'validation Accuracy', even for regression.", "detail": "Empty-string default \u2192 always the else branch. A regression manuscript claims models were selected by validation Accuracy.", "ev": "pages/06_Train_and_Compare.py line 1648: `_task_type = st.session_state.get('task_type', '')` \u2014 no code anywhere writes a session key named 'task_type' (task type lives in data_config.task_type and task_type_detection.final). Line 1649 then\u2026", "act": "Read data_config.task_type / task_type_detection.final. In TurboTab, task type must be a single Project attribute with exactly one accessor.", "status": "UNVERIFIED"}, {"id": "STATE-018", "area": "STATE", "sev": "landmine", "item": "Feature-dropout results never reach the manuscript: writer and reader use different session keys.", "detail": "`sensitivity_summary['feature_dropout_conducted']` is never set, so the LaTeX sensitivity section omits it. The seed-stability half of the same summary works, which makes the omission look like a deliberate scope choice rather than a typo.", "ev": "pages/08_Sensitivity_Analysis.py line 499 writes `st.session_state['sensitivity_dropout_results']` (and line 500 'sensitivity_dropout_baseline'); pages/10_Report_Export.py line 877 reads `st.session_state.get('sensitivity_feature_dropout')`\u2026", "act": "Fix the key; then make the Record own named result slots so a writer/reader mismatch is a type error, not a missing dict entry.", "status": "UNVERIFIED"}, {"id": "STATE-019", "area": "STATE", "sev": "landmine", "item": "Three report sections are permanently dead because nothing ever writes their session keys.", "detail": "Each read is a .get() with a falsy default, so the section is skipped with no message. The readiness audit reports 'bland_altman: inferred / recomputable' \u2014 an honest-sounding claim about a code path that does not exist.", "ev": "`bland_altman_results` \u2014 read at pages/10 lines 1526-1543 (whole Bland-Altman report section) and 1760-1761 (offered as an explainability method), but written NOWHERE in the repo; pages/07 lines 1077-1098 computes Bland-Altman and discards \u2026", "act": "Decide per feature: persist it (pages 07's BA and subgroup results are computed and thrown away) or delete the reader. Do not port a consumer whose producer is missing \u2014 the rebuild will look feature-complete and ship empty sections.", "status": "UNVERIFIED"}, {"id": "STATE-020", "area": "STATE", "sev": "landmine", "item": "The Baseline Comparison table never appears in the exported markdown report.", "detail": "Double .get() with a {} fallback yields None; the whole 'is your model better than trivial?' section is skipped. Baselines still render on page 06, so the author sees them and assumes they exported.", "ev": "pages/10_Report_Export.py line 1337: `_baselines_for_report = (export_ctx.get('manuscript_context') or {}).get('baseline_results')` \u2014 build_export_context() (lines 306-334) returns 23 keys and 'manuscript_context' is not among them. The bas\u2026", "act": "Pass the bundle explicitly. Structurally: export_ctx and manuscript_context are two overlapping snapshots of the same state \u2014 collapse them into one Record view during the rebuild.", "status": "UNVERIFIED"}, {"id": "STATE-021", "area": "STATE", "sev": "landmine", "item": "The methods-section fallback can never build a missing-data summary from the audit, because it reads an audit key page 01 does not produce.", "detail": "Both branches are skipped; execution reaches the third fallback at line 772 which recomputes from the dataframe \u2014 so it usually still works, by accident, and the dead code looks load-bearing. When df is None it silently produces no missing-data statement at all.", "ev": "pages/10_Report_Export.py lines 732, 754: `data_audit['missing_counts']`. pages/01_Upload_and_Audit.py builds audit_results with keys 'cardinality', 'dtypes', 'missing' (a list of {Column, Missing Count, Missing %} records), 'duplicates', '\u2026", "act": "Delete the two dead branches; keep the recompute. Then make the audit a typed artifact so 'which keys does the audit have' is answerable without grep.", "status": "UNVERIFIED"}, {"id": "STATE-022", "area": "STATE", "sev": "landmine", "item": "Statistical-test provenance loses the variable and the statistic for every test.", "detail": "test_name falls back to `action` (which happens to be the test name, so it looks right), variable becomes the literal string 'unknown variable', statistic becomes None. The LaTeX supplementary prints 'Shapiro-Wilk on unknown variable'.", "ev": "pages/10_Report_Export.py lines 892-896 read `details.get('test_name')`, `details.get('variable', 'unknown variable')`, `details.get('statistic')`; pages/09_Hypothesis_Testing.py logs details with keys var1/var2 (line 273), numeric_var/grou\u2026", "act": "Normalize the statistical-test record at write time (one StatTest record type used by all six branches), not at read time.", "status": "UNVERIFIED"}, {"id": "STATE-023", "area": "STATE", "sev": "landmine", "item": "The 'Models Perform Similarly' auto-recommendation branches on model names that the registry never produces.", "detail": "'RIDGE', 'LASSO', 'RF' match; 'LOGISTIC' (registry: logreg), 'ELASTIC_NET' (elasticnet), 'RANDOM_FOREST', 'SVM_LINEAR', 'SVM_RBF', 'XGB' (xgb_clf/xgb_reg), 'LGBM' (lgbm_*) never match. So a logistic-regression winner falls to the generic 'but consider if interpretability is important' and every boosted model reports 'Unknown complexity'. No error.", "ev": "pages/06_Train_and_Compare.py lines 2380-2385: `if model1_name in ['LOGISTIC','RIDGE','LASSO']` / `elif model1_name in ['RF','RANDOM_FOREST']`. model1_name comes from comparison_df['Model'] = name.upper() where name is a registry key ('logr\u2026", "act": "Drive both from ml.model_coach._get_model_info() / the registry, which is already keyed correctly. This is the pattern to hunt for generally: the page layer repeatedly re-encodes model taxonomy by hand (06:834 EXPLAINABILITY_ORDER 'Trees'/'\u2026", "status": "UNVERIFIED"}, {"id": "STATE-024", "area": "STATE", "sev": "landmine", "item": "Page 05's model-coaching fallback reads a session key that is never written, so scoped preprocessing insights only appear AFTER models have been trained.", "detail": "The fallback yields [], so on a first pass (models selected, nothing trained yet) generate_preprocessing_insights is never called and the model-scoped coaching silently does not exist. The whole block is inside a bare `try/except: pass` (line 328), so even a real error is invisible.", "ev": "pages/05_Preprocess.py line 309: `st.session_state.get(\"model_registry\", {})` \u2014 no writer exists in the repo. The expression is the `or` fallback for `_selected_for_coaching`, whose first operand is the keys of `trained_models`.", "act": "Iterate the train_model_* session keys as the rest of the page does (line 413). Ban bare `except: pass` around coaching \u2014 8 such swallows exist across pages 02/05/06 (02:1473, 05:296, 05:328, 06:1731, 06:1985, 06:2294, plus every `except Ex\u2026", "status": "UNVERIFIED"}, {"id": "STATE-025", "area": "STATE", "sev": "landmine", "item": "Positional test_indices are dereferenced against a frame that can change identity between split time and use.", "detail": "iloc on a different-length or differently-ordered frame either raises IndexError (loud, fine) or returns the WRONG ROWS (silent). Wrong rows produce a plausible y vector, so permutation importance, external-validation labels, and subgroup stratification are quietly computed against mismatched outcomes. pages/07 wraps the whole reconstruction in `except Exception: pass` (line 204-205) and falls back to X_test \u2014 hiding even the loud case.", "ev": "Written pages/06 lines 681-693; read pages/07 lines 185-196 (`df_raw.iloc[test_indices]` for permutation/SHAP raw rows AND `df_raw[target].iloc[test_indices]` for y) and line 1196 (subgroup labels). get_data() precedence is df_engineered > \u2026", "act": "Stop passing positions. The split should emit a TestSet carrying its own index labels (the lockbox already stores LABELS, not positions \u2014 utils/test_lockbox line 411 uses `set(_lockbox['labels'])`). Make row identity label-based end to end.", "status": "UNVERIFIED"}, {"id": "STATE-026", "area": "STATE", "sev": "landmine", "item": "Page 03 rebinds `selected_features` mid-file, so one name means two different things.", "detail": "When data_config.feature_cols is empty, pre_fe_feature_cols is set to the TRANSFORM selection instead of the original feature list. reset_downstream_results then 'restores' that wrong list into both selected_features and data_config.feature_cols (utils/session_state.py ~307-313), permanently shrinking the model's feature set on a Reset/Skip. Also line 495 (`untransformed = [f for f in _skew_ins.affected_features if f not in selected_features]`) depends on the second binding \u2014 correct there, which is why the shadowing looks intentional.", "ev": "pages/03_Feature_Engineering.py line 118 binds `selected_features = data_config.feature_cols or [non-target columns]` (the modeling feature set); line 423 REBINDS `selected_features = st.multiselect('Features', numeric_features, ...)` (the \u2026", "act": "Rename before touching anything else. During extraction, each tab's answer must be its own named field on a FeatureRecipe; there should be no page-scoped mutable name shared across seven tabs.", "status": "UNVERIFIED"}, {"id": "STATE-027", "area": "STATE", "sev": "landmine", "item": "Every technique tab mutates one shared X_engineered frame and mirrors it back into three session keys by hand \u2014 12 near-identical write triples.", "detail": "Miss one of the three and the frame, the name list, and the log go out of sync. The removal path at 1148-1156 already does exactly this \u2014 it updates X_engineered and engineered_features but NOT engineering_log, so a removed feature keeps its 'created' entry in the log that becomes provenance and manuscript text.", "ev": "pages/03 lines 344-346, 386-388, 486-489, 577-579, 665-668, 851-853, 922-925, 969-972, 1060-1062, 1106-1108, 1154-1155 (all: fe_work_in_progress['X_engineered'] / ['engineered_features'] / ['engineering_log'] = ...).", "act": "One append(operation) API returning a new recipe state. Never three parallel assignments.", "status": "UNVERIFIED"}, {"id": "STATE-028", "area": "STATE", "sev": "landmine", "item": "Two independent, drifting copies of the theory demos.", "detail": "Already drifted \u2014 demo_skewness in utils uses height=300 and an abbreviated 'Train your eye' paragraph, page 11's uses height=320 and the full text. A rebuild that migrates the page and keeps the util (or vice versa) silently ships the losing variant, or ships both and they diverge further.", "ev": "pages/11_Theory_Reference.py contains 18 inline demos (lines 442, 581, 710, 1045, 1151, 1327, 1408, 2075, 2172, 2706, 3408, 3565, 3728, 3880, 3981, 4108, 4678, 4829); utils/theory_demos.py contains 19 portable `demo_*(page_context, expanded\u2026", "act": "Diff all 18/19 pairs, pick a winner per demo, delete the other. Page 11 also has demos with NO util counterpart (model-family fits at 2172, PDP-hides-interaction at 3981, subgroup disparity at 4108, target trimming at 905) and utils has dem\u2026", "status": "UNVERIFIED"}, {"id": "STATE-029", "area": "STATE", "sev": "landmine", "item": "The export ZIP \u2014 joblib dumps of every model and every preprocessing pipeline, plus server-side PNG rendering of every figure \u2014 is rebuilt on EVERY page render, before the user clicks anything.", "detail": "Not silent in behaviour (it is slow), but silent in ATTRIBUTION \u2014 a typed character in the 'Paper title' box triggers the whole pipeline, and figure-export failures are counted into _plot_export_failures and surfaced only as an aggregate warning at 2448-2454. save_matplotlib_fig (2188) does not count its failures at all, so missing SHAP images are invisible.", "ev": "pages/10_Report_Export.py lines 2234-2445, unconditional module-level code. Also unconditional per render: _build_latex_export_bundle (1802), generate_latex (1819-1839, cache-keyed), generate_report (1846), validate_manuscript_bundle (1958)\u2026", "act": "This is the clearest Jobs candidate in the codebase: export = one cancellable job with progress and a per-artifact success/failure manifest. Make save_matplotlib_fig count failures like its plotly twin before porting.", "status": "UNVERIFIED"}, {"id": "STATE-030", "area": "STATE", "sev": "landmine", "item": "TDA features are dataset-level constants tiled into every row \u2014 zero variance, yet they enter selection, training, and importance rankings.", "detail": "Constant columns cannot help any model, but they survive: page 04 excludes only NON-numeric features, LASSO/RFE will simply give them zero weight, and page 01's constant-column detector runs on the AUDIT frame, not on the engineered frame. They then appear in the feature count reported to reviewers.", "ev": "pages/03_Feature_Engineering.py lines 839-844: `tda_features_array = np.array(tda_features_list).reshape(1,-1)` then `np.tile(tda_features_array, (n_samples,1))`. The comment at 772-774 confirms the intent (one point cloud \u2192 dataset-level s\u2026", "act": "Either drop them at creation with an explanation, or record them as dataset-level metadata rather than columns. At minimum re-run the constant-column check after feature engineering.", "status": "UNVERIFIED"}, {"id": "STATE-031", "area": "STATE", "sev": "landmine", "item": "Baselines and the export cohort are each derived from an arbitrary or recomputed source, so they describe slightly different populations than the models.", "detail": "Both produce valid-looking numbers. Table 1 describes a population the models were not trained on; baselines are preprocessed under a config the user may have selected for a different model family.", "ev": "pages/06 lines 2122-2124 borrow `fitted_preprocessing_pipelines[first_model]` for ALL baselines even though pipelines are per-model and can differ. pages/10 _build_analysis_cohort_df (lines 527-554) re-derives the trim thresholds from the f\u2026", "act": "Materialize the analysis cohort ONCE at split time as a Project artifact and have Table 1, baselines, and the flow diagram all read it. Give baselines their own explicit, documented preprocessing choice.", "status": "UNVERIFIED"}, {"id": "STATE-032", "area": "STATE", "sev": "landmine", "item": "Training cancellation does not cancel anything.", "detail": "The button says 'Training canceled. Trained models saved.' \u2014 but a running fit continues to completion; the message describes a state the code did not reach.", "ev": "pages/06_Train_and_Compare.py lines 1251-1266: `st.session_state.cancel_training` is initialized, and a '\ud83d\uded1 Cancel Training' button is rendered ONCE before the model loop; clicking it sets the flag and calls st.stop(). The flag is never read\u2026", "act": "Real Job semantics with cooperative cancellation checkpoints. Take page 07's between-unit checks as the minimum bar and the honest per-step progress as the model.", "status": "UNVERIFIED"}, {"id": "STATE-033", "area": "STATE", "sev": "landmine", "item": "Multiclass SHAP explains one class at compute time and a different class at render time.", "detail": "For >2 classes the stored class_label can say 'Class 0' while the array is the last class, or the stored 2D array gets re-sliced. Plots and labels are individually plausible.", "ev": "pages/07_Explainability.py lines 476-495 (compute): `if isinstance(sv_raw, list)` \u2192 len==2 takes index 1 labelled 'Class 1 (Positive)', otherwise index 0 labelled 'Class 0'; `if sv_plot.ndim == 3` \u2192 `sv_plot[:, :, -1]` (LAST class). Lines 7\u2026", "act": "Normalize once, at compute time, into an explicit {class_index, class_label, values} record and never re-normalize at render.", "status": "UNVERIFIED"}, {"id": "STATE-034", "area": "STATE", "sev": "landmine", "item": "Two independent robustness verdict systems are shown side by side and can contradict each other.", "detail": "A metric with a small absolute range but a small mean (e.g. R\u00b2=0.05\u00b10.02) reads 'Very stable' on range and 'Unstable' on CV, on the same screen. Downstream, the manuscript records one and the interpretation records the other.", "ev": "pages/08_Sensitivity_Analysis.py lines 253-260 (coefficient-of-variation bands: <2% highly robust, <5% moderately robust, <10% some instability, else unstable) and lines 304-321 (absolute-range bands: <0.03 'publication-ready without caveat\u2026", "act": "Pick one verdict function, name it, use it everywhere, and record the raw statistics alongside the verdict.", "status": "UNVERIFIED"}, {"id": "STATE-035", "area": "STATE", "sev": "landmine", "item": "Page 02 is the sole producer of dataset_profile, which three later pages silently require.", "detail": "Skipping EDA is legal \u2014 no page blocks on it. The Model Coach block (05:194) is `if _profile:`, so the coach, the evidence probe, the auto-model-selection and all scoped insights simply do not render; smart defaults fall back to scaling='standard' and missing_indicators=False; page 06 shows no viability verdicts and no class-imbalance handling even on imbalanced data. Nothing says why.", "ev": "pages/02_EDA.py line 166 `st.session_state['dataset_profile'] = profile` is the only writer. Consumers: pages/05 lines 178, 192 (Model Coach, smart defaults' outlier/missing/skew detection, generate_preprocessing_insights), pages/06 lines 8\u2026", "act": "Make profile computation a Project-level artifact computed on demand (it is already a pure ml.dataset_profile call), not a side effect of visiting a page. This is the single most important Router consequence in the audit: today the app's in\u2026", "status": "UNVERIFIED"}, {"id": "STATE-036", "area": "STATE", "sev": "landmine", "item": "train_indices / val_indices / test_indices are POSITIONS, while the lockbox and the cohort are LABELS. The only test that covers the seam explicitly notes it passes because the fixture uses a default RangeIndex.", "detail": "Any frame whose index is not 0..n-1 \u2014 a cohort run (apply_cohort returns df.loc[hit], labels preserved), a plausibility-filtered frame, a target-NaN-dropped frame \u2014 makes df_raw.iloc[test_indices] select the WRONG ROWS. SHAP, subgroup explainability and the residual tables are then computed on people who were not in the test set, with no error and plausible-looking numbers.", "ev": "pages/06_Train_and_Compare.py:385 `original_indices = np.where(mask)[0]` vs :382 `orig_labels = df.index[mask]`; written at :683-693; consumed at pages/07_Explainability.py:185 `raw_candidate = df_raw.iloc[test_indices]`, :196, :1196. tests\u2026", "act": "Store labels everywhere. Make SplitResult carry train_labels/val_labels/test_labels (pd.Index), and make the one place that needs positions derive them at use time via frame.index.get_indexer(labels), asserting no -1. Add a test with a shuf\u2026", "status": "UNVERIFIED"}, {"id": "STATE-037", "area": "STATE", "sev": "landmine", "item": "get_data()'s precedence makes filtered_data DEAD whenever df_engineered exists, and reset_downstream_results never clears filtered_data.", "detail": "A researcher enables plausibility filtering in 'filter' mode AFTER applying feature engineering: the rows they excluded are silently still in the training data, and the manuscript's n is wrong. Conversely, a filtered_data left over from an abandoned preprocessing config keeps shrinking the dataset across every reset that is not a full data change.", "ev": "utils/session_state.get_data:207-212 (`df_engineered` first, `filtered_data` only in the else branch); pages/05_Preprocess.py:863 writes filtered_data; reset_downstream_results:301-358 never touches it (only set_data:264 and reset_data_depe\u2026", "act": "Collapse the three frames into one explicit derivation chain on the Project (base \u2192 cleaned \u2192 filtered \u2192 engineered) with each step recorded, so 'engineered' is computed FROM 'filtered' rather than instead of it. Add filtered_data to the in\u2026", "status": "UNVERIFIED"}, {"id": "STATE-038", "area": "STATE", "sev": "landmine", "item": "pdp_results, sensitivity_dropout_results, sensitivity_dropout_baseline, bland_altman_results, preprocessing_summary, manuscript_export_context, compiled_pdf, manuscript_table1_df/metadata, table1_custom_test_footnotes and preprocess_built_model_keys are read by the manuscript pipeline and cleared by NOTHING.", "detail": "After a data change, a target change, a feature-selection change or a lockbox redraw, the exported manuscript contains partial-dependence curves, feature-dropout sensitivity numbers and Bland-Altman statistics computed from models that no longer exist \u2014 presented as results for the current analysis.", "ev": "reset_downstream_results _analysis_keys (utils/session_state.py:350-356) and the report list (:362-367) omit all of them. Consumers: pages/10_Report_Export.py:289 `st.session_state.get('pdp_results') or st.session_state.get('partial_depende\u2026", "act": "Make the Project's result registry EXHAUSTIVE and derive the invalidation set from it: a result key that is not registered against a stage should be a schema error at startup, not a survivor. Add a test that walks every session_state write \u2026", "status": "UNVERIFIED"}, {"id": "STATE-039", "area": "STATE", "sev": "landmine", "item": "pages/03_Feature_Engineering.py's Save button hand-rolls a cascade clear instead of calling reset_downstream_results, and the two have drifted.", "detail": "(a) Saving engineered features leaves a target_transformer and y_train_original from the previous split alive \u2014 page 08 then inverse-transforms new predictions with the old transformer. (b) The ledger keeps asserting a resolved target transform and resolved training that were just invalidated. (c) ml/publication and pages/10 read data_config.feature_cols, which still names the PRE-FE feature list, so the Methods section reports the wrong predictors while page 06 trains on the engineered ones.", "ev": "pages/03_Feature_Engineering.py:1229-1250 (21 keys) vs utils/session_state.py:283-415 (~60 keys + provenance + ledger). The Save path omits: train/val/test_indices, split_config, target_transformer, target_label_encoder, y_*_original, cv_st\u2026", "act": "Delete the hand-rolled block and route page 03's Save through the canonical resetter (which pages/03's own Reset and Skip buttons already do, at :172 and :185). Then make selected_features and data_config.feature_cols a single field on the \u2026", "status": "UNVERIFIED"}, {"id": "STATE-040", "area": "STATE", "sev": "landmine", "item": "exploratory_used (the manuscript's honesty watermark) is popped by EVERY reset_downstream_results call, including the calls that deliberately keep df_engineered.", "detail": "Engineer features in exploratory mode \u2192 turn exploratory mode off \u2192 train \u2192 export. The manuscript claims a clean held-out evaluation, and there is no warning anywhere, because the flag that would have watermarked it was removed by the very reset that was supposed to guarantee cleanliness. The in-file rationale at :412-415 ('only quarantine-clean if EVERY surviving result was computed with the lockbox on') is correct \u2014 the implementation just does not hold when a survivor is kept.", "ev": "utils/session_state.py:415 `st.session_state.pop('exploratory_used', None)` is unconditional; pages/01_Upload_and_Audit.py:1114-1119 `_on_exploratory_toggle` calls `_rdr(clear_feature_engineering=False)` \u2014 so toggling the quarantine OFF\u2192ON \u2026", "act": "Make quarantine_ever_off monotone: it can be cleared ONLY by a full data reset, never by a partial one. Better: tag every derived artifact with the quarantine state it was produced under, and let the manuscript ask the artifacts rather than\u2026", "status": "UNVERIFIED"}, {"id": "STATE-041", "area": "STATE", "sev": "landmine", "item": "exploratory_mode survives reset_data_dependent_state(), and it is a Streamlit WIDGET key that only page 01 knows how to claim from the deferred restore dict.", "detail": "(a) Upload a brand-new dataset while exploratory mode is on: the full reset clears everything except the quarantine-off flag, so the new study starts with no test-set protection and no banner. (b) Restore an exploratory session and go anywhere except page 01: is_exploratory() reads False, the app behaves as if quarantined, but the restored artifacts were produced without quarantine.", "ev": "utils/session_state.reset_data_dependent_state:418-445 never touches it; pages/01_Upload_and_Audit.py:1158 `st.checkbox(..., key='exploratory_mode')`; session_manager._SAFE_WIDGET_KEYS:131 stores it; pages/01:1130-1143 is the ONLY place tha\u2026", "act": "quarantine_off becomes an ordinary persisted Project field with no widget backing; include it in the full reset; and on load, apply it before any stage renders.", "status": "UNVERIFIED"}, {"id": "STATE-042", "area": "STATE", "sev": "landmine", "item": "session_manager's lockbox serialization is LOSSY (7 of 10 fields) while preserving the signature that suppresses a redraw.", "detail": "A restored session that was originally split BY SUBJECT comes back believing it was a plain row split: render_lockbox_status stops saying 'so no subject appears on both sides', the disclosure sentence vanishes from the manuscript, and cohort/lockbox reporting loses the grouping claim \u2014 while the labels (and therefore the actual protection) are still correct, so nothing looks wrong.", "ev": "utils/session_manager.py:298-306 encodes only labels/fraction/seed/n_total/n_test/signature/stratified; :565-573 restores the same 7. `strata`, `group_col` and `n_test_groups` are dropped. ensure_lockbox:139-141 returns the existing lockbox\u2026", "act": "Serialize the whole LockboxSpec. Add strata/group_col/n_test_groups to the encoder and decoder, and assert round-trip equality in a test.", "status": "UNVERIFIED"}, {"id": "STATE-043", "area": "STATE", "sev": "landmine", "item": "Lockbox labels are coerced to str for any non-integer index type on save.", "detail": "A float, datetime, or string-with-different-repr index round-trips to labels that match nothing in the restored frame. train_row_mask then returns all-True for every row, is_exploratory() is False, no banner fires, and the quarantine is silently gone for the rest of the session.", "ev": "utils/session_manager.py:288-296 `_plain_label`: bool\u2192str, int\u2192int, numpy scalar\u2192.item() then int-or-str, everything else\u2192`str(x)`. The comment correctly explains the numpy-int case but the str fallback is the remaining hole.", "act": "Store label_dtype alongside the labels and re-cast on load; on restore, assert |set(labels) \u2229 set(frame.index)| == n_test and raise a visible SessionLoadError otherwise (the loader already has a warnings channel for exactly this).", "status": "UNVERIFIED"}, {"id": "STATE-044", "area": "STATE", "sev": "landmine", "item": "_lockbox_signature and _content_fingerprint both rest on int(pd.util.hash_pandas_object(df, index=False).sum()), which silently wraps on int64 overflow, varies across pandas versions, and returns None/degrades on unhashable cells.", "detail": "Two materially different frames can produce the same sum (wraparound is a real collision channel over large frames), so set_data treats a genuine data change as a benign rerun and every stale model, metric and figure survives. Conversely a pandas upgrade changes the hash for identical data, so the first render after a deploy redraws the lockbox and wipes the user's work with a banner that says the data changed.", "ev": "utils/test_lockbox.py:46 and utils/session_state.py:228 (`except Exception: return None` \u2192 set_data falls back to schema-only invalidation, per the docstring at :224-226).", "act": "Use an explicit, version-pinned digest (e.g. blake2b over the parquet bytes of a canonicalized frame) and store the digest algorithm id in the Project. Never let 'cannot fingerprint' silently mean 'unchanged' \u2014 make it mean 'assume changed'\u2026", "status": "UNVERIFIED"}, {"id": "STATE-045", "area": "STATE", "sev": "landmine", "item": "reset_downstream_results restores data_config.feature_cols only when it is ALREADY truthy.", "detail": "After an FE reset with an empty data_config.feature_cols, selected_features is restored but data_config.feature_cols stays empty. Page 06 works (it prefers selected_features), so nothing errors \u2014 but ml/publication:213-215 and pages/10 read data_config.feature_cols and produce a manuscript with an empty predictor list.", "ev": "utils/session_state.py:311-313: `dc = st.session_state.get('data_config'); if dc is not None and getattr(dc, 'feature_cols', None): dc.feature_cols = list(pre_fe)`. pages/03:1218 falls back to `list(selected_features)` when data_config.feat\u2026", "act": "Drop the truthiness guard and make the two lists one field. If they must stay separate during migration, assert they are equal at every stage boundary.", "status": "UNVERIFIED"}, {"id": "STATE-046", "area": "STATE", "sev": "landmine", "item": "ledger.prune_auto_generated({'02_EDA','05_Preprocess','06_Train_and_Compare'}) runs on EVERY reset, including the feature-selection reset that explicitly asks to keep the selection.", "detail": "Applying a consensus or manual feature selection deletes every auto-generated EDA insight (skew, missingness, leakage, collinearity findings). Today they come back because the user can revisit page 02. In TurboTab's single scrolling interview there is no revisit \u2014 the findings are gone permanently, and the manuscript's limitations section quietly shrinks.", "ev": "utils/session_state.py:407-410 \u2014 outside both `if clear_feature_selection` blocks. Called from pages/04:447 and :494 with clear_feature_selection=False.", "act": "Gate the prune on the same stage graph as the rest: pruning EDA insights is only correct when the DATA changed, not when the feature list did. Make invalidate() return the prune set so it is visible and testable.", "status": "UNVERIFIED"}, {"id": "STATE-047", "area": "STATE", "sev": "landmine", "item": "Provenance sections upload and statistical_validation are never nulled, and hypothesis_test_results IS cleared.", "detail": "After a reset, WorkflowProvenance.statistical_validation still lists the tests that were run, get_completeness() reports statistical_validation=True, and the generated Methods section asserts hypothesis tests whose results were just deleted. The Router (which will key off get_completeness) will also believe that stage is done and never re-ask the question.", "ev": "utils/session_state.py:379-385 `_sections = ['eda','split','preprocessing','training','explainability','coach']` \u2014 statistical_validation and upload are absent; :353 pops hypothesis_test_results.", "act": "Null statistical_validation with the rest. Keep `upload` deliberately (it survives because it describes the data configuration, which the reset preserves) but assert that explicitly in a test so the omission reads as a decision.", "status": "UNVERIFIED"}, {"id": "STATE-048", "area": "STATE", "sev": "landmine", "item": "split_config is POPPED by the resetter but re-created with DEFAULTS by the next init_session_state().", "detail": "A 60/20/20 split with target_transform='yeo-johnson' and target trimming silently reverts to 70/15/15, transform='none', no trimming after any downstream reset. Nothing tells the user; Prepare Splits just uses different settings than the ones on screen a moment ago.", "ev": "utils/session_state.py:330 pops it inside the node-3 loop; :135 seeds `'split_config': SplitConfig()` and :183-185 re-applies any missing default at the top of every page.", "act": "Reset to an explicit default instance and surface it in the InvalidationReport, or exclude configuration objects from the pop loop entirely (they are configuration, not results \u2014 which is the resetter's own stated contract).", "status": "UNVERIFIED"}, {"id": "STATE-049", "area": "STATE", "sev": "landmine", "item": "ensure_lockbox is fed the ENGINEERED frame once feature engineering exists, so both its content signature and its subject-ID auto-detection run against generated columns.", "detail": "(a) The signature changes purely because FE added columns, so revisiting the upload step after FE can redraw the lockbox and fire a downstream reset. (b) Worse: detect_repeated_subjects scans engineered column NAMES; any generated column containing 'id'/'case'/'record'/'person' with 1.5\u201350 rows per level flips the split to GroupShuffleSplit, producing a completely different sealed set and a subject-level split the user never asked for.", "ev": "pages/01_Upload_and_Audit.py:619 `df = get_data(full_study=True)` \u2192 utils/session_state.get_data:207-215 returns df_engineered when present \u2192 pages/01:1074 `ensure_lockbox(df, target_col, task_type_final, group_col=_entity_col)` \u2192 utils/tes\u2026", "act": "Draw and validate the lockbox against the PRE-FE frame only (the frame that defined the modeling problem), and pass the candidate subject columns explicitly (detect_repeated_subjects already accepts candidate_cols) rather than scanning ever\u2026", "status": "UNVERIFIED"}, {"id": "STATE-050", "area": "STATE", "sev": "landmine", "item": "Two independent resets fire for one user action on a content change.", "detail": "A one-column cleaning action on a variable that is neither the target nor a feature redraws the sealed test set. Every prior number becomes incomparable, and the two resets interleave with the ledger prune/rollback so that which insights survive depends on ordering rather than on intent.", "ev": "utils/session_state.set_data:276-280 detects a same-schema content change \u2192 clear_cohort() + reset_downstream_results(); then on the same render pages/01:1074 calls ensure_lockbox, whose signature also changed \u2192 a second reset_downstream_re\u2026", "act": "Make the lockbox signature depend only on (eligible row labels, target values, task_type, fraction, seed, group_col, strata) \u2014 not on unrelated columns. Then a cleaning action on an unrelated column leaves the sealed set intact, which is al\u2026", "status": "UNVERIFIED"}, {"id": "STATE-051", "area": "STATE", "sev": "landmine", "item": "ensure_lockbox returns the PREVIOUS lockbox (not None) when it cannot draw a new one, and the caller cannot tell the difference.", "detail": "After a target change that leaves too few non-null rows, or a target column that no longer exists, the page happily reports and LEDGERS a holdout that describes a different modeling problem. The manuscript then contains 'A 15% test set (n=147) was held out at upload' for a study that has no valid lockbox.", "ev": "utils/test_lockbox.py:114-115 and :123-124 both `return get_lockbox()`; pages/01:1076-1104 then reads `_lb['fraction']`, `_lb['n_test']`, `_lb['seed']`, `_lb['signature']` and writes a resolved Insight asserting the holdout.", "act": "Return a discriminated result (Drawn | Unchanged | Refused(reason)) instead of an Optional dict, and let the caller/Router surface the refusal as the next question.", "status": "UNVERIFIED"}, {"id": "STATE-052", "area": "STATE", "sev": "landmine", "item": "Every provenance and ledger write in pages/ is wrapped in a bare `except Exception: pass`.", "detail": "Any schema drift in the Record layer during the migration \u2014 a renamed section, a changed record_* signature, a page id that no longer exists \u2014 produces a manuscript that is quietly missing its Methods statements, its TRIPOD completions and its audit trail, with zero diagnostics anywhere.", "ev": "utils/session_state._log_to_ledger:655-656; pages/01:1105-1106 and :1194; pages/03:1268-1269; pages/04:471-472; pages/06 (record_split); and ~15 more sites, all commented 'Provenance recording should never break the workflow'.", "act": "Replace the swallow with a structured error appended to a Project-level `record_errors` list that the Feed can surface. Keep the 'never break the workflow' property; drop the 'never tell anyone' property.", "status": "UNVERIFIED"}, {"id": "STATE-053", "area": "STATE", "sev": "landmine", "item": "All three import cycles are currently invisible because every edge is a function-local import, and the obvious refactor (methods on AnalysisProject) turns them into module-level cycles.", "detail": "Nothing fails today, so a mechanical port that keeps the same call graph looks correct until the imports move to module scope \u2014 at which point the app fails to boot with an ImportError that points at whichever module happened to be imported first, not at the design problem.", "ev": "Cycle 1: utils/insight_ledger.py:1077 \u2194 ml/publication.py:98,155,1739,1829. Cycle 2: utils/cohorts.py:474 \u2194 utils/test_lockbox.py:269. Cycle 3: utils/session_state.py:216,278,428 \u2194 utils/cohorts.py \u2194 utils/test_lockbox.py:228. Plus a fourth\u2026", "act": "Fix the layering before moving anything: engine (lockbox math, cohort math, fingerprints, TRIPOD_ITEMS, model vocabulary) \u2190 project (AnalysisProject owns lockbox + cohort + invalidate) \u2190 record \u2190 router \u2190 jobs \u2190 view. Concretely: extract TR\u2026", "status": "UNVERIFIED"}, {"id": "STATE-054", "area": "STATE", "sev": "landmine", "item": "engineering_log is not persisted while df_engineered, feature_engineering_applied and engineered_feature_names are.", "detail": "Save and restore a session with engineered features: the engineered frame and its column names come back, the app looks fully restored, but the list of transforms that produced them is gone \u2014 so the Methods section silently stops describing how the features were built.", "ev": "utils/session_manager.py:91-117 _PLAIN_KEYS includes engineered_feature_names, engineered_feature_transforms and pre_fe_feature_cols but NOT engineering_log; utils/session_state.py:305 pops it on FE reset; ml/publication.py:601 reads it for\u2026", "act": "Add engineering_log to the serialized recipe (it is a plain List[str]). More generally: audit every ml/publication.py session_state read (32 refs) against session_manager's persist buckets \u2014 anything the manuscript reads must survive a rest\u2026", "status": "UNVERIFIED"}, {"id": "STATE-055", "area": "STATE", "sev": "landmine", "item": "cohort_runs_done is not persisted, and completed_runs() filters on isinstance(r, CohortRun).", "detail": "Save a session mid-way through 'model the men, then model the women' and reload: the cross-cohort comparison table and comparison_caveats() come back empty, the run sequencer's position/of counters restart, and the multiple-comparisons disclosure that comparison_caveats() generates silently disappears from the manuscript.", "ev": "utils/cohorts.py:462-465 and :488; utils/session_manager.py \u2014 cohort_runs_done appears in no bucket, so it is dropped on save and never restored.", "act": "Persist cohorts_done as a list of dicts and rebuild via a CohortRun.from_dict (the dataclass is already JSON-safe). The isinstance filter must then accept the reconstructed objects \u2014 today it would silently discard restored dicts.", "status": "UNVERIFIED"}, {"id": "STATE-056", "area": "STATE", "sev": "landmine", "item": "InsightLedger.from_list uses .add() (skip duplicates) rather than .upsert(), and Insight.to_dict omits manuscript_text.", "detail": "(a) A save file containing two entries with the same id silently keeps the first and drops the second. (b) manuscript_text \u2014 the field that exists specifically so coaching voice never reaches the manuscript \u2014 is lost on every save/restore, so restored sessions fall back to _clean_for_manuscript(finding) and the Discussion section quietly changes register.", "ev": "utils/insight_ledger.py:1346-1355 (from_list \u2192 ledger.add(...) inside `except (TypeError, KeyError): continue`) and :617-644 (to_dict lists 23 fields; manuscript_text is not among them).", "act": "Use upsert in from_list, add manuscript_text to to_dict, and add a round-trip property test (to_list \u2192 from_list \u2192 to_list is identity) \u2014 the ledger is the Record layer's whole contract and currently has no such test.", "status": "UNVERIFIED"}, {"id": "CONTRACT-008", "area": "CONTRACT", "sev": "high", "item": "Page 10 reads data_audit['missing_counts'] which page 01 never writes", "detail": "pages/01 builds `audit_results` with keys 'cardinality', 'dtypes', 'missing' (list of records), 'duplicates', 'numeric_stats' and assigns it to `st.session_state.data_audit`. pages/10 twice tests `if data_audit and 'missing_counts' in data_audit` to build the manuscript's missing-data summary. That key is never present, so the missing-data statement in the Methods section silently falls through to the dataset_profile path or is omitted entirely.", "ev": "pages/01_Upload_and_Audit.py:657-743, :828 vs pages/10_Report_Export.py:732-733, :754-755", "act": "record \u2014 data_audit is a Record artifact; define its schema once and make the report read the declared field", "status": "UNVERIFIED"}, {"id": "CONTRACT-009", "area": "CONTRACT", "sev": "high", "item": "record_split() reads three SplitConfig attributes that do not exist \u2014 target trimming never reaches the manuscript", "detail": "`target_trim_enabled=bool(getattr(split_config, 'trim_target', False))`, `target_trim_lower=float(getattr(split_config, 'trim_lower', 0.0))`, `target_trim_upper=float(getattr(split_config, 'trim_upper', 1.0))`. SplitConfig's real fields are `target_trim_enabled`, `target_trim_lower`, `target_trim_upper` (utils/session_state.py:78-80). The getattr defaults mask the typo, so SplitProvenance always records 'no trimming' even when rows were removed \u2014 the generated Methods section understates the exclusions and the flow diagram's N is wrong.", "ev": "pages/06_Train_and_Compare.py:714-716 vs utils/session_state.py:78-80", "act": "record \u2014 bug; the Project must expose typed accessors so a renamed field is an AttributeError, not a silent default", "status": "UNVERIFIED"}, {"id": "CONTRACT-010", "area": "CONTRACT", "sev": "high", "item": "st.session_state['task_type'] is read but never written anywhere in the codebase", "detail": "`_task_type = st.session_state.get('task_type', '')` then `_selection_metric = 'RMSE' if _task_type == 'regression' else 'Accuracy'`. Since the key is never set, TrainingProvenance.selection_criteria is ALWAYS recorded as 'validation Accuracy' \u2014 including for every regression study. The manuscript's model-selection sentence is wrong for half of all projects.", "ev": "pages/06_Train_and_Compare.py:1648-1650; no writer found for key 'task_type' repo-wide", "act": "record \u2014 bug; Project.task_type must be a single computed property with no string-keyed shadow", "status": "UNVERIFIED"}, {"id": "CONTRACT-011", "area": "CONTRACT", "sev": "high", "item": "Four sources of truth for task type, consulted inconsistently by page", "detail": "(1) `TaskTypeDetection.detected` (2) `TaskTypeDetection.final` property (override-aware) (3) `DataConfig.task_type` \u2014 explicitly marked DEPRECATED at utils/session_state.py:65 but still the value page 01 writes (4) the never-written `session_state['task_type']`. Page 06 branches on `task_type_final` but passes `data_config.task_type` into perform_cross_validation and into the y_test_proba guard; pages 07, 08 and 10 use `data_config.task_type` exclusively. Page 08 does `task_type = getattr(data_config, 'task_type', 'regression') or 'regression'` \u2014 a stale/blank DataConfig silently produces regression metrics for a classification model.", "ev": "utils/session_state.py:22-27,65; pages/06:1556,1575; pages/07:461; pages/08:64; pages/10:281,540", "act": "project \u2014 one `task_type` property on AnalysisProject; delete DataConfig.task_type", "status": "UNVERIFIED"}, {"id": "CONTRACT-012", "area": "CONTRACT", "sev": "high", "item": "y_test_proba can be referenced unbound when task_type_final and data_config.task_type disagree", "detail": "`y_test_proba` is assigned only inside the classification branch (`else:` of `if task_type_final == 'regression'`). The results dict then evaluates `y_test_proba if data_config.task_type == 'classification' else None`. If task_type_final=='regression' while data_config.task_type=='classification' (possible whenever the override is toggled without a config re-save), the conditional's true-branch dereferences an unbound name \u2192 NameError, swallowed by the surrounding `except Exception as e` and shown as a generic 'Training failed' with no cause.", "ev": "pages/06_Train_and_Compare.py:1556 (assignment) vs :1575 (use), handler at :1601-1605", "act": "project \u2014 eliminated by collapsing task_type to one source; the bare Exception handler around the training loop must be narrowed in the Jobs layer", "status": "UNVERIFIED"}, {"id": "CONTRACT-013", "area": "CONTRACT", "sev": "high", "item": "get_data() precedence makes page 05's plausibility filter a silent no-op after Feature Engineering", "detail": "`get_data()` returns df_engineered if present, ELSE filtered_data, ELSE raw_data. Page 05 writes `st.session_state['filtered_data'] = filtered_df` when any model uses plausibility_mode='filter'. If FE was applied first (the normal order), df_engineered wins and the plausibility filter is silently discarded \u2014 the pipeline was configured and n_output_features measured against filtered rows, but training runs on unfiltered rows. Worse, reset_downstream_results() does NOT pop filtered_data (only set_data and reset_data_dependent_state do), so it persists stale across dataset content changes.", "ev": "utils/session_state.py:206-217; pages/05_Preprocess.py:863; absent from utils/session_state.py:283-415", "act": "project \u2014 model row-filters as an explicit ordered stack of masks on the Project, not a precedence chain of shadow DataFrames", "status": "UNVERIFIED"}, {"id": "CONTRACT-014", "area": "CONTRACT", "sev": "high", "item": "Feature Selection silently drops all categorical predictors while telling the user it does not", "detail": "Page 04 computes `numeric_features` only, shows `st.info(\"...These features are retained in your dataset and can still be used for modeling.\")`, then on Apply does `data_config.feature_cols = consensus` where consensus is drawn exclusively from numeric_features. Every categorical predictor is removed from the modeling feature set, contradicting the on-screen promise. Manual selection has the same defect (`options=numeric_features`).", "ev": "pages/04_Feature_Selection.py:100-101, :112-119, :440-442, :488-491", "act": "router \u2014 the question 'which predictors survive selection' must return a full feature set; engine keeps numeric-only ranking but the Project must union back the non-ranked columns", "status": "UNVERIFIED"}, {"id": "CONTRACT-015", "area": "CONTRACT", "sev": "high", "item": "Lockbox save/restore drops group_col, strata and n_test_groups", "detail": "The encoder writes only labels, fraction, seed, n_total, n_test, signature, stratified. A grouped (split-by-subject) lockbox restores as an ungrouped one: render_lockbox_status stops disclosing 'split by subject', and any manuscript sentence derived from it understates the leakage control that was actually applied. Because `signature` is restored, ensure_lockbox will NOT redraw and thus never recover the lost fields.", "ev": "utils/session_manager.py:290-301 vs utils/test_lockbox.py:212-224", "act": "record \u2014 round-trip the whole lockbox dict; it is a Record artifact, not a cache", "status": "UNVERIFIED"}, {"id": "CONTRACT-016", "area": "CONTRACT", "sev": "high", "item": "Active cohort run and completed runs are not persisted at all", "detail": "`cohort_run` and `cohort_runs_done` appear in none of _DATAFRAME_KEYS/_DATACLASS_KEYS/_PLAIN_KEYS/_NEVER_PERSIST. A save taken mid-cohort-run restores with the lockbox intact but no cohort filter, so get_data() silently returns the WHOLE study under a session that was labelled 'women'. CohortRun is a dataclass with only JSON-safe fields, so this is an omission, not a technical limit.", "ev": "utils/cohorts.py:334-344 (CohortRun), :389-407 (_ACTIVE_KEY/_DONE_KEY); utils/session_manager.py:73-162 (key taxonomy)", "act": "project \u2014 cohort membership is core Project state; add it to the serialized schema with the same label-integrity check as the lockbox", "status": "UNVERIFIED"}, {"id": "CONTRACT-017", "area": "CONTRACT", "sev": "high", "item": "EDA (page 02) runs target-aware analyses on ALL rows including the sealed test set", "detail": "test_lockbox.py's module docstring states 'Every target-aware step upstream of Train & Compare \u2014 feature-engineering fits, feature selection, target-association views \u2014 operates on training rows only, via train_row_mask()'. Page 02 never calls train_row_mask; it uses `df = get_data()` unfiltered. The target-aware actions target_profile, dose_response_trends, collinearity_map, leakage_scan and especially quick_probe_baselines (which does its OWN 80/20 train_test_split on all rows and fits models) therefore see held-out people. dataset_profile \u2014 which drives the model coach's picks \u2014 is likewise computed on all rows.", "ev": "pages/02_EDA.py:79 (`df = get_data()`), :71-72 (caption only); ml/eda_actions.py:1206-1224; utils/test_lockbox.py:8-11", "act": "router \u2014 the Router owns 'may this question see test rows'; engine functions must take an explicit row mask rather than deciding for themselves", "status": "UNVERIFIED"}, {"id": "CONTRACT-018", "area": "CONTRACT", "sev": "high", "item": "Preprocessing pipelines are stored FITTED on a sample, then re-fit at train time \u2014 two different fitted states share one slot", "detail": "Page 05 does `pipeline.fit(X_sample); X_transformed = pipeline.transform(X_sample)` purely to record `n_output_features`, then stores the (now fitted-on-a-sample) Pipeline in `preprocessing_pipelines_by_model`. Page 06 later calls `model_pipeline.fit(X_train)` on the SAME object, mutating it in place, and stores it again under `fitted_preprocessing_pipelines`. Both dict entries can end up aliasing one mutated object. Anything that reads preprocessing_pipelines_by_model after training (page 10 export context) gets a pipeline fitted on training rows, not the configured-but-unfitted recipe it thinks it has.", "ev": "pages/05_Preprocess.py:999-1006; pages/06_Train_and_Compare.py:1310-1315, :1596; utils/session_state.py:463-471", "act": "project \u2014 separate 'recipe' (config, JSON-serializable) from 'fitted artifact' (pickle-only, job output); never let one object be both", "status": "UNVERIFIED"}, {"id": "CONTRACT-019", "area": "CONTRACT", "sev": "high", "item": "get_provenance() only catches ImportError, not the no-script-run-context error", "detail": "`try: import streamlit as st; if 'workflow_provenance' not in st.session_state ... except ImportError: return WorkflowProvenance()`. In a background worker streamlit imports fine but touching st.session_state raises StreamlitAPIException/RuntimeError, which is not caught. Every engine path that records provenance will hard-crash the first time it runs off the main script thread \u2014 which is exactly what the Jobs layer introduces.", "ev": "utils/workflow_provenance.py:662-671", "act": "record \u2014 provenance must be passed in explicitly, never fetched from ambient context", "status": "UNVERIFIED"}, {"id": "CONTRACT-020", "area": "CONTRACT", "sev": "high", "item": "Five session keys are read by Report Export but written by nobody", "detail": "`shap_values` (pages/10:288, :1754), `partial_dependence` (pages/10:289, :1756 \u2014 only ever cleared, never populated: page 07 writes `pdp_results`), `explainability_robustness` (pages/10:290), `bland_altman_results` (pages/10:1526-1534, :1760 AND ml/publication.py:1174), `sensitivity_feature_dropout` (pages/10:877 \u2014 page 08 writes `sensitivity_dropout_results`). Bland-Altman and feature-dropout can therefore NEVER appear in the manuscript even after the user runs them.", "ev": "pages/10_Report_Export.py:288-290,877,1526,1754-1760; pages/07_Explainability.py:623-625; pages/08_Sensitivity_Analysis.py:499-500; ml/publication.py:1174,1382", "act": "drop the dead aliases; record \u2014 wire sensitivity_dropout_results and a real bland_altman artifact into the Record", "status": "UNVERIFIED"}, {"id": "CONTRACT-021", "area": "CONTRACT", "sev": "high", "item": "Report's analysis cohort N is recomputed with different trim semantics than the split actually used", "detail": "_build_analysis_cohort_df applies target trimming using quantiles of the WHOLE frame. Page 06 deliberately computes trim thresholds from TRAINING rows only when the lockbox applies, and never drops test rows (`trim_mask = trim_mask | pd.Series(is_test_row, ...)`). The manuscript's reported analysis N and Table 1 population can therefore differ from the population that was actually modeled, with no warning.", "ev": "pages/10_Report_Export.py:539-554 vs pages/06_Train_and_Compare.py:421-433", "act": "record \u2014 persist the realized cohort membership at split time; the report must read it, never re-derive it", "status": "UNVERIFIED"}, {"id": "CONTRACT-022", "area": "CONTRACT", "sev": "high", "item": "Two different precedence rules for 'the current feature list'", "detail": "Page 03 uses `data_config.feature_cols if data_config.feature_cols else [c for c in df.columns if c != target]` \u2014 it ignores session_state.selected_features entirely. Pages 05/06/07/10 use `st.session_state.get('selected_features') or data_config.feature_cols`. After FE, selected_features is overwritten with EVERY engineered column (`[col for col in df_engineered.columns if col != target]`), so the two lists routinely disagree and page 03 re-engineers from the pre-selection list.", "ev": "pages/03_Feature_Engineering.py:118, :1223; pages/06_Train_and_Compare.py:357; pages/05_Preprocess.py (selected_features'); pages/10:379-383", "act": "project \u2014 one ordered feature list on the Project with an explicit provenance tag (uploaded / engineered / selected); delete the duplicate", "status": "UNVERIFIED"}, {"id": "CONTRACT-023", "area": "CONTRACT", "sev": "high", "item": "ml/eda_actions.py functions take st.session_state as a positional argument", "detail": "All 17 action functions have the signature `(df, target, features, signals, session_state) -> Dict`. Two of them actually read it: `session_state.get('unit_overrides', {})` and `session_state.get('eda_outlier_method', 'iqr')`. The other 15 accept it and ignore it. This is the single biggest false coupling in ml/ \u2014 replacing the 5th parameter with an explicit typed options object frees the whole module.", "ev": "ml/eda_actions.py:29-34 (plausibility_check), :50, :781; call sites pages/02_EDA.py:1500, :1588", "act": "engine \u2014 change the signature to (df, target, features, signals, opts: EdaOptions); Project supplies opts", "status": "UNVERIFIED"}, {"id": "CONTRACT-024", "area": "CONTRACT", "sev": "high", "item": "Boundary Upload\u2192Target: test_lockbox dict", "detail": "`{labels: List[label], fraction: float, seed: int, n_total: int, n_test: int, signature: str, stratified: bool, strata: List[str], group_col: Optional[str], n_test_groups: Optional[int]}`. JSON-serializable IF labels are coerced to plain int/str. `signature` embeds a pandas content hash; ensure_lockbox rebuilds only on signature change, and a rebuild that changes labels calls reset_downstream_results(clear_feature_engineering=False) as a SIDE EFFECT from inside utils/, plus sets a `_lockbox_redrawn` flag the page must pop to disclose it.", "ev": "utils/test_lockbox.py:102-234, esp. :212-234", "act": "project \u2014 the lockbox belongs to Project; the cascading reset must move out of the accessor and into the invalidation DAG", "status": "UNVERIFIED"}, {"id": "CONTRACT-025", "area": "CONTRACT", "sev": "high", "item": "Boundary Preprocess\u2192Train: the coach writes page-06 widget keys directly", "detail": "`for pick in _coach_picks: st.session_state[f'train_model_{pick.model_key}'] = True`, guarded by a one-shot `_coach_applied` flag. Page 06 reads the same key namespace at :814 and :887 and additionally force-sets it True for every key in preprocess_built_model_keys. This is the existing advisor\u2192router seam and it is implemented as cross-page widget-key mutation \u2014 the single most fragile thing to port.", "ev": "pages/05_Preprocess.py:290-294; pages/06_Train_and_Compare.py:808-817, :887-889", "act": "router \u2014 this IS the Router in embryo; replace with an explicit Router decision the Feed renders, never a widget-key write", "status": "UNVERIFIED"}, {"id": "CONTRACT-026", "area": "CONTRACT", "sev": "high", "item": "Boundary Train: X_train/X_val/X_test are DataFrames but y_* type varies by split branch", "detail": "X_* are always pd.DataFrame (X.iloc[idx]). y_* are pd.Series in the lockbox and stratified branches, but raw np.ndarray in the group-split branch (`y_arr = to_numpy_1d(y)`) and the time-split branch. set_splits() normalizes only when a target transform is applied (via to_numpy_1d); otherwise the heterogeneous type propagates to every consumer. Page 08 does `list(X_train.columns) if hasattr(X_train,'columns')` precisely because of this.", "ev": "pages/06_Train_and_Compare.py:511-517, :541-547, :571-577, :613-616, :655-660; ml/splits.py:9 (to_numpy_1d); pages/08:65", "act": "project \u2014 Splits must be one typed object with a single normalized representation; do the coercion once at construction", "status": "UNVERIFIED"}, {"id": "CONTRACT-027", "area": "CONTRACT", "sev": "high", "item": "Boundary Train: target_transformer is a UNION of a string sentinel and a fitted object", "detail": "`st.session_state['target_transformer']` is either the literal string 'log1p' or a fitted sklearn PowerTransformer. Every consumer must branch: `if _bt_transformer == 'log1p': np.expm1(...) else: _bt_transformer.inverse_transform(...)`. That branch is duplicated in page 06 (twice: test preds, train preds), the CV path (TransformedTargetRegressor with func/inverse_func vs transformer=clone), and page 08. One missed branch inverts predictions with the wrong function and produces plausible-looking wrong numbers.", "ev": "pages/06_Train_and_Compare.py:644, :1560-1566, :1590-1598, :1531-1546; pages/08_Sensitivity_Analysis.py (target_transformer')", "act": "engine \u2014 one TargetTransform ADT with .forward/.inverse; picklable-only when it wraps PowerTransformer, so Project stores the SPEC and Jobs re-fits", "status": "UNVERIFIED"}, {"id": "CONTRACT-028", "area": "CONTRACT", "sev": "high", "item": "Boundary Train: y_train_original / y_val_original / y_test_original", "detail": "np.ndarray copies of the pre-transform targets, written ONLY when a target transform is active and popped otherwise. All metric computation reads `st.session_state.get('y_train_original', y_train)` \u2014 the .get default silently substitutes the TRANSFORMED target if the key is missing, which happens whenever the pop/write pair desynchronizes. Metrics would then be reported on the log scale while labelled original-scale.", "ev": "pages/06_Train_and_Compare.py:645-647, :679-682 (pop branch), :1559, :1587", "act": "project \u2014 Splits carries y in both scales as explicit fields; never use .get() with a silently-wrong default for a scale-bearing value", "status": "UNVERIFIED"}, {"id": "CONTRACT-029", "area": "CONTRACT", "sev": "high", "item": "Boundary Train: cv_strategy and cv_groups_train", "detail": "cv_strategy: Literal['standard','group','time'] and cv_groups_train: Optional[np.ndarray] (group labels for TRAIN ROWS ONLY). Set as a side effect of the split branch, consumed by ml.eval.perform_cross_validation. cv_groups_train is a numpy array \u2014 not JSON-safe. Both are cleared by reset_downstream_results. If the split branch changes and these are not updated in lockstep, CV folds silently leak across the grouping variable.", "ev": "pages/06_Train_and_Compare.py:494-497, :507-508, :528, :1543-1546; utils/session_state.py:330-334", "act": "project \u2014 derive the CV scheme FROM the recorded split strategy rather than storing it as a parallel field that can desync", "status": "UNVERIFIED"}, {"id": "CONTRACT-030", "area": "CONTRACT", "sev": "high", "item": "Boundary Train: feature_names vs feature_names_by_model", "detail": "feature_names: List[str] = the RAW input columns (`list(feature_cols)`). feature_names_by_model: Dict[model_key -> List[str]] = post-transform names from ml.pipeline.get_feature_names_after_transform (PCA\u2192PC1..PCn, KMeans\u2192kmeans_*). Page 07 has three-tier fallback logic to reconcile a length mismatch against the actual SHAP/PI array width, ending in generic 'feature_0..n' names \u2014 i.e. mislabeled importance plots are already an accepted failure mode.", "ev": "utils/session_state.py:488-496; pages/06_Train_and_Compare.py:1595-1598; ml/pipeline.py:427-433; pages/07_Explainability.py:391-404, :492-506", "act": "project \u2014 post-transform feature names are an output of the fitted pipeline artifact and must travel WITH it, not in a parallel dict that can go out of sync", "status": "UNVERIFIED"}, {"id": "CONTRACT-031", "area": "CONTRACT", "sev": "high", "item": "Boundary Train\u2192Explain: fitted_estimators vs trained_models \u2014 two objects per model with different semantics", "detail": "trained_models holds the WRAPPER; fitted_estimators holds the bare sklearn estimator via `model.get_model()`, EXCEPT for 'nn' where it holds `model.get_sklearn_estimator()` and force-fits it on one row if unfitted (`fitted_estimator.fit(X_train_model[:1], y_train[:1])`). Page 07 builds `SklearnPipeline([('preprocess', prep), ('model', estimator)])` from fitted_estimators + fitted_preprocessing_pipelines. Three dicts must stay key-aligned; nothing enforces it.", "ev": "pages/06_Train_and_Compare.py:1583-1594; pages/07_Explainability.py:171-177", "act": "jobs \u2014 one TrainedModel record per key holding {wrapper, estimator, pipeline, feature_names_out}; three parallel dicts is the bug factory", "status": "UNVERIFIED"}, {"id": "CONTRACT-032", "area": "CONTRACT", "sev": "high", "item": "Boundary Explain: cancel_explainability / cancel_training are cooperative flags polled inside the render loop", "detail": "Both are booleans set by an st.button inside the same script run that is doing the work, then polled between models (`if st.session_state.cancel_explainability: break`). This only works because Streamlit re-runs the script; there is no actual cancellation of an in-flight sklearn call. permutation_importance at 3000 features is explicitly documented as ~141s uninterruptible.", "ev": "pages/07_Explainability.py:321-352, :419-422, :540-543; pages/06_Train_and_Compare.py:1252-1266", "act": "jobs \u2014 this is the concrete requirement the Jobs layer exists to satisfy; the current flags cannot interrupt anything and must be replaced, not ported", "status": "UNVERIFIED"}, {"id": "CONTRACT-033", "area": "CONTRACT", "sev": "high", "item": "Boundary \u2192Report: methodology_log has last-wins semantics for six steps and append for the rest", "detail": "`List[{timestamp, step, action, details}]`. REPLACE_STEPS = {'Upload & Audit','Feature Engineering','Feature Selection Applied','Preprocessing','Model Training','Explainability'} scan backwards and replace in place; everything else appends. Every call ALSO writes a pre-resolved Insight to the ledger via _log_to_ledger with a fixed id for replace-steps and an action-slug id for additive ones \u2014 so the ledger inherits the same replace/append duality through id collision, not through explicit logic.", "ev": "utils/session_state.py:505-544, :578-587, :590-656", "act": "record \u2014 this dual-write bridge is the most load-bearing undocumented contract in the app; the id-collision-as-upsert trick must be made explicit", "status": "UNVERIFIED"}, {"id": "CONTRACT-034", "area": "CONTRACT", "sev": "high", "item": "Boundary \u2192Report: workflow_provenance", "detail": "WorkflowProvenance with 11 optional section dataclasses (upload, eda, feature_engineering, feature_selection, split, preprocessing{shared, per_model: Dict[str, ModelPreprocessingConfig]}, training, explainability, sensitivity, statistical_validation, coach). to_dict via asdict(); from_dict reconstructs each section with field filtering and hand-rolls the nested per_model dict. Fully JSON-safe. reset_downstream_results nulls sections by setattr on a hard-coded name list ['eda','split','preprocessing','training','explainability','coach'] \u2014 'statistical_validation' and 'sensitivity' are NOT in that list and therefore survive a data change, describing analyses of data that no longer exists.", "ev": "utils/workflow_provenance.py:190-214, :580-655; utils/session_state.py:377-387", "act": "record \u2014 the section list must be derived from the DAG, not typed by hand; the two omissions are live bugs", "status": "UNVERIFIED"}, {"id": "CONTRACT-035", "area": "CONTRACT", "sev": "high", "item": "Boundary \u2192Report: manuscript_context is a frozen snapshot that markdown and LaTeX both read", "detail": "_build_manuscript_context() freezes selected_results, selected_bootstrap, workflow_feature_names, train_n/val_n/test_n, dataset_descriptor and feature_counts so 'markdown and LaTeX share the same boundary'. Its feature-name resolution has a FOUR-level fallback (selected_features \u2192 data_config.feature_cols \u2192 X_train.columns \u2192 feature_names \u2192 []), each level describing a different stage of the pipeline. Whichever level fires determines the predictor count printed in the abstract.", "ev": "pages/10_Report_Export.py:370-400; ml/publication.py:332-356 (_resolve_manuscript_context), :178-246 (_resolve_workflow_feature_counts)", "act": "project \u2014 a single authoritative final-predictor list removes the whole fallback chain and the abstract/methods disagreement it causes", "status": "UNVERIFIED"}, {"id": "CONTRACT-036", "area": "CONTRACT", "sev": "high", "item": "ml/publication.py reaches into st.session_state from 8 separate call sites at function scope", "detail": "generate_methods_from_log (methodology_log), _resolve_workflow_feature_counts (pre_fe_feature_cols, engineered_feature_names, selected_features), generate_methods_section (feature_engineering_applied, engineering_log, engineered_feature_names, preprocessing_config_by_model), plus shap_results, bland_altman_results, sensitivity_seed_results, sensitivity_dropout_results, engineered_feature_transforms, methodology_log. All use local `import streamlit as st`. This is the 32-reference coupling and it is entirely READ-ONLY \u2014 every one can become a parameter.", "ev": "ml/publication.py:160-165, :212-215, :597-611, :731-732, :1171-1174, :1220-1222, :1349-1350, :1381-1382, :1401-1403, :1826-1827", "act": "engine \u2014 mechanical: thread a single ManuscriptInputs dataclass through; no logic changes needed", "status": "UNVERIFIED"}, {"id": "CONTRACT-037", "area": "CONTRACT", "sev": "high", "item": "Import cycles utils.cohorts \u2194 utils.test_lockbox and utils.cohorts \u2194 utils.session_state \u2194 utils.test_lockbox", "detail": "All three are resolved today by FUNCTION-LOCAL imports: test_lockbox.render_lockbox_status does `from utils.cohorts import active_cohort` (line 269), cohorts.record_run does `from utils.test_lockbox import get_lockbox` (line 466), session_state.get_data does `from utils.cohorts import apply_cohort` (line 216), set_data/reset_data_dependent_state do `from utils.cohorts import clear_cohort` (lines 278, 428), test_lockbox.ensure_lockbox does `from utils.session_state import reset_downstream_results` (line 228). The cycles exist because row-membership state is split across three modules that each mutate the others.", "ev": "utils/test_lockbox.py:228,269; utils/cohorts.py:216 (in session_state),466; utils/session_state.py:216,278,428", "act": "project \u2014 lockbox + cohort + active-rows are ONE concern; merging them into the Project object removes all three cycles without any deferred-import tricks", "status": "UNVERIFIED"}, {"id": "CONTRACT-038", "area": "CONTRACT", "sev": "high", "item": "session_manager persists preprocessing_config but not preprocessing_summary, baseline_results, calibration_results, sensitivity_dropout_results, data_audit, engineering_log, consensus_features or feature_selection_results", "detail": "The taxonomy comment says 'Anything not listed is dropped on save (not persisted) and ignored on load'. Eight consequential keys fall through: they are neither in _PLAIN_KEYS nor _NEVER_PERSIST, so they vanish without appearing in the manifest's skipped_keys list. A restored session silently loses its feature-selection record and its calibration numbers.", "ev": "utils/session_manager.py:69-162, :323-330 (manifest skipped_keys only records encode FAILURES, not omissions)", "act": "project \u2014 make the taxonomy exhaustive-by-construction (every Project field declares its persistence class) so omission becomes a compile-time error", "status": "UNVERIFIED"}, {"id": "MINE-015", "area": "MINE", "sev": "high", "item": "macro_shape monkey-patches sklearn.utils.validation at MODULE IMPORT, globally and unconditionally", "detail": "At import time ml/macro_shape.py rebinds `_skval.check_array = _compat_check_array` (line 41) and `_skutils.check_array = _compat_check_array` (line 45) \u2014 process-wide, affecting all sklearn code, not just gtda/umap. ml/compat.py implements the same fix properly (surgical, opt-in, idempotent via `global _PATCHED`, patches only gtda modules \u2014 ml/compat.py:22-78) and its docstring says 'This is surgical \u2014 it only affects gtda's calls, not the rest of the application' (lines 9-10). Two implementations of the same patch coexist; importing macro_shape wins because it is unconditional.", "ev": "ml/macro_shape.py:16-45; ml/compat.py:22-30, 48-56", "act": "engine \u2014 delete the macro_shape block and call ml.compat.patch_gtda_for_sklearn() explicitly from the job that needs TDA. WRONG-BUT-PLAUSIBLE OUTPUT: in a long-lived worker process, whether sklearn's check_array is patched depends on whethe\u2026", "status": "UNVERIFIED"}, {"id": "MINE-016", "area": "MINE", "sev": "high", "item": "EDA's _data_fingerprint is schema-only \u2014 it cannot see value changes", "detail": "`_data_fingerprint = (len(df), len(df.columns), tuple(sorted(df.columns)))` (pages/02_EDA.py:125) is threaded as `data_id=` into _compute_profile, _compute_signals, _get_skewed_features, _compute_outlier_heatmap, _compute_corr, _top_corr_pairs, _sort_by_target_corr and _compute_interactions. It captures shape and column NAMES only. Contrast utils/session_state._content_fingerprint, which hashes values (utils/session_state.py:228). Any cleaning action that changes values without changing shape \u2014 recode_missing (ml/import_doctor.py:876-887 sets values to NaN in place, same shape), coerce_numeric, unit override \u2014 leaves the fingerprint identical.", "ev": "pages/02_EDA.py:122-125, 146-169, 809, 920, 957, 988, 1082; ml/import_doctor.py:876-895", "act": "project \u2014 the invalidation DAG must key on a content hash, not a schema tuple. WRONG-BUT-PLAUSIBLE OUTPUT: the researcher recodes 999 \u2192 missing in a blood-pressure column, returns to EDA, and every cached panel (profile, signals, skew list,\u2026", "status": "UNVERIFIED"}, {"id": "MINE-017", "area": "MINE", "sev": "high", "item": "@st.cache_resource shares one mutable model registry across every session, and one call site hands out the shared params dict", "detail": "`@st.cache_resource def _get_registry_cached(): return get_registry()` appears three times (pages/05_Preprocess.py:31-33, pages/06_Train_and_Compare.py:795-799, pages/07_Explainability.py:30-32). cache_resource returns THE SAME object to all sessions \u2014 not a copy. get_registry() itself builds a fresh dict per call (ml/model_registry.py:178-180), so the singleton-ness comes entirely from the decorator. ModelSpec.default_params and ModelCapabilities.notes are mutable and live inside it. Page 06 defensively copies \u2014 `spec.default_params.copy()` (pages/06_Train_and_Compare.py:1437) \u2014 but Report Export does not: `params = selected_model_params.get(model_key, spec.default_params if spec else {})` \u2026", "ev": "pages/05_Preprocess.py:31-33; pages/06_Train_and_Compare.py:795-799, 1437; pages/07_Explainability.py:30-32; pages/10_Report_Export.py:1372; ml/model_registry.py:41-46, 178-180", "act": "engine \u2014 get_registry() is already pure; drop the cache_resource wrappers entirely and let the Project hold per-analysis params. WRONG-BUT-PLAUSIBLE OUTPUT (borderline, requires a mutating caller): any code path that writes into the dict re\u2026", "status": "UNVERIFIED"}, {"id": "MINE-018", "area": "MINE", "sev": "high", "item": "Two different has_outliers thresholds for the same field name, and the coach re-thresholds on top of them", "detail": "For FEATURE columns: `profile.has_outliers = profile.outlier_rate > 0.01 # >1% outliers` (ml/dataset_profile.py:215). For the TARGET: `profile.has_outliers = profile.outlier_rate > 0.05` (ml/dataset_profile.py:258). ml/model_coach.py then applies a THIRD check to the already-thresholded target field: `target_outliers = bool(tp and tp.has_outliers and (tp.outlier_rate or 0) > 0.01)` (lines 394 and 1355).", "ev": "ml/dataset_profile.py:214-215, 257-258; ml/model_coach.py:394, 460, 1355, 1387", "act": "engine \u2014 decide one threshold and make the coach read outlier_rate directly rather than the pre-thresholded boolean. WRONG-BUT-PLAUSIBLE OUTPUT today: a target with 3% outliers gets has_outliers=False, so the coach never recommends Huber lo\u2026", "status": "UNVERIFIED"}, {"id": "MINE-019", "area": "MINE", "sev": "high", "item": "detect_outliers returns an all-False mask for an unrecognized method name", "detail": "After the iqr/mad/zscore/percentile branches, the function falls through to `return mask, info` where mask is still `pd.Series(False, index=series.index)` from line 27. An unknown method is indistinguishable from 'zero outliers found'. The EDA heatmap compounds this: `except Exception: row[method.upper()] = 0.0` (pages/02_EDA.py:822-823).", "ev": "ml/outliers.py:27, 38-89; pages/02_EDA.py:819-823", "act": "engine \u2014 raise ValueError on an unknown method. WRONG-BUT-PLAUSIBLE OUTPUT: rename 'zscore' to 'z_score' anywhere during the migration (it is a user-facing selectbox value at pages/02_EDA.py:398-403 and a config string in preprocessing) and\u2026", "status": "UNVERIFIED"}, {"id": "MINE-020", "area": "MINE", "sev": "high", "item": "The JSON wrapper-key guess still lives in the engine while the disclosure lives in the view", "detail": "FINDINGS_LEDGER C4 ('Several wrapper keys present \u2192 resolved by _JSON_WRAPPER_KEYS iteration order, silently') was fixed by adding inspect_json, which computes candidates and returns a note the page renders (data_processor.py:400-420). But _json_obj_to_frame still contains the original silent first-wins loop for when records_key is None: `for wrapper in _JSON_WRAPPER_KEYS: value = obj.get(wrapper); if isinstance(value, list) and value: return ...` (data_processor.py:283-287). The user's answer travels as a Streamlit WIDGET KEY: `st.session_state.get(f\"records_key_{_fk}\", \"\")` \u2192 cached_parse_upload \u2192 load_tabular_data (pages/01_Upload_and_Audit.py:311, 385-413; utils/perf_cache.py:22-35).", "ev": "data_processor.py:124, 236-239, 283-287, 400-420; pages/01_Upload_and_Audit.py:311, 385-388, 413; utils/perf_cache.py:22-35", "act": "jobs + engine \u2014 the ingest job must carry records_key as an explicit parameter, and _json_obj_to_frame should refuse (not guess) when multiple wrapper keys are present and no key was supplied. WRONG-BUT-PLAUSIBLE OUTPUT: the upload job is w\u2026", "status": "UNVERIFIED"}, {"id": "MINE-021", "area": "MINE", "sev": "high", "item": "apply_plausibility_filter resets the index, destroying lockbox and cohort label correspondence", "detail": "`return df.loc[mask].reset_index(drop=True)` (ml/pipeline.py:124). Called from the Preprocess page (pages/05_Preprocess.py:860). Every downstream label-based mechanism \u2014 test_lockbox labels (utils/test_lockbox.py:213, 248) and cohort labels (utils/cohorts.py:420, 446-447) \u2014 assumes the original index survives row filtering; the lockbox docstring states this as a contract (utils/test_lockbox.py:16-17).", "ev": "ml/pipeline.py:109-124; pages/05_Preprocess.py:860; utils/test_lockbox.py:16-17, 248", "act": "engine \u2014 return `df.loc[mask]` without the reset. WRONG-BUT-PLAUSIBLE OUTPUT: cohorts partially survive because apply_cohort has a fallback that re-derives membership from the grouping column (`if col and col in df.columns: return df.loc[co\u2026", "status": "UNVERIFIED"}, {"id": "MINE-022", "area": "MINE", "sev": "high", "item": "Session save reports only a COUNT of skipped items, and load silently drops unknown keys", "detail": "Five save paths append to skipped_keys and swallow the reason: dataclass configs (utils/session_manager.py:236-237), insight_ledger (264-265), workflow_provenance (275-276), test_lockbox (309-310), coach_probe (320-321). The UI shows only `f\"\\n- **Note:** {len(skipped)} items skipped (non-serializable)\"` (line 729). On load, dataclass reconstruction failure is `except Exception: pass` (lines 518-519) and the comment at line 523 is explicit: '# unknown keys are silently ignored'. Restore also begins with `try: reset_data_dependent_state() except Exception: pass` (lines 421-425).", "ev": "utils/session_manager.py:236-237, 264-265, 275-276, 309-310, 320-321, 421-425, 514-523, 728-729", "act": "project \u2014 this module is the direct ancestor of Project serialization; name every skipped key in the message and make an unrecognized key a loud migration event. WRONG-BUT-PLAUSIBLE OUTPUT: the lockbox fails to encode, the sidebar says '1 i\u2026", "status": "UNVERIFIED"}, {"id": "MINE-023", "area": "MINE", "sev": "high", "item": "datasets_registry keys are coerced to str on both save and load", "detail": "Save: `index[str(dataset_id)] = member_name` (utils/session_manager.py:222). Load: `registry[str(dataset_id)] = _read_parquet(zf, member)` (line 504). Any non-string key (the pages write `st.session_state.datasets_registry[dataset_id] = df` at pages/01_Upload_and_Audit.py:285 and 518) comes back as a string.", "ev": "utils/session_manager.py:214-225, 492-507; pages/01_Upload_and_Audit.py:285, 518", "act": "project \u2014 pin dataset_id to str at creation, or store the original type alongside. WRONG-BUT-PLAUSIBLE OUTPUT: if dataset_id is ever an int or UUID object, `registry[dataset_id]` misses after a restore and the multi-file combine UI shows an\u2026", "status": "UNVERIFIED"}, {"id": "MINE-024", "area": "MINE", "sev": "high", "item": "data_processor can install a caller-supplied index from JSON, breaking every label invariant downstream", "detail": "For pandas orient='split' payloads: `if isinstance(obj.get(\"index\"), list) and len(obj[\"index\"]) == len(df): df.index = obj[\"index\"]` (data_processor.py:273-274), inside a `try/except Exception: pass # fall through` (276-277). set_data only resets the index when it is NON-UNIQUE (`if not df.index.is_unique: df = df.reset_index(drop=True)`, utils/session_state.py:249-253), so a unique but arbitrary index (strings, timestamps, floats) survives into raw_data.", "ev": "data_processor.py:271-277; utils/session_state.py:249-253; utils/test_lockbox.py:213; utils/session_manager.py:288-296", "act": "engine \u2014 normalize to a RangeIndex on ingest and keep the file's index as an ordinary column. WRONG-BUT-PLAUSIBLE OUTPUT: lockbox labels become the JSON's strings; _plain_label coerces them via `str(x)` (utils/session_manager.py:296); after\u2026", "status": "UNVERIFIED"}, {"id": "MINE-025", "area": "MINE", "sev": "high", "item": "next(iter(dict.values())) picks a 'representative' preprocessing config by insertion order", "detail": "`first_pca = next(iter(pca_details.values()))` with the comment '# Use the first PCA config as representative' (ml/publication.py:626-628), and `first_cfg = next(iter(_per_model_configs.values()))` (line 798), plus `all_cfgs = list(_per_model_configs.values()); shared_scaling = all_cfgs[0].get(...)` (lines 807-808). The source dict is `st.session_state.get('preprocessing_config_by_model', {})` \u2014 insertion-ordered by whatever sequence the Preprocess page built pipelines in.", "ev": "ml/publication.py:626-634, 780-810", "act": "record \u2014 key these by the model actually being described, or assert that the configs agree before collapsing them. WRONG-BUT-PLAUSIBLE OUTPUT: preprocessing_config_by_model is JSON-round-tripped or rebuilt in a different order by the Projec\u2026", "status": "UNVERIFIED"}, {"id": "MINE-026", "area": "MINE", "sev": "high", "item": "Macro Shape colors rows via .loc on an index it assumes is shared", "detail": "`df_numeric = df[numeric_features].dropna()` then `color_vals = df.loc[df_numeric.index, target_col].values` (pages/02_EDA.py:1140, 1144). This is correct only because dropna preserves labels. If df_numeric is ever reset (or df is), and df has a RangeIndex 0..N-1 while df_numeric was reset to 0..M-1, the .loc lookup SUCCEEDS (those labels exist) and returns the first M target values instead of the surviving rows' values.", "ev": "pages/02_EDA.py:1140-1147", "act": "engine \u2014 return the color vector from the compute function alongside the embedding, aligned by construction. WRONG-BUT-PLAUSIBLE OUTPUT: a UMAP/PCA scatter colored by the wrong target values. The plot renders, the colorbar has the right ran\u2026", "status": "UNVERIFIED"}, {"id": "MINE-027", "area": "MINE", "sev": "high", "item": "calculate_regression_metrics drops non-finite pairs and reports only to the logger", "detail": "`finite = np.isfinite(y_true) & np.isfinite(y_pred); if not finite.all(): logging.getLogger(__name__).warning(...); y_true, y_pred = y_true[finite], y_pred[finite]` (ml/eval.py:29-37). The metrics are then computed on the SUBSET and returned as ordinary floats with no indication of how many pairs were dropped. The docstring names the exact trigger: 'a degenerate target-transform back-mapping (e.g. a power transform fit on a near-constant target) can produce NaN/inf predictions.'", "ev": "ml/eval.py:18-51", "act": "engine \u2014 return n_dropped in the metrics dict and have the job surface it into the ledger. WRONG-BUT-PLAUSIBLE OUTPUT: a degenerate target transform makes 30% of test predictions NaN; those rows are dropped; R\u00b2 is computed on the 70% the mo\u2026", "status": "UNVERIFIED"}, {"id": "MINE-028", "area": "MINE", "sev": "high", "item": "The de-facto Router logic lives in the view, and four modules disagree about what 'done' means", "detail": "utils/theme.render_sidebar_workflow computes step completion by probing session_state directly: data_configured / features_selected / pipeline_built / models_trained / explainability_run / sensitivity_run / report_generated / feature_engineering_applied (utils/theme.py:744-758). Three other definitions exist: WorkflowProvenance.get_completeness returns a 10-key dict keyed on `self.<stage> is not None` (utils/workflow_provenance.py:461-477); utils/insight_ledger.WORKFLOW_PHASES groups the 10 pages into 5 phases (lines 495-501); utils/storyline.PAGE_ORDER + RECOMMENDED_PAGE_IDS defines the navigable spine and a 'quick' mode that skips pages (lines 11-34, 55-64).", "ev": "utils/theme.py:744-758; utils/workflow_provenance.py:461-477; utils/insight_ledger.py:495-501; utils/storyline.py:11-34, 55-64", "act": "router \u2014 pick get_completeness() as the single source of truth (it is already serializable and already feeds TRIPOD), and delete the theme.py probe. WRONG-BUT-PLAUSIBLE OUTPUT: the Router asks a question the user already answered (or skips \u2026", "status": "UNVERIFIED"}, {"id": "MINE-029", "area": "MINE", "sev": "high", "item": "NHANES reference is lru_cached for the process lifetime and keyed on a parameter the function ignores", "detail": "`@lru_cache(maxsize=1) def load_nhanes_reference(reference_url: Optional[str] = None)` reads `os.getenv(\"NHANES_REFERENCE_URL\")` INSIDE the body when the arg is None (ml/physiology_reference.py:51-58), so the cache key is None regardless of the env var. A fetch failure falls back silently: `except (URLError, HTTPError, ValueError): pass` \u2192 `return DEFAULT_NHANES_REFERENCE` (lines 65-68). load_reference_bundle is likewise `@lru_cache(maxsize=4)` (line 71) and its docstring says 'Cached per process.'", "ev": "ml/physiology_reference.py:51-77; ml/pipeline.py:78-79, 105", "act": "engine \u2014 pass the resolved URL in explicitly and make fallback an explicit return value the caller can record. WRONG-BUT-PLAUSIBLE OUTPUT: in a long-lived worker, one transient network blip at first call freezes the bundled defaults for the\u2026", "status": "UNVERIFIED"}, {"id": "MINE-030", "area": "MINE", "sev": "high", "item": "Sensitivity seed loop swallows failed seeds and then reports the survivor count as the analysis size", "detail": "`except Exception: pass` inside the per-seed loop (ml/sensitivity.py:92-93), followed by `description=f\"Results across {len(variations)} different random seeds (baseline seed={baseline_seed}).\"` (line 99). ml/publication.py:1253-1260 writes this into the manuscript: 'using {d.get(\"n_seeds\", \"multiple\")} random seeds to assess sensitivity of ...'.", "ev": "ml/sensitivity.py:85-100; ml/publication.py:1249-1281", "act": "engine \u2014 count attempts and failures separately and expose both. WRONG-BUT-PLAUSIBLE OUTPUT: 6 of 8 seeds fail (OOM in a worker, a convergence error), the analysis silently becomes a 2-seed study, the coefficient of variation is computed ac\u2026", "status": "UNVERIFIED"}, {"id": "MINE-031", "area": "MINE", "sev": "high", "item": "Univariate feature screening records p=1.0 for tests that CRASHED", "detail": "`except Exception: p_values.append(1.0); test_stats.append(0.0); test_names.append(\"failed\")` (ml/feature_selection.py:178-181). p=1.0 is a perfectly ordinary p-value; the only distinguishing signal is the string 'failed' in a parallel test_names list, and the p_values array is what feeds `p_values = np.array(p_values)` and the multiple-testing correction on the next lines.", "ev": "ml/feature_selection.py:172-185", "act": "engine \u2014 use NaN and exclude failed columns from the correction denominator. WRONG-BUT-PLAUSIBLE OUTPUT: a feature whose correlation test crashed (constant after masking, dtype issue) is reported as p=1.00 \u2014 'no association with the outcome\u2026", "status": "UNVERIFIED"}, {"id": "SWEEP-017", "area": "SWEEP", "sev": "high", "item": "utils/dataset_db.py \u2014 797 loc, ZERO importers anywhere in the repo", "detail": "SQLite project/dataset store: class DatasetDB (create_project/add_dataset/save_merge_config/set_working_table), module singleton `_db_instance: Optional[DatasetDB] = None` at :765, get_db() at :768, plus three pure helpers detect_common_columns(:598), suggest_join_keys(:626), execute_merge(:680). Repo-wide grep for DatasetDB/get_db()/execute_merge returns only one hit outside the file: utils/session_projects.py:29 docstring 'Drop-in replacement for DatasetDB using only session state.' ARCHITECTURE.md:179-180 and CODE_REVIEW.md both name it dead; CODE_REVIEW's 'Known remaining work' lists 'removing or wiring the dead DatasetDB'. Carrying it forward re-introduces a disk-persistence path (sqlit\u2026", "ev": "utils/dataset_db.py:14,765,768; utils/session_projects.py:29; ARCHITECTURE.md:179-180", "act": "drop", "status": "UNVERIFIED"}, {"id": "SWEEP-018", "area": "SWEEP", "sev": "high", "item": "The EDA cache fingerprint is SCHEMA-only while set_data() invalidation is CONTENT-based \u2014 a same-shape cleaning action resets results but not the profile", "detail": "pages/02_EDA.py:125 `_data_fingerprint = (len(df), len(df.columns), tuple(sorted(df.columns)))`. utils/session_state.py:220 `_content_fingerprint(df)` is a 'cheap deterministic fingerprint of a DataFrame's contents\u2026 hashes values, not identity', used by set_data() at :259-263 to fire reset_downstream_results on a same-schema content change (this was critical finding C7's fix). A cleaning action that repairs values without changing shape or column names therefore correctly clears trained models \u2014 and silently leaves DatasetProfile and dataset signals describing the pre-cleaning data. Two invalidation notions coexist and disagree.", "ev": "pages/02_EDA.py:125 vs utils/session_state.py:220-226,259-263", "act": "project (one fingerprint, one DAG \u2014 the Project's invalidation must own memo keys, not let the view invent a second scheme)", "status": "UNVERIFIED"}, {"id": "SWEEP-019", "area": "SWEEP", "sev": "high", "item": "utils/llm_ui.py:225 reads session_state['working_df'] \u2014 a key nothing writes; ctx['sample_size'] is never populated", "detail": "gather_session_context() at :200 assembles the LLM's background context. Line 225-227: `df = st.session_state.get(\"working_df\"); if df is not None and hasattr(df,\"__len__\"): ctx[\"sample_size\"] = len(df)`. Repo-wide grep: the only writes are LOCAL variables named working_df in pages/01_Upload_and_Audit.py:572,593,599; the session key that page writes is `working_table` (:596). No code anywhere assigns session_state['working_df']. Same defect class as the C4 `selected_models` read-never-written finding that killed model-aware coaching \u2014 CODE_REVIEW design principle #2 ('State first, widgets second') was written to close this class, and this instance survived it.", "ev": "utils/llm_ui.py:225; pages/01_Upload_and_Audit.py:596 (writes working_table, not working_df)", "act": "project (a Project with declared, typed fields makes this a compile-time error instead of a silent empty dict)", "status": "UNVERIFIED"}, {"id": "SWEEP-020", "area": "SWEEP", "sev": "high", "item": "utils/llm_ui.py:213 type-guards dataset_profile as a dict, but the session holds a DatasetProfile dataclass \u2014 the profile never reaches the LLM", "detail": "`profile = st.session_state.get(\"dataset_profile\"); if profile and isinstance(profile, dict): ctx[\"dataset_profile\"] = profile`. pages/02_EDA.py:166 writes `st.session_state[\"dataset_profile\"] = profile` where profile comes from ml.dataset_profile.compute_dataset_profile, and ml/dataset_profile.py:110 declares `class DatasetProfile`. The isinstance check is therefore always False. Same pattern at :268-275: preprocessing_pipelines_by_model is typed Dict[str, Pipeline] in ARCHITECTURE.md:324, but the code does `if isinstance(pipeline_data, dict)` before extracting config. Both fail closed and produce a plausible-but-thinner LLM answer with no error \u2014 the exact 'silent' failure mode the app's g\u2026", "ev": "utils/llm_ui.py:212-214,268-275; pages/02_EDA.py:166; ml/dataset_profile.py:110; ARCHITECTURE.md:324", "act": "project", "status": "UNVERIFIED"}, {"id": "SWEEP-021", "area": "SWEEP", "sev": "high", "item": "utils/llm_ui.py:532 spawns `ollama serve` via subprocess.Popen on the request thread, behind a spinner that promises ~60s but waits up to 120s", "detail": "The only subprocess/thread spawn in utils/, ml/ or models/. _call_ollama (:507), _call_openai (:621), _call_anthropic (:641) are all blocking network calls executed inline. CODE_REVIEW flags this: 'if Ollama is installed but stopped, the app Popens ollama serve on the request thread and can block up to 120s behind a spinner that promises ~60s'. This is the clearest existing candidate for the Jobs layer. Also note the privacy surface: _MAX_TABLE_ROWS = 20 (:14) \u2014 the cloud-LLM opt-in sends column names and up to ~20 raw data rows to OpenAI/Anthropic.", "ev": "utils/llm_ui.py:14,507,532,621,641", "act": "jobs (the three _call_* backends); view (render_*); project/record (context builders)", "status": "UNVERIFIED"}, {"id": "SWEEP-022", "area": "SWEEP", "sev": "high", "item": "utils/llm_ui.py + utils/theme.py + pages/01 share an undocumented three-way protocol over session_state['_pending_widget_state_restore'], and the ORDER is load-bearing", "detail": "session_manager.py:134 defines _PENDING_WIDGET_RESTORE_KEY; on session restore it defers widget keys rather than setting them (comment :120). Then llm_ui._apply_pending_llm_widget_restore() (:663) POPS the entire dict, claims six LLM keys, and re-stores everything else for 'the next owner' (:684-686). theme.render_sidebar_workflow (:719-725) then reads it for 'workflow_mode_selector' and re-stores the remainder. pages/01:1131-1139 does the same for its own key. theme.py:694 calls render_llm_settings_sidebar() FIRST, before theme's own read at :719 \u2014 reverse that order and llm_ui's pop discards nothing but theme's read runs against a dict llm_ui hasn't yet re-stored. There is no registry, no \u2026", "ev": "utils/session_manager.py:120,134; utils/llm_ui.py:663-686; utils/theme.py:694,719-725; pages/01_Upload_and_Audit.py:1131-1139", "act": "project (make widget-restore an explicit, ordered, tested Project.restore step; the pop-and-repost relay cannot survive a rewrite)", "status": "UNVERIFIED"}, {"id": "SWEEP-023", "area": "SWEEP", "sev": "high", "item": "utils/seed.py:33 get_global_seed() reads st.session_state directly \u2014 the reproducibility module is Streamlit-coupled", "detail": "`def get_global_seed() -> int: import streamlit as st; return st.session_state.get('random_seed', 42)`. The seed is the single most manuscript-critical scalar in the app (CODE_REVIEW C3: the Methods section omitted the split design and seed; the fix was calling record_split). Its accessor lives behind st.session_state with a silent default of 42 \u2014 so a Project that forgets to carry random_seed does not error, it publishes a paper claiming seed 42.", "ev": "utils/seed.py:32-35", "act": "project (seed is Project state with no default; record must read it from Project, not from a fallback)", "status": "UNVERIFIED"}, {"id": "SWEEP-024", "area": "SWEEP", "sev": "high", "item": "INVARIANT \u2014 reconcile_pipeline_columns: drift must self-heal loudly, never crash cryptically; its sibling reconcile_state_with_df is imported but never called", "detail": "CODE_REVIEW 2026-07 'Backstop (defense in depth)': 'pages/06 reconciles the feature list vs the data before Prepare Splits, and rebuilds any preprocessing pipeline whose columns drifted before fit() (ml.pipeline.reconcile_pipeline_columns) \u2014 loud warning, self-heal, never a cryptic crash. Any future drift path degrades gracefully.' That is a design stance, not a function: the rebuild must keep a reconciliation checkpoint at the split boundary even if the DAG is 'correct'. Contrast utils/reconcile.py's reconcile_state_with_df() \u2014 CODE_REVIEW C7: 'The intended safety net\u2026 is imported but never called by any page (only scripts/smoke_check.py).' Two reconcilers, one wired, one not.", "ev": "CODE_REVIEW.md 2026-07 'Backstop'; CODE_REVIEW.md C7; utils/reconcile.py", "act": "project (keep an explicit reconciliation checkpoint before every fit); drop or wire reconcile_state_with_df \u2014 do not port it dormant", "status": "UNVERIFIED"}, {"id": "SWEEP-025", "area": "SWEEP", "sev": "high", "item": "INVARIANT \u2014 CV strategy is bound to the SPLIT strategy, and cv_strategy/cv_groups_train are cleared WITH the split", "detail": "CODE_REVIEW 2026-07: 'CV previously used shuffled KFold regardless of the split. A group split (longitudinal) or time split now gets GroupKFold/StratifiedGroupKFold / TimeSeriesSplit, so CV can't leak across entities or time and inflate the score. CV still runs on training rows only; cv_strategy/cv_groups_train are recorded at split time and cleared with the split.' Also: 'Under the lockbox, the Train & Compare Test % slider is now disabled and pinned to the lockbox fraction\u2026 so a live control can never contradict the actual held-out set. Group/time splits (which draw their own test set) keep the slider live and say so.' Both are couplings between two Project fields that a field-by-field por\u2026", "ev": "CODE_REVIEW.md 2026-07 'Strategy-aware cross-validation' and 'Test-set slider guardrail'; tests/test_cv_strategies.py", "act": "project (cv_strategy is a derived field of the split, with a shared lifetime); router (a control that cannot contradict state must be disabled, not merely validated)", "status": "UNVERIFIED"}, {"id": "SWEEP-026", "area": "SWEEP", "sev": "high", "item": "WORK IN PROGRESS \u2014 named open items carried in RESUME.md, plus a stale-verdict trap", "detail": "docs/audit/RESUME.md:91-95: '(a) Page 01: transpose preview/apply consistency has a regression test gap (preview and commit now share one frame, but the Excel-sheet \u00d7 transpose combination is untested). (b) The remaining tail of the original 48 that neither I nor the adjudication has reached \u2014 the ledger table is the source of truth.' Plus the trap at :70-72: 'Note both [audit runs] were launched against the FROZEN tree at 413671a, so a \"still_broken\" verdict on findings 5, 7, 11, 13 or 27 refers to the pre-fix code \u2014 check the ledger before re-fixing.' Anyone reading the audit output cold will re-fix already-fixed code. Verification harness note at :51-54: 'Driving the multi-file screen end\u2026", "ev": "docs/audit/RESUME.md:43-54,70-72,91-95; tests/integration/test_combine_ui.py", "act": "project (replace the sp_projects AppTest harness with a Project-level fixture BEFORE porting pages/01)", "status": "UNVERIFIED"}, {"id": "SWEEP-027", "area": "SWEEP", "sev": "high", "item": "WORK IN PROGRESS \u2014 cohort runs are the NEWEST subsystem (landed 5 commits ago) and they change the meaning of get_data() itself", "detail": "The last five commits are all cohort work: b299a8a 'Cohort runs: the engine for \"same question, different people\"', e72f2ec 'Put the cohort-run engine on screen', c3cef15 'Walk both cohort runs in a browser; two more numbers that contradicted', c56bf0b 'Four defects only a screenshot could find', fbe422a (HEAD) 'Write the app in American English, and stop mixing \"cohort\" with \"group\"'. Surface: utils/cohorts.py (21.5k), utils/cohort_ui.py (14k), the apply_cohort() call inside get_data() (session_state.py:216-217), render_cohort_chip() called from theme.render_sidebar_workflow:713 ('A run filter you cannot see is a run filter you forget, and every number on every page below is then quietly ab\u2026", "ev": "git log -5; utils/session_state.py:216-217; utils/theme.py:711-714; tests/test_cohort_runs.py; tests/test_american_spelling.py", "act": "project (model the active cohort filter as a first-class Project field from day one \u2014 a Project that models only 'the working table' silently deletes the newest feature)", "status": "UNVERIFIED"}, {"id": "TEST-011", "area": "TEST", "sev": "high", "item": "tests/integration/test_state_contracts.py pins no numbers at all \u2014 it is 17 render-smoke assertions", "detail": "LANDS: drop (replace with project contract tests). Despite its name and docstring claim to 'verify that data actually flows correctly across the multi-page workflow', every test body ends in `assert_no_exception(at, page_name)` (defined at :35-38, fails only if at.exception is non-empty). The only value assertions in the whole file are existence checks: `assert at.session_state['raw_data'] is not None` (:79), `assert profile is not None` (:93), `assert len(...feature_cols) > 0` (:82). No metric, no shape, no numeric output is compared. It therefore cannot detect a refactor that produces correct-shaped but WRONG numbers \u2014 the exact failure mode a Streamlit-to-Python port produces.", "ev": "tests/integration/test_state_contracts.py:35-38, and all 17 test bodies terminating in assert_no_exception", "act": "Do not port. Replace with Project-level contract tests asserting the produced VALUES (split index sets, feature name lists, metric dicts) at each stage boundary.", "status": "UNVERIFIED"}, {"id": "TEST-012", "area": "TEST", "sev": "high", "item": "95 of 98 integration tests are AppTest-bound and cannot survive; the 3 non-AppTest ones can", "detail": "LANDS: view (rewrite) / drop. AppTest-bound counts: test_pages.py 19, test_state_contracts.py 17, test_widget_interactions.py 14, test_upload_roster.py 13, test_import_ui.py 10, test_cascade_invalidation.py 10, test_combine_ui.py 6, test_wide_guards.py 4, test_lockbox_split.py 2. All construct `AppTest.from_file(\"pages/NN_*.py\")`, which requires both Streamlit AND the pages/ directory that is being deleted. tests/integration/test_pdf_pipeline.py is the exception \u2014 it is streamlit-transitive but not AppTest, so it can be salvaged. Verdict: tests/integration/ as a directory does not survive. Roughly 10% of its content (the behavioral assertions, not the smoke) is worth re-expressing at the Pro\u2026", "ev": "grep AppTest over tests/integration; Makefile:27 `test-apptest` target; .github/workflows/ci.yml Tier 2 step", "act": "Rewrite as Feed/Router tests driving the Project object directly. Budget for ~10 real tests, not 95 \u2014 most of the 95 are duplicate render-smoke across pages.", "status": "UNVERIFIED"}, {"id": "TEST-013", "area": "TEST", "sev": "high", "item": "tests/integration/test_lockbox_split.py is the ONLY integration test that exercises real behavior end-to-end \u2014 it must be ported, not dropped", "detail": "LANDS: project. Unlike its neighbors it clicks the real button (`_click_button(at, \"Prepare Splits\")` at :67) and asserts a hard set equality on production output: `assert set(test_indices) == set(test_labels)` (:77) plus disjointness `assert not (train_indices & set(test_labels))` (:84-85). Its second test (:88) pins the negative case \u2014 exploratory_mode ignores the lockbox. This is the only place the structural-leakage guarantee is verified at the orchestration level; every other lockbox test is unit-level. If it is dropped with the rest of tests/integration/, the migration loses its only end-to-end leakage check.", "ev": "tests/integration/test_lockbox_split.py:45-112", "act": "Port FIRST, before the cut: re-express as a Project-level test calling the extracted prepare_splits engine function with a frozen lockbox dict, keeping the identical set-equality and disjointness assertions.", "status": "UNVERIFIED"}, {"id": "TEST-014", "area": "TEST", "sev": "high", "item": "ALL of models/* is uncovered, and models/rf.py carries literal duplicate method definitions proving nothing exercises it", "detail": "LANDS: engine. models/rf.py defines predict_proba at :75-81 and AGAIN identically at :87-93, and supports_proba at :83-85 and again at :95-97. The second definition silently wins. Behaviorally harmless today (bodies are identical) but it is direct evidence that no test ever imported the class \u2014 a copy-paste artifact that survived review. models/ is 1,000 loc across 7 files with ZERO streamlit, so it is the cheapest and safest thing to move to engine \u2014 but 'safe to move' is not 'verified to move' without golden outputs. BaseModelWrapper's contract (models/base.py:10-76) is `fit(X_train, y_train, X_val=None, y_val=None, **kwargs) -> Dict[str, Any]`, `predict(X) -> np.ndarray`, `predict_proba(X\u2026", "ev": "models/rf.py:75-97 (duplicated block); models/base.py:10-76", "act": "Write tests/characterization/test_model_wrappers_golden.py: for each of GLMWrapper, HuberGLMWrapper, RFWrapper, RegistryModelWrapper, on tests/conftest.py build_regression_df(n=200, seed=42) with numeric features only, assert (a) fit() retu\u2026", "status": "UNVERIFIED"}, {"id": "TEST-015", "area": "TEST", "sev": "high", "item": "WorkflowProvenance.from_dict silently drops renamed/unknown fields \u2014 and the round-trip test hand-picks fields so it cannot detect the loss", "detail": "LANDS: record. utils/workflow_provenance.py:585-655 filters every section through `{k: v for k, v in data[X].items() if k in XProvenance.__dataclass_fields__}`. Rename a dataclass field and old saved state silently loads the DEFAULT \u2014 no KeyError, no warning. The guarding test tests/test_workflow_provenance.py:342 asserts ~20 individually-named fields (prov2.upload.target_col, prov2.split.random_seed, ...) and never asserts `prov2.to_dict() == d`, so any field it does not name can vanish undetected. Worse, two whole sections are never round-tripped: the test never calls record_feature_engineering() or record_coach(), and never asserts prov2.feature_engineering or prov2.coach. `coach` is prec\u2026", "ev": "utils/workflow_provenance.py:585-655; tests/test_workflow_provenance.py:342-407 (no equality assert; no coach/feature_engineering)", "act": "Add `assert WorkflowProvenance.from_dict(d).to_dict() == d` over a MAXIMALLY populated provenance (all 12 sections incl. record_feature_engineering and record_coach). Add a meta-test asserting the set of keys in to_dict() equals a frozen li\u2026", "status": "UNVERIFIED"}, {"id": "TEST-016", "area": "TEST", "sev": "high", "item": "Source-text grep assertions go vacuously green the instant a file moves", "detail": "LANDS: drop. tests/test_review_fixes.py:424-426 and :292-294 define `_read(self, rel)` returning file text, then assert on substrings: `assert \"feature_options[:min(10\" not in src` and `assert \"default_features = list(feature_options)\" in src` (:433-434), reading 'pages/01_Upload_and_Audit.py'. When pages/ is deleted the negative assertions pass trivially (nothing to find) and the positive ones raise FileNotFoundError. Same pattern in tests/test_issues_7_15.py (:94, :196, :220, :242) and tests/test_distribution.py:17-18. These encode real past bugs (the wide-data first-10-features truncation) whose protection evaporates silently for the negative form.", "ev": "tests/test_review_fixes.py:424-434; tests/test_issues_7_15.py:94,196,220,242; tests/test_distribution.py:17-18", "act": "Convert each to a behavioral assertion against the extracted engine function before the move (e.g. call the default-feature-selection function with a 3000-column frame and assert len(defaults) == 3000), then delete the text-scraping form.", "status": "UNVERIFIED"}, {"id": "TEST-017", "area": "TEST", "sev": "high", "item": "RESULT_KEYS is a hand-maintained duplicate of the production key list \u2014 adding a result key gives false confidence", "detail": "LANDS: project. tests/test_review_fixes.py:141-152 hardcodes 22 keys with an inline comment dating the last manual sync ('2026-07 layer-contract audit'). The production function's docstring at utils/session_state.py:290-291 states the coupling explicitly: 'Any page that introduces a new result key must add it here' \u2014 a convention enforced by nothing. The test at :162 loops RESULT_KEYS, so a key added to production but not to RESULT_KEYS is simply never checked; a key added to neither is never cleared at all. This is the mechanism that produced the 21-key gap above.", "ev": "tests/test_review_fixes.py:141-152; utils/session_state.py:286-297", "act": "In the Project layer make result fields declarative (a dataclass or registry) so the clear-set is DERIVED, not listed. The characterization test then asserts derived-set == frozen golden set.", "status": "UNVERIFIED"}, {"id": "TEST-018", "area": "TEST", "sev": "high", "item": "OutlierCapping in MAD mode silently collapses constant columns to a single value", "detail": "LANDS: engine. ml/preprocess_operators.py:86-92: when method=='mad', `mad = np.nanmedian(np.abs(X_arr - med), axis=0)`, `scale = 1.4826 * mad`, bounds = med -/+ threshold*scale. For a constant column mad==0, so lower_bounds_ == upper_bounds_ == med, and transform's `np.clip(X_arr, self.lower_bounds_, self.upper_bounds_)` (:104) forces every value in that column to the median. Any near-constant column (mad==0 with a few outliers, common in clinical flags and one-hot-ish integer codes) is silently flattened \u2014 the feature becomes constant, contributes nothing, and no warning is emitted. Zero test coverage on this module.", "ev": "ml/preprocess_operators.py:86-92 and :100-104", "act": "Characterization test with a deliberately constant column and a near-constant column (mad==0 plus one outlier); assert current behavior explicitly so the migration cannot change it accidentally, then decide whether to guard scale==0. Note a\u2026", "status": "UNVERIFIED"}, {"id": "TEST-019", "area": "TEST", "sev": "high", "item": "ml.outliers has the widest fan-in of any uncovered ml module (6 importers) and gates EDA-to-preprocess handoff", "detail": "LANDS: engine. ml/outliers.py:12 `detect_outliers(...)` and :93 `outlier_rate(series: pd.Series, method: str = 'iqr', params: Optional[Dict] = None) -> float`. Six importers make it the highest-blast-radius zero-coverage module in ml/. outlier_rate returns a float that drives displayed rates and (via the ledger) manuscript claims about data quality; detect_outliers drives the preprocessing recommendation. A change in the iqr/mad/percentile boundary condition (inclusive vs exclusive) shifts reported rates with no test to catch it.", "ev": "ml/outliers.py:12, :93; 6 import sites", "act": "Golden test: pd.Series([1..100] + [1000, -1000]); assert outlier_rate to 10 decimals for each of method='iqr','mad','percentile' with default and explicit params, and assert detect_outliers returns the exact index positions of the two injec\u2026", "status": "UNVERIFIED"}, {"id": "TEST-020", "area": "TEST", "sev": "high", "item": "ml.stats_tests (7 public functions, 4 importers) is uncovered and its p-values are quoted verbatim in manuscripts", "detail": "LANDS: engine. ml/stats_tests.py:12 correlation_test, :44 two_sample_location_test, :69 k_sample_location_test, :91 categorical_association_test, :116 `normality_check(series) -> Tuple[float, float, str]`, :135 `friedman_test(block_data: np.ndarray) -> Tuple[float, float, str]`, :153 `paired_location_test(diff: np.ndarray, parametric: bool) -> Tuple[float, float, str]`. Several return a third element that is a human-readable test NAME \u2014 that string is what ends up printed in the methods section, so renaming it changes the manuscript's claim about which test was run. pages/09_Hypothesis_Testing.py is the main consumer and carries 6 of the 47 silent-swallow blocks.", "ev": "ml/stats_tests.py:12-153 (7 functions); 4 import sites; pages/09_Hypothesis_Testing.py", "act": "Golden test with fixed seeds pinning BOTH the numeric statistic/p-value (10 dp) AND the returned test-name string for each function, including the parametric/non-parametric branch of each (normality_check driving the switch). The name strin\u2026", "status": "UNVERIFIED"}, {"id": "TEST-021", "area": "TEST", "sev": "high", "item": "All three import cycles are held together only by function-local deferred imports; hoisting any one to module level is an instant startup ImportError", "detail": "LANDS: engine/record/project boundary. Every edge is a deferred import inside a function body: ml/publication.py:98,155,1739,1829 import from utils.insight_ledger, while utils/insight_ledger.py:1077 does `from ml.publication import TRIPOD_ITEMS` inside get_tripod_status(). utils/cohorts.py:474 `from utils.test_lockbox import get_lockbox` vs utils/test_lockbox.py:269 `from utils.cohorts import active_cohort`. utils/session_state.py:216,278,428 import utils.cohorts while utils/test_lockbox.py:228 imports `reset_downstream_results` from utils.session_state. A refactor that tidies imports to the top of the file \u2014 the single most natural cleanup during a move \u2014 converts all three into hard circul\u2026", "ev": "ml/publication.py:98,155,1739,1829 / utils/insight_ledger.py:1077; utils/cohorts.py:474 / utils/test_lockbox.py:269; utils/session_state.py:216,278,428 / utils/test_lockbox.py:228", "act": "Break the cycles BEFORE moving files: TRIPOD_ITEMS is pure data \u2014 lift it to a standalone module both sides import. Move reset_downstream_results' invocation out of test_lockbox via a callback/event so Project owns invalidation. Add an impo\u2026", "status": "UNVERIFIED"}, {"id": "TEST-022", "area": "TEST", "sev": "high", "item": "perform_cross_validation's leakage semantics are documented in prose but only partially tested \u2014 and CV strategy is a cleared-but-unasserted key", "detail": "LANDS: engine + project. ml/eval.py:97 `perform_cross_validation(model, X, y, cv_folds=5, task_type='regression', scoring=None, cv_strategy='standard', groups=None) -> Dict[str, np.ndarray]` implements three branches with explicit leakage rationale (:107-119): 'group' -> StratifiedGroupKFold/GroupKFold, 'time' -> TimeSeriesSplit (no shuffle), else StratifiedKFold/KFold with random_state=42. It returns {'scores','mean','std','folds','strategy'}. tests/test_cv_strategies.py exists (86 loc) but cv_strategy and cv_groups_train are both in the 21-key unasserted-clear list \u2014 so the fold scheme can go stale relative to the split after a data change with nothing detecting it. The fallback at :143-14\u2026", "ev": "ml/eval.py:97-171; silent downgrade at :143-149; cv_strategy/cv_groups_train absent from all three test key-lists", "act": "Golden test asserting the returned 'strategy' and 'folds' for each branch, plus the concrete fold membership (list of test-index arrays) for a fixed 60-row frame with 12 groups \u2014 fold MEMBERSHIP is what leakage depends on, and it is what a \u2026", "status": "UNVERIFIED"}, {"id": "TEST-023", "area": "TEST", "sev": "high", "item": "Insight is a mutable dataclass with normalization in __post_init__ that from_dict must re-trigger \u2014 a silent record-corruption path", "detail": "LANDS: record. utils/insight_ledger.py:523 `class Insight` with `__post_init__` at :602 performing vocabulary normalization (tests/test_review_fixes.py:395-398 shows model_scope ['Boosting','Neural Net'] normalizing to ['tree','neural'], and the test must call `ins.__post_init__()` MANUALLY to trigger it after mutation). to_dict at :617, from_dict at :647, to_list at :1342, from_list at :1347. If from_dict bypasses or a future refactor changes construction so __post_init__ does not run, un-normalized family strings enter the ledger and silently mismatch every get_for_models() lookup \u2014 insights simply stop appearing, with no error. Round-trip is covered only at tests/test_unified_ledger_integ\u2026", "ev": "utils/insight_ledger.py:523,602,617,647,1342,1347; tests/test_review_fixes.py:395-398 (manual __post_init__ call)", "act": "Characterization test: construct an Insight with DENORMALIZED model_scope and un-normalized fields, round-trip through to_dict/from_dict and to_list/from_list, and assert the normalized form survives both directions. Then assert ledger.get_\u2026", "status": "UNVERIFIED"}, {"id": "TEST-024", "area": "TEST", "sev": "high", "item": "Only 3 of 12 pages' state contracts involve any value assertion; page-to-page handoff shape is effectively unspecified", "detail": "LANDS: project. The keys crossing page boundaries are enumerated only inside test fixtures (tests/integration/conftest.py:48-123 inject_data_state/inject_trained_state) and inside the cascade key-lists. There is no single declaration of what a stage PRODUCES vs CONSUMES. inject_trained_state at :79 sets 12 keys; _inject_full_downstream_state at tests/integration/test_cascade_invalidation.py:57 sets 25. The divergence between the two 'complete pipeline' fixtures is itself evidence that no canonical contract exists. Building Project without first extracting this contract means guessing.", "ev": "tests/integration/conftest.py:48-123 (12 keys) vs tests/integration/test_cascade_invalidation.py:57-121 (25 keys)", "act": "Derive the produces/consumes table from the two fixtures plus the 65-key clear list BEFORE writing Project, and encode it as a stage-contract test: for each stage, assert the exact set of Project fields that are non-None after it runs, and \u2026", "status": "UNVERIFIED"}, {"id": "TEST-025", "area": "TEST", "sev": "high", "item": "CI runs only two pytest invocations and has no coverage gate \u2014 a migration can delete tests without turning CI red", "detail": "LANDS: jobs/infra. .github/workflows/ci.yml runs exactly `python -m pytest tests/ --ignore=tests/integration -q --timeout=60` then `python -m pytest tests/integration -q --timeout=120`, followed by a deploy job gated only on `needs: test`. There is no --cov, no minimum test count, no coverage delta check. During a migration that deletes ~40 test modules, CI stays green throughout and auto-deploys to the self-hosted runner on every push to main. Combined with the vacuous-green patterns above, the safety net can quietly reach zero while reporting success.", "ev": ".github/workflows/ci.yml Tier 1/Tier 2 steps and the `deploy: needs: test` gate", "act": "Before the first file moves: add a collected-test-count floor (assert >= N collected) and a coverage gate on engine/ + project/, and disable auto-deploy on main for the duration of the migration.", "status": "UNVERIFIED"}, {"id": "COACH-022", "area": "COACH", "sev": "invariant", "item": "The evidence probe must only ever see TRAINING rows. It never touches the held-out test set.", "detail": "run_probe moves into Jobs and is invoked from a job spec that hands it the whole frame \u2014 which is the natural thing to do, since the function signature `run_probe(X_train, y_train, task_type)` merely NAMES its arguments X_train/y_train and validates nothing. Every probe number then silently becomes contaminated, and because probe verdicts flow into CoachProvenance and from there into narrative_engine's Methods draft, the leakage reaches the manuscript.", "ev": "NOT enforced inside ml/coach_probe.py \u2014 its docstring (lines 19-21) states 'the probe must ONLY ever see training rows (callers apply the lockbox mask)'. The single enforcement is at pages/05_Preprocess.py:224: `_pm = df[target_col].notna()\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "COACH-023", "area": "COACH", "sev": "invariant", "item": "Probe numbers are advisory diagnostics, never reportable results.", "detail": "A Router surfaces probe.linear_score / probe.tree_score as numbers in a results table or a metric tile. Nothing in ProbeResult marks the fields as non-reportable \u2014 the discipline lives entirely in prose.", "ev": "ml/coach_probe.py:20-23 (contract docstring); pages/05_Preprocess.py:269-274 renders the caption '_Advisory only \u2014 computed on training rows, never a reportable result._'; ml/narrative_engine.py:947-951 emits 'screen scores were advisory an\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "COACH-024", "area": "COACH", "sev": "invariant", "item": "Blockers are never bulk-acknowledged: passing a workflow gate is not evidence the user reviewed the worst findings.", "detail": "The Router reimplements gating with its own sweep loop, or calls a generalized `acknowledge_all(...)`. The consequence is fabricated 'accepted risk' provenance for target leakage and insufficient-sample findings \u2014 the exact class of confidently-wrong assertion the governing rule forbids.", "ev": "utils/insight_ledger.py:789-794 \u2014 the explicit `if i.severity == 'blocker': continue` inside auto_acknowledge_gate, with a four-line in-code justification.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "COACH-025", "area": "COACH", "sev": "invariant", "item": "The Methods draft may cite the coach's rationale only when the trained lineup actually overlaps the shortlist.", "detail": "The Router auto-runs its own picks. Overlap then becomes tautological, `_followed` is always True, and the Methods section claims a rationale the user never adopted. Note pages/05_Preprocess.py:290-294 ALREADY partially defeats this by auto-checking the picks.", "ev": "ml/narrative_engine.py:933-936 \u2014 `_pick_keys = {p.get('model_key') for p in coach_picks\u2026}; _followed = bool(_pick_keys & set(models))`, then both the headline sentence (937) and the probe sentence (946) are gated on `_followed`.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "COACH-026", "area": "COACH", "sev": "invariant", "item": "An insight's identity is its `id` string, and nothing else.", "detail": "(a) Two producers pick the same id \u2014 the second silently overwrites the first, and upsert's field-preservation makes the result a chimera. (b) The scanner's shape assumptions change: it keys on the constructor name ENDING in 'Insight', on the variable name `_ACTION_TO_INSIGHT_MAP`, and on the receiver expression containing the substring 'ledger'. Rename the map, alias the ledger as something without 'ledger' in it, or build Insights via a factory function, and the guard goes quiet without failing.", "ev": "utils/insight_ledger.py:670-675 (`add`) and 677-707 (`upsert`) both scan `i.id == insight.id`. Cross-file consistency is enforced only by tests/test_insight_id_integrity.py, an AST scan that collects `*Insight(id=\u2026)` constructor kwargs plus\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "COACH-027", "area": "COACH", "sev": "invariant", "item": "model_scope must use the canonical family vocabulary ('linear','tree','neural','distance','margin','prob'), never display groups.", "detail": "A Router constructs scoped questions from TopPick.group (which is the string 'Trees/Boosting' at ml/model_coach.py:540 \u2014 not in FAMILY_ALIASES) or from ml/model_coach.GROUP_DISPLAY_NAMES. Normalization yields 'trees/boosting', matches no family, and utils/coaching_ui.py:170-173 discards the insight with no trace.", "ev": "utils/insight_ledger.py:609-615 \u2014 Insight.__post_init__ lowercases and maps every scope entry through FAMILY_ALIASES ('trees'->tree, 'boosting'->tree, 'neural net'->neural, \u2026); and ml/model_coach.py:1076-1082, where _detect_overfit delibera\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "COACH-028", "area": "COACH", "sev": "invariant", "item": "Every model key in ml/model_registry.get_registry() has a viability verdict.", "detail": "A model is added to the registry without adding a branch to model_viability. The test catches it \u2014 this is the healthiest invariant in the domain and must survive the split intact.", "ev": "tests/test_coach_intelligence.py::TestModelViability::test_covers_full_registry \u2014 `missing = registry_keys - set(v.keys()); assert not missing`.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "COACH-029", "area": "COACH", "sev": "invariant", "item": "The probe is seeded and reproducible.", "detail": "Jobs parallelizes the probe legs, or run_probe is handed a differently-ordered dataframe (feature selection by variance uses nlargest, whose ties break on column order). The test only compares linear_score, so tree_score / permuted_score drift would pass.", "ev": "ml/coach_probe.py SEED=42 threaded through np.random.default_rng, KFold/StratifiedKFold(random_state=SEED), LassoCV(random_state=SEED) and HistGradientBoosting*(random_state=SEED); asserted by tests/test_coach_intelligence.py::test_seeded_a\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "COACH-030", "area": "COACH", "sev": "invariant", "item": "A resolution claim must not outlive the results that earned it.", "detail": "Both calls are guarded by `if hasattr(ledger, 'rollback_resolutions')` / `hasattr(ledger, 'prune_auto_generated')`. A Record class that renames or splits either method makes invalidation a NO-OP with no error, and the manuscript keeps asserting a target transform / imputation config / training run that no longer exists.", "ev": "utils/session_state.py:396-411 \u2192 InsightLedger.rollback_resolutions({'05_Preprocess','06_Train_and_Compare','07_Explainability','08_Sensitivity_Analysis','09_Hypothesis_Testing'} plus {'03','04'} when clear_feature_selection) and prune_auto\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "COACH-031", "area": "COACH", "sev": "invariant", "item": "Coaching voice must never reach the manuscript verbatim.", "detail": "A producer omits manuscript_text and its finding survives the regex. ALL FOUR coach preprocessing insights omit it (ml/model_coach.py:673-799), and their findings contain '(s)' pluralization the rest of the codebase avoids via pages/02_EDA.py:194 `_count_word`. Verified output: 'Your linear models (Ridge Regression, LASSO, etc.) are scale-sensitive; the app's default pipeline already standardizes features for them.' lands in the manuscript LIMITATIONS list.", "ev": "utils/insight_ledger.py:1188 \u2014 `text = (i.manuscript_text or '').strip() or _clean_for_manuscript(i.finding)`, with _clean_for_manuscript (lines 388-445) stripping coaching parentheticals, bracket tags and stock phrases and rewriting model \u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "COACH-032", "area": "COACH", "sev": "invariant", "item": "'high' confidence is the only tier the UI pre-selects, so 'high' means the app is asserting (docs/FINDINGS_LEDGER.md, Governing rule, lines 20-27).", "detail": "THREE ways it is already weaker than stated. (1) utils/combine_ui.py:266-273 hands the non-low candidates to an st.selectbox, which pre-selects index 0 \u2014 so a 'medium'-confidence join key IS pre-selected. (2) TaskTypeDetection.final (utils/session_state.py:22-27) returns `detected` regardless of confidence, so a 'low'-confidence task type propagates into DataConfig, the lockbox and the coach with full authority. (3) THE COACH HAS NO CONFIDENCE TIER AT ALL, yet pages/05_Preprocess.py:290-294 pre-selects its picks unconditionally. Promoting the coach to Router without giving it a tier promotes an unhedged assertion to the control plane.", "ev": "PARTIALLY, and only in the import/join domain: ml/join_doctor.py:922 `if include_low or c.confidence != 'low'` in suggest_best; utils/combine_ui.py:262 `usable = [c for c in cands if c.confidence != 'low']`; utils/import_ui.py auto-applies \u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "COACH-033", "area": "COACH", "sev": "invariant", "item": "ml/ is engine code: pure, importable, streamlit-free.", "detail": "Already broken at CALL time, not import time. Verified: `import ml.model_coach` leaves streamlit unimported, but `ml.model_coach.generate_preprocessing_insights(['ridge'], profile)` raises ModuleNotFoundError: No module named 'streamlit' via ml/model_coach.py:634 \u2192 utils/insight_ledger.py:46. Same for _detect_overfit via ml/model_coach.py:1080. Any headless Engine/Jobs process without streamlit installed loses BOTH the preprocessing coaching and the ENTIRE post-training diagnostics batch (run_post_training_diagnostics has no internal try/except, so _detect_overfit raising discards the six other detectors' findings too).", "ev": "Convention plus the structural measurement (only eda_actions, macro_shape and publication touch streamlit). tests/test_model_scoped_coaching.py:12-13 works around it with `sys.modules['streamlit'] = MagicMock()` and lines 18-22 COPY the fam\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "MODELS-015", "area": "MODELS", "sev": "invariant", "item": "Preprocessing is fit on training rows only, and cross-validation re-fits it inside every fold \u2014 a pre-transformed matrix must never be scored directly.", "detail": "Anyone 'optimizes' CV by passing X_train_model (already transformed) to perform_cross_validation. Scores stay plausible and improve slightly \u2014 the leak is invisible.", "ev": "ml/eval.py:182 make_cv_pipeline() composes Pipeline([('prep', clone(preprocessing)), ('densify', ...), ('est', estimator)]); pages/06:1555 wraps every CV call in it; pages/06:1312 fits the pipeline on X_train before transforming val/test. T\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "MODELS-016", "area": "MODELS", "sev": "invariant", "item": "Metrics are reported on the ORIGINAL target scale: when target_transformer is active, predictions are inverse-transformed and compared against y_*_original.", "detail": "Moving evaluation into the engine without carrying target_transformer + y_*_original. Every metric silently becomes log-scale RMSE \u2014 numerically smaller, therefore looks like an improvement.", "ev": "pages/06:1486-1497 (test) and 1508-1517 (train), plus the CV branch wrapping the estimator in TransformedTargetRegressor at 1536-1550 and swapping in y_train_original. Baselines mirror it at pages/06:2113-2118 by preferring y_train_original\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "MODELS-017", "area": "MODELS", "sev": "invariant", "item": "ModelCapabilities.requires_scaled_numeric determines each model's preprocessing pipeline; a model that needs scaling must not be trained on an unscaled pipeline.", "detail": "Collapsing per-model pipelines into one shared pipeline during the rebuild. Ridge/SVR/kNN/LDA/NN silently degrade (unscaled RBF kernels and distance metrics still return numbers) while RF/boosting are unaffected \u2014 so the leaderboard reorders for a preprocessing reason that looks like a modeling result.", "ev": "pages/05_Preprocess.py:487, 801, 847, 1059 read spec.capabilities.requires_scaled_numeric to build per-model pipelines into preprocessing_pipelines_by_model; pages/06:1286 get_preprocessing_pipeline(model_name) selects it. Test: tests/workf\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "MODELS-018", "area": "MODELS", "sev": "invariant", "item": "Every model factory has the signature (task_type: str, random_state: int) -> estimator, and every model object satisfies fit/predict, with predict_proba only reachable through the runtime supports_proba().", "detail": "Gating on spec.capabilities.supports_predict_proba instead \u2014 it is task-agnostic and wrong for nn/glm/rf (see landmine 8). Also breaks if a new factory adds a third positional parameter.", "ev": "ml/model_registry.py:44 declares factory: Callable[[str, int], Any]; all 22 factories honor it. pages/06:1502, 1522, 2050, 2171, 2722, 2769 all gate on model.supports_proba() before calling predict_proba.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "MODELS-019", "area": "MODELS", "sev": "invariant", "item": "The registry dict and its ModelSpec/ModelCapabilities objects are read-only to consumers; per-model hyperparameters must be copied before mutation.", "detail": "ModelSpec is a NON-FROZEN dataclass with mutable dicts, and pages/05, /06, /07 each cache get_registry() under @st.cache_resource (process-wide, cross-session). One dropped .copy() permanently rewrites a model's defaults for every user of the process. pages/06:1449 already mutates `params['n_neighbors']` in place, which writes back into selected_model_params when the key existed.", "ev": "pages/06:1437 `params = selected_model_params.get(model_name, spec.default_params.copy())` \u2014 the .copy() is the entire enforcement. Nothing else guards it.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "MODELS-020", "area": "MODELS", "sev": "invariant", "item": "No trained artifact is persisted; a saved project must be rebuildable from raw data + config + seed alone.", "detail": "The rebuild is only sound if training IS reproducible from the seed \u2014 and it is not: NNWeightedHuberWrapper.fit seeds the GLOBAL torch/numpy RNG (order-dependent under concurrency), optuna.create_study() at pages/06:1224 takes no sampler seed at all, and GLMWrapper/RFWrapper hardcode random_state=42 regardless of the project seed.", "ev": "utils/session_manager.py:138-162 _NEVER_PERSIST drops trained_models, fitted_estimators, fitted_preprocessing_pipelines, model_results, cv_results and all splits; the save is an allow-list (_PLAIN_KEYS, line 91) so anything unlisted is drop\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "MODELS-021", "area": "MODELS", "sev": "invariant", "item": "Bootstrap CIs are deterministic given (y_true, y_pred, metric_fn, n_resamples, random_state) \u2014 the module touches no global RNG.", "detail": "Swapping RandomState for the global np.random, or for default_rng, changes every published interval. Note NNWeightedHuberWrapper.fit calls np.random.seed(random_seed) globally at line 315 \u2014 if bootstrap were ever changed to use the global RNG, its results would become dependent on whether an NN was trained first.", "ev": "ml/bootstrap.py:115 `rng = np.random.RandomState(random_state)` is the sole randomness; every public entry point threads random_state: int = 42 through. Tests: tests/test_bootstrap.py, tests/test_review_fixes.py:53-58.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "MODELS-022", "area": "MODELS", "sev": "invariant", "item": "Degenerate bootstrap resamples are DROPPED, never substituted with the point estimate, and a run with too few valid replicates returns a NaN CI rather than a narrow one.", "detail": "A well-meaning 'fill NaN with the estimate' refactor. Intervals narrow, nothing errors, and the manuscript overstates precision \u2014 exactly the failure the current code was written to prevent.", "ev": "ml/bootstrap.py:136-149 \u2014 boot_stats initialized to NaN, failures left as NaN, `valid_boot = boot_stats[np.isfinite(boot_stats)]`, and an early return when len(valid_boot) < max(50, n_resamples // 2). The reasoning is written in the source \u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "MODELS-023", "area": "MODELS", "sev": "invariant", "item": "The NN's best-epoch weights are snapshotted by CLONE, not by reference, so later optimizer steps cannot mutate the saved state.", "detail": "Reverting to a bare state_dict() copy. Early stopping then restores the LAST weights instead of the best ones \u2014 training still completes, metrics just get quietly worse.", "ev": "models/nn_whuber.py:430 and 529 \u2014 `best_model_state = {k: v.detach().clone() for k, v in self.model.state_dict().items()}`, with the reason in the comment at line 428-429.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "MODELS-024", "area": "MODELS", "sev": "invariant", "item": "Cross-validation is skipped for the neural network because its sklearn shim cannot retrain.", "detail": "Both guards match the literal string 'nn'. A registry that ever gains a second neural key ('nn_deep', 'mlp') silently routes it into cross_val_score, where SklearnCompatibleNN*.fit() is a no-op \u2014 producing CV scores from a network that already saw every fold's held-out rows. See landmine 1.", "ev": "pages/06:1529 `if use_cv and model_name != 'nn'` with the else-branch at 1565 explaining it to the user; pages/08:121 has the parallel guard for seed sensitivity.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "RECORD-020", "area": "RECORD", "sev": "invariant", "item": "Every quantitative statement in the compiled draft traces to a recorded workflow event; sections whose sources were never recorded are OMITTED, not invented, and are declared 'NOT RECORDED'.", "detail": "A _gen_* method gains a default/boilerplate sentence for a stage that was never recorded, or the evidence map is generated from a different context than the draft (already true today at pages/10_Report_Export.py:2094, which constructs NarrativeEngine WITHOUT manuscript_context while the draft at :616 is built WITH it).", "ev": "ml/narrative_engine.py NarrativeEngine.generate_evidence_map() (rows keyed off prov.get_completeness()); every _gen_* returns \"\" on missing input; tests/test_manuscript_trust.py::TestEvidenceMap::{test_full_provenance_traced,test_empty_prov\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "RECORD-021", "area": "RECORD", "sev": "invariant", "item": "Author-owned passages are marked with exactly one marker string, '[AUTHOR REQUIRED \u2014 \u2026]', and each cites the evidence it expects the author to interpret.", "detail": "The LaTeX path is used: ml/latex_report.py emits '[INVESTIGATOR: \u2026]' and '[PLACEHOLDER: \u2026]' instead, and ml/publication.py emits '[PLACEHOLDER: \u2026]'. Three conventions already coexist, and manuscript_validator's artifact check only tolerates '[PLACEHOLDER:' (with colon) while failing bare '[PLACEHOLDER]'.", "ev": "ml/narrative_engine.py _gen_discussion() (5 scaffolds); ManuscriptDraft.OWNERSHIP_PREAMBLE; ManuscriptDraft.count_author_inputs(); tests/test_manuscript_trust.py::TestAuthorScaffolds::{test_marker_standardized,test_prior_work_scaffold_cites\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "RECORD-022", "area": "RECORD", "sev": "invariant", "item": "Coaching voice never reaches the manuscript register verbatim: manuscript_text wins over finding, and stock coaching phrases are stripped.", "detail": "Insight.manuscript_text is lost \u2014 which it is, on every save/restore, because Insight.to_dict() does not serialize it (see landmines). Also breaks if a new coaching phrase is added to a producer without adding it to both _clean_for_manuscript and the validator's coaching_patterns list.", "ev": "utils/insight_ledger.py discussion_points_for_manuscript() (manuscript_text preferred) and _clean_for_manuscript(); ml/manuscript_validator.py coaching_patterns check ('no action needed', 'favorable to analysis', 'workflow-derived abstract'\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "RECORD-023", "area": "RECORD", "sev": "invariant", "item": "The software never certifies adequacy: no unconditional effectiveness claim, a negative R\u00b2 is stated plainly as below a mean-only baseline, 'strongest' is claimed only when more than one model was compared, and the Conclusions state the recorded fact then hand adequacy to the author.", "detail": "A Results/Discussion generator is rewritten to summarize metrics without the _comparative and negative-R\u00b2 branches, or the frontend renders its own 'headline' summary card from raw metrics bypassing the engine.", "ev": "ml/narrative_engine.py _gen_discussion() (_comparative flag, the r2>=0 branch, the Conclusions [AUTHOR REQUIRED] block); tests/test_manuscript_trust.py::TestDiscussionHonesty (all five)", "act": "preserve", "status": "UNVERIFIED"}, {"id": "RECORD-024", "area": "RECORD", "sev": "invariant", "item": "The coach's model-shortlist rationale is cited in Methods only when the models actually trained intersect the coach's picks.", "detail": "CoachProvenance.picks stops carrying 'model_key' (it is an untyped List[Dict[str, Any]]), or the Router starts writing picks in a different shape \u2014 _pick_keys silently becomes {None}, _followed goes False, and the TRIPOD model-selection rationale silently vanishes from every manuscript.", "ev": "ml/narrative_engine.py _gen_model_development(): _pick_keys = {p.get('model_key') \u2026}; _followed = bool(_pick_keys & set(models)); both the headline and the probe sentence are gated on _followed", "act": "preserve", "status": "UNVERIFIED"}, {"id": "RECORD-025", "area": "RECORD", "sev": "invariant", "item": "Exploratory mode is sticky and must produce an optimistic-bias limitation; the 'test set frozen at upload' guarantee may only be stated when the lockbox was actually enforced.", "detail": "The literal ledger id 'upload_test_lockbox' (created at pages/01_Upload_and_Audit.py:1089) is renamed \u2014 the guarantee sentence disappears with no error; or exploratory_used is cleared on a path that does not also discard the results computed under it.", "ev": "pages/10_Report_Export.py:469 ('exploratory_mode' OR sticky 'exploratory_used'); ml/narrative_engine.py _gen_discussion() (appends the exploratory limitation) and _gen_study_design() (lockbox sentence gated on NOT exploratory_mode AND ledge\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "RECORD-026", "area": "RECORD", "sev": "invariant", "item": "Markdown export, LaTeX export and pre-export validation must all be derived from ONE frozen manuscript_context computed once per render.", "detail": "Any consumer rebuilds its own context \u2014 which already happens three times: the 'Compile Draft from Workflow' button at :2057, generate_report's fallback at :1088, and the evidence map at :2094.", "ev": "pages/10_Report_Export.py:1802 _cached_bundle = _build_latex_export_bundle(...), reused as table1_bundle (:1858) and validation_bundle (:1951) and passed to _generate_latex (:1820)", "act": "preserve", "status": "UNVERIFIED"}, {"id": "RECORD-027", "area": "RECORD", "sev": "invariant", "item": "Exported LaTeX contains no markdown artifacts, no raw bare [PLACEHOLDER] tags, no internal model keys, no coaching phrases and no dangling punctuation or 'Table X'/'Figure X' references.", "detail": "_MODEL_NAMES gains a key containing '_' that is also an ordinary word or appears in prose, or a generator emits **bold** into a path that skips _convert_markdown_to_latex.", "ev": "ml/manuscript_validator.py checks 10-12 (artifact_patterns, internal_keys from _MODEL_NAMES, coaching_patterns, punctuation_issues); tests/test_manuscript_validator.py::test_validate_manuscript_bundle_allows_investigator_placeholders_but_re\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "RECORD-028", "area": "RECORD", "sev": "invariant", "item": "Reported population and predictor counts are identical across abstract, Methods, Table 1 and the split arithmetic: analysis_total == train_n+val_n+test_n == abstract N == Study Design N == Table 1 Overall N, and feature_counts.selected == abstract predictors == Methods predictors, with every finalized predictor present in Table 1.", "detail": "Any generated sentence is reworded so it no longer matches one of the three regex templates per quantity \u2014 the validator reads None, the check FAILs, and every download is disabled with a message that blames the data rather than the wording.", "ev": "ml/manuscript_validator.py checks 1-5, driven by the regex templates in _extract_analysis_n / _extract_final_predictor_count / _extract_table1_overall_n / _table1_contains_feature; the sentence templates in ml/narrative_engine.py _gen_study\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "RECORD-029", "area": "RECORD", "sev": "invariant", "item": "Selection-metric language matches the task type, and primary-model statements are internally consistent (never both 'selected as the primary model' and 'no manuscript-primary model was explicitly selected').", "detail": "Either generator changes the phrase 'selected as the primary model' or 'no manuscript-primary model was explicitly selected'; or a regression manuscript legitimately discusses an 'accuracy' of imputation etc. in the Model Development prose (the term check is task-global, not context-aware).", "ev": "ml/manuscript_validator.py checks 7-8, which grep the literal strings emitted by ml/narrative_engine.py _gen_model_development and ml/latex_report.py generate_latex_report; tests/test_manuscript_validator.py::{test_validate_manuscript_bundl\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "RECORD-030", "area": "RECORD", "sev": "invariant", "item": "Exports are blocked when pre-export validation fails, unless the user explicitly overrides.", "detail": "A new download path is added without the disabled= flag. Already broken for four artifacts today: tripod_checklist.csv (:1907), table1_final.csv/.tex (:1941,:1946), evidence_map.md (:2097) and flow_diagram.mmd (:2132) all download regardless of validation state.", "ev": "pages/10_Report_Export.py:1985 exports_blocked = (not validation_report.passed) and (not validation_override); applied as disabled= on the ZIP, report.md, metrics.csv, manuscript.tex, manuscript.pdf, PDF-preview and methods_section.md butto\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "RECORD-031", "area": "RECORD", "sev": "invariant", "item": "The exported package actually contains what the manuscript's Supplementary section promises reviewers: software versions, random seeds and a data hash.", "detail": "The manifest write is skipped or the hash computation raises (it is wrapped in a bare `except Exception: pass`, leaving data_sha256=None) while the LaTeX still asserts the manifest exists.", "ev": "pages/10_Report_Export.py _build_reproducibility_manifest() written into manifest.json['reproducibility'] at :2434; the promise text lives in ml/latex_report.py generate_latex_report Supplementary block", "act": "preserve", "status": "UNVERIFIED"}, {"id": "RECORD-032", "area": "RECORD", "sev": "invariant", "item": "WorkflowProvenance round-trips losslessly through to_dict/from_dict as JSON.", "detail": "A recorded value is not natively JSON-serializable \u2014 json.dumps(..., default=str) then converts it to a STRING and from_dict restores the string, changing the TYPE of the fact without any error. numpy float metrics do exactly this today.", "ev": "tests/test_workflow_provenance.py::{test_to_dict_from_dict_round_trip,test_to_dict_is_json_serializable}; utils/session_manager.py:268-276 and :544-556", "act": "preserve", "status": "UNVERIFIED"}, {"id": "RECORD-033", "area": "RECORD", "sev": "invariant", "item": "Recording provenance must never break the workflow.", "detail": "Nothing \u2014 and that is the problem. This invariant is currently implemented as total silence: a broken recorder loses the entire manuscript basis with zero user-visible signal. In TurboTab it must become 'record failures are surfaced as feed warnings', not swallowed.", "ev": "Bare `except Exception: pass` around every get_provenance().record_* call site (e.g. pages/06_Train_and_Compare.py:1646-1666)", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-057", "area": "STATE", "sev": "invariant", "item": "Every selected model has its OWN preprocessing pipeline. Two models must be able to receive genuinely different transformed matrices from the same raw data.", "detail": "AnalysisProject stores a single pipeline/spec slot, or the rebuild collapses per-model configs into one 'preprocessing' section of the interview. This is the app's stated core differentiator (ARCHITECTURE.md:29 and :215).", "ev": "tests/workflow/test_per_model_pipelines.py::TestPerModelPipelineConfigs::test_ridge_and_rf_get_different_training_data asserts `not np.allclose(X_ridge, X_rf, atol=1e-6)`; ::test_pipelines_by_model_dict_contract asserts the dict shape; page\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-058", "area": "STATE", "sev": "invariant", "item": "Preprocessing is FIT ON TRAINING ROWS ONLY and merely applied to val/test.", "detail": "Any code path calls .transform() on a pipeline object taken from preprocessing_pipelines_by_model without refitting. Those objects ARE fitted \u2014 on the full dataframe, at build time (pages/05:945 and :999). pages/05:1275 and pages/10:2285 both do exactly this.", "ev": "pages/06_Train_and_Compare.py:1312-1315 (`model_pipeline.fit(X_train)` then `.transform(X_val)` / `.transform(X_test)`); the page's own contract text at pages/05_Preprocess.py:66-72 ('The settings you configure here are not applied yet... f\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-059", "area": "STATE", "sev": "invariant", "item": "Cross-validation re-fits preprocessing inside every fold; CV never scores a pre-transformed matrix.", "detail": "clone() raises (PlausibilityGate), or the caller passes X_train_model instead of X_train. Both degrade to a warning, not an error: pages/06:1561 `except Exception as cv_error: st.warning(...). Skipping CV.`", "ev": "ml/eval.py:182 make_cv_pipeline(preprocessing, estimator) with its `clone(preprocessing)`; called at pages/06:1555 with RAW X_train (not X_train_model).", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-060", "area": "STATE", "sev": "invariant", "item": "The CV fold scheme matches the split's leakage semantics: group split -> GroupKFold/StratifiedGroupKFold, time split -> TimeSeriesSplit, else KFold/StratifiedKFold.", "detail": "cv_strategy or cv_groups_train survive an invalidation. Both ARE popped by reset_downstream_results (utils/session_state.py:330-334) \u2014 but NOT by pages/03_Feature_Engineering.py's hand-rolled cascade at 1229-1252. Save FE after preparing a group split and the next standard split silently inherits cv_strategy='group'.", "ev": "pages/06:496,507,528 set st.session_state['cv_strategy'] and ['cv_groups_train'] at split time; ml/eval.py:134-157 branches on cv_strategy; pages/06:1559-1560 passes both through.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-061", "area": "STATE", "sev": "invariant", "item": "Target-aware steps upstream of Train & Compare (feature selection, stateful FE fits, target-association views) see TRAINING ROWS ONLY.", "detail": "The active dataframe no longer carries the ORIGINAL index labels. train_row_mask returns all-False or a nonsense mask rather than raising. ml/pipeline.py:124 apply_plausibility_filter is the one function in the codebase that resets the index.", "ev": "utils/test_lockbox.py:237 train_row_mask(index); applied at pages/04:128 (`mask = df[target_col].notna() & train_row_mask(df.index)`) and pages/03:637,775,910,954 (KBinsDiscretizer, StandardScaler, PCA, UMAP fits).", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-062", "area": "STATE", "sev": "invariant", "item": "The lockbox test set is drawn once, before feature engineering/selection, and is THE test set at Train & Compare \u2014 the split only divides the remaining rows.", "detail": "Group split or time split is selected (documented, disclosed), OR fewer than 2 lockbox rows survive filtering (pages/06:411-417 falls back to a fresh random split with a warning), OR page 08's seed sensitivity pools train+val+test and re-splits (08:130-141).", "ev": "utils/test_lockbox.py module docstring (lines 4-17); pages/06:552-582 (the `elif _lockbox_applicable:` branch); pages/06:391-405 explicitly WARNS and disables the lockbox for group and time splits.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-063", "area": "STATE", "sev": "invariant", "item": "Target trimming thresholds are computed from TRAINING rows only, and test rows are never trimmed away.", "detail": "_lockbox_applicable is False (group/time/exploratory/too-few-rows) \u2014 then thresholds come from the full target distribution including whatever will become the test set.", "ev": "pages/06:420-425: `_trim_basis = y[~is_test_row] if _lockbox_applicable else y` and `trim_mask = trim_mask | pd.Series(is_test_row, index=y.index)`.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-064", "area": "STATE", "sev": "invariant", "item": "Features already transformed in Feature Engineering are not transformed again in Preprocessing.", "detail": "engineered_feature_transforms is stale (it is NOT cleared by reset_downstream_results) or misclassifies (pages/03:26 _build_transform_map keys on column-name string patterns). Also this partition is what misaligns the positional unit/plausibility arrays.", "ev": "pages/05:954-972 partitions numeric_features into numeric_features_safe (gets log/power/PCA) and numeric_features_passthrough (imputation + scaling only); ml/pipeline.py:270-287 builds the 'numeric_passthrough' ColumnTransformer branch.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-065", "area": "STATE", "sev": "invariant", "item": "A stored pipeline whose column selectors no longer match the data must self-heal loudly rather than crash the run.", "detail": "The pipeline contains a transformer that fails sklearn clone() \u2014 reconcile clones unconditionally before it knows whether anything drifted. Then EVERY training run raises, not just drifted ones. tests/test_feature_drift.py uses a plain scaler pipeline and cannot catch this.", "ev": "ml/pipeline.py:504 reconcile_pipeline_columns; tests/test_feature_drift.py:56-72 covers the drop path and the no-op path; pages/06:1298-1308 warns with the dropped names.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-066", "area": "STATE", "sev": "invariant", "item": "Every result computed from the current data is cleared when the data, target, features, or feature set changes. 'Any page that introduces a new result key must add it here.'", "detail": "A key is missed. Already missed today: preprocess_built_model_keys, preprocessing_summary, engineered_feature_transforms, filtered_data. And pages/03:1229-1252 bypasses the function entirely with a drifted hand-rolled copy.", "ev": "utils/session_state.py:283 reset_downstream_results() (its own docstring states the rule); tests/workflow/test_state_invalidation.py and tests/integration/test_cascade_invalidation.py assert specific keys are cleared.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-067", "area": "STATE", "sev": "invariant", "item": "Fitted sklearn objects are never persisted; pipelines are derived artifacts rebuilt from config.", "detail": "AnalysisProject is made serializable by pickling the sklearn objects instead of storing PreprocessSpec + SplitConfig. The existing save layer already made the right call \u2014 do not regress it for convenience.", "ev": "utils/session_manager.py:138-162 _NEVER_PERSIST explicitly lists preprocessing_pipeline, preprocessing_pipelines_by_model, fitted_preprocessing_pipelines, all splits and index lists.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-068", "area": "STATE", "sev": "invariant", "item": "sklearn custom transformers must store constructor parameters unmodified so clone() works.", "detail": "PlausibilityGate.__init__ (preprocess_operators.py:50-55) converts its lists to np.arrays; OutlierCapping.__init__ (line 79) does `self.params = params or {}`. Add a parametrized test that calls sklearn.base.clone on every transformer in the module \u2014 it is three lines and it would have caught both.", "ev": "Only by convention and one comment: ml/preprocess_operators.py:13 'Store conversion_factors by reference (no copy) so sklearn clone works.' There is NO test.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-069", "area": "STATE", "sev": "invariant", "item": "The pipeline recipe printed into the manuscript describes what the pipeline actually did.", "detail": "Any step the renderer does not have a branch for. Currently unrendered: power_transform / power_nan_guard, MinMaxScaler, TargetEncoder, OrdinalEncoder, the entire numeric_passthrough branch, and the actual outlier thresholds. Add a round-trip test: build a pipeline from a spec, render it, assert every configured option appears.", "ev": "Nothing. ml/pipeline.py:326 get_pipeline_recipe() introspects the fitted object; pages/10:1226 and :1243 paste the string into the report; pages/06:2576 shows it in the UI.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-070", "area": "STATE", "sev": "invariant", "item": "The held-out test set is drawn EXACTLY ONCE, at upload, before feature engineering, feature selection, or any target-aware EDA can see it; page 06 is the only page permitted to open it.", "detail": "Any new target-aware step forgets train_row_mask; or the lockbox is drawn after FE/FS; or the page-06 applicability rule loses one of its two documented bypass tests. Failure is silent \u2014 metrics simply get better.", "ev": "pages/01 lines 1062-1081 (ensure_lockbox after target/task are fixed); utils/test_lockbox.train_row_mask() gating in 03 (binning 637-649, TDA 775-794, PCA 908-913, UMAP 954-960), 04 (line 128), 05 (probe, line 222); pages/06 lines 393-418 d\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-071", "area": "STATE", "sev": "invariant", "item": "Group-based and time-based splits BYPASS the lockbox and must say so out loud.", "detail": "The warnings are treated as UI chrome and dropped during the view rewrite \u2014 the bypass then happens with no disclosure, and the manuscript still claims a protected holdout.", "ev": "pages/06 lines 395-404 (two \ud83d\udd13 st.warning blocks) and lines 250-254 (caption when the slider is not lockbox-governed).", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-072", "area": "STATE", "sev": "invariant", "item": "Once ANY result has been computed in exploratory mode, the manuscript is permanently watermarked until those results are cleared.", "detail": "Anyone 'simplifies' the OR to the live toggle, or clears exploratory_used outside the reset. Toggling exploratory off would then launder full-data feature selection into a clean-looking manuscript.", "ev": "pages/01 _on_exploratory_toggle (line 1111) sets exploratory_used; only utils/session_state.reset_downstream_results pops it (final lines); pages/10 _build_manuscript_context lines 469-470 reads `exploratory_mode OR exploratory_used`.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-073", "area": "STATE", "sev": "invariant", "item": "Changing the feature set, the target, OR the task type invalidates every downstream result.", "detail": "The hash drops the target or task component (the comment at 1043-1046 records that exact historical bug: a target swap kept the previous outcome's models, metrics, and SHAP alive under the new outcome's name).", "ev": "pages/01 lines 1042-1060: md5 over sorted(feature_cols) + '|target=' + target + '|task=' + task_type \u2192 reset_downstream_results(clear_feature_engineering=False).", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-074", "area": "STATE", "sev": "invariant", "item": "reset_downstream_results is the SINGLE source of truth for invalidation; every new result key must be registered there.", "detail": "Already partially broken: pages/03 lines 1229-1250 hand-rolls a 22-key competing cascade that omits the ledger rollback, the provenance sections, coach_probe_result, and exploratory_used. Any second copy drifts.", "ev": "utils/session_state.reset_downstream_results docstring and body (pipelines, splits, targets, models, 11 analysis keys, report artifacts, coach probe, provenance sections, ledger rollback+prune, exploratory_used).", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-075", "area": "STATE", "sev": "invariant", "item": "All reported metrics are on the ORIGINAL target scale, even when models train on a transformed target.", "detail": "A fifth consumer of predictions forgets to invert. Nothing type-checks the scale \u2014 the number just changes.", "ev": "pages/06 lines 1485-1497 (test) and 1506-1517 (train) back-transform predictions and evaluate against y_*_original; lines 2109-2118 force baselines onto the original scale; pages/08 lines 178-187 and 400-405 repeat it; a banner at 06:760-77\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-076", "area": "STATE", "sev": "invariant", "item": "Preprocessing is fit on training rows only, and cross-validation re-fits it inside each fold.", "detail": "CV is handed the pre-transformed matrix \u2014 the comment at 06:1551-1554 records this exact prior bug, which optimistically biased every CV score. NOTE pages/05 lines 945 and 999 fit the preview pipeline on X_sample = df[all_features] (ALL rows, including sealed test rows) \u2014 that fitted object is a preview only and must never be reused for training.", "ev": "pages/06 line 1312 (model_pipeline.fit(X_train)) and lines 1551-1555 (ml.eval.make_cv_pipeline wraps preprocessing + estimator so CV re-fits per fold); pages/08 lines 161-166 and 448-452 clone-and-refit per seed/ablation.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-077", "area": "STATE", "sev": "invariant", "item": "Model coefficients are labelled with POST-transform feature names, or omitted entirely.", "detail": "A caller passes raw column names of the same length as the coefficient vector \u2014 then PCA components get labelled with clinical variable names and no check fires.", "ev": "pages/10 extract_model_coefficients returns None when len(feature_names) != len(coef) (lines 220-221); the caller passes feature_names_by_model[model_key] and prints an explicit '_Coefficient table omitted\u2026mislabeled coefficients are worse \u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-078", "area": "STATE", "sev": "invariant", "item": "Applying a feature selection preserves the selection and its record while destroying everything built on the previous feature set.", "detail": "Either flag defaults back to True \u2014 the page then erases the result it just produced, or silently keeps stale pipelines that name dropped columns.", "ev": "pages/04 lines 447-448 and 494-495: reset_downstream_results(clear_feature_engineering=False, clear_feature_selection=False).", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-079", "area": "STATE", "sev": "invariant", "item": "Insight IDs are a cross-page API; resolution depends on exact string equality or prefix.", "detail": "An id is renamed, or a cluster id changes because its first three sorted members changed (02:304), or a page resolves an id that no page emits. All failures are silent no-ops.", "ev": "pages/02 emits 13 fixed ids and maps 5 of them in _ACTION_TO_INSIGHT_MAP (line 1418); pages/03 resolves 'eda_skew_group' (line 493); pages/05 resolves eda_missing_severe, eda_missing_moderate, eda_sufficiency_* and eda_skew_group (lines 113\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-080", "area": "STATE", "sev": "invariant", "item": "No manuscript leaves the app while pre-export validation fails, unless the user explicitly overrides.", "detail": "A new download button is added without the disabled flag. Two already slip through by design \u2014 the TRIPOD checklist (line 1907) and the Evidence Map (line 2097).", "ev": "pages/10 lines 1958-1987: validate_manuscript_bundle \u2192 exports_blocked; every st.download_button passes disabled=exports_blocked (ZIP, report.md, metrics.csv, .tex, .pdf, methods_section.md).", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-081", "area": "STATE", "sev": "invariant", "item": "test_indices / train_indices / val_indices are POSITIONAL offsets into whatever get_data() returned at split time.", "detail": "get_data()'s precedence result changes after the split \u2014 and it can: df_engineered > filtered_data > raw_data, then cohort filtering (utils/session_state.get_data lines 194-219). pages/05 sets filtered_data whenever any model enables plausibility 'filter' mode (line 863). Row identity is then silently scrambled.", "ev": "pages/06 lines 681-693 store original_indices[...] .tolist(); pages/07 lines 185-196 and 1196 and pages/10 consume them via df_raw.iloc[test_indices].", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-082", "area": "STATE", "sev": "invariant", "item": "Page 01 works on the WHOLE study; every other page works on the cohort-filtered view.", "detail": "Page 01 loses the full_study flag \u2014 the lockbox then covers only the active cohort and the two runs are no longer the same question asked of different people.", "ev": "pages/01 line 619 calls get_data(full_study=True) with a comment explaining why (the audit describes the data, the lockbox spans all groups, and target/features are fixed across runs); all other pages call plain get_data().", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-083", "area": "STATE", "sev": "invariant", "item": "The held-out test rows are frozen ONCE, at the moment the modeling problem is defined, and every target-aware step upstream of training sees only the complement.", "detail": "Any new target-aware step is added without a train_row_mask() call, or train_row_mask is rewritten to read the lockbox from a default-None argument \u2014 a missing lockbox and 'everything is training data' are the same return value today (pd.Series(True, index=index)).", "ev": "utils/test_lockbox.py ensure_lockbox() called from pages/01_Upload_and_Audit.py:1074 right after the config save; train_row_mask() consumed at pages/03:637,775,910,954 (FE fits), pages/04:126-128 (selection), pages/05:220-222 (coach probe),\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-084", "area": "STATE", "sev": "invariant", "item": "The lockbox stores index LABELS, not positions, so membership survives feature engineering, row filtering and column subsetting.", "detail": "A frame is round-tripped through parquet with index=False (utils/persistence.save_dataframe, session_manager._df_to_parquet_bytes both do this), or labels are coerced to str on save (session_manager._plain_label), or downstream code stores positions instead (pages/06:683-693 already does).", "ev": "utils/test_lockbox.py module docstring lines 16-18; ensure_lockbox stores list(test_labels) from df.index; train_row_mask does `lbl not in test_set` over the passed index; utils/session_state.set_data:249-253 resets a non-unique index BEFOR\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-085", "area": "STATE", "sev": "invariant", "item": "When two runs analyze different cohorts, the lockbox is drawn BEFORE the filter, so every cohort inherits its slice of one split.", "detail": "ensure_lockbox is ever called with a cohort-filtered frame (i.e. get_data() without full_study=True), or the cohort is started before the config save.", "ev": "utils/cohorts.py module docstring invariant #1; pages/01 draws it from get_data(full_study=True); utils/session_state.get_data applies the cohort AFTER the precedence chain; cohorts.record_run computes n_test as |cohort \u2229 lockbox|. Test: te\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-086", "area": "STATE", "sev": "invariant", "item": "A cohort switch throws away every model, split, pipeline and figure \u2014 decisions replay, fits do not.", "detail": "A new cohort entry point is added that calls start_cohort() without the reset \u2014 carrying a scaler or imputer across cohorts is leakage that produces an irreproducible result.", "ev": "utils/cohorts.py docstring invariant #2; utils/cohort_ui.py:_switch_to and :_advance_to both call reset_downstream_results(clear_feature_engineering=True) immediately after start_cohort/clear_cohort.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-087", "area": "STATE", "sev": "invariant", "item": "reset_downstream_results is the SINGLE source of truth for downstream invalidation; any page that introduces a new result key must add it there.", "detail": "ALREADY BROKEN in two places: pages/03_Feature_Engineering.py:1229-1250 hand-rolls its own 21-key cascade instead of calling the resetter, and pdp_results / sensitivity_dropout_results / bland_altman_results / preprocessing_summary / manuscript_export_context are never listed anywhere.", "ev": "utils/session_state.py:288-291 docstring; tests/test_review_fixes.py::TestDownstreamReset::test_reset_clears_every_result_key over a 22-key RESULT_KEYS list; tests/test_feature_drift.py.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-088", "area": "STATE", "sev": "invariant", "item": "Exploratory mode is explicit, never enabled by default, and every result produced under it is watermarked in the manuscript.", "detail": "reset_downstream_results pops exploratory_used while clear_feature_engineering=False leaves df_engineered alive \u2014 the toggle-off path does exactly this (landmine below). Also breaks if exploratory_mode survives a dataset change (it does).", "ev": "utils/test_lockbox.is_exploratory():32 (`Never enabled by default`); pages/01:1116-1119 _on_exploratory_toggle resets downstream in BOTH directions and sets exploratory_used on the way on; pages/01:1140-1143 re-arms the flag for a restored \u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-089", "area": "STATE", "sev": "invariant", "item": "A rebuild that REPLACES an existing lockbox invalidates downstream results, and the redraw is disclosed rather than silent.", "detail": "The comparison is on LABEL LISTS, so a redraw that produces the same labels in a different ORDER would fire a spurious reset; and a redraw fired from anywhere other than page 01 leaves _lockbox_redrawn set forever with no consumer.", "ev": "utils/test_lockbox.ensure_lockbox:226-231 \u2014 `if existing is not None and existing.get('labels') != lockbox['labels']` \u2192 reset_downstream_results(clear_feature_engineering=False) + _lockbox_redrawn=True; pages/01:1166 pops and renders the ba\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-090", "area": "STATE", "sev": "invariant", "item": "Repeated measures split by SUBJECT, not by row \u2014 the same person must never appear in both training and the sealed test set.", "detail": "detect_repeated_subjects is run against the ENGINEERED frame (it is \u2014 page 01 passes get_data(full_study=True)) and picks up a generated column, or the name-substring heuristic is 'improved' without re-running the four pinning tests.", "ev": "utils/test_lockbox.detect_repeated_subjects + ensure_lockbox:130-133 (auto-detect only when the caller named no group_col) and :176-191 (GroupShuffleSplit when nunique >= 8). A declared cohort_structure_detection.entity_id_final always wins\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-091", "area": "STATE", "sev": "invariant", "item": "Stratification is decided AFTER the grouped attempt resolves, so a group column with too few subjects still yields a stratified split.", "detail": "The function is refactored into 'decide the split strategy, then execute' \u2014 the natural cleanup reintroduces exactly the silent failure the comment describes.", "ev": "utils/test_lockbox.py:193-210 with an explicit six-line comment naming the bug this ordering fixed. Test: tests/test_cohorts.py::test_stratification_survives_a_grouped_split_falling_back asserts lb['group_col'] is None AND lb['strata'] == [\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-092", "area": "STATE", "sev": "invariant", "item": "Session files never carry executable content: no pickle, joblib, dill or cloudpickle is ever loaded from user input; trained models and pipelines are regenerated, not deserialized.", "detail": "The AnalysisProject artifact store is made loadable from a user-supplied project file. Artifacts must be server-side, written only by Jobs workers, never uploaded.", "ev": "utils/session_manager.py module docstring lines 4-16; _NEVER_PERSIST (all fitted objects); _looks_like_pickle() magic-byte rejection; _validate_zip() path-traversal + 64-member + 500MB caps; schema_version allowlist {'2.0','2.1'}. Tests: te\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-093", "area": "STATE", "sev": "invariant", "item": "A workflow gate can never auto-acknowledge a BLOCKER \u2014 passing a gate is not evidence the user reviewed the worst findings.", "detail": "The Router is given blanket 'advance past this question' authority without re-implementing the blocker carve-out.", "ev": "utils/insight_ledger.InsightLedger.auto_acknowledge_gate:789-794 with an explicit comment. Test: tests/test_review_fixes.py::test_gate_never_acknowledges_blockers.", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-094", "area": "STATE", "sev": "invariant", "item": "An invalidated resolution is rolled back (the finding survives, the claim does not), and an auto-generated insight whose producer will re-detect is deleted outright \u2014 absent is better than false.", "detail": "prune_auto_generated's source_pages set is not renamed in lockstep when the 10 page ids become stage ids \u2014 a set that matches nothing prunes nothing and the manuscript keeps asserting deleted work.", "ev": "utils/session_state.py:389-410 + InsightLedger.rollback_resolutions / prune_auto_generated. Tests: tests/test_review_fixes.py::test_downstream_reset_rolls_back_resolutions, ::test_rollback_keeps_finding_drops_resolution, ::test_reset_prunes\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-095", "area": "STATE", "sev": "invariant", "item": "set_data distinguishes three cases: schema change (full reset), same-schema content change (results cleared, config kept), identical content (no-op) \u2014 because page 01 re-sets the same working table on every visit.", "detail": "The fingerprint returns None (unhashable cells) or collides \u2014 both degrade to 'no reset' silently.", "ev": "utils/session_state.set_data:266-280 with the _raw_data_fingerprint comparison. Tests: tests/test_review_fixes.py::test_same_schema_content_change_resets_results, ::test_identical_content_rerun_is_noop; tests/test_cohort_runs.py::test_re_se\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "STATE-096", "area": "STATE", "sev": "invariant", "item": "apply_cohort returns NOTHING rather than everything when the run's rows cannot be identified.", "detail": "An 'obvious' cleanup returns df unchanged on failure \u2014 a full table under a heading that says 'women' is a result the researcher would publish.", "ev": "utils/cohorts.apply_cohort:441-454 (fall back to the grouping column, else set _cohort_filter_broken and return df.iloc[0:0]) + cohort_ui.render_cohort_chip's repair button. Test: tests/test_cohort_runs.py asserts len(apply_cohort(stranger)\u2026", "act": "preserve", "status": "UNVERIFIED"}, {"id": "CONTRACT-039", "area": "CONTRACT", "sev": "medium", "item": "Report Export computes n_train/n_val/n_test with len() over values that are legitimately None", "detail": "`train_n = len(st.session_state.get('X_train', []))` \u2014 init_session_state sets 'X_train': None, so .get returns None (not the default) and len(None) raises TypeError. Occurs in both _build_manuscript_context and _build_methods_section_for_export. Currently masked only because the page gates on trained_models existing; any reordering during migration exposes it.", "ev": "pages/10_Report_Export.py:395-397, :628-630; utils/session_state.py:136-141", "act": "project \u2014 Project.splits should be a single Optional[Splits] object; absent means absent, not a dict of Nones", "status": "UNVERIFIED"}, {"id": "CONTRACT-040", "area": "CONTRACT", "sev": "medium", "item": "Boundary Upload\u2192WorkingTable: datasets_registry", "detail": "`Dict[dataset_id -> pd.DataFrame]`, plus `merge_steps: List`, `last_merge_columns: List[str]`, `_combine_signature: str`, `_working_table_source_id`. Serialization: DataFrames go to parquet under datasets/entry_N.parquet with a name map in datasets/index.json; dataset_id keys are stringified on save and never converted back, so an int id restores as a str id and `single_dataset['id'] in st.session_state.datasets_registry` fails silently \u2192 'Dataset not in memory'.", "ev": "utils/session_manager.py:214-225; pages/01_Upload_and_Audit.py:541-545, :592-598, :560-567", "act": "project \u2014 typed DatasetId; restore must round-trip key types", "status": "UNVERIFIED"}, {"id": "CONTRACT-041", "area": "CONTRACT", "sev": "medium", "item": "Boundary Upload\u2192Target: raw_data + _raw_data_fingerprint", "detail": "`raw_data: pd.DataFrame` (parquet-serializable, index currently lost), `_raw_data_fingerprint: Optional[int]` from `hash((df.shape, pd.util.hash_pandas_object(df, index=False).sum()))` \u2014 returns None on unhashable cells, in which case set_data falls back to schema-only invalidation and a genuine content change is silently NOT invalidated. The fingerprint is not persisted, so the first set_data after a restore sees old_fp=None and skips the content-change branch.", "ev": "utils/session_state.py:220-230, :259-280; not in any session_manager key list", "act": "project \u2014 fingerprint is the DAG's invalidation input; it must be a first-class persisted field with an explicit 'unknown' state", "status": "UNVERIFIED"}, {"id": "CONTRACT-042", "area": "CONTRACT", "sev": "medium", "item": "Boundary Upload\u2192Target: cohort_run dict and cohort_runs_done", "detail": "cohort_run = `{column, value, label, labels: List[index-label], n_rows, n_total, position, of, order, target_col, dropped_features}`. cohort_runs_done = `List[CohortRun]` (dataclass: column, label, n_train, n_test, dropped_features, completed, metrics). apply_cohort() silently returns `df.iloc[0:0]` and sets `_cohort_filter_broken` when neither the labels nor the grouping column resolve \u2014 an EMPTY table rather than a wrong one, which is the right choice but only if the UI surfaces cohort_filter_broken().", "ev": "utils/cohorts.py:409-432, :434-455, :334-344", "act": "project (membership) + view (the broken-filter banner)", "status": "UNVERIFIED"}, {"id": "CONTRACT-043", "area": "CONTRACT", "sev": "medium", "item": "Boundary Upload\u2192EDA: data_audit", "detail": "`Dict[str, Any]` with keys 'cardinality': List[dict], 'dtypes': List[dict], 'missing': List[dict] (records), 'duplicates': int (numpy int64 \u2014 not JSON-safe without coercion), 'numeric_stats': dict-of-dicts from DataFrame.to_dict(). Fully recomputable from raw_data; listed in _NEVER_PERSIST is FALSE \u2014 it is simply absent from every taxonomy list, so it is dropped on save with no note.", "ev": "pages/01_Upload_and_Audit.py:657-743, :828; utils/session_manager.py:73-162", "act": "engine (recompute) \u2014 do not persist; but the report must stop reading it (see missing_counts finding)", "status": "UNVERIFIED"}, {"id": "CONTRACT-044", "area": "CONTRACT", "sev": "medium", "item": "Boundary EDA: dataset_profile (DatasetProfile)", "detail": "Nested dataclass tree: DatasetProfile{feature_profiles: Dict[str, FeatureProfile], target_profile: Optional[TargetProfile], warnings: List[DataWarning], data_sufficiency: DataSufficiencyLevel (an Enum), recommended/cautioned/discouraged_families: List[str], ...}. The Enum member makes plain asdict()+json fail without a converter. Currently in _NEVER_PERSIST (recomputed on page 02). Consumed by ml.model_coach.select_top_picks and model_viability, and by page 05/06's coach panels.", "ev": "ml/dataset_profile.py:109-165, :23-30 (DataSufficiencyLevel Enum); utils/session_manager.py:152; pages/02_EDA.py:166", "act": "engine (pure computation) + record (persist the derived verdicts the manuscript cites, not the whole object)", "status": "UNVERIFIED"}, {"id": "CONTRACT-045", "area": "CONTRACT", "sev": "medium", "item": "Boundary EDA: DatasetSignals is recomputed on every page-02 load and NEVER stored", "detail": "`compute_dataset_signals(df, target, task_type, cohort_type, entity_id, outlier_method, feature_cols)` -> DatasetSignals (all JSON-safe fields: lists, dicts, floats, Optional[Literal]). It is held only in a local, memoized by @st.cache_data with an explicit data_id. Every eda_action takes it as its 4th positional argument. Nothing downstream can see it \u2014 leakage_flags, collinearity_summary and physio_plausibility_flags never reach the Report except via whatever an action happened to write into the ledger.", "ev": "ml/eda_recommender.py:16-38, :57; pages/02_EDA.py:170-180", "act": "project \u2014 signals is a legitimate derived-state node in the DAG; materialize it so Router and Report can both read it", "status": "UNVERIFIED"}, {"id": "CONTRACT-046", "area": "CONTRACT", "sev": "medium", "item": "Boundary EDA\u2192FeatureEng: eda_results", "detail": "`Dict[action_id -> {'findings': List[str], 'warnings': List[str], 'figures': List[plotly/matplotlib Figure], 'stats': dict}]`. The 'figures' entries make this NOT JSON-serializable and not reliably picklable (matplotlib figures). Correctly in _NEVER_PERSIST. Page 09 also reads eda_results.", "ev": "pages/02_EDA.py:1501, :1515-1531; ml/eda_actions.py return shapes; utils/session_manager.py:150", "act": "engine returns (data, figure-spec); view renders \u2014 figures must never enter Project state", "status": "UNVERIFIED"}, {"id": "CONTRACT-047", "area": "CONTRACT", "sev": "medium", "item": "Boundary EDA\u2192Report: table1_df / table1_metadata / custom_table1_tests / table1_group,pval,smd,miss", "detail": "table1_df: pd.DataFrame; table1_metadata: dict; the four table1_* toggles are widget-keyed booleans/strings read directly by pages/10's _build_manuscript_table1. Page 10 REGENERATES its own manuscript Table 1 from _build_analysis_cohort_df() rather than using page 02's \u2014 so the Table 1 the user reviewed on page 02 and the one in the manuscript are computed from different row sets.", "ev": "pages/02_EDA.py:1718-1719; pages/10_Report_Export.py:572-597, :581-593", "act": "record \u2014 one Table 1 artifact produced once, at a defined boundary; view renders it in both places", "status": "UNVERIFIED"}, {"id": "CONTRACT-048", "area": "CONTRACT", "sev": "medium", "item": "Boundary FeatureEng: fe_work_in_progress", "detail": "`{'X_engineered': pd.DataFrame, 'engineered_features': List[str], 'engineering_log': List}` \u2014 an unsaved scratch buffer keyed by `fe_features_hash` (md5 of the sorted feature list). Not persisted, correctly. It holds a full DataFrame copy in memory alongside raw_data, working_table, df_engineered and filtered_data \u2014 up to five copies of the dataset simultaneously.", "ev": "pages/03_Feature_Engineering.py:145-156, :139-143", "act": "jobs \u2014 the interview's in-progress FE state is a draft the Jobs layer owns; do not persist the frame", "status": "UNVERIFIED"}, {"id": "CONTRACT-049", "area": "CONTRACT", "sev": "medium", "item": "Boundary FeatureEng\u2192FeatureSel: df_engineered and its five satellite keys", "detail": "df_engineered: pd.DataFrame built as `pd.concat([X_engineered, y], axis=1)` \u2014 preserves the index (good: lockbox labels survive) but moves the target to the LAST column and changes column order. Satellites: feature_engineering_applied: bool, engineered_feature_names: List[str], engineering_log: List (or Dict \u2014 record_feature_engineering defensively handles both), engineered_feature_transforms: Dict[str, str] (double-transform guard consumed by page 05 and ml/publication.py:1403), pre_fe_feature_cols: List[str] (restore point for Reset/Skip).", "ev": "pages/03_Feature_Engineering.py:1218-1227, :1266-1270; ml/publication.py:1401-1403; pages/05_Preprocess.py (engineered_feature_transforms\")", "act": "project \u2014 engineered frame is derived state; the log + transform map are Record", "status": "UNVERIFIED"}, {"id": "CONTRACT-050", "area": "CONTRACT", "sev": "medium", "item": "Boundary FeatureSel: feature_selection_results and consensus_features", "detail": "feature_selection_results: `List[FeatureSelectionResult]` (@dataclass, ml/feature_selection.py:13). consensus_features: List[str]. Both are cleared by reset_downstream_results ONLY when clear_feature_selection=True \u2014 page 04's own Apply passes clear_feature_selection=False so the selection survives the cascade it triggers. This conditional-clear flag is the most subtle part of the existing invalidation contract and must survive the port verbatim.", "ev": "pages/04_Feature_Selection.py:277-282, :447-448, :493-494; utils/session_state.py:283-298, :355-357", "act": "project \u2014 the DAG needs an explicit 'this node is the one being recomputed, do not clear it' rule rather than a boolean parameter", "status": "UNVERIFIED"}, {"id": "CONTRACT-051", "area": "CONTRACT", "sev": "medium", "item": "Boundary Preprocess\u2192Train: preprocessing_pipelines_by_model", "detail": "`Dict[model_key -> sklearn.pipeline.Pipeline]` \u2014 PICKLE-ONLY, explicitly in _NEVER_PERSIST. Rebuilt by ml.pipeline.build_preprocessing_pipeline(...23 kwargs...). The companion `preprocessing_config_by_model: Dict[model_key -> Dict]` IS JSON-safe and IS persisted, which is the correct split: recipe travels, fitted object does not. Note reconcile_pipeline_columns() exists purely to self-heal a pipeline whose ColumnTransformer names columns that no longer exist \u2014 a symptom of the missing DAG.", "ev": "utils/session_state.py:463-471; ml/pipeline.py:127-133, :504-510; utils/session_manager.py:99,140-142; pages/06_Train_and_Compare.py:1300-1315", "act": "project (config) + jobs (fitted artifact, cached by config hash)", "status": "UNVERIFIED"}, {"id": "CONTRACT-052", "area": "CONTRACT", "sev": "medium", "item": "Boundary Preprocess\u2192Train: preprocessing_summary, preprocess_built_model_keys, unit_overrides, interpretability_mode", "detail": "preprocessing_summary: nested Dict of {method,label} pairs + n_numeric/n_categorical/models_configured \u2014 JSON-safe, consumed by the Methods generator. preprocess_built_model_keys: List[str] \u2014 drives page 06's model checkbox pre-selection AND the 'no pipeline' badge. unit_overrides: Dict[str,str] read by ml/eda_actions.plausibility_check. interpretability_mode: str. preprocessing_summary is NOT in any persistence list.", "ev": "pages/05_Preprocess.py:1011, :1037-1049; pages/06_Train_and_Compare.py:808-817, :825", "act": "record (summary) + router (built_model_keys is a readiness signal, not a UI detail)", "status": "UNVERIFIED"}, {"id": "CONTRACT-053", "area": "CONTRACT", "sev": "medium", "item": "Boundary Train: target_label_encoder (LabelEncoder)", "detail": "Fitted sklearn LabelEncoder, pickle-only, in _NEVER_PERSIST. Set only when the target dtype is object/category/bool; POPPED otherwise. Page 07 re-applies it to raw y (`if label_encoder is not None and y_raw.dtype == object`) \u2014 if the encoder is stale relative to the current frame's categories, .transform raises ValueError inside a bare `except Exception: pass`, silently degrading to the X_test fallback path.", "ev": "pages/06_Train_and_Compare.py:485-491; pages/07_Explainability.py:198-205", "act": "record the class mapping (JSON: List[str] classes_) in Project; rebuild the encoder in Jobs", "status": "UNVERIFIED"}, {"id": "CONTRACT-054", "area": "CONTRACT", "sev": "medium", "item": "Boundary Train\u2192Explain: trained_models", "detail": "`Dict[model_key -> BaseModelWrapper]` (NNWeightedHuberWrapper | RFWrapper | GLMWrapper | HuberGLMWrapper | RegistryModelWrapper). Interface: fit(X_train,y_train,X_val,y_val,**kw)->Dict, predict(X)->ndarray, predict_proba(X)->Optional[ndarray], get_model()->Any, supports_proba()->bool. The NN wrapper holds a torch nn.Module \u2014 picklable only with torch installed and only on a compatible device. In _NEVER_PERSIST by design (utils/session_manager.py:11-16 documents the security rationale: no pickle is ever loaded from user bytes).", "ev": "models/base.py:10-73; models/registry_wrappers.py:9-74; models/nn_whuber.py:226-636; utils/session_manager.py:139-140", "act": "jobs \u2014 fitted models are job outputs with a content-addressed cache key; Project stores only the key + config", "status": "UNVERIFIED"}, {"id": "CONTRACT-055", "area": "CONTRACT", "sev": "medium", "item": "Boundary Train\u2192Explain: model_results", "detail": "`Dict[model_key -> {'metrics': Dict[str,float], 'train_metrics': Dict[str,float], 'history': Dict[str,List[float]], 'y_test_pred': np.ndarray, 'y_test': np.ndarray|Series, 'y_test_proba': Optional[np.ndarray], 'cv_results': Optional[Dict]}]`. Mixed: metrics/history are JSON-safe, the three y_* arrays are not. Note the comment '(y_test and y_test_pred are always on original scale)' \u2014 which holds only if the y_*_original write/pop pair stayed in sync.", "ev": "pages/06_Train_and_Compare.py:1568-1577, :1579; utils/session_state.py:499-502", "act": "record (metrics, history) + jobs (prediction vectors as a job artifact)", "status": "UNVERIFIED"}, {"id": "CONTRACT-056", "area": "CONTRACT", "sev": "medium", "item": "Boundary Train\u2192Report: bootstrap_results, baseline_results, calibration_results", "detail": "bootstrap_results: Dict[model -> Dict[metric -> BootstrapResult]] (@dataclass, ml/bootstrap.py:12). baseline_results: Dict[name -> {'description': str, 'metrics': Dict}]. calibration_results: Dict[model -> CalibrationResult] (@dataclass, ml/calibration.py:13 \u2014 holds numpy arrays for the curve). Only bootstrap_results and sensitivity_seed_results appear in _NEVER_PERSIST; baseline_results and calibration_results are in NEITHER list, so they are silently dropped on save with no 'skipped' note.", "ev": "pages/06_Train_and_Compare.py:2072, :2140, :2221-2222; ml/bootstrap.py:12-33; ml/calibration.py:13-34; utils/session_manager.py:138-162", "act": "record \u2014 CI/calibration numbers are manuscript facts; persist the scalars, recompute the curves", "status": "UNVERIFIED"}, {"id": "CONTRACT-057", "area": "CONTRACT", "sev": "medium", "item": "Boundary Explain\u2192Report: shap_results carries raw arrays plus a duplicate", "detail": "`Dict[model -> {'shap_values': ndarray(2D), 'X_eval': ndarray, 'feature_names': List[str], 'class_label': Optional[str], 'all_shap_values': <original explainer output, list-or-ndarray, possibly 3D>, 'kernel_capped': bool, 'n_eval_samples': int}]`. 'all_shap_values' duplicates the full payload for class switching \u2014 memory doubles per model. shap_matplotlib_figs: Dict of matplotlib Figures (unpicklable across processes).", "ev": "pages/07_Explainability.py:512-520, :793", "act": "jobs (arrays as artifacts) + view (figures rendered on demand, never stored)", "status": "UNVERIFIED"}, {"id": "CONTRACT-058", "area": "CONTRACT", "sev": "medium", "item": "Boundary Explain\u2192Report: permutation_importance and pdp_results", "detail": "permutation_importance: Dict[model -> {'importances_mean': ndarray, 'importances_std': ndarray, 'feature_names': List[str]}]. pdp_results: Dict[model -> {'pd_per_feature': Dict[int, sklearn Bunch], 'pd_2d': Dict[Tuple[int,int], Bunch], 'feature_indices': List[int], 'feature_names': List[str]}]. pd_2d is keyed by a TUPLE \u2014 not JSON-serializable as a dict key. Note pdp_results is written under that name while init_session_state declares 'partial_dependence' \u2014 see the dead-key finding.", "ev": "pages/07_Explainability.py:406-411, :601-606; utils/session_state.py:158-159", "act": "jobs (arrays) + record (top-k importance table, which is what the manuscript cites)", "status": "UNVERIFIED"}, {"id": "CONTRACT-059", "area": "CONTRACT", "sev": "medium", "item": "Boundary Sensitivity\u2192Report: sensitivity_seed_results / sensitivity_dropout_results / sensitivity_dropout_baseline", "detail": "Two pd.DataFrames (seed: columns seed + metric names + optional '_error'; dropout: feature, score_without, impact, optional '_error') and a float baseline. Only sensitivity_seed_results is in _NEVER_PERSIST; the dropout pair is in no list. ml/publication.py reads sensitivity_dropout_results directly from session_state at module scope (line 1382), which is one of the 32 publication\u2194streamlit couplings.", "ev": "pages/08_Sensitivity_Analysis.py:221, :499-500; ml/publication.py:1220-1222, :1381-1382; utils/session_manager.py:159", "act": "record \u2014 sensitivity verdicts are manuscript claims; pass them into publication as arguments, never via ambient state", "status": "UNVERIFIED"}, {"id": "CONTRACT-060", "area": "CONTRACT", "sev": "medium", "item": "Boundary Hypothesis Testing: hypothesis_test_results holds only the LAST test run", "detail": "Six separate `st.session_state.hypothesis_test_results = {...}` assignments (correlation, t-test, chi-square, ANOVA, etc.), each overwriting the previous. The audit trail survives only because log_methodology appends a 'Statistical Validation' entry per test, and pages/10 rebuilds the list by filtering methodology_log for that step. So the Record is authoritative and the state key is vestigial.", "ev": "pages/09_Hypothesis_Testing.py:260,441,586,707,819,915; pages/10_Report_Export.py:272-284", "act": "record \u2014 make the ledger/methodology log the only store; drop the single-slot key", "status": "UNVERIFIED"}, {"id": "CONTRACT-061", "area": "CONTRACT", "sev": "medium", "item": "Import cycle ml.publication \u2194 utils.insight_ledger is a genuine layering inversion", "detail": "utils/insight_ledger.py imports model_display_name, format_resolution_detail, models_to_families, families_display from ml.publication (lines 128-520 of publication are pure formatting helpers with no ML content), while ml.publication reads the ledger's output. The formatting helpers belong to neither: they are presentation.", "ev": "ml/publication.py:128,182,453,460; utils/insight_ledger.py (imports at module top)", "act": "view \u2014 extract the label/format helpers into a presentation module; both cycles dissolve", "status": "UNVERIFIED"}, {"id": "CONTRACT-062", "area": "CONTRACT", "sev": "medium", "item": "Cleaning actions that drop rows leave the lockbox labels stale until the next config save", "detail": "`new_df = apply_fn(df)` then `set_data(new_df, is_schema_change=False)`. set_data clears the cohort and calls reset_downstream_results on a fingerprint change, but reset_downstream_results does NOT pop test_lockbox (only reset_data_dependent_state does). ensure_lockbox re-runs later on the same page render and its signature includes a content hash, so it self-heals in practice \u2014 but only if the user reaches the Step-4 config block. Navigate away first and train_row_mask silently quarantines fewer rows than n_test claims.", "ev": "pages/01_Upload_and_Audit.py:786-796; utils/session_state.py:274-280 vs :437-439; utils/test_lockbox.py:137-141", "act": "project \u2014 lockbox validity must be a DAG edge from raw_data, not a side effect of visiting a page", "status": "UNVERIFIED"}, {"id": "MINE-032", "area": "MINE", "sev": "medium", "item": "import_doctor's duplicate-label branch swallows check failures WITHOUT the disclosure the main loop adds", "detail": "The main check loop collects failures and appends an explicit `checks_failed` ShapeFinding whose why_it_matters says 'treat a clean result here as incomplete rather than as a guarantee' (ml/import_doctor.py:800-824) \u2014 this is the fix for FINDINGS_LEDGER finding 13. But the duplicate-label early-return branch above it does `for check in (check_empty_rows_and_columns, check_footer_rows): try: out.extend(check(df)) except Exception: continue` (lines 792-796) with no such disclosure and an early `return out` at line 798.", "ev": "ml/import_doctor.py:786-798 vs 800-824; docs/FINDINGS_LEDGER.md:36, 42", "act": "engine \u2014 apply the same `failed`/`checks_failed` disclosure to the duplicate-label branch. WRONG-BUT-PLAUSIBLE OUTPUT: a file with duplicate column labels AND a footer row reports only the rename suggestion; the footer check crashed and not\u2026", "status": "UNVERIFIED"}, {"id": "MINE-033", "area": "MINE", "sev": "medium", "item": "Bare `except:` sets skewness to 0.0 \u2014 perfectly symmetric \u2014 on any failure", "detail": "`try: profile.skewness = float(valid.skew()) except: profile.skewness = 0.0` at ml/dataset_profile.py:203-206 and again at 250-253. Bare except also catches KeyboardInterrupt and SystemExit.", "ev": "ml/dataset_profile.py:203-206, 250-253", "act": "engine \u2014 use None and let consumers treat it as unknown. WRONG-BUT-PLAUSIBLE OUTPUT: skewness=0.0 is the value that means 'no transform needed'. A column whose skew computation fails is reported as perfectly symmetric, the Feature Engineeri\u2026", "status": "UNVERIFIED"}, {"id": "MINE-034", "area": "MINE", "sev": "medium", "item": "perf_cache's cached_target_correlations computes Spearman by a rank identity that differs from pairwise-deletion Spearman", "detail": "`spearman = sub.rank().corrwith(target.rank())` (utils/perf_cache.py:145). The docstring is honest about it: 'With missing values the rank-based Spearman can differ from pairwise-deletion Spearman in the 3rd decimal; the consumers threshold at coarse cutoffs (0.7, 0.08) where this is immaterial.' That justification is a statement about the CURRENT consumers.", "ev": "utils/perf_cache.py:129-146", "act": "engine \u2014 keep the fast path but tag the returned Series with the method used, so a future consumer cannot silently inherit the approximation. WRONG-BUT-PLAUSIBLE OUTPUT (borderline): if any new consumer reports these numbers rather than thr\u2026", "status": "UNVERIFIED"}, {"id": "MINE-035", "area": "MINE", "sev": "medium", "item": "cached_parse_upload keys on file bytes and options but is decorated with max_entries=8", "detail": "`@st.cache_data(show_spinner=\"Parsing uploaded file\u2026\", max_entries=8)` (utils/perf_cache.py:21). The key includes file_bytes, filename, transpose, excel_sheet and records_key \u2014 this one is correctly keyed (no leading underscores). The hazard is the opposite of macro_shape: the cache is doing real work in the ingest path and the multi-file combine flow can exceed 8 entries, causing silent re-parses (slow, not wrong) \u2014 and the cache is the ONLY thing making the upload page's repeated re-parse tolerable.", "ev": "utils/perf_cache.py:21-35; pages/01_Upload_and_Audit.py:307-313, 405-413", "act": "jobs \u2014 the ingest job replaces this cache entirely; parse once, store the frame in the Project. Listed so it is not mistaken for a correctness cache that must be preserved. WRONG-BUT-PLAUSIBLE OUTPUT: none for correctness, but dropping it w\u2026", "status": "UNVERIFIED"}, {"id": "MINE-036", "area": "MINE", "sev": "medium", "item": "utils/seed.get_global_seed reads st.session_state \u2014 the only reason utils/seed touches Streamlit", "detail": "`def get_global_seed() -> int: import streamlit as st; return st.session_state.get('random_seed', 42)` (utils/seed.py:34-37). It is imported by pages/06_Train_and_Compare.py:17 alongside set_global_seed but the page then reads session_state directly anyway (`st.session_state.get('random_seed', 42)` at lines 95, 117, 120, 263).", "ev": "utils/seed.py:34-37; pages/06_Train_and_Compare.py:17, 95, 117, 120-122, 263", "act": "project \u2014 the seed is Project state; get_global_seed becomes project.random_seed. set_global_seed (utils/seed.py:14-31) stays in jobs as a per-job initializer. Flagged because utils/seed.py is a 37-line file that looks trivially portable bu\u2026", "status": "UNVERIFIED"}, {"id": "MINE-037", "area": "MINE", "sev": "medium", "item": "utils/theme.flash uses a session_state single-slot queue that a second writer silently overwrites", "detail": "`st.session_state[\"_flash\"] = (level, message)` (utils/theme.py:926) \u2014 one slot. render_flash pops and renders it (lines 930-936). The comment explains why it exists: 'Disclosure banners rendered immediately before st.rerun() are destroyed before the user can read them' (lines 920-922).", "ev": "utils/theme.py:920-936", "act": "view \u2014 becomes a message list in the Feed. WRONG-BUT-PLAUSIBLE OUTPUT: two disclosures queued before one rerun (e.g. 'lockbox redrawn' from utils/test_lockbox.py:231 plus a cleaning-action confirmation) and only the second is ever shown. Th\u2026", "status": "UNVERIFIED"}, {"id": "MINE-038", "area": "MINE", "sev": "medium", "item": "_pending_widget_state_restore is a Streamlit-specific two-phase restore with a mutation-during-read", "detail": "`pending_widget_restore = st.session_state.get(\"_pending_widget_state_restore\", {})` then `st.session_state[\"workflow_mode\"] = pending_widget_restore.pop(\"workflow_mode_selector\")` \u2014 popping from a dict fetched by reference, then conditionally writing it back or deleting the key (utils/theme.py:719-725). The mechanism exists because 'Streamlit requires setting widget keys via _pending_widget_state_restore on the next rerun' (utils/session_manager.py:119-121).", "ev": "utils/theme.py:719-725; utils/session_manager.py:119-134, 249-258", "act": "drop \u2014 the entire deferred-widget-restore mechanism is a Streamlit workaround with no analogue in a real frontend. _SAFE_WIDGET_KEYS (utils/session_manager.py:123-132) becomes ordinary Project fields. Flagged so it is not faithfully ported:\u2026", "status": "UNVERIFIED"}, {"id": "MINE-039", "area": "MINE", "sev": "medium", "item": "GroupShuffleSplit failure silently degrades the lockbox to a non-grouped split", "detail": "`try: gss = GroupShuffleSplit(...); _, test_pos = next(gss.split(eligible, groups=groups)); test_labels = eligible[test_pos]; grouped = True except Exception: test_labels = None` then `if test_labels is None: group_col = None # too few groups to split by subject` (utils/test_lockbox.py:183-191). The subsequent stratified fallback also swallows: `except ValueError: stratify, strata_used = None, []` (lines 206-210).", "ev": "utils/test_lockbox.py:176-210, 282-294", "act": "engine \u2014 record WHY the grouped split was abandoned into the lockbox dict so the record can state it. Partially mitigated: render_lockbox_status only claims 'split by <col> so no subject appears on both sides' when lb['group_col'] is set (l\u2026", "status": "UNVERIFIED"}, {"id": "MINE-040", "area": "MINE", "sev": "medium", "item": "UnitHarmonizer and PlausibilityGate coerce everything to float and return bare ndarrays", "detail": "`X_arr = np.asarray(X, dtype=float)` in UnitHarmonizer.transform (ml/preprocess_operators.py:22), PlausibilityGate.transform (line 61), plausibility_row_mask (line 33) and OutlierCapping.fit/transform (lines 85, 101). Column names are gone after the first numeric step. plausibility_row_mask additionally truncates: `n_cols = min(X_arr.shape[1], len(lower_bounds), len(upper_bounds))` (line 34) \u2014 a short bounds list silently leaves trailing columns ungated \u2014 and NaN comparisons are False, so `keep &= X_arr[:, idx] >= float(low)` (line 40) DROPS every row with a missing value in a gated column.", "ev": "ml/preprocess_operators.py:22, 33-43, 61, 85, 101", "act": "engine \u2014 this is the mechanism behind the critical misalignment finding above; listed separately because the NaN-drop and the min() truncation are independent silent behaviors. WRONG-BUT-PLAUSIBLE OUTPUT: 'filter' plausibility mode quietly \u2026", "status": "UNVERIFIED"}, {"id": "MINE-041", "area": "MINE", "sev": "medium", "item": "ml/eval.py logs but the module is a metrics engine with no error channel", "detail": "ml/eval.py is entirely streamlit-free and pure (34 of 70 core modules are test-imported; eval is among the covered ones). Its only reporting channel for degraded results is `logging.getLogger(__name__).warning` (ml/eval.py:30-35). Classification metrics swallow ROC-AUC failures in a try that starts at line 74. No function returns a 'quality' or 'n_dropped' field.", "ev": "ml/eval.py:29-37, 74-80", "act": "engine + jobs \u2014 jobs must capture the module logger and attach records to the job result, otherwise every degradation notice in ml/ disappears. This is a cross-cutting requirement: 108 broad except blocks across ml/, models/, utils/ and dat\u2026", "status": "UNVERIFIED"}, {"id": "SWEEP-028", "area": "SWEEP", "sev": "medium", "item": "utils/theme.py:924 flash() / :929 render_flash() \u2014 a rerun-survival hack that has no meaning after Streamlit", "detail": "flash(level,message) writes session_state['_flash']; render_flash() pops and dispatches to getattr(st, level). It exists solely because 'disclosure banners rendered immediately before st.rerun() are destroyed before the user can read them' (comment at :919-922). CODE_REVIEW lists 'flash messages that survive reruns' as a shipped fix from the design-review round. In a feed there is no rerun, so the mechanism is obsolete \u2014 but the REQUIREMENT it encodes (a disclosure raised by an action must still be visible after the action completes) is not. Callers: pages/01:819, pages/03:1276.", "ev": "utils/theme.py:919-940; pages/01_Upload_and_Audit.py:819; pages/03_Feature_Engineering.py:1276", "act": "view (keep the requirement, drop the mechanism)", "status": "UNVERIFIED"}, {"id": "SWEEP-029", "area": "SWEEP", "sev": "medium", "item": "utils/perf_cache.py \u2014 4 pure computations wearing Streamlit caching; bodies are engine-ready, one carries a documented numerical approximation", "detail": "cached_parse_upload(file_bytes, filename, transpose, excel_sheet, records_key) at :21 (function-local `from data_processor import load_tabular_data` at :30 to dodge an import cycle); cached_audit_tables(df) -> Tuple[List[dict], List[dict]] at :38; cached_numeric_summary(df, cols: Tuple[str,...]) at :114; cached_target_correlations(df, target_col, cols) -> Tuple[pd.Series, pd.Series] at :129. Importers: pages/01:35, pages/02:34, plus two tests. Note cached_target_correlations' own docstring: Spearman is computed via the rank-transform identity, and 'with missing values the rank-based Spearman can differ from pairwise-deletion Spearman in the 3rd decimal; the consumers threshold at coarse cuto\u2026", "ev": "utils/perf_cache.py:21,30,38,114,129,136-141", "act": "engine (bodies); jobs (upload parse); carry the 3rd-decimal caveat forward as a test, not a comment", "status": "UNVERIFIED"}, {"id": "SWEEP-030", "area": "SWEEP", "sev": "medium", "item": "utils/theory_demos.py \u2014 869 loc, 19 pure-render Plotly demos, ZERO session_state refs; but its widget-key namespacing assumption dies in a single-scroll feed", "detail": "Every demo has the identical signature demo_X(page_context: str = 'ref', expanded: bool = False, wrapped: bool = True) -> None. DEMO_REGISTRY at :833 maps 19 keys; render_inline_demo(anchor_key, page_context, expanded, wrapped) -> bool at :856 returns False when no demo exists. Widget keys are namespaced by page only: demo_skewness uses key=f\"{page_context}_demo_skew_alpha\" (:29) and key=f\"{page_context}_skew_chart\" (:57). In a TurboTax-style single scrolling document the same demo can legitimately appear twice (inline on a coaching card via theory_anchors.py:518, and again in a reference section) \u2014 page_context no longer disambiguates and the keys collide. Second trap: render_inline_demo re\u2026", "ev": "utils/theory_demos.py:19,29,57,833,856-868; utils/theory_anchors.py:518", "act": "view (keys must namespace on instance, not page; assert DEMO_REGISTRY.keys() == THEORY_ANCHORS.keys() \u2014 19/19 today)", "status": "UNVERIFIED"}, {"id": "SWEEP-031", "area": "SWEEP", "sev": "medium", "item": "utils/column_utils.py:7 make_unique_columns \u2014 19 loc, order-dependent renaming, 4 production callers", "detail": "make_unique_columns(cols: List[Any]) -> List[str] dedupes by appending _1, _2. The FIRST occurrence keeps its bare name and later ones get suffixes, so column identity depends on input column order: reorder the source columns and 'age'/'age_1' swap which physical column they name, silently. Callers: data_processor.py:54, ml/import_doctor.py:848 and :863, pages/01:27. Relevant because docs/FINDINGS_LEDGER.md finding 13 records that duplicate column labels previously crashed two import-doctor checks and diagnose() swallowed it, showing a clean bill of health.", "ev": "utils/column_utils.py:7-19; data_processor.py:54; ml/import_doctor.py:848,863", "act": "engine", "status": "UNVERIFIED"}, {"id": "SWEEP-032", "area": "SWEEP", "sev": "medium", "item": "setup.py \u2014 stale and currently uninstallable; python_requires excludes the repo's own Python", "detail": "python_requires='>=3.8,<3.10' while .python-version pins 3.12 (and the classifiers in the same file list 3.10 and 3.11) \u2014 `pip install .` refuses outright. py_modules=['app','data_processor','models','visualizations'] treats models/ as a module though it is a package, and omits ml/, utils/, pages/ entirely. name='regression-model-trainer', url='.../glucose-mlp-interactive' \u2014 both predate the 'Tabular ML Lab' rename (QUICKSTART also carries the stale project name per CODE_REVIEW).", "ev": "setup.py:20,22 vs .python-version (3.12)", "act": "drop (replace with pyproject.toml naming the real packages)", "status": "UNVERIFIED"}, {"id": "SWEEP-033", "area": "SWEEP", "sev": "medium", "item": "WORK IN PROGRESS \u2014 RESUME.md's git instructions are stale; the branch it says is unmerged is identical to HEAD and its stated merge target no longer exists", "detail": "RESUME.md:20-22: 'Not yet merged. The intended finish is a single merge into claude/tabular-ml-lab-review-nashh7 once the outstanding findings are fixed.' Reality: `git log A..B` and `git log B..A` are both EMPTY and merge-base --all returns fbe422a \u2014 claude/all-of-us-framework-exploration-smydgz (current, checked out) and claude/tabular-ml-lab-review-nashh7-audit-fixes point at the same commit. The stated merge target claude/tabular-ml-lab-review-nashh7 exists neither locally nor on origin; there is no main branch. RESUME.md:12-16 also describes a git worktree setup ('developed in a separate git worktree so the audits could keep measuring a frozen tree') \u2014 `git worktree list` shows only the\u2026", "ev": "docs/audit/RESUME.md:9-22; `git merge-base --all` \u2192 fbe422a; `git branch -r` \u2192 only the two claude/* remotes", "act": "project (correct RESUME.md's branch story before it misdirects the transition; the audit-fix work IS in HEAD)", "status": "UNVERIFIED"}, {"id": "SWEEP-034", "area": "SWEEP", "sev": "medium", "item": "Deferred-work list from CODE_REVIEW that a rebuild will otherwise inherit silently", "detail": "CODE_REVIEW 'Known remaining work (deliberately deferred, none demo-blocking)': (a) 'explicit theory anchors at every EDA producer (inference fallback is currently benign but fragile)' \u2014 confirmed: utils/theory_anchors.py:421 infer_theory_anchor falls through category matching to raw substring matching on finding text (:438-462, e.g. 'skew'\u2192skewness, 'ece'\u2192calibration), and repo-wide there is exactly ONE literal theory_anchor= outside ml/model_coach.py (which sets four: skewness :692, outliers :735, scaling :767, missing_data :798). Reword a finding string during the rewrite and its theory link vanishes silently. (b) 'coaching badge counts vs scoped body coherence'. (c) 'removing or wiring t\u2026", "ev": "CODE_REVIEW.md 'Known remaining work'; utils/theory_anchors.py:421,438-462; ml/model_coach.py:692,735,767,798; ml/calibration.py:264", "act": "record (make theory_anchor mandatory at construction \u2014 the substring fallback cannot survive a copy rewrite); drop (decision_curve_analysis, example_data.csv)", "status": "UNVERIFIED"}, {"id": "SWEEP-035", "area": "SWEEP", "sev": "medium", "item": "THEORY_ANCHORS and DEMO_REGISTRY are a 19/19 key-for-key pair with no test asserting it", "detail": "utils/theory_anchors.py:27 THEORY_ANCHORS declares skewness, outliers, collinearity, class_imbalance, sample_size, high_dimensionality, missing_data, leakage, scaling, transforms, calibration, cross_validation, threshold_choice, shap, pdp_ice, seed_sensitivity, bootstrap, regularization, bias_variance. utils/theory_demos.py:833 DEMO_REGISTRY declares the identical 19 keys. Nothing asserts the correspondence; render_inline_demo returns False silently on a miss (:865-868) and render_theory_link degrades to a caption on exception (:530-531). CODE_REVIEW already caught adjacent rot: 'theory-anchor feature_scale key doesn't exist (works by accidental text match)' and 'several theory breadcrumbs p\u2026", "ev": "utils/theory_anchors.py:27-372; utils/theory_demos.py:833-868; utils/theory_anchors.py:518,530", "act": "view (add an equality assertion on the two key sets \u2014 cheap, and it is the only guard on the pedagogy layer)", "status": "UNVERIFIED"}, {"id": "TEST-026", "area": "TEST", "sev": "medium", "item": "scripts/integration_test_apptest.py and scripts/integration_test.py are unreachable from CI and duplicate tests/integration/", "detail": "LANDS: drop. scripts/integration_test_apptest.py (655 loc) builds its own build_test_dataframe() at :44 duplicating tests/integration/conftest.py:18, and defines 16 AppTest page tests (test_01_upload_page at :157 through test_enriched_feature_scaling at :570) that overlap tests/integration/test_pages.py. It is invoked by no Makefile target and no CI step. scripts/integration_test.py (511 loc) is the Playwright suite, reachable only via `make test-e2e` which requires a live server at localhost:8501 and is explicitly excluded from `make ci`. Both are 100% Streamlit/browser-coupled. Retaining them creates the illusion of coverage that the migration would then try to preserve.", "ev": ".github/workflows/ci.yml (only `pytest tests/` and `pytest tests/integration`); Makefile:22-40; scripts/integration_test_apptest.py:157-570", "act": "Delete both at the start of the migration and state explicitly that E2E coverage is being rebuilt against the new frontend, so nobody budgets time porting them.", "status": "UNVERIFIED"}, {"id": "TEST-027", "area": "TEST", "sev": "medium", "item": "utils/persistence.py is dead code \u2014 zero importers \u2014 yet contains the reproducibility manifest the new Project layer needs", "detail": "LANDS: drop the module, salvage one function. A repo-wide search for `from utils.persistence` / `import utils.persistence` returns 0 hits outside the file itself (the many 'persistence' greps are topological persistence in ml/macro_shape.py). It is 158 loc defining save_dataframe, load_dataframe, save_model (joblib.dump at :57), load_model, save_session_metadata, load_session_metadata, list_cached_sessions, data_hash(df) at :109, and generate_reproducibility_manifest at :116. It is listed as 'uncovered' but the real status is unreachable. Note the trap: it is tempting to adopt it wholesale as the Project serializer, which would import the unpicklable-pipeline bug (finding above) straight int\u2026", "ev": "utils/persistence.py:1-158; zero import sites repo-wide", "act": "Do not adopt as-is. Lift `data_hash` and `generate_reproducibility_manifest` into project with tests, delete the rest. Verify save_model against a pipeline containing the log_transform step before relying on joblib.dump.", "status": "UNVERIFIED"}, {"id": "TEST-028", "area": "TEST", "sev": "medium", "item": "visualizations.py returns figures nobody inspects \u2014 the safest module to move and the easiest to break unnoticed", "detail": "LANDS: engine. 5 public functions: visualizations.py:12 `plot_training_history(history: Dict[str, List[float]]) -> go.Figure`, :53 plot_predictions_vs_actual, :91 plot_residuals, :129 plot_bland_altman, :170 `create_metrics_display(metrics: Dict[str, float]) -> str`. Confirmed it never calls st.pyplot/st.plotly_chart, so it is pure. But zero coverage means a refactor can silently swap which trace is which (predicted vs actual axes), invert a residual sign, or change create_metrics_display's string format \u2014 all of which render fine and are wrong. Consumers: pages/06 (:69), pages/07 (:1078), scripts/smoke_check.py.", "ev": "visualizations.py:12,53,91,129,170; consumers pages/06_Train_and_Compare.py:69, pages/07_Explainability.py:1078", "act": "Structural golden test, not image comparison: assert len(fig.data), each trace's .name/.type/.mode, axis titles, and the first/last 3 values of trace .x/.y against frozen literals. For create_metrics_display assert the exact returned string\u2026", "status": "UNVERIFIED"}, {"id": "TEST-029", "area": "TEST", "sev": "medium", "item": "ml.feature_steps and ml.baseline_models are uncovered and produce the numbers the manuscript compares against", "detail": "LANDS: engine. ml/feature_steps.py:11 `create_pca_step(...)` and :39 `class KMeansFeatures(BaseEstimator, TransformerMixin)` \u2014 both stochastic (PCA sign convention, KMeans init) so their outputs are seed-sensitive and will differ across sklearn versions unless random_state is pinned; a migration that touches the seed changes engineered feature VALUES silently. ml/baseline_models.py:21 `train_baseline_models(...)` and :150 `format_comparison_table(...)` produce the reference numbers a model is claimed to beat. Both have 1 importer each (low fan-in, borderline) but high consequence because their outputs are published.", "ev": "ml/feature_steps.py:11,39; ml/baseline_models.py:21,150", "act": "Pin random_state explicitly in a golden test and assert baseline metric dicts to 10 dp on tests/conftest.py build_regression_df(seed=42). For KMeansFeatures also assert cluster-label counts (not labels, which permute) to survive sklearn ver\u2026", "status": "UNVERIFIED"}, {"id": "TEST-030", "area": "TEST", "sev": "medium", "item": "tests/workflow/* is order-dependent by design and silently degrades to false passes if pytest ordering changes", "detail": "LANDS: project (port carefully). tests/workflow/conftest.py:9-12 warns in caps: 'Tests using these mutable state fixtures MUST be defined in sequential order within a class (test_step1, test_step2, etc.). Pytest runs class methods in definition order by default. Do NOT reorder test methods or add parallel execution \u2014 state is shared and mutated.' The fixtures are scope='module' mutable dicts. tests/workflow/test_state_invalidation.py:145 test_phase5 explicitly depends on phase 3 having run ('No test indices (phase 3 must run first)'). Adding pytest-xdist for a faster migration loop \u2014 an obvious move when the suite grows \u2014 silently breaks the state chain, and several phases assert `is not Non\u2026", "ev": "tests/workflow/conftest.py:9-15; tests/workflow/test_state_invalidation.py:145-150", "act": "Port these to explicit fixture chaining (each phase returns state consumed by the next) rather than module-scoped mutation, so ordering is expressed in the dependency graph. Do this during the port, not after; never enable -n auto against t\u2026", "status": "UNVERIFIED"}, {"id": "TEST-031", "area": "TEST", "sev": "medium", "item": "The plain-dict fake state in tests/workflow/ is the ready-made bridge to the Project object \u2014 the only asset that transfers directly", "detail": "LANDS: project. tests/workflow/conftest.py:43 `state = {}` then `inject_uploaded_state(state, regression_df, ...)`; tests/conftest.py:60 `inject_uploaded_state(session_state, df, target_col='glucose', task_type='regression')` accepts ANY mapping \u2014 it never touches streamlit, only `session_state[key] = value`. This means tests/conftest.py's helpers (build_regression_df:14, build_classification_df:40, make_data_config:49, prepare_splits:88, train_ridge_model:125, inject_trained_state:148) already run against a dict and will run unchanged against a Project that implements __getitem__/__setitem__. This is the cheapest possible migration bridge and should anchor the sequencing.", "ev": "tests/conftest.py:60-85, :88-122, :125-145; tests/workflow/conftest.py:41-45", "act": "Give Project a dict-like adapter early so tests/conftest.py helpers work verbatim; that converts the 25 transitive-streamlit test modules from 'rewrite' to 'reimport'. Highest leverage single step in the whole migration.", "status": "UNVERIFIED"}, {"id": "TEST-032", "area": "TEST", "sev": "medium", "item": "tests/test_page_imports.py and tests/test_insight_id_integrity.py are AST-based over pages/ \u2014 they die with pages/ but encode rules worth keeping", "detail": "LANDS: drop the harness, keep the rule. Both parse source: tests/test_page_imports.py:123 `tree = ast.parse(f.read())` and tests/test_insight_id_integrity.py:52 same. They are in the streamlit-FREE list (they never import streamlit, they read pages/ as text), so they will keep 'passing' after the migration while silently checking a directory that no longer exists or is empty \u2014 a vacuous-green risk identical to the source-grep tests. test_insight_id_integrity in particular enforces that Insight ids are unique across the codebase, which remains a real Record-layer invariant.", "ev": "tests/test_page_imports.py:123; tests/test_insight_id_integrity.py:52", "act": "Re-point insight-id uniqueness at the Record layer (assert over the registry of emitted Insight ids at runtime, not over source text) and add an explicit guard that the scanned directory is non-empty so the test fails loudly rather than pas\u2026", "status": "UNVERIFIED"}, {"id": "CONTRACT-063", "area": "CONTRACT", "sev": "low", "item": "Boundary Upload\u2192Target: DataConfig / SplitConfig / ModelConfig / TaskTypeDetection / CohortStructureDetection", "detail": "All five are plain @dataclass, all fields JSON-safe, all round-tripped via asdict() + field-filtered reconstruction. DataConfig(target_col, feature_cols, datetime_col, task_type). SplitConfig(train_size, val_size, test_size, random_state, stratify, use_time_split, datetime_col, target_trim_enabled, target_trim_lower, target_trim_upper, target_transform). TaskTypeDetection and CohortStructureDetection each expose a computed `.final` property that asdict() does NOT capture \u2014 restore is lossless only because the underlying fields are stored.", "ev": "utils/session_state.py:13-102; utils/session_manager.py:82-88, :396-404", "act": "project \u2014 these five dataclasses are the cleanest existing prototype for AnalysisProject sub-objects; keep them nearly as-is", "status": "UNVERIFIED"}, {"id": "CONTRACT-064", "area": "CONTRACT", "sev": "low", "item": "Boundary FeatureSel\u2192Preprocess: X is passed as .values, losing the index", "detail": "`X = df.loc[mask, numeric_features].values; y = df.loc[mask, target_col].values`, then median-imputed with a throwaway SimpleImputer. The mask correctly ANDs `df[target_col].notna() & train_row_mask(df.index)` \u2014 page 04 is the ONE page that honours the lockbox properly. But the numpy conversion means the selection results carry no row provenance.", "ev": "pages/04_Feature_Selection.py:126-140", "act": "engine \u2014 selectors should take (DataFrame, mask) and return names; Project owns the mask", "status": "UNVERIFIED"}, {"id": "CONTRACT-065", "area": "CONTRACT", "sev": "low", "item": "Boundary Preprocess\u2192Train: coach_probe_result (ProbeResult)", "detail": "@dataclass with float/str/List[str] fields plus four computed @property verdicts (has_signal, nonlinearity_gain, data_hungry, underpowered). asdict() captures fields but NOT the properties \u2014 correct, since they are derived. Persisted as coach_probe.json (schema 2.1). Cleared by reset_downstream_results with an explicit comment that a probe verdict must never outlive the data it measured.", "ev": "ml/coach_probe.py:47-70; utils/session_manager.py:309-318; utils/session_state.py:372-374", "act": "record \u2014 a measured verdict is evidence; keep it in the Record with its data fingerprint attached", "status": "UNVERIFIED"}, {"id": "CONTRACT-066", "area": "CONTRACT", "sev": "low", "item": "Boundary \u2192Report: insight_ledger", "detail": "InsightLedger of Insight dataclasses (id, source_page, category, severity, finding, implication, recommended_action, manuscript_text, relevant_pages, affected_features, tripod_keys, model_scope, auto_generated, theory_anchor, action_page, resolved/resolved_by/resolved_on_page/resolved_at/resolution_details, acknowledged, metadata, created_at). Has to_dict/from_dict/to_list/from_list, all JSON-safe. Consumed for narrative_for_report(), to_manuscript_narrative(), discussion_points_for_manuscript(), get_tripod_status(), provenance_timeline(), get_methodology_log().", "ev": "utils/insight_ledger.py:522-656, :1342-1355, :1140-1341", "act": "record \u2014 this is already the Record; keep the schema, replace get_ledger()'s session_state lookup with injection", "status": "UNVERIFIED"}, {"id": "CONTRACT-067", "area": "CONTRACT", "sev": "low", "item": "RFWrapper defines predict_proba and supports_proba twice", "detail": "Lines 75-85 and 87-97 are byte-identical duplicates; the second pair silently shadows the first. Harmless today, but it means the file has been edited by paste at least once and models/ has zero test coverage to catch a divergence.", "ev": "models/rf.py:75-97", "act": "drop the duplicate \u2014 and note models/* is entirely untested, so this is the class of defect that will not be caught during the port", "status": "UNVERIFIED"}, {"id": "CONTRACT-068", "area": "CONTRACT", "sev": "low", "item": "visualizations.py is already clean and can move to engine unchanged", "detail": "Five functions, all returning plotly go.Figure or a formatted string, zero streamlit imports, zero session_state: plot_training_history(history), plot_predictions_vs_actual(y_true, y_pred), plot_residuals(y_true, y_pred), plot_bland_altman(...), create_metrics_display(metrics). Untested but structurally portable as-is.", "ev": "visualizations.py:12,53,91,129,170", "act": "engine \u2014 move verbatim; add tests before the port, not after", "status": "UNVERIFIED"}, {"id": "CONTRACT-069", "area": "CONTRACT", "sev": "low", "item": "models/* (7 files, 1000 loc) has zero streamlit and a single stable ABC \u2014 port it first", "detail": "BaseModelWrapper(ABC) with abstract fit/predict and concrete predict_proba/get_model/supports_proba. Five concrete wrappers all conform. The only environmental dependency is torch in nn_whuber (guarded: page 06 raises 'The neural network needs the optional torch package' when NNWeightedHuberWrapper is None). This is the lowest-risk, highest-confidence first migration slice \u2014 and it is also entirely untested, which is the reason to do it first with tests attached.", "ev": "models/base.py:10-73; models/nn_whuber.py:226; pages/06_Train_and_Compare.py:1371-1375", "act": "engine \u2014 port first, with characterization tests written against the current behavior before any code moves", "status": "UNVERIFIED"}, {"id": "MINE-042", "area": "MINE", "sev": "low", "item": "utils/table_export.table() is a view function that also decides whether an export button exists", "detail": "`def table(df, width=\"stretch\", hide_index=True, **kwargs)` calls st.dataframe and, only when a `key` kwarg is supplied, emits a TSV download button (utils/table_export.py:9, 38-56). It has 9 importers across 9 pages. Whether a table is exportable is currently a side effect of whether a caller happened to pass `key=`.", "ev": "utils/table_export.py:9, 37-56", "act": "view \u2014 port as a Feed table component. Flagged because the export-availability rule is implicit: a mechanical port that drops the `key` argument silently removes the download button from every table, and nobody will notice until a researche\u2026", "status": "UNVERIFIED"}, {"id": "MINE-043", "area": "MINE", "sev": "low", "item": "pages/06 defines fallback plotting stubs that shadow visualizations.py on ImportError", "detail": "`_get_visualization_functions()` catches ImportError and defines inline replacements for plot_training_history, plot_predictions_vs_actual and plot_residuals (pages/06_Train_and_Compare.py:66-90). The stubs differ from the real ones: the fallback plot_predictions_vs_actual omits the y=x perfect-agreement reference line that visualizations.py:70-79 draws, and the fallback plot_residuals omits the zero line (visualizations.py:110-117).", "ev": "pages/06_Train_and_Compare.py:66-90; visualizations.py:53-88, 91-126", "act": "drop the fallback; visualizations.py is a first-party module in the repo root and cannot legitimately be missing. WRONG-BUT-PLAUSIBLE OUTPUT: an unrelated import error inside visualizations.py (it imports matplotlib at module level, visuali\u2026", "status": "UNVERIFIED"}, {"id": "MINE-044", "area": "MINE", "sev": "low", "item": "visualizations.plot_residuals will pandas-align if handed two Series with different indexes", "detail": "`residuals = y_true - y_pred` (visualizations.py:94) with no np.asarray coercion, unlike plot_bland_altman which does `a = np.asarray(a).ravel()` (lines 141-142). Called as `plot_residuals(results[\"y_test\"], results[\"y_test_pred\"], ...)` (pages/06_Train_and_Compare.py:2623). plot_predictions_vs_actual similarly calls `y_true.min()` / `y_pred.min()` (lines 71-72), which works on Series and ndarray but not on a list.", "ev": "visualizations.py:91-126 vs 141-142; pages/06_Train_and_Compare.py:2604, 2623", "act": "engine \u2014 add the same np.asarray().ravel() coercion plot_bland_altman already has. WRONG-BUT-PLAUSIBLE OUTPUT: if y_test keeps its original labels and y_test_pred is ever wrapped in a Series with a RangeIndex, subtraction aligns on labels a\u2026", "status": "UNVERIFIED"}, {"id": "SWEEP-036", "area": "SWEEP", "sev": "low", "item": "utils/table_export.py:9 table() \u2014 the universal table renderer imported by 9 of 11 pages, and its export promise is conditional", "detail": "table(df, width='stretch', hide_index=True, **kwargs) wraps st.dataframe and adds a TSV download button \u2014 but ONLY when a `key` kwarg is supplied (`table_key = kwargs.pop('key', None)`; `if table_key:`). Most call sites pass no key, so 'every table is downloadable' is not true today. It also carries a compatibility shim mapping the removed `use_container_width` onto `width` (CODE_REVIEW records a repo-wide sweep of that deprecation). Importers: pages 01,02,04,05,06(:1898),07,08,09,10.", "ev": "utils/table_export.py:9,40-56", "act": "view (in the rebuild make export unconditional \u2014 CODE_REVIEW design principle #6 makes every result either exportable or explicitly UI-only)", "status": "UNVERIFIED"}, {"id": "SWEEP-037", "area": "SWEEP", "sev": "low", "item": "utils/widget_helpers.py:7 safe_option_index \u2014 21 loc, one caller, raises on a bad default", "detail": "safe_option_index(options: List[str], value: Any, default: str) -> int returns options.index(default) when value is None or absent. If `default` is itself not in `options` it raises ValueError \u2014 the 'safe' helper is only safe on one of its two arguments. Sole importer: pages/05_Preprocess.py:34.", "ev": "utils/widget_helpers.py:7-21; pages/05_Preprocess.py:34", "act": "view (or drop \u2014 trivially reimplemented in the new form layer)", "status": "UNVERIFIED"}, {"id": "SWEEP-038", "area": "SWEEP", "sev": "low", "item": "launcher/make_icon.py \u2014 175 loc build-time asset generator, not app code; its docstring is the product's identity statement", "detail": "Renders icon.png/.ico/.icns from numpy+Pillow at 4096px, run manually (`python launcher/make_icon.py`). No app module imports it. Worth noting for the transition because the icon encodes the app's thesis: '\u2026the bottom-right cell held out as a sealed ring (the test-set lockbox)'. Also flags a shipping surface the rebuild inherits: .gitignore lists '.tools/', 'Tabular ML Lab.app/', '.venv-stamp' \u2014 there is a one-click desktop launcher, plus .github/workflows/release.yml and sync-university-docker.yml.", "ev": "launcher/make_icon.py:1-13; .gitignore ('One-click launcher artifacts'); .github/workflows/release.yml", "act": "drop (from the app); keep as a build script", "status": "UNVERIFIED"}, {"id": "SWEEP-039", "area": "SWEEP", "sev": "low", "item": "ml/__init__.py, utils/__init__.py, pages/__init__.py \u2014 one-line docstrings, no re-exports, no side effects", "detail": "ml/__init__.py = '\"\"\"Machine learning utilities.\"\"\"'; utils/__init__.py = '\"\"\"Utility modules for the modeling lab.\"\"\"'; pages/__init__.py = '\"\"\"Pages module for multi-page Streamlit app.\"\"\"'. Nothing is exported, so no import surface is lost by restructuring these packages \u2014 a genuinely free move, and the only one in this inventory.", "ev": "ml/__init__.py:1; utils/__init__.py:1; pages/__init__.py:1", "act": "engine (ml, utils); drop (pages)", "status": "UNVERIFIED"}]