# TurboTab feature register

**Generated from `data/register.json` — do not hand-edit.** Update via `tools/register.py`, then `regen`. Rationale and rules: `FEATURE_PARITY.md`.

| State | Meaning | Count |
|---|---|---:|
| `both` | exposed in Classic and Guided | 47 |
| `core` | extracted into the shared core | 4 |
| `classic-only` | a claim to be justified, never a shrug | 36 |
| `guided-only` | a debt owed back to Classic | 25 |

## Data & Target (Classic: pages/01, Step 4) — 34

| ID | Capability | Classic | State | Reason |
|---|---|---|---|---|
| `audit-binary-text` | Binary-coded text detected as binary, outranking numeric coercion, with a 'read as binary' repair | (none) - import_doctor proposes coerce_numeric for the same column | **guided-only** | GUIDED-001. ml/binary_text.py is engine code and Classic could call it, but pages/01 renders import_doctor.diagnose directly and would show both proposals for one column. The merge lives in turbotab/engine.diagnose because ml/import_doctor.py is frozen as engine-move-only (TRANSITION_PLAN §05) and folding it into diagnose() there would be a behavior change to a frozen file. Owed back to Classic when the freeze lifts. L18 DISPOSITION: guided-only, and it stays guided-only because it is REAL DEBT — Classic is expected to gain this, and the count is what makes 'we owe Classic N things' a number rather than a feeling. Engine capability, not interface. ml/binary_text.py is engine code Classic could call; the blocker is that pages/01 renders a frozen import path. Debt with a freeze in front of it. |
| `audit-preview` | Preview before apply | (none) | **guided-only** | Classic applies straight from a button — the blind consent PRODUCT_VISION §04 argues against. Debt owed to Classic: T0-CLASSIC-001. L18 DISPOSITION: guided-only, and it stays guided-only because it is REAL DEBT — Classic is expected to gain this, and the count is what makes 'we owe Classic N things' a number rather than a feeling. PRODUCT_VISION 04 names applying without a preview as blind consent, and it binds BOTH doors. FEATURE_PARITY's own text uses this row as the canonical example of convergence running both ways. |
| `audit-undo` | Undo an applied repair | (none) | **guided-only** | Engine always supported it (apply_fix never mutates); Classic never exposed it. Owed to Classic with the preview. L18 DISPOSITION: guided-only, and it stays guided-only because it is REAL DEBT — Classic is expected to gain this, and the count is what makes 'we owe Classic N things' a number rather than a feeling. Same argument as audit-preview: a fix you cannot undo forces certainty before the user can see what it did, which is the blind consent the preview exists to avoid. Classic is owed it. |
| `bulk-repair` | One preview, a selectable set, one apply, one decision covering N features that need the same repair | none — pages/01 applies one import-doctor fix per button, with no diff and no undo | **guided-only** | A DEBT, not a native. Classic already runs every one of these repairs; what it lacks is the grouping, the selection and the single receipt, and there is nothing about Classic that prevents them. The register's own line is the test: a reasonable Classic would still be missing something. The grouping rule is engine-side in turbotab/repairs.py and takes findings and nothing else, so it is portable the day pages/01 is converged (and pages/01 is frozen until the import tail closes, which is why this is owed rather than done). |
| `data-domain-packs` | Five domain packs — metabolomics, genomics, dietary, clinical, survey — supplying detectors, priors and prose, with a confidence marker on every default and no new card type | No equivalent, and no place to put one: the domain knowledge Classic has (physiologic bounds, unit harmonization) is unconditional rather than lens-gated, so it cannot be wrong about a field but also cannot be right about one. | **guided-only** | Built at L19. DEBT rather than native, and the distinction matters here: a pack is detectors and reference data, which is engine-shaped, not interaction-shaped. Guard 1 forbids a pack adding a card type precisely so that nothing about a pack presupposes the Guided door's design model - the one exception, the reverse-coding question, is an ordinary FACT that Classic could ask as easily. What Classic lacks is the lens to gate them on, which is data-lens-question's debt and not a second one. Thin where thin is right: the clinical pack has ZERO detectors and one prior, because physiologic plausibility bounds and unit harmonization already exist in the shared core; the genomics pack asserts no normalization default at all, and its priors carry a normalization key with variant None so that declining is recorded rather than absent. |
| `data-lens-question` | The lens question — "What kind of measurements are in this table?" — multi-select, asked after upload and before the structural diagnosis, recording a decision and a methods sentence | No equivalent. pages/01 runs import_doctor.diagnose with no notion of what field the table comes from, so an untargeted assay panel is diagnosed by the same rules as a clinic export and its wide shape reads as malformed. | **guided-only** | Built at L19 in turbotab/packs.py + project.set_lens + router's state_lens question, per lockbox constitution clause 01 and DOMAIN_PACKS.md. DEBT, NOT NATIVE, by the L18 test - would a reasonable Classic still be missing something? Yes: clause 01 binds both doors and places the lens FIRST, before the diagnosis, because the diagnosis is field-sensitive. Classic diagnoses wide assay data as malformed today and proposes melt_repeated on it, which is a pre-barrier structural repair that rebuilds the index. That is a defect Classic has and Guided does not, which is the clearest form of debt this register recognizes. STATE-106 is the tracked row and it closes with this. The forward path is the same as the grain question's: packs.py is deliberately UI-free and imports only pandas, numpy and the engine's own physiology reference, so Classic gaining it is a call-site change at the L11 convergence of pages/01 rather than a port. |
| `data-repeated-measures-chain` | Questions 4 to 7 — repeats-or-time-points (stated from spacing, overturnable), unit of analysis (no default), aggregation (pre-seal, executed), temporal prediction (routes to the chronological split) | No equivalent. pages/06 carries all four split strategies and nothing decides when each applies; there is no question anywhere in Classic about what varies between one person's rows, so a longitudinal file and a repeated-recall file are treated identically. | **guided-only** | Built at L19 in turbotab/repeats.py plus four project methods and four router questions, per lockbox constitution clause 01 and OPENING_SEQUENCE.md section 03. DEBT by the L18 test: clause 01 binds both doors, and Classic already HAS the chronological and grouped split strategies in ml/splits.py with sixteen equivalence tests - what it lacks is the routing that decides between them, which is exactly a missing question rather than a missing capability. The aggregation half is a stronger form of the same debt: Classic can neither combine a person's rows nor refuse to, so a user with two dietary recalls per person gets a row-level model and no sentence saying so. repeats.py is UI-free and imports only pandas and numpy, so the convergence is a call-site change at pages/06 rather than a port. |
| `model-purpose` | What is this model for? — prediction or inference, and the handling defaults that read it (opening sequence 2.5) | none — Classic assumes prediction and offers no way to say otherwise | **guided-only** | A DEBT. Classic ships the same missing-indicator route and the same class-imbalance advice, and both are wrong for an inference objective there exactly as they were here; nothing about Classic prevents the question. turbotab/purpose.py and ml/imbalance_advice.py are both headless and take no project, so they are portable the day pages/02 or pages/05 is converged. |
| `target-grain-question` | The grain question — "Can one person appear in more than one row?" — asked before the seal, with the heuristic demoted to a suggestion and a name-blind contradiction detector | pages/01_Upload_and_Audit.py infers it: ensure_lockbox calls detect_repeated_subjects and seals on whatever the name-token gate returns. No control exists to declare the subject column (IMPORT-257). | **guided-only** | Built at L13 in turbotab/grain.py + project.set_grain + engine.draw_holdout, per constitution 02 and ASSEMBLY_SPEC 05. GUIDED-ONLY, NOT A SCOPING-DOWN, and dated: the forward path is Classic calling the same turbotab/grain.py functions from pages/01 at the L14 convergence of pages/01_Upload_and_Audit.py. grain.py is deliberately UI-free and imports only pandas plus utils/test_lockbox constants, so the convergence is a call-site change rather than a port — that is what makes the date credible rather than aspirational. Until it lands, IMPORT-020 and IMPORT-022 stay OPEN against Classic with the convergence phrase, and tests/test_the_seal_states_its_basis.py::test_KNOWN_GAP_an_unrecognized_id_name_is_still_recorded_as_cross_sectional stays GREEN because it pins Classic's ensure_lockbox, which still guesses. That test going red is the signal the convergence landed. L18 DISPOSITION: guided-only, and it stays guided-only because it is REAL DEBT — Classic is expected to gain this, and the count is what makes 'we owe Classic N things' a number rather than a feeling. Lockbox constitution 02 binds both doors. IMPORT-020 and IMPORT-022 are Classic's versions of this defect and they are open. |
| `target-positive-class` | The outcome is asked which of its levels is the event being predicted, never whether it is binary; the event is never pre-selected at any confidence | (none) - Classic encodes a two-level target by whatever sklearn's LabelEncoder does with it | **guided-only** | GUIDED-009, on the product owner's ruling that the target's question is different in kind from a feature's. Classic does not ask. ml/splits.py:300 label-encodes a categorical target with LabelEncoder().fit_transform(y.astype(str)), which orders classes ALPHABETICALLY, so the event is whichever level sorts last - verified, not assumed. On messy-clinic that is 'responder' over 'non-responder', which happens to match convention; on the longitudinal fixture it is 'stable' over 'improved', so the model is trained to detect NOT improving, which is almost certainly backwards; and for alive/dead it makes death the event by alphabet. The choice is real, consequential and made by string ordering, with no place in the Classic interface where it is stated or changeable. Guided asks it as its own question, offers convention as a suggestion with the reasoning shown, and refuses to apply without a named level. Dated debt owed back to Classic per PRODUCT_VISION 'Guided is never the less capable door': it lands with the L11 convergence of pages/01, and until then Classic's encoding is alphabetical and undisclosed. L18 DISPOSITION: guided-only, and it stays guided-only because it is REAL DEBT — Classic is expected to gain this, and the count is what makes 'we owe Classic N things' a number rather than a feeling. Classic label-encodes a two-level target alphabetically, so the event is whichever level sorts last. That is a defect in Classic, not a difference in design model — Classic is owed the question. |
| `evidence-badge` | SETTLED / CONVENTION / DISPUTED on every pack advisory, with a source that resolves to a research file and section | none — Classic states its advisories at uniform confidence | **guided-native** | Presupposes the pack layer and the question grammar: the badge is a token rendered beside an advisory the Router served, and Classic has no advisory object to hang it on - its recommendations are page-local prose. The register's line is the test and a reasonable Classic would not be missing something it has no place to put. The primitive itself is portable: turbotab/packs.Evidence takes no project and the gate reads the source tree. |
| `nutrition-pack-content` | The nutrition pack's first real content: the Atwater energy-unit reconstruction, NHANES survey-design reading, and the prevalence-of-inadequacy refusal | none — Classic reads an energy column as a number and has no survey-design or DRI vocabulary | **guided-native** | Presupposes the lens and the pack layer: every one of these fires only under a dietary lens, and Classic has no lens to gate them on - asking Classic for them is asking Classic to be Guided. The register's line is the test and a reasonable Classic would not be missing something it has no mechanism to hold. turbotab/nutrition.py takes a frame and nothing else, so the detectors are portable the day a door wants them; what is not portable is the gate. |
| `table-orientation` | Which way round is this table? — transposing a feature-major assay export before the diagnosis (opening sequence 1.5) | none — Classic reads column dtypes off whatever it is handed | **guided-native** | Presupposes a Router with a fixed pre-seal sequence and a question that can act before the diagnosis rather than annotate its output. Classic's diagnosis runs at pages/01 with no question before it and no place to put one; asking Classic for this is asking Classic to be Guided. NOT filed as a debt for that reason, and the register's own line is the test: a reasonable Classic would not be missing something it has no sequence to hold. The underlying reading and the transpose live in turbotab/orientation.py and take a frame and nothing else, so they are portable the day a door wants them. |
| `target-why-we-ask` | Every question of fact carries a 'Why we ask' disclosure naming who consumes the answer and what changes; the Router refuses to emit one that cannot | pages/01 help tooltips on the target and task-type widgets | **guided-native** | DESIGN_LANGUAGE §09: 'A FACT that cannot state its consumer is a question we have no right to ask.' Classic's tooltips explain what the control does; they do not name what reads the answer or what changes downstream, which is the part that lets a user judge whether to think hard about it. The text lives on ml.router.Question.consumer and router.audit raises on a pushed question of fact without one, so the rule is enforced where a new question would have to pass it rather than in the page. The Router is shared, so Classic can adopt this by asking instead of computing - the same move readiness already made. L18 DISPOSITION: guided-native. Question grammar. DESIGN_LANGUAGE 09 says a FACT that cannot name its consumer is a question we have no right to ask, and it is enforced in router.audit(). Classic has no question objects to carry a consumer. |
| `audit-apply-repair` | Apply a proposed repair | 'Suggested Actions' | **both** | import_doctor.apply_fix, same nine fix kinds |
| `audit-diagnosis` | Structural diagnosis | import_doctor.diagnose | **both** | Same function, same findings — asserted field-for-field by test_findings_match_a_direct_engine_call |
| `audit-profile` | Dataset profile (types, missingness, cardinality, numeric stats) | Step 3 expanders | **both** | compute_dataset_profile; rendered as one ranked stack rather than six expanders |
| `ingest-single-file` | Single delimited file upload | st.file_uploader (Step 1) | **both** | engine.read_table, plain pandas inference to match what the doctor expects |
| `target-selection` | Target column selection | Step 4 | **both** |  |
| `target-task-detection` | Task-type detection | triage.detect_task_type | **both** |  |
| `target-task-override` | Task-type override | 'Override Task Type' expander | **both** | Added because this register found it missing — offered whenever confidence is below high; detection and answer both recorded. |
| `audit-cardinality-table` | Cardinality table, per column | Step 3 expander | **classic-only** | Guided surfaces the high-cardinality finding but not the full table. Pull-based exploration, deferred. |
| `audit-duplicate-rows` | Duplicate-row detection | Step 3 expander (inline) | **classic-only** | No engine home — computed inline in pages/01, orchestration still trapped (T0-PAGES-001). |
| `ingest-excel-sheets` | Excel .xlsx + sheet selection | Step 1 | **classic-only** | RESUME.md names Excel-sheet × transpose as untested; exposing it in Guided first would build on that gap. |
| `ingest-json-records` | JSON + records-key selection | Step 1 | **classic-only** | Same door as multi-file import; frozen with it. |
| `ingest-large-file-guard` | Large-file guard ('Load anyway') | Step 1 | **classic-only** | Guided uses a hard 64 MB ceiling instead — frame is held in memory, never spooled. Different mechanism, same intent. |
| `ingest-multi-join` | Multi-file join + key detection | Step 2, join_doctor | **classic-only** | Frozen (§05). |
| `ingest-multi-roster` | Multi-file upload + roster (rename / remove / replace) | Step 1 | **classic-only** | Multi-file path frozen pending the open ledger tail (TRANSITION_PLAN §05). Deliberate, not forgotten. |
| `ingest-practice-data` | Built-in practice datasets | Step 1 | **classic-only** | Classic has a built-in dataset PICKER (utils/datasets.py generators). Guided has no picker: turbotab/sample_data/ holds four fixture tables (clinic_visits, leaky_sepsis, longitudinal_visits, wide_assay) that you point the uploader at by hand, and three of the four exist for the routing measurement rather than for practice. Re-verified L1: the earlier reason said one table. |
| `ingest-repair-ledger-log` | Import-repair logging to the insight ledger | _log_import_repairs | **classic-only** | Guided records the repair as a project decision (project.apply_fix -> record(kind='apply'), turbotab/project.py:489-513), so the ACT is not lost. What is Classic-only is the write into the InsightLedger. Re-verified L1: the earlier reason said the ledger singleton is not cut yet (L7). L7 landed and did something narrower - it moved the streamlit import inside get_ledger() (RECORD-009), which untainted the module - but get_ledger() is still a session_state singleton, so there is still no headless ledger for Guided to write to. |
| `ingest-transpose` | Transpose on import | Step 1 | **classic-only** | Belongs with the Excel path above. |
| `target-feature-picklist` | Feature selection (select all / clear) | Step 4 | **classic-only** | Not yet asked in Guided. |
| `target-goal-selection` | Goal selection (Prediction vs Hypothesis Testing) | Step 4 | **classic-only** | Guided assumes prediction; the hypothesis-testing branch is a different interview. |
| `target-lockbox-settings` | Test-holdout / lockbox settings | Step 4 expander | **classic-only** | Updated after L5: the project now OWNS a lockbox — sealing raises the identity barrier and the apply path refuses pre-barrier repairs — but the fraction/seed settings and the act of sealing are not yet asked in the Guided interview. Half-watched, no longer unmodeled. |

## Cross-step infrastructure — 11

| ID | Capability | Classic | State | Reason |
|---|---|---|---|---|
| `cancel-training` | Stopping a long run | removed — the button set a flag nothing read | **guided-only** | T0-LIVE-002. Classic's button is removed rather than wired: Streamlit runs one script per session on one thread, so during training no widget is interactive and the button could not be clicked at all. The page now says a run cannot be interrupted, which is true. Guided has real cancellation via the job queue. L18 DISPOSITION: guided-only, and it stays guided-only because it is REAL DEBT — Classic is expected to gain this, and the count is what makes 'we owe Classic N things' a number rather than a feeling. Stopping a long run is not a Guided idea, it is a missing capability. Same prerequisite as job-queue. |
| `job-queue` | Observable, cancellable background work with explicit per-job RNG | none — Streamlit reruns instead of scheduling | **guided-only** | turbotab/jobs.py. Owed back to Classic in the sense that Classic cannot have it without a client/server split — this is the component whose absence caused the migration. Jobs that touch process-global RNG are serialized, because snapshot/restore does not isolate threads sharing one RNG; measured, not assumed. L18 DISPOSITION: guided-only, and it stays guided-only because it is REAL DEBT — Classic is expected to gain this, and the count is what makes 'we owe Classic N things' a number rather than a feeling. The row's own reason already says it: owed back in the sense that Classic cannot have it without a client/server split. That is a debt with a hard prerequisite, not a design-model difference, and collapsing it into guided-native would make the migration's central justification invisible. |
| `cross-deferral-destination` | Deferral affordances name their destination on the control, and the coach ledger shows each waiting item with the step it comes back at | (none) - Classic's ledger has no defer-to-step contract at all | **guided-native** | GUIDED-008, and the narrower sibling of cross-deferral-resurface. The API has always rejected a deferral without a target_step, so the destination existed at click time and the button did not say it. ml/router.defer_destination now decides it - structural repairs to Explore, profile findings to Preprocess - and the label travels on every finding. Classic cannot express the contract, so this is owed back with the resurface guarantee, not separately. L18 DISPOSITION: guided-native. Same: a deferral affordance that names its destination presupposes a component that owns 'which step can act on this'. Without a Router there is no destination to name. |
| `cross-deferral-resurface` | Deferral that resurfaces: defer requires a target_step, deferred items resurface at the step that can act on them (measured by the routing value check's deferral_closes metric) | utils/insight_ledger.py records acknowledge/resolve and surfaces insights on relevant pages, but has no defer-to-step contract - no target_step, no resurface guarantee | **guided-native** | turbotab/api.py rejects a defer without target_step ('a deferral without a target is a discard with manners'); Classic's ledger cannot express the contract. In the definition of done and measured in the value check, yet had no register row - the check could not see the absence. A debt owed back to Classic. L18 DISPOSITION: guided-native. Deferral as a first-class disposition is the Router's design model, and Classic has no Router — TRANSITION_PLAN 02.5 measured the coach as a pure annotator that can order questions but not gate them. Promoting it is new construction, so this is not a thing Classic is failing to do; it is a thing Classic is not. |
| `cross-read-as-draft` | Read-as-draft panel: the recorded decisions rendered as draft methods prose with [AUTHOR REQUIRED] gaps visible | pages/10_Report_Export.py generates the manuscript at the END of the workflow | **guided-native** | GUIDED-007, DESIGN_LANGUAGE §10. Classic can generate a manuscript; it cannot show you the draft WHILE you still have time to change the decisions in it, which is the whole pedagogical claim - layer 3 teaching on the user's own data. turbotab/draft.py is a pure fold over the record and holds no state, so Classic could mount it against WorkflowProvenance; it has not. L18 DISPOSITION: guided-native. The accumulating manuscript IS the product thesis (DESIGN_LANGUAGE 10, layer 3: read-as-draft is a faded worked example). Classic gains a report at the end; the draft-while-you-decide panel is what Guided is FOR. |
| `macro-shape-cache` | PCA / UMAP / persistence / Mapper caching keyed on dataset content | pages/02_EDA.py _macro_fp | **both** | T0-LIVE-001. The engine's caches keyed on nothing and served the first dataset's results to every later dataset and user. Caching moved to the host, which is what knows when a dataset changed; the fingerprint hashes values, not shape, because two cohort runs of one study share shape and columns. |
| `core-invalidation-dag` | Downstream invalidation cascade | utils/session_state.py reset_downstream_results | **core** | The L5 DAG reproduces the production cascade key-for-key across all four flag combinations, pinned by test_the_dag_matches_the_production_cascade against a real run of the function. Re-verified L1: pages/03 no longer hand-rolls its own copy (CONTRACT-002 FIXED) - test_page_03_no_longer_hand_rolls_its_own_cascade AST-scans the page and fails if it clears a cascade-owned key, and the fifteen keys that copy used to miss are pinned by test_the_reconciled_call_clears_what_the_hand_rolled_one_missed. Known gap: the DAG has no restore edge for restore_pre_fe_features, and the ledger rollback is not modeled (SWEEP-009, SWEEP-013). |
| `core-lockbox-barrier` | Lockbox + identity barrier | utils/test_lockbox.py (no barrier) | **core** | L5: sealing raises the barrier; promote_header/melt_repeated unreachable behind it, enforced at the API apply path. Guided-ahead-of-Classic on the barrier itself. |
| `core-readiness-model` | Step-completion / readiness model (ten predicates + quick/advanced) | utils/theme.py:685 render_sidebar_workflow | **core** | Extracted to turbotab/readiness.py in L6; the page asks instead of computing, and a test asserts the expressions are gone. The Router's first real input. |
| `engine-headless` | Engine imports and runs with no Streamlit in the process | n/a — this is the core | **core** | All 46 core modules import with streamlit blocked, enforced by tests/test_engine_is_headless.py with a stub-first blocker so it cannot pass vacuously. Was 45 at L7; ml/router.py made it 46 at L8, and the census is computed from the tree rather than written down, so the test moved with it. Six modules were tainted at the L7 baseline; the last was utils/insight_ledger, whose module-level import also tainted narrative_engine, manuscript_validator and latex_report. |
| `cross-profile-row-scope` | The dataset profile records which rows it describes, and every export that copies its numbers states that population | pages/02 writes dataset_profile_scope; pages/10 labels the metadata and the Methods sentence | **classic-only** | Raised out of L11 because it is a live assertion of something false in an exported document, not a convergence item. CONTRACT-017 made Classic's dataset_profile training-scope - the profile drives the model coach, and a coach that has seen the held-out people is choosing models with them - and pages/10 copies its p_n_ratio, total_missing_rate and data sufficiency straight into the export metadata and the Methods section. The number moved and the manuscript did not. Classic now records the scope beside the profile and states it at the point of export, including the case the fix exposed: the missing-data sentence mixes two populations, a feature COUNT from the training-scope profile and per-feature RATES from data_audit over the whole frame, so it now names both. THE TWO DOORS DISAGREE ON SCOPE, and this row is where that is said rather than papered over. Classic's profile covers training rows only whenever a lockbox is sealed. Guided's does not: turbotab/engine.profile(project.df) profiles the whole working table, and the Guided interview does not ask for a seal at all yet (target-lockbox-settings), so in practice nothing is sealed and the two agree by accident rather than by design. The moment Guided asks for the seal, its profile becomes a whole-study number driving a coach that must not see the test set - which is the CONTRACT-017 defect, arriving second, in the door built to avoid it. Guided has no export step yet, so there is no false claim in a Guided document today; there will be one the day there is. The debt is owed FORWARD, and the L11 convergence of pages/02 is where both sides adopt one scope and one label. |

## Explore / EDA (Classic: pages/02) — 38

| ID | Capability | Classic | State | Reason |
|---|---|---|---|---|
| `explore-blocker-acknowledgment` | Recorded acknowledgment to proceed past an unresolved blocker (required sentence; 400 without one) | (none — Classic has no acknowledgment path at all) | **guided-only** | The tool refuses silence, not the user's judgment. Classic lets a user proceed past a known leak with no record; owed to Classic with the named-column push at L11 convergence of pages/02. L18 DISPOSITION: guided-only, and it stays guided-only because it is REAL DEBT — Classic is expected to gain this, and the count is what makes 'we owe Classic N things' a number rather than a feeling. Routing constitution 03 binds both doors: exiting past a blocker writes an acknowledgment into the record. A constitutional obligation Classic does not meet is debt by definition. |
| `explore-finding-offers` | A finding with no proposed repair still offers something — the engine's suggested_actions classified into app operations that preview and earmarks that name where they resurface | pages/02 renders suggested_actions as a bulleted list inside an expander and offers nothing to act on. The same list, the same prose, and no branch that does anything with it. | **guided-only** | Built at L22 for GUIDED-031, found by the product owner driving the app. DEBT, and one of the clearer ones: the classification lives in turbotab/actions.py keyed on the engine's own phrases, imports only pandas, and binds to catalogues both doors read. Classic gains it by calling offers() where it currently loops over suggested_actions. The critique this repairs is DESIGN_LANGUAGE 01.4 - three attributes wearing a sentence costume - which is the critique the whole project started from, so a door that still ships it is a door that has not had the fix. |
| `figure-spec` | The figure spec: layers, annotations, a scored checklist, a generated caption, a tier, and companions that gate admissibility | pages/02 renders seven geometries with no checklist, no caption generation and no companion rule | **guided-only** | A DEBT. Classic draws the same figures from the same engine and ships them into the same manuscript chain with none of the annotation the packs call publication-grade - so a Classic user exports a calibration plot with no risk histogram and no intercept, which is the defect the spec exists to fix rather than a difference of design model. turbotab/figures.py takes no project and no frame; the specs are data. Owed back at L11 convergence of pages/02. |
| `trim-arms-obligation` | A post-seal robustness trim narrows the training partition only and ARMS the extrapolation obligation the report must discharge | PARTIAL and in the wrong direction. pages/05_Preprocess.py's apply_plausibility_filter narrows rows, and since L13 it restores the sealed rows it would otherwise have removed - so it obeys clause 04. It arms nothing: no record survives the step, so pages/07 exports a single aggregate metric with nothing to object. | **guided-only** | BUILT in Guided at L16 as the ARMING half of clause 05 only. GUIDED-ONLY and owed back to Classic, which has the trim and not the memory. The obligation carries the count of HELD-OUT rows outside the trimmed range, computed at trim time because it is unrecoverable afterwards - the training rows outside the range are gone by then. The FIRING half - export refuses without a stratified in-range / out-of-range breakdown - is deliberately NOT built and is tracked as STATE-105: there is no Report step, and a blocker with nothing to block cannot be tested, which would make it the half-built clause the split exists to prevent. L18 DISPOSITION: guided-only, and it stays guided-only because it is REAL DEBT — Classic is expected to gain this, and the count is what makes 'we owe Classic N things' a number rather than a feeling. Lockbox constitution 05 binds both doors. Classic has the trim and not the memory, so pages/07 exports a single aggregate metric with nothing left to object. |
| `explore-blocker-costume` | The CONSEQUENCE treatment: small-caps BLOCKER signal word, full-width interruption band in --stop, the reserved notched glyph, two terminal exits | (none) - Classic renders a leakage warning box in the same style as every other warning | **guided-native** | DESIGN_LANGUAGE §09. The costume is what makes the tier legible before a word is read, and it is reserved: --stop, the signal word and the notched square appear in exactly one component, enforced by a test that scans the page for the tokens outside it. Both exits are terminal - Resolve drops the column through import_doctor's own drop_columns repair, Acknowledge records the typed object-specific sentence - and the blocker never re-fires on the same facts afterwards. Classic has no acknowledgment path and no reserved tier; owed to Classic with explore-blocker-acknowledgment at L11. L18 DISPOSITION: guided-native. The CONSEQUENCE treatment — signal word, full-width interruption, the silhouette surviving color and typography removal. A visual grammar, and grammars do not port as capabilities. |
| `explore-evidence-on-the-card` | Finding cards embed their evidence: the flagged entries with values and bounds, or the distribution the claim is about | pages/02_EDA.py renders plots per analysis, in tabs, separate from the finding text | **guided-native** | GUIDED-003 / GUIDED-005. Classic has more plots than Guided; what it does not have is the plot ATTACHED TO THE CLAIM. A skew finding in the coach ledger and a histogram in the Feature Explorer tab are two screens apart, which is the split-attention cost DESIGN_LANGUAGE §10 layer 2 exists to remove. ml/card_evidence.py is engine code and portable; the attachment is a rendering decision Classic has not made. L18 DISPOSITION: guided-native. DESIGN_LANGUAGE 09: a finding carries its evidence. That is a property of the card vocabulary, and Classic's vocabulary is Streamlit widgets in page order. Asking Classic for it is asking Classic to be Guided. |
| `explore-missingness-decisions` | Per-feature missingness decision cards: features named, routed by dtype, each option carrying its execution timing and its decision sentence | pages/02_EDA.py missingness_scan (a scan, not a decision) + pages/05 global imputation controls | **guided-native** | GUIDED-002. Classic has the analysis and the controls, but not the decision: the EDA page reports missingness patterns and the Preprocess page offers imputation strategies as widgets, with no card that names the feature, routes the question by dtype and writes a methods sentence. The action-timing ruling - structural repairs now, statistical transforms recorded now and fitted in the per-model pipeline on training folds - is carried as data on every option (ml/missingness_plan.py TIMING_*), so it moves to Classic with the module. L18 DISPOSITION: guided-native. The row's own reason draws the line precisely: Classic has the ANALYSIS and the CONTROLS and not the DECISION. Analysis and controls are capabilities; a decision card that names the feature, routes by dtype and writes a methods sentence is the interview form. The clause-07 OBLIGATIONS Classic genuinely owes are tracked separately as prep-informative-missingness-question, prep-outcome-out-of-imputation and prep-stability-assumption, which is why this one can move without losing the debt. |
| `explore-pull-capability-audit` | Every pull affordance declares whether it is wired; unwired ones render disabled with the reason on the control | (none) - Classic's EDA buttons all run | **guided-native** | GUIDED-006. Classic does not need this: every button on pages/02 runs an analysis. Guided needed it because the palette is generated from the recommender and outran the endpoints behind it - before L9 no pull chip ran at all, and three of them looked live. The roster is served from turbotab/api.PULL_CAPABILITIES so the page cannot claim a capability the server lacks. L18 DISPOSITION: guided-native. 'Push the notable, pull the rest' is Guided's interaction thesis. Classic renders everything at once, so there is no pull side to audit — the distinction does not exist there to be honored or violated. |
| `explore-eda-class-imbalance` | EDA trigger: eda_class_imbalance (warning) | pages/02_EDA.py:415 | **both** | Raised by the profile and rendered; the imbalance finding is in the Explore stack. |
| `explore-eda-leakage-col` | EDA trigger: eda_leakage_<var> (blocker) | pages/02_EDA.py:279 | **both** | T0-ROUTE-001 built: leakage is now a pushed question of consequence, not a palette offer. The Router raises one question per column correlating above 0.95 with the target, naming the column, ranked first in the plan. router.audit refuses a blocker offered as pull or skipped at any confidence. Leaving the step with one unresolved is permitted and recorded — the tool does not refuse the user's judgment, it refuses silence, and the acknowledgment is what the manuscript carries as a stated limitation. Classic still only offers a Run leakage detection action and never names the leaking column, so the Classic side of this row remains owed at L11 convergence of pages/02. |
| `explore-eda-missing-moderate` | EDA trigger: eda_missing_moderate (info) | pages/02_EDA.py:380 | **both** | Same source, info severity. |
| `explore-eda-missing-severe` | EDA trigger: eda_missing_severe (warning) | pages/02_EDA.py:363 | **both** | Surfaced as a profile finding in the Explore stack and as the missingness pull card. |
| `explore-eda-sufficiency-borderline` | EDA trigger: eda_sufficiency_borderline (warning) | pages/02_EDA.py:261 | **both** | Same source as the insufficient case, at warning severity. |
| `explore-eda-sufficiency-insufficient` | EDA trigger: eda_sufficiency_insufficient (blocker) | pages/02_EDA.py:245 | **both** | Raised by the profile as a critical warning and rendered in Explore. One of the two blocker-severity triggers that only the page emitted; it is engine-derived now. |
| `explore-histogram-pager` | Per-feature histogram pager and correlation matrix, offered only below 50 numeric features | pages/02_EDA.py Feature Explorer pager + correlation heatmap (no feature-count gate) | **both** | GUIDED-005. Both doors offer both analyses. The difference is the gate: Guided refuses above 50 numeric features and says why, because a grid that cannot be read is a claim that it can. Classic pages them without a ceiling. Registered as both because the capability is present on both sides; the gate is the Guided door being stricter, not a missing feature, and it converges at L11. |
| `explore-impossibility-band` | Two-tier plausibility: impossible entries (hard floor/ceiling) get a repair proposal with the rows shown; improbable entries stay advisory | pages/02_EDA.py plausibility_check - one tier, advisory only | **both** | GUIDED-004. The band itself is core: ml/physiology_reference.py now publishes floor/ceiling beside p01/p99, and get_impossibility_band returns None rather than falling back to the reference interval, so both doors read the same two tiers from the same reference. What is Guided-only is the CHOICE card - Classic still renders the single advisory list, and converges at L11 with pages/02. Marked both because the capability being registered is the tiering, and Classic's check reads the tiered reference today. |
| `explore-rec-r1-plausibility` | EDA recommendation card: Physiologic Plausibility Check | ml/eda_recommender.py:267 (already engine) | **both** | Already engine code; the page only rendered it. Guided offers it as a pull affordance in the Explore palette — present, and never counted as a question. |
| `explore-rec-r10-baselines` | EDA recommendation card: Quick Baseline Models | ml/eda_recommender.py:541 (already engine) | **both** | Already engine code; the page only rendered it. Guided offers it as a pull affordance in the Explore palette — present, and never counted as a question. |
| `explore-rec-r2-missingness` | EDA recommendation card: Missingness Pattern Analysis | ml/eda_recommender.py:290 (already engine) | **both** | Already engine code; the page only rendered it. Guided offers it as a pull affordance in the Explore palette — present, and never counted as a question. |
| `explore-rec-r3-cohort-structure` | EDA recommendation card: Longitudinal Data Split Guidance | ml/eda_recommender.py:322 (already engine) | **both** | Already engine code; the page only rendered it. Guided offers it as a pull affordance in the Explore palette — present, and never counted as a question. |
| `explore-rec-r4-leakage` | EDA recommendation card: Target Leakage Risk Assessment | ml/eda_recommender.py:345 (already engine) | **both** | Already engine code; the page only rendered it. Guided offers it as a pull affordance in the Explore palette — present, and never counted as a question. |
| `explore-rec-r5-target-classification` | EDA recommendation card: Class Balance & Baseline | ml/eda_recommender.py:403 (already engine) | **both** | Already engine code; the page only rendered it. Guided offers it as a pull affordance in the Explore palette — present, and never counted as a question. |
| `explore-rec-r5-target-regression` | EDA recommendation card: Target Distribution & Outliers | ml/eda_recommender.py:378 (already engine) | **both** | Already engine code; the page only rendered it. Guided offers it as a pull affordance in the Explore palette — present, and never counted as a question. |
| `explore-rec-r6-dose-response` | EDA recommendation card: Dose-Response Trends | ml/eda_recommender.py:428 (already engine) | **both** | Already engine code; the page only rendered it. Guided offers it as a pull affordance in the Explore palette — present, and never counted as a question. |
| `explore-rec-r7-interactions` | EDA recommendation card: Stratified Trends by Demographics | ml/eda_recommender.py:458 (already engine) | **both** | Already engine code; the page only rendered it. Guided offers it as a pull affordance in the Explore palette — present, and never counted as a question. |
| `explore-rec-r8-collinearity` | EDA recommendation card: Collinearity Heatmap | ml/eda_recommender.py:483 (already engine) | **both** | Already engine code; the page only rendered it. Guided offers it as a pull affordance in the Explore palette — present, and never counted as a question. |
| `explore-rec-r9-outlier-influence` | EDA recommendation card: Outlier Influence Analysis | ml/eda_recommender.py:517 (already engine) | **both** | Already engine code; the page only rendered it. Guided offers it as a pull affordance in the Explore palette — present, and never counted as a question. |
| `explore-eda-corr-cluster-col` | EDA trigger: eda_corr_cluster_<var> (warning) | pages/02_EDA.py:338 | **classic-only** | Collinearity clusters. Offered via the collinearity heatmap card; not yet a question. |
| `explore-eda-low-dimensionality` | EDA trigger: eda_low_dimensionality (opportunity) | pages/02_EDA.py:1228 | **classic-only** | Raised from the macro-shape panel, which Guided does not render yet. |
| `explore-eda-opportunity-balanced` | EDA trigger: eda_opportunity_balanced (opportunity) | pages/02_EDA.py:563 | **classic-only** | Metric guidance; Train step. |
| `explore-eda-opportunity-clean-data` | EDA trigger: eda_opportunity_clean_data (opportunity) | pages/02_EDA.py:475 | **classic-only** | An encouragement, not a decision. Deliberately not surfaced: an interview that congratulates the user on clean data is ceremony, which the clean-dataset guard forbids. |
| `explore-eda-opportunity-high-np` | EDA trigger: eda_opportunity_high_np (opportunity) | pages/02_EDA.py:544 | **classic-only** | Model-selection guidance; Train step. |
| `explore-eda-opportunity-nonlinear` | EDA trigger: eda_opportunity_nonlinear (opportunity) | pages/02_EDA.py:525 | **classic-only** | Model-selection guidance; Train step. |
| `explore-eda-opportunity-strong-signal` | EDA trigger: eda_opportunity_strong_signal (opportunity) | pages/02_EDA.py:503 | **classic-only** | Model-selection guidance; Train step. |
| `explore-eda-skew-group` | EDA trigger: eda_skew_group (info) | pages/02_EDA.py:451 | **classic-only** | Feature-level skew grouping. Its action is a Preprocess transform; deferred to that step. |
| `explore-eda-target-skew` | EDA trigger: eda_target_skew (warning) | pages/02_EDA.py:395 | **classic-only** | The action is a target transform on the Train page, which is outside the explore window. Belongs to the Train step's pre-list. |
| `explore-eda-tda-loops` | EDA trigger: eda_tda_loops (opportunity) | pages/02_EDA.py:1300 | **classic-only** | Raised from the persistence diagram; requires the optional TDA extra and the macro-shape panel. Neither is in Guided. |
| `explore-lockbox-scoped-modeling` | Explore's model-fitting analyses and its dataset profile are scoped to training rows, with the scoping stated on screen | pages/02_EDA.py (train_row_mask; the profile and quick_probe_baselines) | **classic-only** | L9 narrow cut on CONTRACT-017. Classic's Explore step now runs the two paths that model - the dataset profile that drives the coach, and quick_probe_baselines, which fits models on its own 80/20 split - on train_row_mask(df.index) only, and says so in the same words pages/04 uses. Guided does not: turbotab/engine.py:166 profiles the whole working table, and the Guided interview does not yet ask for a lockbox at all (see target-lockbox-settings), so there is usually nothing sealed to exclude. This is Classic-ahead-of-Guided and a debt owed forward, not backward: when the Guided door asks for the seal, its Explore step has to arrive with this scoping already in place. The display analyses on both sides still see every row - deliberately, per the lockbox status line - and converge at L11. |

## Feature engineering & selection (Classic: pages/03, 04) — 18

| ID | Capability | Classic | State | Reason |
|---|---|---|---|---|
| `feat-pca-umap` | Dimensionality reduction: PCA and UMAP components as features | pages/03 tab 6 — method picker, n_components, compute | **guided-native** | PCA is BUILT as a DEFERRED spec - recorded now, fitted inside each training fold - which is a stronger position than Classic's, where pages/03 computes components over the whole table and writes them to the working frame. Filed guided-only rather than both because the two doors do materially different things under the same name, and calling that parity would hide the difference. UMAP is NOT built: it is an optional dependency the app does not install, and its absence is a recorded decision rather than an oversight. L18 DISPOSITION: guided-native. Not a missing capability — Classic HAS PCA, at pages/03, computed over the whole table and written to the working frame. Guided's version is a deferred spec fitted inside training folds. Calling this debt would say Classic owes itself a different implementation of something it already has; the difference is clause 06's design model, and that is what guided-native names. |
| `feat-selection-methods` | Feature selection by LASSO, RFE-CV, univariate, and stability selection | pages/04 — checkboxes per method, run, consensus across methods | **guided-native** | BUILT as declarations. Five methods (mutual_info, lasso, rfe, univariate, stability), each producing a spec that a per-model pipeline fits inside each training fold; turbotab/selection.py never selects. Guided-only, and the reason is the point: Classic RUNS selection and stores a result, Guided RECORDS what will be selected. Those are different objects, so this is not parity. L18 DISPOSITION: guided-native. Same shape, and sharper: Classic RUNS selection and stores a result, Guided RECORDS what will be selected. Both doors have the five methods. The difference is declaration-versus-execution, which is constitution 06 rather than a gap in Classic's feature list. The row's original text already said 'those are different things' and then filed it as debt anyway. |
| `feat-binning` | Binning a numeric column into ordinal or one-hot bins | pages/03 tab 4 — strategy quantile / uniform / kmeans, encoded ordinal or onehot | **both** | BUILT, and it SPLIT rather than resolved. `bin_fixed` (user-supplied cut-points) is row-local and executes now; `bin_quantile`, `bin_uniform` and `bin_kmeans` are stateful and defer. bin_uniform is the subtle one and defers for a reason worth keeping: equal-WIDTH bins look like fixed cut-points until you notice the range comes from the data, so one extreme value moves every other row's bin. bin_fixed REFUSES without edges rather than deriving them, because deriving them is the other transform. |
| `feat-categorical-encoding` | Ordinal encoding of categorical columns | pages/03 tab 7 — multiselect, encode | **both** | BUILT, also split. `ordinal_declared` (the user states mild < moderate < severe) is row-local; `ordinal_frequency` is stateful and defers. Clause 06 says default to deferral when unsure, so the derived form defers and the declared form is the one that executes. |
| `feat-column-transforms` | Per-column transforms: log(x), log(x+1), sqrt(x), x squared, x cubed, 1/x | pages/03 tab 2 — multiselect of six transforms applied per selected feature | **both** | BUILT in Guided as six row-local entries (log, log1p, sqrt, square, cube, inverse), each executing immediately with a receipt. Guided adds what Classic lacks: the DOMAIN is reported before applying, not discovered after. log of a non-positive value is undefined, and how many rows will become missing is a fact the user needs while choosing. |
| `feat-custom-interaction` | Custom pairwise interaction builder (A times B, A over B, A squared) | pages/03 tab 1 — Feature A / Feature B / operation, added one at a time | **both** | BUILT as `product` and `difference`, row-local. The targeted form Classic offers in tab 1: a researcher who wants ONE named interaction should not generate a full polynomial basis to get it. Carries explainability_cost 'high', which is Classic's own guidance made structural rather than left in an expander. |
| `feat-explainability-cost` | Each transform states its explainability cost before it is applied | pages/03 — every tab's 'What & when to use' expander carries an explainability impact rating and a when-to-SKIP | **both** | BUILT as a field on every catalogue entry, so it travels with the transform to the interface instead of living in a guidance expander nobody opens. Classic's own content, made structural: the interaction `product` carries 'high', a ratio carries 'low'. |
| `feat-missing-indicators` | Missingness indicator columns (is this cell blank) | pages/03 tab 7 — multiselect of columns, creates a binary indicator each | **both** | BUILT as `missing_indicator`, row-local: whether row i's cell is blank reads only row i. Deliberately separate from IMPUTATION, which is stateful and belongs to clause 07 and the missingness step - not this one. |
| `feat-ratios` | Ratio features from a numerator and denominator column | pages/03 tab 3 — pick numerator and denominator, queue several, create | **both** | BUILT as `ratio`, row-local. Promoted from a tab to a first-class catalogue entry because it is the most clinically meaningful transform here - BMI, eGFR, biomarker ratios - and Classic buried it behind polynomial expansion. Division by zero is reported as undefined rather than producing an infinity. |
| `feat-remove-one` | Remove a single engineered feature after creating it | pages/03:1177 — a per-feature remove control in the summary | **both** | BUILT as remove_feature, which refuses any column it did not create - removing a source column is not this step's to do. Cascades like an addition does. |
| `feat-selection-train-scope` | Selection sees training rows only, and says so on screen | pages/04:126-135 — mask = target.notna() & train_row_mask(index), with a caption naming n | **both** | BUILT, and Guided goes further while SAYING which is which. selection.evidence ranks on training rows only and records n_rows_seen; declare() takes an explicit scope with two values and no third - train_folds (Guided's) and train_rows (Classic's) - so a project can state which happened. Asking for scope='all_rows' raises 'no third option'. Stated precisely, because the improvement is easy to overclaim: Classic prevents the TEST-SET leak; Guided also prevents the fold-level one. |
| `feat-skip` | Skip feature work entirely and proceed with the original columns | pages/03:189 — an explicit Skip button, recommended for a first pass | **both** | BUILT as settle_features(skipped=True), which RECORDS the skip. Load-bearing rather than cosmetic: a silent skip is indistinguishable from a step nobody reached, and the transcript has to be able to tell those apart. |
| `feat-stale-cascade` | Adding or removing features invalidates downstream results, visibly | pages/03:1245 — Save triggers reset_downstream_results; pages/04 likewise | **both** | BUILT, and as a LIST NAMING ITS CAUSE rather than a boolean. findings_stale is cleared by recompute (correctly - findings really are recomputed), but models and metrics are not, so a boolean that cleared would assert the cascade had been dealt with when nothing had. stale_downstream records 'the column `age_x_bmi` was added', which is actionable where 'something changed' is not. DESIGN_LANGUAGE 10. |
| `feat-polynomial` | Polynomial features and interactions (degree 2-3, interaction-only option) | pages/03 tab 1 — sklearn PolynomialFeatures over the selected numeric columns | **classic-only** | NOT BUILT, and this is a deliberate scope decision with TWO arguments rather than one. The individual pieces exist and are row-local - `square`, `cube`, `product` - so a researcher can build any specific term they want, with a receipt each. What Guided does not do is GENERATE the full degree-2 or degree-3 basis. FIRST ARGUMENT, the interview's premise: degree 2 over ten numeric columns produces 55 new terms - 10 squares and 45 pairwise products - that nobody chose one at a time, each carrying explainability cost. Mass generation is the opposite of asking. SECOND ARGUMENT, and it is the harder one because it is arithmetic rather than taste: on the 140-row study fixture those 55 terms are p/n = 0.39, which is the overfitting regime. The expansion is most attractive on exactly the small studies where it does the most harm, so this is not only a stylistic disagreement with Classic. Clause 06 classifies the expansion row-local, so there is NO leakage argument for withholding it - stating that plainly matters, because a reader who assumes the refusal is a leakage refusal will misread the clause. THE GAP BECAME ROUTING at L16: asking for it by any of five spellings now returns the answer instead of 'unknown key' - if the question really is about interactions, use a model that captures them rather than columns that manufacture them. Trees and gradient boosting get interactions free, so it is a model choice at the modeling step, not a feature choice here. Pinned by test_asking_for_the_polynomial_basis_gets_the_routing_answer_not_unknown_key. Left classic-only so the difference is recorded rather than assumed. |
| `feat-reset` | Reset all engineered features and start over | pages/03:179 — a Reset button clearing the engineered frame | **classic-only** | NOT BUILT. remove_feature gives fine-grained reversal and covers the need; a bulk reset is a convenience whose absence is worth recording rather than a capability worth claiming. Cheap to add if a driver asks for it. |
| `feat-selection-consensus` | Consensus across selection methods, with a minimum-methods threshold | pages/04 — consensus_features(results, min_methods=N) | **classic-only** | NOT BUILT in Guided, deliberately, and this is the honest disposition rather than a quiet omission. Consensus across methods is a user decision (how many must agree) layered on top of selection, and it only means something once several methods have RUN. Under fold-local selection each fold produces its own consensus, which is a different and unsettled object - averaging agreement across folds is not the same as agreeing across methods. Left classic-only until that is designed rather than approximated. The spec carries `consensus_min_methods` so the field exists when it is. |
| `feat-selection-methodology-log` | The selection run is logged to the methodology record with its methods and threshold | pages/04:285-305 — log_methodology plus a record_feature_selection call | **classic-only** | NOT BUILT. Guided records every decision in its own append-only transcript, which the archive persists - so the information exists and survives. What is missing is the bridge into utils/insight_ledger, which is what the manuscript's methods section reads. Filed classic-only rather than both because Guided cannot yet produce that section, and claiming parity would promise a methods paragraph nothing writes. resolved-in-core; closes at L11 convergence of the manuscript step. |
| `feat-tda` | Topological data analysis features (persistence homology) | pages/03 tab 5 — homology dims, normalization, subsampling; needs giotto-tda | **classic-only** | NOT BUILT. Stateful (persistence over the whole point cloud) AND behind giotto-tda, an optional dependency the app does not install and which caps Python at 3.12. Recorded so its absence from Guided is a decision with a reason attached rather than a gap somebody notices later. |

## Preprocess (Classic: pages/05) — 25

| ID | Capability | Classic | State | Reason |
|---|---|---|---|---|
| `prep-comparison-caveat` | Choosing per-model preparation writes the comparison caveat into the manuscript automatically | NOT PRESENT. Classic's per-model pipelines produce the same confound and nothing records it, so the limitation reaches the methods section only if the user thought of it. | **guided-only** | BUILT at L18. AUTOMATICALLY is the load-bearing word: a caveat the user has to remember to add appears in the methods sections of careful people and nowhere else, and the careful people were never the risk. Recorded on the decision, drafted into Limitations, and asserted against draft.draft output rather than against the decision record - the record would prove only that the app knew. Not recorded under uniform, because a limitation the study does not have costs the reader as much credibility as omitting one it does. OWED BACK TO CLASSIC alongside prep-preparation-mode-question, which it is the other half of. |
| `prep-informative-missingness-question` | Ask whether missingness is INFORMATIVE before choosing how to fill it | NOT PRESENT. pages/05 offers imputation strategies and a missing-indicator checkbox, and never asks whether a blank could mean something. The mechanism question is not asked in either door. | **guided-only** | BUILT, and it did not exist in either door before L17. `Could a blank in X mean something?` is asked PER COLUMN and BEFORE the strategy, which is the ordering clause 07 requires - asked the other way round, a column that carried information gets a median written over it by a well-meaning default. A FACT that is never skippable, for the same reason as the grain: the app cannot know. `I'm not sure` is first-class and deliberately does NOT block. OWED BACK TO CLASSIC: pages/05 offers imputation strategies and never asks the mechanism. L18 DISPOSITION: guided-only, and it stays guided-only because it is REAL DEBT — Classic is expected to gain this, and the count is what makes 'we owe Classic N things' a number rather than a feeling. Lockbox constitution 07 binds both doors. pages/05 offers imputation strategies and never asks the mechanism. |
| `prep-outcome-out-of-imputation` | The outcome is never placed in the imputation model | NOT ASKED AND NOT ASSERTED on pages/05. MICE is offered as 'models each feature conditioned on others' with no statement about which columns are in scope. | **guided-only** | BUILT as a REFUSAL rather than a setting. Passing the target inside a MICE scope raises, and no acknowledgment gets round it - unlike the informative-missingness blocker, which a user may legitimately override. Clause 07 says it is a blocker in ANY configuration, so it is not offered as a choice. OWED BACK TO CLASSIC: pages/05 describes MICE as 'models each feature conditioned on others' and says nothing about which columns are in scope. L18 DISPOSITION: guided-only, and it stays guided-only because it is REAL DEBT — Classic is expected to gain this, and the count is what makes 'we owe Classic N things' a number rather than a feeling. Lockbox constitution 07, and it is the hardest kind of debt: Classic offers MICE with no statement about which columns are in scope, so the defect is reachable today. |
| `prep-preparation-mode-question` | Ask once whether each model gets the preparation it needs or all get the same preparation | NOT PRESENT. pages/05 configures a pipeline per model and never asks the question, so every Classic comparison is a per-model comparison and none of them say so. | **guided-only** | BUILT at L18. Per-model recipes are correct AND create a real methodological problem: if one model gets scaled transformed features and another gets raw ones, a win is ambiguous between the model and the pipeline. Asked ONCE because it is a property of the comparison rather than of any model in it, and asked AFTER selection because it is not a question until there is a set to ask it about. A CHOICE and never skippable - the engine has a recommendation and a recommendation is not certainty. OWED BACK TO CLASSIC: Classic already has the per-model pipelines that create the confound. |
| `prep-shelf-never-shortened` | Every model stays selectable; the judgment is carried by order and prominence, never by absence | PRESENT AND WHISPERED. ml.model_coach.model_viability already buckets every model into good/ok/poor with an evidence-bearing clause citing this dataset's numbers, and pages/06 renders the list in taxonomy order with the verdict as a badge. The ranking exists and the eye never reaches it. | **guided-only** | BUILT at L18 per PRODUCT_VISION 'the shelf is never shortened'. turbotab/models.py sorts by bucket, returns all three groups including empty ones, and carries the coach's clause verbatim rather than summarized. The only exclusion is structural - a classifier cannot fit a continuous outcome - which is the ladder's top rung, no legitimate use exists. OWED BACK TO CLASSIC: the bucketing is already engine code that both doors can read, so this is a rendering debt and not a capability gap, which makes it the cheapest row in this table to pay off and the one the product owner named by name. |
| `prep-stability-assumption` | Record the stability assumption - that missingness means the same thing at deployment - as a methods assumption | NOT PRESENT. No page records it; nothing in the manuscript surface carries it. | **guided-only** | BUILT. Stating a mechanism as informative records the stability assumption on the declaration - that a blank will mean the same thing wherever the model is deployed, which is not checkable from this dataset because missingness patterns are a property of how a site collects data. Recorded as an ASSUMPTION rather than a warning, and the distinction is the point: a warning is dismissed, an assumption is carried by a manuscript. OWED BACK TO CLASSIC. L18 DISPOSITION: guided-only, and it stays guided-only because it is REAL DEBT — Classic is expected to gain this, and the count is what makes 'we owe Classic N things' a number rather than a feeling. Lockbox constitution 07. No Classic page records it and nothing in its manuscript surface carries it. |
| `preprocess-bulk-missingness` | Per-column questions asked over SETS defined by an editable rule — one decision covering N columns, grouped by dtype, with evidence-driven exceptions and grouped rendered skips | No equivalent, and the shape of pages/05 forbids one: preprocessing is configured per column through Streamlit widgets, so a 300-column table is 300 widgets. Classic does not ask 300 questions only because it never asks the mechanism question at all. | **guided-only** | Built at L21 for GUIDED-029. DEBT rather than native by the L18 test - would a reasonable Classic still be missing something? Yes, and more sharply than most rows here: the remedy is not an interaction Guided invented, it is the p >> n specification this project already wrote and applied to transforms but not to per-column questions. Classic will need it the moment it gains a mechanism question, and it will gain one because clause 07 binds both doors. turbotab/bulk.py is UI-free and imports only pandas and numpy, so the convergence is a call-site change rather than a port. |
| `prep-ask-when-it-changes-the-answer` | A variant question is raised only when the two variants would produce materially different answers on this data | NOT PRESENT. pages/05 offers standard / robust / minmax unconditionally, on every dataset, for every model. | **guided-native** | BUILT at L18. Both variants are derived, compared, and the question surfaces only on divergence. The statistic for scaling is deliberately not 'are the numbers different' - they always are, by a constant - but whether the two put the columns on DIFFERENT RELATIVE FOOTINGS: the coefficient of variation of sigma over IQR across columns. Near-constant means one global factor no scale-equivariant model can detect; varying means heavy tails in some columns and not others. GUIDED-NATIVE: this is the interview deciding what to ask, and Classic has no interview to decide for. An operation with a pushed alternative and no comparison is RAISED, never suppressed - an unmeasured difference is not a measured sameness. |
| `prep-model-selection-here` | Model selection happens at Preprocess, and per-model preparation hangs off it | PRESENT BUT ELSEWHERE. pages/06 selects models at Train, and pages/05 configures per-model pipelines for models the user has not yet chosen - so the pipeline page offers a column per model in the registry and the Train page decides afterwards which of them mattered. | **guided-native** | BUILT at L18. Guided asks which models the user intends to train BEFORE configuring preparation, because per-model preprocessing has nothing to hang off until there is a selection. Train then becomes execution rather than choice. GUIDED-NATIVE rather than a debt: this is a sequencing property of Guided's interview, and Classic's page order is not something Classic is expected to change - moving model choice earlier in Classic would mean reordering pages other pages already depend on, which is exactly the STATE-101 problem one level up. |
| `prep-recipe-table` | Per-model preprocessing defaults expressed as a resolvable table - model x operation -> variant plus reason - rather than as branches | NOT PRESENT AS DATA. pages/05 derives per-model defaults in conditionals keyed on model family, which is correct today and cannot be extended without editing the page. | **guided-native** | BUILT at L18. turbotab/recipes.py resolves by selector specificity (exact model > capability flag > registry group > every model), and the model-determined layer is SEEDED FROM ModelCapabilities rather than hand-listed, so 22 models are not a second copy of a fact that already exists. GUIDED-NATIVE: DOMAIN_PACKS 02 says a pack supplies detectors, reference data, conventions and prose and never interface, which only holds if the defaults it adjusts are a lookup. Classic is not expected to gain a pack system, so this is Guided's design model rather than a debt. The extension point is proved by a fake pack in the suite that adds one operation and overrides one variant. |
| `prep-categorical-encoding` | Categorical encoding: onehot / target / ordinal, with the leakage caveat on target encoding | pages/05 per-model tab 3 - selectbox; target encoding's help says the pipeline uses cross-fitting to mitigate leakage | **both** | L18: Guided gained this through turbotab/recipes.py, and it is settled by a test rather than by assertion - a categorical-bearing table is driven through Guided over HTTP, the resolved recipe is executed, and both selected models fit on the unsealed rows only. Trees and boosted ensembles resolve to ordinal because they split on order and one-hot starves the splits at high cardinality; everything else resolves to onehot so no ordering is implied between categories that have none. The operation declares itself STATEFUL and says what the state is a property of - the rows the encoder saw - which is the sentence that tells a reader why it goes inside the split. The leakage caveat Classic renders as prose is now clause 06's litmus answer carried on the operation. |
| `prep-categorical-imputation` | Categorical imputation: most_frequent / constant | pages/05 per-model tab 1 - selectbox; 'constant' fills a placeholder category | **both** | BUILT as `impute_mode` (deferred) and `explicit_category` (row-local). Guided's default for an informatively-missing column is the explicit category rather than the mode, which inverts Classic's ordering, and clause 07 is the reason: an explicit Missing level preserves the signal while the mode destroys it. |
| `prep-missing-indicators` | Missing-data indicator columns, so a model can learn whether missingness is itself informative | pages/05 per-model tab 1 - checkbox 'Add missing-data indicator columns', help names the MNAR pattern | **both** | BUILT as `indicator`, and it is ROW-LOCAL by the clause-06 litmus: the new column is 1 where this row's own value is blank. So it executes immediately and posts a receipt, unlike everything else in this step. The underlying value is deliberately left blank - filling it too would be the imputation the user just declined. |
| `prep-numeric-imputation` | Numeric imputation: median / mean / iterative (MICE) / constant, with per-strategy guidance | pages/05 per-model tab 1 - selectbox with help text per option, MICE recommended above 5% missing (Rubin 1987) | **both** | BUILT as `impute_median` / `impute_mean` / `impute_mice`, all DEFERRED per clause 06 - a median, a mean and a MICE model are each statements about the column's distribution, so they are recorded now and fitted inside training folds. What Guided adds is that each carries a `because` in words, and that the strategy is offered only AFTER the mechanism question, because the answer to 'could a blank here mean something?' decides which of them are legitimate at all. |
| `prep-numeric-scaling` | Numeric scaling: standard / robust / minmax / none, with the model-family reason for each | pages/05 per-model tab 2 - selectbox; caption states trees are scale-invariant | **both** | L18: Guided gained this through turbotab/recipes.py. WHETHER to scale is a FACT the table settles from ModelCapabilities.requires_scaled_numeric, pre-selected with the reason rendered where the question would have been. WHICH scaling, once it happens, is a judgment about this data - standard and robust are put against each other and the question surfaces only when the two would weight the features differently against one another. minmax stays available and is never raised unprompted, which is 'push the notable, pull the rest' applied to variants. Classic offers all four unconditionally on every dataset. |
| `prep-outlier-handling` | Outlier handling: none / percentile winsorizing / MAD, with thresholds | pages/05 per-model tab 4 - selectbox plus number inputs for lower/upper percentile and MAD k | **both** | L18: Guided gained this through turbotab/recipes.py as a DATA-DETERMINED operation. Whether an extreme value is an error or a finding is a question about the study and not about the number, so the default is none and the question is asked. Percentile cut-points and a median absolute deviation are both computed across rows, so the operation declares itself stateful and is fitted inside the training folds. Classic's thresholds are configured on pages/05; Guided records the declaration and executes nothing, which is the honest state of a step where almost nothing runs. |
| `prep-pca` | PCA dimensionality reduction with an explicit interpretability warning | pages/05 per-model tab 5 - checkbox plus n_components, with a warning that SHAP will refer to PC1/PC2 and EDA-conditional justifications | **both** | BUILT at L14 as `pca` in the Features catalogue, DEFERRED. Classic offers it here at Preprocess with an interpretability warning; Guided offers it at Features with the same warning as `explainability_cost: high`. Same capability, different step, and the difference is deliberate: PCA is a feature decision the user makes, not a preprocessing default. |
| `prep-per-model-config` | Per-model pipelines: each selected model gets its own preprocessing configuration | pages/05 - one st.tabs entry per selected model, all settings keyed per model | **both** | L18: Guided gained this, and gained the question Classic's version does not ask. Each selected model resolves its own recipe from the table; under the uniform answer every model resolves against the first selected model's settings and each borrowed row says whose settings it is showing, because a user reading a linear model's recipe under a tree's settings needs to know why it says what it says. What remains CLASSIC-ONLY is the before/after pipeline preview, which is its own row. |
| `prep-power-transform` | Power transform: none / log1p / Yeo-Johnson | pages/05 per-model tab 2 - selectbox, with backward compatibility for an older log_transform boolean | **both** | L18: Guided gained this through turbotab/recipes.py as a DATA-DETERMINED operation, which means it is asked and stays asked. Whether skew is a measurement artifact or the phenomenon is a judgment about this study, and no confidence in the engine makes a judgment moot - so unlike scaling it is never pre-selected however certain the profile looks. Yeo-Johnson fits lambda from the column and is therefore stateful; log1p is row-local and lives in the Features catalogue, offered here as the null-lambda case for continuity. |
| `prep-double-transform-guardrail` | Warn when a feature already transformed at Feature Engineering would be transformed again | pages/05:161-180 - reads engineered_feature_transforms and auto-excludes those features from redundant transforms | **classic-only** | NOT BUILT at L17, and Guided has a structural head start: the project records `engineered` receipts and `deferred_transforms` as first-class lists, so 'was this column already transformed?' is a lookup rather than the session-state scan Classic performs. Recorded so that advantage is claimed rather than forgotten. |
| `prep-high-cardinality-guardrail` | Warn when a high-cardinality categorical would explode into one-hot columns, and record it in the ledger | pages/05:114-135 - unconditional warning above 50 levels, with an insight upserted to the ledger | **classic-only** | NOT BUILT at L17. It belongs with encoding, which is not built. Worth keeping as a row because it is a genuinely good Classic behavior - it warns unconditionally in both modes and records an insight in the ledger. |
| `prep-pipeline-preview` | Before/after preview of each model's pipeline output | pages/05:1336-1370 - an expander per model with Before and After subheaders | **classic-only** | NOT BUILT at L17. The preview a deferred transform is allowed is narrower than Classic's before/after: clause 06 permits a read-only preview NOT persisted to the modeling table and computed on TRAINING ROWS ONLY, which the Features step already implements as `_deferred_preview`. Extending it here is the same mechanism, not a new one. |
| `prep-plausibility-gating` | Domain-specific range filtering, clip-to-NaN or remove rows | pages/05 per-model tab 4 - checkbox plus a clip/filter radio; bounds from ml/physiology_reference.py | **classic-only** | NOT BUILT in Guided. Classic's version is the clause-01 impossibility pass in its post-seal form, and STATE-101 is the open row for the sequencing problem - the pass belongs BEFORE the seal and runs after it in both doors. Building it in Guided without moving it would inherit the defect. |
| `prep-smart-defaults` | Smart Defaults mode: auto-configure from the data profile and EDA findings | pages/05 - a radio between Smart Defaults and Advanced; auto-detects robust scaling when EDA found outliers | **classic-only** | NOT BUILT, and this one is a deliberate REFUSAL rather than a scheduling gap. Auto-configuring preprocessing from the data profile is pre-selection at a confidence tier the governing rule reserves for the user (PRODUCT_VISION 07.1), and clause 07's mechanism question cannot be auto-answered even in principle - it is knowledge the app does not have. A Smart Defaults mode that guessed the mechanism would be exactly the defect clause 07 exists to prevent. |
| `prep-unit-harmonization` | Unit harmonization - detect and convert mixed units before modeling | pages/05 per-model tab 4 - checkbox; ml/clinical_units.py does the conversion | **classic-only** | NOT BUILT at L17. Row-local by the litmus - a conversion factor applied to one row's own cell - so it belongs in the Features catalogue rather than here when it lands. Recorded so that placement is a decision rather than an accident. |

## data — 2

| ID | Capability | Classic | State | Reason |
|---|---|---|---|---|
| `elig-participant-flow` | An exclusion records its participant-flow numbers - N before, N excluded, N after, and the reason - as a methods sentence | NOT PRESENT. The plausibility filter drops rows and reports a count on screen; it writes no criterion, no reason, and nothing a flow diagram can consume. | **guided-only** | BUILT in Guided at L16. TRIPOD+AI names continuous-variable restrictions as an eligibility item reported in participant flow, so the numbers are the deliverable rather than a side effect. A restriction with no stated reason is REFUSED: participant flow reports how many AND why, and a criterion that cannot be written down as a methods sentence should not be applied. The excluded rows' LABELS are deliberately not stored - participant flow needs the count, and keeping the labels would be a second copy of the row identities of people who are no longer in the study. L18 DISPOSITION: guided-only, and it stays guided-only because it is REAL DEBT — Classic is expected to gain this, and the count is what makes 'we owe Classic N things' a number rather than a feeling. Same clause. Classic's plausibility filter drops rows and reports a count on screen, writing no criterion and no reason a flow diagram can consume. |
| `elig-question` | The eligibility question — is your study restricted to part of this data? — asked pre-seal in scientific terms, with the target's distribution withheld | NOT PRESENT. pages/01 and pages/02 have no eligibility step; the nearest thing is the pages/05 plausibility filter, which is a robustness trim and is post-seal, which is exactly the pair constitution 04 says are different objects. | **guided-only** | BUILT in Guided at L16 as `turbotab/eligibility.py` plus `state_eligibility` on the Router. GUIDED-ONLY, and it is a debt owed back to Classic rather than a Guided luxury: clause 04 binds both doors, and Classic currently has no way for a user to state an exclusion criterion at all, so a restriction they apply by hand in Excel before uploading is invisible to participant flow. The register's fourth state exists for exactly this. The withheld distribution is the part Classic would have to import as well - `permitted_evidence` returns observed min/max, missing count and impossible-value flags and nothing else, because an eligibility criterion comes from the research question and not from the histogram. L18 DISPOSITION: guided-only, and it stays guided-only because it is REAL DEBT — Classic is expected to gain this, and the count is what makes 'we owe Classic N things' a number rather than a feeling. Lockbox constitution 04 binds both doors, and Classic has no way for a user to state an exclusion criterion at all, so a restriction applied by hand before upload is invisible to participant flow. |
