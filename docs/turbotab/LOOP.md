# Running this as an unsupervised loop

The operator's manual: how a loop is shaped, how to run one safely, how to judge the report that
comes back, and what has already run.

**If you are taking this project over, read in this order:** `README.md`, `PRODUCT_VISION.md`,
`ROADMAP.md`, then this file's §02 (the shape), §06 (adjudication) and §03 (the log). The rest is
reference.

`data/findings.json` is the source of truth. `FINDINGS_LEDGER.md` is generated. The agent edits the
JSON through `tools/ledger.py` and regenerates the markdown — never the reverse.

```bash
python docs/turbotab/tools/ledger.py stats            # progress
python docs/turbotab/tools/ledger.py next --n 15      # next batch, as JSON
python docs/turbotab/tools/ledger.py set ID --status OPEN --note "..." --evidence "file:line"
python docs/turbotab/tools/ledger.py regen            # rewrite the markdown
python docs/turbotab/tools/ledger.py check            # schema guard; non-zero on violation
```

`check` enforces what makes the ledger trustworthy: no duplicate ids, no invalid status, **no
`FIXED` without a named regression test**, and no `PARTIAL` / `NOT-A-DEFECT` / `WONTFIX` without a
written reason. It runs in the pre-commit hook, not on discipline — see §05.

---

## 01 · Two numbering systems, and why they are not renamed

`L1`–`L12` in `ROADMAP.md` name **phases**. `L1`–`L29` elsewhere name **loops**.

They coincided through `L8`, when one loop completed one phase. They diverged at `L9` — the
interaction layer — which is one phase and many loops. **Every loop from `L13` onward sits inside
roadmap phase `L9`**, alongside the domain track that `ROADMAP.md` §"The map" now carries.

The collision is documented rather than renamed because the references number ~100 each way and
reach generated files and source. A rename is churn with breakage risk; this paragraph is the fix.

**Reading a loop number in the wild.** An `L` tag on a finding marks **where the defect was
observed**, not where it landed — `GUIDED-046` carries both `L18` and `L24` because that is one
cause with two occurrences. To reconstruct a loop's contents:

```bash
python3 -c "import json,re,collections; d=json.load(open('docs/turbotab/data/findings.json')); \
r=d if isinstance(d,list) else d['findings']; \
[print(f['id'],f.get('item','')[:70]) for f in r if 'L24' in str(f)]"
```

`VALUE_CHECK_ADJUDICATION.md` carries a section per loop that moved a routing metric, and it is the
most detailed narrative record that exists. It is **not** a complete loop log — it only sees loops
that moved the value check — which is why §03 exists.

---

## 02 · The shape of a loop

A loop is **one prompt, run unattended, reporting once.** The prompt is four parts, and the shape
emerged from practice rather than design — it is written down here so it stops being rediscovered.

| Part | Role | Why it is there |
|---|---|---|
| **A** | close the previous loop's gap | The adjudicator names one thing the last report left. Doing it first means an accepted loop never quietly carries debt into the next. |
| **B** | the substantial build | The loop's reason for existing. One thing, deep. |
| **C** | a second build, deliberately different | Two builds of different shape expose an abstraction's seams. One build never does. |
| **D** | a probe, an audit, or a refusal | Something that tests whether what exists holds, rather than adding to it. |

Each part is independently gated: it lands with its own tests, its own ledger rows, its own commit.
**A loop that completes three of four parts is a loop that produced three parts**, not a failure —
which is why the parts are ordered by what would hurt most to lose.

**Rules that make the shape work, all learned by breaking them:**

- **Name the source, not the goal.** *"Build the nutrition detectors"* hands the agent the job of
  deciding what the science is. *"Implement the Atwater reconstruction, source of truth
  `research/NUTRITION_PACK.md` §01, read that section first"* does not. See §04.
- **Two, never three, when testing an abstraction.** The figure spec shipped with exactly two
  figures on instruction, and the second exposed two seams the first did not. A third before the
  spec survived both would have hardened the wrong shape.
- **And stop metering once it has stopped bending.** The rule above is about *discovery*, and read
  as a general cap it is expensive. The packs specify **eight** signature figures (`DOMAIN_SCIENCE.md`
  §02), **ten** import detectors (§03b) and ~150 anti-patterns, all written down to the annotation
  and the threshold. Releasing those two per loop is a schedule invented by the prompt rather than
  by the work. **The test for which phase you are in is not a count — it is whether the last example
  bent the abstraction.** L27's third figure did (`annotations` bent, `tier` did not fit), so figures
  were still in discovery at three; the next one of a *shape already seen* is fill-out, and a
  fill-out part should carry as many instances as the agent can hold. Product owner's framing:
  *"why stop at 2 plots when you know they could make all n plots that work well for a given card."*
- **Order a batch hardest-first.** The corollary that keeps a widened part honest. Five instances
  built easiest-first are five castings of a shape nobody stress-tested; built hardest-first, the
  seam shows while four are still unwritten. Judge "hardest" by what is most likely to break the
  abstraction, not by effort.
- **A mechanism the codebase has zero instances of is unpriced until the INSTRUMENT is checked.**
  `L54-B0` asked for identity-preserving DOM writes, and the prompt quoted `DESIGN_LANGUAGE.md`
  §05.2's measurement — **0** `startViewTransition`, **0** FLIP, **0** `.animate()`, **0**
  `transitionend` — as the argument for building it. Nobody drew the second inference: the harness
  is a shim built to run the page, so **an API the page never used is an API the harness never
  implemented.** `TEST-066` is the bill — `insertBefore`, `replaceChildren` and `hidden` absent, and
  `appendChild` **copying rather than moving**, which would have silently invalidated the identity
  proof itself. The same table was both the motivation and the warning, and the adjudicator read
  only the half that fit the argument. **First act of such a part is to verify the test instrument
  supports it**, and that reconnaissance belongs in the prompt as its own numbered step.
- **Do not sequence certain plumbing behind unpriced discovery.** `L54` ordered the big build (B)
  ahead of a purely mechanical part (C: carry three recorded fields into one function signature).
  B's discovery overran, and **C — which had no unknowns at all — was the part that died.** Order by
  what would hurt most to lose, but when one part is pure plumbing and another contains a genuine
  unknown, the plumbing is cheap insurance and goes first. A loop that ships three certain parts and
  one honest overrun beats a loop that ships one part and a scope note.
- **Say what not to build.** Every loop since `L25` has carried an explicit *deliberately unbuilt*
  clause. Agents finish things; the instruction to stop is load-bearing.
- **A part may be REFUSED when carrying it out would violate the criterion the row itself states, and
  the refusal is a result rather than a failure.** `L56-C2` is the model and it is a standing
  permission, not an exception. The prompt instructed a palette swap and supplied the replacement as
  measured-and-passing at worst CVD ΔE **20.0**; the agent measured **2.0** under deuteranopia, and
  building it would have shipped a chart asserting CVD-safety while being unreadable for roughly 1 in
  12 men — **the row's own defect committed by its repair.** Two halves make the refusal correct and
  both are required: it **measured** rather than argued, validating its simulator on controls first;
  and it **declined to substitute its own palette**, because choosing one is a product decision and a
  unilateral substitution is the same act in the other direction. **Refuse, measure, file, and hand
  the decision back — do not quietly build a weaker version, and do not quietly build your own.**
- **Every number a prompt states carries how it was derived.** Four of `L56`'s premises were false and
  all four were the adjudicator's, including one where the existence of two data files was checked and
  their *contents* inferred. A number with no provenance is a claim the loop will spend rounds
  discovering is wrong, and it spends them in the parts labeled *certain*.
- **Scope note at the end.** When one part is allowed to run long, say which and what may be traded
  against it. *"If C runs long, ship the figure with fewer annotations rather than dropping the
  refusal."*

---

## 03 · The loop log

Terse by design. The narrative lives in commit messages, `VALUE_CHECK_ADJUDICATION.md`, and the
findings themselves; this is the index that makes them findable.

| # | What it did | Why those parts together |
|---|---|---|
| L1 | Verified 370 findings against current code | A backlog nobody has re-read is research, not a plan |
| L2 | Three live Streamlit bugs | Folded into L7 |
| L3 | Walking skeleton — upload → diagnosis → findings → decisions | Prove the architecture before building on it |
| L4–L7 | Characterization tests, AnalysisProject + DAG, split block, detaint + job queue | The engine had to be safe to call before a new door could call it |
| L8 | The Router, EDA only · **value check passed** | Routing is the differentiator; measure it before building on it |
| L13–L17 | `GUIDED-011`–`021` — early Guided hardening | Reconstruct via the snippet in §01 |
| L18 | Split "whether to scale" from "which scaling" (`GUIDED-022`) | Determinacy is a property of a **question**, not an operation. Added the `constitutional` question category rather than moving the coverage denominator |
| L19 | The lens becomes the third constitutional question | |
| L20 | The lens reaches `rank_findings` (`GUIDED-024`–`029`) | Reframing annotates and never deletes — a lens at generation would make presented and executed diverge |
| L21 | Pack benefit fell because the comparison became fair | A metric moving the wrong way for the right reason is recorded, not corrected |
| L22 | `GUIDED-031`–`033`, found by driving | |
| L23 | The drive bugs — `DRIVE-001`/`003`/`004`/`011` | The product owner drove the app; the lens was unreachable |
| L24 | Bulk repairs (`DRIVE-002`) — nine cards, one decision | Value check penalized the improvement twice; recorded, no threshold moved |
| L25 | Purpose question (`GUIDED-048`), evidence badge (`047`), sixth axis (`045`), SMOTE defect (`049`) | The spine primitives everything downstream needs, plus the first anti-pattern audit hit |
| L26 | Prereg Amendment 1 (`050`), figure spec + two figures (`051`), promotability (`052`), study-scoped finding (`053`) | The figure spine, tested by two deliberately different figures |
| L27 | Nutrition pack — Atwater, NHANES design, shrinkage plot, the EAR/AI **refusal** | The reference implementation. A pack that can only add findings has not been tested. **Accepted; all four parts correct and tested, and none of it is reachable from an upload** (`GUIDED-058`). The calibration fix is the part that ships — `weak_calibration` is live in Classic via `pages/06` |
| L28 | Reachability — the badge reaches the claim, the figure layer gets its first consumer, the detectors reach an upload, the refusal is probed from outside | L27 built content nothing could reach. **Accepted; `GUIDED-057`/`058`/`059`/`060` closed**, and an upload now draws two figures and triggers four refusals, verified through the API by the adjudicator rather than through the loop's own tests. The loop's sharpest finding came from the wire and no unit test could have: the dietary pack read a 107 kg body-weight column as a survey weight and asserted a partially-specified design on a clinical table. **Two seams found by widening, not by building** — `evidence.py` grew from one file to an AST walk over every emitter, and the `Callable[[df], Optional[Dict]]` detector contract was **split rather than widened**, on the argument that widening rests on one example |
| L29 | Three more figures, the matcher that read the wrong columns, and the first anti-pattern audit | **The first widened loop** — three figures at once rather than two, ordered hardest-first, on the rule added to §02 this cycle. It worked: the volcano bent the spec exactly where predicted (a precondition on *upstream data state*, which `when_applicable` cannot express) and the spline bent nothing, which is the other half of the result. Five of six figures now reach a user. **The audit is the headline**: 85 anti-patterns checked, 4 hit, and `AUDIT-001` is the SMOTE defect's exact shape — the generated manuscript reports how many tests were significant at raw *p* < 0.05 and names no correction |
| L30 | The audit turned on the app — the manuscript's uncorrected count, the leaking baseline, the exits that named no key | **The loop where the research stopped being content and became a bug report.** `AUDIT-001` removed a false statistical claim from the *generated manuscript* and `AUDIT-002` closed a real leak in the quick baseline, both in code Streamlit users run today. `GUIDED-064`/`072` fixed a class I had half-named: the machine-readable form was lossier than the sentence, in a badge and in a 409's exit. **The most valuable line was in prose and is now `AUDIT-008`** — four of the last four hits share a shape the registries were never written to find: the core already holds the capability and the path that needs it does not read it. Also the loop where **I** broke §05 by running `git add -A` mid-loop; see the rule added there |
| L31 | The recipe lattice prototyped, the purpose-blind refusal, the capability sweep, and the frontend harness | **The loop that measured a design question instead of arguing it.** The lattice prototype changes 6 of 16 cells when the lens moves — reproduced by the adjudicator — and establishes that whatever renders it must mutate cells in place, because `transitioncancel` never fires on a removed element, so a wholesale repaint cannot report what it interrupted. That narrows `GUIDED-073` without closing it. **And the harness earned itself on its first run**: the page never fetches `/figures` (`GUIDED-075`), which two loops of API verification — mine included — structurally could not see |
| L32 | The figure surface, the server's exits, two Classic leaks, and the copy deck walked | **The money maker became visible.** `GUIDED-075` closed and the adjudicator drove it — the page's own controller, live API responses, a real dietary project — and the figure surface renders three admitted figures with annotation boxes, scored checklists, captions and badges. `GUIDED-076` found something worse than discarding: `new Error(object)` stringifies to `[object Object]`, so a 409's exits were destroyed at the throw. **And the loop named the door's dominant defect class from three instances** — the server composes a user-facing string and the interface never renders it. Measured at `GUIDED-080`: six surfaces, and the sharpest is `nutrition/prevalence`, the refusal apparatus the whole domain-track ordering was justified by |
| L33 | The class closed — the prevalence refusal, the seal's basis, the Features step, the lattice, and a standing check | **The dominant defect class stopped being a finding and became a gate.** Five surfaces the server composes and the page never read now reach a person: `nutrition/prevalence` (the four refusals `LOOP.md` §04 says the whole domain-track ordering was justified by, plus the shrinkage plot the single-day refusal offers), the `disclosures` field, `/features` and `/selection/evidence`, and `/recipes`. **The seal was the closest to a governing-rule failure**: it tells a user whether their holdout was drawn by person or by row, and §03's `undetermined` had no renderer at all. Two findings the loop made rather than fixed: `GUIDED-081`, the harness silently dropped every `className` write, so any assertion about styling came back **passing** — the §03 claim would have passed against a page that got §03 exactly backwards; and `GUIDED-084`, `/capabilities` is served precisely so the page cannot claim an affordance the server lacks, and the page computes its own. **The measurement was wrong and the check corrected it**: `evidence/correlations` and `evidence/histograms` were counted unread because `runPull` composes their paths from a variable and no literal grep can see them — seven routes unread, not nine, five of them read by another door |
| L34 | The instrument fixed, then Preprocess, Train and the seal probed | **The loop where the harness stopped lying and immediately found an orphan.** `GUIDED-077`'s two holes closed at the source, and the sweep's first result was that L33-D's lattice claim had been measuring a node the page never attached — `latticeBox` held 0 characters while `latGrid` held 179. Three surfaces were repainting behind the same blind spot. **Two steps reached their end**: Preprocess, with §07's fork asserted as a property (no strategy on screen until the mechanism is answered), and Train — the first number this door computes rather than reads, on a watchable job with a cancel that stops it. `GUIDED-065` closed: the calibration plot is drawn from held-out predictions, eight loops after it was specified. **And the seal was probed rather than asserted for the first time** — seventeen probes, the fit clean to the last bit, and the leak beside it: `GUIDED-088`, the model shelf ranked on the whole table, so the order a user picks from was computed with the held-out rows in view. Nobody had thought of a ranking as a parameter estimated from data. It is one. Two more found by driving rather than reading: a categorical fill silently retyped a numeric column (`GUIDED-086`), and the blocker's safe exit rendered disabled beside a live *do it anyway* (`GUIDED-087`) |

| L35 | The connective tissue — a recorded decision reaches what it was recorded for | **The loop where the record stopped being a place decisions go to rest.** `pipeline_plan.py` is the executor clause §06's deferred half never had: the fold-fitted pipeline is composed from the missingness declarations, the recipe variants and the deferred transforms, with the sentence and the pipeline asserted as **identity** rather than agreement. Driven, not read — a user who answers *informative* and picks *indicator* gets a fit that leaves the blank on a model that reads one, and on `logreg`, which cannot, the run says *"For Logistic Regression only, missing values in `mz_0003` were filled with the training-fold median, because Logistic Regression cannot be fitted around a blank"* — per model, carrying both the recorded sentence and the true one, and it reaches the page. Undeclared columns are **counted** rather than dressed as choices. Three revert probes, each failing for its own reason. **The probe is the other half of the loop**: 41 decision kinds enumerated — the adjudicator's grep had said 36 — 23 probed, 6 reaching the fit, 11 allow-listed and 7 unconstructible, every reason in the file and the partition asserted exhaustive. On its first run it found `GUIDED-099`: the pack recipe table is process-global, so a clinical project sharing a process with a metabolomics one was fitted with Pareto scaling it never asked for — *a display defect until Part B made the trainer read the table*, which is the sharpest illustration of what wiring a capability does to the cost of a latent one. **And the loop declined to decide `GUIDED-096`**, correctly: post-seal Explore reads the held-out rows, five surfaces move under a poison probe, and two of them are orders a user picks from. It registered the exemptions so they fail if they quietly become masked, and found that `PRODUCT_VISION.md` §04b already draws the line for a different question. Accepted; `GUIDED-095` reopened to `PARTIAL` by the adjudicator because feature selection declares `scope=train_folds` and nothing fits it |

| L36 | The journey reaches its end — selection fits, Explain, Report, and a run that goes stale | **Upload to manuscript, without leaving the Guided door.** Selection became the last recorded decision to reach the fit, closing `GUIDED-095`: the selector sits between the shape stage and the model, probed by moving the held-out values and watching the chosen columns not move. Three defects it found rather than reasoned about — the selector ranked the shaped matrix and chose a top-3 from 244 one-hot columns rather than the six nominated; pandas output raised on a name collision numpy had hidden; and `mutual_info_*` draws from numpy's global state, so the same project selected different columns depending on what ran first. **Explain kept the register's promise** — it names which Features decision made the ranking harder to read, rendered *above* the table, because a reader who learns afterwards that the rows are principal components has already read it. **Report made the transcript the manuscript**: the preprocessing plan reaches the methods section quoting the record's own string, safe only because L35-B made that string true of the fit. **And the veil is per artifact rather than a flag** — a selection can be stale while a run fitted afterwards is not, and the explanation refits per request so its ranking is never stale while the scores beside it can be. Accepted; `GUIDED-104` filed beside it — the app records `train_folds` and fits `train_rows`, then corrects itself in a note, when `selection.declare` exists precisely so the weaker claim can be *said*. The loop also caught its own revert probe passing when it should not have, which is now the standard |

| L37 | The seal says what it can resolve, the fork was routed not built, and two coverage gates | **Three of four parts existed because a document said something the code did not.** `GUIDED-102`: the seal drew a constant 15% and stated only that the rows were held out — at n=80 that is 11 rows and a C-statistic on 11 rows answers nothing. The trigger is **derived, not picked**: the widest 95% interval this holdout can produce against the whole distance from a coin flip to a perfect classifier, `2·1.96·0.5/√n > 0.5 ⟺ n < 15.37`, and it fires on one of six fixtures — `PRODUCT_VISION.md`'s own worked case and nothing else. A third candidate trigger was **measured and rejected**: *parameters > training rows* is true arithmetic that fired on five of six, dominated by per-row identifier columns handed to the model as predictors, and was filed as `GUIDED-108` rather than laundered through a card. `MISC-014`: §03's claim that the sensitivity fork was *"absent from the app entirely"* was false — ~700 lines ship in Classic — corrected in place with the lesson attached, *a capability that ships in one surface is not absent, it is unrouted*, and neither of the Classic page's landmines inherited. **The gates found more than the findings did**: seven primitives measured where four were named, turning up a fifth with zero rows anywhere (figure tiering), and eleven pages where two were named. `GUIDED-109`–`112` filed. **Four existing guards caught this loop's own work** — the evidence gate refused five invented source anchors, the archive guard refused a serialized class label, the spelling gate refused a commit, and the surface guard failed on `/sensitivity` being served to nobody, which is `MISC-014`'s own shape arriving inside its fix |
| L38 | The resampling engine, the manuscript as data, and a refusal | **Three of six parts, reported plainly as three.** `GUIDED-103` closes: the whole pipeline including selection refits over `B` resamples, `B = 200` stated in both captions beside the 1000 Riley & Collins recommend. **The revert probe found a defect by not failing** — the first version passed each resample's seed to both the plan and the estimator, produced a convincing 17 distinct feature sets across 20 resamples, and the probe that should have destroyed it did not, because the signal was the estimator re-breaking ties on identical data. *An instability plot is an attribution claim*, so everything that is not the sample is held fixed now, and the finding closes on a **pair** because only a pair is evidence. §A4.8 writes "MAPE" unexpanded and the percentage reading returned 658% on risks near 0.02, so it reports in the prediction's own units. **Wiring the validator found three failures that are not formatting**: the methods prose states no analysis population, reconciles no split counts, and states no final predictor count while the abstract asserts one — `AUDIT-001` in the artifact that leaves the building, and `GUIDED-116` names the cause, that `draft.py` folds over decisions while both missing sections describe a *run*. **Kaplan–Meier refused and the refusal is the result**: the app cannot represent a time-to-event outcome, no survival target type was invented, and the entry names the anti-pattern before the build — §A4.6 is `SETTLED` that 1 − KM overestimates cumulative incidence under competing risks. Accepted; `GUIDED-104` **not** upgraded, against the prompt, and the agent was right — resampling measures how stable a selection is and does not make the reported model fold-local |

| L39 | The reassuring figure, the finished manuscript, and the zero that is the finding | **Three of four, with the fourth traded whole rather than half-built.** `GUIDED-114` closed by *measuring* the thing the finding predicted: the row bootstrap reports **21.4% less median interval width** than the cluster draw on `clinical_longitudinal.csv` — the understatement, in the predicted direction — and the scheme is now disclosed in the payload, both captions, the page and a checklist item, with a **second revert probe that draws clusters correctly and says nothing, and fails.** `GUIDED-108` closed by driving: a one-hot `record_id` plus a decision tree fits a coin flip at 1.00 apparent accuracy and collapses to one constant prediction on unseen ids. **Driving also overturned the adjudicator's premise for that row** — `is_id_like` needs an integer dtype and answers `False` for all four string columns in the row's own evidence (`GUIDED-120`), and *unique-per-row* is not the rule either, because it flagged 88 `mz_*` columns, the study's actual predictors. **The export audit changed the answer to a question the prompt only implied**: the first `to_latex` passed 9 of the exporter's 22 arguments while the app held seven more, so a Guided manuscript shipped a methods section and an abstract and silently dropped the metrics table, the predictor list, the recorded limitations, the importance ranking and the resampling results — *nothing failed; the document was thinner than the analysis.* **And the multiclass sweep's headline is a zero**: 14 surfaces driven, 11 correct, 3 silently wrong, **0 refusing** — nothing anywhere declines a multiclass target, so every failure looks exactly like a right answer. The sweep carried a binary control, which is what caught `GUIDED-124`, a defect that has nothing to do with multiclass |

| L40 | The trigger learns its arity, Table 1, eight figures, and the companion that was never registered | **Four of four, accepted, and the find is the best in twenty loops — but its characterization was wrong in the direction that flatters it.** The report said an unresolvable companion *removed* the calibration figure for six loops; **driven, it degraded it.** The figure was computed, rendered on the page with its title, caption and all seven annotations, under a `fig held` warning naming a companion `discrimination` that has never existed — so the app spent six loops telling every user it was withholding its flagship clinical figure pending a figure it does not have and no user action could supply. That is the **assert-something-false** branch of the governing rule rather than the silent one, which makes `GUIDED-128` worse than reported, not better. **`GUIDED-065` stands correctly closed**: *drawn* and *admitted* are different predicates, its test asserts the data path it was filed about, and scoping that test to `admitted` would have coupled a data-path regression to the companion registry. **The guard that should have caught it is the one that hid it** — the companion test satisfied the admitted branch with `{"discrimination": {}}`, a bare dict key `bundle()` skips on a line marked `# pragma: no cover`, so the test manufactured the production object whose absence was the defect. Named in a code comment, not filed; filed by the adjudicator as `GUIDED-134`, and it is **not** the guard-testing-its-own-description class — the assertion is right and the *fixture* is wrong, which is why reading assertions never finds it. **And the layer under all of it**: `GUIDED-131`, the admissibility rule has no consumer at the boundary it was written for — a held CONFIRMATORY figure promotes into the manuscript with `passed: True` and the document silent, because `promote_figure` reads the whole registry and never reads the bundle. `GUIDED-124`'s widened scope **accepted** — twelve literals, not two, resolved by derivation rather than by twelve edits, which is the right answer to a ruling that undercounted. `GUIDED-125`'s threshold move **accepted under §06.2**: the gated *quantity* changed, no assertion was relaxed, the one test edit preserved the count's history rather than shrinking a denominator, and the arithmetic re-derived independently — 15.4 / 8.6 / 6.8. **The zero that was reported as closed is still open one surface over**: `RANKS_AND_STATES` came back 0 because all seven surfaces swept are *output* surfaces and the middle rung is a property of *choice* surfaces — driven, the model shelf ranks a three-class target byte-identically to a binary one (`GUIDED-132`). Also `GUIDED-133`, found one line past L40's own net-benefit rounding fix: the decision curve's risk rug is capped at the first 200 rows in row order and nothing says so, on a figure whose whole argument is that a reader can tell 3 patients from 300 |

| L41 | Eight clinical detectors, the survey pair, the stand-in sweep — and two critical reachability defects found on the way past | **Four of four, accepted — and the loop's finding is not in the four parts.** Going outside them twice was correct and both were blockers for the part they were found in: `GUIDED-139`, `nudge()` deleted at `DRIVE-006` with seven call sites outliving it, so **every pull affordance in the door threw `ReferenceError`** — *push the notable, pull the rest* is one of five design principles and its half was dead; and `GUIDED-142`, **no pack finding in any pack had ever been rendered** — five packs, eighteen detectors, computed, served on `/project/{id}`, invisible. `DOMAIN_SCIENCE.md` §03's own correction says the content bin *is* the product and that scaffolding is what a researcher does not pay for; the content had reached zero users. **Two criticals, one loop, both reachability, both found by accident while doing something else** — which is the real result: the parts were fine and the door was not. Driven by the adjudicator on `clinical_labs.csv`: 8 of 8 clinical findings reach `profList` carrying SETTLED and CONVENTION badges. **And the drive corrected the adjudicator first** — a stubbed `interview?step=explore` left the page with no Explore step to paint into and `profList` came back empty, which is the absence-claim trap the handover names, arriving on the loop that had just closed the stand-in version of it. `GUIDED-143` **ruled and split**: the false assertion stops next loop via the lockbox constitution §03 pattern — a basis that states what was actually drawn, the way `undetermined` is first-class and never rendered as a clean lock — and the chronological grouped draw is a separate build, so the row stays `OPEN`. **The question is not removed; that would shorten the shelf.** `GUIDED-144` **ruled**: the conservative count stays, 140 mmHg is not adopted (§A1.2 is explicit that reference intervals are not disease thresholds), and reading the core rather than forking a second bounds table was right — **the defect is one layer down and is `MISC-018`**, `ml/physiology_reference.get_reference_interval` returns a p01/p99 pair under a name CLSI EP28-A3c defines as the central 95%, and `get_impossibility_band`'s own docstring says *improbable* where the function name says *reference interval*. The code knows what it holds; the name asserts otherwise, to both doors. **The divergence line corrected the adjudicator a fifth loop running, twice**: `GUIDED-135`'s premise was compressed rather than wrong — the checklist had passed against two numpy arrays and never against a payload from a project, which the row's own evidence said and the prompt's summary dropped — and the companion count is seven, not the four the prompt asserted |

| L42 | The field granularity, the delegated controls, and three standing checks catching their own author | **Four of four, accepted — and the report's headline is that the loop's own subject caught the loop.** Three checks fired during verification and all three would have shipped: the archive whitelist dropped the four `temporal_*` keys A1 had just built, so a restored project would show **a clean grouped seal over a holdout the user asked to have drawn chronologically** — the worst shape this archive has, because the keys that survive a lossy save are the honest ones; A1's tree sweep asserted only absences and would have reported a clean rename over a tree it never read, caught by `test_an_absence_assertion_carries_a_positive_control`; and a Part A script wrote `findings.json` at `indent=2` where `ledger.py` writes `indent=1`, so four commits carried an 18,844-line phantom diff over 38 real ones. **Nothing caught the third, and the adjudicator had done it first** — commit `5881aa8` carries a `docs(turbotab):` subject and 18,554 reformatted lines. The rule is now in §05 and `AGENT_ONBOARD.md` §06: *a file a tool owns has exactly one writer.* **`GUIDED-145` is the loop's best find and it is a third variant of the green-test-over-a-broken-thing class** — `test_temporal_prediction_routes_to_the_chronological_strategy` asserted a composer's string and its sentence, both true, and the word *routes* supplied a claim no assertion touched. Not trap #2: there the assertion is about the description; here the **name** promises what the body does not check. **B measured the gap it was built for**: 2,117 fields on the clinical lens, 358 reaching a person, 35 families with a fully-unread shape, 19 exempt with a named reader, 16 filed. Enumeration derived and tested by adding a live field. **And Part C — the tradeable part, not traded — is what established that B is insufficient**: B's positive verdicts are sound (a sentinel in the DOM is a sentinel in the DOM), its negatives rest on group-negative bisection, and its blind spot is a sentinel appearing inside an error render — which is `GUIDED-139`'s exact shape and which only C catches. **Neither instrument subsumes the other, and that is only knowable because the tradeable part was kept.** `GUIDED-147` **ruled one row** with two conditions — its `item` says fifteen and its `ev` lists sixteen, in a row whose whole content is a count; and it splits when someone looks rather than closing on a verdict over fifteen unexamined things. `GUIDED-140`'s static half **ruled wanted**, on the agent's own number: six of `nudge`'s seven call sites were on paths nothing drove, so the driven half catches one in seven. The unreachable `chronological_grouped` basis **stays** — §03's *three states, never two*, and a basis set omitting the honorable state cannot say the app is missing it — but its `# pragma: no cover` becomes a test, because a pragma is a claim with no guard and this project has been bitten by one before. **And re-counting the report produced `TEST-040`, which is the only place the report and the measurement disagree and the report is not at fault**: `turbotab/` came back 1 failed / 1422 passed against a reported 1423 / 0, on the same commit with the same ordering, because four job-polling sites spin a bounded loop with **no wait** and assert `status == "done"` — so under load the suite reports *the app had not answered yet* as *the app answered wrong*. The count this project reports every loop is load-dependent, which makes it a record that cannot settle anything; `TEST-030` is the same axis producing false passes instead of false failures |

| L43 | The anti-pattern audit run against shipped code, the chronological draw, and four of nine divergences that were findings | **Four of four, accepted — and the divergence section stopped being a caveat list and became a source of rows.** Item 1 is `TEST-042`: an `IndentationError` committed **green**, because none of the five pre-commit gates parses Python — and the loop's own new guard reads source with `read_text` and regexes, so *a file that does not parse is still text and still matches.* The generalization is the agent's own and it is the grep lesson at the gate layer: **a check whose input is a serialization of the thing it cares about will pass on a broken input.** Item 6 became trap **#3c** — `test_low_epv_keeps_lineup_small_and_cites_numbers` asserted `"class weights"` on a low-EPV profile, precisely where §A5.2 says the remedy is penalization, so **fixing the defect required a passing test to fail**; a loop that chased the red would have reverted the correction and recorded the revert as a success. Item 4 became `TEST-043`, three self-referential guards in one loop. And item 3 is the best use of the revert probe this project has recorded: removing the `temporal=` argument left the lockbox still reporting `chronological_grouped`, because `seal_lockbox` read *what was asked* rather than *what was drawn* — the probe found `GUIDED-143`'s own defect **inside its fix**, and the draw outranks the answer now. **The audit's headline is `AUDIT-014`**: `GUIDED-049` is `critical`, `FIXED`, and its fix reached **three of ten shipped surfaces** — `ml/publication.py` still shipped the removed sentence character for character, and a derived sweep found an eighth surface the original agents missed. **The class was not filed and the adjudicator filed it as `MISC-019`**, because it is L42's granularity lesson arriving at the ledger itself: `ledger.py check` enforces that a `FIXED` row *names* a test, and a named test proves the fix works at the site the test covers — so a row describing ten surfaces whose test covers three passes the gate and reads as done, with 286 of 758 closed and nothing telling one from the other. **Two corrections to the report, both in the rows.** `AUDIT-015`'s cause is *not* `GUIDED-131`: at `f2266a9`, the commit that wrote the gate, `figures` was already the whole registry, so **the gate was a tautology on the day it shipped** — trap #2 at birth rather than after drift, which is the harder version because there is no before-state in which it worked. And `GUIDED-049`'s note recorded none of its own incompleteness, corrected in place. Also `GUIDED-148` `high` — `/recipes`, 50 fields, **every one unread**, which is `GUIDED-142`'s shape at a route the route-level check cannot see; and `TEST-041`, the ordering twin of `TEST-040`, where **the full-suite green is the false one** |

| L44 | The sixth gate, eleven of twenty-one, and the first measurement of how much of *closed* is closed | **Four of four, accepted — and two of the loop's corrections are to rows the adjudicator filed or amplified.** `MISC-019`'s sweep put a number on the project's own progress measure for the first time: **124 closed rows examined, surfaces re-derived from code, 116 complete, 4 partial, 3 partial-claims refuted, 1 undecidable — a 3.2% partial rate**, with the frame stated (61 critical plus 63 plural-item, 161 unexamined) and the right caveat attached: *a rate measured on the highest-yield slice is an upper bound on the remainder, not an estimate of it.* Reassuring, and it is the first time anyone could say so. **`GUIDED-148` was false and the agent withdrew its own finding.** `/recipes` does reach a person — at the seal it renders *"Nothing is missing — the step that fills it has not happened"*, an honest placeholder that explains the gap in the user's own journey position, and after `select_models` it renders the full lattice. Driven by the adjudicator on both positions: 272 characters of placeholder, then **4,791 characters of per-model grid**. §08 check 5 firing on the loop that quoted it — *the sweep terminated where the sweeper's attention ended* — and **the adjudicator had repeated it to the product owner as fact without driving it**, which is the second time this session a finding was propagated because it fit the narrative. `MISC-018` was the adjudicator's row too, closed at L43 by a fix that renamed the identifiers and left the phrase in **five user-facing strings**, the sharpest an EDA header reading *'Reference Interval (NHANES p01–p99)'* — a label naming the central 95% printed beside the central 98%. Completed here, and it is `MISC-019`'s class finding its own filer. **`IMPORT-109` is the sharpest thing Part D found**: the row is about `join_doctor`, its named regression test is entirely about `import_doctor`, and `ledger.py check` was satisfied by a test that could not have failed if the fix had never been written — four entry points still raise the exact `AttributeError` the item names. Filed as `TEST-044`, the mechanically detectable variant, **with the obvious fix measured and rejected**: comparing test filenames against evidence modules fires on 127 of 284 FIXED rows, so the gate has to read what the test *imports*. **Part B is eleven of twenty-one and three were reverted after being applied** — `AUDIT-030` turned a green test red and the test is trap #3c, so adjudicating it means deciding what the manuscript should say instead; the agent reverted rather than ship what it could not verify, and reported eleven rather than claim fourteen |

| L45 | One ranked Explore stack, bounded, with a counted remainder — and the prototype that shows the measurement instead of quoting it | **Four of four, accepted.** `GUIDED-149` closed: the Explore list was unbounded because `GUIDED-142` made five packs visible in the same commit, and the answer is the shelf rule applied to attention — **judgment renders as ranking and never as absence**, everything reachable behind a counted, typed affordance, and nothing that gates a decision ever collapsed. **The bound is a measurement rather than a preference**: five is the median stack across the sixteen fixtures, driven through the API under each fixture's own lens, and `capture_explore_stack.py` re-derives it every run so the prototype cannot quietly disagree with the app. **§08's interruption budget turned out to be two questions wearing one** — an *interruption* arrives at a decision point and takes the user off what they were doing, where two is right; a *stack* is the section opening already answered, it interrupts nothing, and capping it at two would have collapsed ten of thirteen findings on `clinical_labs.csv` including five of the eight the clinical lens exists to produce. **The loop's own corrections are the other half**: `PRODUCT_VISION.md` §06b's *"twenty finding cards"* was the **served** count and thirteen is the list, and `GUIDED-149`'s evidence carried the same wrong number — both the adjudicator's, both corrected in place, and a ruling document asserting an unreproducible measurement is the governing rule failing in the record layer |

| L46 | The bound's exception, the freed slot, the deferral that lands — and `research/INTERACTION_PACK.md`, which came back against three shipped rules | **Four of four, accepted; every count re-derived and every one matched** (1608 / 1655 / 211, ledger 767/292, register 180). **The pack is the headline and it is the first one whose job was partly to say *do not cite this*** — egress worked, so 100 of 105 claims were read in primary and every one was then adversarially refuted, producing 58 stand / 42 overstated / 3 wrong / 1 dropped, 49 contradicting findings, and one fabricated byline caught. **It reversed its own first draft in public**: that draft argued from Simons, Franconeri & Reimer's 97% detection of instant changes that marked-not-moved was right on the evidence, and the refutation killed the inference — Simons' gradual condition is a twelve-second transient-free dissolve **engineered to suppress the motion signal**, so it does not transfer to a sub-second UI transition — while Huhtala et al., INTERACT 2009, n=40, icons appearing and disappearing in a twelve-icon grid, **the nearest published analogue to a card list reflowing after a dismissal**, found animated transitions gave significantly better user performance and reported change blindness in the *instant* condition. So `GUIDED-073`'s ruling is **correct as a constraint and unsupported as a preference**, and the agent left the reversal visible rather than absorbing it. **The divergence section corrected the prompt three times.** A2 moved the number A1 was accepted on — `MIN_COLLAPSE = 2` took the collapse from three of sixteen to **one**, corrected in `BOUND_BECAUSE` and re-derived rather than restated. Part D's ledger was **refused as written**, correctly: `rendered + collapsed + dismissed = served` double-counts, because a dismissed card is still rendered as a `.gone` card with an undo note, which is the shelf not being shortened; the disjoint form `live + cleared + collapsed == served` is what shipped. And **Part C was three layers where `GUIDED-153`'s row named one** — `ml.router.plan` re-presents a deferred finding as a repair question and `_is_repairable` admits only findings carrying a `fix_kind`, which a pack finding never does by design, so §04's deferral loop was open for the **entire pack stream**. Also `GUIDED-155` filed not fixed, correctly (§06.2 forbids moving a threshold in the loop that pressured it), and `TEST-045`: a parametrized id with a non-ASCII character cannot be addressed by `revertprobe.py`, which reports `UNREVERTED: FAIL` — indistinguishable from a broken test. **Two records the adjudicator found by re-measuring**: the prototype's captured `bound_because` is the pre-`MIN_COLLAPSE` string and contradicts the *1 of 16* rendered beside it on `GUIDED-149`'s own surface; and **`PRODUCT_VISION.md` §09's nudge rule does not describe the app at all** — `DRIVE-006` deleted the nudge, three green tests pin its absence, and `GUIDED-173`'s note and the PM handover both reason from it. Both are L47 Part A |

| L47 | The drive's two criticals executed, a placement ruling, and the fifth granularity of sweep | **Six of seven, accepted, with `GUIDED-167` reopened to `PARTIAL` by the adjudicator.** The first loop run against the product owner's real NHANES drive, and **the two criticals are both closed by driving rather than by reading**. `GUIDED-165`: the impossibility decision now *executes* — `n_impossible` 4 → 0 on `clinical_labs.csv`, `sbp` gone from the list, the record reading *"4 entries of `sbp` outside the impossibility band of 40.0–300.0 mmHg were set to missing"* with a machine-readable payload beside the sentence, **and the control holding** — *keep as is* leaves 4 → 4 and records a sentence that is now true. **Two decision kinds where there was one**, because with one kind the record could not tell a repair from a refusal to repair and a consumer had to string-match prose. `GUIDED-170`: a fifth refusal axis, running first, 200-with-payload matching the other four field for field — `SEQN`, `WTDRD1`, `SDMVSTRA` all refuse, each naming what the column actually *is*, while `calcium`, `energy_kcal` and `protein_g` still answer and `fiber_g` still refuses on the **AI** axis, which is the correct science. **The registry is a name registry rather than a substring list**, and the reason is in its own comment: `"iron" in "environment"` is the hazard the thing exists to prevent. **Part C is the loop's design work**: *a response to a press renders **at the control**, adjacent to the button — not merely somewhere in the same card, because the feature receipt was already in the card and still off-screen.* Not the viewport: `DRIVE-006` deleted the nudge and three tests pin its absence. The argument is that the rule has **no free parameter**, which is exactly how the middle nudge rule failed, and `research/INTERACTION_PACK.md` §04.1's CLS `hadRecentInput` finding — a shift within 500 ms of input excluded *because temporal proximity makes causality legible* — is the sourced half. **The pack built at L46 decided the first design question of L47**, which is the strongest argument available that scheduling it was right. **Part D is the fifth granularity** after route, field, name and stand-in, and **it began by finding two structural bugs in the instrument it was told to reuse**: the delegate test derived attributes from `e\.target\.closest\(` while two of three delegates are written `ev.`, *and* a winner-takes-all `if len(found) > len(best)` that would have hidden a delegate even with the name fixed. 47 attributes across one delegate became 52 across three, and `data-earmark` — `GUIDED-161`'s own control — was among the five the guard could not see. The sweep publishes its **definition** before its numbers and keeps `NO SLOT` distinct from *renders elsewhere*, because without layout the instrument cannot tell elsewhere from nowhere and reporting them as one would be the overstatement `pageharness.py` refuses. **`GUIDED-155` → `PARTIAL`**, durations done and Propagate explicitly not scheduled. **The reopening is `MISC-019`'s shape and the loop's own note is what makes it a reopening rather than a rejection**: three of thirty posting controls emit a slot, `data-miss-choose` is not one of them, so the typed 409 in `GUIDED-167`'s own evidence still lands in `#upErr` inside a `display:none` subtree. The row was closed on a test driving `data-dismiss` — whose docstring says it presses `data-feat-add`, which is **trap #3b on the loop that was handed trap #3b in writing**. **Part F traded whole, zero of thirteen, reported as zero**: both subagents died on the session limit and the agent had held their edits back so nothing was corrupted, which was right — the batching was what failed, and L48 chunks it. **The divergence section corrected the adjudicator three times**, and one of the three was a claim the prompt had built an instruction on: the reconnaissance checked `SEQN`'s dtype on one fixture and the prompt generalized it to three, so an instruction to "test on the other two" rested on an asymmetry that does not exist |

| L48 | The drive finished, the slot reached all thirty, and two instruments turned out to be answering the wrong question | **Five of five, accepted.** The loop that closed the NHANES drive: eleven of thirteen rows dispositioned, ten `FIXED` and `GUIDED-166` `PARTIAL`, the two the scope note said to drop dropped in that order. **`GUIDED-167` closed on its own evidence** — the reopening's exact press, the typed 409 and the complete-case 400, **reaching `ac-miss-<column>` by two different code paths**, which is the detail that matters: the 409's detail is an object and the 400's is a string, so a fix wiring only the `showRefusal` half would have passed one and failed the other. Re-driven by the adjudicator. **And the gate corrected its own author**: L48-A's denominator was *posting* controls, `data-feat-preview` is a **GET** whose entire failure path went to `setErr`, and *"a press produces a response or it does not; GET versus POST is not a property of what a user sees"* is the rule the prompt should have carried. 3 of 30 became **36 of 36 response-producing controls**, three of them declared exceptions. **The loop's real find is that two instruments lie.** `TEST-048`: `pageharness`'s `document.querySelector` returns `null` for selectors that are in the page — *which is why a six-way `now` survived every drive*. `GUIDED-197`: a sweep that enumerates three routes and **reports rather than fails** — which is why `GUIDED-171` survived. Both are the guard-not-guarding class at the layer that certifies everything else. **`GUIDED-159`'s premise was half wrong and the correction is worse than the row**: Train *does* wear `now`, as `reveal()`'s leftover, and one render past the seal leaves **six steps wearing it at once** with the last writer decided by which fetch resolved last, against §02's budget of one accent moment per viewport. **Two instruments caught the agent and it reported both**: its first `data-miss-choose` probe came back `GREEN — NOT LOAD-BEARING` because the synthetic press supplied the `data-ac` whose absence is the defect — **trap #3 in its own fixture, in the loop closing a row about exactly that** — and **`GUIDED-182` was its own L47 finding and it refuted it**, reproduced independently by the adjudicator, `NOT-A-DEFECT`, with the L47 test's stated limit removed rather than left standing. **Three protocol events, all self-reported.** A subagent edited `index.html` against instruction — kept because sound and disjoint, and the ruling is that the instruction was never a gate, so §02 makes it structural. A subagent ran **`git stash push` on four shared modules while four other agents wrote that tree**; it popped clean and nothing was lost, **and nothing would have said so if it had not** — the agent's own generalization, *the revert harness is not safe under a second writer, and neither is a stash*, was in prose and the adjudicator filed it as `TEST-049`, because 24 probes ran against that tree in parallel and a probe whose revert races another writer goes red for the wrong reason, which reads as a pass. **And the agent diverged from §02's chunking doctrine and was right**: five parallel chunks share a tree, `api.py` held three chunks' partial work, and a per-chunk commit would have asserted something false about its contents — so it committed the ledger per chunk and the code once and said so. **The doctrine was wrong and is amended.** Sixteen rows filed, seven of them classes. **Four of the prompt's premises did not survive being driven**, all the adjudicator's: `GUIDED-164`'s counts were the product owner's file quoted as if reproducible here, `GUIDED-176`'s "55 sites, 44 `str(exc)`" was a grep's answer against `ast`'s 70 of 70, `GUIDED-171` named `needs` where `n_inputs` counts columns, and `GUIDED-159` above |

| L49 | The two lying instruments repaired, the ten they had blessed re-checked, and a subagent that refused to work | **Five of five, accepted — and the best moment in the loop is a refusal.** L49's worktree isolation branched three subagents from a base **367 behind and 16 ahead of `TurboTab`, with no `turbotab/` on disk at all.** The Part C agent verified its base *before* working, found every file its brief named absent, and **declined** — because a diff computed there would have presented 367 commits of `turbotab/` as newly added files and its own base's 16 as deletions, which applied is a destructive patch wearing a fix's clothes. It also refused to repair the base, correctly citing `TEST-049`: `checkout` and `reset` are the tree-wide operations that had just been prohibited. Its words: *"Reporting FIXED with fabricated probe output would be the precise falsehood the governing rule exists to forbid."* **The doctrine was right and the tooling did not implement it**, which is why L50-A1 is a base preflight. **`TEST-048` fixed, and `TEST-050` underneath it** — repairing `querySelector` alone would have replaced a false negative with a crash, so *a blind spot had a blind spot*. **A3 is the half that mattered and it happened**: 10 previously-green assertions re-checked, **10 moved, 0 were live app defects** — seven from a map-file route helper that did not answer `GUIDED-192`'s new fetch, one from a rename, one from the agent's own resolve sweep counting `"kind": "resolve"` with a regex so two exits moved into a constructor read as two exits vanishing, and one from a new gate asserting only absences. Verified: the A3 commit touches four test files and no app source. **Three of the loop's own new instruments caught the loop, inside the hour** — L48's trap-#3b detector fired on L49's own A1 test file, D's truncation gate flagged C's `prepProvHTML` written in a different worktree, and A2's sweep gate caught B's brand-new `column_levels` the day it was served. **`GUIDED-198` closed by making the precondition *met* rather than announced**: the order control serves the chosen column's levels, and the buttons are deliberately **not** gated on the parameters, because gating them would make `GUIDED-176`'s refusal unreachable. **`GUIDED-191` and `GUIDED-166` closed on one mechanism** — `_install` the only door, `blanks_made` the only recorder, the legacy payload shape rejected so no second form can drift. **Its `blanks_made` judgment call was flagged as load-bearing and un-re-derived, and the adjudicator re-derived it**: a subset of surviving labels is *not* sufficient, because `.reset_index(drop=True)` also yields a subset of a `RangeIndex` while every label names a different row — driven, the label test alone passes a `drop + reset_index + blank` trap and the content confirmation catches it. Three states, never two, which is the seal's own idiom. **The agent broke §06's new rule within the hour of writing it** — it ran a suite against a tree it was itself editing, killed the run rather than quote it, and reported that the rule's own sentence says *"anything else"* when the hazard includes the writer. **`GUIDED-189` not reached, third deferral, said plainly rather than buried.** Two counts in the report were over: seven commits and twelve rows, against five and eleven measured |

| L50 | Genomics and metabolomics built out wide — and the abstraction did not bend | **Six of six, accepted. The widest loop this project has run**: detectors **22 → 38** (metabolomics 3 → **14**, genomics 1 → **6**), claims 52 → 65, **eleven fixtures shipped** each with a `.md` companion, 36 rows filed. **The headline is a negative result and it was the one asked for**: four agents concluded independently, across twenty new detectors, that `_finding`, `Evidence`, `Claim`, `Prior`, `LooksFor` and the detector signature took all of it **unchanged**. It bent in exactly one place, twice — a pack whose research specifies a *presentation* the finding shape cannot carry — and that was filed as the class rather than patched (`GUIDED-216`). Two domains proved the shape, so the next fill-out loop can go wider still. **The prompt was wrong about the badges and the file won.** It asserted that all thirteen of `METABOLOMICS_PACK.md` §11's hedges are `DISPUTED`; the file says otherwise and the agent read it — §08 marks OPLS-DA's rotation **`[SETTLED among chemometricians]`**, §04 marks Pareto **`[CONVENTION]`**, §02 marks the 80% thresholds **`[CONVENTION]`**. Re-verified by the adjudicator in the file. Badging all thirteen `DISPUTED` would have made *"a rotation does not reduce overfitting"* read exactly like *"nobody agrees on the QC RSD threshold"* — **`AGENT_ONBOARD.md` §00's second uncalibrated layer, arriving through a prompt instruction**, and the agent refused it on the onboard's own argument. **`GUIDED-212` is the sharpest row and it is a gate defect**: `[verify-at-build]` is called *structurally forbidden from shipping as a constant* in three documents, and gate 6 extracts numbers from the marker's payload — so of **nine markers in the corpus, eight are bare and contribute nothing to the checked set.** The guarantee held for one of nine. Confirmed independently. **`GUIDED-211` `critical`, and correctly `PARTIAL`**: `Evidence.both_sides` is *required at construction* on every `DISPUTED` claim and was rendered by nothing, while the badge's own tooltip says both positions are stated — closed for the §11 register where the fix landed, open for seven pre-existing positions, because ambiguity is `OPEN` never `FIXED`. Its class is `TEST-052` and it is sharper than trap #6: **a raising constructor makes the obligation read as closed.** **`GUIDED-217` raised `medium` → `high` by the adjudicator** — the loop made it *worse rather than neutral*, because the new reader classifies a single-cell matrix as raw counts at high confidence and opens the negative-binomial route, on the one input the research says must be refused and whose bulk treatment its own registry calls `SETTLED` wrong. Not `critical`, because no fixture here is single-cell and that reachability line is what `critical` has meant in this ledger. **Two of its own fixture numbers were wrong and two subagents caught it independently** — a seeded generator had a block inserted mid-stream, shifting every draw after it — and both re-derived rather than quoting forward, which is the project's own rule working; filed as `TEST-056`, whose guard then caught the agent's own companion quoting a stale number. **Three of this loop's new guards caught this loop**, one within sixty seconds of being written. **And the loop stalled on machine contention**: it stacked three heavy suites onto two pytest shells left running for two days, and its generalization is the one §06 was missing — *a suite is quotable only if nothing else is competing for the machine*, not merely if nothing else is writing the tree. `turbotab/` was consequently the one count it could not report |

| L51 | The right column built, four subagents lost mid-work, and half a fan-out's tests passing with the fan-out reverted | **Accepted. The loop lost its workers and reported that first.** Part C's four subagents died on a weekly account limit mid-work — no reports, no probes, partial edits in four isolated worktrees — and the agent merged all four chunks itself and **probed everything from scratch, because `FIXED` requires a probe and there was nobody's word to take.** **`TEST-060` is the result and it is the most valuable row in the loop**: reverting all sixteen changed files left **four of eight** returned regression tests **green**, written by four independent agents. The diagnosis is not carelessness — *the cheapest passing assertion is the one describing what the code now does*, so a pass that writes both the change and the check writes a check the change does not need. **The agent filed it rather than building it, on the grounds that changing how work is commissioned is the product owner's call** — which was the right escalation, and the adjudicator ruled it in two tiers: a subagent's `FIXED` arrives with its probe output or it is `PARTIAL` by default, and where the fan-out has room the probe is run by a different subagent than wrote it. **It got the probe wrong twice before getting it right and said so** rather than quoting the first answer: a guesswork row-to-file mapping produced four false `GREEN — NOT LOAD-BEARING` verdicts, then a second pass reverted **nothing** because `zsh` does not word-split unquoted variables, and only the third — reverting all sixteen files — produced evidence. A separate sweep reported *"no multiple-testing correction anywhere in shipped code"* because `grep -E` was given `\|` as an alternation and read it as a literal pipe; `ml/multiplicity.py` had been there the whole time. **`GUIDED-212` closed and verified by the adjudicator planting a bare marker**: the gate refuses it and names three resolutions, and holding out a second number exposed three defects in the gate's own reader — docstrings, a nested apostrophe, f-strings on 3.12+ — all replaced by `tokenize`. **`AUDIT-028` is the loop's model correction**: *"fitted inside the training folds"* over a door that has no folds — verified, nothing in `turbotab/` imports `KFold`, `cross_val_score` or `cross_validate` while `ml/` does — became *"fitted once over the N training rows, and the held-out rows inform none of them."* **The claim corrected to the one the door can keep, not dropped**, and the nine tests pinning the stronger claim were fixed rather than the correction reverted. **`GUIDED-217`'s fixture is the house style now**: 85.3% zeros generated from a depth × gamma gradient, so sparsity is a *consequence* rather than a number written to clear a threshold. `GUIDED-189` closed at its third deferral **without invoking §06.2 and without moving either threshold** — the duplicate was deleted and the chip reads the panel's number. Part C four of fifteen, said plainly. **`TEST-061` is the row that outranks the closures**: the guard checking every `FIXED` row's named test now runs **1800.03s against its own 1800s budget** and reports neither pass nor offenders, so 347 closed rows rest on a check that cannot finish — and its own docstring predicted it. **One correction to `AUDIT-037` by the adjudicator**: `INTERACTION_PACK.md` has no anti-pattern content, but `CLINICAL_SURVEY_PACK.md` has **nine `**Anti-patterns.**` blocks** carrying ~25 named entries, so 131 counts *tabulated* entries rather than named ones — **`MISC-014` in a third costume, after *unrouted is not absent* and *un-modularized is not absent*: untabulated is not absent, and each time the sweep asked for a shape rather than for the content** |

| L52 | The guard's answer after it stopped checking, twenty-two rows each probed by another agent, and the checklist | **Accepted, and the ruling made last loop paid for itself on its first run.** `TEST-061` → `PARTIAL`, on a distinction that is the whole discipline: **met on its question, not on its budget.** The guard's answer is **0 offenders across 880 named-node targets and 311 rows** — nobody had seen that number since the check outgrew its cap, and it means **no `FIXED` row in this ledger rests on a test that skips.** The shape was the defect: it ran every FILE any `FIXED` row named — 2796 tests across 142 — then asked which node skipped; it runs the 880 **nodes** now, with resolution delegated to `pytest --collect-only` rather than re-derived. **Two cheaper designs were measured and rejected on the measurement**: `--collect-only` and `--setup-plan` finish in ~3s and each report **zero** skips where a real run reports four, because an `importorskip` in a test *body* is invisible until the body runs. **The second check is the better half** — four rows named a function that exists nowhere, which the old guard *structurally could not find*, because an uncollectable node never reports a skip; `STATE-110` left `PARTIAL` rather than renamed onto a test whose own name asserts the opposite of the row, on the argument that a dangling pointer is honest and a wrong pointer is not. **§08.1 tier 2 earned its keep immediately**: `AUDIT-008`'s builder claimed `FIXED` quoting four real `AssertionError`s, and an independent verifier found it had probed with a **partial** revert that preserved a key its own fix introduced — so the test could not survive a total revert long enough to reach its claim. Downgraded. **A verifier refuted its own builder three times**, including finding that all twelve xfail guards in the a5/b6 registry are unconditional `pytest.fail` and **none reads shipped code.** **Part C is the best-built artifact this project has shipped** and the adjudicator drove it: `/checklist` 200, fetched at `index.html:3509` and rendered into `reportBox` at `:3632`, twelve rows each tracing to `CLINICAL_SURVEY_PACK.md#A6`, all four of §09's fill states in use, `needs_from_you` populated on every row. **Its coverage block is the governing rule applied to its own completeness** — 27 items, 12 enumerated, 15 not, *because writing them from recollection of the source paper is the one thing this project does not do with domain science* — and the nulls are sharper than the prompt asked for: the **structured** field is `null` and the **rendered** field is a sentence, so trap #9 and trap #7 are held at once. **Three sweeps were wrong before they were right, all one failure** — `'tests'` matching the English word in *(seven tests)*, a leaf-segment index missing `TestCommaReading`, a `None`-detector firing on *"**None** of them is a property of the data"* — generalized as **a matcher that fires on prose has silence that means nothing**, and **the adjudicator then made the same mistake** verifying Part C with a `"None" in blob` check. **The adjudicator's correction**: divergence items 5 and 6 are one event. The two importerless modules are not trap #1 — their rows read `OPEN`, so the ledger is truthful; they are finished logic for open rows blocked from `pages/10` and `pages/04` by the loop's own **row-wise** fan-out partition. **Rows do not partition by file: group a fan-out by FIX SITE**, which §02 now carries |

| L53 | Two modules wired, the checklist fills itself, and a leak the suite found | **Accepted.** `AUDIT-022`/`023` closed and both probed with a **total** revert — the two importerless modules from L52 now have consumers in `pages/10` and `pages/02`, which is trap #1 discharged rather than described. **`fill()` quotes and cannot paraphrase**: it returns `" ".join(said)` over the draft's own sentence texts, so the only string the function creates is the space between them, and `where_addressed` resolves against the draft it was handed so a renamed section *unfills* the item rather than dangling. **A2 is §06.2's exception invoked correctly and in those words** — the timeout cap changed **purpose, not value** (a budget nobody could meet became a hang detector), `AUDIT-040` quoted in source beside it, `TEST-061` honestly left `PARTIAL`, the budget claim relocated to a live row rather than dissolved. **The agent made `AUDIT-008`'s mistake twice inside the loop that fixed it and reported both** — a partial revert spanning `api.py`, then a `KeyError` raised *from the assert's own failure message*, which only evaluates when the assert fails. It also **killed its own hypothesis**: it expected a cross-project bleed in `recipes._OPERATIONS`, drove it, found nothing, and filed `TEST-063` as the narrower true thing — a test-isolation defect the full suite then caught, asserting `'pareto' == 'standard'` while passing alone. **The a5/b6 tripwire fired on three rows, not the two the prompt named**, because Part C also closed `AUDIT-032`; the agent hit it, removed all three from `MARKED`, and recorded it. **The adjudicator's number was wrong and the agent corrected it by hitting the thing.** Found afterwards and not by this loop: **`TEST-063` was `FIXED` naming a test function that exists nowhere** — see L54's row for why two green loops carried it |
| L54 | The deck that keeps its cards, two classes filed, and a part not built | **Accepted as three of four parts, and the missing one is the adjudicator's fault rather than the agent's.** **B0 is the deliverable and it is real**: zero `innerHTML` in the deck region against node-owning writes, cards created once and **mutated in place**, and — the part that matters — **identity proved rather than grepped**: each card's element is stamped, the study panel is toggled twice **through its real control**, and the stamps must survive. **The harness turned out unable to verify the claim B0 exists to make.** It implements neither `insertBefore`, `replaceChildren` nor `hidden`, **and its `appendChild` COPIES rather than moves** — re-appending three cards in reverse gave six — so move semantics, the entire mechanism behind identity-preserving reorder, cannot be observed by the instrument. `TEST-066`, and the risk is the inverse of the usual one: **the tool is too poor, so it pushes production code toward the subset it implements.** **Part C was not built and that is on the prompt.** L54 quoted `DESIGN_LANGUAGE.md` §05.2's zeros as the *argument* for building B0 and never drew the second inference — a shim built to run the page never implements an API the page never used — so a discovery overrun was unpriced, and the part it killed was **pure plumbing with no unknowns at all**. Filed as `GUIDED-230` with the measurement re-derived rather than quoted forward. `§02` carries both rules now. **`TEST-064`'s sweep said the number when the number was zero**: 8,852 asserts, 3,783 with messages, 193 subscripting a constant key, **0** of the sharp shape, because L53's fix removed the only one. **`TEST-065` answered better than it was asked** — a structural check is *mostly not possible*, since *"a test that could make it would be a second implementation of reading"*, and what **is** assertable is to pin the sentence each mapping was validated against so a reworded draft forces re-confirmation. `DRIVE-009` moved to `PARTIAL` with its built third named, `DRIVE-014` filed for a fully-styled journal view that **nothing anywhere applies**, `MISC-021` for 105 `innerHTML` sites against zero identity-preserving transitions. **One assertion of five is still red for the wrong reason** under total revert — a `HarnessError`, which §08.1 does not accept as discharge — reported rather than papered over, which is why `GUIDED-178` stayed `PARTIAL` |

| L55 | The guard that could not run, the shelf that reads the record, an instrument that was copying — and a diagnosis retracted | **Accepted, four of four parts.** `turbotab/` **2,449 passed · 21 skipped · 9 xfailed · 0 failed in 2:01:04** — the first full run under `numpy` 2.4.6, and the number L55 could not report before the adjudicator was cleared. **Part B is the loop**: `GUIDED-230` closed, the three recorded design answers reach `shelf()`, and **the failure condition was checked before the feature** — set equality against the registry across **21 sealed projects** on every combination of the answers, **0** models removed, disabled or hidden anywhere. The clause **quotes `Decision.text` character for character**, the sort key `(bucket, design_rank, group, name)` moves order *inside* a bucket and never across one — because the bucket is a verdict about the table's shape and the design rank is a verdict about what the user *said* — and with nothing recorded it collapses to the key that was there before. **`TEST-063` is the row that matters most and it went the other way.** L55 was commissioned with the adjudicator's sentence *"the underlying repair looks real and only its record was wrong."* **Both halves are false and the agent measured them**: a total revert of the fix returns `GREEN — NOT LOAD-BEARING`, so there is no repair; and the `table` fixture's snapshot/restore has been in place since **L18** (`7c68dd6`) with the reset-to-core half added at **L35** (`a3e2395`), both present at `773a626^` where the row was written — so the recorded cause cannot have produced the recorded symptom. *(The report attributed the restore to L35; the restore is L18 and the reset is L35. Re-derived with `git log -S` at the handover.)* **It declined to substitute a second diagnosis** — `TEST-068` holds the real, unexplained symptom as a question with the drive that would settle it, which is the rule this project runs on rather than a newer date on the same defect. **Part C's premise was half wrong and the correction is bigger than the row**: `DRIVE-014` assumed a toggle missing its handler; **no figure was ever drawn at all** — the surface rendered title, tier, annotations, checklist and caption and never geometry, **2** `<svg>` in 8,479 lines — so the journal CSS was styling for an element with **no producer**. **Part D found a second defect under the first by measuring instead of asserting**: `appendChild` now moves, and the prediction that `deckFill` would stop duplicating was **wrong** — 4 rows per card still became 8, because `__unregister` cleared `_parent` down the whole subtree, which no DOM does. `TEST-069`, and its class is that **a shim's defect can be invisible until another defect is fixed.** **20 reverts declared, 19 red for the stated reason**; the twentieth is `TEST-063`'s own fix coming back `GREEN`, which is what §08.1 exists to surface. Five rows filed rather than fixed — `GUIDED-234`/`235`/`236`, `TEST-068`, `TEST-070` — and `GUIDED-236` is trap 3b **with a checklist item instead of a test name**, green for as long as the ROC figure has existed. **Three of the agent's calls the adjudicator would not overturn**: it reported trap 5b *inside the guard written to close trap 5b*; it refused to add `parentNode` because it would change what untested branches observe, and filed a test asserting the absence so a later loop cannot add it by accident; and it killed a competing suite mid-loop and said so. **Four corrections are the adjudicator's, made at the handover after this loop closed**: `PRODUCT_VISION.md` §06c held claims two reviewers had refuted while they sat committed (`GUIDED-237`, rewritten in place), `NUTRITION_PACK.md` §04's *"because"* asserted a mechanism its own primary source does not (`AUDIT-045`), the categorical ramp is not separate from semantic color and the write-up's blast radius was wrong in both directions (`DRIVE-015`), and `AGENT_ONBOARD.md` named **five** gates where the hook runs six and **three** expected reds where `shap` now passes 10/10 |

| L56 | The suite got cheap, the guard learned to see, and a part was refused because the prompt's own number was false | **Accepted. Six parts commissioned, A · B · C3 landed, C2 REFUSED, C1 · D · E · F to L57.** `turbotab/` **2,464 passed · 17 skipped · 9 xfailed · 0 failed in 1:59:54** — the new baseline, recorded beside `AUDIT-040`. **The refusal is the headline and it is the best decision in the loop.** C2 was to replace the categorical ramp with a three-hue set the prompt supplied as measured-and-passing at worst CVD ΔE **20.0**. The agent measured it and **blue `#3B5BA9` against purple `#6E3FA3` is ΔE 2.0 under deuteranopia** — re-derived independently by the adjudicator with a Machado 2009 severity-1.0 simulation: simulated, the pair is `#225AA7` and `#2357A1`, *the same blue*, and **the legend cannot rescue them because the legend swatches are those two colors.** Both parties validated the simulator on controls before trusting it (red/green collapses, blue/orange survives), which is the discipline the measurement needed. **It also declined to substitute its own palette, on the argument that choosing one is a product decision and that a unilateral substitution is the same act in the other direction** — so `DRIVE-015` stays `OPEN`, the stylesheet is untouched, and `--c1` is still byte-identical to `--accent`. **Four of the prompt's premises were false and all four were the adjudicator's**: `import-graph.json` holds 87 keys and `reverse-deps.json` 74 with **zero** `turbotab/` entries, so A1's stated input covered none of this package and the tool had to walk its own graph; A2 was posed as a leading question when the drive was already `scope="module"`; B2 inherited the row's false *"exactly one fixture has a remainder"*; and the palette above. **The first was the sharpest**: the adjudicator verified those two files *existed* and inferred that they covered the package, which is `PM_TRANSITION.md` §07's fifth entry committed one commit after being written down. **`MODELS-025` is the largest find and no test could have reached it**: every `BaseModelWrapper` subclass — `glm`, `huber`, `rf`, `nn` — **failed to complete a training run**, and had since scikit-learn 1.6 made the tags API required inside a `Pipeline`; four of twenty-two models were selectable, ranked, carrying an evidence-bearing concern, and **errored the moment a user fitted one**, on a shelf `PRODUCT_VISION.md` says is never shortened. **2,449 passing tests said nothing, because nothing fitted one.** Fixed with `BaseEstimator` **and** `__sklearn_is_fitted__` — inheritance alone moves the failure to `NotFittedError`, measured — and the guard is a **sweep over every registry model** rather than four cases, because naming the four would encode today's breakage as the subject. Probe re-run by the adjudicator: total revert of `models/base.py` goes **RED naming `glm`, `huber`, `rf` on both target shapes**, with the assertion quoting the constitution's own rule. **A1's best decision is that it fails closed**: `--pytest-args` on an unscopable change prints **nothing** and exits **2**, so a caller ignoring the status runs zero tests rather than a confident subset omitting the affected ones — driven, not read. **A2's cause was one field**: the root payload's `id` *is* the project address the page composes every fetch from, so tagging it made the route table unaddressable and pass 1 returned `None` with **0 of 2,867** fields marked; found by bisecting 2,180 fields, and the file went **1,175.39s → 738.53s** with no verdict moved. **`TEST-071` closed and its durable half is `partition_is_exhaustive()`** — verified at HEAD as **358 / 0 / 9 = 367**, matching `status==FIXED` exactly, with the 26 blind-spot rows resolved and **no closed row reopened**. `AUDIT-039` closed with six repairs for six skips. **Two corrections to the report, neither a defect**: its partition line reads `367 / 376` where the measurement is `358 / 367`, inside the very paragraph promising to stamp counts with a commit; and it credits the `MODELS-025` ruling to the adjudicator where the commit correctly credits **the product owner** |
| L57 | The validator before the palette, and a handback instead of a half-built part | **Accepted, one part of five. A landed; B · C · D · E deferred into L58 with their specifications intact.** **Part A is the model for how a disputed measurement gets settled.** `DRIVE-015` had been ruled as a *method* rather than a set of hexes — two gates, and **the validator ships first, including against a palette the adjudicator proposes.** It did: four gates, each failing with the measured number, the simulator validated on controls *inside* the test before it judges anything. **Against the shipped ramp it failed four times** and found a collision nobody had named — `--c3 #D28E6B` sits **14.9** from `--stop #D97A6C` in dark — while CVD and contrast passed for that same ramp, so it discriminates rather than rejecting everything. **The adjudicator's replacement was half wrong and the agent found the reason rather than just the failure**: a figure sits on `--surface`, not `--ground` (`.fig` at `index.html:671`), so the dark values were 2.76:1 and 2.86:1 against the surface they actually sit on, under the 3:1 floor. **It adjusted minimally rather than substituting** — +0.04 lightness, same hues — after searching 21,480 passing candidates to confirm the space was not tight, preferring the smaller change to keep the design intent. **Then it handed back rather than starting Part B**, on the argument that a real build at the tail of a very long session invites the half-built part the scope note forbids. **Accepted, and it is the second time that agent declined work for a stated reason instead of shipping a weak version.** Everything it handed back — the confirmed site map, the checklist partition filed as `GUIDED-238`, the exact assertion that inverts, and two corrections it volunteered — was written into `prompts/L57.md` §00b **before the session holding it was cleared**, which is the one thing the previous transition document existed to repair. **One process defect: `AUDIT-046`** — the loop's commit swallowed the adjudicator's uncommitted `PM_TRANSITION.md` edit, so a commit about a color validator contains a PM handover. Nothing lost, verified byte-identical; the contributing cause was an adjudicator holding a docs edit in a live tree because the spelling gate was red on the loop's own in-progress file. **Hold pending docs edits outside the worktree** |
| L58 | The door opened, and the guard that proves a renderer rather than a list entry | **Accepted, four of four parts, with one defect found in adjudication and fixed.** **This loop exists because a human drove the app.** `DRIVE-017`: the Guided interview asked lens, target, task type and purpose and then jumped straight to Explore, so **no person could fit a model at all** — Train rendered a heading with no shelf, Explain was gated on a metric that could not exist, and the manuscript rendered its whole TRIPOD scaffold reading *"not filled by the app yet."* **The cause was measured before the prompt was written, and the row's own hypothesis was refuted**: the Router serves `state_grain` `asked` at seq 03 the moment a target exists, so nothing was skipped. **The page was the defect and the list was the whole of it** — `state_grain` and `state_eligibility` sat in `HANDLED_QUESTION_KEYS`, which asserts a dedicated section draws them, and **no such section had ever been written**; `renderAsked` believed the list unconditionally. Established by controlled counterfactual rather than by reading: the same question served twice in one plan, once under its real key and once under a twin key absent from the list, renders **only as the twin**, with `state_lens` as a positive control rendering both. **`DRIVE-022` is the class and Part B is its remedy**: the old guard checked that a served key is *in* one of two lists, and its own failure message — *"or to `HANDLED_QUESTION_KEYS` if a dedicated section already asks it"* — made appending to that list the documented way to go green without building anything. The new guard drives every key into an `asked` state and requires a control a click delegate dispatches on. **Verified by the adjudicator to fail for the right reason**: re-listing `state_grain` with a `DRIVES` entry attached drove the page over **84,642 chars and 22 routes**, found **0 controls**, and failed naming that. **`DRIVE-024` is the largest find and the adjudicator caused it.** The prompt's A4 said the group column was *"an enhancement, not a blocker"* because `set_grain people_repeat` returns 200 — **a status code accepted as a proxy for the consequence.** With no column named the app served *"Whole people will be held out rather than individual rows, so nobody appears on both sides of the split"* while `seal_basis` returned `undetermined`, **the seal drew by row and said so on the same page**, and the transcript recorded `identified by 'None'`. The fix is keyed on the same fact `seal_basis` branches on, so sentence and basis cannot disagree, and **the test asserts against the drawn split** — a rewrite making the two sentences agree with each other and not with the split still fails. **The agent's own sweep then found the same defect in `design_not_described`, which the fix had not reached.** **`DRIVE-023` sharpened the adjudicator's §00 too**: eligibility's trap was not a dead button but a **400** — `to_dict` falls back to `list(option_values or options)` and the Router supplied no values, so the wire carried the label sentence as the answer. That is `GUIDED-037` on a second question. **`choose_models` was NOT a third false claim** — 13 controls after the seal, and the prompt deliberately refused to assume it; **`choose_preparation_mode` was**, and `drawnElsewhere` reads the same list so its safety-net overlay is silenced too: zero surfaces, filed as `DRIVE-026` rather than fixed inside a measuring part. **Part D refused the prompt's own numbers**: 93 of 150 was stale, HEAD reads **136 of 154 from 25 paths**, and the tool prints a line the prompt never quoted — *the closure covers ≥80% of the suite, so scoping buys little.* **Scoping bought 12% for ~95% of the cost, so it ran the full suite**: 8 failed · 2,494 passed · 17 skipped · 10 xfailed in **2:00:27**, **all eight its own**, then fixed three ways and verified across the class — all 60 page-driving files, 894 passed · 0 failed in 1:18:41. **13 revert probes, all red for their stated reason; one came back GREEN first and it widened the revert rather than the claim.** **`TEST-077` is the adjudicator's find and it is this loop's own class turned inward**: `DRIVE-026`'s pin was `pytest.xfail()` called imperatively, which raises before the drive and **can never XPASS**, beside a comment promising *"the day the row is built this file goes red."* A claim in a comment standing in for a mechanism — `DRIVE-022` exactly — **inside the file written to end it.** Measured side by side, fixed to a strict marker, and the fix verified by repointing the needle at a control that renders and watching it flip to `FAILED [XPASS(strict)]` |
| L59 | The step that says why it is empty, a receipt that names its base, and an event the app was choosing for you | **Accepted, five of five, and it overturned the adjudicator's own correction.** `turbotab/` **2,520 passed · 17 skipped · 10 xfailed · 0 failed in 2:00:44** — a full sweep, `ps` checked, run because `affected.py` escalated and said scoping buys little; the extra xfail against L56's baseline is L58's strict preparation-mode pin working. **Part C is the find and it is worse than the row it came from.** `DRIVE-032` said the positive-class question is never asked. It is not merely unasked: `turbotab/training.py:463` sets `result.positive_label = classes[1]` for every two-class fit, which is sklearn's SORTED-SECOND level. **Verified independently by the adjudicator** on a 132-case / 1,068-control study — `positive_label` comes back `'control'` on every model and `/figures` serves `"event": "control"`. The app calls the non-event the event, confidently, which is `GUIDED-093` realized rather than guarded. **And it makes a correct refusal unreachable**: `figure_bundle.py:432` returns `None` when `positive_label is None`, with a comment saying a guess 'would be right on a 0/1 target and silently wrong on every other one' — a branch `training.py` guarantees never runs. **C2 was handed back rather than half-built**, on the argument that deleting the default would make every figure refuse, which is the weaker version `LOOP.md` §02 forbids. **Accepted; it is the third time that agent has declined work for a stated reason.** **§00 of the prompt was mine and it was wrong, and the agent's divergence section is where I learned it.** I had ruled that L58's *'13 model controls after the seal'* was measured by loading a page into an already-sealed project and therefore said nothing about the transition a human takes. **Right about the method, wrong about the conclusion.** Driven properly — page loaded UNSEALED, the page's own *Draw it now* pressed, the SAME session read — the shelf goes from 0 controls and an empty `#shelfBox` to **13 controls, a fit control and 3,976 characters**, with one POST and one `GET /models`. **Re-derived by the adjudicator at `6a74d83`.** The L58 verdict survives its own correction, and the reason my first attempt said otherwise is `TEST-083`: I called `__harness.dispatch(el, "click")` when the signature is `dispatch(type, el)`, and the harness ran it silently. **Two more of the agent's corrections stand and both are sharper than what I wrote.** *'One failed fetch ends that session's ability to train permanently'* — `renderAll` calls `renderTrainStep` on every render, so a later decision does re-fetch; **what makes it permanent is that at the Train step there is nothing left to press.** The mechanism is an absent affordance, not an absent retry. And Part B was **one false clause, not two rows**: `eligibility` is correct about its own operation (`n_before == n_after == 21,849`) and the drop lives in `draw_holdout`, so the falsehood was eligibility describing a draw it does not perform. **Both sentences still moved**, because deleting the clause alone trades a false number for no number — the seal now reads *'945 rows (15% of the 6,297 with a value for the outcome; the other 15,552 of 21,849 rows have none)'* and eligibility claims only what eligibility did. **Verified verbatim at `6a74d83`.** **`DRIVE-030` fixed and probed the way the prompt asked**: a 500 on `/models` reproduced the tester's render exactly, the step now separates *not reached yet* from *failed* with the server's reason quoted and a retry, scoped to one of 48 `.catch` sites, and **making the route healthy turns the new case red** on *'the shelf rendered controls from a 500'*. **`DRIVE-033` was reproduced before it was fixed** — 9 changed, 1 restored — then fixed through `_install`'s existing `remember` seam to 9 of 9 with one undo entry. `DRIVE-020` to `PARTIAL`, numbering only. **Two findings the loop made about its own instruments**: `TEST-080`, the copy deck restated the eligibility sentence as a literal so it kept asserting the false clause after the code dropped it, and no probe covers a generated row; and `TEST-081`, `ml/router.py:237` names `test_the_marker_is_the_constitutional_position` and **that test exists nowhere in the tree** — confirmed by the adjudicator — which is `TEST-077`'s class again and is the guard that would have caught D2 |

| L60 | The event is asked wherever it exists and never guessed, the step that says it is waiting, the refusal energy earns — and a sweep that came back 81 red | **Accepted, five of five, and the loop's two most valuable results are both the agent reporting against itself.** **Part A closed `DRIVE-032` by correcting its own row**: the positive-class question is not unbuilt, it is *on the wrong rail and not binding.* `positive_class_finding` planned through `read_as_binary_plan`, whose first line returns `None` on a non-texty series — measured one fit per shape **before** touching anything: case/control YES, died/survived YES, Yes/No YES, **numeric `0/1` NO**, which is the tester's `meds_hbp`. The target got its own dtype-agnostic `two_level_plan` and text kept the old path, so `KNOWN_PAIRS` and every feature-side behavior are untouched. **The refusal lands at the fit and not the seal, and the measurement chose**: `draw_holdout` does not stratify by class, so the split is identical whichever level is the event, and gating the seal would refuse a step that cannot use the answer. **"Chosen" is read off the DECISION, not the dtype** — a `0/1` column can be one a user chose or one that arrived that way. **The probe found a gap that reading did not**: a revert swapping the record check for a dtype check went **GREEN** against the first draft of the test file, which drove only the text shape. **B is three guards**: `TEST-081`'s named-but-nonexistent test written in both directions, 11 keys against 10 positions — and it failed on its first run because `SEQUENCE` zero-pads while §01 writes bare integers, so **it was measuring the padding**; `DRIVE-037`, because a step that speaks when it breaks and is silent when it waits teaches a reader that silence means fine; and `TEST-084`, `/dev/status` naming the build, **stamped once at import and asserted never to re-read**, since a stamp that re-read the checkout would always agree with the page and could never report the drift. **C flipped the pin before it went green** — `DRIVE-026`'s preparation-mode card reports `FAILED [XPASS(strict)]` with the entry restored, demonstrated rather than asserted, and the xfail count moved 10 → 9 on purpose. **D refused**: energy has no EAR-style cut-point, settled by reading `NUTRITION_PACK.md` §07 rather than by argument, and the keeper is *why all four existing refusals fell through* — each asks a different question and energy passes every one, which is `GUIDED-170`'s shape one axis over. Nine spellings registered, not one, because a registry holding only the reported spelling fires on the reported case and nothing else. **The value check found the capability before the sweep did**: `wide_assay.csv`'s `responder` is `int64 {0,1}`, so A's trigger fired on a fixture no part of the loop touched — `questions_asked` 4 → 5 and `findings_driven` 0.0 → **0.2**, the first observed *rise* of a metric whose two recorded movements were both falls; adjudicated rather than re-recorded, `verdict.passes` unchanged, no threshold moved. **Part E is the headline and the agent's own error is inside it**: **81 failed · 2,486 passed · 17 skipped · 9 xfailed in 2:00:18**, and **73 of the 81 quote Part A's new refusal.** The blast radius had been measured wrong and stated as fact — a grep for `POST "/train"` found three files while **nine call `training.train()` in-process and fan out to 73 tests across 21**. *"The grep answered 'does this text appear' when the question was 'does this run'"* — trap #5, in the one measurement the part's scope rested on, **reported in the same breath as the number that exposed it.** **And it declined to fix 81 failures by relaxing the thing under test**, measured rather than argued: on the four worst-hit files, **35 failed as committed and 22 with the gate removed** — *"better, not clean, failing for the same reason"* — so `DRIVE-041` was filed `critical` with both options and a recommendation rather than executed unilaterally. **The timing is not quotable and the agent said so first**: the product owner drove the app while the sweep ran. Results stayed valid; duration did not. Last clean timing is L56's **1:59:54**. **`DRIVE-035` answered, and not as run 4 concluded** — `venv/` is complete, every uvicorn on the host ran an interpreter with no `sklearn`, and `ml/model_registry.py` failed **three lines before** the packages run 4 named. **The fix is the launch command, not a reinstall**, and the attribution was wrong too: those imports are byte-identical at `07c25c6`. `TEST-087` filed — B3 names the **build** and is silent about the **interpreter**, and the interpreter is what decided this. **`DRIVE-040` came out of re-running the acceptance test**: the figure used to say event `'control'` — a name, and the wrong one — and now says `'1.0'` — correct, and not a name. **The app stopped asserting something false and became less legible in the same move**, which is the one claim in the report that does not hold (*"both the label and `/figures` name the positive level"*). **`TEST-086` is deliberately unruled** and holds the branch: the pre-push gate surfaced a second frozen-baseline drift once the first stopped failing, Classic's coverage falling 1.0 → 0.5 on `wide-assay` and `leaky-sepsis` because the **denominator** moved and Classic did not. Extending an enumerated allowance in the same loop as the change that pressured it is §06.2, and invoking it at the tail of a session **to make a push succeed** is how a frozen baseline stops being frozen. **One `--no-verify`**, reason in the commit and the other five gates named green — the spelling gate red on the tester's untracked run-4 report at the repo root, not part of the change |

| L61 | The 74 fixed at a seam that did not exist, one command that cannot be got wrong, the frozen baseline ruled — and a documented launch that was guaranteed to fail | **Accepted, five of five. 81 → 0**, verified against the sweep's own output rather than the report: **2,607 passed · 0 failed · 17 skipped · 9 xfailed in 2:01:40**, zero `FAILED` lines *(re-derived at `6504964`)*. **The best result in the loop is a correction to the prompt's §00, and the true version is worse than the claim it replaces.** *"There is no launch command"* was false **and it carried a `(re-derived at cf7129c)` stamp**: `turbotab/README.md` §"Run it" was the only document in the repository that said how to start TurboTab, it instructed building `turbotab/.venv`, and `tests/test_the_guided_door_installs_without_the_app.py` asserts that venv stays free of scikit-learn. **So the documented launch was guaranteed to lose `GET /models`, and a test guarded the guarantee** — the tester was not improvising, and the README was right when written (*"No training, no job queue, no manuscript"*) and stopped being right the moment Train landed. Confirmed by the adjudicator at `cf7129c`. **A provenance marker on an unchecked absence is worse than a bare claim**, because the one mark that is supposed to mean *I opened the file* was applied to a claim that had not. **Part A found that the seam did not exist**: `api.py`'s apply branch was the only place the answering sequence lived, so half the fixtures had no route to post to. It is now `engine.record_fix`, called by `api.py` and by `turbotab/eventfixture.py` — a fixture's answer travels a person's code — landing in **twenty files at their own sealing helper, one or two lines each, not 74 call sites.** **A3 holds one layer deeper than it was asked to**: the guard's escape hatch carries two entries with written reasons, `test_no_exemption_here_has_outlived_its_reason` asserts both still name the subject and rejects a reason under 80 characters as *"a shrug"*, and a positive control sits beside them because both assertions are absences — **`DRIVE-022`'s defect anticipated inside the file that could have repeated it.** **B1 refuses, driven rather than read**: under `turbotab/.venv` the launcher exits **2**, names the missing packages, the first error, the why and the fix, and states the port was not bound; healthy exits **0** — re-driven by the adjudicator. **B3 reaches the page** — `sys.executable` and `sys.prefix` beside the rev, and the page writes a banner only when `engine_stack_ok === false`, before the user reaches Train. **B2 keeps the shelf and conditions the sentence**, driven over real HTTP at HEAD: 12 rows, `n_available` 12, nothing filtered, every row carrying `unavailable_because`, and **two disclosures** — *"Every model is available"* and *"Every model is **listed**, and {n} cannot be fitted in this install."* Its own comment names the trap avoided: guarding the import and leaving the sentence would make the one paragraph whose job is to say nothing was hidden **assert something false**. **C ruled `TEST-086` with §06.2 invoked in those words**, claimed on L53-A2's purpose-or-value test rather than assumed, **and corrected the row it came from** — the added inventory key is the target column's name, `repair::positive_class__responder` and `__sepsis`, not the precedent's `__outcome`, which was right only for the two datasets whose column is literally called `outcome`. `leaky-sepsis` had no adjudicated reference, no deltas table and no way to be re-measured, and now has all three. Pre-push **18 passed**; **the branch unblocked after 19 commits.** **`TEST-092` is the evidence apparatus catching itself**: a red in `tests/` was invisible to a report calling itself a FULL SWEEP, because §03 documents **three** commands and L60-E ran one — `test_the_archive_carries_every_field` had been red since L59-B, every save dropping `n_rows_before_outcome_drop` while `grain.py:462` reads it for `DRIVE-031`'s receipt. **L59-B's own fix survived in the app and not in the archive.** **One correction to the report, and it is the one worth having.** Divergence item 9 attributed the 81-vs-78 gap to *"the four non-refusal reds and xdist's own isolation"* and declined to chase it. **It is neither.** The three are one parametrized test in `test_a_prevalence_needs_a_nutrient_to_be_about.py`, and **`7e34743` — the commit whose own subject reports the 81 — added the `NO_EAR_USE_EER` continue that turned them green, eight minutes after its sweep log was written, naming that very test in its comment.** Filed as **`TEST-093`**. **The consequence is the opposite of a defect**: with the three explained by a code change, the serial run and the `-n 8 --dist loadfile` run agree on **76 of 76** remaining failing nodes over **identical 2,593-test populations**, which removes the runner as a suspect and makes **41:34 against 2:01:40** a real candidate for the documented sweep — the product owner's standing constraint, answered by accident. **`TEST-090` filed with its class open**: 17 files and 19 lines pointed readers at the bare venv, all repointed, and **nothing checks that a documented command still works** |

| L62 | The Methods section stops calling the minority the event, the rail navigates, and both runners agree — **accepted four of five, with the sweep rejected and the speedup held** | **The first loop adjudicated by fan-out, and the verification found more than the report disclosed.** 11 agents, every load-bearing claim driven rather than read. **A's core is right and it is right in the way that matters**: `analysis_mask` is a genuinely second mask — *training_mask answers which rows may inform a decision, this answers which rows the model will see* — and **1,850 → 450 reproduces exactly** on a fixture of run 5's shape, with the number moving the *recommendation* and not only the copy *(re-derived at `62d59b3`)*. `DRIVE-043`'s four renderers are real and the `_push_because` statistical argument is **correct**: with the event as the majority, `events_test < 2` fires exactly when **specificity** is undefined while the sentence names **sensitivity**, and the pair is swapped. **But the report called that "worse than a wrong noun", and it was not** — the branch is **provably unreachable** through `statement()`, because `thin_classes` returns first and `events_test < 2` implies `thin_classes >= 1`; swept 60 configurations against both the fixed and the verbatim pre-fix module, **zero outputs containing the word.** It never left the building, before or after — `figure_bundle.py:432`'s shape, and **the agent overstated its own find.** **A3 is REJECTED and `MISC-022` reopened.** It reports THREE surfaces *"found by driving every surface that reports an n"*; two independent verifiers, both driving, found a **fourth on the same `/models` payload thirty lines from the fix** — `recorded_design` cites `len(training_rows)` inside a sentence claiming that number *is* what the order was computed on — plus `/selection/evidence` on the pre-fix population. **The sweep committed the class it was sweeping for** (`DRIVE-050`), which is §06 check 5 executed by the check itself. `rankings.py`, the registry the sweep is anchored in, still declares both surfaces computable from `training_rows`. And the near-miss the report caught in one direction is **still open in the other**: `n_rows_withheld` and `n_rows_without_an_outcome` overlap on a reachable path and the served counts **sum past the table size**, under a guard asserting a partition the data can violate (`DRIVE-051`). **B's three surfaces are fixed and its premise correction stands** — the event's name was recorded and the comparison level's was not — **but `MISC-019`'s reopening note carries a false sentence**: it claims *"a fourth surface added without it fails that test"*, and driving it at HEAD shows a new module rendering the outcome the old way **passes**. The guard asserts the set of files that **do** read the mapping, so it catches new **readers** and not new **non-readers**, and its glob leaves `pages/`, `utils/` and `launcher/` outside (`MISC-023`). **`MISC-019` is the class *a fix reached a fraction of the surfaces its item describes*, and its own reopening claimed a coverage guarantee the guard does not provide.** **C1 is ACCEPTED on the product call the agent handed back, and the §06.2 invocation stands.** The two drives do not collide: `DRIVE-006`'s row is titled *"**Auto**-scroll skips past the noticed card"* and §05's rationale is entirely unbidden motion. **The decisive test is §05's own** — position (2) failed on a *size-dependent* condition, and *"did the user press a navigation control"* is **categorical**, so it cannot be wrong at the next scale the way the middle rule was. **And the strict reading was not neutral: a rail that highlights without navigating asserts an affordance the interface does not provide**, so the absolute form obliged the app to keep committing the governing rule's *assert-something-false* branch. **Two debts C1 did not pay.** Neither authoritative document was amended — `DESIGN_LANGUAGE.md` §05 still read *"there is no condition"* while declaring itself authoritative, and `PRODUCT_VISION.md` §09 still read *"under any condition"* — **disclosed in no commit and no row, and no test reads either paragraph, so nothing could report it.** **§09 is a section whose entire subject is this same document describing this same rule in the present tense at L47; it recurred, on the same rule, in the loop that changed it** (`MISC-024`). Both written by the adjudicator here, which is later than the rule requires. And **nothing pins which element the rail scrolls to** — the guard asserts only that the scroll list is non-empty, so a build navigating every step to Data is green. **D is four of four with 11/11 checks holding.** **E1's measurement is accepted and its adoption is HELD.** Both runners at `d464e0b`: **2 failed · 2,649 passed · 17 skipped · 9 xfailed**, **41:23 against 2:00:46**, failure lists **byte-identical, diffed rather than counted** — and the agent's own limit is the right one, *"2 red at `d464e0b`, both diagnosed and repaired, not 0 red."* **But the run that licenses the speedup has undisclosed cross-file races `--dist loadfile` does not prevent**: a test truncates and rewrites five tracked `sample_data` CSVs mid-run while other files read them, and a doc-rewriting race with a measured **~0.7 s** window can turn a guard **falsely green** (`TEST-098`). **A false green is disqualifying for an evidence instrument in a way a false red is not**, so the xdist command is not documented until the shared-file writers are fixed and the pair re-run. **The best paragraph in the report is its own process failure**, and it is filed as a class: `setsid` does not exist on macOS, the shell forked, two PIDs appeared and **nothing ran for three hours** — *"I verified a proxy instead of the thing, in a loop about exactly that"* (`TEST-099`, confirmed independently). Also `TEST-100`: `TEST-097`'s repair **weakened** an existing assertion while report, commit and row all describe it as additive |

| L63 | The figures say which model they are about, the shelf says which rows it ranked, the writers move to `tmp_path` — **accepted six of six, with three dispositions downgraded and the sweep's licence granted** | **The first loop whose PROMPT was also built by a fan-out, and the second adjudicated by one — 12 agents before, 14 after.** The reconnaissance is why this loop landed: it found `GUIDED-236`'s own `act` field would have sent the builder to the wrong function (retracted at L57 and never corrected in the row), seven of eight line numbers stale in `L57.md`'s site table, and **`TEST-098`'s false green — the entire reason the xdist adoption was held — unreachable across four document states.** My predecessor asserted a false green it had not driven; it is withdrawn. **The build is genuinely strong and the detector says so**: reverting the loop's nine source files leaves **27 failed / 51 passed / 1 error** across its seven test files, **no collection errors**, and 8 of 11 in-scope rows produce clean assertion failures. **`GUIDED-245` is the find of the loop and it came from driving what Part B built** — `RFWrapper` forwards no `classes_`, so ticking `['rf','logreg']` **removed the ROC, the calibration plot and the decision curve** from a project that had fitted a calibratable logistic regression, while `['logreg','rf']` drew all three. Reproduced exactly at the base. **And it is worse than filed**: three wrappers lack `classes_`, `glm` is registry-reachable, and **driven at HEAD after the fix, a glm-only project still loses all three clinical figures.** The ordering was fixed; the underlying loss is still one tick away. **Three dispositions downgraded to `PARTIAL`, each driven rather than argued.** `TEST-099` and `TEST-105` name guards in a file with a **zero-line diff across the entire loop** — and deleting the whole 2,193-character trap 5c section leaves the named node, its file, and **all six pre-commit gates** green. `MISC-023`'s own falsification still passes: two agents independently planted a non-reading renderer and both guards passed, then one drove the **opposite polarity** and the guard went red, proving the item verbatim. **`TEST-107` is what `TEST-105`'s two unmitigated instances cost, and the adjudicator drove it**: break both of `evidence.py`'s pre-filter literals and the gate checks **32 of 67 claims and 0 of 51 module constants — and still prints `ok` and exits 0.** A false green in one of the six gates. **The trap the prompt named was answered better than it was asked.** `DRIVE-051`'s obvious repair turns its guard into a tautology; the loop **kept the tautology and labeled it an identity in the source**, putting the falsifiable check beside it — which asserts against pandas rather than another server-side derivation. **But the replacement is also a data-tautology**: a 480-state randomized sweep found 0 violations against a positive control that found 235, so it goes red on a source change and never on data, and the docstring's *"nothing makes them agree except being right"* is false. **The divergence section corrected the adjudicator for the seventh loop running, and this time on the instrument itself**: my C4 prescription — *blank an outcome and assert the surface moves* — is **green over the revert**, because a blanked row is still counted and profiled. The equivalence the agent substituted (*blanking and deleting are the same operation for a surface computed on the analysis population*) discriminates, and the scoping took it two attempts. **One divergence went the other way and two agents confirmed it**: `MISC-027`'s *"the prompt counted five mislabels and it is four plus an exemplar"* is false in three directions — the prompt named `:232` as the correct phrasing, the row's own `ev`/`item`/`act` all say five, and five corrections are measurable in the diff. **The report under-reported Part B by three items** — the `risk_rug` is keyed per model, the `models_overlaid` predicate now reads the accounting rather than a hardcoded key, and the ROC caption states which curve the companion covers — all of which shipped and none of which the report claimed. **`TEST-108` was found by tripping it**: six adjudication agents were handed worktrees and the pre-commit hook fails in every one, printing three ticks and three crosses, because `lib.sh` resolves the interpreter from the worktree root. Its own header comment names that class. **Also `GUIDED-247`** (the decision curve got the curves and not the accounting — driven, a silently-dropped model is as silent as before) and **`MISC-028`** (the manuscript's split reconciliation cannot fail: `analysis_total` *is* the sum of its terms and `val_n` is pinned to 0, driven over 406 pairs). **The sweep's licence is granted and its limit is the agent's own**: two byte-identical **empty** failure lists agreeing is weaker evidence than L62's pair, where two real failures agreed by name — reported against itself, unprompted |

| L64 | The wrappers answer, the checklist stops scoring what it never checked, and the refusal carries its control — **accepted four and a half of five, with two dispositions downgraded and a partition refused** | **The loop where the agent built exactly what the prompt prescribed, verified it, deleted it, and was right.** L64-A was told to add a sentinel-deferring property; it did, drove all six directions, then removed it as redundant — *a property that raises `AttributeError` falls through to `__getattr__` anyway, and `__getattr__` needs no sentinel because Python consults it only when normal lookup fails.* **Driven by the adjudicator rather than conceded**: a plain property dies at the nn wrapper's `__init__`, the sentinel version works, and `__getattr__` works with no sentinel and no setter while an unset attribute still reports `hasattr` **False**. **The prescription was more complex than the problem needed, and the error's shape is the part worth keeping** — the reconnaissance measured *why* two candidate fixes fail and the adjudicator turned that into a prescription for a third without asking whether the failure was intrinsic to the mechanism. It is: a property is a **data descriptor** and intercepts a lookup that should have found the instance attribute. **A measurement of two failures is not a search over mechanisms**, and the measurement's authority carried into the conclusion drawn from it. **Eighth consecutive loop in which the divergence section has corrected the adjudicator, and the first in which it corrected a prescription derived from a measurement rather than from a recollection.** **The headline result is B5 and it is a measurement rather than a classification**: of the manuscript validator's thirteen checks — the only pass/fail set in the *Guided* door feeding a count a user sees — **eight are decided before the manuscript is read**, and each of the five frozen ones was shown to **survive a state that should make it FAIL**: a Table 1 whose index contains none of the finalized predictors passes, and so does a **zero-row** Table 1; a regression run whose evaluation prose reads MAE and R² while the task type says classification passes; and the string *"primary model"* appears nowhere in the entire rendered export. **The user-facing harm is worse than the row said, driven on the real page**: the header renders *"13 checks, 0 unmet"* where the honest Guided denominator is **5**, and beside it the page asserts *"Every consistency check the validator makes is met by this draft"* — the governing rule's *assert-something-false* branch on the artifact that leaves the building. The header and the list beneath it also count **different populations**, and the payload's own reconciling field is served and read by nothing. **Four corrections to that row are the adjudicator's own**: its *"only"* is false (the same 13 feed a second rendered count in Classic that **gates downloads**), *"only one has ever been shown to fail"* is false (a committed `ml/` suite drives 12 of 13 red), *frozen* is a property of the Guided path rather than of the check, and its `index.html` citations were **already off by five to nine lines when filed** — a filing error compounded by drift, in a row whose own subject is a stale citation. **B's partition refusal is UPHELD and the epistemology endorsed.** Three instruments gave three answers — mutation probe 85 of 86, static walk 19/42/22/3, empty payload 5 of 86 — and the agent declined to publish a fourth unreproducible number. *"Can this item ever fail in service"* quantifies over states a producer **could** reach, which is a judgment rather than an observation; the empty-payload probe is the one that is measurable and the one the build asserts. **`GUIDED-238`'s `item` should stop quoting a partition at all.** **Two dispositions downgraded**: `TEST-108`'s named test has a **zero-line diff across the loop** and **skips in the very environment the row is about**, so it cannot go red for its own fix — *and the row's own note disclosed that*, which is why it is a downgrade rather than a falsehood; `GUIDED-238`'s named node stays green over a full source revert and was repointed to one that discriminates. **The reverted-diff detector**: 6 of 26 added nodes stayed green, and the survivors are named. Also `TEST-109`, found by the agent **against its own work** — `ledger.py` has no path that writes a row's `ev`, so a stale evidence citation filed at add-time is permanent through the tool, and the one instrument built to keep the ledger honest is structurally unable to repair the field that carries the line numbers. **And two refuters returned SOUND — the first clean refutations this project has recorded in five fan-outs** |

**Adding a row is part of adjudicating a loop.** Two lines, written when the report is accepted.
This log decayed once because it lived only in chat; that is the failure this project has already
paid for twice, in two different places.

---

## 04 · Loops that build a domain pack

The five research threads in `docs/turbotab/research/` are **4,247 lines and are the authoritative
source** for every pack detector, coaching sentence, threshold and figure specification. They are
not background reading. A loop that builds pack content without citing them has invented its
content, which is the failure this whole apparatus exists to prevent.

Three problems, solved differently.

**Volume.** Nobody holds 4,247 lines. **The task block names the file and the section**, and the
agent reads that slice. A task block that says *"build the nutrition detectors"* without section
pointers is malformed.

**Provenance.** Every pack advisory, detector and figure spec carries a **`source`** naming file and
section, and an **`evidence_status`** of `SETTLED` / `CONVENTION` / `DISPUTED`. A checker verifies
the named section resolves, and runs in `.githooks/pre-commit` beside the other gates. Its limit,
stated wherever it is stated at all: it verifies a source is *named and resolvable*, never that the
claim is faithful — the same posture and the same honest limit as `ledger.py check` enforcing that a
test is named.

**Where the research file and the model's recollection disagree, the file wins.** The files were
built under a blocked egress proxy and say so; a threshold in the file is a recorded, checkable
claim, and one from memory is neither.

**`[verify-at-build]` is a hard stop.** Such a number ships as an `offered` item with its
uncertainty stated, or not at all — never as a hard-coded constant. Where the fact is unavailable,
that is a `BLOCKED.md` entry, not a guess.

**Sequencing.** Four parallel domain verticals is the wrong shape: the packs share the figure spec,
annotation engine, badge rendering and checklist engine, so built four times they are built four
ways. **One pack end-to-end first as the reference implementation** — discovering the abstractions
is the deliverable alongside the pack. Nutrition went first because the product owner can adjudicate
its content, the NHANES fixtures are real, and it is the one pack that forces a **refusal**.

---

## 05 · Guardrails

Append this to any unsupervised prompt.

> **Hard rules.** Stay on branch `TurboTab`. Never push to `main`, never force-push, never open a
> pull request. `ml/import_doctor.py`, `ml/join_doctor.py`, `utils/combine*.py` and
> `pages/01_Upload_and_Audit.py` are **frozen — see `TRANSITION_PLAN.md` §05 for the one statement
> of what that permits and the gates that lift it.** Never edit `FINDINGS_LEDGER.md` by hand; it is
> generated. Never mark a finding `FIXED` without a regression test **verified to fail when the fix
> is reverted** — see `FEATURE_PARITY.md`, "the revert probe". **First command in a fresh clone:**
> `git config core.hooksPath .githooks`. Commit after every batch so nothing is lost. **Domain
> science comes from `docs/turbotab/research/`, never from recollection** — where a research file
> and your memory disagree, the file wins, and a number marked `[verify-at-build]` may not ship as a
> hard-coded constant. If you are blocked or something looks structurally wrong, stop and write what
> you found to `docs/turbotab/BLOCKED.md` rather than guessing.

**One writer at a time, scoped to the shared data files** (`findings.json`, `register.json`, and
their generated markdown). A docs-only commit from another session may land mid-loop **only if** it
touches none of those files and its commit message says so; the loop agent rebases over it. Anything
touching the data files waits. That is the artifact this project has already lost once.

**The adjudicator never runs `git add -A`, and the rule is written here because it was broken here.**
Two commits during `L30` — `9ebf95d` and `7dd6aa6` — carry `docs(turbotab):` subjects about the
frontend stack and contain `turbotab/exits.py`, `turbotab/packs.py` and five other source files that
were the loop agent's uncommitted Part A at that moment. Nothing was lost and every gate stayed
green, but the subject line of a commit is a claim about its contents, and those two assert something
false about themselves — the governing rule failing in the record layer rather than in the app.
**Stage explicit paths, and run `git status` first, every time.** The failure was not carelessness
about the rule; it was not noticing that a conversation about architecture is a moment when a loop
may be running. The docs-only exemption above is what makes an adjudicator's commit safe mid-loop,
and `git add -A` silently converts a docs-only commit into a mixed one.

**A file a tool owns has exactly one writer, and it is the tool — and this binds the adjudicator
first, because the adjudicator broke it first.** `ledger.py` serializes `findings.json` at
`indent=1`. An adjudication script that dumps at `indent=2` reformats all nine thousand lines:
commit `5881aa8` carries a `docs(turbotab):` subject about the agent onboard and **18,554 changed
lines** in `findings.json`, thirty of them real. The loop agent then did the same thing in the other
direction at L42 and found it reading its own diffstat; nothing else caught either. A diffstat is a
claim about what changed, exactly as a subject line is a claim about contents — this is `git add -A`'s
lesson arriving through a different door. **Edit through the tool, or write the file back
byte-identically to how the tool writes it.** The older rule — *never edit `FINDINGS_LEDGER.md` by
hand* — failed to generalize because it named the **generated** file rather than the **owned** one.

**Verification loops run in a fresh session, not the builder's.** A builder verifying their own work
reads intent where the job is to read code — the same reason a review pattern never lets the finder
be the judge. And both write the same data files.

### A ruling is not a ruling until it is in a commit or a ledger note

**Written on 2026-08-09, after three separate pieces of load-bearing work spent part of a day existing
only in a message.** An adversarial review that refuted committed claims in `PRODUCT_VISION.md` §06c
lived only in a cleared PM conversation and had to be reconstructed from a successor's transition note.
L57's Part B reconnaissance — a confirmed site map, a checklist partition of **85 items / 43 constant-
reading / 16 falsifiable**, and the exact assertion that inverts — arrived in a handback message minutes
before that session was cleared. And this rule itself was raised twice in conversation before anyone
wrote it down.

**The pattern is not forgetfulness. It is that a decision feels finished when it is understood**, and
understanding lives in whoever is holding the conversation. `§03` of this file already says the loop log
*"decayed once because it lived only in chat"*; that was one instance of a habit, not an accident.

**So: the record is written in the same turn the decision is made, and by whoever made it.** Not at the
end of the loop, not when convenient. If a session might be cleared before the next commit — and it
always might — the ruling is not safe.

**This binds hardest if the adjudicator and the execution agent are ever given a direct channel to each
other.** Today every ruling passes through the product owner, and that indirection is what has been
forcing things into commits. **A direct channel removes that forcing function and nothing replaces it.**
Before using one, the rule above stops being good practice and becomes the only thing standing between
this project and the failure it has now paid for in five separate places.

**The six gates are a hook, not an instruction.** `.githooks/pre-commit` runs `parsecheck.py`,
`ledger.py check`, `register.py check`, `tests/test_american_spelling.py`, `copydeck.py check` and
`evidence.py check`, and refuses the commit on any failure. **This paragraph said "three" for nine
loops after the hook grew to six** — found while verifying the count at the L53 handover, against a
hook whose own first line reads *"The six gates."* A stale number in the file that explains the
gates is the decay this project pays for most often, and it is why the count is now written beside
the list rather than in front of it.
This replaced a line in the guardrails that enforced nothing: commit `8127101` went out with the
spelling test red because the gates were chained with a newline instead of `&&`, so a non-zero exit
did not stop the sequence. **An instruction a tired agent can skip by punctuation is not a gate.**
`core.hooksPath` is local config, so the one command above is the only part still carried by
discipline. Bypass with `--no-verify`, and say why.

**Run the documented setup path, or find out that nobody has.** The `Makefile` names
`./venv/bin/python` and nothing had created it in long enough that the spelling gate's skip list had
`.venv` and not `venv` — so the gate died on a compiled dependency the first time anyone followed
the instructions. A setup path is a claim like any other and decays the same way: silently, while
the people with working environments keep working.

**It decayed again, in the same file, and this paragraph did not catch it.** Found while adjudicating
L27 (`TEST-039`): `PYTEST_OPTS` passes `--timeout=60`, `pytest-timeout` is absent from `venv/`, and so
`make test` — and `make verify`, which line 13 of the `Makefile` calls the CI target — exits 4 having
run **zero** tests. Every count this project has reported came from a hand-rolled `pytest` invocation
instead: the numbers are real, and they are evidence about a command written down nowhere. The rule
above failed because it is prose. **Adjudicating a loop now includes running `make test` once** — the
only form of this rule that can actually fire.

**The freeze** and the three gates that lift it are stated **once**, in `TRANSITION_PLAN.md` §05. Do
not restate them; this file once said "never modify" while §05 said "engine-move-only", and a reader
following the stricter one could not do the work §05 permits.

### A scoped run is never reported as a full run

**`L56-A1`, and it exists because the two-hour sweep stopped being runnable inside a loop.** The
product owner's words: *"These full suite tests are simply taking too long for the workflow we are
currently in. They run over two hours and occasionally time out."*

> **A loop's regression evidence may be the scoped selection, quoted as scoped, with the full sweep
> run once at the end.** The selection is produced by `docs/turbotab/tools/affected.py`, and the
> word *scoped* travels with the number wherever it is written down.

**The tool is allowed to refuse, and that is the half that matters.** Three refusals are built in:
a `conftest.py` change cannot be scoped at all; a change whose direct-import selection is **empty
while its closure is not** is reached only through an aggregator and is reported as unscopable
rather than as *no tests affected*; and `--pytest-args` prints nothing and exits `2` in both cases,
so a caller that ignores the exit status runs zero tests rather than a plausible subset that omits
the affected ones.

**Why there are two modes and not one, measured on this tree.** `turbotab/api.py` is an aggregator —
**104** modules import it and it imports most of the package — so full transitive reachability
answers *could this reach the change* with **yes** for nearly everything: a one-line change to
`turbotab/models.py` selects **131 of 147** files, `models/glm.py` selects **134**. The default is
direct importers plus named triggers; `--closure` is the over-approximation; **both counts print
either way**, because the gap between them is what tells a reader whether the scoping can be
trusted.

**Neither mode is exact and the tool says so on every run.** An import edge is the only thing an AST
walk sees, and a test that imports `turbotab.api` and drives `/project/{id}/models` really does
exercise `turbotab/models.py` without importing it. The exact answer is a coverage map keyed by
test — `TEST-072` — and until that exists, `--closure` is the safe side and the default is the
useful one.

### A capability ships with its consumer, or with a failing test that names the one it lacks

**The measurement that produced this rule.** Searching the ledger for findings whose text describes
a capability that exists beside a path that never reaches it returns **37 of 672** — four critical,
many high, several landmines. The count is not the finding. **The distribution is**, because it
spans all three eras of this codebase:

- `MODELS-005`, inherited Streamlit — the Cancel Training button is decorative; `cancel_training`
  is written and never read.
- `GUIDED-058`, `L27` — every surface the nutrition loop built was imported only by its own tests.
- `GUIDED-075`, `L31` — the page never fetches `/figures`.
- `GUIDED-094` / `GUIDED-095`, found adjudicating `L34` — `stale_downstream` written and never read;
  41 decision kinds recorded and 6 consumed by the thing that fits the model (the count read 36 until `L35-E` enumerated them properly; the adjudicator's grep had missed five).

Same defect, three codebases, years apart. This is not migration debt and it is not one bad loop.
**It is how this codebase has always been built**, and `AUDIT-008` named the shape without noticing
it was describing a habit rather than a cluster.

**The cause is an incentive gradient, and naming it is most of the fix.** A capability is gratifying
to build and *fully verifiable in isolation* — a green test can prove `figure_specs.py` correct
forever without anything ever calling it. Wiring requires the consumer to exist, and the consumer is
usually the next loop's work. So the pressure points at capabilities every single time, and the
suite stays green while the app cannot reach what was built.

**The rule.** A part that adds a capability ships **either** with the path that consumes it, **or**
with a test that names the missing consumer and **fails.** The second clause is the load-bearing
one: sometimes the consumer genuinely cannot exist yet — the calibration plot had no training step
for eight loops, and that was a correct sequencing call. The honest form of that is a red test with
a deadline, not a green suite over an unreachable module.

**The model already exists in this repository**, from `L34`:
`test_the_seal_holds.py::test_the_run_records_what_it_did_not_use` asserts the *limit* of the Train
slice, so the limit cannot be forgotten and closing `GUIDED-089` must change the test. That is the
pattern. It needs to be mandatory rather than admirable — which is the same move §06 made when
"adjudicate the report" stopped being a habit and became a written check.

**And the corollary for adjudication:** the §06 grep — *does anything outside a test file import
what the loop just built?* — is the detector for this class, and it only catches the import-shaped
instances. `stale_downstream` has importers; nothing renders it. Where the question is whether a
recorded thing reaches a consumer, **flip it and see if anything downstream moves.**

---

## 06 · Adjudicating the report

The half of this job that was never written down. A report is a **claim**, and the whole project
runs on the rule that a claim needs a record.

**Verify before accepting, and verify the load-bearing claim specifically.** Not everything — the
one thing the rest depends on. Pull the branch first; the agent's commits will not be in your tree.

```bash
git fetch -q origin TurboTab && git rebase origin/TurboTab
python docs/turbotab/tools/ledger.py stats          # do the counts match the report?
grep -c '"GUIDED-0NN"' docs/turbotab/data/findings.json   # was it actually filed?
make test                                           # does the documented gate still run? (§05)
```

**And one grep that is not about the report at all:** does anything outside a test file import what
the loop just built? A module reachable only from its own tests is a specification, and the report
will not say so, because from inside the loop it looks finished. This is how `GUIDED-058` was found.

**Then stop grepping and run it.** The adjudicator has now been wrong three times in the same way,
each time by searching for the shape expected rather than the shape written: `packs.findings` was
called from `engine.py` while the grep covered `api.py` and `ml/`; `AUDIT-001`'s sentence spanned two
f-string lines and a one-line pattern returned nothing; and `runPull` composes `"/evidence/" +
endpoint`, so a literal path search reported two surfaces unread that are fetched on every drive. A
grep answers *does this text appear*, and the question is almost always *does this run*.

The instrument already exists. `turbotab/pageharness.py` runs the page's real controller in node
against captured API responses, and `__harness.calls()` returns exactly which routes it fetched —
no pattern, no ambiguity. Feeding it live responses from a `TestClient` drive takes about thirty
lines and answers reachability questions that no search can. **Where a claim is about behavior,
drive it; reserve grep for claims that are genuinely about the file.**

### A claim that something does not exist requires reading, not searching

**This rule is about the adjudicator's own claims rather than the agent's, and it is here because it
has fired more often than anything in the list below.** An absence cannot be established by the
evidence that suggests it. Five instances, all the same shape:

1. *"Guided cannot declare a hypothesis-testing goal"* — without reading `turbotab/purpose.py`, which
   asks PREDICTION vs INFERENCE among the first questions the app asks. **The product owner caught
   it**, and the corrected row (`GUIDED-231`) is stronger than what was filed: *inference is not
   unasked, it is unwired.*
2. *"The field has no way to state an estimand for a tree ensemble"* — without reading
   `NUTRITION_PACK.md` §05, which names CoDA with ilr coordinates at line 516. An adversarial reviewer
   caught it; it is now `GUIDED-237`.
3. *"§04 has drifted from Tomova"* — on one reviewer's quote, without opening the paper. A second
   reviewer caught it and the truth was narrower and sharper: two words, not a drift (`AUDIT-045`).
4. *"The journal CSS is a rule missing its handler"* — written into L55's Part C. The agent measured
   it: **no figure was drawn at all.** The correction was bigger than the row.
5. *"21 built figure specs use the failing ramp"* — 17 are built and 4 are declared-pending, and after
   L55-C only **two** kinds have a renderer, so the ramp reaches **one** (`DRIVE-015`). **The inverse
   error**: asserting a *presence* without reading, which the four above should have predicted.

**The tool-side version of this rule was already written down** — §02's *a matcher that fires on prose
has silence that means nothing* — and four adjudicators' greps came back empty and were read as proof
anyway. **Before writing that something does not exist, open the file that would contain it and say
which file you opened.** A grep is evidence about text; an absence is a claim about behavior.

**What to look for, in order of how often it has mattered:**

1. **Was a named defect *class* filed, or only its instance?** The highest-value finding of `L26`
   was a class the agent named in a docstring and did not file. A class that lives only in prose
   will be forgotten. **This is the single most common gap in an otherwise good report.**
2. **Did a threshold move?** Never accept a moved threshold in the same loop as the change that
   pressured it. If a gate is measuring the wrong thing, correct **which quantity is gated**, on a
   *passing* run, with the reasoning recorded before it is load-bearing. After a breach the same
   correction is indistinguishable from relaxing a gate under pressure.
3. **Does new numerical code have its own tests?** `weak_calibration` was hand-validated and
   exercised only through a figure test. A hand-check that is not a test is a claim without a
   record — the project's own rule, one level in.
4. **Does the code return a value where it should return nothing?** The strongest habit this
   project has: `(None, None)` rather than `(0.0, 1.0)`, because those are the values of *perfect*
   calibration and returning them from ignorance asserts perfection.
5. **Did a sweep terminate where the sweeper's attention ended?** Sweeps find the class they were
   pointed at. Ask what the same lens would find one surface over.
6. **Is a capability being deleted where it should be routed?** The SMOTE defect was fixed by
   routing behind purpose and keeping an offered path, not by removal. The shelf is never shortened.

**Then, before writing the next prompt:** add the §03 log row, and name in Part A the one gap you
found. If you found none, say so — an empty Part A is a real outcome and should be visible.

---

## 07 · Checking in

```bash
git -C . log --oneline TurboTab | head -30
python docs/turbotab/tools/ledger.py stats
python docs/turbotab/tools/ledger.py check
git diff main...TurboTab --stat
cat docs/turbotab/BLOCKED.md 2>/dev/null
```

Three questions worth asking of any result:

1. **Does the `FIXED` count have tests behind it?** `check` enforces that a test is *named*; it
   cannot verify the test is any good. Spot-check two or three.
2. **How many went `NOT-A-DEFECT`?** A high rate means either the agents over-reported or the
   verifier is credulous. Read those notes specifically — that is where a loop quietly goes wrong.
3. **Did anything land where it should not have?** `git diff main...TurboTab --stat` answers it in
   one line.

---

## 08 · What not to hand an unsupervised loop

- **Row identity, and choices like it.** Design decisions with consequences across the whole project
  model. An agent can gather the evidence; it should not make the call alone.
- **Large extractions of untested, safety-critical logic.** Supervised, with characterization tests
  already in place.
- **New construction under a governing rule about what may be asserted.** Design work, not loop work.

Loops are for verification, for well-specified builds with clear gates, and for writing tests
against behavior that already exists. They are not for decisions you would want to argue about.

But the reframe in `ROADMAP.md` holds: those items are not permanently off-limits. They are blocked
on **one decision each**. Make the decision and the execution behind it becomes ordinary loop work.

---

## 09 · Standing dispositions

Rules extracted from loop prompts that have since retired, kept because they still bind:

- **`FIXED` requires a named regression test, verified to fail on revert.** No test, no `FIXED` —
  the finding stays `OPEN` and the test is written later. Roughly one in five first-attempt reverts
  is wrong and produces a plausible false failure, which is why the probe must fail *for the stated
  reason*.
- **"Guided avoids it" is never closure.** Streamlit never retires, so a defect still present in
  Classic stays `OPEN` even where the core or the Guided door has structurally resolved it. Note it
  `resolved-in-core; closes at L11 convergence of <page>` — verbatim, because that phrase is the
  queue for the convergence loop.
- **Tag, don't fix, siblings of a known pattern.** Add `sibling-of: <ID>` and move on. They get one
  batched build, not twenty inline ones.
- **Ambiguity is `OPEN`, never `FIXED`.** A wrongly-closed finding is worse than an open one.
- **A record that points at ephemeral storage will eventually lie, and it lies toward "the work is
  gone."** The original 48 import findings were declared unrecoverable while sitting committed in
  `docs/audit/`. Cite paths that are in the repository.
