[
 {
  "id": "AUDIT-042",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "narrative_for_report prints 'Exploratory analysis identified {total} data observations. {resolved} were addressed during the modeling workflow' from summary(), which counts EVERY insight and every insight with resolved=True \u2014 including the bridge entries _log_to_ledger writes resolved=True for on every log_methodology call (i.e. every button press). The 'Addressed observations:' list immediately beneath is filtered through _is_narrative_worthy, which excludes exactly those bridge entries. Count and list come from different filters.",
  "reader": "That N data-quality observations were identified and R of them were acted on during modeling. Both numbers are inflated by page visits and button presses, and the count can assert 'R were addressed' above an empty list. This paragraph is a Methods-facing sentence in the downloadable report.",
  "ev": "utils/insight_ledger.py:1170-1181 (narrative_for_report reads s['total'] and s['resolved']) vs :1173 (the list filters on _is_narrative_worthy) and :1060-1073 (summary() counts all insights unconditionally); _is_narrative_worthy at :1142-1168 excludes audit_only and 'Logged methodology decision' bridge entries. The bridge that sets resolved=True for every call is utils/session_state.py:674 onward (_log_to_ledger).",
  "reach": "Yes. pages/10_Report_Export.py:1256 appends _report_ledger.narrative_for_report() into report_lines under '## Key Observations and Resolutions'; report_lines is joined and returned at :1845 as the downloadable report.",
  "verifier": "Opened utils/insight_ledger.py:1170-1181 (narrative_for_report), :1060-1073 (summary), :1142-1168 (_is_narrative_worthy) and utils/session_state.py:675-712 (_log_to_ledger). All confirmed at HEAD: summary() counts len(self._insights) and every insight with resolved=True unconditionally, while the 'Addressed observations:' list at :1173 is filtered through _is_narrative_worthy, which excludes exactly the auto_generated implication=='Logged methodology decision' bridge entries that _log_to_ledger writes with resolved=True on every log_methodology call. I could find no filter on the counting side and no second producer of that sentence. The sentence lands in the downloadable report at pages/10_Report_Export.py:1256 under '## Key Observations and Resolutions' and is joined into report_lines at :1845 and written to report.md at :2402. A researcher reading 'Exploratory analysis identified N data observations; M were addressed' gets a count inflated by button presses that the list beneath does not contain."
 },
 {
  "id": "CONTRACT-001",
  "category": "WRONG_NUMBER",
  "severity": "critical",
  "mech": "pages/06 resolves split membership to POSITIONS in the frame `get_data()` returned at training time (`df.index.get_indexer(_split.train_labels)`), stores them as train/val/test_indices, and also stores the labels \u2014 but nothing reads the labels. pages/07 calls `get_data()` FRESH and does `df_raw.iloc[test_indices]` for the SHAP/permutation matrix and target, and again for subgroup membership. If the row set changed between Train and Explain, `.iloc` silently selects different people. Where the new frame is shorter an IndexError is caught by `except Exception: pass` and it falls back safely; where the new frame is the same length or longer, there is no error at all. The subgroup path is worse: `y_test_sub`/`y_pred_sub` come from the stored model results (old vintage) while `subgroup_labels` come from the fresh frame by position, so one person's prediction is paired with another's subgroup.",
  "reader": "That the SHAP plot, the permutation-importance table, and the subgroup performance table with its N, metric and 95% CI per stratum were computed on the held-out test set. They can be computed on a different set of people \u2014 often people the model trained on, which inflates every number in the direction of a better result.",
  "ev": "Opened pages/06_Train_and_Compare.py:567-574 (get_indexer into `df` from `df = get_data()` at :121) and pages/07_Explainability.py:181-205 (`df_raw = get_data()`; `raw_candidate = df_raw.iloc[test_indices]`; `y_raw = df_raw[target].iloc[test_indices]`; the trailing `except Exception: pass`) and :1179-1215 (`subgroup_labels = df_raw.iloc[test_indices][subgroup_var]` paired with stored y_test/y_pred, then `subgroup_analysis(..., n_bootstrap=200)` and `plot_forest_subgroups`). Also opened utils/session_state.get_data (:220-260) to confirm it returns df_engineered / filtered_data / raw_data and applies the active cohort filter.",
  "reach": "Yes. Any action between Train and Explain that changes the frame get_data() returns: starting or switching a cohort run, page 05 writing filtered_data, re-applying feature engineering, or a row-dropping cleaning action. Cohort switching is the canonical NHANES flow this app is built for (train on cycle A, switch to cycle B, open Explainability).",
  "verifier": "Mechanism reproduces at HEAD: pages/06_Train_and_Compare.py:567-572 stores positions via df.index.get_indexer; pages/07_Explainability.py:181-205 and :1179-1215 re-fetch get_data() and index it with .iloc[test_indices]; the subgroup path has NO try/except, pairs stored y_test/y_pred with fresh-frame subgroup_labels, then bootstraps 200 resamples and draws a forest plot \u2014 a subgroup table with CIs is exactly what goes into a paper. BUT the row's stated flow is refuted: I opened utils/session_state.py:409-419 and 420-424 \u2014 reset_downstream_results POPS train/val/test_indices and the row labels AND clears fitted_estimators/model_results, and utils/cohort_ui.py:235 and :396 both call it, as does reset_data_dependent_state (:530) and pages 01/03/04. Cohort switching therefore cannot reach this. The live route I could establish is pages/05_Preprocess.py:1014/:1017, inside the 'Build Pipelines' button, which writes or pops filtered_data (changing the row set get_data() returns) and never calls any reset: train, return to Preprocess, toggle the plausibility filter, rebuild, open Explainability. Popping the filter makes the frame LONGER, so .iloc raises nothing and the misalignment is silent."
 },
 {
  "id": "CONTRACT-005",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "Session archives write every DataFrame with to_parquet(index=False) and read it back with a bare pd.read_parquet, so the restored frame gets a fresh 0..n-1 RangeIndex. The lockbox's row labels are restored VERBATIM. If the frame carried any index other than a contiguous RangeIndex at save time, the restored labels name different rows and train_row_mask returns a wrong mask with no error \u2014 the sealed test set is silently no longer sealed. There is no restore-time assertion that any lockbox label exists in the restored index.",
  "reader": "That the held-out metrics in the paper came from rows the model never saw. After a save/restore cycle some sealed rows can be in training and some training rows in the test set, and every reported held-out number is optimistic by an unknown amount, with the lockbox status chip still reporting the original n.",
  "ev": "utils/session_manager.py:169-171 (_df_to_parquet_bytes; df.to_parquet(buf, index=False)), :211 and :221 (used for every frame), :504-506 (_read_parquet \u2014 plain read, no index restore, no assertion), :309-320 (_plain_label coerces and stores lockbox labels verbatim). utils/test_lockbox.py:534-545 (train_row_mask: `lbl not in test_set` over the restored index).",
  "reach": "Save a session archive and restore it, when the working frame carries a non-RangeIndex. The established route is MINE-024 below (a JSON upload installs its own index and utils/session_state.py:291-295 resets only NON-unique indexes). I checked the two paths that would have made this ordinary and they are safe: utils/combine.py execute_stack uses pd.concat(ignore_index=True), and page 01's cleaning actions drop COLUMNS, not rows. So this is real but narrower than the row implies \u2014 it needs the JSON route or an externally-indexed parquet.",
  "verifier": "Opened utils/session_manager.py and utils/test_lockbox.py. Mechanism confirmed verbatim: :169-171 to_parquet(index=False), :504-506 bare pd.read_parquet, :309-320 lockbox labels stored verbatim, and train_row_mask (utils/test_lockbox.py:534-545) is a pure `lbl not in test_set` membership test over whatever index it is handed \u2014 no restore-time assertion anywhere. I tried to refute it on reachability and instead found a MUCH more ordinary generator than the JSON route the flagger cited: pages/01_Upload_and_Audit.py:809 offers a one-click suggested action 'Drop duplicate rows' -> `d.drop_duplicates()`, which DROPS ROWS and leaves a gappy Int64 index; :827 then calls set_data(new_df), and utils/session_state.py:291-295 resets only NON-unique indexes, so the gappy index survives into raw_data. _DATAFRAME_KEYS (utils/session_manager.py:73-78) saves raw_data, df_engineered, working_table and filtered_data \u2014 all four with index=False. Save/restore then renumbers 0..n-1 while the sealed labels stay at their original values, and train_row_mask silently seals the wrong rows or none at all. This does not need the JSON route."
 },
 {
  "id": "CONTRACT-021",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "The report page re-derives the analysis cohort instead of reading the realized split. ml/splits.py computes target-trim thresholds from TRAINING rows only when a lockbox applies and exempts test rows from the trim; pages/10 recomputes quantiles over the WHOLE frame and drops rows on both tails including sealed test rows. Two different populations, no warning.",
  "reader": "The manuscript's stated analysis N and the Table 1 population are the population the models were fit and evaluated on. They are not \u2014 the reported N is the more aggressively trimmed one, and the trim thresholds it used were computed on data that includes the held-out set. The number that reaches the paper is the less defensible of the two.",
  "ev": "pages/10_Report_Export.py:600-627 (_build_analysis_cohort_df): target_numeric.quantile(...) over the entire frame at :622-623, trim_mask drops both tails at :624-625, no test-row exemption. Contrast ml/splits.py:270-285: basis = y[~is_test_row] if lockbox_applied else y (:274), and trim_mask = trim_mask | pd.Series(is_test_row, ...) (:279). The N is printed at pages/10:639-642 ('Table 1 can be regenerated from the analysis cohort (N=...)') and the same function feeds _build_manuscript_table1 at :649.",
  "reach": "Regression study with target trimming enabled on Prepare Splits (pages/06:295-334) -> train -> Report Export -> Manuscript Table 1 / analysis-cohort readiness. Both the readiness caption and the generated Table 1 come from _build_analysis_cohort_df.",
  "verifier": "Opened pages/10_Report_Export.py:600-660 (_build_analysis_cohort_df, _table1_readiness, _build_manuscript_table1) and ml/splits.py:245-290. Divergence is exactly as filed and live: page 10 takes quantiles over the whole frame at :622-623 and drops both tails at :624-625; splits.py:274 uses basis = y[~is_test_row] when lockbox_applied and :279 exempts test rows from trim_mask. Refutation attempts failed: the trim UI is reachable (pages/06:295-334 writes split_config.target_trim_enabled/lower/upper, consumed at :423-425), and both the printed N at :639-642 and the generated Table 1 at :649 come from the same function. One narrowing worth stating: the two populations coincide when lockbox_applied is False (grouped or chronological strategy, or a fallback re-draw at splits.py:257-262), so the mismatch requires the ordinary random/stratified-with-lockbox path \u2014 which is the default. Magnitude is bounded (roughly the trimmed test rows plus the threshold shift from including test targets in the quantile basis), but Table 1's N and per-feature summaries are manuscript numbers."
 },
 {
  "id": "CONTRACT-034",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "`reset_downstream_results` nulls provenance sections from a hand-typed list \u2014 eda, split, preprocessing, training, explainability, coach (+ feature_selection / feature_engineering under flags). `sensitivity` and `statistical_validation` are not in the list, so they survive a data change. The same function DOES pop `hypothesis_test_results` and `sensitivity_seed_results` from session_state. So the UI forgets and the manuscript remembers: the pages go blank while the Methods draft still carries the old numbers.",
  "reader": "That the statistical tests listed in the Methods, the corrected-significance count, and the seed-stability / feature-dropout numbers were computed on the data the paper describes. They can be from a superseded dataset, cohort or feature configuration. `get_completeness()` also reports both stages as done, so the TRIPOD-style checklist counts them as satisfied.",
  "ev": "Opened utils/session_state.py:461-472 (the `_sections` list, `setattr(prov, section, None)` \u2014 sensitivity and statistical_validation absent) and :436-443 (the _analysis_keys pop list, which DOES contain hypothesis_test_results and sensitivity_seed_results). Opened utils/workflow_provenance.py get_methods_context (:749-759 \u2014 puts seed_stability, feature_dropout and statistical_tests into ctx) and get_completeness (:643-644). Opened ml/narrative_engine.py:415 (`self.ctx = provenance.get_methods_context()`) and `_gen_statistical_validation` (:1275+, which prints the test names and the corrected count). Opened pages/10_Report_Export.py:682-697, where NarrativeEngine is the primary path.",
  "reach": "Yes. Any data change that triggers reset_downstream_results \u2014 new/replaced file, changed feature configuration, cleared cohort \u2014 followed by generating the report. The user sees page 09 and page 08 empty and reasonably assumes the report is clean.",
  "verifier": "Reproduces at HEAD. utils/session_state.py:461-464 lists sections eda/split/preprocessing/training/explainability/coach (+ the two flagged), and utils/workflow_provenance.py:262-263 shows `sensitivity` and `statistical_validation` are real fields that survive; :436-443 pops sensitivity_seed_results and hypothesis_test_results from session_state, so pages 08 and 09 go blank. get_methods_context (:749-751, :758-759) still passes seed_stability, feature_dropout and statistical_tests into the draft, and ml/narrative_engine.py:433-434 emits both sections \u2014 _gen_statistical_validation (:1275-1330) prints the test names and the corrected count from that stale record. One correction to the row: the 'new/replaced file' route is clean \u2014 reset_data_dependent_state pops workflow_provenance entirely (utils/session_state.py:521). The sharp route is the cohort switch (utils/cohort_ui.py:235/:396), which calls reset_downstream_results but keeps the provenance object, so cohort B's Methods carries cohort A's hypothesis tests and seed-stability numbers; feature/target change (pages/01:1090, :1150), FE apply (pages/03), feature-selection apply (pages/04:517, :564) and same-schema value changes (session_state:335) do the same."
 },
 {
  "id": "IMPORT-020",
  "category": "WRONG_NUMBER",
  "severity": "critical",
  "mech": "SUBSTANTIALLY NARROWED SINCE FILING \u2014 read the detail. Of the three shapes this row records, two are now fixed and one still reproduces silently. The ratio lower bound (rows_per < 1.5) has been deleted with a comment naming IMPORT-020, so partial follow-up at 1.30 rows/subject is now detected and grouped. The upper bound now routes to `unclear`, sets seal_basis=undetermined, and renders a loud warning instead of a clean lock. What survives is the whole-token NAME list: `_id_kind()` returns None for any column name not in `_SUBJECT_ID_TOKENS`, that column is never even considered, group_col stays None, the split is drawn by row, and seal_basis stays `cross_sectional` so the chip prints the ordinary clean lock.",
  "reader": "That no participant appears in both training and the held-out set, because the app's lock chip says the set was held out and says nothing about repetition. In fact every reported held-out metric on a repeated-measures cohort keyed by an unrecognised name is measured partly on subjects the model already trained on, so discrimination and calibration are inflated.",
  "ev": "utils/test_lockbox.py:210 rank_grouping_candidates \u2014 the lower bound is gone (comment at :242-250 cites IMPORT-020 by name); :261-264 sends over-repeating columns to `unclear`; :258-260 `kind = _id_kind(col); if kind is None: continue` is the surviving gate. utils/test_lockbox.py:177-188 _id_kind requires _name_looks_like_a_subject_id; :126-132 _SUBJECT_ID_TOKENS. utils/test_lockbox.py:427-428 sets SEAL_UNDETERMINED only when `unclear` is non-empty, which a name-gate miss never populates. MEASURED at HEAD in venv: 100 subjects/30 second visits keyed SUBJECT_ID -> ('SUBJECT_ID',100,130) [now detected]; 10x60 merge product -> None but routed to undetermined+warning; 60 subjects x 3 visits keyed 'SUBJ' -> None, no warning; same shape keyed 'SEQN' -> ('SEQN',60,180). Also None for 'ptid', 'subj_num', 'study_no', 'animal'.",
  "reach": "pages/01_Upload_and_Audit.py seals the lockbox at upload via ensure_lockbox on every dataset; no user action is needed to reach it. The trigger is only the column name: 'SUBJ', 'ptid', 'subj_num', 'study_no' all miss. 'SEQN', 'subject_id', 'patient_id' hit.",
  "verifier": "Opened utils/test_lockbox.py:126-188 (_SUBJECT_ID_TOKENS, _name_looks_like_a_subject_id, _id_kind), :210-264 (rank_grouping_candidates, lower bound gone, `kind = _id_kind(col); if kind is None: continue` surviving), :342-350, :427-428. Measured at HEAD in venv: _id_kind returns None for SUBJ, ptid, subj_num, study_no, animal; 'subject' for SEQN, subject_id, patient_id. My best refutation was the upstream override at pages/01_Upload_and_Audit.py:1104-1106, which passes cohort_structure_detection.entity_id_final as group_col and would bypass the name gate \u2014 ml/triage.py's _ENTITY_ID registry DOES declare 'subj' and 'ptid'. That refutation fails: `grep -rn 'detect_cohort_structure('` over pages/utils/ml finds callers only in tests, and `entity_id_detected`/`entity_id_override_value` are never assigned anywhere in the app (utils/state_reconcile.py:47-48 only clears them). So _entity_col is always None and the name gate is the only door. A repeated-measures dataset keyed 'SUBJ' is split by row, the same person lands on both sides, held-out metrics are optimistically biased, and seal_basis stays cross_sectional so the chip prints the ordinary clean lock."
 },
 {
  "id": "IMPORT-022",
  "category": "WRONG_NUMBER",
  "severity": "critical",
  "mech": "The lockbox's grouping detector rejects an ID column by NAME before it ever measures repetition. A column named 'SUBJ' holding 60 people x 3 visits is not recognised, no group column is found, the holdout is drawn by simple random split, and the same person lands in both training and the sealed test set. The seal then records seal_basis='cross_sectional' with basis_source='detected'.",
  "reader": "That the reported held-out performance is an out-of-sample estimate and that the study is one-row-per-person. Both are false: within-subject leakage inflates every test metric, and the app's own provenance record asserts a cross-sectional design over repeated measures.",
  "ev": "Opened utils/test_lockbox.py: _SUBJECT_ID_TOKENS (:126-132) is a whole-token allow-list; _name_looks_like_a_subject_id (:168-174) and _id_kind (:177-187) return None for anything not in it; rank_grouping_candidates (:255-260) does 'kind = _id_kind(col); if kind is None: continue' \u2014 the repetition it already computed is discarded. ensure_lockbox (:337-345) only sets group_col from ranked[0]; seal_basis defaults to SEAL_CROSS_SECTIONAL at :426 and is only overridden to SEAL_GROUPED at :439 when a group column exists; basis_source is recorded as BASIS_DETECTED at :517. MEASURED in the venv: _id_kind('SUBJ') -> None, rank_grouping_candidates -> [], detect_repeated_subjects -> None; the identical frame renamed to 'SEQN' -> [{'column':'SEQN','n_groups':60,'n_rows':180,'kind':'subject'}].",
  "reach": "Upload & Audit draws the lockbox automatically on every dataset. No user action can override the name gate \u2014 the guided grain question exists only in turbotab/, not in the Classic pages. Any cohort whose ID column is spelled outside the 25-token list (SUBJ, ptno, 'Anonymised Code', study-specific codes) is affected.",
  "verifier": "Opened utils/test_lockbox.py in full. rank_grouping_candidates does `kind = _id_kind(col); if kind is None: continue` BEFORE the `unclear` bucket is filled, so a name outside the 25-token allow-list is discarded even though its repetition (k=60, n=180) was already computed \u2014 it cannot even reach seal_basis='undetermined'. Measured in the venv: _id_kind('SUBJ')/'ptno'/'Anonymised Code' -> None, rank_grouping_candidates -> [], detect_repeated_subjects -> None; renamed to 'SEQN' -> [{'column':'SEQN','n_groups':60,'n_rows':180,'kind':'subject'}]. I tried three refutations and all failed: (1) pages/01_Upload_and_Audit.py:1104-1106 passes group_col=_entity_col from cohort_structure_detection, which looked like a user override \u2014 but grep shows detect_cohort_structure is IMPORTED at pages/01:38 and never called anywhere in the app, and no page writes entity_id_detected/entity_id_override_value, so _entity_col is unconditionally None; (2) ml/triage.py's own registry DOES declare 'subj' as a subject alias, so it would have caught this name \u2014 but it never runs, and even if it did, its `unique_ratio >= 0.5` gate at triage.py:255 rejects any column with more than 2 rows per entity (measured: detect_cohort_structure on the 3-visits-per-subject frame returns entity_id_detected=None); (3) no other repeated-measures warning fires on page 01. So the split is row-wise, the same person is in train and in the sealed holdout, and ensure_lockbox writes seal_basis=SEAL_CROSS_SECTIONAL with basis_source=BASIS_DETECTED. Both halves are paper risks: an inflated held-out metric, and a seal asserting a cohort structure it never measured."
 },
 {
  "id": "IMPORT-207",
  "category": "WRONG_NUMBER",
  "severity": "medium",
  "mech": "A pandas orient='split' JSON payload can install a non-unique index, applied with no uniqueness check and never reset. The lockbox draws its test set by index LABEL and the split decides membership by label, so sealing one label seals every row carrying it \u2014 but the lockbox's own n_test and fraction are computed from the drawn label list, not from the rows those labels reach.",
  "reader": "The seal chip on Upload & Audit states, in the researcher's own words, 'Test set: 15% (n=18 rows ...)'. The measured reality on a 40-subject x 3-visit payload is 51 rows held out and 69 trained on \u2014 42% where 15% was stated. A researcher writes the chip's number into the paper's split description.",
  "ev": "data_processor.py:270-277 \u2014 df.index = obj['index'] applied whenever the length matches, no is_unique check, no reset_index. utils/test_lockbox.py:333 eligible = df.index[y.notna()] (row-level, with duplicates), :478 _actual_fraction = len(test_labels) / len(eligible), :485 n_test = int(len(test_labels)), and the chip at :704 / :718 prints \"Test set: {lb['fraction']:.0%} (n={lb['n_test']} rows...\". ml/splits.py:257-258 then sets is_test_row = [lbl in sealed for lbl in kept_labels] \u2014 label membership, which sweeps in every duplicate.",
  "reach": "Upload a .json exported with pandas to_json(orient='split') from a repeated-measures frame indexed by subject ID, via pages/01_Upload_and_Audit. Narrow but real. Mitigating: seal_basis correctly reports repetition_found_grouping_abandoned, so the user IS told grouping was abandoned; it is the fraction and count that are wrong.",
  "verifier": "Opened data_processor.py:268-277, utils/test_lockbox.py:325-345 / :420-495 / :698-725, ml/splits.py:235/:257-258. Confirmed: df.index = obj['index'] at :274-275 gates on length only, and I grepped for is_unique / reset_index across data_processor.py, pages/01, test_lockbox.py and splits.py \u2014 there is no uniqueness guard anywhere on the load path. eligible at :333 is row-level with duplicates, n_test/_actual_fraction at :478-485 count drawn ENTRIES, but splits.py:258 seals by set membership so every row carrying a drawn label is swept in. The magnitude is larger than 'a rounding error': at k rows per subject the realized held-out share is roughly 1-(1-f)^k, so a requested 15% with 3 rows per subject seals about 39% of rows while the chip at :704/:718 and the ledger insight (pages/01:1128-1131) both say 15% \u2014 and that fraction reaches the Methods via ml/narrative_engine.py:806-812. One correction to the flag, against the owner: the stated mitigation does not apply. SEAL_ABANDONED (test_lockbox.py:448-453) only fires when a group COLUMN was found and had too few groups; repetition living in the INDEX is invisible to rank_grouping_candidates, so seal_basis stays SEAL_CROSS_SECTIONAL and the user is told nothing. Trigger is narrow (pandas to_json(orient='split') from a subject-indexed frame) \u2014 rank it by likelihood, not by size."
 },
 {
  "id": "IMPORT-213",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "The external-validation uploader loads the file with a bare load_tabular_data() and gates it on a missing-column check alone. None of the front-door import safeguards run: no inspect_json wrapper-key disclosure, no Import Doctor diagnose(), no transpose option, no encoding or duplicate-key repair. Every defect that preserves column names passes straight through, and the page then computes bootstrapped metrics with confidence intervals on whatever loaded.",
  "reader": "That the externally-validated metrics describe the external cohort as supplied. A JSON payload holding several record lists silently yields whichever wrapper key comes first \u2014 the page reports 'Loaded external dataset: 20 rows x 4 columns' as a success and validates on 20 rows of a 200-row file. The page's own copy calls external validation 'the gold standard for publication', so these are precisely the numbers a researcher copies.",
  "ev": "pages/07_Explainability.py:1110-1165 \u2014 render_guidance 'is the gold standard for publication' at :1112-1114; ext_df = load_tabular_data(ext_file, filename=ext_file.name) at :1126 with no records_key, no inspect_json, no diagnose(); the ONLY validation is missing_cols at :1131-1133; bootstrap_all_regression_metrics / bootstrap_all_classification_metrics at :1156-1158 and format_metric_with_ci at :1161. Contrast pages/01_Upload_and_Audit.py's JSON path, which calls inspect_json, renders the records-key selectbox and layout caption, and runs diagnose(). The silent first-wins wrapper loop is still live at data_processor.py:284-287.",
  "reach": "pages/07 Explainability -> 'External Validation' tab -> 'Upload External Validation Dataset' expander -> Validate on External Dataset. Requires trained models, which is the normal state at that point in the workflow.",
  "verifier": "Opened pages/07_Explainability.py:1108-1165 and data_processor.py:255-300 and ml/import_doctor.py:871-925. Verbatim as filed: load_tabular_data at :1126 with no records_key/inspect_json/diagnose, missing_cols at :1131-1133 as the sole gate, bootstrapped CIs at :1156-1161 under a banner calling this 'the gold standard for publication'. My refutation attempt was to show that every survivable defect breaks column names and therefore errors out loudly \u2014 it fails. Two concrete counterexamples: (1) data_processor.py:282-286, the wrapper loop returns the FIRST key in _JSON_WRAPPER_KEYS holding a list, silently, so a JSON file with two sibling record arrays (identical columns) validates on the wrong subset \u2014 page 01 hands this to a records-key selectbox instead; (2) import_doctor's recode_missing check (fix_kind at :596/:623/:642) catches sentinel codes like -999/7/9, which preserve dtype and column names perfectly and pass straight into predict(). Either produces an external-validation point estimate and CI that is simply wrong, on the surface a reviewer weighs most heavily."
 },
 {
  "id": "IMPORT-233",
  "category": "WRONG_NUMBER",
  "severity": "critical",
  "mech": "normalize_key decides case-folding from whichever single column it is handed (fold only if folding does not reduce distinct IDs), and diagnose_join/repair_keys/execute_join each normalize the two sides in separate calls. One stray-case duplicate on one side disables folding for that side only, so the two files end up in different canonical spaces: rows silently vanish AND a surviving row is paired with the wrong partner.",
  "reader": "That the analysis dataset is the correct merge of the two source files. Every N, every metric, every interval and every figure in the paper is then computed on a table that lost subjects and mis-attached measurements to the subjects it kept \u2014 and nothing on screen says so.",
  "ev": "ml/join_doctor.py:100-121 normalize_key (fold_case computed at :115-118 from text.str.lower().nunique() == text.nunique() on the single column passed in). MEASURED at HEAD with ./venv/bin/python: sites.specimen=['A01','B02'] normalizes to ['a01','b02']; labs.specimen=['A01','a01','B02'] normalizes to ['A01','a01','B02']. execute_join(inner) returns 1 row, and that row is (specimen='A01', site='x', g=2.0) \u2014 B02 is gone and A01 carries a01's lab value (2.0) instead of its own (1.0). The ledger recorded the row loss; the mis-pairing is worse than filed.",
  "reach": "Yes. pages/01_Upload_and_Audit.py:586 imports render_combine_step from utils.combine_ui; utils/combine_ui.py:37-38 imports diagnose_join/execute_join from ml.join_doctor and calls them at :285 and :311. This is the ordinary multi-file upload flow.",
  "verifier": "Re-measured at HEAD with ./venv/bin/python. ml/join_doctor.py:100-121 normalize_key still computes fold_case per-column (:115-118). sites.specimen=['A01','B02'] -> ['a01','b02']; labs.specimen=['A01','a01','B02'] -> ['A01','a01','B02']. execute_join(inner) returns exactly one row: specimen='A01', site='x', g=2.0 \u2014 B02 dropped and A01 carrying a01's value. I tried to refute on disclosure: I ran diagnose_join and describe_join (utils/combine_preview) on the same frames. diagnose_join returns blocking=[] and can_proceed=True, and combine_ui.py:290-292 explicitly suppresses the 'have no match' warnings. The ChangeMap DOES disclose the row loss ('in both files: 1 kept', '3 dropped', plus a subsample-bias consequence sentence), so the row-loss arm is visible to the user. What no surface discloses is the MIS-PAIRING: the change map reports '1 shared IDs' and the result looks normal while A01 carries the wrong lab value. That is a wrong number on the ordinary multi-file path (pages/01_Upload_and_Audit.py:586 -> utils/combine_ui.py:37-38, :285, :311) with no warning of any kind. Could not refute."
 },
 {
  "id": "IMPORT-234",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "join_doctor's key canonicalizer protects only against float conversion IT performs. When the ID column arrives already float64 \u2014 which happens the moment one row's ID is blank in any loader \u2014 values above 2^53 have already collapsed, and the decimal branch canonicalizes the collapsed digits as though exact. Nothing anywhere checks whether a numeric key column could have lost precision. Two participants become one, and the resulting duplicate keys are then explained to the researcher as repeated visits producing a fan-out.",
  "reader": "That N participants were merged. Two distinct people are silently one row-identity, and the app's own diagnosis ('Both files have several rows per ID, so every combination is produced') tells the researcher the duplication is expected study structure rather than data corruption.",
  "ev": "ml/join_doctor.py:59-84 (_canon_scalar) \u2014 its own docstring states 'passing IDs through float64 silently collides values above 2^53 ... which is a false merge \u2014 the worst outcome this module can produce', and the function then does `s = str(v).strip()` on whatever it was handed. There is no float64-magnitude check anywhere in the module.",
  "reach": "Any multi-file merge on a large numeric subject ID (NHANES SEQN, MRN, national registry IDs) where at least one row's ID is blank. Same root as IMPORT-260 at the stack path and IMPORT-209 at the JSON loader \u2014 one guard at the loader closes all three.",
  "verifier": "Opened ml/join_doctor.py:59-100. Mechanism confirmed and unguarded: _canon_scalar takes str(v) of whatever it is handed, and `grep '2\\*\\*53|9007199254740992|precision'` over ml/join_doctor.py, utils/combine.py and data_processor.py returns only the docstring at :62 that names this exact failure. A float64 ID at 9007199254740993 arrives as '9007199254740992.0', matches _DECIMAL_RE at :90, and is canonicalized to the collapsed integer as though exact \u2014 two participants become one key, and the duplicate is then narrated to the researcher as repeated visits. I could not refute the mechanism. I can refute one cited example and must narrow the severity accordingly."
 },
 {
  "id": "IMPORT-236",
  "category": "WRONG_NUMBER",
  "severity": "critical",
  "mech": "The index-like penalty is computed as `_looks_like_row_index(left) AND _looks_like_row_index(right)`. A measurement column (age, dose, visit number) paired against a bare 1..N row counter therefore has index_like=False, escapes both the confidence downgrade and the score penalty, and outranks the honest counter-to-counter pairing that IS penalised. The penalty makes the ranking strictly worse than no penalty at all, and the winner is offered at MEDIUM confidence \u2014 above combine_ui's low-confidence filter \u2014 so it is silently pre-selected as the default answer.",
  "reader": "That the two uploaded files were joined on a shared participant identifier, so each row of the working table describes one person. In fact the rows were fused on an arithmetic coincidence between two unrelated columns. Everything computed downstream \u2014 n, Table 1, every association, the grain check, the lockbox seal basis \u2014 describes a table whose row identity is fictional.",
  "ev": "ml/join_doctor.py:599 `index_like=_looks_like_row_index(lraw) and _looks_like_row_index(rraw)`; :293-299 the confidence branch that keys off self.index_like and returns 'low'/'medium' only when it is True; :610 `out.sort(key=lambda c: c.score, reverse=True)`. MEASURED at HEAD: two unrelated 50-row frames, left {row, age}, right {row, chol} -> find_key_candidates returns ('age','row') MEDIUM index_like=False score 0.319 ranked ABOVE ('row','row') LOW index_like=True score 0.150. The nonsense pairing wins and is the only non-low candidate.",
  "reach": "utils/combine_ui.py:261 calls find_key_candidates in the render path; :262 filters to non-low (which here keeps only the junk pairing); :277-282 puts the survivors in a plain st.selectbox, which pre-selects its first option. Multi-file upload on pages/01 with any two files that both carry a row counter \u2014 an extremely common shape for CSVs exported from a spreadsheet.",
  "verifier": "Opened ml/join_doctor.py:599 (`index_like=_looks_like_row_index(lraw) and _looks_like_row_index(rraw)`), :285-305 (the confidence branch keyed on self.index_like), :610 (sort by score), :321-339 (headline), and utils/combine_ui.py:261-282, :300-320. The AND is confirmed, so the honest counter-to-counter pairing is the only one penalised and the measurement-to-counter pairing outranks it at MEDIUM. My refutation attempt was the downstream preview: combine_ui does render a change map and commits nothing until a button. That mitigation is weaker than it looks, for two reasons I read in the code. First, combine_ui.py:293-296 explicitly suppresses the warnings that would name the problem \u2014 `if 'have no match' in w or 'have no ID at all' in w: continue`. Second, the caption under the pre-selected box is KeyCandidate.headline, which for this pairing renders \"'age' and 'row' share N IDs (X% of ..., Y% of ...)\" \u2014 the app asserts the junk pairing is an ID match. Pre-selected default plus a suppressed diagnostic plus an affirming caption is enough for a wrongly-merged analysis frame to reach a manuscript."
 },
 {
  "id": "IMPORT-248",
  "category": "WRONG_NUMBER",
  "severity": "critical",
  "mech": "_canon_scalar returns None for any key value whose lower-cased text is in _KEY_MISSING_TOKENS \u2014 a fixed set that includes 'na', 'none', '-', '--', '?', '.'. A study centre, cohort or site legitimately named NA is deleted as a missing-value token, and the app then warns that the row has 'no ID at all'.",
  "reader": "That every subject with an identifier was matched. In fact a whole named stratum is dropped from the merged table, and the disclosure the app does give is itself false \u2014 it says the row had no ID, when the app refused to read the ID it had. A reader takes the reported N and the per-site breakdown at face value.",
  "ev": "ml/join_doctor.py:48-49 _KEY_MISSING_TOKENS = {\"\", \"nan\", \"none\", \"null\", \"na\", \"n/a\", \"n.a.\", \".\", \"-\", \"--\", \"?\", \"missing\", \"unknown\", \"not available\"} and :83-84 in _canon_scalar. MEASURED at HEAD: centres ['NA','NB','NC','ND'] present identically in both files normalize to [None,'nb','nc','nd']; execute_join(inner) returns 3 rows (NB, NC, ND). Centre NA is gone.",
  "reach": "Yes, same path as IMPORT-233: pages/01:586 -> utils/combine_ui.py:37-38, :285, :311 -> ml/join_doctor.",
  "verifier": "Re-measured at HEAD. _KEY_MISSING_TOKENS at ml/join_doctor.py:48-49 and the check in _canon_scalar at :83-84 are unchanged. centres ['NA','NB','NC','ND'] identical in both files -> execute_join(inner) returns 3 rows; centre NA is gone. I tried to refute via the change map, which strengthened the row instead: describe_join produces RowGroup(label='no ID in a', n=1, detail='a blank ID matches nobody \u2014 these are never paired with b'). The value 'NA' is not blank, so the app deletes a real stratum AND asserts a false reason for it. The diagnose_join warning that says the same thing is additionally suppressed at utils/combine_ui.py:291. Reachability caveat worth recording: this bites categorical join keys with a legitimate token value (centre/site/region = 'NA' for North America, or a genuine 'Unknown' category), not numeric subject IDs \u2014 so it is real but narrower than the token list makes it look."
 },
 {
  "id": "IMPORT-254",
  "category": "WRONG_NUMBER",
  "severity": "moderate",
  "mech": "plan_stack intersects and unions RAW column labels with no normalisation, and combine.py has no equivalent of join_doctor's _name_similarity. Cycle files whose headers differ only by capitalisation or a trailing space stack into two half-empty columns that are really one variable, and nothing says so.",
  "reader": "That `age` has 50% missing data in the pooled file \u2014 a missingness pattern they will then median-impute or report as a limitation \u2014 when in fact `age` and `Age` are one complete variable that was split in half by a capital letter. Every statistic about that variable is computed on half the real values plus imputed fill.",
  "ev": "utils/combine.py plan_stack \u2014 shared/union sets built from raw labels. MEASURED at HEAD: cycle 1 ['SEQN','age','glucose'] stacked with cycle 2 ['SEQN','Age','Glucose '] gives shared_columns ['SEQN'] and a 4-column result carrying age, Age, glucose and 'Glucose ' separately, each null in half the rows.",
  "reach": "Multi-file stack path, utils/combine_ui.py:339. STATED AGAINST THE RECORD SO THIS IS NOT OVERSTATED: plan_stack DOES warn \"Only 1 of 5 columns appear in every file (20%). The rest will be blank for the files that lack them\", and combine_ui.py:342 renders it. That describes the shape, not the cause; a researcher has no reason to read it as \"two of these columns are the same variable\". Partial disclosure is why this is moderate and not high.",
  "verifier": "Opened utils/combine.py plan_stack:69-129 and execute_stack:140-170. Confirmed shared/union are built from raw labels (`shared &= set(col_sets[n])`) with no case-fold or strip anywhere in the module \u2014 the only normalisation in the file is at :294, inside a different helper, and execute_stack just pd.concat's the frames. So 'age', 'Age' and 'Glucose ' stack as separate half-empty columns. I tried to refute via the disclosure the reader honestly recorded, and it holds up less well than stated: the 'Only N of M columns appear in every file' warning is gated on `overlap_frac < 0.5` at :118-123. A survey file where one or two of twenty column names differ only in capitalisation sits at ~90% overlap and produces no warning at all \u2014 only the milder note at :124-128 that some columns 'will be blank for those rows'. A variable silently split in two, each half missing, then median-imputed downstream, is a corrupted analysis frame. Moderate rather than high because the resulting missingness is loud on later pages, but its cause is never named."
 },
 {
  "id": "IMPORT-256",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "After a merge, the subject-ID join key is pre-selected as a model feature. The only reserved-column filter is utils.combine.reserved_columns(), which returns ['__source_file'] and nothing else; neither the join key recorded by execute_join nor the lockbox's group_col is ever registered, and the feature multiselect defaults to every column on offer.",
  "reader": "That the reported held-out performance measures signal in the predictors. With a random (cross-sectional) split the model can memorise row identity from the ID, inflating the test metric; and the ID routinely rises to the top of the SHAP / permutation-importance table, so the manuscript's 'most important predictor' figure is a participant number.",
  "ev": "Opened utils/combine.py:194-196 \u2014 reserved_columns() returns [SOURCE_COLUMN] literally, and is_reserved_column (:199-207) is a prefix match on '__source_file' only. Opened pages/01_Upload_and_Audit.py:931-933 \u2014 feature_options = [c for c in all_cols if c != target_col and not _is_reserved(c)], the only filter applied. Opened pages/01_Upload_and_Audit.py:967-989 \u2014 on first render default_features = list(feature_options), i.e. everything, and that is passed as the multiselect default. The join key is known to ml/join_doctor.execute_join and group_col to utils/test_lockbox.ensure_lockbox; neither writes anywhere pages/01 reads.",
  "reach": "Combine two files on Upload & Audit -> the merged frame keeps the ID column -> the feature multiselect renders with every column pre-ticked -> Save configuration -> train. No warning is shown. Sharper on a grouped seal, where group_col IS the subject ID and handing it to the model leaks group membership into the predictor set.",
  "verifier": "Opened utils/combine.py:194-207 \u2014 reserved_columns() returns literally [SOURCE_COLUMN] and is_reserved_column is a prefix test on '__source_file' alone. Opened pages/01_Upload_and_Audit.py:931-933 (feature_options excludes only the target and reserved columns) and :966-989 (on first render default_features = list(feature_options) \u2014 everything \u2014 passed as the multiselect default). Neither ml/join_doctor.execute_join's join key nor utils/test_lockbox.ensure_lockbox's group_col is written anywhere page 01 reads; I grepped for a downstream ID guard and the only one is pages/05_Preprocess.py:115-158, which fires solely for CATEGORICAL features above 50 levels, so a numeric SEQN/MRN/join key passes every gate silently. Two things keep me from calling it a top-category wrong number, and they are in the revised statement."
 },
 {
  "id": "IMPORT-257",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "There is no control anywhere for declaring the subject column. pages/01 imports detect_cohort_structure and never calls it; nothing in the repository ever writes cohort_structure_detection, so it is always the empty default. The comment at pages/01:1101-1103 says 'A declared subject/entity ID always wins over auto-detection' \u2014 true of the code and vacuous in practice, because group_col is always None from that route and ensure_lockbox falls back to a heuristic over column names and cardinality. When the heuristic misses, the same person is split across training and the sealed test set and the user has no way to correct it.",
  "reader": "That the held-out evaluation is independent of the training data. With repeated measures and a missed subject column, the model was scored on visits from people it trained on, and the reported discrimination or R2 is optimistic by an amount the paper cannot bound.",
  "ev": "pages/01_Upload_and_Audit.py:38 (detect_cohort_structure imported), :1104-1106 (_entity_col read from cohort_structure_detection and passed as group_col). Grep for writers of cohort_structure_detection across pages/ utils/ ml/: only the default construction at utils/session_state.py:152 and the reset at :508 \u2014 every other hit (pages/01, 02, 05, 06, 10) is a read. utils/test_lockbox.py:341-346 is the heuristic fallback.",
  "reach": "Every upload. Credit where due: when the heuristic DOES fire, pages/01:1107-1114 discloses the grouped split plainly and test_lockbox.py:700-712 warns when the group column clusters rather than identifies. The defect is the silent case, and the absence of any way to override it.",
  "verifier": "Opened pages/01_Upload_and_Audit.py, utils/state_reconcile.py, utils/session_state.py, ml/triage.py, utils/test_lockbox.py. I tried to refute via a hidden writer \u2014 attribute-style mutation would evade a session_state grep, so I grepped `entity_id_final` and `detect_cohort_structure` across pages/ utils/ ml/ instead. Result: detect_cohort_structure is defined at ml/triage.py:139 and imported at pages/01:38 and called NOWHERE. Every entity_id_final hit is a read (pages/01:1105, 02:221, 05:234, 06:171, 10:1245); utils/state_reconcile.py:44-52 only CLEARS entity_id_detected/entity_id_override_value, never sets them. So group_col reaching ensure_lockbox is always None and the utils/test_lockbox.py:341-346 heuristic is the only subject detector, with no user override anywhere. When it misses on repeated-measures data the same person lands in training and in the sealed test set and the reported test metrics are optimistic, silently. Additional consequence the row did not state: cohort_type_final is likewise always 'cross_sectional', so pages/06:422 `use_group_split=bool(use_group_split and entity_id_final)` is always False \u2014 the train/val split never groups by subject either."
 },
 {
  "id": "MINE-002",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "The lockbox seals index LABELS and tests membership by label, but session save writes every dataframe with to_parquet(index=False), so restore hands back a fresh RangeIndex. Any frame whose index was non-contiguous \u2014 which is exactly what the row filters produce \u2014 comes back renumbered, and the sealed labels then select whoever now sits at those positions.",
  "reader": "That the resumed session's held-out metrics were computed on the same people the lockbox sealed. After a save/resume the train/test partition silently changes membership: rows that were quarantined become trainable and vice versa, so the reported test metric is contaminated and is not reproducible from the saved file.",
  "ev": "Opened utils/session_manager.py:169-172 (_df_to_parquet_bytes -> df.to_parquet(buf, index=False)), :74-79 (_DATAFRAME_KEYS = raw_data, df_engineered, working_table, filtered_data), :504-506 (_read_parquet, no index restore). Opened utils/test_lockbox.py:480 ('labels': list(test_labels)) and :534-546 (train_row_mask -> 'lbl not in test_set for lbl in index'). The contradiction is written down twice in the repo: session_manager.py:300-305 says the sealed holdout 'must survive a save/restore VERBATIM ... string labels would no longer match the dataframe's integer index on restore', and ml/pipeline.py:255-262 (apply_plausibility_filter) says in as many words that 'renumbering the survivors 0..n-1 would leave both of those sets pointing at whoever now happens to sit at those positions'. apply_plausibility_filter returns df.loc[mask] (:272) and pages/05_Preprocess.py:1014 stores that non-contiguous frame as filtered_data.",
  "reach": "Page 05 plausibility filter (or any cohort run, which filters by label in utils/cohorts.apply_cohort:500-502) -> Save session -> Resume. filtered_data/df_engineered return renumbered; get_data() prefers them over raw_data (utils/session_state.py:233-238) and train_row_mask is called on their index from pages/02, 03, 04, 05 and utils/cohort_ui. turbotab/archive.py:275-278 fixed this in the core with index=True; Classic did not inherit it.",
  "verifier": "Opened utils/session_manager.py: _df_to_parquet_bytes at :169-172 is `df.to_parquet(buf, index=False)`; _DATAFRAME_KEYS at :73-78 includes filtered_data and df_engineered; the restore loop at :613-616 is `st.session_state[key] = _read_parquet(zf, member)` and _read_parquet (:504-506) does no index restoration. Opened utils/test_lockbox.py:480 ('labels': list(test_labels)) and :534-546 (train_row_mask tests `lbl not in test_set` against the frame's index). Opened ml/pipeline.py:245-272 \u2014 apply_plausibility_filter returns df.loc[mask] and its docstring names this exact hazard; pages/05_Preprocess.py:1014 stores that non-contiguous frame as filtered_data (and :995-1005 deliberately puts sealed rows back into it, so it is a label-identified frame). Refutations tried and all failed: (1) utils/state_reconcile.py is 56 lines and reconciles only column names, never the index; (2) there is no index or label validation in the lockbox restore block (session_manager.py:764-791); (3) I expected the post-restore ensure_lockbox call on page 01 to notice and redraw \u2014 but _lockbox_signature (test_lockbox.py:104-118) hashes with `hash_pandas_object(df, index=False)`, so renumbering the index leaves the signature IDENTICAL and ensure_lockbox returns the existing lockbox at :355-356. Stale labels are therefore silently applied to renumbered rows by pages 02/03/04/05 and cohort_ui. Precondition: a row filter (or cohort run) plus save-and-resume."
 },
 {
  "id": "MINE-010",
  "category": "WRONG_NUMBER",
  "severity": "medium",
  "mech": "_content_fingerprint returns None when a frame contains unhashable cells, and set_data's value-change branch fires only when BOTH the old and new fingerprints are non-None and differ. Unknown therefore means clean: re-uploading corrected data with the same column set clears nothing, and every model, metric, figure and report artefact computed from the OLD values survives under the new dataset's name.",
  "reader": "That the metrics on screen after a data correction describe the corrected data. They describe the superseded upload. Nothing on any page distinguishes the two, and the schema-only fallback is not disclosed anywhere in the UI.",
  "ev": "Opened utils/session_state.py:262-271 \u2014 the try/except returns None, and the docstring now states the behaviour as intended ('callers treat None as unknown and fall back to schema-only invalidation'). :300-317 \u2014 the branch is 'elif (old_df is not None and old_fp is not None and new_fp is not None and old_fp != new_fp)', so a None on either side skips reset_downstream_results entirely. MEASURED in the venv: _content_fingerprint(DataFrame with a list-valued cell) returns None; a normal frame returns an int. On reachability I checked the loaders: the JSON path is now protected \u2014 _finalize_json_frame calls _stringify_nonscalar_cells (data_processor.py:472-477) \u2014 but load_tabular_data's parquet branch (data_processor.py:513-514, load_parquet) applies no such conversion, and a parquet list/array column lands as object-dtype cells that hash_pandas_object cannot hash.",
  "reach": "Established for the parquet upload route by code inspection (no stringify on that branch) but I did NOT load an actual list-typed parquet file end to end, so mark this one down: the mechanism is confirmed, the specific file shape is inferred. The nested-JSON route named in the original row is closed at HEAD.",
  "verifier": "Opened utils/session_state.py:262-271 and :300-317 \u2014 _content_fingerprint returns None on any exception and the value-change branch is `elif (old_df is not None and old_fp is not None and new_fp is not None and old_fp != new_fp)`, so None on either side skips reset_downstream_results entirely; unknown really does mean clean. I closed the row's own stated inference gap by MEASURING the parquet route end to end in the venv: a DataFrame with a list-valued column written by to_parquet and read back through data_processor.load_tabular_data yields object cells holding numpy.ndarray, and _content_fingerprint on that frame returns None. Refutation attempted and only partly succeeded: I looked for a second invalidation that would cover the gap. utils/test_lockbox._lockbox_signature (:104-118) has its own except branch falling back to f'{shape}|{columns}|{notna().sum().sum()}', so a re-upload that changes MISSINGNESS still redraws the lockbox and resets downstream. But a corrected value that does not change shape, columns or the non-null count leaves that fallback signature identical too, so nothing fires and every model, metric, figure and report artefact from the old values survives under the new dataset's name. Real, but narrow: it needs a parquet upload with a list/array-typed column AND a same-schema re-upload \u2014 worth stating in that order so it is not over-prioritised."
 },
 {
  "id": "MINE-011",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "Stability selection skips failed bootstrap fits with a bare 'except Exception: continue' that decrements nothing, then divides the selection counts by n_bootstrap (attempted) rather than by the number of fits that succeeded. Every feature's selection probability is deflated in proportion to the failure rate.",
  "reader": "That each reported selection probability is the fraction of subsamples in which the feature was chosen, and that the stated subsample count was achieved. If half the fits fail, every probability is halved, the threshold quietly selects fewer features (or none), and the result string still asserts 'across 100 subsamples' \u2014 a count the run did not achieve.",
  "ev": "ml/feature_selection.py:248-258 \u2014 try: model.fit(...) / except Exception: continue at :252-253, selection_probs = selection_counts / n_bootstrap at :256, selected thresholded at :257. details['n_bootstrap'] at :263 reports the ATTEMPTED count and the description string at :273-274 states 'across {n_bootstrap} subsamples'. Nothing anywhere records a failure count.",
  "reach": "pages/04_Feature_Selection.py:326-337 runs it when 'stability' is among the selected methods; result.description is rendered at :395 and the per-feature selection_probabilities at :445-448. A concrete partial-failure path exists for classification: rng.choice subsampling can hand LogisticRegression a subsample containing a single class, which raises and is swallowed \u2014 routine with a rare outcome class. ml/publication.py:270 maps the method into the Methods section.",
  "verifier": "Opened ml/feature_selection.py:212-276 and pages/04_Feature_Selection.py:326-337, :395, :445-452. Unchanged at HEAD: rng.choice at :234 is unstratified, the bare 'except Exception: continue' at :252-253 decrements nothing, and :256 divides by n_bootstrap. Refutation attempt failed \u2014 I looked for a stratification or single-class guard on the subsample and there is none, so LogisticRegression(penalty='l1', solver='saga') at :243-246 raises whenever a rare outcome class is entirely excluded from a 50% subsample, which is common at low prevalence. Two distinct manuscript surfaces are affected: the per-feature selection probabilities rendered at :395/:445-452 are uniformly deflated by the failure rate, and the threshold at :257 then silently drops borderline features from the final model. The description string at :273-274 asserting 'across {n_bootstrap} subsamples' is false by the same amount. Note the Lasso (regression) branch rarely raises; this is a classification-path defect."
 },
 {
  "id": "MINE-014",
  "category": "WRONG_NUMBER",
  "severity": "critical",
  "mech": "Train & Compare stores the held-out set as POSITIONS into the frame it saw (df.index.get_indexer(...)). Explainability re-fetches a frame with get_data() and indexes those positions with .iloc. get_data() prefers df_engineered over filtered_data over raw_data and applies the active cohort filter, so the frame can have a different row set on the second visit and the positions land on different people. Page 06 also writes test_row_labels \u2014 the label-based answer \u2014 and page 07 never reads it.",
  "reader": "That the SHAP values, permutation importances, PDPs, Bland-Altman agreement and subgroup breakdowns in the paper describe the same held-out patients the reported test metrics were computed on. They can silently describe a different set of rows, with the correct-looking n.",
  "ev": "pages/06_Train_and_Compare.py:567-569 (train/val/test_indices stored as get_indexer positions) and :574 (test_row_labels stored). pages/07_Explainability.py:181-185 (df_raw = get_data(); raw_candidate = df_raw.iloc[test_indices]), :196 (y_raw = df_raw[target].iloc[test_indices]), :1179-1180 and :1196 (subgroup_labels = df_raw.iloc[test_indices][subgroup_var]). No occurrence of test_row_labels anywhere in pages/07. utils/session_state.py:232-238 (get_data precedence) and :259 (return apply_cohort(df)).",
  "reach": "Train on page 06, then start or switch a cohort run (get_data applies the cohort filter at utils/session_state.py:259), then open Explainability. Also reachable if df_engineered is rebuilt between the two visits (pages/03_Feature_Engineering.py:1252) . No error, no warning, no length check on either read.",
  "verifier": "Opened pages/06_Train_and_Compare.py:565-574, pages/07_Explainability.py:181-205 and :1179-1200, utils/session_state.py:232-259, utils/cohort_ui.py:202-238, pages/03_Feature_Engineering.py:1245-1280. Mechanism confirmed: positions in, .iloc out, test_row_labels written at pages/06:574 and read nowhere (repo-wide grep: only the write and the invalidation list at utils/session_state.py:414). No length check on either read; the permutation path's try/except catches only a raised IndexError, so a frame that is still long enough silently yields the wrong people, and the subgroup path at :1180 has no try/except at all. BOTH routes the row cites are refuted, but a third survives."
 },
 {
  "id": "MINE-027",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "calculate_regression_metrics silently drops non-finite (y_true, y_pred) pairs and computes MAE/RMSE/R2/MedianAE on the survivors, returning the ordinary four-key dict with no count of what was dropped. The only trace is a logging.warning nobody sees in a Streamlit app.",
  "reader": "That the reported test R-squared / RMSE were computed on the full held-out set. If a degenerate target back-transform (the docstring names the trigger: a power transform fit on a near-constant target) makes 30% of test predictions NaN, R-squared is computed on the 70% the model handled and reported as the model's test R-squared, on an unstated N.",
  "ev": "ml/eval.py:27-52 \u2014 finite = np.isfinite(y_true) & np.isfinite(y_pred) at :29, logging.getLogger(__name__).warning(...) at :32-36, y_true, y_pred = y_true[finite], y_pred[finite] at :37, then the ordinary {'MAE','RMSE','R2','MedianAE'} return at :47-52 with no n_dropped. The all-NaN return below 2 surviving pairs (:38-40) is honest; the partial case is not.",
  "reach": "pages/06_Train_and_Compare.py:1426 (test_metrics) and :1446 (train_metrics) \u2014 the headline metrics rendered on Train & Compare and carried into model_results, which pages/10_Report_Export.py:1145-1152 prints as 'Test RMSE' / 'Test R2'. Trigger requires box-cox/yeo-johnson target transform with an inverse that goes non-finite on some test rows; the CI/bootstrap path on page 07 uses a different function.",
  "verifier": "Opened ml/eval.py:14-52 and pages/06_Train_and_Compare.py:490-525 and :1410-1450. Code is unchanged: the finite mask at :29, the logging.warning at :31-36, the survivor-only scoring at :37-52 with no n_dropped key. Reachability holds \u2014 pages/06:504-517 offers box-cox and yeo-johnson via PowerTransformer(standardize=False), :519 stores the fitted transformer, and :1413-1420 inverse-transforms test predictions before :1426 calls calculate_regression_metrics with the original-scale y. A box-cox inverse is exp(log(1+lam*x)/lam), which returns NaN for any prediction with lam*x+1 <= 0 \u2014 routine when a regressor extrapolates below the transformed training range. The dropped rows are exactly the ones where the model blew up, so the surviving-subset RMSE/R2 is biased optimistic, and pages/10:1145-1152 prints it as 'Test RMSE'/'Test R2'. I could not refute it: there is no upstream guard on inverse-transform output, and the honest all-NaN branch only triggers below 2 survivors."
 },
 {
  "id": "MODELS-009",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "The null and linear baselines \u2014 the comparator the headline claim is made against \u2014 are transformed through `fitted_preprocessing_pipelines[first_model[0]]`, i.e. whichever model's per-model pipeline happens to be first in dict insertion order, which is checkbox order. Per-model pipelines are the product's differentiator precisely because they differ: if the first model's pipeline reduces to 5 principal components or applies no scaling, the LinearRegression baseline is fitted on that. Training {ridge, rf} and {rf, ridge} give different baseline numbers. The transform is wrapped in a bare except that falls back to raw arrays with no message.",
  "reader": "\"Our model outperforms a linear baseline by X\" \u2014 where X moves with the order the analyst ticked the model checkboxes, and can be inflated by a baseline handicapped through a dimensionality-reducing pipeline it had no reason to inherit. The on-screen caption says only \"Baselines are evaluated on the same held-out test set, on the original target scale\" \u2014 nothing indicates the baseline's feature preprocessing came from another model.",
  "ev": "pages/06_Train_and_Compare.py:2105-2114 \u2014 comment `# Use the first model's preprocessing pipeline for baselines`, then `first_model = list(st.session_state.get(\"fitted_preprocessing_pipelines\", {}).keys())` / `pipe = st.session_state[\"fitted_preprocessing_pipelines\"][first_model[0]]` / `try: X_train_t = pipe.transform(...) except Exception: X_train_t = np.array(X_train_base)`. The dict is populated in training order at :1522.",
  "reach": "Automatic. The comment at :2085 states baselines are \"advertised as automatic: compute them whenever models exist and no (current) baseline results do\". Any run with two or more models whose preprocessing differs reaches it with no user action.",
  "verifier": "Opened pages/06_Train_and_Compare.py:2082-2122. Confirmed the comment, the `first_model[0]` selection out of fitted_preprocessing_pipelines (populated in training order, i.e. checkbox order), and the bare `except Exception` falling back to raw arrays with no message. Tried to refute on the grounds that the baseline never reaches a manuscript surface; it does, twice: pages/10_Report_Export.py:586-597 _summarize_baselines feeds manuscript_context, :1428-1442 writes the Baseline Comparison markdown table under the sentence 'evaluated on the same held-out test set', and ml/latex_report.py:785-796 writes the same numbers into the .tex as 'For reference, null and simple baseline models evaluated on the same held-out test set achieved: ...'. Neither surface names which model's preprocessing produced them. The comparator the headline claim is made against changes with checkbox order, and can silently be computed on raw untransformed arrays."
 },
 {
  "id": "STATE-003",
  "category": "WRONG_NUMBER",
  "severity": "critical",
  "mech": "Sibling of MINE-001 with a second, independent trigger inside pages/05 itself, and this is the one that cannot fail loudly. pages/05 builds the factor and bound lists over the full numeric_features, then passes a FILTERED list (numeric_features_safe, with already-log/power/PCA-transformed engineered columns removed) into build_preprocessing_pipeline while still handing over the unfiltered `uf` and `pb`. UnitHarmonizer would raise a numpy broadcast error \u2014 but plausibility gating is a SEPARATE per-model checkbox, so a model with plausibility_gating=True and unit_harmonization=False gets pb without uf, PlausibilityGate's min() truncation absorbs the length mismatch, and bound j is applied to column j' != j in silence.",
  "reader": "That \"Plausibility gate (NHANES)\" in the recipe means each variable was checked against its own reference band. In fact one variable was checked against another's; every value outside that foreign band became NaN and was immediately median-imputed, so the exported data have no missingness, the recipe string is unchanged, and the affected variable's distribution has been quietly flattened toward its median.",
  "ev": "pages/05_Preprocess.py:930 and :932 build unit_config and plausibility_bounds over the full `numeric_features`; :1029-1030 `use_unit`/`use_plaus` are read as INDEPENDENT per-model checkboxes; :1068-1069 `uf = ... if unit_config and use_unit else None`, `pb = plausibility_bounds if use_plaus and plausibility_bounds else None`; :1119-1120 `numeric_features_safe = [f for f in numeric_features if f not in _all_excluded]` is what is passed as `numeric_features=` to the real build at :1123, while `uf`/`pb` are passed unchanged. ml/preprocess_operators.py PlausibilityGate.transform: `n_cols = min(X_arr.shape[1], len(self.lower_bounds_), len(self.upper_bounds_))`.",
  "reach": "Requires feature engineering to have produced log/power/PCA columns (so _all_excluded is non-empty) plus a model with plausibility gating on and unit harmonization off. Both are ordinary checkbox states on pages/03 and pages/05. The asymmetry is the danger: the loud operator is optional and the silent one is not.",
  "verifier": "Opened pages/05_Preprocess.py:928-932 (unit_config and plausibility_bounds built over the full `numeric_features`), :1029-1030 (use_unit and use_plaus read as independent per-model checkboxes), :1068-1069, :1116-1123 (numeric_features_safe passed as numeric_features= while uf/pb are passed unchanged), :798-820 (the two checkboxes are genuinely separate widgets), and ml/preprocess_operators.py:82-95. Confirmed the asymmetry the row rests on: UnitHarmonizer would raise on the length mismatch, PlausibilityGate's `n_cols = min(...)` truncates and applies bound j to column j'. One narrowing I verified and the row does not state: pages/05:931 makes plausibility_bounds conditional on `any_unit`, so the silent case needs at least two configured models \u2014 one with unit harmonization on (so bounds get built at all) and another with plausibility gating on and unit harmonization off \u2014 plus an FE-produced log/power/PCA column with the matching transform enabled so _all_excluded is non-empty. Inside that conjunction the harm is exactly as filed and completely silent: an NHANES improbability band for one biomarker is applied to a different column, the out-of-range values become NaN, and the very next step median-imputes them."
 },
 {
  "id": "STATE-008",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "`get_pipeline_recipe` is an introspector that re-derives the preprocessing description from the fitted objects, and it has no branch for several options `build_preprocessing_pipeline` can produce. The MAD branch reads `mad_threshold` / `n_mad`, neither of which OutlierCapping ever sets (it stores a `params` dict), so `getattr(..., 3)` returns the literal 3 \u2014 a hardcoded constant printed as if it were the applied parameter. Yeo-Johnson, MinMax scaling, target encoding, ordinal encoding and the whole numeric_passthrough block emit nothing at all.",
  "reader": "The Methods sentence describing preprocessing. Measured examples: with MAD capping at a user-set threshold of 2.0 or 5.0 the recipe prints 'MAD capping (3\u00d7 MAD)' in both cases \u2014 a wrong number in a Methods section. With Yeo-Johnson + MinMax + ordinal encoding + one passthrough numeric column, the entire recipe reads 'Numeric features (2): Impute (median) / Categorical features (1): Impute (most_frequent)' \u2014 the power transform, the scaler, the encoder and a whole feature block are absent, and the feature count is wrong too. With target encoding the encoder vanishes entirely, so the Methods never mentions that a categorical variable was encoded USING THE OUTCOME, which is the single most leakage-sensitive preprocessing choice available.",
  "ev": "Opened ml/pipeline.py `get_pipeline_recipe` (transformer loop matches only 'numeric'/'categorical'; numeric step loop handles 'log' but not 'power_transform' or 'power_nan_guard'; scaler branch covers StandardScaler and RobustScaler only; MAD branch reads attributes that do not exist) and `build_preprocessing_pipeline` (:327-407, which builds 'power_transform', 'power_nan_guard', MinMaxScaler, TargetEncoder, OrdinalEncoder). Opened ml/preprocess_operators.py OutlierCapping (:98-118 \u2014 stores self.params, never lower_percentile/mad_threshold). Measured: built and fitted three pipelines and printed the recipe (results above).",
  "reach": "Yes. The recipe is rendered into the exported manuscript at pages/10_Report_Export.py:1317 and :1334, and shown in the UI at pages/06_Train_and_Compare.py:2564. All the unreported options are ordinary selections on the Preprocess page.",
  "verifier": "Reproduces at HEAD and is worse than stated. ml/pipeline.py get_pipeline_recipe (:474-550): the transformer loop matches only 'numeric'/'categorical', so the whole 'numeric_passthrough' block built at :435 is omitted; the numeric step loop handles 'log' but not 'power_transform' (:357, yeo-johnson) or 'power_nan_guard'; the scaler branch covers only StandardScaler and RobustScaler, so MinMaxScaler (:378, :433) prints nothing; the encoder branch covers only OneHotEncoder, so TargetEncoder (:396) and OrdinalEncoder (:401) print nothing. The MAD branch at :523-525 reads mad_threshold then n_mad then falls back to the literal 3, and I opened ml/preprocess_operators.py OutlierCapping (:98-133): it stores self.params and its MAD default is params.get('threshold', 3.5). So at DEFAULTS the recipe asserts '3\u00d7 MAD' for a capping that used 3.5 \u2014 a hardcoded wrong number, not merely a missing one, and unlike the percentile branch it has no hasattr guard to fall through. Reachable in the export: pages/10_Report_Export.py:1317 and :1334 write the recipe into the report body (and pass no plausibility_mode, so the 'rows filtered' line is dropped there too); pages/06:2564 shows it in the UI."
 },
 {
  "id": "STATE-010",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "Three compounding defects in the number that decides which predictors enter the published model. (1) Feature Selection hands the selectors a raw, unscaled matrix \u2014 there is no scaler anywhere in pages/04 \u2014 while ml/feature_selection.py fits Lasso at a FIXED absolute alpha=0.01, so a variable in mg/dL and the same variable in mmol/L are shrunk differently by ~18x purely on units. (2) stability_selection's per-bootstrap `except Exception: continue` skips the increment while the denominator stays n_bootstrap, so a bootstrap that failed to converge is reported as evidence the feature was not selected. (3) consensus_threshold = max(1, len(results)//2), so running one or two methods makes 'consensus' mean 'any single method said yes'.",
  "reader": "That the reported selection probabilities are the fraction of bootstraps in which the feature was chosen, and that the 'consensus' predictor set survived agreement between methods. A feature at 0.30 may have been selected in 30 of 40 bootstraps of which 10 fitted, and the unit of measurement of a covariate changes the published variable list.",
  "ev": "pages/04_Feature_Selection.py:137-145 \u2014 SimpleImputer(strategy='median') then `X = df.loc[mask, numeric_features].values`; grep for 'Scaler|scale' in pages/04 returns NOTHING. ml/feature_selection.py:240 (Lasso(alpha=0.01, max_iter=10000)), :248-253 (`except Exception: continue`), :256 (selection_probs = selection_counts / n_bootstrap). pages/04:345 (consensus_threshold = max(1, len(results) // 2)), :359 (logged), :523/:529 (read back into the export).",
  "reach": "The standard Feature Selection page. Worth crediting what is right: the page correctly scopes selection to training rows via train_row_mask (:141-145) and discloses the temporary imputation \u2014 the leakage discipline is sound; only the scaling and the denominators are wrong.",
  "verifier": "Opened pages/04_Feature_Selection.py and ml/feature_selection.py. All three mechanisms live at HEAD. (1) `grep -ni 'scaler|scale'` over pages/04 returns NOTHING; :137-145 is SimpleImputer(median) then raw .values straight to the selectors; ml/feature_selection.py:241 is Lasso(alpha=0.01) at a fixed absolute alpha. I checked whether the module scales internally \u2014 it does not; the only other Lasso is LassoCV at :43, which tunes alpha but is equally unit-dependent unscaled. (3) pages/04:345 consensus_threshold = max(1, len(results)//2) confirmed. The consequence reaches the manuscript directly: pages/04:510-512 assigns `data_config.feature_cols = consensus` and `selected_features = list(consensus)`, i.e. the consensus IS the published predictor set, and :529-542 records it into provenance. Reachability is the ordinary Feature Selection page."
 },
 {
  "id": "STATE-014",
  "category": "WRONG_NUMBER",
  "severity": "medium",
  "mech": "The split now reports row LABELS, but page 06 stores POSITIONS derived from them and page 07 consumes those positions against a freshly fetched frame with no check that it is the same frame. If get_data() returns a different number of rows than at split time, .iloc[test_indices] selects the wrong rows silently; when the positions run off the end, the IndexError is swallowed by a bare 'except Exception: pass' and the code falls back without saying anything.",
  "reader": "That the SHAP/explainability figures describe the held-out test observations and that the paired true values are theirs. They can describe a different set of rows entirely \u2014 a figure plotted from the wrong data, with no error and no visual cue. test_row_labels are already stored in session state and are not used here.",
  "ev": "pages/07_Explainability.py:181-206 \u2014 df_raw = get_data() at :181, test_indices = st.session_state.get('test_indices') at :182, raw_candidate = df_raw.iloc[test_indices] at :185, y_raw = df_raw[target].iloc[test_indices].values at :196, except Exception: pass at :204-205. Same pattern for subgroup analysis at :1179-1196. utils/session_state.py:409-414 pops train/val/test_indices alongside train/val/test_row_labels, so the label-based keys exist and are unread on this path.",
  "reach": "pages/05_Preprocess.py never calls reset_downstream_results \u2014 verified by grep, it is absent from that file. So: train on page 06 (positions recorded), return to Preprocess and rebuild pipelines with plausibility_mode='filter' (writes a SHORTER filtered_data at :1014), then open Explainability. With no feature engineering in play, get_data() now serves the shorter frame and the stored positions no longer address the same rows. Requires an out-of-order revisit, which the Streamlit sidebar permits freely; I did not drive the app to measure it.",
  "verifier": "Opened pages/06_Train_and_Compare.py:569-574, pages/07_Explainability.py:175-206 and :1179-1196, utils/session_state.py:400-420, pages/05_Preprocess.py:965-1020. Confirmed live: page 06:569 stores df.index.get_indexer(test_labels) \u2014 positions against the frame at split time \u2014 while :574 stores test_row_labels that page 07 never reads; page 07:185/:196 iloc's those positions into a freshly fetched get_data() with no identity check, and :204-205 swallows the IndexError. I verified the row's key claim myself: reset_downstream_results (session_state.py:400-420, which pops test_indices) is genuinely absent from pages/05_Preprocess.py, so a post-training return to Preprocess that rebuilds pipelines with plausibility_mode='filter' writes a shorter filtered_data at :1014 while trained_models, fitted_preprocessing_pipelines and test_indices all survive. The silent case is the dangerous one \u2014 a slightly shorter frame keeps positions in range, so SHAP inputs, y_raw and subgroup labels are drawn from the wrong rows with no error. Confidence marked down on likelihood, not mechanism: this needs an out-of-order revisit the sidebar permits but nothing prompts, and I did not drive the app. If the fix is cheap, take it: page 07 should use test_row_labels via .loc, which is already stored."
 },
 {
  "id": "STATE-025",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "The test set is passed to Explainability as POSITIONS into whatever frame get_data() returns, not as row labels. pages/05 writes and pops `filtered_data` inside its Build Pipelines handler \u2014 a row-dropping write to the frame get_data() serves \u2014 and never calls reset_downstream_results, so test_indices computed on page 06 survive a frame identity change. `df_raw.iloc[test_indices]` on a shorter or differently-ordered frame either raises IndexError (loud) or returns the WRONG ROWS (silent), and the whole reconstruction is wrapped in a bare `except Exception: pass` that swallows even the loud case and falls back to X_test.",
  "reader": "That permutation importance, the SHAP background, the external-validation labels and the subgroup forest plot were computed on the held-out cohort. In fact X and y can come from different rows: a plausible outcome vector attached to the wrong participants. Permutation importance against mismatched outcomes reads as \"no feature matters\"; the subgroup table's per-subgroup N and metrics are for the wrong people.",
  "ev": "pages/06_Train_and_Compare.py:569 `st.session_state.test_indices = df.index.get_indexer(_split.test_labels).tolist()` (positions; labels are also written and go unread). pages/07_Explainability.py:185 `raw_candidate = df_raw.iloc[test_indices]`, :196 `y_raw = df_raw[data_config.target_col].iloc[test_indices].values`, :204-205 `except Exception: pass`, and :1196 `subgroup_labels = df_raw.iloc[test_indices][subgroup_var].values` with NO try/except at all. pages/05_Preprocess.py:1014 `st.session_state[\"filtered_data\"] = filtered_df` and :1017 `st.session_state.pop(\"filtered_data\", None)` \u2014 grep confirms pages/05 never calls reset_downstream_results.",
  "reach": "Train on 06, return to 05 and press Build Pipelines with plausibility mode = filter (or toggle it off, which pops filtered_data), then open 07. Sidebar navigation makes this two clicks. Cohort switching is NOT a route \u2014 utils/cohort_ui.py:234 and :396 both call reset_downstream_results, which pops test_indices.",
  "verifier": "Opened pages/06_Train_and_Compare.py:560-575 (positions written via get_indexer, labels also written and unread), pages/07_Explainability.py:182-207 (`df_raw.iloc[test_indices]`, `df_raw[target].iloc[test_indices]`, and the bare `except Exception: pass` at :204-205), :1194-1197 (subgroup_labels with no try/except), pages/05_Preprocess.py:1005-1017, and utils/session_state.py:220-238. Two refutations attempted. The first partly succeeds and narrows the row: get_data() prefers df_engineered over filtered_data, so the drift only bites when feature engineering has NOT been applied. The second fails and makes the rest worse: I read the sealed-row restoration at pages/05:995-1013 and it puts the held-out rows BACK into filtered_df while dropping training rows, so the frame is shorter and the test rows sit at different positions but are all still present \u2014 which is precisely the case where iloc returns wrong rows silently instead of raising. Explainability's y_raw, SHAP/PDP inputs and the subgroup performance table are then computed on mismatched rows, and the loud case is swallowed at :204-205."
 },
 {
  "id": "STATE-031",
  "category": "WRONG_NUMBER",
  "severity": "moderate",
  "mech": "Table 1's denominator population is RE-DERIVED at export time instead of being read from what the split used: _build_analysis_cohort_df takes the frame get_data() currently returns, re-applies target-notna, and RECOMPUTES the trim quantiles from that frame. Any row-membership change since the split \u2014 most concretely pages/05's plausibility-filter write to `filtered_data`, which never triggers an invalidation \u2014 gives a different frame, hence different quantiles and a different cohort. (The row's baseline half is MODELS-009, reported separately.)",
  "reader": "That Table 1 describes the participants the models were trained and evaluated on. It can describe a slightly different population \u2014 different N, different means, different trim boundaries \u2014 while the Results table beside it reports models fitted on the other one. Both tables are well-formed, so neither can fail visibly.",
  "ev": "pages/10_Report_Export.py:600-627 _build_analysis_cohort_df \u2014 `analysis_df = df.copy()` from the page-level get_data(), `analysis_df.loc[analysis_df[target_col].notna()]`, then `q_lo = float(target_numeric.quantile(split_config.target_trim_lower))` / `q_hi = ...quantile(...)` recomputed rather than read from the split; :633 _table1_readiness calls it. Compare ml/splits.py:283-289, which computes its own q_lo/q_hi at split time and records n_trimmed. pages/05_Preprocess.py:1014/:1017 is the frame-changing write with no reset (same root as STATE-025).",
  "reach": "Established as far as the code: the cohort is recomputed on every export rather than read. I did NOT construct an end-to-end run showing the two cohorts diverging numerically \u2014 that needs the pages/05 filter-after-split sequence \u2014 so treat the divergence magnitude as unquantified. The structural defect (a decision implemented twice) is confirmed.",
  "verifier": "Opened pages/10_Report_Export.py:599-643 and ml/splits.py:269-289 side by side, and the divergence is stronger and more deterministic than the row claims \u2014 it needs no data change at all. When target trimming is on, ml/splits.py:274-280 computes q_lo/q_hi from TRAINING ROWS ONLY (`basis = y[~is_test_row]` when the lockbox applied) and then explicitly re-admits every test row (`trim_mask = trim_mask | is_test_row`), so no held-out row is ever trimmed. pages/10:615-624 recomputes both quantiles from the whole current cohort and drops every row outside them, test rows included. Two different thresholds and a different membership rule, on every export, with no state drift required. Table 1's n and every characteristic in it therefore describe a population the model was neither fitted nor evaluated on. Narrowing worth recording: split_config.target_trim_enabled defaults False (utils/session_state.py:78) and is user-enabled on pages/06:295-333 for regression only, so the deterministic form needs trimming on; without it the row still holds through the pages/05 filtered_data write (same root as STATE-025)."
 },
 {
  "id": "STATE-033",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "For a multiclass outcome where the explainer returns a 3-D array (n_samples, n_features, n_classes) \u2014 the modern shap shape for tree models \u2014 the compute path silently reduces it to sv_plot[:, :, -1], the LAST class, and sets class_label = None. The render path then draws the summary plot with no class annotation at all (the title is only set 'if cl'), and the bar chart is titled 'Mean Absolute SHAP Value (Global Importance)'.",
  "reader": "That the SHAP figure and the mean-|SHAP| ranking describe the model globally. They describe one arbitrary class. Every intermediate value is individually plausible \u2014 a valid SHAP array, a plot that renders, a sensible-looking ranking \u2014 so nothing in the app or the figure can reveal the substitution, and the figure is stored in shap_matplotlib_figs for export.",
  "ev": "Opened pages/07_Explainability.py:477-488 \u2014 class_label is set only on the isinstance(sv_raw, list) branch; the else branch sets class_label = None. :490-493 \u2014 'if sv_plot.ndim == 3: sv_plot = sv_plot[:, :, -1]  # take last class', with no label written. :513-520 stores {'shap_values': sv_plot, 'class_label': class_label}. :757 reads cl = s.get('class_label'); :787-788 'if cl: ax.set_title(f\"SHAP Values ({cl})\")' \u2014 so with cl None the plot carries no class attribution. :768-770 re-slices ndim==3 again at render (dead for stored values, but the second normalisation point the row names). :793-794 stores the figure into shap_matplotlib_figs for the report.",
  "reach": "Multiclass classification (>2 classes) + any tree/boosting model on the Explainability page \u2014 the default SHAP route. Note the two-class 3-D case picks the positive class, which is correct content but still unlabelled.",
  "verifier": "Opened pages/07_Explainability.py. Compute path :477-493 confirmed verbatim: class_label is assigned only inside `isinstance(sv_raw, list)`; the ndarray branch sets class_label=None and then `if sv_plot.ndim == 3: sv_plot = sv_plot[:, :, -1]` with no label written. Render path :757/:787-788 gates the title on `if cl:`, and the bar chart title at :816 is the unconditional 'Mean Absolute SHAP Value (Global Importance)'. The figure is stored to shap_matplotlib_figs at :793-794 for the report. Refutations tried and failed: 'all_shap_values' is stored at :519 'for class switching' but grep across pages/ shows :519 is its ONLY occurrence \u2014 there is no class selector, so the user cannot recover the other classes. And the 3-D shape is not hypothetical at HEAD: measured with the project's own shap 0.52.0 / sklearn 1.9.0, TreeExplainer.shap_values on a 3-class RandomForest returns a (10, 4, 3) ndarray, so the live path is the unlabelled last-class slice. A figure captioned as global importance that is in fact one arbitrary class is a figure plotted from the wrong data and it leaves the building in the report. (Binary is content-correct \u2014 [:,:,-1] is the positive class \u2014 but equally unlabelled.)"
 },
 {
  "id": "STATE-037",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "get_data() serves df_engineered when it exists and only falls through to filtered_data otherwise. Feature engineering runs before preprocessing in the workflow, so a user who then enables plausibility filtering in 'filter' mode writes filtered_data that no page will ever read \u2014 the rows they excluded stay in the training data and in every count. Separately, reset_downstream_results never clears filtered_data (only set_data and reset_data_dependent_state do), so a leftover filtered_data keeps shrinking the dataset across every partial reset.",
  "reader": "That the reported analytic n reflects the exclusions the app told them it applied. Page 05 prints a message about rows being removed from the training data; if df_engineered exists, nothing downstream honours it, and the models are fitted on the rows the researcher believes they excluded.",
  "ev": "utils/session_state.py:232-238 (`df_eng = st.session_state.get('df_engineered'); if df_eng is not None: df = df_eng` \u2014 filtered_data only in the else branch) and :259. pages/05_Preprocess.py:1014 (`st.session_state['filtered_data'] = filtered_df`), :1005-1012 (the st.info telling the user rows were removed). pages/03_Feature_Engineering.py:1252 (`st.session_state['df_engineered'] = df_engineered`). filtered_data is popped only at utils/session_state.py:306 (set_data) and :512 (reset_data_dependent_state); reset_downstream_results spans :338-502 and never touches it.",
  "reach": "Apply feature engineering on page 03, then turn on plausibility filtering in filter mode on page 05. Both are ordinary steps in the documented order.",
  "verifier": "Opened utils/session_state.py, pages/05_Preprocess.py, pages/03_Feature_Engineering.py. get_data (utils/session_state.py:232-238) confirmed: df_engineered wins outright and filtered_data is only reached in the else branch. pages/05 writes st.session_state['filtered_data'] at :1014 and \u2014 the part that makes this a claim, not just dead state \u2014 :1005-1012 prints an st.info asserting that plausibility filtering removed rows from the training data and that N held-out rows were kept. With df_engineered present that sentence is false: nothing reads filtered_data, the excluded rows train the model and appear in every N. I tried to refute via an invalidation that would clear df_engineered on page 05 \u2014 `grep reset_downstream_results|reset_data_dependent` over pages/05 returns NOTHING, so page 05 never invalidates anything. Second half also confirmed by reading: filtered_data is popped only at utils/session_state.py:306 (set_data) and :512 (reset_data_dependent_state) and explicitly by utils/cohort_ui.py:215; reset_downstream_results (:338-502) never touches it."
 },
 {
  "id": "STATE-038",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "reset_downstream_results is the single invalidation point, and a set of manuscript-facing result keys is absent from both of its key lists, so they survive every data change, target change, feature-selection change and lockbox redraw. `partial_dependence` is cleared but `pdp_results` is not; `sensitivity_seed_results` is cleared but `sensitivity_dropout_results` and `sensitivity_dropout_baseline` are not; `table1_df`/`table1_metadata` are cleared but `manuscript_table1_df`/`manuscript_table1_metadata` are not; `manuscript_export_context` and `compiled_pdf` are never cleared.",
  "reader": "That the partial-dependence curves, the feature-dropout sensitivity table and Table 1 in the exported manuscript were computed from the models and cohort the Results section reports. They can be artefacts of a superseded analysis, presented as current. ml/publication.py:1331 sets `_has_feature_dropout` from the stale key and emits a \"### Sensitivity Analysis\" section on that basis \u2014 so the Methods asserts a sensitivity analysis was performed on a model that no longer exists.",
  "ev": "utils/session_state.py:390-475 reset_downstream_results \u2014 the `_analysis_keys` list at :435-440 and the report-artifact tuple at :447-453 contain none of those keys (verified by reading both lists). Producers/consumers: pages/07_Explainability.py:625 writes pdp_results, pages/10_Report_Export.py:328 and :1565 read it; pages/08_Sensitivity_Analysis.py:499-500 write the dropout keys, ml/publication.py:1331 reads sensitivity_dropout_results; pages/10:1061-1062 write manuscript_table1_*; pages/10:2171 writes compiled_pdf and :2606 puts it in the ZIP. Only preprocess_built_model_keys has since been fixed (:398).",
  "reach": "Train -> Explainability (run PDP) -> Sensitivity (run dropout) -> back to Upload/Feature Selection and change the target or feature set (reset fires, models are cleared) -> retrain -> Export. The reset visibly clears the models, which is exactly what makes the survivors invisible.",
  "verifier": "Read both key lists in utils/session_state.py:390-475 (_analysis_keys at :435-440 and the report-artifact tuple at :447-453) and traced every named key. Three of the six survive and matter: pdp_results (written pages/07:625, read pages/10:328 and :1565, cleared nowhere \u2014 `partial_dependence` next to it IS cleared at :430), sensitivity_dropout_results/_baseline (written pages/08:499-500, read by ml/publication.py:1331 and :1500 to assert a feature-dropout sensitivity analysis was performed), and manuscript_export_context (written pages/10:1060/:2214, read at :1177 under `if manuscript_context is None`, so a STALE one wins over rebuilding). Two of the six are inert and should be dropped from the row so the fix is not oversized: compiled_pdf is popped at pages/10:1971 whenever _latex_cache_key changes, and manuscript_table1_df/manuscript_table1_metadata are written at :1061-1062 and, by grep, never read anywhere. The confirmed harm is a Results/Discussion carrying PDP figures and a dropout-sensitivity claim belonging to a model and target that the reset visibly destroyed."
 },
 {
  "id": "T0-BUILD-006",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "A categorical target is encoded with LabelEncoder().fit_transform(y.astype(str)). LabelEncoder sorts class labels lexicographically, so class 1 \u2014 the positive class, the event, what every classification metric is about \u2014 is whichever level sorts LAST alphabetically. 'stable' beats 'improved', 'dead' loses to 'alive', 'no' loses to 'yes'. Nothing in Classic renders or offers the mapping: a repo-wide grep for `classes_` across pages/ returns nothing.",
  "reader": "That precision, recall, F1, AUROC, the sign of every coefficient, the direction of every SHAP value and the meaning of the calibration curve all describe the outcome they intended. Where the alphabet disagrees with the clinical convention, every one of those describes the complement \u2014 internally consistent, entirely plausible, and answering the opposite question.",
  "ev": "ml/splits.py:294-302 \u2014 `if _target_is_categorical(y): label_encoder = LabelEncoder(); y = pd.Series(label_encoder.fit_transform(y.astype(str)), index=y.index)`. The encoder is stashed at pages/06_Train_and_Compare.py:449 as `target_label_encoder` and read only at pages/07_Explainability.py:198 to re-encode raw labels \u2014 never to display the mapping. `grep -rn \"classes_\" pages/` returns zero hits.",
  "reach": "Every classification run with a string/categorical target, which is the normal case for clinical outcomes. No user action can surface or change it. Contrast ml/binary_text.positive_class_finding, which exists and is not on this path.",
  "verifier": "Opened ml/splits.py:293-302 (LabelEncoder().fit_transform(y.astype(str)), lexicographic), pages/06_Train_and_Compare.py:449 (encoder stashed) and pages/07_Explainability.py:198 (read only to re-encode). Refutation attempts, both failed: (a) `grep -rn classes_ pages/ utils/ ml/` returns zero hits, and there is no inverse_transform on the classification path \u2014 pages/06:2704-2772 plots the confusion matrix from the encoded 0/1 arrays with no class labels; (b) ml/binary_text.py:289 positive_class_finding and :420 detect_binary_text, which would put the choice in front of the user, are never called from pages/, utils/ or ml/ outside binary_text.py itself. So the event class is decided by alphabetical order and is never rendered, offered or recorded. 'Case' vs 'Control' makes Control the positive class; 'improved' vs 'stable' makes stable the event. Every precision, recall, F1, PPV, sensitivity and specificity in the manuscript is then reported for the wrong class with nothing on screen to reveal it."
 },
 {
  "id": "TEST-018",
  "category": "WRONG_NUMBER",
  "severity": "high",
  "mech": "OutlierCapping in MAD mode computes scale = 1.4826 * median(|x - median|). For any column where at least half the values equal the median \u2014 every binary flag with prevalence != 50%, every integer code with a dominant level, every mostly-zero count \u2014 mad is 0, so lower_bounds_ == upper_bounds_ == median, and transform's np.clip forces the entire column to that constant. The predictor is destroyed inside the pipeline. Shapes stay valid, nothing warns, and the recipe line still reads \"MAD-based outlier clipping\".",
  "reader": "That the model was fitted on the predictor set named in the Methods and that outlier handling only trimmed extremes. In fact one or more binary/categorical-coded predictors contributed exactly nothing: their coefficients, SHAP values and permutation importances are zero for a mechanical reason, and every reported metric belongs to a smaller model than the one described.",
  "ev": "ml/preprocess_operators.py OutlierCapping.fit (method=='mad' branch): `mad = np.nanmedian(np.abs(X_arr - med), axis=0); scale = 1.4826 * mad; self.lower_bounds_ = med - threshold*scale; self.upper_bounds_ = med + threshold*scale`, and transform: `return np.clip(X_arr, self.lower_bounds_, self.upper_bounds_)`. MEASURED at HEAD: n=500, a 30%-prevalence binary `smoker` goes from 2 unique values / var 0.1914 to 1 unique value / var 0.0000; bounds for that column come back [0.0, 0.0]. A 50/50 binary and a continuous age both survive intact, which is why this reads as fine in casual testing.",
  "reach": "pages/05_Preprocess.py:775-795 offers \"MAD-based removal\" per model in the outlier-treatment selectbox, help text \"More robust than z-score\", threshold default 3.5; pages/05_Preprocess.py:1026-1027 packs it into the config and it becomes a numeric-branch step in build_preprocessing_pipeline. One selectbox choice on the Preprocess page.",
  "verifier": "Opened ml/preprocess_operators.py:98-133 (OutlierCapping.fit mad branch and transform) and pages/05_Preprocess.py:775-795, :1024-1027, :1123-1140. Re-measured at HEAD in venv: n=500, 30%-prevalence binary + normal age; MAD bounds come back [0.0, 0.0] for the binary and it goes 2 unique values -> 1, variance 0.0, while age survives with 500 unique values. Refutation attempts that failed: (a) binary flags are not routed to the categorical branch \u2014 pages/05:110-112 assigns by dtype via get_numeric_columns, so a 0/1 numeric flag is a numeric feature; (b) nothing downstream catches a zero-variance column \u2014 I read the numeric step order in ml/pipeline.py:350-385 and there is no VarianceThreshold; the next step is the scaler, which maps the constant to 0 silently. The model is then fitted and reported on a feature set that no longer contains that predictor, while the recipe line still says MAD-based clipping."
 },
 {
  "id": "COACH-003",
  "category": "FALSE_CLAIM",
  "severity": "moderate",
  "mech": "pages/06 passes `primary_model=st.session_state.get(\"primary_model\", \"\")` and nothing in the repository ever WRITES that session key, so the value is always empty and _detect_heteroscedastic_residuals always takes its fallback: `key = primary_model if primary_model in model_results else next(iter(model_results))` \u2014 the first model in dict insertion order, i.e. checkbox order. The finding's manuscript_text carries no model name at all.",
  "reader": "Reading the Discussion limitations: \"residual variance was not constant across the predicted range (Spearman rho = X between absolute residuals and predictions), so uniform-width prediction intervals would be miscalibrated\" \u2014 as a statement about the model the paper reports. It is a statement about whichever model was ticked first, with no attribution, so the reader cannot even tell there is a question.",
  "ev": "ml/model_coach.py:1435 the fallback; :1467-1472 the manuscript_text, which unlike the `finding` string at :1454-1457 omits the model name; :1498 the call from run_post_training_diagnostics. pages/06_Train_and_Compare.py:1660 passes the never-written key; :1662-1676 upserts the result as an Insight carrying manuscript_text. utils/insight_ledger.py:1218 `text = (i.manuscript_text or \"\").strip() or ...` inside discussion_points_for_manuscript, consumed at ml/narrative_engine.py:1681. Repo-wide grep: the only write to any `primary_model` name is utils/workflow_provenance.py's template context, never the session key. pages/06:1587 computes the right answer (`primary_model=best_model_name or ''` into record_training) one block earlier and does not store it.",
  "reach": "Any regression run where some model's |Spearman rho| between absolute residuals and predictions is >= 0.3 and at least 20 finite test rows exist. Fires automatically after training; goes to the Discussion via the ledger.",
  "verifier": "Opened ml/model_coach.py:1425-1476 and pages/06_Train_and_Compare.py:1657-1679. Confirmed `key = primary_model if primary_model in model_results else next(iter(model_results))` and that manuscript_text at :1467-1472 omits the model name that the `finding` string at :1454-1457 carries. Confirmed the session key is never written: grep for assignments to primary_model finds only utils/workflow_provenance.py:710 and ml/narrative_engine.py:614-616, both template-context dicts, never st.session_state['primary_model'] \u2014 while pages/06:1587 computes the right answer one block earlier and discards it. Also checked that the insight actually reaches the Discussion rather than being filtered: it is unresolved, not audit_only, and its implication is not 'Logged methodology decision', so utils/insight_ledger.py:1142-1167 _is_narrative_worthy passes it and :1207 discussion_points_for_manuscript emits its manuscript_text verbatim. The manuscript then asserts non-constant residual variance with a specific Spearman rho, unattributed, measured on whichever model happened to be first in checkbox order \u2014 which may not be the model the paper reports."
 },
 {
  "id": "COACH-007",
  "category": "FALSE_CLAIM",
  "severity": "medium",
  "mech": "Three of the coach's four preprocessing insights have no resolver anywhere in pages/, ml/ or utils/ (only preprocess_outlier_handling is resolved, at pages/05:1372), and none of the four carries a manuscript_text. discussion_points_for_manuscript sends every unresolved, narrative-worthy insight into the Discussion's limitations list, so they can never be anything but limitations.",
  "reader": "That the study's stated limitations are limitations. The generated Discussion lists a reassurance \u2014 'Your ... are scale-sensitive; the app's default pipeline already standardizes features for them' \u2014 and a neutral fact \u2014 'N feature(s) have missing values' \u2014 as study limitations, in the app's coaching voice and with the '(s)' pluralization the rest of the codebase avoids.",
  "ev": "ml/model_coach.py:845 (preprocess_skewness_transform), :890 (preprocess_outlier_handling), :918 (preprocess_feature_scaling), :947 (preprocess_missing_tree_native); the exact strings at :922-925 and :950-952. Only preprocess_outlier_handling appears in a resolver (pages/05_Preprocess.py:1370-1372). utils/insight_ledger.py:1233-1238 routes unresolved narrative-worthy insights to limitations; consumed at ml/narrative_engine.py:1681.",
  "reach": "Yes. generate_preprocessing_insights runs from pages/05_Preprocess.py; the limitations reach the Discussion through NarrativeEngine on the ordinary export path.",
  "verifier": "Opened ml/model_coach.py:840-960, pages/05_Preprocess.py:315-344, utils/insight_ledger.py:1207-1240 and ml/narrative_engine.py:1677-1690. I tried three refutations and all failed. (1) 'They are only rendered, not persisted' \u2014 false: pages/05:329-342 upserts all four into the singleton ledger with no manuscript_text and no resolution. (2) 'Something resolves them' \u2014 grep for the four IDs outside model_coach.py returns only pages/05_Preprocess.py:1370-1372 for preprocess_outlier_handling. (3) '_is_narrative_worthy filters them out' \u2014 no: they have no audit_only flag and their implication is not 'Logged methodology decision', so they pass, are unresolved, and hit the limitations branch at :1235-1239. MEASURED the exact string that reaches the Discussion: _clean_for_manuscript yields \"Your Ridge Regression/Lasso Regression are scale-sensitive; the app's default pipeline already standardizes features for them.\" That is printed under '### Strengths and Limitations' in the exported draft \u2014 a step the app DID perform, asserted as an unaddressed limitation, in coaching voice naming 'the app'."
 },
 {
  "id": "CONTRACT-010",
  "category": "FALSE_CLAIM",
  "severity": "critical",
  "mech": "pages/06 reads st.session_state['task_type'] to choose the word it records as the model-selection criterion, and nothing in the repository ever writes that key. The value is therefore always '' , so the ternary always takes the 'Accuracy' branch and TrainingProvenance.selection_criteria is recorded as 'the held-out Accuracy' for every project, regression included. The task-aware fallback that would have rescued it fires only when selection_criteria is EMPTY, which it never is.",
  "reader": "That the regression study's primary model was chosen on held-out accuracy. The Methods sentence reads '<Model> was selected as the primary model, based on the held-out Accuracy' in a paper whose outcome is continuous and where no accuracy was ever computed.",
  "ev": "pages/06_Train_and_Compare.py:1575-1576 (_task_type = st.session_state.get('task_type', ''); _selection_metric = 'RMSE' if _task_type == 'regression' else 'Accuracy') and :1588 (selection_criteria=criterion_phrase(_selection_metric)). Grep for writers of session_state['task_type'] across pages/ utils/ ml/ models/ returns nothing \u2014 the only two hits, utils/llm_ui.py:220 and utils/workflow_provenance.py:657, write a plain ctx dict, not session_state. ml/holdout_selection.py:61-69 turns 'Accuracy' into 'the held-out Accuracy'. utils/workflow_provenance.py:715-727 has a task-aware default guarded by `if not selection_criteria`, so it cannot fire. ml/narrative_engine.py:1180-1184 emits the sentence.",
  "reach": "Every training run on Train & Compare, both task types. No optional feature involved. This is the highest-confidence row in the slice.",
  "verifier": "Opened pages/06_Train_and_Compare.py, ml/holdout_selection.py, utils/workflow_provenance.py, ml/narrative_engine.py. Could not refute; it is worse than filed. pages/06:1575 reads st.session_state.get('task_type','') and the ONLY session_state keys matching are 'task_type_detection' (grep over pages/ utils/ ml/ models/). The refutation I tried was 'maybe something writes it at runtime' \u2014 nothing does. Decisively: 31 lines ABOVE, at :1544, the same block computes task_type_final_local = task_type_detection.final or data_config.task_type and uses it correctly to rank regression runs by RMSE (:1547-1549). Twenty-six lines later the record is written from the dead key, so a regression project is ranked on RMSE and RECORDED as 'the held-out Accuracy'. The provenance default at utils/workflow_provenance.py:715-727 is guarded by `if not selection_criteria` and criterion_phrase never returns empty (ml/holdout_selection.py:61-69), so the rescue cannot fire. ml/narrative_engine.py:1180-1184 emits it verbatim into the Methods sentence. Every training run, both task types, no optional feature."
 },
 {
  "id": "CONTRACT-014",
  "category": "FALSE_CLAIM",
  "severity": "high",
  "mech": "Feature Selection computes `categorical_excluded` purely in order to print a reassurance, then contradicts it. Both Apply paths assign a numeric-only list into `data_config.feature_cols`, which is what downstream pages model on. Manual selection has the same defect because its multiselect `options=numeric_features`, so a categorical predictor cannot even be re-added by hand.",
  "reader": "The screen says, verbatim: 'These features are retained in your dataset and can still be used for modeling.' A researcher clicks Apply believing sex, race, smoking status and every other categorical predictor are still in the model. They are not \u2014 they were deleted from the modeling feature set by the same button. Every metric, every importance ranking and the predictor list in the paper then describe a model the author did not intend to fit.",
  "ev": "Opened pages/04_Feature_Selection.py:124-133 (categorical_excluded and the caption), :510 (`data_config.feature_cols = consensus`), :553-559 (`options=numeric_features`, then `data_config.feature_cols = manual_selection`), and :102-116 where numeric_features is derived. Opened ml/feature_selection.py consensus_features (:278) to confirm consensus is drawn only from the numeric ranking.",
  "reach": "Yes \u2014 this is the primary path through the Feature Selection page, and it is on the recommended workflow spine.",
  "verifier": "Reproduces at HEAD. pages/04_Feature_Selection.py:123-133 computes categorical_excluded and prints 'These features are retained in your dataset and can still be used for modeling'; :510 sets data_config.feature_cols = consensus and :511 selected_features = consensus; the manual path's multiselect is options=numeric_features (:553-556) and :560-561 assigns that numeric-only list to both keys. I then checked the consumer to see whether the caption could still be true: pages/05_Preprocess.py:104 derives all_features from selected_features or data_config.feature_cols and :112 splits categorical_features out of THAT list \u2014 so after either Apply, categorical_features is empty and the categorical branch of build_preprocessing_pipeline never runs. The categorical predictors are genuinely gone from the model and cannot be re-added on this page. Caveat for honesty: the false sentence is an in-app caption, not an exported one; the exported feature list reflects the numeric-only set. The paper risk is that a researcher who read that caption writes 'models were adjusted for sex/smoking/race' about a model that contains none of them."
 },
 {
  "id": "MINE-005",
  "category": "FALSE_CLAIM",
  "severity": "moderate \u2014 mechanism confirmed, live reachability narrower than the row states",
  "mech": "`train_row_mask` returns an all-True Series when `get_lockbox()` is None, and `render_lockbox_status` returns without rendering anything in the same case. Exploratory mode warns (it is a chosen state); an ABSENT lockbox does not. Page 04 then prints its caption guarded only by `if not is_exploratory()` \u2014 which is False \u2014 so it asserts the quarantine while the mask excluded nothing.",
  "reader": "Feature Selection states, verbatim: 'Selection methods see n=\u2026 training rows; held-out test rows are excluded to prevent selection leakage.' With no lockbox, n is every row with a target and the test rows voted on which predictors entered the model \u2014 textbook selection leakage (Ambroise & McLachlan 2002), asserted as prevented. The same absence silently un-scopes the EDA dataset profile and the feature-engineering fits.",
  "ev": "Opened utils/test_lockbox.py `get_lockbox` (returns None on any key rename or missing key), `train_row_mask` (`if lb is None or is_exploratory(): return pd.Series(True, index=index)`), `render_lockbox_status` (`lb = get_lockbox(); if lb is None: return`), and `ensure_lockbox` (returns `get_lockbox()` \u2014 i.e. None \u2014 when the target is missing from df or `len(eligible) < _MIN_ROWS_FOR_LOCKBOX`, which is 10). Opened the caption at pages/04_Feature_Selection.py:145-149 and the consumers at pages/02_EDA.py:137-148, pages/05_Preprocess.py:222-224.",
  "reach": "Partly. I checked the paths the row implies and most are defended: reset_data_dependent_state pops the lockbox but only on a new dataset, which forces a page-01 visit; the save/restore path encodes and decodes the lockbox and raises a visible warning on failure. The live routes I could establish are (a) restoring a schema-2.0 save file, where `if \"lockbox.json\" in names` is simply skipped with no warning, (b) fewer than 10 rows with a non-null target, and (c) STATE-043 below, which produces the same all-True mask with a lockbox that is present but matches nothing. I could NOT establish a common flow; treat the page-04 caption as the thing to fix (make absence a rendered state) rather than treating this as a routine occurrence.",
  "verifier": "Code confirmed: utils/test_lockbox.py:541-545 returns an all-True mask when get_lockbox() is None, :585-587 renders nothing in the same case, and pages/04_Feature_Selection.py:141-149 prints 'held-out test rows are excluded to prevent selection leakage' guarded only by `not is_exploratory()`. The consequence is not just a caption: with an all-True mask the selectors (and EDA target views, FE fits) see the future test rows, then page 06 draws a fresh split, so the reported test metrics carry selection leakage while the app says the opposite. On reachability I could confirm exactly one route and I killed another: utils/session_manager.py:765 is `if \"lockbox.json\" in names:` with NO else, so restoring a schema-2.0 save leaves the session with data, config and models but no lockbox and no warning. The <10-eligible-rows route (ensure_lockbox :334) is irrelevant at paper scale, and the STATE-043 route is refuted below. Keeping it because the fix is small (render absence as a state; warn when lockbox.json is missing on restore), not because it is common."
 },
 {
  "id": "MINE-025",
  "category": "FALSE_CLAIM",
  "severity": "moderate",
  "mech": "PARTLY STALE \u2014 two of the three cited sites are now guarded and one is not. ml/publication.py:849 sits inside `if len(unique_sigs) == 1:` (all configs provably identical) and :859-860 collapse only when `len(set(...)) == 1`; neither is a defect. What remains live is :660-661, `first_pca = next(iter(pca_details.values()))` under the comment \"Use the first PCA config as representative\", which emits a Methods sentence naming a component count taken from an arbitrary model. It compounds a second error: pca_details is built from preprocessing_config_by_model, but the sentence it annotates is in the FEATURE ENGINEERING subsection.",
  "reader": "\"PCA dimensionality reduction (10 components)\" in the Methods, as a statement about the feature-engineering step. The number can belong to a different model's preprocessing PCA, and per-model pipelines differ by design \u2014 'representative' is the one thing a member of that dict is not.",
  "ev": "ml/publication.py:642-651 builds pca_details keyed by model from `st.session_state.get('preprocessing_config_by_model', {})`; :659-668 `if 'PCA' in technique_name.upper() and pca_details: first_pca = next(iter(pca_details.values()))` then appends \"PCA dimensionality reduction (N components)\" to the Feature Engineering techniques list. The source dict is insertion-ordered by the Preprocess page's build sequence.",
  "reach": "Requires feature engineering applied with a PCA entry in engineering_log AND at least one model with use_pca in preprocessing. Narrower than the row implies, hence moderate.",
  "verifier": "Opened ml/publication.py:640-668. Confirmed the reader's narrowing is right: :849 and :859-860 are genuinely guarded and are not defects. What survives at :659-661 is live \u2014 `first_pca = next(iter(pca_details.values()))` under 'Use the first PCA config as representative', emitting 'PCA dimensionality reduction (N components)' where N comes from an arbitrary model in an insertion-ordered dict built at :643-651 from preprocessing_config_by_model. The compounding error is confirmed too: the sentence is appended to the `techniques` list inside the Feature Engineering subsection while its number is taken from preprocessing configs, so a feature-engineering PCA is annotated with a preprocessing-stage component count. Trigger is narrow \u2014 a PCA entry in engineering_log AND at least one model with use_pca \u2014 and harmless when all models agree, but where they disagree the Methods states one model's component count as the study's. Moderate."
 },
 {
  "id": "MINE-030",
  "category": "FALSE_CLAIM",
  "severity": "high",
  "mech": "THE CITED SITE IS NOT REACHABLE FROM CLASSIC \u2014 ml/sensitivity.py's sensitivity_random_seeds has no caller in pages/, utils/ or ml/ (only tests and turbotab). The same defect is live at the site that IS reachable. pages/08 runs its own seed loop: a seed that raises is appended as NaN with an '_error' key and nothing counts or surfaces it; the methodology log then records n_seeds as the SLIDER value; and the coefficient of variation is computed over `.dropna()`, i.e. survivors only.",
  "reader": "'Seed stability analysis was performed on <Model> using 8 random seeds' \u2014 when six of the eight raised and the coefficient of variation quoted in the same paragraph was computed across two points. Nothing distinguishes 'we ran eight' from 'eight were attempted and two finished'.",
  "ev": "pages/08_Sensitivity_Analysis.py:212 (`results.append({'seed': seed, primary_metric: float('nan'), '_error': str(e)})`), :224 (`'n_seeds': n_seeds` \u2014 the st.slider value from :106), :228-233 (_vals = _df_seeds[primary_metric].dropna(); _cv_pct from those). ml/publication.py:1362-1366 (`using {d.get('n_seeds','multiple')} random seeds`). Contrast pages/08:492-493, where the feature-dropout loop DOES count its errors (_n_drop_errors) and disclose them \u2014 the seed loop was never given the same treatment. Grep confirms ml/sensitivity.py is imported only by tests/ and turbotab/.",
  "reach": "Sensitivity Analysis page, seed stability. The relocation matters for whoever fixes it: patching ml/sensitivity.py alone would change nothing a Classic user sees.",
  "verifier": "Opened pages/08_Sensitivity_Analysis.py and ml/publication.py. Relocation is correct and the defect is live: pages/08:212 appends {'seed': seed, primary_metric: nan, '_error': str(e)} with no counter and no st.warning; :224 logs 'n_seeds': n_seeds, the st.slider value, not the number that succeeded; :228-233 computes _cv_pct from _df_seeds[primary_metric].dropna(), i.e. survivors only. ml/publication.py:1362-1366 turns that log entry into 'Seed stability analysis was performed on X using {n_seeds} random seeds'. So a run where 4 of 20 seeds raise publishes 'using 20 random seeds' and a CV% over 16. I tried to refute by looking for a disclosure elsewhere on the page and found the opposite: the feature-dropout loop 30 lines down (:492-495) DOES count and st.warning its errors, which proves the seed loop's silence is an omission rather than a convention."
 },
 {
  "id": "RECORD-017",
  "category": "FALSE_CLAIM",
  "severity": "moderate \u2014 cheap to fix, and it is in the supplementary",
  "mech": "`_format_audit_timestamp` treats a tz-naive timestamp as UTC (`dt.replace(tzinfo=timezone.utc)`) and renders '%Y-%m-%d %H:%M UTC'. Every timestamp it is given is `datetime.now().isoformat()` \u2014 local wall clock, no tzinfo.",
  "reader": "Every line of the Decision Audit Trail in the LaTeX supplementary is stamped 'UTC' and is actually local time, off by the machine's offset. A reviewer or co-author reconciling the audit trail against anything else \u2014 lab notes, a commit log, another export \u2014 finds a systematic skew and cannot explain it. Ordering also breaks silently for any project spanning a daylight-saving boundary.",
  "ev": "Opened ml/publication.py `_format_audit_timestamp` (the `if dt.tzinfo is None: dt = dt.replace(tzinfo=timezone.utc)` branch and the '%Y-%m-%d %H:%M UTC' format string), used in the audit appendix. Counted the sources: 13 `datetime.now()` calls in utils/workflow_provenance.py and 4 in utils/insight_ledger.py; spot-checked workflow_provenance.py:294, :348, :360 \u2014 all naive.",
  "reach": "Yes \u2014 the Decision Audit Trail is part of the exported supplementary from pages/10.",
  "verifier": "Reproduces exactly. ml/publication.py:1865-1877 parses the timestamp and, when tzinfo is None, does dt.replace(tzinfo=timezone.utc) then formats '%Y-%m-%d %H:%M UTC'. The only caller is :1968 inside generate_decision_audit_trail, and utils/session_state.log_methodology:605 writes datetime.now().isoformat() \u2014 naive local wall clock. So every row of the exported Decision Audit Trail (pages/10_Report_Export.py:1824-1825) carries a local time stamped 'UTC'; near midnight the DATE is wrong too. This is squarely a false provenance record in an artifact that leaves the building, it is the cheapest fix on this list (drop the ' UTC' suffix or record tz-aware timestamps), and I found nothing upstream that makes the timestamps aware. Low scientific impact \u2014 no result changes, and the ordering is internally consistent \u2014 but the assertion is unambiguously false."
 },
 {
  "id": "STATE-011",
  "category": "FALSE_CLAIM",
  "severity": "high",
  "mech": "KMeansFeatures is appended as a top-level pipeline step AFTER the ColumnTransformer, and its transform returns only the cluster features \u2014 `return result`, under a comment saying \"we're replacing X with cluster features (not appending)\". So enabling the option discards every original predictor and trains the model on nothing but distances to 5 centroids. The recipe line and the UI both say \"added\".",
  "reader": "That KMeans distances were added alongside the study's predictors. In fact the predictors are gone: the reported metrics belong to a model with no clinical variables in it, SHAP and feature importance render on kmeans_dist_cluster_* columns, and nothing on screen says so. The code and the copy assert opposite things and the copy is the one the researcher reads.",
  "ev": "ml/feature_steps.py:95-104 \u2014 `# For now, we're replacing X with cluster features (not appending) / # To append, would need: np.hstack([X, result])` followed by `return result`. ml/pipeline.py:447-455 appends it as a top-level step over the whole preprocessed matrix; :553-560 the recipe string `f\"KMeans features added: {kmeans.n_clusters} clusters\" + \", distances to centroids\"`. pages/05_Preprocess.py:877 UI copy: \"Adds derived columns (distance to each KMeans centroid).\" ml/pipeline.py:579 has an internal docstring that states the truth (\"KMeans replaces preprocessor output\") \u2014 so the module knows and the interface does not.",
  "reach": "pages/05_Preprocess.py:872 per-model checkbox (labelled Experimental), plus n_clusters and add-distances controls at :882-884. One checkbox. Interpretability mode 'high' disables it (:945-947), so it is reachable in balanced and performance modes.",
  "verifier": "Opened ml/feature_steps.py:73-104 \u2014 transform builds `features` from distances and/or one-hot, then `result = np.hstack(features)` and `return result`, under the comment naming the append it does not do. Confirmed ml/pipeline.py:447-455 appends it as a top-level step after the ColumnTransformer (so it sees the whole preprocessed matrix), :553-560 writes the recipe string 'KMeans features added: N clusters, distances to centroids', and :579 has the internal docstring that states the truth. UI copy at pages/05_Preprocess.py:876-878 says 'Adds derived columns'. Reachability confirmed: one per-model checkbox at :869-873, disabled only in interpretability mode 'high' (:943-946), so live in balanced and performance modes. Enabling it trains the model on nothing but distances to k centroids while every description the user and the manuscript see says the columns were added to the existing predictors."
 },
 {
  "id": "STATE-027",
  "category": "FALSE_CLAIM",
  "severity": "low",
  "mech": "Every technique tab on Feature Engineering mirrors state by hand into three session keys (X_engineered, engineered_features, engineering_log). The removal path updates the frame and the name list and NOT the log, so a removed feature keeps its 'created' entry, and that log becomes provenance and then manuscript text.",
  "reader": "The Methods 'Feature Engineering' section lists a transformation technique that produced nothing surviving into the model. If the user removes every feature a technique created, the manuscript still asserts that technique was applied.",
  "ev": "pages/03_Feature_Engineering.py:1180-1188 \u2014 the removal loop drops the column from X_engineered and removes it from engineered_features, then writes back only fe_work_in_progress['X_engineered'] and ['engineered_features']; engineering_log is untouched (contrast the nine creation sites at :352/364, :403/412, :508/513, :598/611, :695/700, :880/885, :952/957, :999/1004, :1087). ml/publication.py:634 reads st.session_state['engineering_log'] and :640-707 turns each entry into a named technique in the Methods section.",
  "reach": "pages/03 Feature Engineering -> create features -> click the \u274c remove button on the Engineered Features list -> Save -> Report Export. Narrower than the row implies: the Methods section names techniques, not the '+N features' counts, so the false claim only bites when a technique's entire output is removed.",
  "verifier": "Opened pages/03_Feature_Engineering.py:1170-1190 (removal loop writes back only X_engineered and engineered_features), :1246-1258 (save writes the untouched engineering_log) and :1291-1300, plus ml/publication.py:628-707, utils/workflow_provenance.py:370-384 and :665, ml/narrative_engine.py:826-840. Mechanism confirmed unchanged. I nearly refuted this: ml/publication.py's technique parser is only the FALLBACK (pages/10:673-699 uses NarrativeEngine whenever any provenance stage is recorded), so I checked whether the primary path avoids the stale log \u2014 it does not. pages/03:1294-1296 passes the same engineering_log into record_feature_engineering, workflow_provenance.py:665 exposes it as ctx['engineering_transforms'], and ml/narrative_engine.py:838 joins those entries VERBATIM into 'Feature engineering was performed: {...}.'"
 },
 {
  "id": "STATE-040",
  "category": "FALSE_CLAIM",
  "severity": "critical",
  "mech": "Toggling exploratory mode OFF calls reset_downstream_results(clear_feature_engineering=False), so df_engineered \u2014 built while the test rows were visible \u2014 deliberately survives, while the honesty watermark 'exploratory_used' is popped unconditionally at the end of the same function. The manuscript context then computes exploratory_mode = False.",
  "reader": "That the evaluation was quarantine-clean \u2014 that no held-out row informed any modelling decision. In fact the engineered features that the model is built on were derived with the test rows in view, and nothing anywhere in the exported artefact says so.",
  "ev": "Opened utils/session_state.py:497-500 \u2014 the comment ('A manuscript is only quarantine-clean if EVERY surviving result was computed with the lockbox on') sits directly above st.session_state.pop('exploratory_used', None), which is outside every flag block and runs on every call including clear_feature_engineering=False. Opened pages/01_Upload_and_Audit.py:1145-1153 \u2014 _on_exploratory_toggle calls _rdr(clear_feature_engineering=False) FIRST (popping the flag) and only then re-sets exploratory_used if exploratory_mode is currently True; on the ON->OFF transition it is False, so the flag stays gone. Opened pages/10_Report_Export.py:520-524 \u2014 'exploratory_mode': bool(session_state['exploratory_mode'] or session_state['exploratory_used']), with a comment asserting the flag 'is cleared only by the downstream reset, so toggling the mode off cannot launder exploratory results'. It can.",
  "reach": "Documented UI sequence on Upload & Audit: enable exploratory mode -> engineer features on page 03 -> disable exploratory mode -> train -> export. The toggle is in the '\ud83d\udd12 Test holdout settings' expander and its own caption invites toggling in either direction.",
  "verifier": "Opened utils/session_state.py:497-500 \u2014 `st.session_state.pop('exploratory_used', None)` is the last statement of reset_downstream_results, outside every flag block, so it runs on the clear_feature_engineering=False call too, while the feature-engineering block above it (:378-389) is skipped and df_engineered survives. Opened pages/01_Upload_and_Audit.py:1146-1153 \u2014 _on_exploratory_toggle calls _rdr(clear_feature_engineering=False) first and re-sets exploratory_used only `if st.session_state.get('exploratory_mode')`, which on the ON->OFF transition is already False. Opened pages/10_Report_Export.py:520-524 \u2014 the manuscript context is `exploratory_mode or exploratory_used`, with a comment asserting the flag is cleared only by the downstream reset; grep confirms page 01 is the sole writer of exploratory_used. Refutation attempted and failed: I checked whether the surviving df_engineered is actually contaminated \u2014 pages/03_Feature_Engineering.py scopes its fits with train_row_mask at :669, :807, :942 and :986, and utils/test_lockbox.train_row_mask returns all-True when is_exploratory(), so features engineered in exploratory mode really were fit on the sealed rows. The engineered frame then survives the toggle-off with the watermark deleted, and ml/narrative_engine.py:670-671 and :1714 read that context, so the manuscript states an unqualified held-out result."
 },
 {
  "id": "STATE-041",
  "category": "FALSE_CLAIM",
  "severity": "critical",
  "mech": "exploratory_mode (the quarantine-off switch) is persisted ONLY as a deferred Streamlit widget key, and pages/01 is the only page that claims it out of the pending-restore dict; the exploratory_used watermark is in neither persistence bucket, so it is lost on save. Restore a session that was saved in exploratory mode and go anywhere except Upload & Audit, and both flags read False.",
  "reader": "The Methods section states verbatim: 'The held-out test set (X% of eligible observations) was frozen at data upload, before any feature engineering or feature selection, and was accessed only for the final evaluation.' In an exploratory-mode session that sentence is false \u2014 every target-aware step (feature selection, preprocessing, trimming) saw the test rows. The app's own exploratory disclaimer (narrative_engine.py:1714-1716) is simultaneously suppressed. Reported test performance is not held-out performance.",
  "ev": "utils/session_manager.py:_SAFE_WIDGET_KEYS (line ~131) carries 'exploratory_mode' as a deferred widget key; _PLAIN_KEYS (lines 91-117) does NOT carry 'exploratory_used' and neither does _SAFE_WIDGET_KEYS, so the watermark does not survive a save. pages/01_Upload_and_Audit.py:1165-1181 is the only site that pops 'exploratory_mode' from _pending_widget_state_restore (utils/theme.py:719-725 claims only 'workflow_mode_selector'). pages/10_Report_Export.py:523-524 computes the manuscript context flag as bool(exploratory_mode or exploratory_used) \u2014 both absent after restore. ml/narrative_engine.py:799-813 then appends the frozen-lockbox sentence because ctx['exploratory_mode'] is falsy.",
  "reach": "Sidebar session restore is rendered on EVERY page (utils/theme.py:695 calls render_session_controls). Flow: work in exploratory mode -> save session -> restore later from the sidebar while on Report Export (or any page other than Upload & Audit) -> generate the Methods/Results draft. Nothing forces a visit to page 01 first, and nothing on page 10 re-reads the pending dict.",
  "verifier": "Opened utils/session_manager.py (_PLAIN_KEYS 91-117, _SAFE_WIDGET_KEYS 123-133, _NEVER_PERSIST 136-163, save loops 240/251, restore 658/668), utils/llm_ui.py:663-686, utils/theme.py:695-726, pages/01_Upload_and_Audit.py:1095-1181, pages/10_Report_Export.py:505-525, ml/narrative_engine.py:799-813 and 1714-1722. All three legs hold at HEAD. exploratory_used is in neither persistence bucket. exploratory_mode is a deferred widget key; llm_ui.py:681-686 explicitly RE-STORES non-LLM keys back into the pending dict and theme.py:719-725 claims only workflow_mode_selector, so page 01 really is the sole claimant. I tried three refutations and all failed: (1) no switch_page/redirect forces a visit to page 01 on restore; (2) I hoped the manuscript's frozen-lockbox sentence would need something not persisted, but the insight_ledger IS saved (session_manager.py:257-265) and restored (:678-679), and the resolved 'upload_test_lockbox' insight is upserted whenever a lockbox exists regardless of exploratory mode (pages/01:1118-1144) \u2014 so ml/narrative_engine.py:799 fires; (3) I hoped 'artifacts must be regenerated after restore anyway' would make the claim true, but selected_features / engineered_feature_names / feature_engineering_applied ARE in _PLAIN_KEYS, so the leaked feature-selection choices survive while the manuscript asserts the test set 'was frozen at data upload, before any feature engineering or feature selection.' Positive false assertion plus omission of the exploratory limitation at :1714."
 },
 {
  "id": "STATE-047",
  "category": "FALSE_CLAIM",
  "severity": "high",
  "mech": "reset_downstream_results nulls six provenance sections and deletes hypothesis_test_results, but never nulls statistical_validation. The Record therefore keeps StatisticalValidationProvenance.tests_run after the results those tests produced have been deleted, and get_completeness() keeps reporting statistical_validation=True.",
  "reader": "The generated Methods section names specific tests and reports a significance COUNT for an analysis that no longer exists \u2014 e.g. 'Statistical validation was performed using: Shapiro-Wilk, Mann-Whitney' followed by 'N remained significant at q < 0.05' \u2014 computed against a target or dataset that was invalidated. A TRIPOD-style completeness map reports the stage as done.",
  "ev": "Opened utils/session_state.py:438-441 \u2014 _sections = ['eda','split','preprocessing','training','explainability','coach'] (+feature_selection/feature_engineering under their flags); statistical_validation and sensitivity are absent, while hypothesis_test_results IS popped at :436. Opened utils/workflow_provenance.py:263 (the field), :629-645 (get_completeness reports statistical_validation as present whenever non-None), :759 (get_methods_context sets ctx['statistical_tests'] = self.statistical_validation.tests_run). Opened ml/narrative_engine.py:1305-1339 (_gen_statistical_validation) \u2014 it emits 'Statistical validation was performed using: {names}.' and then either a corrected-count sentence or an expected-by-chance sentence, entirely from that ctx key.",
  "reach": "Any downstream reset: change the target or features on Upload & Audit, re-upload corrected data with the same columns, toggle exploratory mode, or re-apply feature selection. Then export from page 10.",
  "verifier": "Opened utils/session_state.py:438-441 \u2014 _sections is exactly ['eda','split','preprocessing','training','explainability','coach'] (+feature_selection/feature_engineering under flags); statistical_validation and sensitivity are absent, while 'hypothesis_test_results' IS in the popped _analysis_keys at :436. Opened utils/workflow_provenance.py:263, :629-645 (get_completeness reports statistical_validation on non-None) and :758-759 (ctx['statistical_tests'] = tests_run). Opened ml/narrative_engine.py:1305-1345 \u2014 from that one ctx key it emits 'Statistical validation was performed using: {names}.' plus either '...across the {len(with_p)} tests reported here; {len(corrected)} remained significant at q < {alpha}' or the expected-by-chance sentence. Refutation attempted and failed: my first grep suggested record_statistical_test had no production caller, which would have made the whole row unreachable \u2014 but pages/09_Hypothesis_Testing.py calls get_provenance().record_statistical_test at :281, :462, :605, :723, :836 and :936, and apply_multiplicity_correction at :1028. So the section is really populated, really survives every downstream reset, and really re-emits counts describing tests whose results were just deleted."
 },
 {
  "id": "SWEEP-008",
  "category": "FALSE_CLAIM",
  "severity": "critical",
  "mech": "Classic asserts, in a status chip rendered on every workflow page, that the sealed test set was \"opened once at Train & Compare\", and page 06 adds \"This page opens the held-out test set exactly once.\" Nothing counts openings and nothing prevents a second one: there is a single unguarded `st.button(\"Train Models\")`, and a user can train, read held-out metrics, go back to Preprocess or Feature Selection, change something, return and train again \u2014 scoring on the same sealed rows each time. The ledger insight written at upload (implication=\"Held-out evaluation is protected from selection and preprocessing leakage.\", resolved=True) goes into the manuscript narrative and auto-marks TRIPOD 4a regardless.",
  "reader": "That the reported held-out performance is a single, untouched evaluation on data never seen during model or preprocessing selection \u2014 i.e. that it is unbiased. If the analyst iterated against the test metric (which the interface invites and never counts), the reported figures are optimistically biased by an unknown amount, and the Methods/TRIPOD record still says the holdout was protected.",
  "ev": "utils/test_lockbox.py:706 and :720 (render_lockbox_status) emit \"held out since upload \u2014 opened once at Train & Compare.\"; pages/06_Train_and_Compare.py:100 calls render_lockbox_status(\"This page opens the held-out test set exactly once.\"); pages/06_Train_and_Compare.py:1778 is the only train button and has no guard. Repo-wide grep for an open counter/state (\"opened\", \"open_count\", \"n_opens\") in utils/test_lockbox.py and pages/06 finds only those prose strings. pages/01_Upload_and_Audit.py:1130 writes the \"protected from selection and preprocessing leakage\" implication into the ledger.",
  "reach": "Ordinary recommended workflow: pages 05 -> 06 -> back to 05 -> 06. The sidebar allows free navigation between all pages, so the second opening costs two clicks. Confirmed the row's own note is wrong for Classic on this point: page order gives you \"before anyone looks\" (nothing reads test before stage 6), but it gives you nothing at all for \"exactly once\" \u2014 that is a count, and no count exists in either door.",
  "verifier": "Opened utils/test_lockbox.py:700-720, pages/06_Train_and_Compare.py:1732-1795, pages/01_Upload_and_Audit.py:1116-1143, ml/narrative_engine.py:797-813. Tried to refute by looking for a counter, a re-train guard, or an exploratory downgrade on second training: there is none. Two unguarded buttons (train_models_button AND train_models_optimized_button, both re-runnable), and grep for opened/open_count/n_opens in the lockbox and page 06 returns only prose. The flag is STRONGER than filed: the falsehood is not confined to a UI chip. ml/narrative_engine.py:807-812 emits into the Methods section, for any non-exploratory run where the upload_test_lockbox insight is resolved, the sentence 'The held-out test set ... was frozen at data upload ... and was accessed only for the final evaluation.' That sentence is generated from a resolution recorded at upload and is unconditioned on how many times the test set was actually scored. It is the governing rule's exact violation on the artifact that leaves the building."
 },
 {
  "id": "SWEEP-023",
  "category": "FALSE_CLAIM",
  "severity": "high",
  "mech": "MECHANISM CORRECTED FROM THE ROW. The filed harm (a Project that forgets to carry random_seed publishes seed 42) is TurboTab-only \u2014 in Classic every consumer reads st.session_state.get('random_seed', 42) consistently, so an unset seed genuinely is 42. What DOES reproduce in Classic is a truthiness bug one layer up: _build_reproducibility_manifest computes `int(getattr(_split_cfg, 'random_state', st.session_state.get('random_seed', 42)) or 42)`. The seed widget's min_value is 0. With seed 0, split_config.random_state is 0, `0 or 42` evaluates to 42, and the manifest publishes 42.",
  "reader": "That re-running the analysis with the seed stated in manifest.json reproduces the published numbers. It does not \u2014 the split, the models and every bootstrap ran with seed 0. The same ZIP contains a second artifact (export_metadata at pages/10:193) that correctly reports 0, so the package contradicts itself, and the one a reviewer would use is the wrong one. The function's own docstring says \"The LaTeX supplementary promises reviewers that the exported package contains exactly these; this function is what makes that claim true.\"",
  "ev": "pages/10_Report_Export.py:528-562 _build_reproducibility_manifest, specifically :560-561 `'random_seed': int(getattr(_split_cfg, 'random_state', st.session_state.get('random_seed', 42)) or 42)`; :2597-2598 `manifest['reproducibility'] = _build_reproducibility_manifest()` written to manifest.json in the ZIP. pages/06_Train_and_Compare.py:108-113 `st.number_input(\"Random Seed\", min_value=0, ...)`; :287 `random_state=st.session_state.get('random_seed', 42)` into SplitConfig. pages/10:193 reports the same seed correctly. utils/seed.py:34-37 get_global_seed is the Streamlit-coupled accessor the row names \u2014 real, but not the Classic falsehood.",
  "reach": "Set the sidebar seed to 0 on the Train page \u2014 an allowed value and the single most common seed in ML tutorials \u2014 then export. No other condition.",
  "verifier": "Opened pages/10_Report_Export.py:556-562 and confirmed `'random_seed': int(getattr(_split_cfg, 'random_state', st.session_state.get('random_seed', 42)) or 42)`, with the result written to manifest['reproducibility'] at :2597-2598 and into the export ZIP. Opened pages/06_Train_and_Compare.py:107-116 and confirmed the widget's min_value is 0, and :287 puts st.session_state['random_seed'] into SplitConfig.random_state unchanged. So with seed 0 the getattr succeeds, returns 0, and `0 or 42` publishes 42. Tried to refute on the grounds that the seed is reported correctly elsewhere: pages/10:193 does report it correctly, which makes it worse rather than better \u2014 the same export ZIP then carries two contradictory seeds, and the reproducibility manifest is the one a reviewer would re-run from. Seed 0 is an allowed value and the most common seed in ML tutorials. The row's originally filed mechanism is indeed TurboTab-only; the reader's corrected Classic mechanism is the live one."
 },
 {
  "id": "CONTRACT-013",
  "category": "SILENT_SKIP",
  "severity": "high",
  "mech": "get_data()'s precedence is df_engineered > filtered_data > raw_data. Page 05's plausibility row-filter writes its result to filtered_data. When Feature Engineering ran first \u2014 the normal page order \u2014 df_engineered wins and the filter is never seen by any downstream page, while page 05 tells the user it was applied. Nothing pops filtered_data on a downstream reset either, so a stale one persists.",
  "reader": "That physiologically implausible rows were excluded, as the pipeline recipe on the Preprocess screen states. They were not: the pipeline was configured and its output feature count measured against the filtered rows, and training then ran on the unfiltered ones.",
  "ev": "utils/session_state.py:220-238 (get_data precedence, df_engineered checked first at :233). pages/05_Preprocess.py:976-1018 builds filtered_df and writes st.session_state['filtered_data'] = filtered_df at :1014, X_sample = filtered_df[all_features] at :1015. pages/05:1446 renders get_pipeline_recipe(pipeline, plausibility_mode=cfg.get('plausibility_mode')), and ml/pipeline.py:486-487 emits the literal line 'Plausibility: rows filtered to NHANES range (before pipeline).' filtered_data is popped only by set_data (utils/session_state.py:306) and reset_data_dependent_state (:512) \u2014 reset_downstream_results (:338-...) never touches it.",
  "reach": "Default page order 01 -> 02 -> 03 Feature Engineering (sets df_engineered) -> 05 Preprocess with plausibility_mode='filter' and gating on -> Build pipelines -> 06 Train. The recipe line claiming the filter is shown on 05; every downstream get_data() returns the unfiltered engineered frame.",
  "verifier": "Opened utils/session_state.py:220-260 (get_data precedence, df_eng first at :233), pages/05_Preprocess.py:965-1020 and :1440-1450, ml/pipeline.py:330-336 and :474-500, pages/03_Feature_Engineering.py:1246-1255. All live. The decisive finding is at ml/pipeline.py:330 \u2014 'if plausibility_bounds and plausibility_mode != \"filter\"' \u2014 so in filter mode the pipeline carries NO plausibility_gate step either. With df_engineered set (page 03:1252), get_data() never returns filtered_data, so neither the row filter nor the clip gate runs: physiologically impossible values reach every model untouched while pages/05:1446 renders 'Plausibility: rows filtered to NHANES range (before pipeline).' I verified reset_downstream_results (session_state.py:400-500) never pops filtered_data, confirming the stale-frame half too."
 },
 {
  "id": "MINE-004",
  "category": "SILENT_SKIP",
  "severity": "high",
  "mech": "The >0.95 feature-target correlation scan that populates signals.leakage_flags / leakage_candidate_cols sits inside a bare try ending 'except Exception: pass'. A scan that failed and a dataset with no leakage are indistinguishable downstream \u2014 and the EDA page then asserts a clean bill of health that is exported to the manuscript.",
  "reader": "The Discussion carries, verbatim as a study strength, 'the dataset contained no blocking data-quality issues (no severe missingness, leakage candidates, or distributional anomalies)'. That sentence is emitted purely because the unresolved-issue count is zero, which a swallowed leakage scan guarantees. This is the governing rule broken directly: silence would be permitted, asserting a clean bill is not.",
  "ev": "ml/eda_recommender.py:468-481 \u2014 the correlation scan, ending 'except Exception: pass  # Skip leakage detection if correlation fails' at :480-481; the r4_leakage recommendation at :639 fires only when those lists are non-empty. pages/02_EDA.py:596-610 upserts insight id 'eda_opportunity_clean_data' when n_issues == 0, with manuscript_text = 'the dataset contained no blocking data-quality issues (no severe missingness, leakage candidates, or distributional anomalies)'. utils/insight_ledger.py:1218 and :1227-1230 route every eda_opportunity_* insight's manuscript_text into the strengths list; ml/narrative_engine.py:1681 calls discussion_points_for_manuscript() for the Discussion section.",
  "reach": "pages/02_EDA renders the recommender on every load; the clean-data insight is auto-generated when the ledger has no blockers/warnings and the dataset has >=5 features. It reaches the paper via Report Export -> Discussion. CAVEAT, stated down: I confirmed the swallow and its consumer, but I did not measure an input that makes DataFrame.corr() actually raise, so I cannot put a frequency on the trigger. The false sentence is guaranteed only when the scan fails.",
  "verifier": "Opened ml/eda_recommender.py:454-482, pages/02_EDA.py:363-380 and :595-611, utils/insight_ledger.py:1210-1232, ml/narrative_engine.py:1678-1684. I tried hard to find a second, independent leakage detector that would keep the ledger honest if this scan died, and there is none: pages/02:364 reads signals.leakage_candidate_cols and nothing else, and that list is written only at eda_recommender.py:479. The insight it creates is severity='blocker' (pages/02:368), which is exactly what n_issues at :596 counts \u2014 so a failed scan flips the gate and the app asserts 'no severe missingness, leakage candidates, or distributional anomalies', routed to strengths at insight_ledger.py:1227-1230 and into the Discussion via narrative_engine.py:1681 (the primary manuscript path). Confidence marked down on the trigger, as the flag itself asked: I could not construct an input that makes corr() raise. The most plausible route is resource exhaustion \u2014 the scan builds the full p x p matrix from signals.numeric_cols with no cap and no feature_cols filter, unlike the collinearity block a few lines below which explicitly samples. Recommend the cheap fix (log the failure into the ledger as a 'scan did not run' finding) rather than days of investigation."
 },
 {
  "id": "RECORD-007",
  "category": "SILENT_SKIP",
  "severity": "high",
  "mech": "generate_latex_report splits the compiled markdown draft at '## Results' and stores everything after it \u2014 the entire Results section AND the entire Discussion, with all its evidence-citing [AUTHOR REQUIRED] scaffolds \u2014 in `draft_results`, which is appended ONLY in the `elif draft_results:` branch. In the normal case (model_results is truthy) that branch never runs. The .tex Discussion is rebuilt from generic `[PLACEHOLDER: ...]` strings, so every honesty guarantee the project tests for applies to the markdown only.",
  "reader": "That the .tex in the export ZIP is the manuscript the app compiled and vouches for. It is not. The guards pinned by tests/test_manuscript_trust.py::TestDiscussionHonesty \u2014 a negative R2 stated as \"below a mean-only baseline\", a single model never called \"strongest\", no unconditional effectiveness claim \u2014 cannot fire on the .tex, whose Principal Findings paragraph just reports RMSE with a placeholder. And pages/10 tells the author \"N author input(s) remain \u2014 each is marked [AUTHOR REQUIRED]\", a count taken from the markdown that is untrue of the .tex.",
  "ev": "ml/latex_report.py:703-709 `methods_latex, results_latex = _convert_markdown_to_latex(methods_section)` then `draft_results = _demote_results_subsections(results_latex)`; :347-359 `_convert_markdown_to_latex` splits on `\\n## Results`; :767-797 `if model_results: ... elif draft_results: sections.append(draft_results)`; :896-994 the Discussion built entirely from _styled_placeholder(\"[PLACEHOLDER: ...]\"). The input is the full draft, not just Methods: pages/10_Report_Export.py:1953 passes `_cached_bundle['methods_text']`, built at :673-692 as `NarrativeEngine(...).generate().to_markdown()`. pages/10:2217-2221 counts '[AUTHOR REQUIRED' in that markdown and displays it.",
  "reach": "Every export with trained models \u2014 i.e. every real run. The .tex is the artifact that goes to the journal.",
  "verifier": "Opened ml/latex_report.py:347-359 (_convert_markdown_to_latex splits on \\n## Results and returns everything after it as results_latex), :700-709 (draft_results = that whole tail), :767-797 (`if model_results: ... elif draft_results:`), :896-994 (the Discussion), and the caller at pages/10_Report_Export.py:1930-1968. Confirmed the elif is dead in every run with trained models. One correction that makes the row worse rather than better: I checked whether the .tex rebuilds its limitations from the ledger independently, which would have refuted the row. It does not. ml/latex_report.py:989 appends `_escape_latex(limitations)` under \\paragraph{Limitations}, `limitations` has the default `\"[Discuss limitations here]\"` at :602, and the call at pages/10:1949-1968 never passes it. So the .tex Limitations paragraph is that literal placeholder string, while pages/10:2216-2221 counts and advertises the [AUTHOR REQUIRED] items in the markdown the user is looking at. Every evidence-cited caveat the workflow generated \u2014 exploratory mode, heteroscedasticity, PROBAST screening \u2014 exists only in the markdown, and the .tex is the artifact that goes to the journal. Weakest link in the chain: the .tex is visibly a skeleton full of [PLACEHOLDER] markers, so nobody submits it unedited; the harm is that the author fills those blanks without the app's specific findings in front of them."
 },
 {
  "id": "STATE-044",
  "category": "SILENT_SKIP",
  "severity": "medium",
  "mech": "_content_fingerprint returns None on any frame it cannot hash (unhashable cells \u2014 list/dict values, e.g. from a nested JSON upload). set_data's stale-results branch is guarded by `new_fp is not None`, so when fingerprinting fails a genuine same-schema content change takes NEITHER branch: no reset fires at all, and every model, metric, interval and figure computed from the previous values survives untouched.",
  "reader": "That the displayed metrics describe the data currently loaded. After a cleaning action on such a frame the app shows the previous dataset's results under the new dataset, with no banner and nothing out of place.",
  "ev": "utils/session_state.py:261-271 (_content_fingerprint, `except Exception: return None`) and :316-318 (`elif (old_df is not None and old_fp is not None and new_fp is not None and old_fp != new_fp)` \u2014 the whole stale-results arm, including reset_downstream_results at :334, is skipped when new_fp is None).",
  "reach": "Partially established. The code path is unconditional in set_data; what I could NOT measure in the time available is how often a Classic working frame actually contains unhashable cells. A JSON upload with nested arrays is the plausible route. I am rating this medium rather than high for that reason. Separately, I judge the row's OTHER claim \u2014 int64 sum wraparound as a 'real collision channel' \u2014 to be overstated: summing 64-bit hashes gives ~2^-64 collision odds, not a practical hazard.",
  "verifier": "Opened utils/session_state.py:261-271 and :300-334; both unchanged. MEASURED: pd.util.hash_pandas_object raises TypeError('unhashable type') on list cells and on numpy.ndarray cells, so _content_fingerprint returns None, and the stale-results arm at :317-318 requires new_fp is not None, so neither branch fires and reset_downstream_results at :334 is skipped. I tried to refute the reachability the flag was unsure about and got a mixed result that I think MAKES the row rather than breaking it. The JSON route the flag named is CLOSED: data_processor.py:192-224 _stringify_nonscalar_cells JSON-encodes list/dict/set/tuple cells and is applied to every JSON path via _finalize_json_frame at :476. But that guard tests isinstance(v, (list,dict,set,tuple)) and is not applied to parquet, and pd.read_parquet (data_processor.py:97-99) yields numpy.ndarray cells for a nested column. MEASURED end-to-end: a parquet with one list column loaded through load_tabular_data gives dtype object holding numpy.ndarray, and _content_fingerprint(out) returns None at HEAD. So the live route is a parquet upload with a nested column, not JSON. Consequence stands: a same-schema cleaning action then leaves every model, metric, interval and figure from the previous values in place. I agree with the flag that the int64-sum-collision claim is not a real hazard."
 }
]